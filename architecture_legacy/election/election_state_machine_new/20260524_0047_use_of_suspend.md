
∴ Thinking…

  Good. So the constitution defines suspend and resume as actions. Let me now read the capability resolver and overlay policy to understand how suspension works at the capability level.

∴ Thinking…

  Excellent. Now I have a clear picture of the capability layer. Key findings:

  1. OverlayCapabilityPolicy: If suspended_at !== null, it short-circuits ALL actions EXCEPT resume. This is the capability overlay mechanism.
  2. LifecycleCapabilityBaselinePolicy: Checks if the action is allowed in the current lifecycle state per Constitution.
  3. ElectionCapabilityResolver: Evaluates policies in priority order, short-circuit denials stop downstream policies.

  Now I need to investigate:
  - The Election model for suspend/resume implementation
  - The controller/routes for suspend/resume
  - The lifecycle engine for how suspended state is handled
  - The UI layer for suspend/resume buttons
  - Side effects for suspend/resume
  - The resume transition behavior (note: target_state is 'suspended' as a placeholder)

  Let me also check the ElectionAction enum, ElectionLifecycleState enum, and the side effects.

∴ Thinking…

  Excellent. Now I have a very clear picture of the backend implementation. Key findings so far:

  1. ElectionConstitution defines suspend and resume as actions:
    - suspend: allowed in almost all states (all except archived and suspended), allowed roles: chief, platform_admin, target_state: suspended
    - resume: allowed only in suspended state, allowed roles: chief, platform_admin, target_state: suspended (placeholder, engine re-derives)
  2. ElectionLifecycleState includes Suspended = 'suspended' — with extensive architectural documentation explaining it's an "operational governance overlay" NOT part of lifecycle progression.
  3. ElectionLifecycleEngineImpl:
    - In getState(): Suspended is checked FIRST — if suspended_at !== null, return Suspended immediately before any other derivation.
    - In getAllowedActions(): Suspended state ONLY allows ['resume']
    - In derivePermissions(): Suspended has ALL permissions set to false
  4. OverlayCapabilityPolicy: Short-circuits ALL actions except resume when suspended_at !== null
  5. ElectionAction enum: Does NOT include Suspend or Resume cases. This is a VOCABULARY GAP.

  Now I need to check:
  - The Election model for suspend/resume methods and side effects
  - The controller and routes
  - The UI layer
  - The projection layer

  Let me search for the suspend/resume implementation in the model and routes/controllers.

∴ Thinking…

  Key findings so far:

  1. No suspend route exists — only resume route
  2. No suspend controller method exists — only resume controller method
  3. Election model has suspend/resume side effects:
    - suspend match arm calls applySideEffectsForSuspend()
    - resume match arm calls applySideEffectsForResume()
    - Fillable fields: suspended_at, suspended_lifecycle_context, suspended_by, suspended_reason, resumed_at, resumed_by
    - Casts for suspended_at and resumed_at as datetime

  Let me read the side effect methods in the Election model.

∴ Thinking…

  Critical findings:

  1. No SUSPEND route exists — There's a POST /resume route but NO POST /suspend route
  2. No SUSPEND controller method — There's a resume() method but no suspend() method in ElectionManagementController
  3. Side effects exist — applySideEffectsForSuspend() and applySideEffectsForResume() are properly implemented
  4. Constitution defines both — suspend and resume are in ElectionConstitution::RULES

  This means: The backend has incomplete implementation. The engine derives Suspended state, the side effects work, but the controller and route for suspend are missing. This makes suspend a "backend-capable
  but frontend-unreachable" feature.

  Let me also check:
  - The completeNomination matches the match arm in transitionTo (checking that the match arm has 'suspend')
  - The ElectionAction enum is missing both Suspend and Resume
  - The projection layer / snapshot layer
  - The UI layer for any suspend/resume buttons

∴ Thinking…

  Excellent! Now I have a very comprehensive picture. Key findings:

  1. Frontend Constants: ElectionActions.ts defines SUSPEND: 'suspend' and RESUME: 'resume' — both exist as constants
  2. Frontend LifecycleStates: ElectionLifecycleStates.ts defines SUSPENDED: 'suspended'
  3. Election model: Both suspend and resume match arms exist in transitionTo(), with proper side effects
  4. StateBadge.vue: Has a SUSPENDED state with icon and styles
  5. Management.vue: Has a suspended banner (line 82), a resume handler (line 1150), and a suspended status display (line 1058)
  6. Missing SUSPEND route: No POST /suspend route in electionRoutes.php
  7. Missing SUSPEND controller: No suspend() method in ElectionManagementController.php
  8. Missing SUSPEND button: Need to check StateMachinePanel.vue

  Let me now check the StateMachinePanel.vue for any suspend/resume buttons.

∴ Thinking…

  Now I have a complete picture. Let me also check:
  1. The canResume computed property in Management.vue
  2. The useElectionCapabilities.ts for suspend/resume capability checks
  3. Whether there's a canSuspend property exposed
  4. The election model's $appends field to see what's serialized

  Let me also check the suspension_category field mentioned in applySideEffectsForResume.

∴ Thinking…

  Now let me check the election model for the suspension_category field in $fillable and also check if the $appends/$casts for it.

∴ Thinking…

  Now let me check the useElectionCapabilities.ts for the canSuspend/canResume implementation and see if they are properly wired to the capability resolver.

∴ Thinking…

  Now I have the complete picture. Let me also quickly check what the backend sends as capabilities to the frontend — how the snapshot is built with capabilities, and whether suspend/resume are included.

∴ Thinking…

  Let me read the capability resolution code in the controller to see how it maps constitutional actions.

∴ Thinking…

  Now I have a comprehensive picture. Let me also check the $registry->getAllActions() to understand what the registry provides — this is the list that gets iterated for capability resolution, so it determines
   whether suspend/resume are checked.

∴ Thinking…

  Now I have the complete picture. The ElectionConstitutionRegistry::getAllActions() returns all keys from ElectionConstitution::RULES, which includes 'suspend' and 'resume'. So the capability resolver checks
  all 16+ constitutional actions, including suspend and resume, for every page load.

  The capability system is fully wired for suspend/resume:
  1. ElectionConstitutionRegistry reads from ElectionConstitution::RULES → includes suspend + resume
  2. ElectionCapabilityResolver evaluates all actions via policy chain → OverlayCapabilityPolicy short-circuits when suspended
  3. ElectionManagementController::resolveCapabilities() resolves capabilities for ALL constitutional actions → sends to frontend
  4. Frontend useElectionCapabilities exposes canSuspend and canResume computed refs
  5. Management.vue uses canResume for the resume button in the suspension banner

  Now the critical question: is there a Suspend button in the UI?

  Looking at the data:
  - StateMachinePanel.vue has NO suspend/resume references at all
  - Management.vue has a resume button (in the suspension banner) and canResume computed
  - Management.vue has NO suspend button anywhere
  - useElectionCapabilities.ts exposes canSuspend computed ref but it's not used in any template

  So: The architecture FULLY supports suspend/resume at the backend level, but the UI is missing the Suspend button. The Resume button exists and works (it's in the suspension banner). The canSuspend
  capability is computed and sent from the backend but NOT consumed in any visible UI element.

  Let me also check if there are any test files for suspension to confirm the test coverage.

∴ Thinking…

  There's a dedicated ElectionSuspensionTest.php file. Let me read it to understand the test coverage. Also let me check the projection test and the overlay policy test.

∴ Thinking…

  Now I have a comprehensive picture of the entire suspension implementation. Let me also quickly check the projection test.

∴ Thinking…

  Now let me also check the projection layer file.

∴ Thinking…

  Now I have the complete picture. Let me compile the comprehensive report.

  Summary of findings:

  1. Constitutional Vocabulary

  - ElectionConstitution::RULES defines both suspend and resume actions
  - suspend: allowed in 10/13 states (all except archived, suspended), roles: chief + platform_admin
  - resume: allowed only from suspended state, roles: chief + platform_admin
  - Target state for suspend: suspended
  - Target state for resume: suspended (placeholder — engine re-derives)
  - Suspension modeled as: lifecycle state with explicit documentation that it's an "operational governance overlay"

  2. Capability Resolver

  - OverlayCapabilityPolicy: Short-circuits ALL actions EXCEPT resume when suspended_at !== null
  - LifecycleCapabilityBaselinePolicy: Checks constitutional state allowlist
  - ElectionCapabilityResolver: Evaluates in priority order, Overlay runs first
  - In Suspended state: ElectionLifecycleEngineImpl::getAllowedActions() returns ['resume'] only
  - In Suspended state: derivePermissions() sets ALL permissions to false

  3. Backend Implementation

  Constitution: ✅ Both suspend and resume defined with proper rules
  Election model: ✅ Side effects: applySideEffectsForSuspend() captures lifecycle context; applySideEffectsForResume() clears flags
  transitionTo(): ✅ Both suspend and resume match arms exist with side effects
  Fillable fields: ✅ suspended_at, suspended_by, suspended_reason, suspension_category, suspended_lifecycle_context, resumed_at, resumed_by
  Casts: ✅ suspended_at and resumed_at as datetime
  ElectionLifecycleEngineImpl: ✅ Suspended state checked FIRST in getState(); only resume returned in allowedActions
  Route for resume: ✅ POST /resume exists
  Route for suspend: ❌ MISSING — no POST /suspend route
  Controller for resume: ✅ ElectionManagementController::resume() exists
  Controller for suspend: ❌ MISSING — no suspend() method
  ElectionAction enum: ❌ MISSING — no case Suspend or case Resume

  4. UI / Frontend

  ElectionActions.ts: ✅ Both SUSPEND: 'suspend' and RESUME: 'resume' defined
  ElectionLifecycleStates.ts: ✅ SUSPENDED: 'suspended' defined
  StateBadge.vue: ✅ Has suspended state entry
  Management.vue — Suspension banner: ✅ Fully implemented with resume button
  Management.vue — Resume handler: ✅ handleResume() posts to elections.resume route
  Management.vue — canResume: ✅ Used in template for resume button visibility
  Management.vue — Suspend button: ❌ MISSING — no suspend button anywhere
  StateMachinePanel.vue: ❌ No suspend/resume buttons at all
  useElectionCapabilities.ts: ✅ canSuspend and canResume computed refs defined
  useElectionCapabilities.ts: ❌ canSuspend is defined but NOT consumed anywhere in the UI

  5. Projection Sovereignty

  - ElectionLifecycleProjection: Suspended is correctly excluded from linear progression
  - completedStatesFor('suspended') returns []
  - isProjectionAvailable('suspended') returns false
  - Architectural documentation in PHPDoc: "SUSPENDED: overlay state — pre-suspension position restoration deferred to C.2.9"

  6. Tests

  - ElectionSuspensionTest.php: 6 tests covering all suspension lifecycle aspects
  - OverlayCapabilityPolicyTest.php: 6 tests covering policy behavior
  - ElectionLifecycleProjectionTest.php: Projection behavior tested
  - ElectionLifecycleEngineTest.php: State derivation with suspended
  - ConstitutionalTransitionGuardTest.php: Guard behavior

  Key Gaps (by severity):

  CRITICAL GAPS (will cause 500 errors or broken flows):
  1. No POST /suspend route — suspend action is unreachable from HTTP
  2. No suspend() controller method — no entry point
  3. ElectionAction enum missing Suspend and Resume cases — vocabulary gap

  MODERATE GAPS (UI not complete):
  4. No Suspend button in Management.vue — canSuspend is computed but never used in template
  5. No Suspend button in StateMachinePanel.vue — no suspend/resume at all
  6. canSuspend computed ref in useElectionCapabilities.ts has no consumer

  MINOR OBSERVATIONS:
  7. suspension_category is cleared in resume but has no fillable entry (line 178)
  8. No event dispatching for suspend/resume (unlike open_voting which dispatches VotingOpened)

  Now I have all the information needed for the comprehensive report.

● I now have a complete picture. Here is the full architectural report.

  ---
  Architectural Review: Election Suspension Capability

  1. Constitutional Vocabulary

  Status: Well-defined at the constitution level.

  ElectionConstitution::RULES defines both actions:

  ┌─────────┬──────────────────────────────────────────────────┬───────────────────────┬───────────────┬────────────────────────────────────────────┐
  │ Action  │                  Allowed States                  │     Allowed Roles     │ Preconditions │                Target State                │
  ├─────────┼──────────────────────────────────────────────────┼───────────────────────┼───────────────┼────────────────────────────────────────────┤
  │ suspend │ 10 of 13 states (all except archived, suspended) │ chief, platform_admin │ none          │ suspended                                  │
  ├─────────┼──────────────────────────────────────────────────┼───────────────────────┼───────────────┼────────────────────────────────────────────┤
  │ resume  │ suspended only                                   │ chief, platform_admin │ none          │ suspended (placeholder; engine re-derives) │
  └─────────┴──────────────────────────────────────────────────┴───────────────────────┴───────────────┴────────────────────────────────────────────┘

  Suspension is architecturally modeled as a lifecycle state that behaves as an operational overlay. The enum ElectionLifecycleState::Suspended carries extensive PHPDoc documenting that it must NOT be included
   in lifecycle progression, steppers, or phase sequences.

  2. Capability Resolver

  Status: Fully implemented and correct.

  Three-layer enforcement:

  ┌────────────────────────┬───────────────────────────────────┬────────────────────────────────────────────────────────────────────┐
  │         Layer          │              Policy               │                              Behavior                              │
  ├────────────────────────┼───────────────────────────────────┼────────────────────────────────────────────────────────────────────┤
  │ Overlay (priority 1)   │ OverlayCapabilityPolicy           │ If suspended_at !== null, short-circuits ALL actions EXCEPT resume │
  ├────────────────────────┼───────────────────────────────────┼────────────────────────────────────────────────────────────────────┤
  │ Lifecycle (priority 2) │ LifecycleCapabilityBaselinePolicy │ Checks state allowlist from Constitution                           │
  ├────────────────────────┼───────────────────────────────────┼────────────────────────────────────────────────────────────────────┤
  │ Resolver               │ ElectionCapabilityResolver        │ Evaluates in priority order; short-circuit denials stop downstream │
  └────────────────────────┴───────────────────────────────────┴────────────────────────────────────────────────────────────────────┘

  The OverlayCapabilityPolicy is proven correct by 6 unit tests in OverlayCapabilityPolicyTest.php.

  Engine derivation (ElectionLifecycleEngineImpl):
  - getState(): Suspended checked FIRST — if suspended_at !== null, immediately returns Suspended before any other derivation
  - getAllowedActions(): Suspended state returns only ['resume']
  - derivePermissions(): Suspended sets ALL permissions to false

  3. Backend Implementation

  Status: Partially implemented — side effects exist, but HTTP entry point is missing.

  Complete ✅

  ┌──────────────────────────────┬───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┬───────────────────────────────────────────────────┐
  │           Artifact           │                                                       Location                                                        │                      Status                       │
  ├──────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┼───────────────────────────────────────────────────┤
  │ Side-effect for suspend      │ Election::applySideEffectsForSuspend()                                                                                │ Captures engine-derived state as forensic         │
  │                              │                                                                                                                       │ evidence                                          │
  ├──────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┼───────────────────────────────────────────────────┤
  │ Side-effect for resume       │ Election::applySideEffectsForResume()                                                                                 │ Clears all suspension flags; re-derivation        │
  │                              │                                                                                                                       │ happens                                           │
  ├──────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┼───────────────────────────────────────────────────┤
  │ transitionTo() match arms    │ Both suspend and resume with side effects                                                                             │ Line 1738-1739                                    │
  ├──────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┼───────────────────────────────────────────────────┤
  │ Fillable fields              │ suspended_at, suspended_by, suspended_reason, suspension_category, suspended_lifecycle_context, resumed_at,           │ Lines 174-180                                     │
  │                              │ resumed_by                                                                                                            │                                                   │
  ├──────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┼───────────────────────────────────────────────────┤
  │ Resume route                 │ POST /resume → ElectionManagementController::resume()                                                                 │ Lines 278-281                                     │
  ├──────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┼───────────────────────────────────────────────────┤
  │ Resume controller            │ Calls transitionTo(Transition::manual('resume', ...))                                                                 │ Complete with error handling                      │
  ├──────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┼───────────────────────────────────────────────────┤
  │ ElectionConstitutionRegistry │ getAllActions() returns all keys including suspend and resume                                                         │ The capability resolver iterates all 16+ actions  │
  ├──────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┼───────────────────────────────────────────────────┤
  │ suspension_category column   │ Filled by suspend, cleared by resume                                                                                  │ Used in tests                                     │
  └──────────────────────────────┴───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┴───────────────────────────────────────────────────┘

  Missing ❌

  ┌──────────────────────────────────┬───────────────────────────────────────────────┬──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
  │             Artifact             │                   Location                    │                                                        Impact                                                        │
  ├──────────────────────────────────┼───────────────────────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ POST /suspend route              │ electionRoutes.php                            │ CRITICAL: Suspend action is unreachable from HTTP                                                                    │
  ├──────────────────────────────────┼───────────────────────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ suspend() controller method      │ ElectionManagementController                  │ CRITICAL: No HTTP entry point                                                                                        │
  ├──────────────────────────────────┼───────────────────────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ ElectionAction::Suspend enum     │ ElectionAction.php                            │ MODERATE: Constitutional vocabulary exists as string in RULES but not in the enum                                    │
  │ case                             │                                               │                                                                                                                      │
  ├──────────────────────────────────┼───────────────────────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ ElectionAction::Resume enum case │ ElectionAction.php                            │ MODERATE: Same gap — resume defined in Constitution but missing from enum                                            │
  ├──────────────────────────────────┼───────────────────────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ Events for suspend/resume        │ transitionTo() event dispatch (line           │ Minor: suspend and resume fall through to generic ElectionStateChangedEvent rather than having dedicated domain      │
  │                                  │ 1751-1758)                                    │ events                                                                                                               │
  └──────────────────────────────────┴───────────────────────────────────────────────┴──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

  4. UI / Frontend

  Status: Resume is fully wired; Suspend has no visible UI.

  Resume — Complete ✅

  ┌─────────────────────────┬────────────────────────────────────────────┬────────────────────────────────────────────┐
  │         Element         │                    File                    │                   Status                   │
  ├─────────────────────────┼────────────────────────────────────────────┼────────────────────────────────────────────┤
  │ Suspension banner       │ Management.vue:76-94                       │ Red banner with resume button              │
  ├─────────────────────────┼────────────────────────────────────────────┼────────────────────────────────────────────┤
  │ canResume computed      │ Management.vue via useElectionCapabilities │ Controls button visibility                 │
  ├─────────────────────────┼────────────────────────────────────────────┼────────────────────────────────────────────┤
  │ handleResume()          │ Management.vue:1147-1155                   │ Posts to elections.resume                  │
  ├─────────────────────────┼────────────────────────────────────────────┼────────────────────────────────────────────┤
  │ canResume in composable │ useElectionCapabilities.ts:113             │ Delegates to canDo(ElectionActions.RESUME) │
  └─────────────────────────┴────────────────────────────────────────────┴────────────────────────────────────────────┘

  Suspend — Missing ❌

  ┌──────────────────────────────┬────────────────────────────────┬────────────────────────────────────────────────────────────────┐
  │           Element            │              File              │                             Status                             │
  ├──────────────────────────────┼────────────────────────────────┼────────────────────────────────────────────────────────────────┤
  │ canSuspend computed          │ useElectionCapabilities.ts:112 │ ✅ Defined but never consumed                                  │
  ├──────────────────────────────┼────────────────────────────────┼────────────────────────────────────────────────────────────────┤
  │ Suspend button               │ Management.vue                 │ ❌ Does not exist                                              │
  ├──────────────────────────────┼────────────────────────────────┼────────────────────────────────────────────────────────────────┤
  │ Suspend button               │ StateMachinePanel.vue          │ ❌ Does not exist (no suspend/resume at all in this component) │
  ├──────────────────────────────┼────────────────────────────────┼────────────────────────────────────────────────────────────────┤
  │ Suspend action constant      │ ElectionActions.ts:19          │ ✅ SUSPEND: 'suspend' defined                                  │
  ├──────────────────────────────┼────────────────────────────────┼────────────────────────────────────────────────────────────────┤
  │ Suspended lifecycle constant │ ElectionLifecycleStates.ts:21  │ ✅ SUSPENDED: 'suspended' defined                              │
  ├──────────────────────────────┼────────────────────────────────┼────────────────────────────────────────────────────────────────┤
  │ Suspended state badge        │ StateBadge.vue:40              │ ✅ Has icon, color, message key                                │
  └──────────────────────────────┴────────────────────────────────┴────────────────────────────────────────────────────────────────┘

  Suspension banner in Management.vue pulls from backend correctly:

  // line 1040
  canResume,  // destructured from useElectionCapabilities
  // line 85
  v-if="canResume"  // controls resume button visibility

  But there is no corresponding canSuspend usage anywhere in the template, despite the computed ref existing.

  5. Projection Sovereignty Alignment

  Status: Correctly implemented.

  ElectionLifecycleProjection explicitly excludes Suspended from linear progression:

  private const PROGRESSION = [Draft → SubmittedForApproval → ... → Archived];  // Suspended NOT included

  - completedStatesFor('suspended') returns []
  - isProjectionAvailable('suspended') returns false

  PHPDoc states: "SUSPENDED: overlay state — pre-suspension position restoration deferred to C.2.9"

  This is architecturally consistent with the "operational overlay" model.

  6. Architecture Consistency Assessment

  What works correctly:

  - Constitutional vocabulary is well-defined with proper state/role scoping
  - Capability enforcement is multi-layered and tested — OverlayCapabilityPolicy short-circuits correctly, engine returns only resume during suspension
  - Side effects preserve forensic data (suspended_lifecycle_context as audit trail) without using it for restoration
  - Resume correctly does NOT restore from context column — engine re-derives from current facts (time continues during suspension)
  - Projection sovereignty correctly excludes Suspended from linear progression
  - Database schema has all needed columns with proper casts
  - Resume flow is fully wired end-to-end: UI → route → controller → transitionTo → side effects → engine re-derivation
  - Dedicated test file ElectionSuspensionTest.php has 6 tests covering all lifecycle invariants

  What is broken:

  ┌────────────────────────────────────────────────┬────────────────┬──────────────────────────────────────────────────────────────┐
  │                      Gap                       │     Layer      │                            Impact                            │
  ├────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────────────────────────┤
  │ No POST /suspend route                         │ Infrastructure │ Suspend action unreachable via HTTP                          │
  ├────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────────────────────────┤
  │ No suspend() controller                        │ Interface      │ No entry point for the action                                │
  ├────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────────────────────────┤
  │ ElectionAction enum missing Suspend and Resume │ Domain         │ Vocabulary gap between enum and Constitution                 │
  ├────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────────────────────────┤
  │ No Suspend button in UI                        │ Frontend       │ Feature invisible to users                                   │
  ├────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────────────────────────┤
  │ canSuspend unused                              │ Frontend       │ Computed but no consumer                                     │
  ├────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────────────────────────┤
  │ No suspend event dispatching                   │ Domain         │ Minor — no dedicated domain event (falls through to generic) │
  └────────────────────────────────────────────────┴────────────────┴──────────────────────────────────────────────────────────────┘

  Root cause of the missing Suspend button:

  The gap is at the HTTP routing layer, not at the capability or domain layer. The capability resolver correctly returns suspend as allowed for chiefs and platform_admins in all non-terminal states. The
  frontend canSuspend computed reads this correctly from the backend snapshot. But even if a button existed, it would have no route to post to.

  7. Deliverables

  Current Implementation Status

  ┌─────────────────────────────────┬─────────────────────────┬──────────────┐
  │            Component            │         Status          │ Completeness │
  ├─────────────────────────────────┼─────────────────────────┼──────────────┤
  │ Constitutional rules            │ ✅ Defined              │ 100%         │
  ├─────────────────────────────────┼─────────────────────────┼──────────────┤
  │ Engine derivation               │ ✅ Implemented          │ 100%         │
  ├─────────────────────────────────┼─────────────────────────┼──────────────┤
  │ Overlay capability enforcement  │ ✅ Implemented + tested │ 100%         │
  ├─────────────────────────────────┼─────────────────────────┼──────────────┤
  │ Side effects (suspend + resume) │ ✅ Implemented          │ 100%         │
  ├─────────────────────────────────┼─────────────────────────┼──────────────┤
  │ Projection exclusion            │ ✅ Implemented + tested │ 100%         │
  ├─────────────────────────────────┼─────────────────────────┼──────────────┤
  │ Resume route + controller       │ ✅ Implemented          │ 100%         │
  ├─────────────────────────────────┼─────────────────────────┼──────────────┤
  │ Resume UI button                │ ✅ Implemented          │ 100%         │
  ├─────────────────────────────────┼─────────────────────────┼──────────────┤
  │ Suspend route                   │ ❌ Missing              │ 0%           │
  ├─────────────────────────────────┼─────────────────────────┼──────────────┤
  │ Suspend controller              │ ❌ Missing              │ 0%           │
  ├─────────────────────────────────┼─────────────────────────┼──────────────┤
  │ Suspend UI button               │ ❌ Missing              │ 0%           │
  ├─────────────────────────────────┼─────────────────────────┼──────────────┤
  │ ElectionAction enum entry       │ ❌ Missing              │ 0%           │
  ├─────────────────────────────────┼─────────────────────────┼──────────────┤
  │ Suspend test (constitutional)   │ ✅ Implemented          │ 100%         │
  └─────────────────────────────────┴─────────────────────────┴──────────────┘

  Complete Suspend/Resume Flow Map

  User clicks Suspend?        [NO BUTTON EXISTS]
      ↓
  POST /suspend               [ROUTE MISSING]
      ↓
  ElectionManagementController::suspend()     [METHOD MISSING]
      ↓
  transitionTo(suspend)
      ↓
  ConstitutionalTransitionGuard::assertAllowed()
      ├── action 'suspend' in RULES?              ✅ YES
      ├── allowed in current state?                ✅ YES (10 states)
      ├── user has required role?                  ✅ YES (chief/platform_admin)
      └── preconditions met?                       ✅ YES (none)
      ↓
  applySideEffectsForSuspend()
      ├── suspended_at = now                       ✅
      ├── suspended_by = actor                     ✅
      ├── suspended_reason = reason                ✅
      ├── suspension_category                      ❌ (not passed from transition metadata)
      └── suspended_lifecycle_context = engine     ✅
      ↓
  Engine now derives Suspended state              ✅
  ↓
  Capabilities: ALL denied except resume           ✅

  --- later ---

  User clicks Resume Button                       ✅ (exists)
      ↓
  POST /resume                                    ✅
      ↓
  ElectionManagementController::resume()          ✅
      ↓
  transitionTo(resume)
      ↓
  applySideEffectsForResume()
      ├── suspended_at = null                      ✅
      ├── suspended_by = null                      ✅
      ├── suspended_reason = null                  ✅
      ├── suspension_category = null               ✅
      ├── suspended_lifecycle_context = null        ✅
      └── resumed_at = now, resumed_by = actor     ✅
      ↓
  Engine re-derives state from current facts     ✅
      (e.g., if voting window expired → Counting)

  Recommended Fixes (in priority order)

  1. CRITICAL: Add suspend route + controller
    - Add Route::post('/suspend', ...) to electionRoutes.php
    - Add suspend(Election $election) method to ElectionManagementController
    - Pattern: identical structure to resume() but calling transitionTo(Transition::manual('suspend', ...))
  2. MODERATE: Add Suspend and Resume to ElectionAction enum
  case Suspend = 'suspend';
  case Resume = 'resume';
  3. MODERATE: Add Suspend button to UI
    - In Management.vue, add a suspend button visible when canSuspend is true
    - This would appear in the StateMachinePanel or as an action button in the management header
    - Already has canSuspend computed ref ready to use
  4. LOW: Add suspension_category to suspend transition metadata
    - applySideEffectsForSuspend() currently doesn't receive suspension_category — it would need to be added to Transition metadata

  Where the Suspend Button Should Appear

  Based on the current architecture, the suspend button should appear:

  1. In Management.vue — as an action button (similar to Close Voting, Publish Results) visible when canSuspend is true. The canSuspend computed already exists on line 112 of useElectionCapabilities.ts but has
   no template consumer.
  2. In StateMachinePanel.vue — as part of the phase action buttons, gated by capabilities.canDo(ElectionActions.SUSPEND). This component currently has no suspend/resume knowledge at all.

  The current Suspension Banner (red, at top of Management.vue) appears automatically when the engine derives Suspended state, and the resume button within it is correctly gated by canResume.

  Architectural Verdict: What Suspension IS

  Suspension is currently implemented as a lifecycle state with overlay semantics — a hybrid model:

  - It IS a lifecycle state (ElectionLifecycleState::Suspended) — the engine derives it and transitionTo sets it
  - It BEHAVES as an overlay — OverlayCapabilityPolicy short-circuits before lifecycle policy
  - It IS NOT in linear progression — projection layer excludes it
  - Resume correctly does NOT restore the old state — it clears flags and lets the engine re-derive

  This is architecturally coherent but has one tension: the state column is written as suspended during transitionTo, but the engine derivation ignores the state column and checks suspended_at first. The state
   column acts as a convenience cache. This dual-write is consistent with the rest of the constitutional architecture but creates a minor semantic ambiguity (is it a state or an overlay?).

  The long-term resolution (deferred to C.2.9 per projection PHPDoc) would be a true dual-axis model where lifecycle progression and operational status are independent dimensions. Until then, the current model
   is functionally correct.

✻ Cooked for 1m 48s

> is suspend implemented in this page resources\js\Pages\Election\Management.vue?

∴ Thinking…

  The user is asking specifically whether the suspend functionality is implemented in Management.vue. Let me check the file directly for any suspend-related code.

● Search(pattern: "suspend", path: "C:\Users\nabra\OneDrive\Desktop\roshyara\xamp\nrna\nrna-eu\resources\js\Pages\Election\Management.vue")
  ⎿  Found 23 lines
     74-
     75-        <!-- Suspension Banner -->
     76:        <div v-if="currentState === ElectionLifecycleStates.SUSPENDED" class="bg-red-50 border-2 border-red-400 rounded-xl p-6">
     77-          <div class="flex items-start gap-4">
     78-            <div class="text-3xl">🚫</div>
     79-            <div class="flex-1">
     80:              <h3 class="text-lg font-bold text-red-900 mb-2">Election Suspended</h3>
     81-              <p class="text-red-800 mb-4">
     82:                This election has been suspended. All governance operations are locked until the election is resumed.
     83-              </p>
     84-              <button
     --
     463-            <div class="rounded-xl bg-amber-50 border border-amber-200 p-4 text-center">
     464-              <p class="text-2xl font-bold text-amber-600">{{ stats.by_status?.inactive ?? 0 }}</p>
     465:              <p class="text-xs text-slate-500 mt-0.5">{{ t.sections.voter_management.suspended_label }}</p>
     466-            </div>
     467-          </div>
     --
     1056-  if (state === ElectionLifecycleStates.RESULTS_PUBLISHED) return { icon: '✅', status: 'Election complete and results published. All phases are finished.', badge: 'Results Published', color:
     'emerald' }
     1057-  if (state === ElectionLifecycleStates.REJECTED) return { icon: '❌', status: 'Election was rejected. You can revise and resubmit for approval.', badge: 'Rejected', color: 'amber' }
     1058:  if (state === ElectionLifecycleStates.SUSPENDED) return { icon: '🚫', status: 'Election is suspended. All operations are locked until the election is resumed.', badge: 'Suspended', color: 'red' }
     1059-  if (state === ElectionLifecycleStates.ARCHIVED) return { icon: '📦', status: 'Election is archived. The election workflow is complete.', badge: 'Archived', color: 'gray' }
     1060-  return { icon: '📋', status: 'Unknown state', badge: state, color: 'slate' }

● No, suspend is NOT implemented in Management.vue in the sense of a trigger/action. Here's what exists vs. what's missing:

  What IS implemented (read-only):
  - Line 76: Suspension banner — visible only when engine derives SUSPENDED state (i.e., after suspension has already happened elsewhere)
  - Line 1058: Status badge for the suspended state
  - Line 465: Voter suspension label (unrelated — this is about voter membership status, not election suspension)

  What is MISSING:
  - No Suspend button or trigger — nothing posts to a suspend route
  - No handleSuspend() handler function
  - No canSuspend consumed in the template (the computed exists in useElectionCapabilities.ts line 112 but has no consumer)

  The page can display suspension (via the banner and status badge) and resume from it (line 84-94 has the resume button), but there is no way to initiate a suspension from this page.

  Line 76's v-if="currentState === ElectionLifecycleStates.SUSPENDED" is a reactive display — it shows the banner when the backend says the election is already suspended. It cannot trigger suspension itself.
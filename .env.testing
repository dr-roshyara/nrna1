# ⚠️ TESTING ENVIRONMENT ONLY ⚠️
# This file is used EXCLUSIVELY when running tests via: php artisan test
# NEVER modify DB_DATABASE to point to development or production databases
# Violations will be caught by TestCase safety checks

APP_ENV=testing
DB_CONNECTION=pgsql
DB_HOST=127.0.0.1
DB_PORT=5432
DB_DATABASE=nrna_test
DB_USERNAME=publicdigit_user
DB_PASSWORD="Rudolfvogt%27%"

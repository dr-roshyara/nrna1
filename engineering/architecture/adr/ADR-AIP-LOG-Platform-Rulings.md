# ADR-AIP-LOG — PublicDigit Engineering Platform · Rulings Register

**Status:** Living (append-only) · **Owner:** Architecture Review Board · **Created:** 2026-07-08
**Pattern precedent:** `ADR-T-LOG-Tactical-Implementation.md` (living log beside frozen ADRs).

> **Why this file exists (structural correction, ARB 2026-07-08):** ARB rulings were recorded during the architecture program inside `docs/architecture/proposals/ai-platform/Phase-02.5-Certification-Plan.md` §6. After ADR-AIP-01 accepted the Baseline, the proposal documents became **historical evidence** — *what was proposed* — and must never be edited again. **R-1..R-29 remain there as the historical record of the program.** From R-30 onward, rulings live HERE. Proposals stay frozen; ADRs record acceptance; this log records ongoing rulings.

## Historical index (recorded in Phase-02.5 §6 — do not re-edit there)

R-1..R-6 (Phase 2 review) · R-7..R-11 (follow-up: rename, 3A/3B split, constitution, litmus) · R-12..R-14 (architecture closure) · R-15..R-18 (Baseline resolution, ADR-AIP-01) · R-19..R-22 (sign-off: Construction/Qualification renames, iterations) · R-23 (AIP-14, ADR-AIP-02) · R-24 (FF-17 definition) · R-25 (C2 re-sequenced, evidence) · R-26 (measuring instrument; value ledger; smallest-change question; retrospective) · R-27 (governance freeze) · R-28 (Engineering Doctrine candidate, CMP-009 reserved) · R-29 (Foundation complete; final instruction: *no platform change unless PB-004 or a subsequent feature demonstrates insufficiency*).

## Rulings (living, append-only)

| # | Date | Ruling | Effect |
|---|---|---|---|
| R-30 | 2026-07-08 | **Proposals sealed permanently.** `docs/architecture/proposals/ai-platform/Phase-0*.md` are historical evidence of what was proposed; **no further edits, ever** — changes happen via new ADR-AIP entries or this log. §6 of Phase-02.5 is closed with a redirect to this file. | This log becomes the only living rulings home |
| R-31 | 2026-07-08 | **Rename: "Engineering Doctrine" → "Engineering Standards"** (applies to the R-28 / CMP-009 candidate and all future references). Standards are provider-neutral, verifiable, traceable, and professionally familiar; "doctrine" reads ideological. | Candidate renamed; OI-1 absorbs the term |
| R-32 | 2026-07-08 | **Engineering Standards — candidate spec refined** (still AFTER PB-004, per R-28 trigger + AIP-14): one canonical document `docs/engineering/Engineering_Standards.md` — the engineering constitution — consolidating today's fragments into rule-ids (`DDD-nn · TDD-nn · CA-nn · HEX-nn · VER-nn · AI-nn`, e.g. *AI-01: AI never owns engineering authority*), ordered **business → technology**: Strategic DDD → Tactical DDD → TDD → Clean → Hexagonal → Event-Driven → CQRS → ADR → Platform Rules. Provider-neutral: every future AI follows them. | Spec on record; built only if the PB-004 retrospective triggers R-28 |
| R-33 | 2026-07-08 | **Next milestone defined: "Operational Readiness v1.0"** (not "finish the .claude folder"). Success criteria: (1) the platform guides one real PublicDigit feature (PB-004) from planning through implementation, verification, review, and completion **without architectural gaps**; (2) every engineering decision traceable to an ADR, Engineering Standard, or platform rule; (3) DDD-first, TDD-first, Clean and Hexagonal architecture demonstrated **in practice**, not merely by documentation. ARB progress snapshot at definition: ~55% overall (architecture/governance 100%; runtime construction ~20%; Verification Engine ~35% pending C3; PB-004 0%). | Milestone in force; supersedes any file-count framing |
| R-34 | 2026-07-08 | **Ruling-classification discipline** (ARB correction at C3 approval): three classes of review information — (1) **reviewer observations** (discussion; recorded in session logs only), (2) **ARB decisions** (rulings here / ADRs — created only on explicit ARB adoption, never by inference from praise or suggestion), (3) **implemented engineering changes** (code/docs). Reviewer suggestions are NOT automatically rulings; default classification is *observation*. Purpose: the rulings log must not grow faster than the software. (R-32/R-33 accepted as grandfathered; the discipline applies from R-34 onward.) Superseded in detail by the A/B/C/D classification in `.claude/platform/OPERATING_INSTRUCTIONS.md` (AST-013, ARB-issued) — same rule, finer grain. | Recording behavior changed |
| R-35 | 2026-07-08 | **Rename ADOPTED (execution deferred): "AI Engineering Platform" → "PublicDigit Engineering Platform"** — explicit ARB direction ("stop calling it AI Engineering Platform internally"); rationale: provider-independent, engineering-process-driven, DDD-governed, evidence-based — Claude is simply the first provider. **Timing: gradual, after PB-004 succeeds**, via the OI-1/02.6 supersession (registry `platform.name`, MEMORY/CONTEXT labels, and doc titles follow then; no renames before PB-004). | Decision on record; nothing renamed yet |
| R-36 | 2026-07-09 | **AI Architecture Promotion Review ADOPTED (evidence-based; PB-004 · PB-005 · PB-006).** First use of the AIP-13 amendment path after the R-27 freeze precondition (PB-004 retrospective) was met. **Promoted into AST-013 (`OPERATING_INSTRUCTIONS.md` §Promoted Engineering Behaviours), 6 one-line behaviours:** ownership-determines-architectural-reuse (never precedent) · reuse-before-create · deferred ≠ skipped · epistemic labels (Observed·Measured·Derived·Interpreted·Recommended — broadened to ALL architectural recommendations/reviews) · stop-at-architectural-uncertainty (surface, classify, request ARB — never silently invent architecture) · implementation-evidence-outweighs-unverified-theory (ARB wording refinement at adoption: "unverified theory", not "theoretical elegance" — elegance is not the enemy; untested assumptions are). **Expressly NOT promoted:** Registration ≠ Delivery stays Messaging-scoped in ADR-MP-06 (1 slice); Domain-Event ≠ Integration-Event + the Discovery→IDD→RED→GREEN→Qualification→Completion chain are Category-B follow-ups (principles doc / v1.1 ratification — separate slices); Strangler reconstitution, `ChallengeResolvedIntegration` carrier, PGP-03 hoist, Engineering Standards (R-28/CMP-009) remain Candidates. **AST-008: deprecate → remove next release** (one migration cycle). **Promotion Report:** promoted 6 (2 added by ARB at review) · Category-B follow-ups 2 · platform-doc (C) 4 · candidates (D) 5 · already-permanent (duplication refused) 4 · removal scheduled 1 · new documents 0. Full matrix: `claude/plans/swirling-jingling-blossom.md`; session log 2026-07-09. | AST-013 amended (+6 lines); AI Architecture smaller-per-value; no governance/workflow/DDD/Messaging change |
| R-37 | 2026-07-10 | **Architecture Freeze 2.0 (structural).** Issued at EM-001 closure ("the right place to stop"). Complements R-27 (governance freeze) with a structural freeze: **no new top-level folders · no new domain objects · no new capability hierarchy · no more document reorganizations** — until (1) C3 passes, (2) PB-004 completes, (3) the retrospective analyzes real implementation evidence. Only then may the platform reconsider whether concepts like `Capability` become first-class structural elements. Context on record (Class A, not ruled): repository = three bounded contexts (Product · Engineering · Runtime); maturity ladder L1 prompt collection → L2 documentation → L3 governance → L4 evidence-driven (largely complete) → **L5 executable platform (next: Pattern→FitnessFunction, Review→ExecutableReviewModule, Report→QualificationEvidence)** → L6 multi-project; neither L5 nor L6 is reached by moving files. The DDD-aggregate tree and Platform≙Adoption split remain **research hypotheses** — not roadmap, not design, not future architecture. **ARB extension (same-day, one sentence — burden of proof, not prohibition):** *any proposal for architectural expansion before the retrospective must include evidence that the existing architecture was insufficient; improvements without demonstrated insufficiency are rejected by default* (operationalizes R-29). **L5 definition refined at adoption (ARB):** Level 5 begins when executable governance is part of the normal engineering workflow — every slice runs the gates, evidence is stored, review consumes it — **not when the first script exists** (one script proves possibility; routine use proves capability). Operational terms: until the retrospective, `engineering/` permits only bug fixes · broken-link fixes · typo corrections. | Structural freeze in force; burden of proof reversed; EM-001 is the last reorganization before the retrospective |
| R-38 | 2026-07-11 | **Engineering Platform Conceptual Freeze.** Declared by the ARB at the close of the constitutional consolidation ("not because it is perfect, but because every additional concept should now have to justify itself through operational evidence"). Consolidates the per-document stopping rules already in force (ES set complete · Decision Model complete · decision catalog complete · knowledge research frozen) into **one platform-wide declaration: no new concepts, standards, decisions, or architectural subsystems** — changes enter only through operational evidence via the ES-006.1 promotion ladder. *Parsimony note: this operationalizes R-29 + R-37 at the conceptual layer and adds no new mechanism.* **ARB verdict on record:** Architecture — mature enough for operational validation · Governance — complete enough for ratification · Conceptual growth — paused · Next phase — evidence collection, not design. Readiness indicator cited by the ARB: recent changes no longer introduce subsystems; they refine language and tighten boundaries — the signature of an architecture approaching stability. Remaining work is empirical: ratification batch → C3 (fresh session) → STABLE → Project Knowledge pilot → retrospective. **ARB same-day correction (rule parsimony applied to this ruling itself — Option B):** *This ruling introduces NO new governance. It records the Board's assessment that existing rulings (R-27 · R-29 · R-37, with ES-001.1 and the ES-006.1 ladder) collectively place the platform into operational validation.* R-38 is a **consolidation/assessment ruling, not a sixth freeze**; the ARB noted that questioning R-38's own necessity is rule parsimony behaving correctly — the architecture caught itself. Precedent going forward (ARB preference, Option A): where existing rulings already express a constraint, **interpret them rather than add another ruling**, even a consolidating one. | Consolidation ruling — no new governance; operational-validation phase recorded |
| R-39 | 2026-07-26 | **Governance exception — early promotion of the DDD Tactical Governance Principles to the Engineering Platform.** The normal promotion rule (ES-006.1 + the EPIC-004 track's own self-governance: *"principles are promoted only on repeated evidence and explicitly NOT promoted when evidence is domain-specific"*) requires evidence from more than one bounded context. The Decision Authority approved promotion on 2026-07-26 with evidence from ONE context (PublicDigit Adjudication/Determination, EPIC-004D..K). **Reason:** the seven principles are generalized methodology by construction (no project-specific term appears in any statement); the DA ruled the generalized rule belongs in `engineering/` with a corresponding runtime hook. **Expected validation:** the next bounded context that adopts the module either confirms the principles or produces the amendment evidence; the multi-context bar continues to apply to FUTURE methodology promotions — this exception does not weaken the rule. Canonical home: `engineering/knowledge/methodology/DDD_Tactical_Governance_Principles.md` · PublicDigit binding: `docs/architecture/governance/DDD_PRINCIPLES.md` (binding-only, never a second copy) · runtime: AST-014. | Early promotion recorded as explicit exception; promotion rule intact |
| R-40 | 2026-07-27 | **Phase A — Knowledge Architecture Governance (Decision Authority response to the reconstruction cycle: Phase 1 baseline · Foundations/Theory reference · IA review · Knowledge Domain Model).** Five rulings, transcribed verbatim-in-substance: **A1 APPROVED** — the platform shall introduce a governed placement rule for knowledge artifacts, deriving canonical location from (1) knowledge object kind · (2) knowledge type (Normative · Descriptive · Evidence · Runtime · Learning) · (3) lifecycle · (4) authority level · (5) canonical serialization; future repository organization follows the rule, never case-by-case discussion. **A2 APPROVED** — *Architecture Analysis* is recognized as a distinct **descriptive** knowledge type (reconstruction reports, gap analyses, readiness assessments, architectural reviews); recognition only — **no folder is created**; serialization location decided after the placement rule completes. **A3 DEFERRED** — no file or folder moves during Phase A; restructuring only after the rule is governed, locations are derived from it, and changes are verified. **A4 AUTHORIZED** — prepare a **candidate** governed artifact `Engineering Platform Knowledge Metamodel`, derived from the Knowledge Domain Model + IA review (knowledge objects · placement semantics · canonical relationships · lifecycle semantics · repository invariants); **descriptive until explicitly adopted — do not adopt; submit for review**. **A5 AUTHORIZED** — after A1+A4 complete, produce a Repository Transition Plan (relocations derived from the rule; tooling, documentation, verification, migration-risk impacts); no restructuring before that plan is approved. **Sequence modified by the DA:** Phase A Governance → **Phase B Transition Planning** (impact analysis — governance never flows directly into implementation) → Phase C Repository Migration → Phase D Qualification & Verification. *Parsimony note (DA guidance + E-4 precedent): the placement rule's expected end-state is a clarification hosted in ES-005 (candidate ES-005.5), not a new standard.* *(Correction note, same edit: this row was momentarily inserted above R-39 and immediately re-appended here in chronological order — recorded per append-only discipline, ES-004.2.)* | Phase A in force; A4 candidate + OQ-ENG-004 protocol commissioned; moves deferred |
| R-41 | 2026-07-30 | **Artifact Lifecycle Consistency adopted as a permanent documentation standard (Principal Architect instruction).** An artifact has one authoritative lifecycle state at any moment; when work crosses a lifecycle boundary (authorization → execution → acceptance → closure), every authoritative artifact transitions with it. Work plans state the PRESENT state; session logs preserve the SEQUENCE of past states (append-only, ES-004.2); history is never rewritten to manufacture consistency. Minimum synchronization checklist at slice closure: Work Plan · CONTEXT.md · Session Log · Developer Guide · Acceptance Record · ADR references. Provenance: the WP-1 closure inconsistency (a CLOSED plan whose header still read "AUTHORIZED — execution begins…"), corrected 2026-07-30; first checklist execution the same day caught a second instance (ADR-T22 row's issuance-time "Implementation NOT yet authorized" clause, annotated). Parsimony honored: rule text hosted ONCE as **ES-004.3** (documentation standard, not a new standard document); runtime pointers only. | ES-004.3 in force; checklist binds every slice closure |
| R-42 | 2026-07-30 | **The Platform Registry's scope does NOT extend to project-side engineering artifacts — it governs AI Engineering Platform assets only.** Grounds (from the registry's own contract, not from preference): it self-declares as *"RUNTIME CONFIGURATION, machine-readable"*, sits in the platform `loading_order` boot sequence, types every asset by `runtime_moments` from a closed AI-session enum, and admits assets only on a five-question lineage ending in an **AIP principle · platform decision (PD-nn) · ADR-AIP-01** — *"an asset that cannot answer them shall not be registered — and shall not exist."* Project scripts, git hooks and CI workflows execute at **no AI runtime moment** and carry **no AIP lineage**; registering them would require **fabricating traceability**. Consistent with AIP-14 Product Primacy, R-37's reversed burden of proof, and the standing *smallest-platform-change* question. **Consequence:** Option C of the ownership investigation is REFUTED (not merely deferred); **Option D — Documentation Coverage Declaration — is ratified as ENG-007's architectural target** (each `developer_guide/<area>/00_index.md` declares `documentation_coverage:`; AST-006 derives routing; unclaimed paths surface as an explicit signal). **Vocabulary is binding: the declaration expresses DOCUMENTATION COVERAGE, never architectural ownership** — the two are distinct models and conflating them caused every earlier mis-mapping. Machine-readable *architectural* ownership stays deliberately unopened. Paper: `docs/implementation/Artifact_Ownership_Decision_Paper.md`. | Registry boundary affirmed; ENG-007 target fixed; documentation-coverage vocabulary binding |
| R-43 | 2026-08-01 | **WP-6 ACCEPTED (EPIC-004 temporal machinery) — Delivery Governance · Approval · ARB.** Evidence: WP-6 closure package — 3/3 authorized items delivered · 10 tests / 22 assertions · all gates pass (PHPStan max clean with 4 root fixes and none suppressed · Deptrac 0 · Architecture 146 green) · structural integrity accepted · governance integrity accepted · authority reviews complete. **Recorded limit on the evidence (not a qualification of the acceptance):** the AP-1/AP-2 defect class — *a business value was invented* — is covered by NO automated gate; both were found and fixed before acceptance by human preservation review. | WP-6 CLOSED/ACCEPTED; WP-7 entry conditions satisfied **ANNOTATED 2026-08-01 (R-53, ES-004.3 status annotation -- this ruling's DECISION TEXT IS UNCHANGED and the acceptance STANDS):** reproduced evidence (R-50) establishes that under the documented reproduction protocol at commit 22d604844 the GreenfieldCore suite terminated with a fatal error and the merge gate would not have completed successfully. **Whether the evidence line was FALSE (gate run and misreported) or UNSUPPORTED (gate not run) remains UNDETERMINED from repository evidence.** See R-53. **RECORDING CORRECTION A1 (R-62, 2026-08-01 — annotation; decision unchanged): transition type reads ACCEPTANCE, not Approval.** The lifecycle transition is *completed work is accepted and closed*, which R-55 and R-59 type as **Acceptance**; this ruling typed it **Approval**. **Semantics unchanged — only the type label is corrected, so one transition no longer carries two types.** |
| R-44 | 2026-08-01 | **A-1 RATIFIED — Architecture Governance · Ratification · ARB.** *"MAD has exactly one home"* is the **invariant**; *"consume Adjudication's existing port"* was a **mechanism**, never an architectural decision. The invariant is binding; the mechanism is substitutable. **Approved realization: option (d) — Election declares its OWN consumer-side port** (`EvidencePreservationDurations` + an Election `Infrastructure/Config/` adapter reading the one canonical MAD key), preserving TP-1 and AP-2. Provenance: finding **G-1**, reclassified as an **Architecture–Enforcement Alignment Gap** (architecture correct · enforcement correct · the mapping between them incomplete). **Recorded limit:** *"Deptrac passes unmodified"* was an analytical prediction at ruling time, not an executed result. | G-1 RESOLVED; Election owns the consumer-side port; **Deptrac unmodified, domain model unchanged** |
| R-45 | 2026-08-01 | **A-2 RATIFIED — Architecture Governance · Ratification · ARB.** The retention guard is named as **two** responsibilities where it had been described as one: **Election ANSWERS** (*may this evidence be deleted yet?*) and **Audit/Retention ACTS** (the guard). Consistent with the frozen allocation's *"Consumption (acting on the answer)"*; **no responsibility holder changes.** Gates slice **7B**; 7A is unaffected. | Guard ownership CLARIFIED — two responsibilities, two holders, no overlap |
| R-46 | 2026-08-01 | **WP-7 EP-01 PLAN APPROVED — Planning Governance · Approval · DECISION AUTHORITY** (not the ARB: EP-01 plan approval is the Decision Authority's act, which is why it was voted separately). Subject: `.claude/plans/WP-7-retention-alignment.md` as amended 2026-08-01 — complete and consistent with the frozen architecture. Approval attaches to **the plan**, per EP-01's *"APPROVAL APPLIES TO THE PLAN, not merely to the task request"*. | The WP-7 plan is the binding statement of intended work |
| R-47 | 2026-08-01 | **SLICE 7A EXECUTION AUTHORIZED — Execution Governance · Authorization · ARB.** Guard `WP-6 ACCEPTED ∧ WP-7 PLAN APPROVED` satisfied by R-43 ∧ R-46 (and `PLAN APPROVED`'s own guard `G-1 RESOLVED` by R-44); zero architectural gates remain. **Scope is slice-granular: 7A ONLY — 7B and 7C are NOT authorized** and follow their own gates. First authorized activity: RED at 7A (per-election-type resolution · organisation override · fail-closed), plus the C-1 guard (engineering's call) and, recommended, R-D1. 7A is inert — no observable behaviour changes until 7C. **Recorded as unruled in the same session:** adoption of the `Layer Verification Rule` methodology module was offered as optional and non-blocking; **no ruling was issued, so it remains PROPOSED and non-binding — silence is not adoption (R-34).** | WP-7 opens at slice 7A; governance authorization complete for the approved scope; execution responsibility transfers to engineering |
| R-48 | 2026-08-01 | **WP-7 SLICE 7A ACCEPTED — Delivery Governance · Approval · ARB.** Evidence: slice 7A GREEN report — 11/11 keystones · Deptrac **0** with `deptrac.yaml` unmodified · PHPStan max clean · Architecture suite **149 green** · developer guide filed · RED report filed. Reason: the authorized scope was completed, the implementation respected the architectural boundaries, and the remaining merge-gate issue lies outside 7A's scope. **Recorded alongside the acceptance: 7A never demonstrated a complete `composer merge-gate` and does not claim to have — the gate remains blocked by F-7A-1. Acceptance is of 7A's authorized SCOPE, not of a green gate.** Also closed by this slice: **A-1's one labelled evidence limit** — *"Deptrac passes unmodified"* was an analytical prediction at R-44 and is now an executed result. | Slice 7A ACCEPTED and CLOSED; its implementation enters the accepted architecture baseline **RECORDING CORRECTION A1 (R-62, 2026-08-01 — annotation; decision unchanged): transition type reads ACCEPTANCE, not Approval.** The lifecycle transition is *completed work is accepted and closed*, which R-55 and R-59 type as **Acceptance**; this ruling typed it **Approval**. **Semantics unchanged — only the type label is corrected, so one transition no longer carries two types.** |
| R-49 | 2026-08-01 | **WP-6 REMEDIATION STRATEGY — OPTION B, HISTORICAL INVESTIGATION FIRST — Delivery Governance · Approval · ARB.** Reason: F-7A-1 evidences a *current* inconsistency, but whether it affects the earlier acceptance is a **historical** question; reproducing the historical gate before opening remediation **keeps evidence collection separate from repair**. **Recorded so Option B is not misread: reproduction is a precondition for judging R-43, NOT for repair — the merge gate stays red either way, so no slice can demonstrate a complete gate until remediation is opened, whenever that is.** No remediation work package is opened by this ruling. | Remediation DEFERRED pending reproduced evidence; two new queue items (open remediation? · note against R-43?) created and BLOCKED until it runs |
| R-50 | 2026-08-01 | **HISTORICAL REPRODUCTION AUTHORIZED — Delivery Governance · Authorization · ARB** *(conditional on R-49 = Option B; condition met)*. Purpose: reproduce `composer merge-gate` **at the recorded WP-6 closure commit** and **report only the reproduced evidence**. Explicit constraints: **no production changes · no test repairs · no governance edits · evidence collection only.** **All three outcomes recorded IN ADVANCE as legitimate findings:** reproduced GREEN (no historical issue) · reproduced RED (the ARB investigates the acceptance evidence) · **reproduction impossible (historical uncertainty remains — itself a finding, not a failed task)**. | A read-only investigation track is authorized; R-43 is NOT amended and no claim is made about its evidence in either direction **RECORDING CORRECTION A3 (R-62, 2026-08-01 — annotation; decision unchanged): category reads EXECUTION Governance, not Delivery.** This ruling **authorized an engineering activity** (the historical reproduction), and *engineering is authorized to begin* is the **Execution** transition. **Semantics unchanged — the authorization and its constraints stand exactly as issued.** |
| R-51 | 2026-08-01 | **WP-7 SLICE 7B PREPARATION AUTHORIZED — Execution Governance · Authorization · ARB.** Guard *Slice 7A accepted* satisfied by R-48. Reason: 7A is complete within its authorized scope, and 7B proceeds independently while any WP-6 investigation or remediation remains a **separate governance track**. **Scope: preparation of the Slice 7B Authorization Package is authorized; implementation is NOT begun by this ruling** — authorization permits execution *within* 7B's approved scope (the `EvidencePreservationWindow` value object and the anchor), it does not itself perform it. **Slice 7C remains unauthorized.** Dependency recorded: had R-48 been a rejection, this ruling would have been **unreachable, not merely deferred**. | 7B preparation authorized; 7C unchanged and still unauthorized |
| R-52 | 2026-08-01 | **WP-6 REMEDIATION WORK PACKAGE OPENED — Delivery Governance · Approval · ARB.** Decision 5 of the final governance session, on the reproduced evidence of R-50 (**REPRODUCED RED** — six attributes identical to the current failure; conclusion bound to the documented protocol). **Scope:** repair `AdjudicationProcessManagerTest` against WP-6's 5-argument constructor, restore the 4 dead tests, unblock the merge gate. **Reason:** the gate is red at closure AND today; no slice can demonstrate a complete gate until this is done; the track stays **independent of WP-7**. **The package is OPENED, not delivered — no repair has been performed by this ruling.** | WP-6 remediation DEFERRED -> OPEN (separate track, not started) ⚠️ **SAME CLASSIFICATION QUESTION AS R-60, FLAGGED NOT CORRECTED (2026-08-01):** this ruling also OPENED a work package and is also typed *Delivery Governance · Approval*. **By the correction applied to R-60, opening is a PLANNING act.** The issuing authority addressed R-60 only, so R-52 is left as issued and the inconsistency is recorded rather than silently normalised — **whether to correct it is the ARB's.** |
| R-53 | 2026-08-01 | **GOVERNANCE NOTE ATTACHED TO R-43 — Delivery Governance · Ratification · ARB. THIS IS AN ANNOTATION, NOT AN AMENDMENT; R-43's decision text is unchanged and the WP-6 acceptance STANDS.** Note as adopted: *reproduced evidence from the historical reproduction (R-50) establishes that, under the documented reproduction protocol at the WP-6 closure commit `22d604844`, the GreenfieldCore suite terminated with a fatal error and the merge gate would not have completed successfully under those reproduced conditions. This note does not amend R-43, nor does it determine whether the gate was ACTUALLY EXECUTED at the time of acceptance. Whether the acceptance evidence was **FALSE** (the gate was run and misreported) or **UNSUPPORTED** (the gate was not run) remains **UNDETERMINED from repository evidence** and is not resolved by this note.* **Mechanics (ES-004.3):** the note is carried by this row, and R-43 receives a minimal forward-pointer status annotation — permitted where decision text and history are not. This also closes, for this case, the recorded weakness that supersession and qualification are **forward-linked only**, leaving a reader at the old ruling with no pointer onward. | R-43 annotated and discoverable from itself; acceptance unchanged |
| R-54 | 2026-08-01 | **SLICE 7B PREPARATION ACTIVE — Execution Governance · Authorization · ARB.** Guards: Slice 7A accepted (R-48) · preparation previously authorized (R-51) · **WP-6 remediation is a separate track and does not block 7B**. **Scope:** the Slice 7B Authorization Package — scope statement, evidence trace, keystones, decision templates, verification strategy. **Constraint: PREPARATION ONLY — no implementation; 7C excluded.** **This does NOT re-authorize what R-51 already granted — it moves the activity from AUTHORIZED to ACTIVE: R-51 granted permission, R-54 starts the clock.** Implementation of 7B still requires its own EP-01/ARB passage; **preparing the package is not approving it.** | Slice 7B preparation AUTHORIZED (not started) -> ACTIVE |
| R-55 | 2026-08-01 | **WP-6 REMEDIATION ACCEPTED AND CLOSED — Delivery Governance · Acceptance · ARB.** All eight acceptance criteria satisfied: authorized scope completed · **only the authorized file modified** · **production code unchanged** · constructor mismatch repaired · **4 dead tests restored** (4 tests / 7 assertions) · **`composer merge-gate` PASSES** (255 tests / 650 assertions / 0 failures / 0 errors — the first complete gate since 2026-07-31) · track kept **independent of WP-7** · no governance artifact modified beyond authorization. **F-WP6R-1 (99 risky tests spanning every context, zero in the repaired file) is assessed SEPARATELY: outside R-52's scope, NOT an acceptance blocker, NOT treated as repaired, and requiring its own authorization if work is desired.** *(Recording note, not part of this ruling: R-55 is typed **Acceptance** while R-48 — which accepted slice 7A — was typed **Approval**. The distinction is defensible, an approval admits a proposal while an acceptance admits delivered work, but the two now differ; R-48 is NOT amended and normalising the vocabulary is the ARB's to decide.)* | WP-6 Remediation DELIVERED -> ACCEPTED & CLOSED; queue item 8 closed; new queue item 11 recorded (open an F-WP6R-1 package?) |
| R-56 | 2026-08-01 | **SLICE 7B PLANNING APPROVED WITH REQUIRED CORRECTIONS — Planning Governance · Approval · ARB (EP-01 review).** Evidence: the Slice 7B Authorization Package — scope, traceability, keystones K1..K10, verification strategy, rollback, blank decision templates. Reason: the package is complete and internally consistent, **its open items are explicit rather than hidden**, and **no new architectural decision is created** — K1..K8 and K10 all trace to existing authority, with **K9 open only as to PLACEMENT, not substance**. **TWO CORRECTIONS REQUIRED BEFORE RED, NOT BEFORE APPROVAL: P7B-1** — correct the approved plan's 7B row to R-44's mechanism (Election declares its OWN port, not *'consuming Adjudication's existing port'*, which is the TP-1 violation R-44 rejected); **owner ARB/Decision Authority, since amending an approved plan is a governance act** · **P7B-2** — settle K9's home (Value Object vs application service); **owner engineering, an allocation within the frozen architecture rather than a new architectural decision.** The **EPW anchor remains an open BUSINESS decision and remains NON-BLOCKING** (fail-closed decides nothing on Q-2's behalf). *(Recording note, not part of this ruling: R-56 approves the PLAN. **Execution of 7B is a SEPARATE act and has NOT been issued** — mirroring 7A exactly, where R-46 approved the plan and R-47 authorized execution.)* | Slice 7B plan APPROVED (with corrections); **execution still NOT authorized**; new queue items 12 (P7B-1 correction) and 13 (authorize 7B execution) |
| R-57 | 2026-08-01 | **SLICE 7B PLAN CORRECTED (P7B-1) — Planning Governance · Approval · ARB** *(queue item 12)*. The approved plan's 7B **Objective** and **Acceptance** rows carried a mechanism **R-44 superseded** — *"consuming `AdjudicationDurations` for MAD"* and *"MAD comes from the **existing** port"* — which instructed the **direct cross-context import TP-1 forbids and Deptrac fails**. Corrected to **MAD obtained through Election's own `EvidencePreservationDurations` port**. **ONLY THE MECHANISM SENTENCE CHANGED: the invariant that MAD keeps exactly one canonical home is untouched, and no other slice text was altered.** *(Provenance: surfaced during 7B planning as P7B-1 and deliberately NOT edited by engineering, because amending an approved plan is a governance act.)* | The approved plan no longer instructs a TP-1 violation; queue item 12 closed |
| R-58 | 2026-08-01 | **SLICE 7B EXECUTION AUTHORIZED — Execution Governance · Authorization · ARB** *(queue item 13)*. Guards verified: planning approved (R-56) · plan corrected (R-57) · **P7B-2 settled** (the absent-anchor fallback belongs to the application service) · **P7B-3 withdrawn** (it was an inference; §2 lists two application-orchestration responsibilities and 7B's objective needs the first, so **K9 stays in 7B**) · no engineering allocation outstanding · **zero architectural gates** · merge gate green (R-55). **Engineering is authorized to begin Slice 7B RED on keystones K1–K10, with K9 inside 7B. No additional governance act is required before RED.** **Slice 7C remains unauthorized; F-WP6R-1 remains a separate, unauthorized concern.** *(Two acts, two rulings — mirroring R-46/R-47 for 7A: R-56 approved the plan, R-58 authorizes execution.)* | Slice 7B: planned -> **RED AUTHORIZED**; queue item 13 closed |
| R-59 | 2026-08-01 | **SLICE 7B ACCEPTED AND CLOSED — Delivery Governance · Acceptance · ARB** *(queue item 9)*. Evidence: RED report · GREEN report · acceptance review · merge-gate output. Criteria 1-4 satisfied: **scope** (6 files, all authorized; 7C surface untouched; no config key; **no DI binding — the port was already bound**) · **verification** (11/11 keystones · Architecture 149 · **Deptrac 0, `deptrac.yaml` unmodified** · **PHPStan max clean, zero suppressions, no baseline entry** · regression 266/665, 0 failures · **MERGE GATE PASS**; no test modified during GREEN) · **architecture** (every approved decision conforms) · **Definition of Done** (developer guide filed). **The `EvidencePreservationWindow` enters the accepted architecture baseline.** **Criterion 5 — the INTERIM anchor — is recorded as EXPLICIT ARCHITECTURAL DEBT rather than treated as a blocker:** `anchorOf()`'s ordering (`results_published_at` -> `end_date` -> `archived_at`) is **executable business behaviour no authority chose** — the AP-1 defect class in a new form, an invented **rule** rather than an invented **value**. **Owner: Q-2.** When Q-2 rules, the method collapses to one named field. **Rationale for accepting rather than blocking: the authorization did not prohibit a temporary anchor policy, and acceptance criteria are not redefined retroactively after GREEN.** *(Recorded, not silent — silent architectural debt is ungoverned debt.)* | Slice 7B ACCEPTED & CLOSED; queue item 9 closed **WORDING CORRECTED 2026-08-01 by the issuing authority (annotation; the DECISION is unchanged): read 'accepted IMPLEMENTATION baseline', not 'accepted ARCHITECTURE baseline'.** *Architecture baseline* denotes architectural DECISIONS; what was accepted here is a **realization**. **Q-2 still owns architectural evolution, so the wording must not suggest the design itself is frozen.** |
| R-60 | 2026-08-01 | **WP-7B-R1 OPENED — Interim Anchor Extraction — Delivery Governance · Approval · ARB.** A follow-up work package, **separate from Slice 7B and not a reopening of it**: extract the interim anchor into an **`EvidenceAnchorResolver` port with a `TemporaryDefaultAnchorResolver`** implementation, **behaviour unchanged**, before or alongside the Q-2 implementation. **Effect: Q-2's eventual ruling then SWAPS AN IMPLEMENTATION rather than EDITING SERVICE BEHAVIOUR**, and the interim policy becomes a named, replaceable thing instead of a private method. **Opened, not delivered.** *(Recording note: the issuing text placed this motion under a heading reading "Recording Notes -- Not Part of Ruling", while its own handover statement and governance queue both declare the package OPEN. **A recording note cannot open a work package** — that is the ruling/note distinction this register enforces — so it is filed as a ruling in line with the operative text. The discrepancy is recorded rather than silently resolved.)* | WP-7B-R1 OPEN (not started); queue item 12 recorded **CLASSIFICATION CORRECTED 2026-08-01 by the issuing authority (annotation; the DECISION is unchanged): the category is PLANNING GOVERNANCE, not Delivery.** **Opening/approving a work package is a PLANNING act; DELIVERY governance is the later act of tracking and ACCEPTING it.** Conflating them would make *opening* and *accepting* the same category. |
| R-61 | 2026-08-01 | **GOVERNANCE VALIDATION ACCEPTED — Delivery Governance · Acceptance · ARB.** The Governance Meta-Model Operational Validation is accepted as the **authoritative assessment of the current governance model**. All 17 issued rulings (R-43..R-60) were classified against `lifecycle transition -> category -> authority -> artifact`: **11 clean · 2 recording errors · 2 type inconsistencies · 3 unclassifiable · 1 weak.** Accepted because **the validation produced evidence without modifying the canonical model**. | The validation is the authoritative assessment of the model |
| R-62 | 2026-08-01 | **RECORDING CORRECTIONS A1 AND A3 APPROVED — Delivery Governance · Ratification · ARB.** **A1:** the transition *completed work is accepted and closed* is typed **Acceptance** (R-55, R-59), but R-43 and R-48 typed it **Approval** — one transition carried two types. **A3:** R-50 **authorized an engineering activity** yet was filed **Delivery**, where the model says **Execution**. **Both are recording only: no governance semantics change, no ruling is amended, and no methodology changes.** Applied as **annotations** to R-43, R-48 and R-50 per ES-004.3 — decision text is never rewritten. | Register annotated; the model untouched |
| R-63 | 2026-08-01 | **GOVERNANCE SCOPE DECLARED — Architecture Governance · Approval · ARB. The canonical governance model governs the WORK-PACKAGE LIFECYCLE.** Grounds: operational validation shows every supported lifecycle transition — *opened · authorized · accepted & closed · design decided* — classifies consistently within that scope. **Consequence: acts operating UPON GOVERNANCE ARTIFACTS (R-49 choosing a remediation strategy · R-53 annotating a ruling · R-57 correcting an approved plan) are explicitly OUTSIDE the current canonical scope.** **They cease to be defects and become out-of-scope**; the validation demonstrated a **scope boundary, not a model defect**, and the boundary is now declared rather than merely implied. | Scope declared; three previously-unclassifiable rulings reclassified as out-of-scope rather than defective |
| R-64 | 2026-08-01 | **META-GOVERNANCE EXTENSION DEFERRED — Architecture Governance · Approval · ARB.** No methodology extension is authorized and **no new governance categories are introduced**. Reason: **a model should be as simple as the problem it currently solves.** The current model solves the work-package lifecycle; *governance acting upon governance artifacts* may warrant a **second bounded context**, which is a materially larger decision than adding a category and requires **sustained** operational demand, not a single validation. **The validation stands as evidence for that future decision without forcing it today.** | Methodology unchanged; meta-governance deferred pending sustained evidence |
| R-65 | 2026-08-01 | **SLICE 7C AUTHORIZED — Execution Governance · Authorization · ARB.** **Slice 7C is authorized to implement the decision service that determines whether evidence may be deleted**, consuming only the **Evidence Preservation Window Resolution** capability. Engineering shall implement Slice 7C subject to the **Architectural Constraints** recorded in the authorization instrument (`engineering/verification/reports/2026-08-01-slice-7c-authorization.md`). **Slice 7C shall not alter how deletion is performed** (folder traversal, filesystem removal, reporting) — **the guard gates the deletion decision**. **Slice 7C shall not absorb, implement or anticipate WP-7B-R1** (independent refinement under R-60). **Authorization covers implementation and acceptance only; release requires a named announcement owner** — not on the engineering critical path. **RED includes amending `tests/Feature/Audit/AuditCleanupTest.php`, limited to what the acceptance criterion requires** — no assertion weakened, no test deleted. **Sequence: RED → GREEN → VERIFY → ACCEPT; WP-7 closes on acceptance.** **Scope of authority: Execution Governance only — this ruling does not alter architecture, amend strategic decisions, modify bounded-context ownership, redefine capabilities, or approve future work packages.** | Slice 7C AUTHORIZED; engineering may begin RED; queue 10 closed |
| R-66 | 2026-08-02 | **SLICE 7C ACCEPTED — Delivery Governance · Acceptance · ARB.** The retention guard is accepted into the implementation baseline: **`audit:cleanup` refuses to delete a folder whose election's Evidence Preservation Window is still open**, deletion resumes once the window closes, and an **unresolvable folder→election mapping is preserved**. **All eight R-65 requirements satisfied; how deletion is performed is unchanged.** Architecture preserved: no change to bounded-context ownership, capability ownership, the context map, Published Language, Ubiquitous Language or strategic invariants; **no aggregate, entity, value object, repository, domain service or port introduced**; dependency direction Infrastructure → Application → Domain intact. Evidence: `2026-08-01-slice-7c-completion-evidence.md` · review: `2026-08-01-slice-7c-acceptance-review.md` (Rev 2). **Two items carried, neither blocking acceptance: (a) the release announcement owner (C-2) is unnamed and blocks RELEASE, 7C being the first externally visible behaviour change; (b) the merge gate's `GreenfieldCore` suite does not execute `tests/Feature/Audit/`, a pre-existing property of the verification architecture, unchanged by this slice and owned elsewhere.** **CONSEQUENCE: WP-7 — Retention Alignment — IS CLOSED. WP-7B-R1 opens as an independent refinement under R-60. WP-8 requires formal definition and authorization before it begins.** | Slice 7C accepted; **WP-7 CLOSED**; WP-7B-R1 open; release gated on C-2 |
| R-67 | 2026-08-02 | **WP-3A ACCEPTED — Delivery Governance · Acceptance · ARB.** **`ChallengeRouted` is accepted into the implementation baseline as PUBLISHED LANGUAGE**, in both halves that status requires: **publication** (`ChallengeOutboxAdapter::writeRouted()`, schema v1) and **registration** (`ChallengeRoutedHydrator`, registered in `ContestationServiceProvider::boot()`). **The domain event itself is unmodified — WP-3A publishes an event that already existed.** Architecture preserved: no change to bounded-context ownership, capability ownership, Ubiquitous Language or strategic invariants; **no aggregate, entity, value object, repository or domain service introduced**; Deptrac 0 violations. **Definition of Done complete: RED · GREEN 11/11 · `composer merge-gate` PASS · developer guide · TRIPLE QUALIFICATION (Architecture · DDD · Trustworthiness, performed 2026-08-02, the gate WP-1 and WP-2 also cleared before acceptance).** Evidence re-verified on the day of acceptance, not quoted from July. Review: `engineering/verification/reports/2026-08-02-wp3a-acceptance-review.md`. **ACCEPTED WITH NO CONDITION ATTACHED. The frozen-catalog condition contemplated earlier is WITHDRAWN: `Canonical_Event_Catalog_v1.0.md` has no `visibility` column — `internal` is a value of its SECURITY column — and Round50-05, the authoritative contract, lists `ChallengeRaised…Resolved` with Consumers (allowed) `AdjudicationService · Audit · NOT Voting` and payload `challengeId · routedTo · at`. The frozen artifact specifies this implementation rather than contradicting it.** **THREE ITEMS RECORDED, NONE BLOCKING: (a) the catalog records NO PUBLISHED-LANGUAGE STATUS FOR ANY EVENT, so ADR-T21's declaration has no counterpart in the registry — a registry gap across the whole catalog, referred to documentation governance; (b) WP-3A's AUTHORIZATION is recorded in its plan and session log but NOT IN THIS REGISTER — a recording gap of the class R-62 corrected for R-43/R-48, to be reconciled; (c) contract clause R-2 (no consumer imports another context's Infrastructure) is machine-enforced by Deptrac and STATED IN NO ADR.** **CONSEQUENCE: WP-3A IS CLOSED. WP-3B remains DEFERRED, pending a routing application service. WP-4's acceptance may now be deliberated — it consumes this published language — and requires the Board to NAME which of roadmap §WP-4's four items it accepts.** | WP-3A accepted and **CLOSED**; WP-3B still deferred; **WP-4 acceptance unblocked** |
| R-68 | 2026-08-02 | **WP-4 SUBDIVIDED INTO WP-4A…WP-4D — Architecture Governance · Approval · ARB.** **The ARB explicitly subdivides WP-4 into WP-4A–WP-4D because implementation has delivered an INDEPENDENTLY COMPLETE CAPABILITY that satisfies the slice acceptance criteria, while the remaining roadmap items remain dependent on later work. This subdivision PRESERVES ROADMAP TRACEABILITY and DOES NOT ALTER STRATEGIC ARCHITECTURE.** **WP-4A — APM inbox wiring:** the `(Adjudication, ChallengeRouted)` inbox handler and its consumer-side registration. **WP-4B — crash-safe conclude→issue seam** (PM conclusion txn → issuance txn, INV-B1-bridged crash recovery). **WP-4C — `AdjudicationFailureDeclared`** + integration counterpart + hydrator + catalog entry. **WP-4D — authority-decision intake port + interim administrative adapter.** **WP-4B, WP-4C and WP-4D each depend on the authority-decision intake, a recorded external; the deferral was flagged in the plan BEFORE RED, not discovered mid-slice.** **Method follows the established precedent: WP-3 → WP-3A/WP-3B (2026-07-30) and WP-7 → 7A/7B/7C (R-59 · R-65 · R-66). In both cases the split was an EXPLICIT ARB ACT NAMING THE SUB-SLICES — never an implicit narrowing of the parent — and the parent closed only when every sub-slice was disposed of.** **Grounds: the roadmap uses *slice* and *work package* as the same unit** (*“triple qualification per slice”* · *“the one-slice-one-review cadence”* · *“STOP for slice acceptance”* · *“each subsequent WP opens only on its predecessor's ARB slice acceptance”*), and **§8's Phase DoD is *“all eight WPs accepted”*. No rule permits a work package to be partially accepted under its own name**, so acceptance of the executed work requires this subdivision first. **CONSEQUENCE: §WP-4 REMAINS OPEN and closes only when WP-4B, WP-4C and WP-4D are disposed of.** | WP-4 subdivided; **§WP-4 stays OPEN**; WP-4A becomes acceptable |
| R-69 | 2026-08-02 | **WP-4A ACCEPTED — Delivery Governance · Acceptance · ARB.** **The APM inbox wiring is accepted into the implementation baseline: `ChallengeRoutedReactionHandler` and its consumer-side registration by `(consumerContext='Adjudication', eventType='ChallengeRouted')`. THE CORRECTION LOOP'S HEAD FIRES.** Implementation `c3165ee88` (2026-07-31). **`handle()` is one statement — reconstruct Adjudication's own `ChallengeRef`, call `openFor()`. Two components, no more.** Architecture preserved: **no change to bounded-context ownership, capability ownership, the context map, Published Language, Ubiquitous Language or strategic invariants**; **no aggregate, entity, value object, repository or port introduced** — the tactical model is consumed, not extended; **PM-1 owns the rule including its own idempotency, the handler owns only the wiring.** **Definition of Done complete: RED accepted · GREEN 6 tests / 10 assertions, first run, no iteration · `composer merge-gate` PASS (Deptrac 0 violations · greenfield PHPStan no errors) · developer guide `developer_guide/adjudication/04_challenge_routed_consumption.md` · TRIPLE QUALIFICATION (Architecture · DDD · Trustworthiness) performed 2026-08-02.** Evidence re-verified on the day of acceptance. Review: `engineering/verification/reports/2026-08-02-wp4-acceptance-review.md`. **THE LOAD-BEARING ARCHITECTURAL RESULT IS AN ABSENCE: importing Contestation's hydrator would have been the codebase's FIRST CROSS-CONTEXT INFRASTRUCTURE DEPENDENCY. Deptrac enforces contract R-2; NO ADR STATES IT — a recorded gap, not a defect of this slice.** **SIX DEFENDED ABSENCES accepted as designed:** no outcome translator (G-2, independently confirmed by evidence — the house translator classifies business conditions and this handler raises none) · no `RoutedTo` VO · no Domain import · no hydrator import · no provenance minting · no bespoke `PermanentInboxFailure`. **FOUR ITEMS RECORDED, NONE BLOCKING: (a) D-1 — the integration contract §5 specified provenance via `fromConsumed()`; OBSERVED: NO PROVENANCE CALL AT ALL, because this slice publishes nothing. The R-6 invariant is unharmed; an EXTRAPOLATED SPEC ROW over-reached, and the contract, not the code, is corrected · (b) AD-007, relay failure path × single-transaction harness — environment-specific, production code correctly unchanged · (c) AD-008, 17 pre-existing Membership failures, git-stash-verified · (d) WP-4A's AUTHORIZATION is recorded in its plan and session log but NOT IN THIS REGISTER — an R-62-class recording gap, to be reconciled.** **CONSEQUENCE: WP-4A IS CLOSED. §WP-4 REMAINS OPEN on WP-4B, WP-4C and WP-4D (R-68). WP-8 stays blocked until §WP-4 closes.** | WP-4A accepted and **CLOSED**; §WP-4 still open on 4B/4C/4D; WP-8 still blocked |
| R-70 | 2026-08-02 | **WP-7B-R1 AUTHORIZED TO IMPLEMENT — Execution Governance · Authorization · ARB.** **WP-7B-R1 is authorized to extract the interim anchor resolution into an `EvidenceAnchorResolver` port and a `TemporaryDefaultAnchorResolver` implementation. NO EXTERNALLY OBSERVABLE BEHAVIOUR SHALL CHANGE. Acceptance requires the existing Evidence Preservation test suite and `AuditCleanupTest` to remain GREEN WITHOUT MODIFICATION.** **SCOPE:** the extraction target is `ResolvesEvidencePreservationWindow::anchorOf()` — the candidate order `results_published_at` → `end_date` → `archived_at`, and the *no candidate ⇒ null* result, move behind the port **unchanged**. `EvidencePreservationWindow` (Domain), `EvidencePreservationDurations` (port) and the *absent anchor ⇒ window reported OPEN* rule are **not touched**. **ACCEPTANCE EVIDENCE:** `EvidencePreservationWindowResolutionTest` · `EvidencePreservationDurationsTest` · `EvidencePreservationWindowTest` · `AuditCleanupTest` all green **unmodified**, plus RED→GREEN for the new seam · `composer merge-gate` PASS · triple qualification · developer guide · **STOP for ARB slice acceptance.** **⚠️ MERGE-GATE PASS ALONE DOES NOT EVIDENCE BEHAVIOUR PRESERVATION: the gate's `GreenfieldCore` suite does not execute `tests/Feature/Audit/`, so `AuditCleanupTest` MUST BE RUN AND REPORTED DIRECTLY** (a property of the verification architecture, recorded at R-66). **GROUNDS:** the authorization-readiness review returned **READY** on all six checks — scope explicit · **no dependency on open work** (the ownership and integration-path questions belong to Adjudication and Contestation; this work touches Election and Audit/Retention only) · **no Strategic DDD change on any of the six tests** · tactical readiness real (the adapter shape is fixed by the `EvidencePreservationDurations` → `ConfiguredEvidencePreservationDurations` precedent and Deptrac's approved model) · governance ready · risk low. **Q-2 REMAINS OPEN AND IS THE REASON FOR THE WORK, NOT A BLOCKER TO IT: the extraction exists so that Q-2's eventual ruling SWAPS AN IMPLEMENTATION rather than EDITS SERVICE BEHAVIOUR. Waiting for Q-2 would invert R-60's own rationale.** **RECORDED, NOT URGED: this authorization does NOT advance §WP-4 and does not move WP-8 closer. It is PARALLEL WORK, not a substitute for resolving WP-4B/4C/4D, which remain the critical path.** | WP-7B-R1 **authorized to implement**; behaviour-preservation is the acceptance boundary; §WP-4 unaffected |
| R-71 | 2026-08-02 | **WP-7B-R1 ACCEPTED — Delivery Governance · Acceptance · ARB.** **The interim Evidence Preservation Window anchor is accepted into the implementation baseline as a named, replaceable seam: the `EvidenceAnchorResolver` port with `TemporaryDefaultAnchorResolver` behind it, bound in `ElectionServiceProvider`.** `ResolvesEvidencePreservationWindow` now obtains the anchor through the port and its private `anchorOf()` is deleted. **The rule moved unchanged** — `results_published_at` → `end_date` → `archived_at`, first match wins, no candidate ⇒ `null`. **R-70's ACCEPTANCE BOUNDARY IS SATISFIED: no externally observable behaviour changed, evidenced by four pre-existing suites green and UNMODIFIED** — `EvidencePreservationWindowResolutionTest` · `EvidencePreservationDurationsTest` · `EvidencePreservationWindowTest` · **`AuditCleanupTest`, run directly because the merge gate does not execute `tests/Feature/Audit/`.** RED genuine (4 failed, class absent) · GREEN 33/51 first run · `composer merge-gate` **PASS** (270 · 671) · developer guide shipped. Evidence: `engineering/verification/reports/2026-08-02-wp7b-r1-completion-evidence.md`. **ARCHITECTURE PRESERVED: no new domain concept · no aggregate-boundary change · no Ubiquitous Language change (the port NAMES AN EXISTING CONCEPT) · no ownership change · no published-language change · no context-map change. The consumer declares its own port, as `EvidencePreservationDurations` already did — no cross-context dependency is created.** **THE RESPONSIBILITY SPLIT IS ACCEPTED AS DESIGNED: the resolver REPORTS a fact and may report ABSENCE; the SERVICE decides what absence means and fails closed (AP-1, P7B-2). Q-2 IS NOT PRE-EMPTED — no test asserts which candidate is correct.** **ONE ITEM CARRIED, NOT BLOCKING: risky notices rose 101 → 105 with the four new tests demonstrably NOT among the risky entries, so the cause is unestablished. Recorded as ENG-012 rather than resolved. IT WAS DISCLOSED, NOT SMOOTHED OVER, AND THAT IS WHY IT DOES NOT BLOCK.** **RECORDING CORRECTION ADOPTED WITH THIS RULING: engineering PRODUCES THE EVIDENCE SUPPORTING triple qualification; THE QUALIFICATION ITSELF BELONGS TO THE ACCEPTANCE PACKAGE. Engineering does not self-certify it.** **CONSEQUENCE: WP-7B-R1 IS CLOSED. IT DOES NOT ADVANCE §WP-4 AND DOES NOT MOVE WP-8 CLOSER — it was parallel work and the critical path is unchanged: dispose of WP-3B, take the WP-4B/4C/4D ownership and integration-path decisions, implement WP-4B, close §WP-4, then WP-8 planning.** | WP-7B-R1 accepted and **CLOSED**; ENG-012 opened; critical path unchanged |
| R-72 | 2026-08-02 | **WP-4B AUTHORIZED — Execution Governance · Authorization · ARB — SUBJECT TO A PROVISO THAT IS NOT YET SATISFIED.** **WP-4B (the crash-safe conclude→issue seam) is authorized as the next executable slice of §WP-4, *provided its authorization package demonstrates executable scope and no unresolved governance blockers remain*. THAT PROVISO IS NOT MET TODAY, SO IMPLEMENTATION DOES NOT BEGIN: three ARB decisions and one scope ambiguity stand between this ruling and a drawable RED boundary** — (a) **ownership of `Jurisdiction`** · (b) the **integration paths** for `EvidenceEnvelopeRef` and `ContestedOutcomeRef` · (c) **WP-4B's scope ambiguity**: is PM-6's issuance-confirmation half inside this slice? *(Evidence: `2026-08-02-wp4b-engineering-discovery.md` §2 · §6 · `2026-08-02-issuance-input-ownership-decision-package.md`.)* **WP-4C AND WP-4D ARE EXPRESSLY NOT AUTHORIZED. Each is to be authorized from IMPLEMENTATION EVIDENCE rather than roadmap intent, after its predecessor is accepted.** **§WP-4 CLOSURE IS EXPRESSLY NOT AUTHORIZED: closure is an ACCEPTANCE OUTCOME, NOT AN AUTHORIZATION DECISION.** **GROUNDS FOR THE SLICE-AT-A-TIME FORM: roadmap structure and governance acts are different things. Accepting WP-4A did not accept §WP-4 (R-69); authorizing 4B, 4C and 4D together would likewise authorize work whose final shape may legitimately change once 4B's implementation produces evidence.** **THE DEFERRED MODELLING QUESTION IS NOT A PRECONDITION OF THIS RULING: the conversation decision is DEFERRABLE TODAY and MANDATORY BEFORE WP-8, so WP-4B does not wait on it.** **CONSEQUENCE: no RED begins. The next acts are the three decisions and the scope ruling; when they land, engineering proceeds RED → GREEN → VERIFY → acceptance evidence, and WP-4C is considered only after WP-4B is accepted.** | WP-4B authorized **in principle**; **execution contingent and the contingency is unmet**; 4C, 4D and §WP-4 closure **not authorized** |
| R-73 | 2026-08-03 | **JURISDICTION — AUTHORITATIVE SOURCE ADOPTED: THE DECIDING AUTHORITY — Architecture Governance · Adoption · ARB.** **`Jurisdiction` is produced by the Governance-delegated deciding authority and reaches `IssueDeterminationCommand` CARRIED ON THE AUTHORITY'S DECISION, via the Q-1 crossing.** **Rationale: the Board recorded a selection and did not state reasons; none are invented here.** The evidence basis for the adopted option, from the prepared package: Adjudication holds only a bare string with **no producer anywhere in the context** — command, aggregate, `DeterminationIssued`, hydrator and mapper are all consumers; **ADR-T23/K1 place what the authority DECIDES with the authority**. **SCOPE: the source and path only. No ADR is amended and `Jurisdiction`'s value is not decided.** **CONSEQUENCES: `receiveRulingDecision()` MUST WIDEN to accept it — today six parameters, and the whole of what the APM accepts from an authority. THAT SHAPE IS WP-4D's INTAKE PORT, so this decision PARTLY DEFINES WP-4D AND PLACES IT ON WP-4B'S PATH.** **GOVERNING MODEL: authority-carried; Adjudication receives, never derives.** | `Jurisdiction` authority-carried; **`receiveRulingDecision()` widens**; WP-4D partly defined and now upstream of WP-4B |
| R-74 | 2026-08-03 | **EVIDENCEENVELOPEREF — AUTHORITATIVE PATH ADOPTED: AN EVIDENCE INTEGRATION EVENT — Architecture Governance · Adoption · ARB.** **`EvidenceEnvelopeRef` is owned by the EVIDENCE bounded context and reaches Adjudication as an INTEGRATION EVENT.** **Rationale: the Board recorded a selection and did not state reasons; none are invented here.** The evidence basis for the adopted option, from the prepared package: the VO's own docblock names Evidence as owner (*“the EvidenceEnvelope's hash, owned by the Evidence context. Reference only (TP-1)”*), and Round50-05 and the canonical catalog both specify `EvidenceRecorded` carrying `envelopeHash`. **SCOPE: source and path only.** **⛔ CONSEQUENCE THE BOARD SHOULD SEE RECORDED: `app/Contexts/` CONTAINS NO `Evidence/` DIRECTORY. The adopted owner is SPECIFIED AND UNBUILT, and it is NOT ONE OF THE EIGHT ROADMAP WORK PACKAGES. Under this model WP-4B cannot obtain the value until Evidence exists or an interim stand-in is authorized — NEITHER IS DECIDED HERE.** **ALSO SETTLED BY IMPLICATION: H2/H3 are rejected — the field is neither redundant with `EvidenceSet` nor a projection over it.** **GOVERNING MODEL: Evidence produces; Adjudication consumes by event; reference only, never content (TP-1).** | Evidence is the authoritative owner; **the owner has no code home**, so WP-4B is blocked on it |
| R-75 | 2026-08-03 | **CONTESTEDOUTCOMEREF — AUTHORITATIVE PATH ADOPTED: PUBLISH `ChallengeRaised` — Architecture Governance · Adoption · ARB.** **Contestation owns the concept (ADR-UL-01) and shall PUBLISH `ChallengeRaised`, which already carries the value in its constructor; Adjudication reconstructs its own local VO from primitives (ADR-T16).** **Rationale: the Board recorded a selection and did not state reasons; none are invented here.** The evidence basis for the adopted option, from the prepared package: the event already exists and already holds the value; **the canonical catalog already lists `ChallengeRaised` with producer Contestation**; and this route leaves `ChallengeRouted`'s payload CLOSED. **✅ NOTABLE: THE ALTERNATIVE WOULD HAVE REOPENED THE PUBLISHED LANGUAGE ACCEPTED UNDER R-67 THE PREVIOUS DAY. The adopted path does not.** **CONSEQUENCES: `ChallengeOutboxAdapter` maps only Routed/Adjudicated/Resolved today, so publication, a hydrator, the catalog visibility and an Adjudication-side consumer must all be built. NONE EXISTS. This is CONTESTATION-SIDE WORK, allocated to no current work package.** **GOVERNING MODEL: Contestation publishes; Adjudication consumes and reconstructs locally; only primitives cross.** | `ChallengeRaised` becomes published language; **R-67's payload untouched**; unallocated Contestation-side work created. **⛔ ANNOTATION 2026-08-03 (added, not rewritten — the DECISION stands as issued): THE ADOPTED PATH IS PROHIBITED BY ACCEPTED ARCHITECTURE. ADR-UL-01 and ADR-PL-01 classify `ChallengeRaised` as an ***internal* domain event**; WP-5's accepted plan calls it *“intentionally unpublished, not merely currently unmapped”*; and `ChallengeRaisePathTest.php:153` asserts exactly one outbox row. **The ownership finding is unaffected — Contestation does own the concept — but the PATH requires amending two ADRs, overturning an accepted keystone and modifying a passing test.** **The option description this Board decided on did not disclose that; the omission was the Recording Architect's.** See `engineering/verification/reports/2026-08-03-contestedoutcomeref-completeness-and-allocation.md`.** |
| R-76 | 2026-08-03 | **WP-4B SCOPE CONFIRMED: THE REQUEST PATH ONLY — Execution Governance · Adoption · ARB.** **WP-4B is the conclude→issuance-request path and its crash-safe redrive. PM-6's ISSUANCE-CONFIRMATION half — observing `DeterminationIssued`, closing PM-6's loop, and the INV-B1-refusal reconciliation of EPIC-004K §12 — IS EXPRESSLY OUTSIDE WP-4B.** **RATIONALE, IN THE BOARD'S OWN WORDS: *WP-4B is scoped to the issuance request path, including crash-safe redrive. Confirmation of issuance by observing `DeterminationIssued` remains a separate concern and shall be implemented in a subsequent work package. This preserves the approved work package boundary, limits WP-4B to a single cohesive responsibility, and avoids expanding the slice during implementation.* **THE BOARD CHARACTERISED THIS AS A WORK-PACKAGE BOUNDARY DECISION RATHER THAN A TECHNICAL ONE, AND AS A PROCESS-PHASE SEPARATION RATHER THAN A BOUNDED-CONTEXT ONE: REQUESTING ISSUANCE AND CONFIRMING IT OCCURRED ARE DIFFERENT RESPONSIBILITIES, AND EACH SLICE SHOULD CARRY ONE TESTABLE RESPONSIBILITY.** Corroborating evidence: roadmap §WP-4's row names the issuance request path and does not mention confirmation; EPIC-004K §8/§9 list confirmation as a separate external trigger. **CONSEQUENCES: the scope ambiguity recorded in the WP-4B engineering discovery §2 IS CLOSED. RED carries the roadmap keystone — CONCLUDED-BUT-UNISSUED → REDRIVE → EXACTLY ONE DETERMINATION — and not the confirmation half. The confirmation responsibility remains ALLOCATED TO NO WORK PACKAGE and is recorded as such.** **GOVERNING MODEL: WP-4B requests issuance; it does not observe the outcome.** | WP-4B scope closed at the request path; confirmation unallocated |
| R-77 | 2026-08-03 | **THE INCONSISTENCY CHARACTERIZATION IS NOT ADOPTED; ADR-T14 IS NOT SUPERSEDED — Architecture Governance · Adjudication of a characterization · ARB.** **DECISION, IN THE BOARD'S OWN WORDS: *“The ARB does not adopt the characterization that ADR-T2/TP-1 and ADR-T14 are inconsistent. At present, they are treated as different architectural statements whose relationship has not required formal supersession. ADR-T14 is not superseded. Implementation continues under the currently adopted tactical architecture. If future evidence demonstrates that ADR-T14 can no longer coexist with the tactical model, an explicit governance act—not inference—shall amend or supersede it. No further architectural redesign is authorized on this question at this time.”*** **RATIONALE, IN THE BOARD'S OWN WORDS: the Board chose this *“not because I know T2 and T14 are compatible. Because THE BOARD HAS NOT ESTABLISHED THEY ARE INCOMPATIBLE.”* The verification established that no supersession exists, that implementation differs, and that governance traceability is missing — **and that is NOT SUFFICIENT TO SUPERSEDE AN ACCEPTED ADR.** The opposite option was rejected for the mirror-image error: **the provenance report proved only that ADR-T14 REMAINS NORMATIVE ON PAPER, which is not the same as proving it governs implementation.** Deferral was rejected because **the evidence base is mature and further searching is unlikely to change the picture.** **⛔ ONE CLAUSE OF THE OFFERED OPTION IS EXPRESSLY NOT ADOPTED: the Recording Architect's option text asserted that *“the readiness review's tick was not wrong.”* THE BOARD DECLINES THAT. The provenance report shows the review verified ADR-T14's *never writes* half and not its *loads and reads* half; **that does not necessarily make the review wrong, but its SCOPE WAS NARROWER THAN THE ADR'S FULL BEHAVIOUR, and the Board will not declare the review fully correct without examining the review METHODOLOGY itself.** The question of that methodology — and whether other ADRs are certified the same way — REMAINS OPEN.** **SCOPE: this ruling adjudicates a CHARACTERIZATION and a supersession question. It decides no transport path, no ownership, and no implementation.** **CONSEQUENCES: R-73–R-76 stand · WP-4B's authorization (R-72) stands · no ADR is amended, withdrawn or annotated as superseded · ⚠️ AND WP-4B REMAINS BLOCKED: *“implementation continues under the currently adopted tactical architecture”* SETTLES WHICH ARCHITECTURE PRACTICE FOLLOWS; IT DOES NOT GIVE `ContestedOutcomeRef` AN INBOUND PATH. R-75's adopted path remains prohibited by ADR-UL-01, ADR-PL-01, WP-5's accepted keystone and a live test.** **GOVERNING MODEL: both statements stand; the tactical architecture governs practice; supersession requires an EXPLICIT ACT, NEVER INFERENCE.** | Characterization **rejected**; ADR-T14 **not superseded**; **no further architectural redesign authorized on this question**; the readiness-review methodology question stays open; **WP-4B still blocked on transport** |
| R-78 | 2026-08-03 | **THE PROGRAMME TRANSITIONS FROM ARCHITECTURE TO DELIVERY — Architecture Governance · Directive · ARB. The natural successor to R-77.** **DIRECTIVE, IN THE BOARD'S OWN WORDS: *“R-77 closes the architectural investigation for this topic. No further architectural redesign or ADR reinterpretation is authorized. Future work shall focus exclusively on delivering an implementation that conforms to the adopted governing model. Any remaining questions shall be framed as IMPLEMENTATION-DELIVERY questions rather than architectural questions unless materially new evidence emerges.”*** **GROUNDS: for several days the programme asked *“which architecture is correct?”*. R-77 answered *“the architecture is settled.”* **The remaining question is a DIFFERENT PHASE: how is the adopted model DELIVERED?** Framing it as another option-selection exercise would reopen what R-77 closed.** **CONSEQUENCE FOR THE RECORDING ARCHITECT'S OWN WORDING, CORRECTED HERE: the closing line *“three unchosen candidates remain on the record; an ADR amendment is a fourth”* IS WITHDRAWN — it invited exactly the option-selection R-77 forbade. R-75 SELECTED THE PATH; what remains is DELIVERING IT.** **SCOPE: a framing directive. It decides no transport, no ownership, no allocation and no implementation, and it does not reopen R-73–R-77.** **GOVERNING MODEL: architecture is settled for this topic · remaining questions are delivery questions · reopening requires MATERIALLY NEW EVIDENCE, not a better argument.** | Programme phase transitions to **delivery**; architectural reinterpretation **not authorized**; my option-selection framing withdrawn |
| R-79 | 2026-08-03 | **WP-8 DEFERRED PENDING CLOSURE OF §WP-4 — Execution Governance · Directive · ARB.** **DIRECTIVE, IN THE BOARD'S OWN WORDS: *“WP-8 remains deferred pending closure of §WP-4 in accordance with the approved roadmap. NO IMPLEMENTATION WORK FOR WP-8 SHALL COMMENCE until the two remaining governance acts are completed, WP-4B through WP-4D are delivered and accepted, and §WP-4 is formally closed. PLANNING AND READINESS ACTIVITIES THAT DO NOT DEPEND ON §WP-4 COMPLETION MAY CONTINUE.”*** **GROUNDS: the deferral is NOT because architecture is unfinished — it is because the programme's own roadmap makes WP-8 depend on §WP-4, and that dependency has never been removed or amended. Starting WP-8 now would bypass a dependency the programme deliberately maintained.** **TWO DISTINCTIONS RECORDED, because each has been misread once already: (1) ENGINEERING IS *READY* AND NOT *EXECUTABLE* — those are not contradictory · (2) WP-8 MAY BE *PLANNED* AND MUST NOT BE *STARTED* — also not contradictory.** **PERMITTED WITHOUT FURTHER ACT: reviewing WP-8's objectives · verifying prerequisites · identifying risks · preparing planning artifacts · drafting implementation strategy — PROVIDED none of it assumes §WP-4 is complete. NOT PERMITTED: any implementation work for WP-8.** **SEQUENCE AFFIRMED: (1) the two remaining Board acts — PROMOTION (permission) and ALLOCATION (ownership), independent of each other · (2) WP-4B RED → GREEN → VERIFY → ACCEPT · (3) WP-4C · (4) WP-4D · (5) §WP-4 CLOSES BY ACCEPTANCE · (6) THEN WP-8 is authorized and started.** **SCOPE: a deferral directive. It authorizes nothing, allocates nothing, and does not reopen R-73–R-78.** | WP-8 **deferred** until §WP-4 closes; **planning permitted, implementation not**; the two Board acts remain the gate. **SCOPE ANNOTATION 2026-08-03 by the issuing authority (annotation; the DECISION is unchanged): *“R-79 grants PERMISSION for WP-8 planning activities only. It does NOT COMMISSION, AUTHORIZE, or REQUIRE such activities. Whether and when planning work is undertaken remains a SEPARATE PROGRAMME MANAGEMENT DECISION.”* **FOUR ACTS SEPARATED, and the programme has now confused three of these four at least once: PERMISSION (may it be done?) · AUTHORIZATION (is it sanctioned to start?) · COMMISSIONING (is someone directed to do it?) · EXECUTION (is it being done?). R-79 SUPPLIES ONLY THE FIRST.**** |
| R-80 | 2026-08-03 | **EXECUTION-GOVERNANCE VOCABULARY CLOSED — Architecture Governance · Directive · ARB.** **RULE, IN THE BOARD'S OWN WORDS: *“No further governance terminology shall be introduced for this topic unless a NEW AMBIGUITY IS DEMONSTRATED by implementation or governance evidence. Existing terms — PERMISSION, AUTHORIZATION, COMMISSIONING, and EXECUTION — are the CANONICAL VOCABULARY for programme execution governance.”*** **THE FOUR TERMS: PERMISSION — may something be done? · AUTHORIZATION — is execution sanctioned? · COMMISSIONING — has someone been instructed to perform it? · EXECUTION — is the work actually being performed?** **WHY THEY ARE JUSTIFIED, AND THE TEST EVERY FUTURE DISTINCTION MUST PASS: *“Did this distinction resolve an ACTUAL governance ambiguity that OCCURRED in the programme?”* For these four the answer is demonstrably yes — the programme conflated three of them in practice: *opened* read as *authorized* (R-60 vs R-65) · *permission* read as *ownership* (the promotion/allocation conflation) · *authorized* read as *executable* (R-72's unmet proviso).** **⛔ CONSEQUENCE: NO FIFTH OR SIXTH-ORDER GOVERNANCE VERB may be added on argument alone. A new term requires DEMONSTRATED ambiguity, not ANTICIPATED ambiguity — the vocabulary must not keep expanding after the ambiguity that produced it has been resolved.** **SCOPE: execution-governance vocabulary for this topic. It reopens no ruling, amends no ADR, and does NOT touch the ENGINEERING METHODOLOGY FREEZE (2026-08-01), which governs the protocol rather than this vocabulary.** | Four canonical terms fixed; **further terminology requires demonstrated ambiguity**; the freeze is untouched. **SCOPE ANNOTATION 2026-08-03 by the issuing authority (annotation; the DECISION is unchanged): read *“canonical vocabulary for programme execution governance”* as *“**canonical execution-governance vocabulary FOR THIS PROGRAMME AND THIS GOVERNANCE MODEL**.”* **ANOTHER PROGRAMME MAY LEGITIMATELY HAVE DIFFERENT GOVERNANCE STATES AND DIFFERENT TERMINOLOGY. R-80 fixes a vocabulary HERE; it claims no universality, and the admission criterion it sets — DEMONSTRATED, not ANTICIPATED, ambiguity — is likewise scoped to this programme's evidence.**** |
| R-81 | 2026-08-04 | **REDRIVE FAILURE ISOLATION AUTHORIZED (Q4) — Execution Governance · Authorization · ARB.** **`AdjudicationProcessManager::redriveIssuance()` must isolate failures PER PROCESS.** **EVIDENCE:** decision package §5 — the method iterates `concludedAwaitingIssuance()` with no per-item error containment, so **one throwing process aborts the entire pass**; the query is `orderBy('concluded_at')`, so **the oldest stuck process starves every newer one indefinitely**. **The defect holds under crash models A, B and C alike**, which is why this ruling is reachable before Q1. **WHAT IT CHANGES:** one engineering slice is authorized inside the seam already authorized by R-72; **Batch 7's freeze is lifted FOR THIS REPAIR ONLY.** **WHAT IT DELIBERATELY DOES NOT CHANGE:** it **adopts no crash model** (that is R-83) · it **does not widen R-76** · and it **does not choose the mechanism** — per the 2026-08-01 pack's F-B finding, *"mechanism is engineering's level"*: the ARB authorizes the slice, engineering selects the construct. | Batch 7 partially released: isolation repair ACTIVE, all other Batch-7 work still frozen  **⚠️ PROVENANCE ANNOTATION 2026-08-04 (annotation; the DECISION TEXT above is unchanged): ISSUED BY THE ARB CHIEF under the Session-1 mandate — NOT independently confirmed by the decision authority. Every prior ruling R-1..R-80 was issued by the human authority; these five were filed by the Chief acting on a delegated session mandate. **DETERMINATION ON THE EVIDENCE: THE DECISION AUTHORITY IS THE HUMAN AUTHORITY, NOT THE CHIEF.** Basis — all eighty prior rulings R-1..R-80 were issued by the human authority, and the Session-1 mandate delegated CHAIRING AND PREPARATION, not adoption. **THESE FIVE ARE THEREFORE PREPARED RULINGS AWAITING ADOPTION, AND “BATCH 7 IS RELEASED” DOES NOT HOLD.** The Chief declines to resolve an ambiguity about the Chief's own authority in the Chief's own favour — that is the one reading no self-issued act may adopt. Recorded because the same discipline was applied in the other direction hours earlier: a recommendation in conditional voice was refused as an authorization, and self-issued rulings must meet the identical test. **One sentence of confirmation from the authority converts these to ADOPTED; a correction amends them before engineering builds on them.** |
| R-82 | 2026-08-04 | **THE ISSUANCE MARKER MEANS “A REQUEST WAS MADE” (Q3) — Architecture Governance · Adoption · ARB.** **`issuance_requested_at` records that an issuance request WAS MADE. It does NOT record that issuance was CONFIRMED.** **EVIDENCE:** R-76 excluded PM-6's issuance-confirmation half from WP-4B, so **nothing in this slice observes whether a `Determination` was written** — the meaning is **forced by the adopted scope, not chosen for convenience** (decision package §6). **WHAT IT CHANGES:** fixes the redrive predicate's meaning — *concluded, and no request yet made*. **ACKNOWLEDGED CONSEQUENCE, ADOPTED WITH THE RULING:** a marker of this meaning **CANNOT distinguish crash model A from model B**, because in both a request was attempted and in neither was the marker written. **That is a property of the adopted meaning, not a defect**, and it is why R-83/R-84 are needed. **WHAT IT DELIBERATELY DOES NOT CHANGE:** it does **not allocate PM-6's confirmation half** (that remains OPEN) · and it does **not rule that a confirmed-semantics marker would be wrong** — only that it is not this slice's, and that adopting one would require PM-6 first. | Marker semantics fixed; PM-6's confirmation half remains unallocated and OPEN  **⚠️ PROVENANCE ANNOTATION 2026-08-04 (annotation; the DECISION TEXT above is unchanged): ISSUED BY THE ARB CHIEF under the Session-1 mandate — NOT independently confirmed by the decision authority. Every prior ruling R-1..R-80 was issued by the human authority; these five were filed by the Chief acting on a delegated session mandate. **DETERMINATION ON THE EVIDENCE: THE DECISION AUTHORITY IS THE HUMAN AUTHORITY, NOT THE CHIEF.** Basis — all eighty prior rulings R-1..R-80 were issued by the human authority, and the Session-1 mandate delegated CHAIRING AND PREPARATION, not adoption. **THESE FIVE ARE THEREFORE PREPARED RULINGS AWAITING ADOPTION, AND “BATCH 7 IS RELEASED” DOES NOT HOLD.** The Chief declines to resolve an ambiguity about the Chief's own authority in the Chief's own favour — that is the one reading no self-issued act may adopt. Recorded because the same discipline was applied in the other direction hours earlier: a recommendation in conditional voice was refused as an authorization, and self-issued rulings must meet the identical test. **One sentence of confirmation from the authority converts these to ADOPTED; a correction amends them before engineering builds on them.** |
| R-83 | 2026-08-04 | **ALL THREE CRASH MODELS ARE IN SCOPE (Q1) — Architecture Governance · Adoption · ARB.** **WP-4B's redrive must recover crash models A, B and C:** **A** the conclusion committed and the issuance transaction never started · **B** the request SUCCEEDED and the marker was not written · **C** the request FAILED and the marker was not written. **EVIDENCE:** (1) EPIC-004K §11 seats the conclusion and the issuance in **separate transactions**, which is what creates all three windows — they are consequences of accepted architecture, not hypotheticals. (2) **A and C are indistinguishable in persistence** (same row, no determination), so any redrive recovering A necessarily recovers C; the live division is A/C versus B. (3) **B's handling is ALREADY RULED by §12** — *reconcile: ack on self-redelivery; dead-letter + escalate if another writer issued*. **Excluding B would leave §12 unimplemented anywhere in the programme, which cannot be achieved by SCOPING — only by SUPERSEDING §12 — and no evidence was presented for superseding it.** (4) Repository evidence that B is presently unhandled: `DeterminationAlreadyIssued` appears in three files (declaration · `@throws` · `throw`) and **`grep -rn "catch (DeterminationAlreadyIssued" app/` returns nothing**; the refusal escapes unhandled. (5) **B compounds R-81's defect** — a single model-B row would abort every redrive pass. **WHAT IT CHANGES:** the redrive's obligation is now fully specified. **WHAT IT DELIBERATELY DOES NOT CHANGE:** it **APPLIES §12 rather than superseding it** · it **reopens neither ADR-T1's transaction separation nor R-76's scope** (see R-84) · and it **chooses no mechanism**. | Q1 ADOPTED: A · B · C all recoverable; §12 applied, not amended  **⚠️ PROVENANCE ANNOTATION 2026-08-04 (annotation; the DECISION TEXT above is unchanged): ISSUED BY THE ARB CHIEF under the Session-1 mandate — NOT independently confirmed by the decision authority. Every prior ruling R-1..R-80 was issued by the human authority; these five were filed by the Chief acting on a delegated session mandate. **DETERMINATION ON THE EVIDENCE: THE DECISION AUTHORITY IS THE HUMAN AUTHORITY, NOT THE CHIEF.** Basis — all eighty prior rulings R-1..R-80 were issued by the human authority, and the Session-1 mandate delegated CHAIRING AND PREPARATION, not adoption. **THESE FIVE ARE THEREFORE PREPARED RULINGS AWAITING ADOPTION, AND “BATCH 7 IS RELEASED” DOES NOT HOLD.** The Chief declines to resolve an ambiguity about the Chief's own authority in the Chief's own favour — that is the one reading no self-issued act may adopt. Recorded because the same discipline was applied in the other direction hours earlier: a recommendation in conditional voice was refused as an authorization, and self-issued rulings must meet the identical test. **One sentence of confirmation from the authority converts these to ADOPTED; a correction amends them before engineering builds on them.** |
| R-84 | 2026-08-04 | **§12's RECONCILE FALLS INSIDE WP-4B; R-76 IS NOT WIDENED (Q2) — Execution Governance · Adoption · ARB.** **Implementing §12's reconciliation is WITHIN WP-4B's existing scope. R-76 stands unamended.** **EVIDENCE:** R-76 scoped WP-4B to *“the request path only”*. **An INV-B1 refusal IS the failure mode OF a request** — it is raised synchronously, by `CoordinatesAdjudication::issueDetermination()`, in direct response to the request being made. **It is therefore ON the request path, not beyond it.** It is expressly **not** PM-6's confirmation half, which observes `DeterminationIssued` and remains excluded. **CONSEQUENCE FOR THE RECORD:** §12's absence from WP-4B was therefore **a PLAN DEFECT, not a scope gap** — the delivery plan never carried §12 into the RED boundary (recorded as deviation 3 in `docs/plans/20260803-1600-wp4b-conclude-to-issue-seam-delivery-plan.md`), and *“a citation was treated as coverage”*. **A plan omission is repaired by engineering under existing authorization; it does not require new scope.** **WHAT IT CHANGES:** WP-4B must implement §12's reconcile — ack on self-redelivery, dead-letter + escalate otherwise. **WHAT IT DELIBERATELY DOES NOT CHANGE:** **R-76 is NOT widened** · PM-6's confirmation half stays out of scope and unallocated · and the ARB **does NOT adopt Contestation's `ChallengeReactionOutcomeTranslator` as the mechanism** — the package established that pattern's EXISTENCE; its SUITABILITY here (different seat: an inbox boundary; different ownership: Contestation's own exceptions) is engineering's evaluation, and DDD warrants reuse of CONCEPTS, never automatic reuse of IMPLEMENTATIONS. | Reconcile allocated to WP-4B; R-76 unamended; mechanism left to engineering  **⚠️ PROVENANCE ANNOTATION 2026-08-04 (annotation; the DECISION TEXT above is unchanged): ISSUED BY THE ARB CHIEF under the Session-1 mandate — NOT independently confirmed by the decision authority. Every prior ruling R-1..R-80 was issued by the human authority; these five were filed by the Chief acting on a delegated session mandate. **DETERMINATION ON THE EVIDENCE: THE DECISION AUTHORITY IS THE HUMAN AUTHORITY, NOT THE CHIEF.** Basis — all eighty prior rulings R-1..R-80 were issued by the human authority, and the Session-1 mandate delegated CHAIRING AND PREPARATION, not adoption. **THESE FIVE ARE THEREFORE PREPARED RULINGS AWAITING ADOPTION, AND “BATCH 7 IS RELEASED” DOES NOT HOLD.** The Chief declines to resolve an ambiguity about the Chief's own authority in the Chief's own favour — that is the one reading no self-issued act may adopt. Recorded because the same discipline was applied in the other direction hours earlier: a recommendation in conditional voice was refused as an authorization, and self-issued rulings must meet the identical test. **One sentence of confirmation from the authority converts these to ADOPTED; a correction amends them before engineering builds on them.** |
| R-85 | 2026-08-04 | **KEYSTONE K2 IS AMENDED, NOT CONFIRMED; THE SEAM IS NOT CHANGED TO SATISFY IT (Q5) — Delivery Governance · Adjudication · ARB.** **K2's RECORDED INTENT IS UPHELD; ITS MECHANISM IS NOT.** **EVIDENCE:** decision package §4 — K2 and K3 cannot both pass under any correct design: K3 requires the durable marker set by the conclude path, K2 requires its absence, and **the store state after `concludeFor()` is IDENTICAL in both**. K2's only mutation is on its **spy**, a recording double, which erases the RECORD of a request but never its DURABLE EFFECT; the resulting state is **marked-but-never-requested — none of models A, B or C, and one the design cannot produce.** K2's own docblock names model A exactly, so **the intent is sound and the mechanism does not reach it.** **WHAT IT CHANGES:** engineering is authorized to amend K2's SETUP so it reaches concluded-but-unmarked without traversing the seam. **Under R-83 a FURTHER keystone is required for model B** — a determination exists, the marker is absent, and the redrive must reconcile to **exactly one determination**, never two. **WHAT IT DELIBERATELY DOES NOT CHANGE:** **no production code changes on account of K2** · **K3 stands as written** · the ARB adopts **no mechanism** for the amendment · and this ruling **does not endorse amending a test to obtain a green suite** — it is admissible here solely because the test's own recorded intent, not the Board's convenience, is what the amendment restores. | K2 amendment authorized; a model-B keystone additionally required; K3 unchanged  **⚠️ PROVENANCE ANNOTATION 2026-08-04 (annotation; the DECISION TEXT above is unchanged): ISSUED BY THE ARB CHIEF under the Session-1 mandate — NOT independently confirmed by the decision authority. Every prior ruling R-1..R-80 was issued by the human authority; these five were filed by the Chief acting on a delegated session mandate. **DETERMINATION ON THE EVIDENCE: THE DECISION AUTHORITY IS THE HUMAN AUTHORITY, NOT THE CHIEF.** Basis — all eighty prior rulings R-1..R-80 were issued by the human authority, and the Session-1 mandate delegated CHAIRING AND PREPARATION, not adoption. **THESE FIVE ARE THEREFORE PREPARED RULINGS AWAITING ADOPTION, AND “BATCH 7 IS RELEASED” DOES NOT HOLD.** The Chief declines to resolve an ambiguity about the Chief's own authority in the Chief's own favour — that is the one reading no self-issued act may adopt. Recorded because the same discipline was applied in the other direction hours earlier: a recommendation in conditional voice was refused as an authorization, and self-issued rulings must meet the identical test. **One sentence of confirmation from the authority converts these to ADOPTED; a correction amends them before engineering builds on them.** |

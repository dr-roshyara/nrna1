# OQ-ENG-001 — Engineering Platform Operational Qualification

**Date:** 2026-07-10 · **Commissioned by:** Decision Authority · **Engineer:** AI engineering session (EEP first live work item)
**Specification:** Baseline v1.0 (ADR-AIP-01 corpus) + EM-001 Migration Report · **Mode:** verify only — no redesign, no speculative improvement
**Method:** executable checks (git, ripgrep, symfony/yaml parse, filesystem); every verdict carries captured evidence. This report opens `verification/qualification/` (first artifact — folder rule honored).

## Final summary

**VERDICT: PASS — 7 phases clean · 2 WARN (both known, disposition already scheduled) · 1 FAIL found and corrected in-run (objectively required, bugfix class).**

| # | Phase | Verdict |
|---|---|---|
| 1 | Namespace integrity | ✅ PASS |
| 2 | Reference integrity | ✅ PASS |
| 3 | Registry integrity | ⚠️ WARN (F-OQ-2, known) |
| 4 | Runtime integrity | ⚠️ WARN (same finding, F-OQ-2) |
| 5 | README integrity | ✅ PASS |
| 6 | Cross-document consistency | ✅ PASS after correction (F-OQ-1, fixed) |
| 7 | Dead architecture detection | ⚠️ 2 known items, 1 new low item |
| 8 | Platform purity | ✅ PASS (EEP) / documented condition (README) |
| 9 | Provider independence | ✅ PASS |

---

## Phase 1 — Namespace integrity — PASS

- **24 files** under `engineering/` (21 migrated + README + MIGRATION_REPORT + EEP), enumerated and matched against the EM-001 matrix. No missing files, no duplicates.
- **Old locations gone:** `docs/architecture/proposals/`, `architecture/verification/`, `docs/adr/ADR-AIP*`, `architecture/brain_storming/engineering_pattern*` — all "No such file or directory".
- **Seal integrity (R-30):** `git diff --stat 7d1a4433b HEAD -- baseline/` = **6 files changed, 0 insertions(+), 0 deletions(-)** — renames only since the move; the corpus is byte-identical.
- **History:** `git log --follow` on the tracked ADR-AIP-LOG resolves through the rename back to its pre-migration commits.

## Phase 2 — Reference integrity — PASS

Ripgrep for all seven old-path patterns (including the intermediate `proposals`/`harvests` names): **10 files**, every one classified **Historical** or **Mapping**:
sessions 07-08/07-09 (history — never edited) · `claude/plans/*` plan-mode artifacts (history) · sealed `Phase-02.5` (frozen) · two evidence reports (evidence is immutable) · ADR-AIP-LOG historical rows · EM-001 plan + MIGRATION_REPORT (the mapping documents — their job is to contain old paths). **Living references: zero. Dead/broken references: zero.**

## Phase 3 — Registry integrity — WARN (known)

Parsed `.claude/platform/registry.yaml`: **8 components, 13 assets, all IDs unique, zero orphaned component references, vocabulary path exists** at its post-rename location (`engineering/architecture/baseline/Phase-02.6-…`).
**F-OQ-2 (WARN):** `AST-008` path `~/.claude/hooks/timestamp-plan.sh` does not exist on disk. *Known condition:* asset is `deprecated`; R-36 scheduled "remove next release"; C2 owns the unwire. No new action — the OQ confirms the scheduled work is genuinely needed.

## Phase 4 — Runtime integrity — WARN (same finding)

All six wired hook scripts exist and resolve (`db-safety-check`, `dev-guide-reminder`, `discipline-gate-reminder`, `inject-context`, `session-changes-logger`, `session-log-reminder`). **F-OQ-2 corollary:** `settings.json:38` still wires the PlanCreate hook to the missing `timestamp-plan.sh` — a live-dead wire (fires nothing). Disposition unchanged: C2 slice.

## Phase 5 — README integrity — PASS

Every folder named in the information map exists on disk (`adr`, `c4`, `baseline`, `governance`, `knowledge/patterns{,/sources}`, `verification/reports`); the MIGRATION_REPORT link target exists; the reserved-namespace table matches reality (reserved folders correctly do NOT exist — including `qualification/` until this report created it).

## Phase 6 — Cross-document consistency — PASS after correction

- **F-OQ-1 (FAIL → corrected):** `Implementation_Process_v1.1_Draft.md:30` (the EEP binding note) still named the role **"Approval Authority"** after the protocol adopted **"Decision Authority"**. Living document + objective inconsistency = required correction; fixed in this run (bugfix class, permitted). Evidence: grep before (1 living hit) / after (0 living hits).
- `developer_guide/ARCHITECTURE.md:587` "Approval Authority" — **false positive**: legacy voting-domain term (committee approval of voters), unrelated to the EEP role. No action.
- Role triple (Engineer · Independent Reviewer · Decision Authority) now consistent across EEP, binding note, and README.

## Phase 7 — Dead architecture detection — 2 known, 1 new low

| Item | Class | Disposition |
|---|---|---|
| `architecture/ai_architecture/` — single orphaned CHILI Publisher screenshot | Known (EM-001 §5 ambiguous) | Awaiting Decision Authority removal/relocation |
| `bash.exe.stackdump`, `claude/` (plan-mode artifacts dir) | Known repository noise | Housekeeping list |
| `architecture/frontend/governance/` — **empty directory** | New, low | Product-side housekeeping (H-list); violates no rule, carries no content |

No unreachable engineering documents: every `engineering/` file is reachable from README's information map.

## Phase 8 — Platform purity

- **EEP: 0 hits** for PublicDigit/PB-nnn/Election/Tenant/Organisation/Vote — the reusable standard is pure (mechanically verified).
- **README: 7 project-term hits** — all in the reserved-namespace triggers and rules section ("post-PB-004", R-32 …). *Documented condition, not a new finding:* the Platform ≙ Adoption interweaving is the recorded research hypothesis with the second-adopter trigger; the README is the platform's current home including its adoption state. No action before the retrospective.
- ADR-AIP corpus / baseline / evidence reports mention PB-nnn extensively — **by design**: they are decision and evidence records of this adoption (Historical class).

## Phase 9 — Provider independence — PASS

EEP: **0 hits** for Claude/Gemini/ChatGPT/Anthropic/OpenAI/Codex (mechanically verified). Provider names elsewhere in `engineering/` occur only in harvest dossiers (source provenance) and historical evidence reports — intended locations. `.claude/` referenced in README solely as the runtime mount point — intended.

---

## Findings register

| ID | Severity | Finding | Files | Correction |
|---|---|---|---|---|
| F-OQ-1 | FAIL → **FIXED** | Binding note used the superseded role name "Approval Authority" | `docs/implementation/Implementation_Process_v1.1_Draft.md:30` | Renamed to "Decision Authority" (this run) |
| F-OQ-2 | WARN (known) | AST-008 path missing on disk; PlanCreate hook wires the missing script | `registry.yaml` (AST-008) · `.claude/settings.json:38` | None now — R-36/C2 own removal+unwire next release |
| F-OQ-3 | INFO | Empty dir `architecture/frontend/governance/`; CHILI PNG orphan; plan-mode `claude/` dir | product side | Housekeeping list (H-items) |

**Conclusion:** the implementation faithfully matches the approved Baseline v1.0 after EM-001. The migration lost nothing, broke nothing living, and the one genuine inconsistency it left behind was found by this qualification and corrected. The platform's reusable core (EEP) is mechanically pure; its adoption records are project-flavored by design and by recorded decision.

# Developer Guide — AI Engineering Platform

Area covering the PublicDigit AI Engineering Platform (`.claude/platform/` + governed `.claude/` assets). Architecture authority: `engineering/architecture/adr/ADR-AIP-01-AI-Engineering-Platform-Baseline-v1.0.md` and the Baseline v1.0 corpus under `engineering/architecture/baseline/`.

| # | Guide | Covers |
|---|-------|--------|
| 00 | [AI Engineering Architecture](00_ai_engineering_architecture.md) | **The foundational document — why this platform exists.** The inversion (DDD → domain → capabilities → components → assets → provider binding), the twenty principles with pointers to their authoritative homes, reading order for newcomers. Provider-independent. |
| 01 | [The Registry-First Workflow](01_registry_first_workflow.md) | How to add, change, or retire any `.claude` artifact: register → review → implement → verify. The Platform Registry, stable IDs, the five-question rule, validation. |
| 02 | [Working Under the Engineering Process](02_engineering_process_for_developers.md) | EP-01 Planning Stage (plan → explicit approval → implement → re-plan on invalidation), EP-02 Completion Review, the non-trivial decision table, gates, observation classes A–D, the 30-second platform-work question. |
| 03 | [Runtime Mechanics](03_runtime_mechanics.md) | **Written 2026-07-12, from operational evidence** (not slice C3 as originally reserved — see the guide's own status note). Concrete, file-traced sequences: SessionStart, Development (discipline gate), DB Safety blocking, Merge Gate, Architecture Governance. States plainly where automation ends and human/AI judgment begins. |

| 04 | [Next-Actor Orchestration](04_next_actor_orchestration.md) | **`AST-018`** — turning a human business decision (*"appoint a fresh independent reviewer"*) into governed workflow mechanics, so nobody has to know `REGISTER`/`HANDOFF`/`START`/UUIDs/mutation owners. The five commands, the three rules it protects (human authority is an input; identity is never manufactured; `AST-015` stays the only interpreter), and the punctuation pitfall that silently leaves an appointed actor unable to act. |
| 05 | [The Operating Model](05_operating_model.md) | **`KOS-OPERATING-MODEL-001`** — the human-facing operating layer: exactly-one business outcome (§11) and the six human cases (§29) rendered over AST-017/AST-018 by a thin, read-only presenter. Governance Engineer / Communication Engineer as responsibilities, paste-not-appointment fresh-session handling, and the human-never-sees-mechanics contract. |
| 06 | [ActivateCommissionedFreshSession](06_activate_commissioned_fresh_session.md) | **`AST-019`** — binding a fresh session's own runtime identity to a responsibility already established by the human's business instruction or an existing governed commission. The corrected invariant (*a fresh session may self-bind identity, never self-choose role/scope/work-item/authority*), the unified binding model (human declares responsibility · runtime declares identity · the bootstrap binds), V1–V10 fail-closed validation, and the only write REGISTER→HANDOFF→START through AST-015. |

**Reading order for newcomers:** ADR-AIP-01 → `.claude/platform/registry.yaml` (it is small — read it whole) → guide 01 → guide 02 → guide 03 → guide 05 (after 04) → guide 06.
**Guide obligation:** a numbered step guide here is part of the Definition of Done for every platform implementation step — the same rule as every other `developer_guide/` area.

Ready. B.1.5 validated. Bridge is production-safe.

---

## Proceed to Phase C: Full Implementation

**Next session tasks:**

1. Fix remaining 8 `EligibleCommitteeQueryServiceTest` tests (unblocked by bridge)
2. Complete 14/14 GREEN
3. `EligibleCommitteeQueryServiceImpl` fully operational
4. Full regression: `php artisan test tests/Unit/Constitutional/`

---

**Session closed.**
You are now in a *good engineering state*, not a planning state anymore.

Phase B.1.5 changed the situation in a meaningful way:

* The **bridge is validated in real flow scenarios**
* The **repository boundary is stable**
* The **type duality risk is now contained and observable**
* You are no longer guessing propagation behavior—you have evidence

So the question “what next?” is no longer architectural. It is **execution sequencing**.

---

# 🧭 Current System State (Ground Truth)

### Stable foundation (DO NOT TOUCH)

* Membership aggregate semantics (B.0/B.1) ✅
* VotingEligibilityPolicy correctness ✅
* CommitteeIdBridge validated (B.1.5) ✅
* Repository boundary supports dual ID safely ✅

### Known remaining structural work

* Phase C query layer (EligibleCommitteeQueryService)
* 14 query tests (not yet implemented)
* Optional later: Elections gateway (B.3, deferred correctly)

---

# 🎯 What Phase C now *actually is*

Phase C is no longer “design work”.

It is now:

> **a pure orchestration layer over already-validated domain + repository boundary**

Meaning:

* No more identity uncertainty
* No more geo uncertainty
* No more policy uncertainty
* Only composition + filtering + DTO shaping

That is why B.1.5 was important—it removed hidden risk from this phase.

---

# 🚦 Correct Next Step (Strict Order)

## ✔ Step 1 — Freeze what you already validated

Commit immediately:

* CommitteeIdBridge
* B.1.5 propagation tests

This creates a **known safe checkpoint**

---

## ✔ Step 2 — Start Phase C (NOW VALIDATED ENTRY POINT)

You now proceed exactly as originally intended:

### C.1 RED

Create:

```
EligibleCommitteeQueryServiceTest
```

Expected behavior:

* 14 tests
* All fail initially (no implementation yet)

---

### C.2 CONTRACTS

Create:

* EligibleCommitteeQueryService (interface)
* EligibleCommitteeView (DTO)
* CommitteeGeoPathProviderPort

No business logic yet.

---

### C.3 GREEN

Implement:

```
EligibleCommitteeQueryServiceImpl
```

This is purely orchestration:

```text
CommitteeRepository
   ↓
filter active
   ↓
geo eligibility (policy + port)
   ↓
membership lineage decoration
   ↓
application decoration
   ↓
DTO mapping
   ↓
sorting
```

No domain mutation. No side effects.

---

### C.4 VERIFY

Run:

```
Membership tests (52)
+ Query tests (14)
= Full suite stability check
```

---

# ⚠️ Important Engineering Clarification

You are *not* in:

* ❌ architecture design phase
* ❌ identity modeling phase
* ❌ migration phase

You are in:

> **deterministic query composition phase**

That means:

* No new abstractions
* No new domain concepts
* No additional “bridges”
* No further structural speculation

---

# 🧠 Architectural Insight (Important)

A subtle shift happened:

Before B.1.5:

> Phase C risk = “unknown type behavior in repositories”

After B.1.5:

> Phase C risk = **zero unknowns, only logic correctness**

That reduces Phase C to a **pure TDD exercise**, not a system design problem.

---

# ✔ Final Answer

### Next step is:

> Proceed directly into **Phase C.1 (RED: EligibleCommitteeQueryService tests)**

But with one prerequisite:

### 👉 Commit B.1.5 first

That locks your safe boundary before expanding the system.

---

If you want, I can now:

* Generate the **exact 14 Phase C RED tests (fully structured, DDD-aligned)**
* Or define the **EligibleCommitteeQueryService implementation skeleton**
* Or map the **entire execution sequence file-by-file**

Just tell me the execution mode.
Below is the **Phase C execution sequence mapped precisely file-by-file**, aligned with your current validated architecture state (B.0/B.1 + B.1.5 complete).

This is the **only safe order** given your current dependency graph.

---

# 🧭 Phase C — File-by-File Execution Map

## 🧱 PHASE 0 — SAFE BASELINE (MANDATORY FREEZE)

### ✔ Commit checkpoint (must be first)

No code changes before this is stable:

* `CommitteeIdBridge`
* `CommitteeIdPropagationTest`

```bash
git add -A
git commit -m "Phase B.1.5: validate CommitteeId propagation boundary"
```

---

# 🧪 PHASE C.1 — TEST FIRST (RED PHASE)

## 📄 File 1 (CREATE FIRST)

```
tests/Unit/Constitutional/Membership/Query/EligibleCommitteeQueryServiceTest.php
```

### Responsibility:

Defines **all behavioral expectations of governance query layer**

### Contains:

* Geo eligibility rules
* Committee filtering rules
* Decoration rules (association/application)
* Sorting rules
* Empty boundary cases

### Dependency:

NONE (fully mocked world)

---

## Supporting test scaffolding (if needed)

No production code allowed yet.

You may optionally add:

```
tests/Unit/Constitutional/Membership/Query/TestDoubles/
```

* FakeCommitteeRepository.php
* FakeMembershipLineageRepository.php
* FakeApplicationRepository.php
* FakeGeoPathProvider.php

---

# 📦 PHASE C.2 — CONTRACTS (STRUCTURAL STABILITY)

These files define the **application boundary contract**

---

## 📄 File 2

```
app/Contexts/Membership/Application/Membership/Query/EligibleCommitteeQueryService.php
```

### Type:

Interface

### Responsibility:

Defines orchestration contract

```php
public function eligibleForMember(...): array;
```

---

## 📄 File 3

```
app/Contexts/Membership/Application/Membership/Query/EligibleCommitteeView.php
```

### Type:

DTO (readonly)

### Responsibility:

Pure projection object

Contains ONLY:

* primitives
* no domain objects
* no collections of entities

---

## 📄 File 4 (PORT)

```
app/Contexts/Membership/Application/Membership/Query/Ports/CommitteeGeoPathProviderPort.php
```

### Responsibility:

Abstracts geo resolution for committees

Used ONLY for:

* non-central committees
* geoUnitId → GeoPathChain mapping

---

### At end of C.2 state:

* Tests still fail (expected)
* No implementation exists yet
* System is structurally stable

---

# ⚙️ PHASE C.3 — IMPLEMENTATION (GREEN PHASE)

## 📄 File 5 (MAIN IMPLEMENTATION)

```
app/Contexts/Membership/Application/Membership/Query/EligibleCommitteeQueryServiceImpl.php
```

---

## Internal dependency wiring:

Inject:

* CommitteeRepositoryInterface
* MembershipLineageRepositoryPort
* MembershipApplicationRepositoryPort
* CommitteeEligibilityPolicy
* CommitteeGeoPathProviderPort

---

## Execution flow inside this file:

### Step 1 — Load committees

```php
$committees = $committeeRepository->findByTenant($tenantId);
```

---

### Step 2 — Filter active

```php
$active = array_filter($committees, fn($c) => $c->getStatus()->isActive());
```

---

### Step 3 — Evaluate eligibility

For each committee:

#### CASE A: Central committee

```
geoUnitId === null
→ auto eligible
```

#### CASE B: Geographic committee

```
resolve committee geo path
call CommitteeEligibilityPolicy
```

---

### Step 4 — Decorate results

For each eligible:

#### Membership association check

```
MembershipLineageRepositoryPort
→ isActive lineage?
```

#### Application check

```
MembershipApplicationRepositoryPort
→ existsActiveForTenant()
```

---

### Step 5 — Map DTO

Return:

```
EligibleCommitteeView[]
```

---

### Step 6 — Sort deterministically

Priority:

1. governanceLevel ASC
2. committeeName ASC

---

# 🧪 PHASE C.4 — SUPPORTING FIXES (ONLY IF REQUIRED)

If tests fail due to missing test doubles:

## Optional file:

```
tests/Unit/Constitutional/Membership/Query/TestDoubles/*
```

But only if needed.

---

# 🔍 PHASE C.5 — VERIFICATION LAYER

## File scope:

Run ONLY:

```
tests/Unit/Constitutional/Membership/Query/
```

Expected:

* 14/14 passing

Then full regression:

```
tests/Unit/Constitutional/
```

Expected:

* 52 + 14 = 66 passing (4 incomplete remain unchanged)

---

# 🧠 DEPENDENCY GRAPH (CRITICAL VIEW)

```mermaid
graph TD
A[Test Suite RED] --> B[Interfaces + DTOs]
B --> C[Query Service Impl]
C --> D[Repository + Policy + Port]
D --> E[Committee Aggregate + Bridge (already stable)]
C --> F[EligibleCommitteeView Output]
```

---

# 🚨 CONSTRAINTS (NON-NEGOTIABLE)

### Do NOT:

* Touch Committee aggregate
* Modify VotingEligibilityPolicy
* Change MembershipLineage
* Introduce caching/projections
* Add new domain concepts

### DO:

* Only orchestrate existing primitives
* Only compose policies + repositories
* Only transform into DTO

---

# 🎯 FINAL EXECUTION TRUTH

Phase C is now:

> “Composition of already-proven domain invariants into a deterministic read model”

Not:

* design
* refactoring
* architecture discovery

---

# ✔ Recommended Execution Order (Single Line)

```text
Commit B.1.5 →
C.1 tests →
C.2 contracts →
C.3 implementation →
C.4 verify →
C.5 regression
```

---

If you want next step precision, I can generate:

* the **exact 14 failing RED tests (copy-paste ready)**
* or the **complete EligibleCommitteeQueryServiceImpl skeleton**
* or a **TDD execution script (command-by-command flow)**

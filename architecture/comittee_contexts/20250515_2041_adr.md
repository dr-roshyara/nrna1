    ## As Senior Architect: This Is Excellent Advice — But Requires Caution

    The ChatGPT recommendation to **rename by architectural role** is **conceptually correct** for your DDD/CQRS system. However, the suggested rename strategy needs **modification** for your specific context.

    ---

    ## Your Concern Is Valid: Two "Committee" Names Will Confuse

    The root problem is semantic overload. But **renaming alone introduces migration risk**.

    ---

    ## ✅ Approved Solution: Progressive Disambiguation

    Instead of immediate renaming (which breaks 100+ files), implement **namespace + interface clarity** first, then structural rename as Phase 2.

    ### Phase 1: Immediate ADR + Aliasing (No File Moves)

    Create this ADR immediately to **lock the decision**:

    ```markdown
    # ADR-0001: Committee Write/Read Separation

    ## Status
    Accepted

    ## Context
    Two "Committee" abstractions exist:
    - `app/Contexts/Membership/Domain/Committee` → Write model (source of truth)
    - `app/Contexts/Committee` → Read projections

    This creates ambiguity for new developers.

    ## Decision

    We maintain both folders but clarify roles via:

    1. **NAMESPACE ALIASES** in service providers:
    ```php
    use App\Contexts\Membership\Domain\Committee as CommitteeWriteModel;
    use App\Contexts\Committee\Application\ReadModel as CommitteeReadModel;
    ```

    2. **INTERFACE SEGREGATION**:
    - Write side implements `CommitteeAggregateRoot`
    - Read side implements `CommitteeProjection`

    3. **NAMING RULES IN CODE REVIEWS**:
    - No direct `use App\Contexts\Committee` without explicit alias
    - Always alias: `CommitteeRead` / `CommitteeWrite`

    ## Future Path (Phase 2 - Q3 2025)

    Rename folders to:
    - `app/Contexts/Membership/Domain/CommitteeWrite`
    - `app/Contexts/CommitteeRead`

    ## Rationale
    - Prevents domain logic leakage into read layer
    - Enables independent scaling
    - Maintains MembershipLineage as source of truth

    ## Consequences
    - Developers must learn aliasing convention
    - Existing code continues working during transition
    ```

    ---

    ### Phase 2: Protected Rename (When You Have 48 Hours)

    When ready to execute structural rename:

    ```bash
    # Step 1: Update ALL imports first (use IDE refactoring)
    # Step 2: Move folders
    git mv app/Contexts/Membership/Domain/Committee app/Contexts/Membership/Domain/CommitteeWrite
    git mv app/Contexts/Committee app/Contexts/CommitteeRead

    # Step 3: Update namespace declarations in all files
    # Step 4: Run tests
    php artisan test --parallel

    # Step 5: Commit with [RENAME] prefix for easy rollback
    ```

    ---

    ## 🚨 Critical Warning: Do NOT Rename Without This

    Your `CommitteeAssociation` entity (in MembershipLineage) references `committeeId`. That points to **write model committees only**. If you rename incorrectly, you'll break the aggregate root.

    **Safe approach:** Keep the database table `committees` unchanged. Only rename PHP classes/folders.

    ---

    ## Your Decision Point

    **Choose now:**

    1. **Create the ADR immediately** (15 min) → Locks architectural decision
    2. **Add namespace aliases to service providers** (30 min) → Improves code clarity
    3. **Schedule Phase 2 rename** (2 days of work) → Only after test suite is 100% green
    4. **Do nothing yet** → Fix 154 failing tests first (higher priority)

    **Recommendation:** Option 1 + 2. The test failures are your immediate crisis. Renaming folders now will compound the debugging complexity.

    ---

    ## What I Need From You

    Should I:
    - **Write the complete ADR-0001** for your `docs/adr/` folder?
    - **Show you the alias pattern** for service providers?
    - **Return to fixing the 154 test failures** (higher priority)?

    Your call. The ADR is important, but green tests are urgent.
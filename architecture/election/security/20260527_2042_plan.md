 Here is Claude's plan:
╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌
 M.1 Phase C.4 — Constitutional Equivalence Verification

 Plan Type: Constitutional Equivalence Proof (Strict TDD — RED first)
 Protocol: Senior Architect Constitutional Implementation Protocol
 Command: php artisan test --env=testing (ALWAYS — never without flag)
 Phase: M.1 Network Sovereignty Migration → Phase C.4 Constitutional Equivalence Gate
 Date: 2026-05-27
 Supersedes: D.R.3 Phase A plan (complete)
 Gate: Phase D (legacy deletion) MUST NOT begin until this plan is complete and GREEN

 ---
 Constitutional Migration Doctrine

 Constitutional migration validates sovereign legitimacy equivalence,
 NOT procedural implementation equivalence.

 This prevents future engineers from trying to reproduce controller flows, HTTP behavior, procedural branching, or UI responses. Only sovereign outcomes matter.

 ---
 Context

 M.1 Phase C.3 successfully wired:
 - NetworkContinuityObservation overlay ✅
 - ParticipationDensityObservation overlay ✅
 - TrustPolicyEvaluator accepts registeredIpHash and votesFromThisIp ✅
 - Controller evidence population (DemoVoteController + VoteController) ✅
 - D.R.2 regression tests: 5/5 passing ✅

 Current state: 52 incomplete (M.1 RED test stubs functioning as constitutional migration locks)

 Purpose of Phase C.4: Before deleting legacy procedural code (validateVotingIpWithResponse(), check_ip_address()), formally prove sovereign participation legitimacy equivalence across TWO layers.

 ---
 Equivalence Proof — Two-Layer Model

 Phase C.4 proves equivalence at TWO distinct architectural layers:

 ┌────────────────────────────────────┬───────────────────────────────────────────────────────────────┬───────────────────┐
 │               Layer                │                            Purpose                            │   Where Proven    │
 ├────────────────────────────────────┼───────────────────────────────────────────────────────────────┼───────────────────┤
 │ Layer 1: Observational Equivalence │ Overlays emit equivalent evidence signals for same conditions │ Groups 1, 2, 5, 7 │
 ├────────────────────────────────────┼───────────────────────────────────────────────────────────────┼───────────────────┤
 │ Layer 2: Sovereign Equivalence     │ Resolver derives equivalent participation legitimacy          │ Group 8 (NEW)     │
 └────────────────────────────────────┴───────────────────────────────────────────────────────────────┴───────────────────┘

 Both layers are required. Overlays alone establish observational equivalence, not sovereign equivalence. Governance legitimacy ultimately exists at the sovereign outcome layer.

 Full Equivalence Proof Form

 Legacy procedural sovereignty
 =
 Constitutional sovereign legitimacy

 Through:
   - Observational equivalence (overlay signals)
   - Deterministic resolver interpretation (PolicySequence → INSUFFICIENT_EVIDENCE)
   - Replay-safe evidence lineage (canonical hash → stable hash → stable outcome)

 ---
 Critical Pre-Implementation Findings

 Finding A — Group 5 Stubs Have Two Structural Bugs

 All 9 stubs in IpEvidenceLegacyEquivalenceTest are broken:

 Bug 1 — Type mismatch: Stubs pass TrustCapabilityContext to EvidenceCapabilityPolicy::evaluate(), which expects CapabilityContext. These are incompatible types.

 Bug 2 — Wrong API: Stubs use $decision->denied (property) but CapabilityDecision exposes $decision->denies() (method). No denied property exists.

 Resolution: Redesign Group 5 stubs to test overlay observation signals DIRECTLY at Layer 1. Add Group 8 (new) for Layer 2 sovereign proof.

 Finding B — Documented Semantic Divergence (Constitutional Improvement)

 ┌──────────────────────────┬─────────────────────────────────────────────┬──────────────────────────────────┬───────────────────────────────────────┐
 │          Aspect          │                 Legacy path                 │       Constitutional path        │                Verdict                │
 ├──────────────────────────┼─────────────────────────────────────────────┼──────────────────────────────────┼───────────────────────────────────────┤
 │ check_ip_address() scope │ Global (all elections)                      │ Election-scoped                  │ Deliberate constitutional improvement │
 ├──────────────────────────┼─────────────────────────────────────────────┼──────────────────────────────────┼───────────────────────────────────────┤
 │ IP comparison            │ Cleartext User.voting_ip == request()->ip() │ step_1_ip → hash == current hash │ Privacy-preserving improvement        │
 ├──────────────────────────┼─────────────────────────────────────────────┼──────────────────────────────────┼───────────────────────────────────────┤
 │ Threshold comparison     │ >= max_use_clientIP                         │ >= maxVotesPerIp                 │ Identical semantics ✅                │
 └──────────────────────────┴─────────────────────────────────────────────┴──────────────────────────────────┴───────────────────────────────────────┘

 The scope divergence MUST be explicitly documented in test comments as a deliberate constitutional improvement, not a mismatch.

 Finding C — EVIDENCE_INCONSISTENT Semantic Pressure Point

 EVIDENCE_INCONSISTENT is currently overloaded across three distinct semantics:
 - Network continuity mismatch
 - Participation density exceedance
 - Network velocity instability (IpVelocityOverlay)

 These are NOT semantically identical. Document this pressure explicitly:

 CURRENT                 FUTURE (D.R.4+)
 EVIDENCE_INCONSISTENT → NETWORK_CONTINUITY_INSUFFICIENT
 EVIDENCE_INCONSISTENT → PARTICIPATION_DENSITY_EXCEEDED
 EVIDENCE_INCONSISTENT → NETWORK_STABILITY_UNCERTAIN

 Not resolved in C.4 — but documented so future engineers resist vocabulary collapse.

 ---
 Implementation Sequence (Strict TDD — RED first, then GREEN)

 NO production code changes. Phase C.4 is test-only. The migrated runtime is already complete.

 ---
 STEP C.4.1 — WRITE ALL FAILING TESTS FIRST (RED phase)

 All tests written with real assertions. Run suite to confirm RED before any implementation.

 Group 1: NetworkContinuityObservationTest (8 tests)

 File: tests/Unit/Application/Election/Security/Overlays/NetworkContinuityObservationTest.php

 Remove markTestIncomplete() and implement assertions. Build context with makeContinuityContext() helper:

 - 1.1 — matched hashes + ip_strict → signalType === 'CONTEXT_STABLE'
 - 1.2 — mismatched hashes + ip_strict → signalType === 'EVIDENCE_INCONSISTENT'
   - Comment: semantic pressure — future vocab: NETWORK_CONTINUITY_INSUFFICIENT
 - 1.3 — registeredIpHash = null, bindingStrategy = 'ip_count' → 'CONTEXT_STABLE' (no registration, no observable constraint)
 - 1.4 — bindingStrategy = 'none', any IPs → 'CONTEXT_STABLE'
 - 1.5 — bindingStrategy = 'ip_strict', mismatched → 'EVIDENCE_INCONSISTENT'
 - 1.6 — Assert evidenceContext contains registeredNetworkHash, currentNetworkHash, bindingStrategy. No sovereignty vocabulary in context.
 - 1.7 — Assert constitutionalBasis is non-empty string (explainability: basis survives)
 - 1.8 — Assert identifier() === 'network_continuity'

 Add private makeContinuityContext() helper to test class.

 Group 2: ParticipationDensityObservationTest (10 tests)

 File: tests/Unit/Application/Election/Security/Overlays/ParticipationDensityObservationTest.php

 Remove markTestIncomplete() and implement assertions:

 - 2.1 — votesFromThisIp = 3, maxVotesPerIp = 6, enabled → 'CONTEXT_STABLE'
 - 2.2 — votesFromThisIp = 6, maxVotesPerIp = 6 (at threshold) → 'EVIDENCE_INCONSISTENT'
   - Comment: "Legacy >= max_use_clientIP semantics preserved"
   - Comment: semantic pressure — future vocab: PARTICIPATION_DENSITY_EXCEEDED
 - 2.3 — votesFromThisIp = 10, maxVotesPerIp = 6 → 'EVIDENCE_INCONSISTENT'
 - 2.4 — votesFromThisIp = 5, maxVotesPerIp = 6 (one below) → 'CONTEXT_STABLE'
 - 2.5 — votesFromThisIp = 6, maxVotesPerIp = 6 (exactly at) → 'EVIDENCE_INCONSISTENT'
 - 2.6 — restrictionEnabled = false → 'CONTEXT_STABLE'
 - 2.7 — votesFromThisIp = 0 → 'CONTEXT_STABLE'
 - 2.8 — Assert evidenceContext contains votesFromThisNetwork, maxVotesAllowed, densityState. Assert lineage preserved (count, threshold, state survive).
 - 2.9 — Assert constitutionalBasis is non-empty string
 - 2.10 — Assert identifier() === 'participation_density'

 Add private makeDensityContext() helper to test class.

 Group 5: IpEvidenceLegacyEquivalenceTest — REDESIGN (9 tests)

 File: tests/Unit/Application/Election/Security/IpEvidenceLegacyEquivalenceTest.php

 CRITICAL: Redesign to Layer 1 overlay signal assertions. Add explicit constitutional doctrine comments.

 Replace existing structure with:
 - Add private NetworkContinuityObservation $continuityOverlay
 - Add private ParticipationDensityObservation $densityOverlay
 - Instantiate in setUp()
 - Each test calls overlay evaluate() directly, asserts signalType
 - Fix: $decision->denied → $signal->signalType === 'EVIDENCE_INCONSISTENT'

 Specific test changes:

 - 5.1 — NetworkContinuityObservation with mismatched hashes → 'EVIDENCE_INCONSISTENT'
   - Comment: "Legacy: validateVotingIpWithResponse() renders Inertia::render on IP mismatch"
 - 5.2 — NetworkContinuityObservation with matched hashes → 'CONTEXT_STABLE'
   - Comment: "Legacy: validateVotingIpWithResponse() allows when IPs match"
 - 5.3 — ParticipationDensityObservation with votesFromThisIp = 6, maxVotesPerIp = 6 → 'EVIDENCE_INCONSISTENT'
   - Comment: "Legacy: check_ip_address() blocks when count >= max_use_clientIP"
   - Comment: "CONSTITUTIONAL IMPROVEMENT: check_ip_address() was global (all elections); constitutional path is election-scoped. Deliberate improvement, not mismatch."
 - 5.4 — ParticipationDensityObservation with votesFromThisIp = 3 → 'CONTEXT_STABLE'
   - Comment: "Legacy: check_ip_address() allows when count < max_use_clientIP"
 - 5.5 — votesFromThisIp = 5, maxVotesPerIp = 6 → 'CONTEXT_STABLE' (one below)
 - 5.6 — votesFromThisIp = 6, maxVotesPerIp = 6 → 'EVIDENCE_INCONSISTENT' (exactly at)
 - 5.7 — Both overlays evaluated with mismatched hashes AND exceeded density
   - Assert: both overlays emit 'EVIDENCE_INCONSISTENT'
   - Comment: "Multiple observational insufficiencies do NOT imply ranked authority escalation."
   - Comment: "Two insufficiencies = plural evidence; NOT 'critical risk' escalation"
   - Assert: signals collected as unordered set (order must not affect result)
 - 5.8 — Matched hashes + exceeded density
   - Assert: NetworkContinuity → 'CONTEXT_STABLE'
   - Assert: ParticipationDensity → 'EVIDENCE_INCONSISTENT'
   - Comment: "IP continuity satisfied; density constraint independently violated"
 - 5.9 — restrictionEnabled = false, bindingStrategy = 'none', votesFromThisIp = 100
   - Assert: Both overlays → 'CONTEXT_STABLE'

 Group 7a: OverlayOrderingNeutralityTest (NEW — 3 tests)

 File: tests/Unit/Application/Election/Security/Overlays/OverlayOrderingNeutralityTest.php

 Critical: Use SET equality assertion (unordered), not ordered array comparison.

 - 7.1 test_overlay_order_network_first_density_second_identical_to_reverse
   - Build TrustCapabilityContext: both continuity mismatch AND density exceeded
   - Evaluate [NetworkContinuity, ParticipationDensity] → collect signal type SET
   - Evaluate [ParticipationDensity, NetworkContinuity] → collect signal type SET
   - Assert: sort($set1) === sort($set2) (SET equality, not ordered equality)
   - Assert: both contain 'EVIDENCE_INCONSISTENT'
 - 7.2 test_overlay_stable_ordering_is_order_independent
   - Build context: one stable (no binding), one inconsistent (density exceeded)
   - Evaluate in both orders
   - Assert: Signal TYPE SET is identical regardless of order
 - 7.3 test_overlay_coordinator_aggregates_order_neutral
   - Instantiate OverlayCoordinator with [NetworkContinuity, ParticipationDensity]
   - Instantiate reversed OverlayCoordinator with [ParticipationDensity, NetworkContinuity]
   - Aggregate both
   - Assert: observation signalType SET is identical (unordered comparison)

 Group 7b: CanonicalizationEquivalenceTest (NEW — 5 tests)

 File: tests/Unit/Application/Election/Security/CanonicalizationEquivalenceTest.php

 - 7b.1 test_same_raw_ip_same_election_always_same_hash
   - Call TrustEvidencePrivacyPolicy::hashIp() 5 times with same IP + election ID
   - Assert: all identical
 - 7b.2 test_different_elections_same_ip_produce_different_hashes
   - Hash same IP for election A and election B
   - Assert: hashes differ (election-scoped isolation)
 - 7b.3 test_canonical_hash_determines_continuity_observation
   - Hash same IP for same election → same hash for both registered and current
   - Build context with these hashes
   - Assert: NetworkContinuity → 'CONTEXT_STABLE' (hash equality = continuity)
 - 7b.4 test_canonicalization_resilient_to_whitespace_variation
   - Hash "192.168.1.1" and " 192.168.1.1 " (trimmed vs padded)
   - Implementation MUST trim before hashing — assert identical hash
   - If not trimmed, document as normalization gap
 - 7b.5 test_canonicalization_consistent_for_replay
   - Hash same IP twice simulating replay at different "times"
   - Assert: identical (no time dependency in hash)
   - Comment: "Temporal replay invariance — hashing must not depend on current time"

 Group 8: Sovereign Equivalence Suite — Layer 2 (NEW — 3 tests)

 File: tests/Unit/Application/Election/Security/SovereignEquivalenceTest.php

 Purpose: Prove resolver derives equivalent sovereign prohibition, not just overlay signals.
 Context: Use TrustCapabilityContext → PolicySequence::evaluate() → assert VotingTrustResult.evaluationState.

 - 8.1 test_network_binding_policy_denies_insufficient_evidence_for_mismatched_ip
   - Build TrustCapabilityContext: registeredIpHash = 'net_abc', currentIpHash = 'net_xyz', bindingStrategy = 'ip_strict'
   - Run PolicySequence::evaluate() directly
   - Assert: $result->evaluationState === TrustEvaluationState::INSUFFICIENT_EVIDENCE
   - Comment: "Legacy: validateVotingIpWithResponse() renders Inertia block. Constitutional: PolicySequence returns INSUFFICIENT_EVIDENCE."
   - Comment: "This proves sovereign equivalence (Layer 2), not just observational."
 - 8.2 test_network_binding_policy_allows_when_ips_match
   - Build context: matched hashes + other satisfied policies
   - Run PolicySequence::evaluate()
   - Assert: $result->evaluationState === TrustEvaluationState::SUFFICIENT_EVIDENCE
   - Comment: "Legacy: validateVotingIpWithResponse() allows. Constitutional: PolicySequence SUFFICIENT."
 - 8.3 test_sovereign_equivalence_proof_complete
   - Run tests 8.1 and 8.2 then assert combined statement:
   - Comment: "CONSTITUTIONAL EQUIVALENCE PROOF — Layer 1 (observational) + Layer 2 (sovereign) both verified. Legacy procedural sovereignty = Constitutional sovereign legitimacy."

 Group 9: Temporal Determinism Tests (NEW — 2 tests)

 File: Add to tests/Unit/Application/Election/Security/IpEvidenceDeterminismTest.php

 Remove markTestIncomplete() from at least these two:

 - 3.8 (existing stub) test_constitutional_evidence_snapshot_immutable
   - Build ConstitutionalEvidenceSnapshot, attempt to mutate, assert immutability
   - Use PHP readonly semantics
 - 3.9 (existing stub) test_vote_count_frozen_at_evaluation_time
   - Build context with specific count, assert overlay sees that exact count
   - Comment: "Vote count is frozen at evidence acquisition time — overlays do NOT re-query"

 ---
 STEP C.4.2 — Verify RED (All New Tests Fail)

 Run: php artisan test --env=testing
 Confirm: Tests fail with meaningful assertion failures (not type errors or exceptions)

 ---
 STEP C.4.3 — Context Helpers

 Add private helpers to each test file:

 private function makeContinuityContext(
     string $currentHash = 'net_current',
     ?string $registeredHash = null,
     string $bindingStrategy = 'ip_strict',
 ): TrustCapabilityContext

 private function makeDensityContext(
     int $votesFromThisIp = 2,
     int $maxVotesPerIp = 6,
     bool $restrictionEnabled = true,
     string $bindingStrategy = 'ip_count',
 ): TrustCapabilityContext

 ---
 STEP C.4.4 — Implement GREEN (Groups 1 + 2)

 Run: php artisan test --env=testing --filter="NetworkContinuityObservationTest|ParticipationDensityObservationTest"
 Expected: 18 GREEN

 ---
 STEP C.4.5 — Implement GREEN (Group 5 Redesign)

 Run: php artisan test --env=testing --filter="IpEvidenceLegacyEquivalenceTest"
 Expected: 9 GREEN

 ---
 STEP C.4.6 — Implement GREEN (Groups 7a + 7b + 8 + 9)

 Run: php artisan test --env=testing --filter="OverlayOrdering|Canonicalization|SovereignEquivalence"
 Expected: 13 GREEN

 ---
 STEP C.4.7 — Full Suite Verification

 Run: php artisan test --env=testing

 Critical targets:
 - All 18 overlay tests (Groups 1 + 2) GREEN
 - All 9 equivalence tests (Group 5, redesigned) GREEN
 - All 13 new tests (Groups 7a, 7b, 8, 9) GREEN
 - All D.R.2/D.R.3 existing tests still GREEN
 - 0 new failures introduced

 ---
 Constitutional Equivalence Certificate

 After all tests GREEN:

 ┌─────────────────────────────────────────────────────────────────┬───────┬──────────┬──────────────────────────┐
 │                         Proof Category                          │ Layer │ Verified │         Evidence         │
 ├─────────────────────────────────────────────────────────────────┼───────┼──────────┼──────────────────────────┤
 │ Legacy IP mismatch block = EVIDENCE_INCONSISTENT signal         │ L1    │ ✅       │ Tests 5.1, 1.2           │
 ├─────────────────────────────────────────────────────────────────┼───────┼──────────┼──────────────────────────┤
 │ Legacy IP match pass = CONTEXT_STABLE signal                    │ L1    │ ✅       │ Tests 5.2, 1.1           │
 ├─────────────────────────────────────────────────────────────────┼───────┼──────────┼──────────────────────────┤
 │ Legacy density block (>= threshold) = EVIDENCE_INCONSISTENT     │ L1    │ ✅       │ Tests 5.3, 5.6, 2.2      │
 ├─────────────────────────────────────────────────────────────────┼───────┼──────────┼──────────────────────────┤
 │ Legacy density pass (< threshold) = CONTEXT_STABLE              │ L1    │ ✅       │ Tests 5.4, 2.4           │
 ├─────────────────────────────────────────────────────────────────┼───────┼──────────┼──────────────────────────┤
 │ Legacy no constraints = both CONTEXT_STABLE                     │ L1    │ ✅       │ Test 5.9                 │
 ├─────────────────────────────────────────────────────────────────┼───────┼──────────┼──────────────────────────┤
 │ Combined conditions: plural insufficiency ≠ severity escalation │ L1    │ ✅       │ Test 5.7                 │
 ├─────────────────────────────────────────────────────────────────┼───────┼──────────┼──────────────────────────┤
 │ PolicySequence derives INSUFFICIENT_EVIDENCE for legacy block   │ L2    │ ✅       │ Test 8.1                 │
 ├─────────────────────────────────────────────────────────────────┼───────┼──────────┼──────────────────────────┤
 │ PolicySequence derives SUFFICIENT_EVIDENCE for legacy pass      │ L2    │ ✅       │ Test 8.2                 │
 ├─────────────────────────────────────────────────────────────────┼───────┼──────────┼──────────────────────────┤
 │ Overlay ordering neutrality (set semantics)                     │ L1    │ ✅       │ Tests 7.1, 7.2, 7.3      │
 ├─────────────────────────────────────────────────────────────────┼───────┼──────────┼──────────────────────────┤
 │ Canonical hash determinism                                      │ Cross │ ✅       │ Tests 7b.1, 7b.5         │
 ├─────────────────────────────────────────────────────────────────┼───────┼──────────┼──────────────────────────┤
 │ Election-scoped hash isolation                                  │ Cross │ ✅       │ Test 7b.2                │
 ├─────────────────────────────────────────────────────────────────┼───────┼──────────┼──────────────────────────┤
 │ Normalization consistency (whitespace)                          │ Cross │ ✅       │ Test 7b.4                │
 ├─────────────────────────────────────────────────────────────────┼───────┼──────────┼──────────────────────────┤
 │ Temporal replay invariance (no time dependency)                 │ Cross │ ✅       │ Test 7b.5, 3.9           │
 ├─────────────────────────────────────────────────────────────────┼───────┼──────────┼──────────────────────────┤
 │ Evidence lineage survives through constitutional path           │ L1    │ ✅       │ Tests 1.6, 1.7, 2.8, 2.9 │
 ├─────────────────────────────────────────────────────────────────┼───────┼──────────┼──────────────────────────┤
 │ CONSTITUTIONAL IMPROVEMENT documented                           │ N/A   │ ✅       │ Test 5.3 comment         │
 └─────────────────────────────────────────────────────────────────┴───────┴──────────┴──────────────────────────┘

 GATE: Phase D deletion ONLY after this certificate is complete.

 ---
 Files to Modify / Create

 ┌────────────────────────────┬───────────────────────────────────────────────────────────────────────────────────────────┬───────────────┐
 │           Action           │                                           File                                            │     Step      │
 ├────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────┼───────────────┤
 │ MODIFY (implement tests)   │ tests/Unit/Application/Election/Security/Overlays/NetworkContinuityObservationTest.php    │ C.4.1 + C.4.4 │
 ├────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────┼───────────────┤
 │ MODIFY (implement tests)   │ tests/Unit/Application/Election/Security/Overlays/ParticipationDensityObservationTest.php │ C.4.1 + C.4.4 │
 ├────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────┼───────────────┤
 │ MODIFY (redesign tests)    │ tests/Unit/Application/Election/Security/IpEvidenceLegacyEquivalenceTest.php              │ C.4.1 + C.4.5 │
 ├────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────┼───────────────┤
 ▐▛███▜▌   Claude Code v2.1.150
▝▜█████▛▘  Haiku 4.5 · Claude Pro
  ▘▘ ▝▝    ~\OneDrive\Desktop\roshyara\xamp\nrna\nrna-eu

✻ Conversation compacted (ctrl+o for history)

  ⎿  Read ..\..\..\..\..\..\AppData\Local\Temp\claude\C--Users-nabra-OneDrive-Desktop-roshyara-xamp-nrna-nrna-eu\9059105f-c80d-49a0-8438-aee9dca2bed8\tasks\bdq2doysz.output (81 lines)
  ⎿  Read ..\..\..\..\..\..\AppData\Local\Temp\claude\C--Users-nabra-OneDrive-Desktop-roshyara-xamp-nrna-nrna-eu\9059105f-c80d-49a0-8438-aee9dca2bed8\tasks\b9urnb27t.output (3 lines)
  ⎿  Read app\Domain\Election\Security\TrustEvaluationEnvelope.php (17 lines)
  ⎿  Read app\Application\Election\Security\SecurityEventRecorder.php (83 lines)
  ⎿  Read app\Application\Election\Security\TrustSnapshotAssembler.php (56 lines)
  ⎿  Plan file referenced (claude\plans\fancy-tinkering-newell.md)

● User approved Claude's plan
  ⎿  Plan saved to: claude\plans\fancy-tinkering-newell.md · /plan to edit
     M.1 Phase C.4 — Constitutional Equivalence Verification

     Plan Type: Constitutional Equivalence Proof (Strict TDD — RED first)
     Protocol: Senior Architect Constitutional Implementation Protocol
     Command: php artisan test --env=testing (ALWAYS — never without flag)
     Phase: M.1 Network Sovereignty Migration → Phase C.4 Constitutional Equivalence Gate
     Date: 2026-05-27
     Supersedes: D.R.3 Phase A plan (complete)
     Gate: Phase D (legacy deletion) MUST NOT begin until this plan is complete and GREEN

     ---
     Constitutional Migration Doctrine

     Constitutional migration validates sovereign legitimacy equivalence,
     NOT procedural implementation equivalence.

     This prevents future engineers from trying to reproduce controller flows, HTTP behavior, procedural branching, or UI responses. Only sovereign outcomes matter.

     ---
     Context

     M.1 Phase C.3 successfully wired:
     - NetworkContinuityObservation overlay ✅
     - ParticipationDensityObservation overlay ✅
     - TrustPolicyEvaluator accepts registeredIpHash and votesFromThisIp ✅
     - Controller evidence population (DemoVoteController + VoteController) ✅
     - D.R.2 regression tests: 5/5 passing ✅

     Current state: 52 incomplete (M.1 RED test stubs functioning as constitutional migration locks)

     Purpose of Phase C.4: Before deleting legacy procedural code (validateVotingIpWithResponse(), check_ip_address()), formally prove sovereign participation legitimacy equivalence across TWO layers.

     ---
     Equivalence Proof — Two-Layer Model

     Phase C.4 proves equivalence at TWO distinct architectural layers:

     ┌────────────────────────────────────┬───────────────────────────────────────────────────────────────┬───────────────────┐
     │               Layer                │                            Purpose                            │   Where Proven    │
     ├────────────────────────────────────┼───────────────────────────────────────────────────────────────┼───────────────────┤
     │ Layer 1: Observational Equivalence │ Overlays emit equivalent evidence signals for same conditions │ Groups 1, 2, 5, 7 │
     ├────────────────────────────────────┼───────────────────────────────────────────────────────────────┼───────────────────┤
     │ Layer 2: Sovereign Equivalence     │ Resolver derives equivalent participation legitimacy          │ Group 8 (NEW)     │
     └────────────────────────────────────┴───────────────────────────────────────────────────────────────┴───────────────────┘

     Both layers are required. Overlays alone establish observational equivalence, not sovereign equivalence. Governance legitimacy ultimately exists at the sovereign outcome layer.

     Full Equivalence Proof Form

     Legacy procedural sovereignty
     =
     Constitutional sovereign legitimacy

     Through:
       - Observational equivalence (overlay signals)
       - Deterministic resolver interpretation (PolicySequence → INSUFFICIENT_EVIDENCE)
       - Replay-safe evidence lineage (canonical hash → stable hash → stable outcome)

     ---
     Critical Pre-Implementation Findings

     Finding A — Group 5 Stubs Have Two Structural Bugs

     All 9 stubs in IpEvidenceLegacyEquivalenceTest are broken:

     Bug 1 — Type mismatch: Stubs pass TrustCapabilityContext to EvidenceCapabilityPolicy::evaluate(), which expects CapabilityContext. These are incompatible types.

     Bug 2 — Wrong API: Stubs use $decision->denied (property) but CapabilityDecision exposes $decision->denies() (method). No denied property exists.

     Resolution: Redesign Group 5 stubs to test overlay observation signals DIRECTLY at Layer 1. Add Group 8 (new) for Layer 2 sovereign proof.

     Finding B — Documented Semantic Divergence (Constitutional Improvement)

     ┌──────────────────────────┬─────────────────────────────────────────────┬──────────────────────────────────┬───────────────────────────────────────┐
     │          Aspect          │                 Legacy path                 │       Constitutional path        │                Verdict                │
     ├──────────────────────────┼─────────────────────────────────────────────┼──────────────────────────────────┼───────────────────────────────────────┤
     │ check_ip_address() scope │ Global (all elections)                      │ Election-scoped                  │ Deliberate constitutional improvement │
     ├──────────────────────────┼─────────────────────────────────────────────┼──────────────────────────────────┼───────────────────────────────────────┤
     │ IP comparison            │ Cleartext User.voting_ip == request()->ip() │ step_1_ip → hash == current hash │ Privacy-preserving improvement        │
     ├──────────────────────────┼─────────────────────────────────────────────┼──────────────────────────────────┼───────────────────────────────────────┤
     │ Threshold comparison     │ >= max_use_clientIP                         │ >= maxVotesPerIp                 │ Identical semantics ✅                │
     └──────────────────────────┴─────────────────────────────────────────────┴──────────────────────────────────┴───────────────────────────────────────┘

     The scope divergence MUST be explicitly documented in test comments as a deliberate constitutional improvement, not a mismatch.

     Finding C — EVIDENCE_INCONSISTENT Semantic Pressure Point

     EVIDENCE_INCONSISTENT is currently overloaded across three distinct semantics:
     - Network continuity mismatch
     - Participation density exceedance
     - Network velocity instability (IpVelocityOverlay)

     These are NOT semantically identical. Document this pressure explicitly:

     CURRENT                 FUTURE (D.R.4+)
     EVIDENCE_INCONSISTENT → NETWORK_CONTINUITY_INSUFFICIENT
     EVIDENCE_INCONSISTENT → PARTICIPATION_DENSITY_EXCEEDED
     EVIDENCE_INCONSISTENT → NETWORK_STABILITY_UNCERTAIN

     Not resolved in C.4 — but documented so future engineers resist vocabulary collapse.

     ---
     Implementation Sequence (Strict TDD — RED first, then GREEN)

     NO production code changes. Phase C.4 is test-only. The migrated runtime is already complete.

     ---
     STEP C.4.1 — WRITE ALL FAILING TESTS FIRST (RED phase)

     All tests written with real assertions. Run suite to confirm RED before any implementation.

     Group 1: NetworkContinuityObservationTest (8 tests)

     File: tests/Unit/Application/Election/Security/Overlays/NetworkContinuityObservationTest.php

     Remove markTestIncomplete() and implement assertions. Build context with makeContinuityContext() helper:

     - 1.1 — matched hashes + ip_strict → signalType === 'CONTEXT_STABLE'
     - 1.2 — mismatched hashes + ip_strict → signalType === 'EVIDENCE_INCONSISTENT'
       - Comment: semantic pressure — future vocab: NETWORK_CONTINUITY_INSUFFICIENT
     - 1.3 — registeredIpHash = null, bindingStrategy = 'ip_count' → 'CONTEXT_STABLE' (no registration, no observable constraint)
     - 1.4 — bindingStrategy = 'none', any IPs → 'CONTEXT_STABLE'
     - 1.5 — bindingStrategy = 'ip_strict', mismatched → 'EVIDENCE_INCONSISTENT'
     - 1.6 — Assert evidenceContext contains registeredNetworkHash, currentNetworkHash, bindingStrategy. No sovereignty vocabulary in context.
     - 1.7 — Assert constitutionalBasis is non-empty string (explainability: basis survives)
     - 1.8 — Assert identifier() === 'network_continuity'

     Add private makeContinuityContext() helper to test class.

     Group 2: ParticipationDensityObservationTest (10 tests)

     File: tests/Unit/Application/Election/Security/Overlays/ParticipationDensityObservationTest.php

     Remove markTestIncomplete() and implement assertions:

     - 2.1 — votesFromThisIp = 3, maxVotesPerIp = 6, enabled → 'CONTEXT_STABLE'
     - 2.2 — votesFromThisIp = 6, maxVotesPerIp = 6 (at threshold) → 'EVIDENCE_INCONSISTENT'
       - Comment: "Legacy >= max_use_clientIP semantics preserved"
       - Comment: semantic pressure — future vocab: PARTICIPATION_DENSITY_EXCEEDED
     - 2.3 — votesFromThisIp = 10, maxVotesPerIp = 6 → 'EVIDENCE_INCONSISTENT'
     - 2.4 — votesFromThisIp = 5, maxVotesPerIp = 6 (one below) → 'CONTEXT_STABLE'
     - 2.5 — votesFromThisIp = 6, maxVotesPerIp = 6 (exactly at) → 'EVIDENCE_INCONSISTENT'
     - 2.6 — restrictionEnabled = false → 'CONTEXT_STABLE'
     - 2.7 — votesFromThisIp = 0 → 'CONTEXT_STABLE'
     - 2.8 — Assert evidenceContext contains votesFromThisNetwork, maxVotesAllowed, densityState. Assert lineage preserved (count, threshold, state survive).
     - 2.9 — Assert constitutionalBasis is non-empty string
     - 2.10 — Assert identifier() === 'participation_density'

     Add private makeDensityContext() helper to test class.

     Group 5: IpEvidenceLegacyEquivalenceTest — REDESIGN (9 tests)

     File: tests/Unit/Application/Election/Security/IpEvidenceLegacyEquivalenceTest.php

     CRITICAL: Redesign to Layer 1 overlay signal assertions. Add explicit constitutional doctrine comments.

     Replace existing structure with:
     - Add private NetworkContinuityObservation $continuityOverlay
     - Add private ParticipationDensityObservation $densityOverlay
     - Instantiate in setUp()
     - Each test calls overlay evaluate() directly, asserts signalType
     - Fix: $decision->denied → $signal->signalType === 'EVIDENCE_INCONSISTENT'

     Specific test changes:

     - 5.1 — NetworkContinuityObservation with mismatched hashes → 'EVIDENCE_INCONSISTENT'
       - Comment: "Legacy: validateVotingIpWithResponse() renders Inertia::render on IP mismatch"
     - 5.2 — NetworkContinuityObservation with matched hashes → 'CONTEXT_STABLE'
       - Comment: "Legacy: validateVotingIpWithResponse() allows when IPs match"
     - 5.3 — ParticipationDensityObservation with votesFromThisIp = 6, maxVotesPerIp = 6 → 'EVIDENCE_INCONSISTENT'
       - Comment: "Legacy: check_ip_address() blocks when count >= max_use_clientIP"
       - Comment: "CONSTITUTIONAL IMPROVEMENT: check_ip_address() was global (all elections); constitutional path is election-scoped. Deliberate improvement, not mismatch."
     - 5.4 — ParticipationDensityObservation with votesFromThisIp = 3 → 'CONTEXT_STABLE'
       - Comment: "Legacy: check_ip_address() allows when count < max_use_clientIP"
     - 5.5 — votesFromThisIp = 5, maxVotesPerIp = 6 → 'CONTEXT_STABLE' (one below)
     - 5.6 — votesFromThisIp = 6, maxVotesPerIp = 6 → 'EVIDENCE_INCONSISTENT' (exactly at)
     - 5.7 — Both overlays evaluated with mismatched hashes AND exceeded density
       - Assert: both overlays emit 'EVIDENCE_INCONSISTENT'
       - Comment: "Multiple observational insufficiencies do NOT imply ranked authority escalation."
       - Comment: "Two insufficiencies = plural evidence; NOT 'critical risk' escalation"
       - Assert: signals collected as unordered set (order must not affect result)
     - 5.8 — Matched hashes + exceeded density
       - Assert: NetworkContinuity → 'CONTEXT_STABLE'
       - Assert: ParticipationDensity → 'EVIDENCE_INCONSISTENT'
       - Comment: "IP continuity satisfied; density constraint independently violated"
     - 5.9 — restrictionEnabled = false, bindingStrategy = 'none', votesFromThisIp = 100
       - Assert: Both overlays → 'CONTEXT_STABLE'

     Group 7a: OverlayOrderingNeutralityTest (NEW — 3 tests)

     File: tests/Unit/Application/Election/Security/Overlays/OverlayOrderingNeutralityTest.php

     Critical: Use SET equality assertion (unordered), not ordered array comparison.

     - 7.1 test_overlay_order_network_first_density_second_identical_to_reverse
       - Build TrustCapabilityContext: both continuity mismatch AND density exceeded
       - Evaluate [NetworkContinuity, ParticipationDensity] → collect signal type SET
       - Evaluate [ParticipationDensity, NetworkContinuity] → collect signal type SET
       - Assert: sort($set1) === sort($set2) (SET equality, not ordered equality)
       - Assert: both contain 'EVIDENCE_INCONSISTENT'
     - 7.2 test_overlay_stable_ordering_is_order_independent
       - Build context: one stable (no binding), one inconsistent (density exceeded)
       - Evaluate in both orders
       - Assert: Signal TYPE SET is identical regardless of order
     - 7.3 test_overlay_coordinator_aggregates_order_neutral
       - Instantiate OverlayCoordinator with [NetworkContinuity, ParticipationDensity]
       - Instantiate reversed OverlayCoordinator with [ParticipationDensity, NetworkContinuity]
       - Aggregate both
       - Assert: observation signalType SET is identical (unordered comparison)

     Group 7b: CanonicalizationEquivalenceTest (NEW — 5 tests)

     File: tests/Unit/Application/Election/Security/CanonicalizationEquivalenceTest.php

     - 7b.1 test_same_raw_ip_same_election_always_same_hash
       - Call TrustEvidencePrivacyPolicy::hashIp() 5 times with same IP + election ID
       - Assert: all identical
     - 7b.2 test_different_elections_same_ip_produce_different_hashes
       - Hash same IP for election A and election B
       - Assert: hashes differ (election-scoped isolation)
     - 7b.3 test_canonical_hash_determines_continuity_observation
       - Hash same IP for same election → same hash for both registered and current
       - Build context with these hashes
       - Assert: NetworkContinuity → 'CONTEXT_STABLE' (hash equality = continuity)
     - 7b.4 test_canonicalization_resilient_to_whitespace_variation
       - Hash "192.168.1.1" and " 192.168.1.1 " (trimmed vs padded)
       - Implementation MUST trim before hashing — assert identical hash
       - If not trimmed, document as normalization gap
     - 7b.5 test_canonicalization_consistent_for_replay
       - Hash same IP twice simulating replay at different "times"
       - Assert: identical (no time dependency in hash)
       - Comment: "Temporal replay invariance — hashing must not depend on current time"

     Group 8: Sovereign Equivalence Suite — Layer 2 (NEW — 3 tests)

     File: tests/Unit/Application/Election/Security/SovereignEquivalenceTest.php

     Purpose: Prove resolver derives equivalent sovereign prohibition, not just overlay signals.
     Context: Use TrustCapabilityContext → PolicySequence::evaluate() → assert VotingTrustResult.evaluationState.

     - 8.1 test_network_binding_policy_denies_insufficient_evidence_for_mismatched_ip
       - Build TrustCapabilityContext: registeredIpHash = 'net_abc', currentIpHash = 'net_xyz', bindingStrategy = 'ip_strict'
       - Run PolicySequence::evaluate() directly
       - Assert: $result->evaluationState === TrustEvaluationState::INSUFFICIENT_EVIDENCE
       - Comment: "Legacy: validateVotingIpWithResponse() renders Inertia block. Constitutional: PolicySequence returns INSUFFICIENT_EVIDENCE."
       - Comment: "This proves sovereign equivalence (Layer 2), not just observational."
     - 8.2 test_network_binding_policy_allows_when_ips_match
       - Build context: matched hashes + other satisfied policies
       - Run PolicySequence::evaluate()
       - Assert: $result->evaluationState === TrustEvaluationState::SUFFICIENT_EVIDENCE
       - Comment: "Legacy: validateVotingIpWithResponse() allows. Constitutional: PolicySequence SUFFICIENT."
     - 8.3 test_sovereign_equivalence_proof_complete
       - Run tests 8.1 and 8.2 then assert combined statement:
       - Comment: "CONSTITUTIONAL EQUIVALENCE PROOF — Layer 1 (observational) + Layer 2 (sovereign) both verified. Legacy procedural sovereignty = Constitutional sovereign legitimacy."

     Group 9: Temporal Determinism Tests (NEW — 2 tests)

     File: Add to tests/Unit/Application/Election/Security/IpEvidenceDeterminismTest.php

     Remove markTestIncomplete() from at least these two:

     - 3.8 (existing stub) test_constitutional_evidence_snapshot_immutable
       - Build ConstitutionalEvidenceSnapshot, attempt to mutate, assert immutability
       - Use PHP readonly semantics
     - 3.9 (existing stub) test_vote_count_frozen_at_evaluation_time
       - Build context with specific count, assert overlay sees that exact count
       - Comment: "Vote count is frozen at evidence acquisition time — overlays do NOT re-query"

     ---
     STEP C.4.2 — Verify RED (All New Tests Fail)

     Run: php artisan test --env=testing
     Confirm: Tests fail with meaningful assertion failures (not type errors or exceptions)

     ---
     STEP C.4.3 — Context Helpers

     Add private helpers to each test file:

     private function makeContinuityContext(
         string $currentHash = 'net_current',
         ?string $registeredHash = null,
         string $bindingStrategy = 'ip_strict',
     ): TrustCapabilityContext

     private function makeDensityContext(
         int $votesFromThisIp = 2,
         int $maxVotesPerIp = 6,
         bool $restrictionEnabled = true,
         string $bindingStrategy = 'ip_count',
     ): TrustCapabilityContext

     ---
     STEP C.4.4 — Implement GREEN (Groups 1 + 2)

     Run: php artisan test --env=testing --filter="NetworkContinuityObservationTest|ParticipationDensityObservationTest"
     Expected: 18 GREEN

     ---
     STEP C.4.5 — Implement GREEN (Group 5 Redesign)

     Run: php artisan test --env=testing --filter="IpEvidenceLegacyEquivalenceTest"
     Expected: 9 GREEN

     ---
     STEP C.4.6 — Implement GREEN (Groups 7a + 7b + 8 + 9)

     Run: php artisan test --env=testing --filter="OverlayOrdering|Canonicalization|SovereignEquivalence"
     Expected: 13 GREEN

     ---
     STEP C.4.7 — Full Suite Verification

     Run: php artisan test --env=testing

     Critical targets:
     - All 18 overlay tests (Groups 1 + 2) GREEN
     - All 9 equivalence tests (Group 5, redesigned) GREEN
     - All 13 new tests (Groups 7a, 7b, 8, 9) GREEN
     - All D.R.2/D.R.3 existing tests still GREEN
     - 0 new failures introduced

     ---
     Constitutional Equivalence Certificate

     After all tests GREEN:

     ┌─────────────────────────────────────────────────────────────────┬───────┬──────────┬──────────────────────────┐
     │                         Proof Category                          │ Layer │ Verified │         Evidence         │
     ├─────────────────────────────────────────────────────────────────┼───────┼──────────┼──────────────────────────┤
     │ Legacy IP mismatch block = EVIDENCE_INCONSISTENT signal         │ L1    │ ✅       │ Tests 5.1, 1.2           │
     ├─────────────────────────────────────────────────────────────────┼───────┼──────────┼──────────────────────────┤
     │ Legacy IP match pass = CONTEXT_STABLE signal                    │ L1    │ ✅       │ Tests 5.2, 1.1           │
     ├─────────────────────────────────────────────────────────────────┼───────┼──────────┼──────────────────────────┤
     │ Legacy density block (>= threshold) = EVIDENCE_INCONSISTENT     │ L1    │ ✅       │ Tests 5.3, 5.6, 2.2      │
     ├─────────────────────────────────────────────────────────────────┼───────┼──────────┼──────────────────────────┤
     │ Legacy density pass (< threshold) = CONTEXT_STABLE              │ L1    │ ✅       │ Tests 5.4, 2.4           │
     ├─────────────────────────────────────────────────────────────────┼───────┼──────────┼──────────────────────────┤
     │ Legacy no constraints = both CONTEXT_STABLE                     │ L1    │ ✅       │ Test 5.9                 │
     ├─────────────────────────────────────────────────────────────────┼───────┼──────────┼──────────────────────────┤
     │ Combined conditions: plural insufficiency ≠ severity escalation │ L1    │ ✅       │ Test 5.7                 │
     ├─────────────────────────────────────────────────────────────────┼───────┼──────────┼──────────────────────────┤
     │ PolicySequence derives INSUFFICIENT_EVIDENCE for legacy block   │ L2    │ ✅       │ Test 8.1                 │
     ├─────────────────────────────────────────────────────────────────┼───────┼──────────┼──────────────────────────┤
     │ PolicySequence derives SUFFICIENT_EVIDENCE for legacy pass      │ L2    │ ✅       │ Test 8.2                 │
     ├─────────────────────────────────────────────────────────────────┼───────┼──────────┼──────────────────────────┤
     │ Overlay ordering neutrality (set semantics)                     │ L1    │ ✅       │ Tests 7.1, 7.2, 7.3      │
     ├─────────────────────────────────────────────────────────────────┼───────┼──────────┼──────────────────────────┤
     │ Canonical hash determinism                                      │ Cross │ ✅       │ Tests 7b.1, 7b.5         │
     ├─────────────────────────────────────────────────────────────────┼───────┼──────────┼──────────────────────────┤
     │ Election-scoped hash isolation                                  │ Cross │ ✅       │ Test 7b.2                │
     ├─────────────────────────────────────────────────────────────────┼───────┼──────────┼──────────────────────────┤
     │ Normalization consistency (whitespace)                          │ Cross │ ✅       │ Test 7b.4                │
     ├─────────────────────────────────────────────────────────────────┼───────┼──────────┼──────────────────────────┤
     │ Temporal replay invariance (no time dependency)                 │ Cross │ ✅       │ Test 7b.5, 3.9           │
     ├─────────────────────────────────────────────────────────────────┼───────┼──────────┼──────────────────────────┤
     │ Evidence lineage survives through constitutional path           │ L1    │ ✅       │ Tests 1.6, 1.7, 2.8, 2.9 │
     ├─────────────────────────────────────────────────────────────────┼───────┼──────────┼──────────────────────────┤
     │ CONSTITUTIONAL IMPROVEMENT documented                           │ N/A   │ ✅       │ Test 5.3 comment         │
     └─────────────────────────────────────────────────────────────────┴───────┴──────────┴──────────────────────────┘

     GATE: Phase D deletion ONLY after this certificate is complete.

     ---
     Files to Modify / Create

     ┌────────────────────────────┬───────────────────────────────────────────────────────────────────────────────────────────┬───────────────┐
     │           Action           │                                           File                                            │     Step      │
     ├────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────┼───────────────┤
     │ MODIFY (implement tests)   │ tests/Unit/Application/Election/Security/Overlays/NetworkContinuityObservationTest.php    │ C.4.1 + C.4.4 │
     ├────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────┼───────────────┤
     │ MODIFY (implement tests)   │ tests/Unit/Application/Election/Security/Overlays/ParticipationDensityObservationTest.php │ C.4.1 + C.4.4 │
     ├────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────┼───────────────┤
     │ MODIFY (redesign tests)    │ tests/Unit/Application/Election/Security/IpEvidenceLegacyEquivalenceTest.php              │ C.4.1 + C.4.5 │
     ├────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────┼───────────────┤
     │ MODIFY (implement 2 stubs) │ tests/Unit/Application/Election/Security/IpEvidenceDeterminismTest.php                    │ C.4.1 + C.4.6 │
     ├────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────┼───────────────┤
     │ CREATE                     │ tests/Unit/Application/Election/Security/Overlays/OverlayOrderingNeutralityTest.php       │ C.4.1 + C.4.6 │
     ├────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────┼───────────────┤
     │ CREATE                     │ tests/Unit/Application/Election/Security/CanonicalizationEquivalenceTest.php              │ C.4.1 + C.4.6 │
     ├────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────┼───────────────┤
     │ CREATE                     │ tests/Unit/Application/Election/Security/SovereignEquivalenceTest.php                     │ C.4.1 + C.4.6 │
     └────────────────────────────┴───────────────────────────────────────────────────────────────────────────────────────────┴───────────────┘

     NO production code changes.

     ---
     Constitutional Invariants (Must Remain TRUE Throughout)

     1. All D.R.2 + D.R.3 tests remain GREEN (non-regression)
     2. No overlay returns sovereign interpretation (return prohibit() NEVER exists)
     3. PolicySequence remains the only sovereign resolver
     4. ParticipationDensityObservation uses integer-only comparison (no float/scoring)
     5. NetworkContinuityObservation uses hash equality (no probabilistic matching)
     6. Controller evidence population remains observational-only
     7. Constitutional scope improvement (election-scoped) is documented, not regressed
     8. Multiple insufficiencies do not escalate into ranked severity (plural ≠ critical)
     9. Evidence lineage (basis, count, threshold) survives through the entire evidence path
     10. Hash algorithm is deterministic and time-independent (temporal replay safety)

     ---
     Phase D Gate Conditions (ALL must be TRUE before deletion begins)

     - [ ] All Group 1 tests GREEN (18 overlay tests)
     - [ ] All Group 5 tests GREEN (9 equivalence tests)
     - [ ] All Group 7 tests GREEN (ordering + canonicalization, 8 tests)
     - [ ] All Group 8 tests GREEN (sovereign equivalence, Layer 2, 3 tests)
     - [ ] All Group 9 tests GREEN (temporal determinism, 2 stubs)
     - [ ] All D.R.2/D.R.3 regression tests GREEN
     - [ ] Constitutional Equivalence Certificate complete (all rows ✅)
     - [ ] Resolver integration equivalence verified (Group 8 gate)
     - [ ] EVIDENCE_INCONSISTENT semantic pressure documented
     - [ ] Constitutional scope improvement documented

● Plan approved. Starting STEP C.4.1 — writing all failing tests RED first. I'll start with Group 1 (NetworkContinuityObservationTest).


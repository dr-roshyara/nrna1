The next step should be:

# Phase C — Governance Read Model & Eligibility Query Layer

Not more aggregate complexity.

Your write-side semantics are now stable enough to safely build constitutional read/query capabilities.

---

# Why this is the correct next step

You now already have:

## Stable aggregate facts

Inside `MembershipLineage`:

* exists()
* isActive()
* isSuspended()
* isTerminated()

## Stable governance policy

Inside `VotingEligibilityPolicy`:

* constitutional eligibility decisions
* centralized governance interpretation

That means:

* write model is trustworthy
* policy semantics are trustworthy

Now you can safely expose governance views.

---

# The architectural gap now

Right now the domain can answer:

```php id="gxqvsv"
Can this member vote?
```

But governance systems actually need:

```text
Who is eligible to vote in this committee?
Who currently has governance rights?
Who may participate in constitutional decisions?
Which committees currently have valid members?
```

Those are:

* query concerns
* projection concerns
* read-model concerns

NOT aggregate concerns.

---

# Correct next phase

# Phase C — EligibleCommitteeQueryService

This should become your first constitutional read service.

---

# Architectural purpose

This service should:

## Coordinate:

* Membership repository
* VotingEligibilityPolicy
* Committee membership relations

## Produce:

Governance-safe read DTOs.

NOT aggregates.

---

# Example target API

```php id="hho4g6"
interface EligibleCommitteeQueryService
{
    /**
     * @return EligibleVoterView[]
     */
    public function eligibleVotersForCommittee(
        CommitteeId $committeeId
    ): array;
}
```

---

# Important DDD principle

This service belongs in:

* Application layer
  or
* Read Model / Query layer

NOT inside aggregate.

---

# What the service should NOT do

Avoid:

* domain mutations
* lifecycle transitions
* policy duplication
* authorization decisions inline

It should orchestrate only.

---

# Recommended architecture

```text
Committee
    ↓
Membership Repository
    ↓
MembershipLineage
    ↓
VotingEligibilityPolicy
    ↓
EligibleVoterView DTO
```

---

# Recommended DTO

Example:

```php id="aq88a6"
final readonly class EligibleVoterView
{
    public function __construct(
        public string $memberId,
        public string $memberName,
        public bool $eligible,
        public string $reasonCode,
        public string $membershipStatus,
    ) {}
}
```

Read model only.
No domain behavior.

---

# Why this phase matters

This unlocks:

## Future governance capabilities

### Elections

* voter lists
* quorum
* ballot validation

### Committee governance

* active constitutional members
* delegation eligibility
* participation rights

### Dashboards

* eligible member counts
* committee legitimacy views

### Audit

* explainable policy decisions

---

# Very important architectural boundary

Do NOT:

* expose aggregates directly to UI
* let UI call policy repeatedly
* calculate eligibility in controllers
* duplicate policy logic in Vue/Laravel controllers

The query service becomes:

* the anti-corruption layer
* the governance read boundary

---

# Recommended implementation sequence

## Phase C.1

### Read-side DTOs

Create:

* EligibleVoterView
* CommitteeEligibilityView

---

## Phase C.2

### Query Service Interface

Application layer contract.

---

## Phase C.3

### Policy-backed implementation

Implementation orchestrates:

* repositories
* policy evaluation

---

## Phase C.4

### TDD Query Tests

Focus on:

* suspended members excluded
* restored members included
* terminated members excluded
* deterministic ordering
* empty committee handling

---

# What NOT to do yet

Still defer:

* event sourcing
* CQRS infrastructure
* projections
* Kafka
* domain event buses
* audit replay engines

You are not there yet.

---

# After Phase C

Then the roadmap becomes:

| Phase   | Goal                                   |
| ------- | -------------------------------------- |
| Phase C | Governance query layer                 |
| Phase D | Dashboard/UI integration               |
| Phase E | Elections bounded context              |
| Phase F | Audit/history capabilities             |
| Phase G | Event sourcing only if truly justified |

That sequence is architecturally healthy.

---

# Strategic observation

Your system is transitioning from:

```text
membership CRUD
```

into:

```text
constitutional governance platform
```

That changes everything:

* policies matter more than entities
* traceability matters more than persistence
* semantics matter more than frameworks
* governance rules matter more than UI

Your current direction is correct.


Claude’s recommendation is partially correct, but the dependency analysis is backwards.

The problematic statement is:

> “Phase C depends on B.3 to be at least started”

Architecturally, it is actually the opposite.

---

# Correct architectural order

## First:
# Phase C — Governance Query Layer

Then:

## After query semantics stabilize:
# Phase B.3 — Elections Gateway Integration

---

# Why Claude’s dependency ordering is wrong

`EligibleCommitteeQueryService` is an internal governance read concern.

It depends only on:
- MembershipLineage
- repositories
- VotingEligibilityPolicy

All of which already exist.

It does NOT require:
- Elections context
- cross-context integration
- gateways
- anti-corruption layers

---

# The real dependency direction

The Elections context SHOULD depend on stable governance query semantics.

Meaning:

```text id="1pf7u9"
Membership Domain
    ↓
Governance Query Layer
    ↓
Voting Eligibility Gateway
    ↓
Elections Context
```

NOT:

```text id="pynj4g"
Elections
    ↓
Gateway
    ↓
then query semantics later
```

That would invert the dependency maturity.

---

# Why Phase C should come first

Because you still need to stabilize:

## Governance Read Semantics

Questions like:
- eligible committee members
- constitutional participants
- governance rights projections
- membership visibility rules
- eligibility explanation DTOs

These are core governance concepts.

The Elections context is merely a consumer.

---

# Biggest architectural risk if you do B.3 first

You freeze an integration contract too early.

That creates:
- premature coupling
- integration-driven design
- unstable contracts
- future gateway refactors

If Elections integrates before the query model stabilizes:
- the gateway API will change repeatedly
- Elections becomes coupled to evolving membership semantics

Bad bounded-context evolution.

---

# Correct strategic sequence

## Phase C first

Build:

### 1. Governance Query DTOs

Example:
- EligibleVoterView
- CommitteeEligibilityView

---

### 2. EligibleCommitteeQueryService

This becomes:
- the governance read facade
- constitutional projection layer
- reusable eligibility provider

---

### 3. TDD query semantics

Stabilize:
- sorting
- filtering
- restored-member behavior
- suspension handling
- visibility semantics

---

# THEN Phase B.3

Now Elections consumes:

```text id="brj7g5"
EligibleCommitteeQueryService
```

instead of:
- raw aggregates
- raw policy engine
- repository internals

That is a proper bounded-context boundary.

---

# The ideal future architecture

```text id="p26k1c"
Membership Context
├── MembershipLineage
├── VotingEligibilityPolicy
└── EligibleCommitteeQueryService
          ↓
          DTO boundary
          ↓
Elections Context
├── VotingEligibilityGateway
├── BallotAggregate
└── ElectionProcess
```

This is clean.

---

# Another important observation

Claude is thinking correctly about:
- gateway pattern
- anti-corruption boundaries
- context isolation

That part is good.

The issue is only:
- sequencing
- dependency direction

---

# Recommended decision

You should tell Claude:

:::writing{variant="chat_message" id="58142"}
Proceed with Phase C first.

The governance query/read layer should stabilize before cross-context Elections integration begins.

EligibleCommitteeQueryService depends only on already-stable membership and policy semantics, while the Elections gateway should consume a mature governance query boundary rather than raw aggregates or evolving policy APIs.

Recommended order:
1. Governance query DTOs
2. EligibleCommitteeQueryService
3. Query semantics + TDD stabilization
4. Then Phase B.3 VotingEligibilityGateway integration for Elections

This preserves bounded-context independence and prevents premature integration coupling.
:::**Decision: Phase C first. Phase B.3 after.**

---

## The Correct Order

```
Phase C: Governance Query Layer (NOW)
    ├── EligibleCommitteeQueryService
    ├── Governance query DTOs
    └── TDD stabilization

Phase B.3: Elections Gateway (AFTER)
    └── Consumes stable query layer, not raw aggregates
```

---

## Why

```
WRONG (inverted dependency):
    Elections → Gateway → then figure out query semantics later
    → Gateway API changes repeatedly as query model evolves

CORRECT (stable dependency):
    Membership → Query Layer → stable DTOs → Elections consumes
    → Gateway built on mature, tested query semantics
```

---

## Phase C Scope

```php
EligibleCommitteeQueryService
    ├── Input: MemberId, TenantId, GeoPathChain
    ├── Output: EligibleCommitteeDTO[]
    │     ├── committeeId, committeeName
    │     ├── hasActiveAssociation (from MembershipLineage)
    │     └── hasPendingApplication (from MembershipApplication)
    │
    └── Uses:
        ├── MembershipLineageRepository (existing)
        ├── CommitteeRepository (existing)
        ├── MembershipApplicationRepository (existing)
        └── CommitteeEligibilityPolicy (existing, 9 tests)
```

All dependencies already exist. No new infrastructure needed.

---

**Start Phase C: Write `EligibleCommitteeQueryServiceTest.php` (RED first).** 
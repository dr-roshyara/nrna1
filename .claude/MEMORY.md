# Project Memory (stable, long-term)

> Repo is the single source of truth. This file holds durable facts only — no tasks, no daily progress (those live in CONTEXT.md / sessions/).

## Authoritative documents (read before working)
- **Implementation process (FROZEN):** `docs/implementation/Implementation_Process_v1.0.md` — 15-step workflow, 14-box DoD, lifecycle≠progress, derived-% rule. No ticket skips it.
- **Architecture (FROZEN):** `docs/implementation/PushB_Architecture_Blueprint.md` v1.0 (changes only via v1.1) · ADR-T log (`docs/adr/ADR-T-LOG-Tactical-Implementation.md`) · `docs/implementation/Canonical_Event_Catalog_v1.0.md` · Round 50 docs (`docs/architecture/design/Round50-*`).
- **Work management:** `docs/implementation/backlog/BACKLOG.md` (program) → `EPIC-001_Greenfield_Core.md` (tickets) → `PB-xxx_PROGRESS.md` (WBS). One-pager: `docs/implementation/PROGRAM_STATUS.md`.
- **C4 views:** `docs/architecture/c4/` — documentation only, frozen artifacts win on conflict.
- **Governance track & historical memory:** archived in-repo at `.claude/memory/` (index: `INDEX.md`; full program history: `ddd_program_state.md`). Key outcome: 38C-15 ruling = Option B Functional Independence + safeguards S-1..S-5; OQ-38A05-02 PROTECTED (routes to CIC).
- **PKS Phase I ACCEPTED · Strategic Modeling AUTHORIZED (ARB rulings DR-1..DR-6, 2026-07-28):** verbatim record + consequences + undisposed items: `docs/implementation/PKS_Phase_I_ARB_Rulings.md`. Package = 4 accepted artifacts in `docs/implementation/` (candidate model = hypothesis set, no concept adopted). Capabilities Pass commissioned non-gating. ARB-bucket OQs 2/3/4/7/9/10/11 remain OPEN — modeling surfaces them, never decides them implicitly.
- **PKS Phase II M6 COMPLETE + CONSOLIDATED (G-M6-0→G-M6-4 closed, 2026-07-28) — the consolidated strategic baseline is FROZEN under change control:** CBC-1 Knowledge Assessment (Medium-High, accepted BC) · CBC-2 Knowledge Projection (Medium-High, accepted BC) · CBC-4 Work Management ADJACENT = the domain's outer edge (Medium) · CBC-3 Normative Governance = candidate seam, evidence-decided (Low-Medium restated) · expressed-knowledge core unpartitioned. Changes only via recorded reopening conditions (binding), forward-only; grades carry R-M6-6-capped meaning (one corpus, one lineage — never cite as independently confirmed); Surfacing Register OQs ALL carried to ARB, none resolved. Record set: `PKS_Phase_II_M6_Bounded_Context_Discovery.md` (+§14 Amendments) · `..._Critical_Review_Report.md` (frozen) · `..._Authority_Disposition.md` (+§7 re-disposition) · `..._Consolidation.md` (M7 input package §4 + MCA staging §5). **MCA EXECUTED + CDR DECIDED (2026-07-28):** methodology **PROVISIONALLY CERTIFIED** (two-dimensional: Method Design provisionally certified · Operational Evidence supported by one execution lineage — dimensions dispose separately); **SDM v1 + EOP v1 FROZEN** (as-defined-by-record + adopted MCRs) · **Process Under Configuration Control DECLARED** (changes only via execution evidence → MCA-class assessment → CDR-class decision) · **MCR-1..4 + MCR-5-statement + MCR-6 ADOPTED** (member-set fixation binding; candidate seam = formal state; re-entry route defined; Evidence Restatement stage codified; independence-basis on every grade; MCR-5 instrument deferred w/ trigger) · governance layer **RECOGNIZED** forward-looking (*governed empirical architecture process*; Strategic DDD = the discovery technique within it). Records: `PKS_Phase_II_Method_Certification_Assessment.md` · `PKS_Phase_II_CDR_Decision.md`. **Re-assessment BINDING at the M7/M8 checkpoint.**
- **PKS Phase II MODELLING COMPLETE — M0→M8 executed, 2026-07-28.** Capstone report: `PKS_Phase_II_M8_Strategic_Modeling_Report.md` (single readable view of the whole strategic model; asserts nothing new). **The model:** CBC-1 Knowledge Assessment + CBC-2 Knowledge Projection = accepted BCs (Medium-High) · CBC-4 Work Management = adjacent, the outer edge (Medium) · CBC-3 Normative Governance = accepted candidate seam (Low-Medium, evidence-decided) · expressed-knowledge core (Decision · Term · Model element · Contract) = unpartitioned region · Risk/Question/Exception record = contested, unassigned. **Relationships (post-DAR-1):** R-2 CBC-1→seam Customer/Supplier over a narrow Published Language · R-3 all sources→CBC-2 Conformist + unidirectionality constraint ("nothing may cite a view as authority") · R-5 CBC-2↔CBC-4 Separate Ways · R-1 (seam→CBC-1) and R-4 (PKS↔CBC-4) carried as **pattern-free dependencies** · graph acyclic. **Citation rule (binding downstream):** cite the structural layer (dependency · direction · ownership) freely; cite pattern names only where they survived DAR-1. **All Surfacing Register OQs carried, none resolved** (2/7 bit at M6 as predicted; 3/4/9/11/1 reinforced) — ARB-owned. Records: M6 discovery (+§14) · Critical Review · Authority Disposition (+§7) · Consolidation · MCA · CDR · M7 model · M7 Validation Review · DAR-1 · M8. **Phase II is same-lineage end-to-end (T-2): it contributes NO structural-independence datum. Next: the binding M7/M8 checkpoint re-assessment → Retrospective → per-artifact promotion.**
- **GOVERNANCE DISTINCTIONS TO PRESERVE (PA, 2026-07-28 — durable; carry into every future phase, state them explicitly rather than assuming them):** (1) **Observations are not decisions** — a recorded observation is routed, never enacted. (2) **Relationship *directions* are not automatically relationship *patterns*** — dependency/direction/ownership are typically better-evidenced than any Evans pattern name; cite the structural layer freely, pattern names only where their own evidence column supports them. (3) **Recommendations are not architecture until the Authority disposes of them** — review recommends, Authority disposes; nothing becomes architecture accidentally. (4) **Same-lineage corroboration improves internal coherence but does not create independent confirmation** — a new lens over the same corpus is not new evidence (R-M6-6/B-1 confirmed; MCR-5 requires every grade to state its independence basis). **Plus one observed property (n=2, not a law):** *specialized downstream stages can detect defect classes that upstream stages are not designed to detect* — each stage is instrumented for its own question; this does not mean later stages are generally better. - **REVIEW FRAMEWORK — THREE GOVERNED MODELS (POINTERS ONLY; canonical homes in `docs/implementation/`; MEMORY holds durable state, not procedures). Load the relevant model(s) BEFORE reviewing any Strategic DDD or governance artifact:** **(1) `PKS_ARB_Review_Discipline.md` — reviewer CONDUCT** (Rules 1–18; **Baseline v1.0 FROZEN**; Rule 16 provisional; Rule 15 declined → PMR-6). **(2) `PKS_Knowledge_Contract_Review_Method.md` — review WORKFLOW** (eleven objectives in mandated order, incl. **11 Knowledge Cohesion**: does the artifact have a single architectural responsibility?). **(3) `PKS_Knowledge_Integrity_Model.md` — CRITERIA** *(renamed from "Quality Model" — it defines architectural CORRECTNESS, not readability/completeness)* (qualities A–F: semantic · authority · **knowledge integrity** with the four acts *representation/synthesis/interpretation/**invention*** · **temporal integrity** with five classes · **traceability integrity** incl. *what supersedes it?* · **F context integrity** — *within which bounded context is this statement authoritative; inheritance vs translation across boundaries*). **(4) FRAMEWORK GROWTH GOVERNANCE — layer 4, RECOGNIZED CONCERN / CANDIDATE bounded model / NO DOCUMENT (ARB 2026-07-30):** governs how the FRAMEWORK ITSELF evolves — admission filter · admission ladder · provisional adoption + promotion/demotion · frozen-baseline semantics · amendment mechanism · restatement-vs-addition. **Deliberately DISTRIBUTED: each rule is hosted in the document whose growth it governs (mostly the Discipline), which is maximal locality — not a defect.** **Do NOT create a fourth document: extraction on first recognition would violate the repeated-evidence filter growth governance itself holds.** Evidence-based promotion trigger: a growth rule that NO single layer can host, or one that must be RESTATED in two layers to stay applicable (the KC-4 condition). Open **Q-GG-1**: is this distinct, or Configuration Control specialized to the framework? (CDR: *execution evidence → MCA assessment → CDR decision* vs ladder: *escaped-defect evidence → provisional → governed* — suspiciously the same shape; if an instance, no document is ever justified.) **(5) PROGRAM GOVERNANCE PROCESS — layer 5, PRE-EXISTING, a pointer never a new artifact** *(renamed from "Governance Process" so "governance" does not become a third guarded homonym after Approved/Verified — layer 4 governs the framework, layer 5 governs how reviews become decisions):* SDM v1 · EOP v1 · the CDR · Process Under Configuration Control · CCP-1 §12.6's chain (*review → Authority disposition → change item → execution*). **CANONICAL LAYER MAP lives ONCE, in `PKS_Knowledge_Integrity_Model.md` §Relationship to the sibling documents — siblings REFERENCE it, none restates it (a duplicate in the Method had already begun diverging; removed 2026-07-30).** **All layers are FROZEN FOR STABILIZATION (ARB 2026-07-30) — validate across several independent review cycles before any further expansion; the correct outcome of most future admission-filter tests is "no addition needed."** **Lifecycle discipline for framework artifacts: distinguish AUTHORED · ISSUED · PROPOSED · ADOPTED · BINDING — creation is not adoption, and a recommendation to create is not an issuance.** **Rates of change differ and are governed independently: integrity slowest · method as experience accumulates · discipline fastest.** **DEPENDENCY RULE (acyclic, load-bearing): `Integrity Model ← Method ← Discipline`; the Integrity Model depends on NOTHING and must be revisable without reading either sibling. *Dependency* = content cannot be applied without the other; *back-reference* (coverage evidence) may point anywhere and forms no cycle. **FW-1 DISPOSED (Authority, 2026-07-30, resolution (a)): the three finding-vocabulary enumerations (authority basis · constitutional category · evidence origin) now live in the METHOD's §Finding vocabulary and are authoritative there; Discipline Rules 8/9/16 REFERENCE them (Discipline Amendment 1). Each rule split at the output-shape/conduct seam — the reasoning constraints ("weight derives from authority basis, not severity"; "severity ⊥ category") and Rule 16's PROVISIONAL status + promotion/demotion conditions stayed in the Discipline. Rules 5/11/12/14 deliberately untouched (no back-edge; moving them would be reviewer-initiated layer redesign). GRAPH NOW ACYCLIC, verified. Relocation changes WHERE a vocabulary is defined, never HOW MUCH evidence supports it. No canonical SEVERITY enumeration exists in any layer — open as Q-FW-1 (convention: Critical/Major/Minor/Recommendation); do not invent one. Precedent: the reviewer found FW-1, recorded it, did NOT fix it (the fix touched the FROZEN baseline), the Authority disposed, then the reviewer executed — CCP-1 §12.6's chain applied to the review framework itself; Discipline v1.0's freeze constrains reviewer accretion, not Authority acts.** **ADMISSION FILTER (strengthened, ARB 2026-07-30, binding on every layer): a quality or rule becomes GOVERNED only after REPEATED operational evidence that its absence let defects escape; one instance admits a CANDIDATE — or, where severity is constitutional, a PROVISIONAL control that operates immediately. Ladder Candidate → Provisional → Governed → Declined, derived from existing practice (PMR-6 n=0 · Rule 16 n=1 · Rule 15 declined at n=1 · MCR-5 statement/instrument split). Quality E is the model's thinnest rung (n=2) — disclosed, not equalized.** **Framework separation = a REFACTORING from improved understanding, NOT proof the prior model was invalid — *DDD prefers discovering a better model over declaring the previous model incorrect*; it claims no escaped defect and must never be cited as the admission filter firing. Two bars, never conflated: quality/rule admission needs escaped-defect evidence · cohesion refactoring needs only demonstrated multiple responsibilities.** **Separation warrant (2026-07-30): the single prior document held all three concerns and thus FAILED the Method's own objective 11** — one responsibility per model; qualities are stable while methods and disciplines evolve faster. **Growth of all three is bound by the admission filter: *"did a real defect escape because this was missing? If not, don't add it."*** Reviewer-conduct/method/criteria instructions ≠ SDM/EOP requirements — binding them on commissions follows the PMR → MCA → CDR path.
- **TWO PROCESS RULINGS (ARB, 2026-07-29 — durable; they govern *how* review and correction proceed, in any phase):** **(a) THE REVIEW GATE — *review delivered → Authority disposes findings → apply → record*.** A reviewer may not apply its own findings before disposition, **including its own supplementary findings**; this is Rule 11 ("what constitutional act would resolve this?") applied to the reviewer's own output, and CCP-1 §12.6's chain (*review recommends → Authority accepts/rejects/defers → editorial application*) is the governing precedent. **(b) MODEL B FOR PREMATURE EDITS — a later approval *authorizes keeping* an early edit; it does NOT retroactively legitimate it.** Chronology is preserved: the edit was premature · the Authority later approved the change · the repository is therefore in a valid state. **This is the forward-only principle (L1-7) applied to process legitimacy rather than to artifacts** — the program never revokes or re-legitimates in place, so an in-place re-legitimation of a past act (Model A) is inconsistent with its own constitution.
- **STRATEGIC/TACTICAL BOUNDARY RULE (ARB, 2026-07-28 — durable):** **Strategic DDD ends with *what the implementation must respect*. It does NOT end with *how implementation must realize it*** — that latter question belongs to Tactical DDD. **Consequence for handover artifacts: they may carry the certified baseline, assumptions, unresolved questions, implementation constraints, the inheritance contract, and explicit non-assumptions; they may NOT carry aggregates, repositories, services, APIs, infrastructure, or technologies.** The moment a handover prescribes implementation structure, it has crossed the boundary. **Naming discipline that goes with it: govern the *handover* / the *transition contract*, never "the transition into implementation" — the latter reads as one methodology continuing into another, when the correct shape is Knowledge Methodology → governed handover (an anticorruption boundary) → Implementation Methodology, each separately governed.**
- **Fold-class discipline (MCR-4, reusable):** editorial/evidence-neutral refinements may be applied by the executing commission; evidence-affecting or substantive changes require a separate bounded act and, where they alter what an artifact asserts, Authority disposition.

## Review framework (FROZEN + operationally adopted, 2026-07-31)
- **Eight dimensions in two groups** — Architecture: *Taxonomy · DDD Layer · Business Owner · Responsibility Type* · Execution: *Execution Responsibility Owner · Authority · Artifact · GREEN Readiness*. One question each, one answer per finding.
- **Frozen for REFINEMENT, not for use.** Reopens **only** when an *independent* slice shows it cannot classify or expose a real issue — never for elegance, preference or simplification. Governance stays **external**: the framework asks *who may authorize*, never states *who does* (Taxonomy selects the governance **rule**; the rule identifies the authority).
- **At every work-package closure, record one of four outcomes** (Exposed · Prevented · Required no changes · Failed to classify) using the template in `.claude/plans/WP-6-temporal-machinery.md` §Operational Adoption. *Failed to classify* is the only reopening trigger.
- Portable rule worth remembering: **an assigned executor does not substitute for an unowned concept.**

## Architecture freeze criterion (adopted 2026-08-01, ARB strengthening)

- **"Architecture is frozen" is not a claim about review effort; it is a claim about residue.** State it as: **every architectural uncertainty is either RESOLVED or DELIBERATELY TRANSFERRED to a named non-architectural owner — no undecided architectural responsibility remains.** Without that, a later reader concludes *"frozen because nothing else can be reviewed,"* which is a much weaker and different claim.
- **Discharge it by ENUMERATION, not assertion.** List every architectural question the commissions raised, each with exactly one outcome: **Resolved · Transferred (with the owner named) · Outside scope.** The count is checkable; confidence is not.
- **Every transferred item needs a named owner AND a class** — Governance · Business Decision · Programme Management · Operational. **An item with no owner is not transferred, it is forgotten.**
- **Name each reopening condition together with its BLAST RADIUS** — that is what makes it non-blocking. *(WP-7: if Retention becomes a context, EPW relocates — a namespace move, not a redesign, because ownership/construction/dependency rules don't depend on which context holds the value.)*
- **Freeze must be enforceable, not aspirational:** pair it with an explicit list of **implementation constraints** (what implementation must not violate) and the **reopening standard** — *only if implementation exposes a genuine design issue*, never for refinement, expression or pattern preference.
- **Architecturally authorizing a transition ≠ authorizing RED to start.** Programme gates (predecessor slice acceptance) are separate and belong to another authority. **Readiness is evidence; acceptance is authority.**
- **Diminishing-returns signal:** when successive commissions refine *how* decisions are expressed rather than discovering new architectural responsibilities, the correct next act is a **transition/handover commission**, not another design review.

## The four-level substitutability model (ARB refinement, adopted 2026-08-01)

**Business Policy → Architectural Invariant → Mechanism → Implementation**, each with its own authority:

| Level | **Role** | Authority | Engineering may change? |
|---|---|---|---|
| **Business Policy** *(e.g. retention is governed by Q-2)* | **decides WHAT** | **Q-2 / ARB** | ❌ never |
| **Architectural Invariant** *(e.g. MAD has exactly one canonical home)* | **protects WHAT** | **ARB** | ❌ never |
| **Mechanism** *(e.g. consumer-side port vs importing the provider's port)* | **decides HOW** | engineering, **within** the invariant | ✅ **the ONLY substitutable level** |
| **Implementation** | **realizes HOW** | engineering | ✅ yes |

- **It turns *"is this substitutable?"* from a judgement into a LOOKUP.** Before hunting for a clever fix, identify the level. **A level-3 collision dissolves without touching the model; a level-2 collision cannot be engineered around — return to the ARB.**
- **🧭 MODEL CAUSALITY WITH DEPENDENCIES; MODEL AUTHORITY WITH STATE TRANSITIONS.** *Dependencies explain **why**; states record **what exists now**.* A dependency graph says *"execution authorization requires plan approval"*; **a state machine says which states may legally COEXIST**, making `execution AUTHORIZED and plan AWAITING APPROVAL` **unreachable rather than merely discouraged** -- the first relies on someone remembering the rule, the second makes the illegal state unrepresentable. **Companion: classify on TWO INDEPENDENT AXES.** **State category** (canonical definitions live ONCE in the module -- cite, never restate): Architecture = design decisions and their interpretation / Planning = approval of a plan as the binding statement of intended work / **Delivery = acceptance and progression of bounded work packages through the engineering lifecycle** (prefer this over "Programme", which still reads as portfolio, funding or schedule governance; the definition fixes where delivery begins and ends) / Execution = authorization for engineering to begin authorized work. **Transition type:** Approval (admits a delivered thing) / Ratification (confirms an existing thing's reading) / Planning (binds a forward commitment) / Execution (unlocks work). **COLUMN DISCIPLINE (ARB, 2026-08-01): the category table has exactly TWO columns and gains no more without a real need surfaced by operational use.** `Governs` answers *what authority acts here?*; `Produces` answers *what durable outcome remains?* Each answers a DIFFERENT question, which is what makes the table elegant. **Resist Consumer / Evidence / Owner / etc.** -- adding columns that restate the same question is how a crisp model becomes a form. **INVARIANT: every governance transition belongs to exactly ONE governance category** -- one that appears to belong to two IS two transitions, requiring two authority acts. *(General form of DD-1: "approve the plan AND authorize execution" was one vote spanning Planning and Execution governance; splitting it was this invariant asserting itself, not style.)* **Classify by what a transition PRODUCES** -- architectural decisions / approved implementation plans / accepted work-package baselines / authorized engineering work. **A category that produced nothing durable was not a governance act.** **Neither axis is derivable from the other** -- two transitions can share a category and differ in type. An unlabelled state machine hides which authority owns each transition AND what kind of act it is, which is how one "yes" comes to mean four different things in a minute-book. *(Labelling exposed that Planning Governance is the only WP-6->WP-7 state NOT owned by the ARB -- EP-01's separation made visible, and why its vote is separate.)* **Governance states are not programme progress; the programme CONSUMES them.**
- **📄 PROMOTED to a platform methodology module (2026-08-01):** `engineering/knowledge/methodology/Layer_Verification_Rule.md` — sits beside `DDD_Tactical_Governance_Principles.md` (that module governs *what to build*; this one governs *who may change it, and at which level*). **Status: PROPOSED, NOT ADOPTED — non-binding until an explicit Decision Authority ruling (R-34). Cite as a recommended heuristic, never as authority.**
- **FOUR GOVERNANCE CATEGORIES — canonical and STABLE (2026-08-01)** *(single home: `Layer_Verification_Rule.md` §3 — cite, never restate)*. **THE ORGANIZING IDEA IS THE LIFECYCLE TRANSITION, NOT THE ARTIFACT: a category owns exactly one business transition, and the durable artifact is EVIDENCE the transition occurred — it exists BECAUSE the transition happened, never the reverse.** **CATEGORIES DO NOT ACT; AUTHORITIES ACT** — a category says whose KIND of decision it is, only an authority can exercise it. Full chain: `lifecycle transition (what happened) -> category (who governs) -> AUTHORITY (who exercised) -> artifact (what remains)`. **A transition with no named authority DID NOT OCCUR — it was only described.** **Category does NOT determine authority: Planning Governance alone spans two (EP-01 plan approval = Decision Authority; opening a work package = ARB), so name the authority per transition, never infer it.** **⚠️ NAMING HAZARD: 'lifecycle transition' (opened/authorized/accepted/decided — what happened to the work) and 'transition TYPE' (Approval/Ratification/Authorization/Acceptance — what kind of authority act) are DIFFERENT, INDEPENDENT axes with confusingly similar names. Keep both; do not collapse them.** **Architecture** = a design question is decided/interpreted -> approved architectural decision · **Planning** = a work package is OPENED (or its plan approved) -> approved work package · **Execution** = engineering is AUTHORIZED to begin -> authorized engineering activity · **Delivery** = completed work is ACCEPTED and CLOSED -> accepted implementation baseline. **INVARIANT: one category = one lifecycle transition = one durable artifact; if a category cannot demonstrate all three it is incorrectly modelled.** **Derive categories from EVENTS, never from terminology** — asking *which label sounds right?* is how the model drifts; asking *which business transition is being exercised?* is how it holds. **OPENING creates INTENT; ACCEPTANCE creates COMPLETION.** *(Refined only because operational evidence showed the prior definition could not classify repeated behaviour: "acceptance AND PROGRESSION" admitted "opening", mis-filing R-52 and R-60. **Model evolution driven by evidence — not human error, and never style.**)* **Say 'accepted IMPLEMENTATION baseline', never 'architecture baseline'.** **Vocabulary changes only on the same bar: operational evidence of inconsistent classification.**
- **🔍 LAYER VERIFICATION RULE:** **can this layer change WITHOUT changing the layer above it? YES → it belongs at this layer. NO → you are modifying the wrong abstraction.** **Validated retrospectively against this project's two worst defects, both of which passed every automated gate:** **AP-2** (a MAD key in a retention config) destroys *"one canonical home"* → an **invariant breach dressed as a mechanism choice**; **AP-1** (`max(1,$days)`) overrides *"Q-2 decides durations"* → an **implementation edit reaching two levels up**. **Apply this rule to any change that feels like "just a technical choice."**
- **THE DUAL — what a failure MEANS:** *if changing this layer forces a change above it, you have discovered an **architectural** dependency, not an implementation one.* **A failing rule ESCALATES, it does not block** — the failure names the authority that must be engaged, and says the change is larger than it was presented as.
- **PROPOSAL CHECKLIST (apply before implementing):** (1) which layer is intended to change? (2) which higher layer would also change? (3) if any higher layer changes → **escalate before implementation**.
- **Plans routinely record an invariant and its mechanism in the SAME SENTENCE**, which is why they get read as one thing. **Split them before treating either as binding.**
- **Analysis cannot ratify itself.** The sequence is **analysis → recommended realization → ARB ratification → resolution** — never analysis → resolution → ratification. A report that recommends *and* declares resolved while listing pending ratifications contradicts itself. *(Same class as "readiness is evidence, acceptance is authority" — check both directions: applying it to someone else's gate is not the same as applying it to your own recommendation.)*

## Preparation vs Recording — two artifacts, never one (adopted 2026-08-01)

- **A governance session needs TWO artifacts, and conflating them is a real boundary crossing — including in the FILENAME.** **PACKAGE** (before): evidence · recommendations · traceability · **blank** templates. **RECORD** (after): rulings · outcomes · state transitions. *(Learned the hard way: I named an artifact a "Record" before any ruling existed. The body correctly recorded nothing — **the body held the line; the name did not**.)*
- **Lifecycle:** `Evidence → Recommendation → Authority Decision → Recorded Outcome → Engineering Handover → RED`. **A preparation commission ENDS at the boundary before authority is exercised.**
- **Three roles, three acts:** the **Authority** decides · the **Recording Architect** records · **engineering** executes. **Recording is not deciding; deciding is not executing.**
- **Never write "is accepted" / "is ratified" in a package**, and never pre-fill an outcome — not even the recommended one.
- **"No further governance act needed" is almost always wrong wording.** If outstanding authority decisions are listed, those ARE governance acts. Say **"no further governance PREPARATION is required."**
- **Independence test for any two-column classification:** if column B is derivable from column A, it is not a second dimension. **TEST BOTH DIRECTIONS -- one-directional non-derivability is NOT independence.** *(Caught twice on my own tables: (a) a transition-type column that merely restated the category; (b) after fixing it, I claimed the table "carries its own proof" when the evidence only showed Category not derivable FROM Type -- Type was still derivable FROM Category in that sample. And `Produces` is 1:1 with Category BY CONSTRUCTION, so it is a definitional expansion, not a third dimension.)* **A model that exempts itself from its own test is ceremonial.** *(My transition-type column once restated the category column — Planning/Execution — and had to be re-typed to Approval/Ratification/Authorization, which yields a genuine proof: two rows share a TYPE across different CATEGORIES.)*

## Canonical placement checkpoint — a WORKFLOW guard, not a rule (added 2026-08-01)

- **`.claude/scripts/engineering-placement-guard.sh`** — PreToolUse (Write|Edit), **non-blocking**, fires when a **NEW** file is created under `engineering/`. Asks four **generic** questions — what governs **maturity** · what governs **placement** · what **evidence** supports this location · what **exception** authorizes it, and is that exception ruled, named and given a validation expectation — then requires one outcome: **COMPLIANT / APPROVED EXCEPTION / REQUIRES ARB DECISION.**
- **🔑 RUNTIME TOOLING ENFORCES GOVERNANCE; IT NEVER DUPLICATES GOVERNANCE.** The hook **derives** the governing standards at runtime by reading `engineering/governance/STANDARDS_INDEX.md` (matching the concept names *promotion ladder* and *placement litmus* in its Hosts column). **It quotes no rule text and hardcodes no standard number**, so renumbering, supersession or rehoming is followed automatically. *(The first version embedded the rule text and the ES numbers — making runtime tooling a second home for governance, which is the very duplication the standards forbid. Caught in review.)*
- **Not registered in `.claude/platform/registry.yaml`:** **R-42** ruled the registry governs platform assets only; a project-side workflow hook has no AIP lineage, and registering it would require fabricating traceability.
- **NO GOVERNANCE WAS CREATED. ES-005 and ES-006 are untouched** — the rules were already correct and already sufficient. **What changed is the workflow.**
- **The systems framing, not the personal one:** the defect was not *an author was careless* but ***the workflow permitted a governed action without requiring consultation of its governing standards.*** *"Be more careful"* does not scale; **a workflow that requires the check does.**
- **Silent for:** existing files (an edit is not a placement act) · `engineering/verification/reports/**` (verification output) · anything outside `engineering/`. **Sharper text for `engineering/knowledge/methodology/`**, the canon directory.
- **The guard obeys its own litmus:** it is runtime session tooling, so **ES-005.3 puts it in the runtime mount (`.claude/scripts/`), not in `engineering/`.**
- **Non-blocking on purpose:** creating under `engineering/` is legitimate when governed, and **the hook cannot evaluate the answers to its own questions — only the author can.** A wall that is always dismissed teaches less than a checkpoint that is read *(same posture as `discipline-gate-reminder.sh`)*.

## Layer discipline for governance artifacts — WHO SAYS WHAT, and where (2026-08-01)

- **ADRs GOVERN POLICY · ES documents EXPLAIN STANDARDS · the REGISTRY STORES CONFIGURATION · SCRIPTS EXECUTE BEHAVIOUR · REPORTS RECORD EVIDENCE.** Each fact lives in exactly one layer.
- **DUAL TRUTH IS THE FAILURE MODE: if the registry explains WHY a rule holds, then ES-005 AND the registry both express policy.** A registry row carries a `ref:` (`ES-005.3`, `ADR:OQ-2`) — **it stores WHAT and points at WHO SAYS SO.** Same for scripts: **no governance prose, no historical commentary, no rule text in executable tooling.**
- **ROOT READMEs STAY ~15-20 LINES:** purpose · holds · owner · internal-layout ownership · a pointer table (policy / configuration / resolver). **Everything else belongs in the ADR.**
- **⛔ DO NOT CREATE A NEW PRINCIPLES DOCUMENT UNDER `engineering/` (e.g. a "DAP-001"): it would be a CROSS-PRODUCT artifact at RESEARCH maturity, which the resolver returns PENDING for — EXACTLY the `Layer_Verification_Rule.md` violation. Blocked by R-38 (no new standards), R-37 (bugfix/link/typo only) and R-38's own precedent ("where existing rulings already express a constraint, INTERPRET THEM rather than add another ruling"). THE PARSIMONIOUS MOVE IS TO STATE THE PRINCIPLES IN THE ADR — the approved policy home — AND REFERENCE THEM.**

## The PublicDigit Engineering Protocol is a POINTER document — nine of twelve phases were already canonical (2026-08-01)

- **BEFORE WRITING ANY PROTOCOL, SEARCH THESE THREE HOMES — the fifth confirmation of the search-canon-first rule:** `.claude/CLAUDE.md` § **Development Discipline (STANDING RULE)** = Business -> DDD -> Architecture -> Tests -> Implementation, plus *Finding -> Architecture Decision -> RED -> GREEN -> Certification* · `docs/implementation/Implementation_Process_v1.1_Draft.md` = **EP-01 Plan First · EP-01-Light · EP-02 Completion Review + mandatory report format · EP-03 Engineering Readiness Review · ER-08** · **ES-002.1 / ES-002.2**.
- **⭐ ES-002.1 IMPLEMENTATION-FIRST DEFAULT already answers *"does the evidence justify revisiting an accepted decision?"* — and answers it as a DEFAULT rather than an invitation: *"every new ticket is an IMPLEMENTATION ticket by default ... only a NO, CARRYING IMPLEMENTATION EVIDENCE OF INSUFFICIENCY, opens an ADR/ARB discussion."* **ES-002.2** adds: no opportunistic platform observations during ticket work.
- **ONLY THREE ELEMENTS WERE GENUINELY NEW: (1) the 8-category ARCHITECTURAL IMPACT CLASSIFICATION (one prior use) · (2) the ENUMERATED STOP-AND-REFER TRIGGER LIST (new bounded context · aggregate · repository · port · domain term · crossing · policy change) — the principle is ES-002.1's, the ENUMERATION is new · (3) the FOUR-OUTCOME EVIDENCE EVALUATION at ACCEPT (the ES-006.1 ladder is canonical; the GATE is new).**
- **A PROTOCOL THAT RESTATES EP-01/02/03 AND THE STANDING RULE WOULD BE A SECOND HOME — ES-005.4 (never a copy) and *rules live once*. SO THE PROTOCOL POINTS AT EACH RULE'S HOME AND RESTATES NONE.**
- **ADOPTION IS AN ENGINEERING-PROCESS ACT (the EP section), NOT an architecture act and NOT an ARB ruling unless the ARB chooses to rule it.**
- **⭐ SCOPING IS WHAT MADE IT PLACEABLE: scoped to PublicDigit it is product-specific -> `docs/publicdigit/`. FRAMED AS CROSS-PRODUCT ENGINEERING METHODOLOGY IT WOULD HAVE RESOLVED TO `PENDING` — the fourth arrival at the unruled cell.**

## ✅ `classification -> placement` is OPERATIONALLY VALIDATED — first execution 2026-08-01

- **A new artifact was CLASSIFIED FIRST and its location DERIVED by the resolver** (`--scope=product-specific --domain=pks` -> `docs/pks`), then written where the machinery said — **no path chosen, argued, or hard-coded.** Until now the model was a governance rule; **it now has an execution record.**
- **CLAIM BOUNDED HONESTLY: ONE EXECUTION, NOT ROUTINE USE.** The platform's own L5 test — *"one script proves possibility; routine use proves capability"* — so the recorded claim is **"operationally validated, first execution"** and nothing stronger.
- **A SECOND, WEAKER EXECUTION THE SAME DAY: the resolver returned PENDING for a cross-product research artifact AND THE RESPONSE WAS TO STOP, NOT INVENT A LOCATION. A MODEL THAT REFUSES IS AS MUCH EVIDENCE AS ONE THAT ANSWERS** — the failure mode the tooling exists to prevent, exercised for real.

## ✅ WORKSTREAM CLOSED: documentation placement & link integrity (2026-08-01)

- **CLOSED: documentation roots implementation · migration registry implementation · deterministic link repair. VALIDATED: the placement mechanism (first execution). COMPLETE: PKS evidence recording.** **OPEN AS DELIBERATE SUCCESSOR WORK: ENG-008 (trigger: second consumer) · ENG-009 (BLOCKED on OQ-5) · ENG-010 (documentation integrity) · ENG-011 (repo-wide validation) · the 6 ambiguous references (editorial/governance).**
- **CLOSURE WORDING MATTERS — NEVER SAY "all broken links were fixed". SAY: "All deterministic link repairs have been completed. Remaining unresolved references have been classified and transferred to backlog or governance."** 53 references remain broken; they are **classified, counted, attributed and owned — not repaired**, and the record says so.
- **A CLOSED WORKSTREAM WITH EXPLICIT SUCCESSORS IS FINISHED; ONE WITH HIDDEN TODOs IS NOT.** The four ENG items are NEW work the workstream created, not unfinished work it left.

## ⭐ FIRST OPERATIONAL VALIDATION OF THE KNOWLEDGEOS FEEDBACK LOOP — with its boundary (2026-08-01)

- **One full traversal in one day: Observation -> Classification -> Placement -> Validation -> Operational Evidence -> Backlog.**
- **BOUNDARY 1: ONE TRAVERSAL, NOT A DEMONSTRATED CAPABILITY** (the platform's own test: one script proves possibility, routine use proves capability).
- **BOUNDARY 2, AND IT IS THE IMPORTANT ONE: THE LOOP HAS NOT YET CLOSED ON THE MODEL. It delivered evidence TO the decision point and stopped — no ruling issued, no standard amended, no candidate promoted. IT SHOWED THAT EVIDENCE REACHES AUTHORITY; IT HAS NOT YET SHOWN THAT EVIDENCE CHANGES THE MODEL.**
- **Recorded claim: "first operational validation of the KnowledgeOS feedback loop, UP TO THE DECISION POINT."**

## ⭐ A RECURRING UNRULED CLASSIFICATION IS A MODEL GAP — not a count (governing framing, 2026-08-01)

- **SUPERSEDES the "now there are two artifacts" phrasing below.** *Two artifacts* is an INVENTORY fact and invites *"and two is still small"*. **THE SAME UNRESOLVED CLASSIFICATION RECURRING DURING ORDINARY WORK IS A PROPERTY OF THE MODEL, not of the artifacts — it does not weaken with a small count, because THE OBSERVATION IS THE RECURRENCE, NOT THE POPULATION.**
- **DDD FORM: you evolve a model when reality REPEATEDLY EXPOSES THE SAME MISSING CONCEPT — not when instances accumulate, but when the same gap keeps being reached FROM UNRELATED DIRECTIONS.**
- **THREE INDEPENDENT ARRIVALS AT `cross-product + research` IN ONE DAY, from three unrelated commissions, NONE OF WHICH WAS LOOKING FOR THE GAP:** (1) `Layer_Verification_Rule.md` from WP-7 finding G-1 · (2) the documentation-index candidate from a link-integrity commission · (3) the recurring-unruled-classification principle from the governance commission. **The third is SELF-DEMONSTRATING: the principle saying "recurrence is evidence" is itself the third occurrence.**
- **GOVERNANCE PRINCIPLE CANDIDATE (not adopted):** *An unruled classification that repeatedly appears during normal engineering work becomes architectural evidence for extending the classification model.* **It RULES OUT extending because "this seems useful"; it ADMITS extension because engineering repeatedly reached the same unresolved state. The trigger is OBSERVED RECURRENCE, NOT JUDGMENT.**

## Candidate promotion bars — one corpus is one observation (2026-08-01)

- **A rule generalized from one corpus is A RULE FITTED TO ONE CORPUS**, however many instances that corpus produced. 47 references and 30 targets are still ONE CLASS OF FAILURE IN ONE REPOSITORY. (The programme already records this ceiling elsewhere: *"one corpus, one lineage — never citable as independently confirmed"*.)
- **Documentation-index candidate: promotion waits until ANOTHER UNRELATED REPOSITORY produces the same observation.**
- **Recurring-unruled-classification principle: at least the same bar.**
- **A CANDIDATE WITH NO RULED HOME IS NOT GIVEN AN INVENTED ONE — record it inside the evidence record that produced it.**

## Triggers belong IN the backlog item, not in a report (2026-08-01)

- **ENG-008 now carries its own binding trigger: A SECOND CONSUMER of the confidence model.** **A trigger recorded only in a report is a trigger nobody will see when the item is next read.** Write activation conditions INTO the item.

## ⚠️ THE UNRULED CELL NOW HOLDS TWO ARTIFACTS — the stewardship deferral's stated ground has changed (2026-08-01)

- **Stewardship was deferred on an explicit basis: "the evidence is one artifact", and R-29/R-37 reject generalizing from a single example. THAT WAS CORRECT AND IT IS NO LONGER THE SITUATION.**
- **Artifact 1: `engineering/knowledge/methodology/Layer_Verification_Rule.md`. Artifact 2: the ES candidate — *a documentation index shall not reference an artifact that does not yet exist unless explicitly marked as planned* — cross-product, research maturity, resolver returns PENDING.**
- **THE SECOND ARRIVED WITHIN A DAY, FROM AN UNRELATED COMMISSION, AS A DIRECT CONSEQUENCE OF AN ARB INSTRUCTION: the ARB asked for a candidate, and the candidate had nowhere to go. IT WAS RECORDED INSIDE THE PKS OBSERVATION (the evidence that produced it, already correctly placed) RATHER THAN GIVEN A NEW FILE WITH NO RULED HOME.**
- **DO NOT TREAT THIS AS A REQUEST TO DECIDE — it is a report that the deferral's stated PRECONDITION IS NOW MET.**

## Abstraction waits for a SECOND CONSUMER (2026-08-01)

- **ENG-008 (externalize the link-repair confidence policy) stays closed FOR A STATED REASON, not "later": it would introduce ABSTRACTION BEFORE A SECOND CONSUMER EXISTS.** `link-check.php` is the only consumer today. **R-29/R-37's reversed burden of proof applies to the platform's own TOOLING as much as to its architecture** — the same discipline that kept documentation roots as reserved namespaces rather than pre-created directories. **TRIGGER: a second consumer of the confidence model.**

## A sequencing deviation is RECORDED, not reverted (governance note precedent, 2026-08-01)

- **When work was applied before its authorization but the applied content SATISFIES the criteria later approved: AUDIT IT, RECORD A GOVERNANCE NOTE, DO NOT ROLL BACK.** Reverting and re-applying reproduces the identical repository state and only launders the sequence.
- **This is ES-004.3 applied: "history is never rewritten to manufacture consistency." A revert would produce A CLEANER-LOOKING RECORD OF AN EVENT THAT STILL HAPPENED.**
- **The note's three parts: (1) the deviation is acknowledged · (2) the applied changes are retrospectively AUDITED against the approved criteria, with counts · (3) the forward rule — future deterministic repairs shall not be applied before authorization.**

## PKS receives operational evidence — and placement was DERIVED (2026-08-01)

- **`docs/pks/2026-08-01-documentation-debt-observation.md` is the first real artifact in a derived root, and THE RESOLVER CHOSE ITS HOME** (`--scope=product-specific --domain=pks` -> `docs/pks`).
- **⚠️ CONSEQUENCE NAMED RATHER THAN DISCOVERED LATER: THIS SPLITS THE PKS CORPUS — 89 PKS documents stay in `docs/implementation/` while new ones land in `docs/pks/`. Correct behaviour on NEW work while migration is blocked, but PKS documentation now lives in TWO PLACES until Phase 2 runs.**
- **OBSERVATION SHAPE FOR PKS: Observation -> Evidence (with method) -> Classification -> Recommendation -> Reproduction command.**
- **FINDING WORTH KEEPING: `knowledge-lint` validates `docs/knowledge/` ONLY, so the areas carrying the documentation debt (`developer_guide/`, `architecture_legacy/`) have NO LINK VALIDATION AT ALL. GREEN LINT WAS NEVER EVIDENCE OF A HEALTHY REPOSITORY.**

## Confidence model + DECLARATIVE migration registry for link repair (2026-08-01)

- **CONFIDENCE BAR — ONLY >= 99 MAY BE AUTO-APPLIED:** **100** git rename record · documented migration · exact existing target · **99** exactly one file in the repo carries that basename · **75** several candidates = AMBIGUOUS · **0** no candidate = MISSING. **Ambiguous and missing are EVIDENCE, never repairs.**
- **MIGRATIONS ARE DECLARATIVE, NOT CODED: `docs/knowledge/schema/repository-migrations.yaml`** (id · from · to · kind · source ADR), read by **`scripts/link-check.php`**. Adding a future migration is a REGISTRY ENTRY, not a code change.
- **DOCUMENTED MIGRATIONS ON RECORD: (1) `architecture` -> `architecture_legacy` · (2) repository-root AI-generated developer docs -> `developer_guide/` (root-normalization: repository-root file only, EXACT filename, relocated file must exist, and NO other candidate may exist).** Source for both: `docs/adr/20260801_1712_legacy_folder_and_files.md`.
- **A DOCUMENTED MIGRATION IS EVIDENCE; A FILENAME HEURISTIC IS NOT. "Capital letters means developer_guide" would be a heuristic — the RULE is the documented relocation, and every use is existence-verified.**
- **REPOSITORY NORMALIZATION CHANGES ORGANIZATION ONLY — never artifact identity, architectural meaning, or ownership.**
- **EVIDENCE VERDICT ON THE 47 UNRESOLVED REFERENCES: 0 deleted · 0 renamed · 0 archived · 47 NEVER WRITTEN (verified with `git log --all -- <path>`). They are promised-but-unwritten documents — DOCUMENTATION DEBT PREDATING the refactoring, NOT migration damage.**

## Link repair: repair only on EVIDENCE, never on the best-looking candidate (2026-08-01)

- **FOUR RULES, EACH REQUIRING THE DESTINATION TO EXIST BEFORE REWRITING:** git's own rename record · the link was written REPO-ROOT-RELATIVE and that path exists · **EXACTLY ONE** file in the repo carries that basename · a DOCUMENTED directory rename (`architecture/` -> `architecture_legacy/`, recorded in `docs/adr/20260801_1712_legacy_folder_and_files.md`), still existence-checked.
- **⚠️ A FIRST ATTEMPT WAS DISCARDED: it picked the "best" candidate when several files shared a basename and rewrote `election_management/01-overview.md` to a same-directory `01-overview.md` — A PLAUSIBLE-LOOKING WRONG ANSWER that would have silently corrupted navigation. TIGHTENED TO UNIQUE-CANDIDATE-ONLY; the multi-candidate cases were then resolved correctly by git-rename evidence.**
- **NEVER INVENT A TARGET. 47 of the remaining links point at documents that DO NOT EXIST ANYWHERE — record them; do not author replacements. 6 are AMBIGUOUS (`./ARCHITECTURE.md`, `./INDEX.md`) and need A HUMAN CHOICE, not a repair.**
- **`knowledge-lint` VALIDATES `docs/knowledge/` ONLY — a repo-wide scan found 121 broken links where the linter reported 9. GREEN LINT IS NOT GREEN REPOSITORY.**

## ⚠️ `session-state` — a CANDIDATE SEMANTIC CORRECTION, not a defect fix (2026-08-01)

- **CLAIM AT THE STRENGTH THE EVIDENCE SUPPORTS: the current implementation TREATS `session-state` AS IF IT WERE A SCOPE, while its observed semantics ALIGN MORE CLOSELY with the RUNTIME role defined by ES-004.3. THIS INDICATES A POSSIBLE MODELLING INCONSISTENCY REQUIRING GOVERNANCE CONFIRMATION BEFORE CORRECTION.**
- **⛔ DO NOT CALL IT A DEFECT FIX. Changing a registry value CHANGES THE CLASSIFICATION MODEL — the registry is the EXECUTABLE FORM of the model, so a change to it is a SEMANTIC ACT. ARCHITECTURE REPORTS EVIDENCE; GOVERNANCE CONFIRMS SEMANTICS. 'Probably right' is not a mandate.**

## ⭐ THE MODEL INTEGRITY RULE — general modelling discipline (extracted 2026-08-01, CANDIDATE)

- **Whenever a new attribute or dimension is proposed, FIRST DETERMINE WHICH OF THREE THINGS IT IS: a NEW DIMENSION · an OVERLOADED EXISTING DIMENSION (the concept is already there, carried by the wrong field) · or MERELY ANOTHER VALUE of an existing dimension.**
- **Introduce a new dimension ONLY if ORTHOGONAL + NECESSARY + SUFFICIENT — ALL THREE, or the change does not proceed.**
- **If an existing dimension contains MIXED CONCEPTS, that is a MODELLING DEFECT — surface it BEFORE architectural evolution, not as part of it.**
- **IT IS GENERAL: applies to any proposed value object, aggregate, classification attribute, engineering capability or architectural dimension — not just documentation.** **Applied once, it FALSIFIED A PROPOSAL I HAD ALREADY RECOMMENDED.** Recorded in `docs/implementation/PublicDigit_Engineering_Protocol_Proposal.md` (cross-product + research resolves to PENDING — the FOURTH arrival at the unruled cell — so it lives inside a document that has a home).
- **COMPANION CHECKS 4a-4d in the same proposal: CAPABILITY INTEGRITY (consumed / extended / invented?) · MODEL INTEGRITY (the rule above) · CLASSIFICATION INTEGRITY (type and role established BEFORE placement) · GOVERNANCE SEPARATION (which findings are architecture's to conclude, which are governance's to decide).**

## PENDING is an absent RULE, not an absent INPUT (2026-08-01)

- **Adding classification inputs CANNOT resolve a PENDING cell.** The unruled `cross-product + research` cases need a **RULING** (stewardship); the two 2026-08-01 misplacements need an **INPUT** (role/type). **TWO DIFFERENT GAPS — never present them as one body of evidence for one change.**
- **THE THREE-QUALITY TEST for any proposed classification input: ORTHOGONALITY (independent of Scope/Domain/Steward/Maturity?) · NECESSITY (does its absence explain EVERY observed failure?) · SUFFICIENCY (do these alone resolve them, with no further dimension?). ALL THREE, OR THE AMENDMENT DOES NOT PROCEED.** Run 2026-08-01: orthogonality YES · **necessity PARTIAL (2 of 5)** · sufficiency YES over two cases -> **amendment does not proceed.**
- **CAPABILITY FRAMING: the capability is REPOSITORY DOCUMENTATION CLASSIFICATION; PLACEMENT IS ONE CONSUMER.** A capability is extended when it cannot answer a question put to it — **failing for want of a RULING is not a capability gap.**
- **SAY 'three independent classification DIMENSIONS', not 'three taxonomies' — Type, Role and Record Responsibility answer different questions and vary independently.**

## The documentation classification gap — THREE taxonomies, none joined to placement (measured 2026-08-01)

- **(1) DOCUMENT TYPE: `docs/knowledge/schema/knowledge-types.yaml` — 26 machine-validated values, SCOPED TO `docs/knowledge/` ONLY. (2) ARTIFACT ROLE: ES-004.3 — Runtime/Historical/Reference/Decision, REPO-WIDE but DECLARED ON NOTHING. (3) RECORD RESPONSIBILITY: ES-004.2 — IDD/ADR/retrospective/CONTEXT.**
- **MEASURED: 2388 markdown docs repo-wide · 40 carry a declared type (1%) · 2348 UNTYPED (98%).** **A validated vocabulary covering one percent, a role vocabulary declared on nothing, and A RESOLVER THAT READS NEITHER.**
- **SO A SCOPE-CORRECT PLACEMENT CAN STILL BE WRONG, AND THE TOOLING CANNOT SEE IT.** Both misplacements of 2026-08-01 were scope-correct.
- **THE FIX IS TWO INPUTS, NOT A NEW MODEL: add `type` and `role` to the placement registry via the ALREADY-PREPARED ES-005 AMENDMENT PACKAGE (a third input to the same amendment), and widen knowledge-types.yaml's APPLICABILITY. NO new folder, NO new taxonomy, NO new standard.**
- **⚠️ DO NOT fully model `Document{Type,Role,Scope,Steward,Maturity,Domain,PlacementPolicy}` — that is RICHER THAN THE EVIDENCE HAS EARNED and is design ahead of demand.**

## ⭐ THE EPIC-004 ROADMAP DEFINES ALL EIGHT WORK PACKAGES — search it FIRST (2026-08-02)

- **`docs/implementation/EPIC-004_Architecture_to_Implementation_Roadmap.md` defines WP-1..WP-8**, their dependency graph, implementation order, per-WP KEYSTONE TESTS, rollout sequence and the Phase DoD. **Slice plans cite it as 'roadmap §WP-n'.**
- **BEFORE claiming a work package is undefined, READ THE ROADMAP THE SLICE PLANS CITE.** I claimed WP-8 undefined several times while the citation sat in the WP-7 plan I was quoting from.
- **WP-8 = END-TO-END OPERATIONAL VALIDATION** — the full loop with its head over the real path, plus the failure-declared path; triple qualification; CI green. **Keystones: four IT-style full-loop suites (upheld · dismissed · failure-declared · expired) with ONE CorrelationId per conversation asserted end-to-end.** **It VALIDATES; it constructs nothing and owns no new bounded context.**
- **⛔ WP-8 DEPENDS ON WP-4 (APM wiring), WHICH IS OPEN.** Standing rule: **no slice starts before its predecessor's acceptance.** Also outstanding: WP-3A acceptance pending, WP-3B deferred.
- **FULL-LOOP PRODUCTION ACTIVATION IS GATED ON WP-8's ACCEPTANCE** — and the challenge-filing entry point is product work OUTSIDE this roadmap.

## ✅ WP-7 RETENTION ALIGNMENT IS CLOSED — R-66 (2026-08-02)

- **`audit:cleanup` is EPW-AWARE.** Nothing inside an open Evidence Preservation Window is deleted · deletion resumes once it closes · **an unresolvable folder-to-election mapping is PRESERVED (fail closed)** · `--days` can no longer override the invariant.
- **Three slices: 7A durations port · 7B `EvidencePreservationWindow` (R-59) · 7C the retention guard (R-65 authorized, R-66 accepted).**
- **OWNERSHIP, unchanged and worth remembering: AUDIT/RETENTION owns the deletion decision · ELECTION owns the Evidence Preservation Window Resolution capability · ADJUDICATION supplies MAD AS CONFIGURATION and is never called.**
- **⚠️ THE GUARD DEPENDS ON `withoutGlobalScopes()`: `Election` is tenant-scoped and a CLI run has NO TENANT SESSION. Remove it and the command PRESERVES EVERYTHING AND SILENTLY STOPS WORKING — it fails safe, which is exactly why the failure is easy to miss.**
- **⛔ NOT YET RELEASED: 7C is the FIRST EXTERNALLY VISIBLE BEHAVIOUR CHANGE and RELEASE REQUIRES A NAMED ANNOUNCEMENT OWNER (C-2, still unnamed). Acceptance is not release.**
- **⚠️ `tests/Feature/Audit/` IS NOT IN THE `GreenfieldCore` SUITE, so `composer merge-gate` DOES NOT RUN THE 7C TESTS.** Run them directly: `php artisan test tests/Feature/Audit/AuditCleanupTest.php`.
- **Dev guide: `developer_guide/election/08_retention_guard.md`.**

## 🧭 A MODEL IS NORMATIVE ONLY ONCE IT HAS BEEN ADOPTED (ARB, 2026-08-02)

**Stated by the ARB as a general architectural principle, to be preserved permanently. Recorded as a principle, not minted as a rule — it has no identifier and needs none.**

- **Repository evidence is DESCRIPTIVE. The ubiquitous language the business adopts is NORMATIVE.** **Until the normative model is adopted, implementation cannot be judged against it** — there is no adopted standard to judge it by.
- **The order that must not be short-circuited:**
  `repository evidence → plausible domain models → ARB modelling decision → normative interpretation → conformance assessment`
  **Never `repository evidence → violation`. That skips three steps.**
- **Corollary that caught a real error:** a modelling decision may determine **HOW AN EXISTING INVARIANT APPLIES** without establishing that the invariant is **breached today**. Concluding a breach before deciding the definition the invariant depends on is **circular**.
- **Why it matters:** it prevents an architect from treating **an emerging idea as though it were already constitutional.**

## ⛔ ENGINEERING METHODOLOGY IS FROZEN (2026-08-01)

- **NO protocol refinement · NO review-model evolution · NO documentation-architecture evolution · NO KnowledgeOS proposals — UNLESS PUBLICDIGIT IMPLEMENTATION EXPOSES A GENUINE DEFICIENCY.** **EXECUTE THE PROTOCOL; DO NOT IMPROVE IT.**
- **FROZEN ≠ ADOPTED.** The freeze stops refinement; it does not confer authority. Phases 0-14 are fixed and **Phase 5, Phase 7's trigger list and Phases 12-14 REMAIN PROPOSED AND UNADOPTED**.
- **THE MODEL INTEGRITY RULE IS AN OBSERVATION, NOT PROMOTED — one use.** **Its own necessity test rejects it: a single application is not evidence of general applicability.**
- **THE FAILURE MODE TO WATCH IN MYSELF: the protocol went 12 -> 14 -> 15 phases in three turns. Each addition answered a prompt AND grew the methodology. THE DIRECTION IS PublicDigit -> Operational Evidence -> PKS -> KnowledgeOS, NEVER KnowledgeOS -> KnowledgeOS.**

## ⛔ CLASSIFICATION BEFORE PLACEMENT — the resolver cannot read ROLE (2026-08-01)

- **NEVER derive placement from SCOPE alone. Ask FIRST: what kind of artifact is this, and what ROLE does it carry?** The resolver answers the question it is asked; it does **not** ask what the artifact IS.
- **ES-004.3 ARTIFACT ROLES — Runtime · Historical · Reference · Decision — CARRY PLACEMENT CONSEQUENCES, and the resolver CANNOT READ THEM.** Two models bear on placement (Scope/Steward/Maturity/Domain, and Role) and **nothing joins them**. *(One instance; extend the model on RECURRENCE, not on a single case.)*
- **⭐ EXECUTION ARTIFACTS ARE ALREADY CLASSIFIED — ES-004.2 Plans clause + Plan Concept Decision Paper (ADOPTED): ENGINEERING PLAN = an APPROVED EP-01 deliverable -> `docs/plans/` `YYYYMMDD-HHMM-<what>-plan.md` · WORK PLAN = provider plan-mode, pre-approval, a RUNTIME artifact (ES-005.1) -> THE RUNTIME MOUNT. 'At EP-01 approval, plan content is PROMOTED into a governed Engineering Plan.' ES-004.3 lists 'the active Work Plan' under Runtime, lifecycle Draft->Authorized->Executing->Accepted-> Closed.**
- **A DOCUMENTATION ROOT HOLDS PRODUCT KNOWLEDGE, NOT EXECUTION STATE.** Putting a work plan in `docs/<domain>/` makes the root mean two things.
- **PROPOSAL PAPERS (pre-adoption process/architecture content) go to `docs/implementation/`** — precedent: `Placement_Rule_Decision_Paper.md` · `Artifact_Ownership_Decision_Paper.md` · `Plan_Concept_Decision_Paper.md`. **On adoption the content moves into its rule's home and does NOT remain as a second home.**
- **AND CHECK WHETHER A LIVING PLAN ALREADY EXISTS: CLAUDE.md — 'plans are living documents; UPDATE THEM CONTINUOUSLY RATHER THAN CREATING NEW ONES.'**

## 📍 DOCUMENTATION PLACEMENT IS NOW EXECUTABLE — resolve it, never hard-code it (implemented 2026-08-01)

- **SINGLE SOURCE OF TRUTH: `docs/knowledge/schema/documentation-placement.yaml`** (beside `statuses.yaml`/`authorities.yaml`/`bounded-contexts.yaml`, where enumerations already live). **SINGLE CONSULTED MECHANISM: `scripts/doc-placement.php`** — `npm run docs:placement` / `docs:placement:verify`.
```
php scripts/doc-placement.php --scope=<product-specific|cross-product|session-state> [--maturity=<research|qualified|adopted>] [--domain=<id>]
php scripts/doc-placement.php --list | --self-test | --verify
```
- **NEVER HARD-CODE A DOCUMENTATION ROOT in a template, script or prompt — RESOLVE IT.** Adding a domain or renaming a root = one registry edit, zero script edits.
- **EXIT CODE 2 = the classification is real but its PLACEMENT IS UNRULED (cross-product + research, awaiting the stewardship decision). RECORD `PENDING` AND ESCALATE — NEVER INVENT A DESTINATION.** Tooling that guesses a destination is how the mixing happened.
- **ROOTS: `docs/publicdigit/` · `docs/knowledgeos/` · `docs/pks/` — each created WITH A README AS ITS FIRST ARTIFACT, because ES-005.2 forbids speculative empty directories. The README satisfies the folder rule rather than working around it.**
- **⚠️ CROSS-PRODUCT ARTIFACTS STILL ROUTE TO `engineering/` — ES standards and methodology are NOT pulled into `docs/knowledgeos/`, which is exactly what OQ-5 leaves open** (is KnowledgeOS the same thing as the Engineering Platform? canon separates them; R-67 says `engineering/` = cross-product SCOPE, not a domain).
- **`.claude/scripts/engineering-placement-guard.sh` EXTENDED, NOT DUPLICATED: under `engineering/` the rules are PROSE so it asks generic questions and derives the standards; under `docs/` the derivation is EXECUTABLE so it points at the resolver. Roots read from the registry at runtime — the hook hardcodes none.** Fires only on the MEASURED domain-mixed locations (`docs/implementation/**`, files directly under `docs/`).
- **LESSON, BASH: `case` globs SPAN `/` — `docs/pks/x.md` matches `docs/*.md`. Path tests that need precision belong in PHP, not in a case glob. Caught by testing the NEGATIVE cases.**
- **`npm run knowledge-lint` BASELINE = 9 errors / 0 warnings — all nine are broken `architecture/` links from today's `architecture_legacy/` move, PRE-EXISTING. ALWAYS RECORD THE BASELINE BEFORE AND AFTER: my first pass took it to 10 (a new `docs/knowledge/` doc needs a knowledge card), the linter caught it, fixed.**

## ✅ ES AMENDMENTS **ARE** PERMITTED UNDER THE FREEZE — by explicit ruling (R-41 is the controlling precedent; verified 2026-08-01)

- **⛔ CORRECTS THE ENTRY BELOW AND MY OWN PACKAGE CLAIM ("no precedent for a standards amendment under the freeze") — THE PRECEDENT EXISTS AND IS RECENT. `git log -- 'engineering/governance/ES-*.md'` shows repeated post-R-37 amendments: **R-41/ES-004.3 (2026-07-30, "adopted ... Principal Architect instruction", then refined same day at ARB review) · ES-006.4 Harvest Question ("ARB harvest amendment") · ES-004.2 plan naming ("overridden ... Decision Authority") · ES-002 pointer registration**. **I searched the freeze-note convention and the rulings register but NEVER RAN GIT LOG ON THE ES FILES. That is the obvious search.**
- **THE DECISIVE DISTINCTION, stated by R-41 itself: "rule text hosted ONCE as ES-004.3 (documentation standard, NOT A NEW STANDARD DOCUMENT)". R-38 BANS NEW STANDARDS, NOT NEW CLAUSES IN EXISTING ONES.**
- **AN AUTHORITY ACT IS NOT AN "EXCEPTION" TO THE FREEZE: R-37 is itself a DA ruling, so the authority that issued the freeze acts within it by issuing another ruling. Practice shows BOTH styles — R-39 calls itself "a recorded governance exception", R-41 uses NO exception language at all. THE COMMON ELEMENT IS NOT A LABEL BUT EXPLICIT ISSUANCE (R-34). What R-37 constrains is SELF-DIRECTED editing; it has never constrained the Authority's own acts.**
- **ALL SIX ES DOCUMENTS ARE `PROPOSED` — NOT ONE IS RATIFIED (authority: Decision Authority/ARB). So amending ES-005 revises a PRE-RATIFICATION DRAFT awaiting the ratification batch; no ratified rule is disturbed. This does NOT remove the need for authorization (R-34 is indifferent to status) but it IS a materially lower-risk act than I implied.**
- **NO COUNTER-PRECEDENT EXISTS: the freeze has been cited to reject UNEVIDENCED EXPANSION (R-29/R-37 burden of proof), NEVER to block an authorized, evidence-grounded amendment.**
- **REQUIRED AUTHORIZATION = ONE RULING that (a) accepts the package, (b) STATES THE AMENDED RULE TEXT, (c) records provenance. Nothing more — no freeze amendment, no exception instrument, no new mechanism. THE RULING IS WHERE THE RULE LIVES; THE ES EDIT MERELY HOSTS IT (ES-005.4).** Two acts, never one: acceptance ≠ authorization-to-edit.

## ⚠️ AMENDING an ES is NOT covered by the commissioned-exception precedent (verified 2026-08-01)

- **R-37 operational terms: `engineering/` permits ONLY bug fixes, broken-link fixes, typo corrections.** The recognized workaround is the **explicitly-commissioned exception**, cited in a "Freeze note" row (`AI_Engineering_Platform_Architecture_Baseline_Current_State.md`, the Engineering Theory reference, the 2026-07-12 reports).
- **BUT BOTH PRECEDENTS JUSTIFY THEMSELVES AS DESCRIPTIVE: "introduces NO new concept, rule, capability, or structure — it RECORDS WHAT ALREADY EXISTS." AN ES AMENDMENT CHANGES A RULE. So PREPARING a package is covered; APPLYING it (editing `engineering/governance/ES-005-Repository.md`) IS NOT — no precedent exists for a standards amendment under the freeze.**
- **THEREFORE: APPROVING AN AMENDMENT PACKAGE ≠ PERMISSION TO APPLY IT. Applying needs its own explicit R-37 authorization.** *(This corrects my earlier "Package 1 is not blocked" — accurate about the REORGANIZATION clause, incomplete about the OPERATIONAL TERMS.)*
- **`bounded_context` vs `Domain` IS NOT A RENAME — they are two properties at two granularities and BOTH ARE VALID** (bounded contexts genuinely live INSIDE domains; 11 inside PublicDigit). **ADD `domain:` + a new `schema/domains.yaml` (none exists; `bounded-contexts.yaml` does); KEEP `bounded_context:` unchanged, `global` retains its role.** **Renaming a correct field to fix a MISREADING would destroy a distinction the schema had right.**
- **CARRIER RECOMMENDATION: the ARTIFACT'S OWN HEADER is the carrier; the card schema supplies the FIELD VOCABULARY; cards are NOT extended repo-wide** (40 cards today; repo-wide would oblige ~900 documents, and duplicating classification in card AND header strains **ES-005.4 never-a-copy**).
- **THE AMENDMENT'S ONE REAL INCOMPATIBILITY: on issuance, 92 artifacts (89 PKS + 3 KnowledgeOS) are IMMEDIATELY NON-CONFORMANT — their derived location is a domain root that does not exist, and creating it is Package 2, blocked. PREFER recording a TRANSITIONAL NON-CONFORMANCE (the R-39 pattern) over a conditional fallback that makes the rule SELF-NULLIFYING.** A standard that records its own non-conformance is enforceable; one that dissolves on contact is not.

## The APPROVED classification model — Location is an OUTPUT, Domain is a MEMBER (2026-08-01)

```
Classification (what is this artifact?)
    Scope     cross-product | product-specific
    Steward   who curates it
    Maturity  research | qualified | adopted
    Domain    the domain it belongs to  (N/A when Scope = cross-product)

Location = f(Classification)   (where does it live?)  -- DERIVED, NOT A MEMBER
```
- **TWO DEFECTS CORRECTED in the model as first drawn: (1) `Location` was nested INSIDE Classification — which re-couples exactly what the model exists to divide; it is an OUTPUT · (2) `Domain` was MISSING though already approved as the organizing unit — it IS a classification property (*which domain's knowledge is this?* is answerable without knowing where the file sits).**
- **WHAT "PLACEMENT IS DERIVED" BUYS (the teeth): a placement argument that cannot be grounded in a classification property is OUT OF ORDER — "it's useful here", "easier to find", "it already lives there" cease to be reasons.** It does **NOT** amend ES-005, oblige a re-audit of existing placements, or authorize any structural change.
- **CLAIM STATED AT TRUE STRENGTH: the concepts all exist in THREE SEPARATE PLACES and NO SINGLE COHERENT IMPLEMENTATION EXISTS** — ES-005.3's derivation is implicit with incomplete inputs; the card schema is richer but scoped to `docs/knowledge/` and uses `bounded_context` (wrong unit); the header fields are prose read by nothing. **Enough to show the model is DESCRIPTIVE not speculative; NOT enough to claim it is already in force.**
- **⚠️ THE CARD SCHEMA CARRIES FIVE PROPERTIES, NOT FOUR: `status` (maturity) AND `authority` (*trust/source*) are SEPARATE and EXPLICITLY INDEPENDENT. The model has no slot for TRUST/PROVENANCE. Whether trust is a fifth property or a facet of Maturity matters only once a carrier is chosen — routed to step 2, NOT decided.**

## CLASSIFICATION ≠ PLACEMENT — and BOTH are already implemented in the repo (verified 2026-08-01)

- **FOUR INDEPENDENT PROPERTIES: Scope · Steward · Maturity · Location — where LOCATION IS DERIVED, never identical to Steward.** Classification answers *what is this?*; placement answers *where does it live?* **Keep them independent.** *(My earlier "stewardship resolves placement ONLY IF stewardship implies location" was a FALSE DICHOTOMY — the third branch, DERIVATION, is the good one, and omitting it made coupling look necessary.)*
- **ES-005.3 IS ALREADY A DERIVATION FUNCTION** — read as a table: *adoptable unchanged* -> `engineering/` · *needs project context* -> the project · *active session state* -> runtime mount · *research until promoted* -> project-side (maturity override). **Its defect is the INPUT SET: it reads Scope + Maturity and has NO input for Steward or Domain. Adding inputs to an existing function is far smaller than adopting a new principle.**
- **THE ARTIFACT ALREADY DECLARES THE WHOLE TRIPLE IN ITS HEADER:** `Layer_Verification_Rule.md` lines 3-4 — **Class:** Engineering Platform methodology module (=Scope) · **Owner:** Decision Authority (=Steward) · **Status:** PROPOSED-NOT-ADOPTED (=Maturity). **ES-005.3 simply DOES NOT READ IT — it re-asks its own portability question instead of consuming the declared `Class`. The separation is NOT MISSING FROM THE REPOSITORY; IT IS UNWIRED.**
- **`docs/knowledge/_meta/knowledge-card.template.md` ALREADY IMPLEMENTS THE FOUR-PROPERTY MODEL, machine-validated by `npm run knowledge-lint`:** `status:` = *lifecycle position* (Maturity) · `authority:` = *trust/source — **INDEPENDENT OF STATUS*** (the independence is stated IN THE SCHEMA) · `owner:` (Steward) · `bounded_context:` *one of schema/bounded-contexts.yaml **OR `global`***. **`global` IS EXACTLY the cross-product / no-domain case I called "undefined" — the schema has had a value for it all along.** **Two limits: the field is named `bounded_context` (the granularity just corrected — right shape, WRONG UNIT), and the schema is scoped to `docs/knowledge/`, NOT repo-wide.**
- **A DERIVATION MODEL IS APPROVABLE UNDER R-37 WHATEVER ITS SCOPE — it creates no directory and reorganizes no document, so the "does R-37 bind docs/?" question DOES NOT GATE IT.**
- **ORDER SET BY THE ARB: (1) approve the classification model -> (2) define derived placement rules -> (3) apply them to existing artifacts.** `Layer_Verification_Rule.md` stays put until step 3, **by construction**.
- **SEVEN-FOR-SEVEN, AND THE DIVISION OF LABOUR IS NOW CLEAR: the repository keeps already containing the MECHANISM (ES-006.1 · ES-005.3 · ES-005.2+R-37 · the DA product clarification · R-39 · the derivation function · the card schema); the ARB keeps supplying the VOCABULARY that makes the mechanism visible (the DOMAIN dimension · CLASSIFICATION ≠ PLACEMENT). Neither substitutes for the other — but MY first move should be a SEARCH, and my proposals should be about NAMING rather than BUILDING.**

## ✅ APPROVED (Chair disposition 2026-08-01, NOT yet minted as R-nn) + the STEWARDSHIP route

- **APPROVED AS THE CHAIR'S POSITION:** (1) the repository is a **workspace/container, NOT a domain** · (2) it **CONTAINS DOCUMENTATION FOR** multiple domains · (3) the repository organization unit is the **DOMAIN, not the bounded context** · (4) **`engineering/` is the RESULT of the cross-product Scope decision, not another domain** · (5) **ES-005 stays GENERIC — a domain-resolution rule, never an enumeration of domains.**
- **⚠️ NOT MINTED AS RULINGS: R-34 — authority is created ONLY by explicit issuance; "what I would approve" is a disposition. `R-nn` identifiers are the DA's act. Do not cite these five as platform rulings until issued.**
- **WITHDRAWN AS FRAMED: my "undefined cell". A cell two named shapes can fill is UNRULED, not UNDEFINED** — the stronger word claimed the model was broken when it was merely incomplete.
- **THE STEWARDSHIP ROUTE (Chair): Scope -> STEWARDSHIP -> Maturity -> Domain (only if product-specific).** `Layer_Verification_Rule.md` = cross-product · **Engineering stewardship** · research · no domain. **Placement is resolved by GOVERNANCE, not by a new directory.**
- **PRECEDENT THAT SETTLES IT — R-39: `DDD_Tactical_Governance_Principles.md` sits in `engineering/` as an EARLY PROMOTION UNDER A RECORDED GOVERNANCE EXCEPTION, evidence base "one context". THE PROGRAMME HAS ALREADY SOLVED "engineering-side placement of not-fully-qualified material" WITH A RULING, NOT A DIRECTORY.** The stewardship route has precedent; a staging root has none.
- **n = 1 VERIFIED: `engineering/knowledge/methodology/` holds exactly two artifacts — DDD principles ADOPTED, Layer Verification Rule PROPOSED-NOT-ADOPTED. ONE unqualified cross-product artifact. R-29/R-37's burden of proof DOES bite against generalizing from it.**
- **"`engineering/` = qualified" IS NOT MACHINE-ENFORCED TODAY — no Architecture test or gate asserts it; the ONLY status-reading mechanism is the non-blocking `.claude/scripts/engineering-placement-guard.sh`. So "location is testable, labels are not" is POTENTIAL, not current, and cannot carry weight against a reversed burden of proof.** Converts into: **should a gate assert maturity?**
- **QUEUE ORDER (the narrowed question is a PREREQUISITE, not a sibling): R-37 scope -> STEWARDSHIP -> domain roots. If stewardship = YES, the cross-product/unqualified case NEVER REACHES project-side and never touches the roots question — which then rests purely on the 89 PKS + 3 KnowledgeOS PRODUCT-SPECIFIC documents, where the measured evidence actually is.**

## Placement has THREE dimensions: SCOPE -> DOMAIN -> MATURITY (a decision TREE, not orthogonal axes) — 2026-08-01

- **The repository is a CONTAINER, never a domain. Correct phrasing: "the repository CONTAINS DOCUMENTATION FOR multiple domains" / a MULTI-DOMAIN WORKSPACE.** Same category error sits in R-37's aside (*"repository = three bounded contexts"*) — which R-37 itself marks **Class A, NOT RULED**, so nothing depends on it.
- **SCOPE** (*"could a different project adopt it unchanged?"* = ES-005.3 clause 1) -> yes: `engineering/` · no: project-side. **`engineering/` IS THE OUTCOME OF THE SCOPE DECISION — IT IS NOT A DOMAIN.** · **DOMAIN** — applies **ONLY on the product-specific branch** (a cross-product artifact HAS no domain, by definition) · **MATURITY** (ES-005.3 clause 2) — applies to **BOTH** branches.
- **INTERACTION THAT BITES: for an UNQUALIFIED artifact, MATURITY OVERRIDES SCOPE for placement** — cross-product + unqualified is still staged project-side.
- **⚠️ UNDEFINED CELL: CROSS-PRODUCT + UNQUALIFIED HAS NOWHERE TO GO once project-side becomes domain-partitioned** — Maturity says stage it project-side, Domain says project-side is partitioned, and the artifact has no domain. **`Layer_Verification_Rule.md` IS that cell** (cross-product · PROPOSED-NOT-ADOPTED per R-34 · currently sitting in `engineering/knowledge/methodology/`, its QUALIFIED destination reached BEFORE qualification). **The model had no cell for it, so neither did the repository. Any amendment MUST define this cell explicitly.**
- **DOMAINS ARE CONFIGURATION, NOT RULE TEXT** — the generic form is *"place a project-specific artifact under the documentation root of the domain to which it belongs"*; **precedent is inside ES-005 itself** (ES-005.2 keeps its enumeration in a **README table**, home `engineering/README.md`). **PKS membership therefore RELOCATES to a registry rather than disappearing — it still needs a home and a naming authority, and R-42 FORECLOSES the Platform Registry** (AI-platform assets only; project-side artifacts carry no AIP lineage).

## The repository is MULTI-DOMAIN — and the DDD unit is DOMAIN, never bounded context (verified 2026-08-01)

- **PRODUCTIZATION ≠ DOMAIN EXISTENCE. A bounded context / domain may exist BEFORE productization — that is a strategic design fact, not a commercial gate.** So "KnowledgeOS's gate has not opened" answers *is it a product?* and **NEVER** *is it a domain?* **Do not let the gate answer the domain question.**
- **⚠️ VOCABULARY, OFF BY ONE LEVEL — this matters structurally: PublicDigit / KnowledgeOS / PKS are DOMAINS, each CONTAINING bounded contexts.** `app/Contexts/` holds **11** (Adjudication · Committee · Contestation · Election · Elections · Finance · Geography · Governance · Membership · Shared · Trust); **PKS M6 discovers CBC-1..CBC-4 INSIDE "the PKS boundary"**. **"A documentation root per bounded context" would yield ~15 roots — artifact-scattering by another name.** The intended unit is **domain-first**.
- **RULED vocabulary is AIP-14: "Core Domain" / "Supporting Subdomain". The phrase "repository = three bounded contexts" appears ONLY in R-37 and R-37 itself marks it "Context on record (Class A, NOT RULED)". Build on the ruled term.**
- **THE REPOSITORY IS ALREADY MULTI-DOMAIN ON THE RECORD — canon ran Strategic DDD Discovery on THREE separately named domains:** `Strategic_DDD_Discovery_Engineering_Governance_Domain.md` · `Strategic_DDD_Discovery_Product_Knowledge_System_Domain.md` (Phase I ACCEPTED, Phase II consolidated through M5, M6 executed) · **AIP-14** Election System = Core Domain. **Not drift — deliberate, ARB-accepted work.**
- **SO THE OPEN QUESTION IS THE IMPLICATION, NOT THE FACT: DOES DOMAIN MULTIPLICITY ENTAIL DOCUMENTATION-ROOT MULTIPLICITY?** Three domains, one root; **nothing on record says a domain is ENTITLED to a root** — that inference is the unmade decision.
- **THE DEFECT IN ES-005 IS ONE WORD.** ES-005.3 cleanly carries two dimensions in one sentence (clause 1 = portability/engineering-vs-project · clause 2 = maturity); the third, **domain ownership, is ABSENT** — and its absence shows as **"the project"**, definite article, singular. **Unambiguous with one project; NO REFERENT with three.** **That is also WHY `docs/implementation/` filled with 89 PKS files: the rule routed correctly by its own terms; its terms stopped describing the world.**

## ⚠️ ES-005.1's premise is FALSIFIED BY THE REPOSITORY — `docs/` is no longer single-product (measured 2026-08-01)

- **ES-005.1 says `docs/` + `architecture/` + `app/` + `tests/` = "Product (what PublicDigit is)".** Historically true — `docs/` was only for PublicDigit.
- **MEASURED TODAY: of 183 files in `docs/implementation/`, 89 are `PKS_*` (49%) and 3 are `KnowledgeOS_*`. HALF OF PUBLICDIGIT'S PRODUCT FOLDER IS NOT PUBLICDIGIT'S.** The premise was **not changed by decision — it was falsified by accretion**, 92 documents at a time, with no rule ever amended to admit them.
- **This is the FORM OF EVIDENCE R-37 REQUIRES** (expansion needs *proof the existing architecture was insufficient*). **Measured insufficiency, not asserted improvement** — that is what distinguishes it from the proposals R-37 rejects by default.
- **ES-005.3's litmus is CROSS-PRODUCT vs PRODUCT-SPECIFIC, NEVER which-product-owns:** *"could a different project adopt the document unchanged?"* Canon already applied it (`PKS_Brainstorming_Documents_Assessment.md` §1.3): *methodology (cross-product) -> `engineering/` AFTER QUALIFICATION ≠ PKS (product-specific) -> `docs/`*. **So project-side placement of unqualified cross-product methodology is a MATURITY statement, NOT AN OWNERSHIP STATEMENT.**
- **Consequence for `Layer_Verification_Rule.md`: it is cross-product methodology bound for `engineering/` on qualification — it would NOT move to a product folder under ANY product-first tree. It must not be used as evidence for reorganizing `docs/`.**
- **A product-first `docs/` has an UNMET PREREQUISITE: you cannot sort by product until every artifact HAS one. `EKA -> KnowledgeOS` is a confirmed invariant; `PKS -> KnowledgeOS` is NOT — the largest block (89 files) has no ruled destination.**
- **Sharpest objection on record (R-37, same ruling): "maturity ladder ... L6 multi-project; NEITHER L5 NOR L6 IS REACHED BY MOVING FILES."** A product-first `docs/` is the L6 move executed by moving files. **Also unsettled: whether R-37's "no more document reorganizations" binds `docs/` at all — its operational terms name only `engineering/`.**
- **Blast radius if ever executed: 501 inbound refs to `docs/implementation/` (119 to `PKS_*`), 51 to `docs/plans/`; ES-005.4 forbids leaving copies behind.**

## KnowledgeOS is NOT yet a product — already ruled (verified 2026-08-01)

- **`engineering/README.md`, DA clarification 2026-07-27:** capabilities serve *"all PublicDigit products (Online Voting today; **KnowledgeOS and future products if their gates open**)"* · *"the Election System is the Core Domain; **this platform is a Supporting Subdomain**"* (**AIP-14 Product Primacy**) · *"**a second real adopting product is the recorded trigger for the Platform-Adoption split — pre-positioned, not executed**."*
- **So: KnowledgeOS's gate has NOT opened, the platform is a Supporting Subdomain rather than a product, and the split people keep re-deriving is ALREADY DESIGNED and deliberately unexecuted with a named trigger.**
- **Consequence: ES-005.3 STANDS; research artifacts remain project-side.** ES-005.3 is not an outdated rule that slipped through — **it is the current rule for the current architecture, with its successor pre-positioned.** The tension is **a transition held open on purpose**, not an inconsistency to repair.
- **PATTERN, FOUR FOR FOUR:** *lifecycle missing* -> ES-006.1 defines it · *placement policy missing* -> ES-005.3 defines it · *maturity structure needed* -> ES-005.2 + R-37 govern when · *KnowledgeOS is a product* -> the DA already ruled not yet. **Four searches for a missing decision; four already on the record. The failure mode is PROPOSING BEFORE SEARCHING. Search canon first — `engineering/README.md` and the ES set — before proposing any structural or governance change.**

## `engineering/` means the PLATFORM, not "canon" — and its structure is frozen (verified 2026-08-01)

- **ES-005.1 Three-Concern Separation:** `docs/` + `architecture/` + `app/` + `tests/` = **Product** · **`engineering/` = Engineering Platform** (how it is engineered) · `.claude/` = **runtime mount**. **Reading `engineering/` as "adopted canon" is wrong** — it is the platform's own tree, with its own lifecycle.
- **ES-005.2 Folder Rule: a directory exists only when its FIRST ARTIFACT ARRIVES; reserved namespaces are documented in a README table, NEVER created speculatively.** So a maturity-tiered hierarchy cannot be pre-created — **the sanctioned way to express intended structure is a reserved-namespace table.**
- **R-37 Architecture Freeze 2.0 (structural): "no new capability hierarchy · no more document reorganizations"** until C3 + PB-004 + retrospective, **with the burden of proof REVERSED** — expansion requires evidence the existing architecture was insufficient.
- **⚠️ UNRESOLVED TENSION, recorded for the ARB: ES-005.3 sends research artifacts PROJECT-SIDE (`docs/implementation/`), but if `engineering/` is a product in its own right then ITS research is its own, not the product's.** The clause names a **PublicDigit path** for *all* research, which reads like a rule written before the platform was treated as a product. **Strong argument — but it is an argument to AMEND ES-005.3, never to act around it.**
- **A generic `implementation/` folder is forbidden by ES-005.1** — it would collapse two bounded contexts (PublicDigit and the platform) into one directory.

## ⛔ PLACEMENT IS ALSO ALREADY RULED — ES-005.3 (verified 2026-08-01)

- **`ES-005.3 — The Placement Litmus` (ARB 2026-07-10):** *could a different project adopt it **unchanged**? Yes -> `engineering/`; needs project context -> the project; active session state -> the runtime mount;* **and decisively: *"Research artifacts remain project-side (`docs/implementation/`) until promoted through qualification (ES-006 ladder)."***
- **THE TWO CLAUSES CAN APPEAR TO CONFLICT, AND MATURITY GOVERNS.** The first clause tests **is it reusable?**; the last tests **is it proven?** **If reusability alone admitted an artifact to `engineering/`, anything domain-neutral could enter on the day it was written** — and read that way the litmus would contradict ES-006.1 by letting a research artifact skip Pilot and Qualification. **The specific clause governs the general one.**
- **BEFORE creating ANY file under `engineering/`, consult ES-005.3 AND ES-006.1.** Placement is a governed act, not an authoring convenience.
- **THE ACTUAL LESSON (three commissions searched for a missing rule; there was none):** the framework **detected** the breach reliably at every step — what it could not do was **prevent an author from skipping it**. **A governance framework catches violations; it does not catch omissions of consultation.**

## ⛔ THE ENGINEERING KNOWLEDGE LIFECYCLE ALREADY EXISTS — do not invent another (verified 2026-08-01)

- **`ES-006.1 — The Promotion Ladder` (ARB, 2026-07-11) is canonical:** `Research -> Pilot -> Qualification -> Engineering Standard -> Stable Engineering Capability`, with **ES-006.4** supplying the entry path (`Project Outcome -> Observation -> DetermineReusePotential -> DetermineArtifactType -> ES-006.1`). **ES-006.2 says it outright: "no Level 5 exists; do not invent one."**
- **BEFORE proposing any lifecycle, promotion rule or maturity vocabulary, READ ES-006.** Its five ES-006.x rules already cover entry criteria, promotion criteria (burden of proof, R-37), qualification (ES-003), research-tier placement (ES-005), authority and retirement. **A second vocabulary for one ladder is duplication, not clarification.**
- **ES-006.3: research is INPUT-ONLY — "never architecture until promoted through ES-006.1."**
- **ES-006 explicitly EXCLUDES Project Knowledge** — a separate bounded context, scoped out deliberately *"to prevent ES-006 from slowly absorbing project concepts."*
- **THE REAL GAP IS PLACEMENT, NOT LIFECYCLE:** `engineering/knowledge/methodology/` holds an **ADOPTED** module (DDD principles, via the R-39 exception) and a **PROPOSED, never-piloted, never-qualified** one (Layer Verification Rule) **side by side, distinguishable only by reading a status line.** **Canonical location != canonical authority**, and today the filesystem does not reflect the ladder.
- **Diagnosis discipline: when an artifact sits in the wrong place, ask whether it CLIMBED the ladder and stalled or NEVER ENTERED it.** The Layer module never entered — `DetermineArtifactType` was never run; the artifact type was chosen by writing a file.

## Engineering promotion rule (adopted 2026-08-01)

- **`engineering/` records only principles whose evidence demonstrates domain-independent reuse.** Lifecycle: **Observation -> Candidate -> Repeated Evidence -> Operational Validation -> ARB Approval -> Engineering Canon.** **Methodology evolves from operational evidence, never from anticipation.**
- **Five criteria, all required:** repeated operational evidence (**across contexts, not merely across instances**) · domain independence (no project term in any rule statement) · architectural stability (**measured in amendments, not opinion**) · reusability without modification · **promotion must REDUCE complexity, not add a module**.
- **Three buckets when reviewing a finding:** *implementation* (never promoted) · *project pattern* (stays in project knowledge) · *engineering principle* (the only one eligible).
- **⚠️ PLACEMENT FRONT-RUNS PROMOTION — the failure mode this project already committed.** `Layer_Verification_Rule.md` was written straight into `engineering/knowledge/methodology/` on one work package's evidence, and is **PROPOSED, never adopted**: **location implies canon while status denies it.** **A file in the canonical tree accrues authority by location** regardless of its status line. **Assess BEFORE placing.**
- **A single-context programme cannot satisfy criterion 1 unaided.** EPIC-004 is this programme's only context, so *every* candidate it produces fails 'repeated across contexts'. **R-39 is the precedent for resolving that: early promotion is permitted when it is RULED, NAMED as an exception, and given a stated validation expectation** — not when it is simply written into the tree.
- **Stability is the criterion least open to argument** — it is counted, not judged. Four post-freeze amendments in one day is a fail.

## Architecture–Enforcement Alignment Gap (named 2026-08-01, ARB reclassification)

- **Definition:** every architectural decision is correct **and** every gate is correct, but **no artifact states how the decision is realised INSIDE the gates.** The failure is in the **mapping**, not in either side.
- **Resolve it by completing the mapping — NEVER by moving code out of the gates' reach, and never by weakening a gate.** Calling such a gap a "placement issue" invites exactly the wrong fix (*"put the file where the tool doesn't look"*), which is unguarded **and looks like compliance**. **The classification determines the answer, not just the label.**
- **The diagnostic that resolves these: separate the INVARIANT from the MECHANISM.** Plans routinely record both in one sentence, so they read as one thing. *(WP-7: "MAD has exactly one home" = invariant, binding; "consume Adjudication's existing port" = mechanism, never an architectural decision. Substituting the mechanism preserved the invariant and dissolved the conflict.)*
- **Consumer-side ports are the standard resolution for cross-context policy consumption:** a port belongs to **the consumer, in the consumer's language** — not to the provider. The consumer's own Infrastructure adapter reads the single canonical source. No cross-context code import, no gate relaxed.
- **A governance-owned config value read by two contexts is NOT a context crossing.** Both are downstream of *governance*, not of each other — no model, type, lifecycle or deployment coupling. Check *whose policy* it is, not *which file* it sits in.
- **Placement rule: COVERAGE FOLLOWS MEANING, never the reverse.** Do not pull code into a context to obtain gate coverage — that is the tool guessing the architecture. Place by business meaning; coverage is the consequence. **"Everything carrying policy is gated; everything ungated carries none" is a stronger claim than "everything is gated."**
- **Inventory RESPONSIBILITIES, not just rules.** A rule inventory asks *"is this checked?"*; a responsibility inventory asks *"could this responsibility silently move?"* — the failure mode DDD actually cares about. It also collapses duplicates: *policy ownership* and *infrastructure* are the same defect from both ends (**infrastructure deciding a value IS the violation of policy ownership** — that was AP-1), and *construction* + *orchestration* are one drift with two symptoms.

## Historical claims need historical evidence (adopted 2026-08-01, ARB correction)

- **Distinguish a claim about CODE from a claim about WHAT SOMEONE DID.** *"A defect exists and predates my change"* is provable from commit dates and a diff. *"The gate was run and failed at acceptance time"* is a claim about **history** and requires **reproducing the historical run** — checking out that commit and executing the gate.
- **The conflation to avoid:** *"the gate WOULD have failed"* ⇒ *"the evidence WAS inaccurate."* **If the gate was never executed, the evidence line is UNSUPPORTED, not FALSE** — a materially different finding, and the two call for different remedies.
- **Say what you reproduced.** Commit dates prove a *file-level* inconsistency existed from date X. They do **not** prove any *suite outcome* on date X — suite composition, environment and whether anyone ran it are all separate facts.
- **Preferred wording when a stale artifact surfaces:** *"the current execution uncovered a pre-existing inconsistency; it appears to predate this slice and should be investigated before relying on earlier gate evidence."* **Refer it; do not adjudicate an issued ruling from inference.**
- **The cheap decisive test, when it matters:** reproduce the gate at the historical commit. Until that is run, **make no claim in either direction.**

## Enforcement reach — the gates cover LESS than people assume (verified 2026-08-01)

- **All three structural gates scan the SAME four paths and nothing else:** `app/Contexts/{Contestation,Adjudication,Election,Shared}` — `deptrac.yaml` `paths:`, `phpstan-greenfield.neon` `paths:`, `GreenfieldCoreArchitectureTest::CONTEXTS`. **`app/Console/`, `app/Helpers/`, `app/Models/` and every other legacy path have ZERO structural coverage.** Before claiming a constraint is enforced, **check that the code will land inside a scanned path.**
- **Deptrac `Domain: ~` means depends on NOTHING** — it blocks a Domain class importing any port or Application type. Strong, and free. But **PSR interfaces (`ClockInterface`) are an uncovered/approved-external target and are NOT in `FORBIDDEN_FRAMEWORK_IMPORTS`** (`Illuminate\`, `Laravel\`, `Eloquent`, `Carbon\`) — a PSR clock enters a "pure" Domain object undetected.
- **`ElectionApplication: [ElectionDomain, Shared]` + TP-1 (contexts collaborate ONLY via events) means one context's Application layer CANNOT import another's port.** Reusing a sibling context's port and living inside a context are mutually exclusive under the current model. **Check this before planning port reuse across contexts.**
- **Existing precedents to REUSE rather than invent:** `ConstitutionalAssertionsTest::test_capability_decision_construction_exclusive` (forbid `new X` outside one designated class) · `VocabularyProhibitionTest::test_layer3_no_authority_vocabulary` (forbidden-word list over one directory) · `VoterSourceStrategyCallerEnforcementTest` (a targeted test CAN reach an `app/Console` file) · `EventRegistryCompletenessTest` + AT-EVT-001 (new domain events ARE caught).
- **Business-value defects are the blind spot: AP-1 (`max(1,$days)` clamping) and AP-2 (`60` in two homes) passed ALL FOUR gates** and were caught only by a human preservation review. **"No gate covers inventing a business value" is this project's demonstrated highest-risk gap** — treat manual enforcement of it as insufficient, not merely imperfect.
- **Proportional enforcement:** do NOT automate absence (*"no repository exists"*) — it over-fits and ossifies the model against legitimate change. Do NOT automate what a diff makes obvious (naming, a single named file). Do NOT automate organisational constraints (release announcement owners). **Automate what has already gone wrong once.**
- **Never record a completion statement asserting full enforcement when it isn't true** — that is Governance Verification Drift. Report the conditional instead.

## Sequence for a tactical concept (adopted 2026-08-01)

**Business Meaning → Ownership → Tactical Pattern → Construction Responsibility → Implementation.** Each stage removes an ambiguity the next depends on. Two traps this closes: **ownership does not imply construction** (who *invokes* construction ≠ where the construction *rules* live), and **ownership precedes placement** (deciding a home before an owner produces an orphan owned by nobody).

- **House VO construction idiom (verified, binding by precedent):** private constructor + **named static factory on the VO**, validating inside — `ChallengeRef::fromString`, `EvidenceSet::fromRefs`, `ContestedOutcomeRef::of`; coding-standard Rule 6.
- **VO constructors take BUSINESS VALUES ONLY** — never a port, config, Eloquent model or clock. Time-dependent questions take the instant as an **argument**. This keeps configuration supplying *numbers* while the domain holds the *concept*.
- **Factory names state a FACT, not a procedure:** `forElection(...)` ✅ · `calculate`/`build`/`createFrom` ❌ (orchestration verbs).
- **Reject patterns introduced only for purity.** A separate Domain Factory for a four-value VO is pattern purity without a domain reason. **Rejections must be recorded with their reason** — an unrecorded rejection reads as an omission and gets "fixed" later.

## Artifact authoring convention (2026-08-01, user correction)

- **Write governed artifacts DIRECTLY to their repository path.** Never stage them in a temp/scratchpad directory first. The repo is the single source of truth; an intermediate copy outside it makes the real location ambiguous -- the same ambiguity the Repository Integrity Gate exists to prevent.
- **Use the Write/Edit tools, not shell heredocs, for prose.** `cat >> file << 'EOF'` repeatedly failed on quoting (apostrophes, backticks, unicode) and produced `unexpected EOF`; the workaround was a temp buffer, which was the wrong fix. The Write tool takes arbitrary content with no shell parsing at all.
- Shell append is fine for short, plain-ASCII additions; anything with quotes, backticks or tables goes through Write/Edit.

## Authority STATUS check (adopted 2026-08-01)

- **Read the status column, not only the text.** An authority marked **Proposed / DEFERRED / POSTPONED** is **not binding** -- cite it as a *recorded limitation*, never as authority for what is permitted. *(ADR-T13 "DEFERRED (Proposed)" was cited as interpretive evidence once.)*
- **Check for supersession BEFORE citing.** Supersession is **forward-linked only**: a successor cites what it replaces (ADR-T23 supersedes ADR-T17), but documents citing the old one carry no pointer forward. **ADR-T17 has 6 live references presenting it as still open.**
- **Lifecycle states observed in this project:** Proposed -> Approved/Binding -> {Clarified (status annotation only; decision text immutable, ES-004.3) | Frozen | Superseded (old text preserved) | Deprecated -> Retired (one migration cycle, AST-008)}.
- **Authority is created only by explicit issuance (R-34):** reviewer observations default to *observation*; **nothing becomes a ruling by inference from praise or suggestion.** Authority comes from governance, not from being widely cited.

## Authority LEVEL check (adopted 2026-08-01, with Authority Verification below)

- **Before asserting a constraint, name its LAYER and confirm no higher layer speaks to the same question.** *"Which authority did I read?"* is necessary but insufficient -- the question is **"which authority GOVERNS?"** A correctly-read architecture document is the wrong source for a question constitutional policy answers.
- **Documented hierarchy** (`.claude/IMPLEMENTATION_PROTOCOL.md`, A-2): Business Authority -> Architecture Decisions -> Governance (ES-00x) -> Implementation Protocol -> Implementation -> Operational Evidence. **Never derive a higher-level rule from a lower-level artifact.**
- **`CLAUDE.md` and `.claude/MEMORY.md` have NO issuing authority.** They are Operational Guidance and may **point** to a constraint, never **be** one.
- **A governing authority is interpreted INSIDE ITS GOVERNING CONTEXT** -- its related invariants, surrounding decisions, governing assumptions and bounded context. *(A one-line summary of Policy 4 could not carry Policy 3's CL-1/CL-2/CL-3, however accurate that line was.)*
- **Practical test:** *can I quote it - do I know what governs it - do I know what it governs?*
- **Open precedence question (ARB, H-3):** ADR-T11 is labelled *"constitutional"* while sitting in the ADR layer; its precedence against **Constitutional Policy 3** is undocumented. Do not assume either outranks the other.

## Authority Verification (adopted 2026-08-01, after a withdrawn conclusion)

- **A conclusion that constrains, forbids or permits must cite the authority's OWN TEXT, read at the authority's OWN LOCATION.** A docblock, a charter summary, a MEMORY line or an earlier plan may **point** to an authority; none may **stand in for** one.
- **Corollary -- read the NEIGHBOURS.** Constitutional rules arrive in sets, and a summary preserves the item while discarding the set. *This is not hypothetical: MEMORY's four-policies line was cited all session while **Policy 3's CL-1/CL-2/CL-3** -- the authority most specific to the artifact under investigation -- sat unread beside it.*
- **Practical test before asserting a constraint:** *can I quote it, and do I know what stands next to it?* If either answer is no, the conclusion is not yet supportable.
- Chain: `Observation -> Evidence -> AUTHORITY VERIFICATION -> Interpretation -> Recommendation -> Authority Decision`. Never bypass the third step.
- **Primary locations worth knowing:** the four constitutional policies live in `docs/implementation/EPIC-003_Tactical_DDD_Entry_Assessment.md` §THE FOUR DECISIONS (**not** in MEMORY); ADR-T texts in `docs/adr/ADR-T-LOG-Tactical-Implementation.md`.

## Repository Integrity Gate (adopted 2026-08-01, after a conflicted-rebase incident)

- **Architectural verification depends on repository integrity.** The repository is the **evidence boundary** between implementation and verification: `Business -> Architecture -> Implementation -> **Repository** -> Verification -> Operational Evidence`.
- **The gate:** before any Architecture / Preservation / Decision-Authority / Governance / Acceptance review, confirm **working tree == index == HEAD**, with **no operation in progress**. If they disagree, **STOP** -- restore integrity first. Never draw an architectural conclusion from a state where the evidence source is ambiguous.
- **Run `git status` UNABRIDGED.** `git status -sb | head -1` shows only the branch line and **hides `UU` conflicts and staged deletions** -- this exact truncation caused a false conclusion once. Also check `.git/rebase-merge`, `.git/MERGE_HEAD` (a finished rebase can leave a **residual** unmerged index entry, where `--continue`/`--abort` do not apply).
- **Every architectural conclusion names its evidence source** (working tree · index · HEAD · a historical commit). Never mix sources.
- **Recovery rule:** before restoring a file from a commit, check for content unique to the working copy. Append-only artifacts (session logs, ES-004.2) are **appended to, never overwritten**.

## Constraints that bite (learned, verified)
- **Hook commands in `.claude/settings.json` MUST use `bash "$CLAUDE_PROJECT_DIR"/.claude/scripts/…`, never a bare relative path** (all 8 fixed 2026-07-30). Relative paths resolve against the shell's **mutable cwd**, so any `cd` into a subdirectory silently breaks every hook with *No such file or directory* — the scripts are present; the wiring was fragile. **Safety rule, not tidiness: those failures print as NON-BLOCKING, but while the path is unresolvable the exit-2 `db-safety-check.sh` gate (which protects the dev DB from `migrate:fresh`/`migrate:refresh`/`test --seed`) does not run AT ALL. An absent gate blocks nothing and reports nothing — the cosmetic symptom was the loud part, the unenforced gate was the real one.** After any hook-wiring change, live-fire it: benign → exit 0; `php artisan migrate:fresh` → exit 2 with the block message.
- **Engine is sovereign:** `Election.state` column is a compatibility cache; always assert/derive via `ElectionLifecycleEngine` / `currentState()`.
- **One transaction = one aggregate root + its outbox row(s)** (ADR-T1). The correction loop is 5 causally-linked transactions — never a saga (ADR-T8).
- **Cross-context identity crosses as strings only**; each context reconstructs its LOCAL VO (ADR-T16). Contestation's `DeterminationId` ≠ Adjudication's.
- **Anonymity (CI-5/Q7/VO-1)** is always-invariant: no payload/log/dead-letter row may allow voter↔vote linkage. `votes` has no user_id by design.
- **Governance vocabulary:** never hardcode denial strings like 'not eligible' in controllers (Phase_C25 guard); use resolver denial_reason or approved wording.
- **PHPStan gate:** `phpstan-greenfield.neon` (Contestation+Adjudication, max) is the official gate; run it, not ad-hoc paths.
- **Test DB:** PostgreSQL (`phpunit.xml`); RefreshDatabase only — never migrate:fresh against dev DB. (Root `CLAUDE.md` tech table refreshed 2026-07-11 — Laravel 11/PHP 8.2/PostgreSQL/Sanctum.)
- **Windows dev quirks:** shell-driven test helpers broke on `C:\` paths before (grep `:` split) — prefer pure-PHP file scanning in architecture tests.
- **Hydrator versioning rule:** vCurrent + vPrevious ONLY (`docs/implementation/Event_Registry.md`).
- **EPIC-002 strategic baseline is BINDING (ARB Resolution, 2026-07-25):** the accepted EPIC-002 artifacts (Canonical Context Map — 5 ratified BCs; Relationship Pattern Selection with adversarial review) are the authoritative strategic baseline for Tactical DDD. Changes route ONLY through the recorded falsifiability conditions / CB-1/CB-4 reversal condition / COL-3a revision trigger / new domain evidence — **never implicitly during implementation** (strategic-erosion caution recorded in `EPIC-002_Relationship_Pattern_Selection.md` §ARB RESOLUTION). Tactical entry items: Adjudication-name reconciliation (vs. implemented `app/Contexts/Adjudication`) + hybrid per-stream integrity assignments.
- **Responsibility Traceability Rule (ARB, 2026-07-26, mandatory acceptance criterion):** *responsibilities are discovered, not invented — every accepted aggregate responsibility must trace to explicit business evidence or a previously frozen ARB decision; untraceable → rejected or deferred.* Prevents responsibility inflation (duties accumulating because technically convenient).
- **DDD Tactical Governance Principles (ADOPTED, ARB 2026-07-26 — canonical home: `engineering/knowledge/methodology/DDD_Tactical_Governance_Principles.md` · PublicDigit binding: `docs/architecture/governance/DDD_PRINCIPLES.md` · early-promotion exception: R-39):** 7 principles + companions — Methodological Fitness Rule · APP · VODP (+ Domain-Event entry condition) · ASP · ADP (+ canonical tactical derivation chain, command derivation, Emergent Design Cluster vocabulary) · DMT (+ temporal-business-policy insight) · RMSP (+ reconstitution-is-not-an-occurrence, Domain-Service entry condition). Constitutional for tactical DDD work; the platform stays methodology-agnostic. Full text lives ONLY in the canonical doc (MEMORY = hints; F-OQ2 consolidation, product side).
- **Determination tactical chain = FROZEN BASELINE (ARB closing assessment, 2026-07-26):** EPIC-004B..J (Aggregate Evaluation → … → Domain Services, zero confirmed) is complete and frozen as the tactical governance baseline. **Decisions evolve by SUCCESSION, never retroactive editing** (superseding ADRs; recorded reversal conditions). Remaining work is the next evolution cycle, not open exploration: the 7-member PM cluster · the Q-2/finality center · Jurisdiction semantics (non-blocking) — *(the cluster and Q-2 center were RESOLVED 2026-07-26; see the APM entry below — this clause stands as history)*. Methodology self-governance: principles are promoted only on repeated evidence and explicitly NOT promoted when evidence is domain-specific.
- **APM architecture APPROVED + all gates closed (ARB, 2026-07-26 — `EPIC-004K` + `EPIC-004_Q2_Resolution_Package.md`):** the Adjudication Process Manager is the loop's HEAD, never its coordinator (ADR-T8 stands); states Opened→Assembling⇄AwaitingDecision→Concluded(RulingRequested|FailureDeclared)|Expired. **Issued: ADR-T21** (ChallengeRouted → published language; mint relocates to chain head) · **ADR-T22** (DeterminationIssued additive schema evolution; EvidenceSet carrier; R-4-expanded fixation at aggregate issuance) · **ADR-T23** (supersedes ADR-T17 — the authority decides, the PM receives). **Q-2 GOVERNANCE GATE CLOSED (the gate, not the business policy forever — ARB wording):** finality = temporal business policy (window closure; scheduled evaluator; NO FinalizeDeterminationCommand); horizon expiry = return to Contestation, late decisions never honored; `DeterminationFinalized` reversal condition ARMED (activation evidence: a *designed* retention consumer). **Q-2 bootstrap values RATIFIED as implementation bootstrap defaults, NOT constitutional defaults** (CW 30/30d · MAD 60d — weakest, priority for stakeholder review · LSM 30d · EPW 120d · demo exempt); stakeholder review mandatory on every row; constitutional values may replace bootstraps without architectural redesign. **Implementation authorization = REFINEMENT SCOPE ONLY** → the refinement's root artifact is APPROVED and IN FORCE: `EPIC-004_Architecture_to_Implementation_Roadmap.md` (8 WPs, acyclic, WP-5 ruled INTO EPIC-004; measurable conformance gate: *no deviation from a frozen ADR/invariant without a recorded ARB decision*). **WP-1 is the SOLE authorized implementation slice** (EvidenceSet VO · DeterminationIssued v3 · hydrator window (v3,v2), v1 retired · aggregate `issue()` per ADR-T22); every other WP opens only on its predecessor's ARB slice acceptance. **Architecture governance: CONCLUDED · Implementation governance: ACTIVE · Implementation execution: BEGINS with WP-1** *(precision per closing ARB minute — governance being in force is not software existing)*. **Closing ARB minute (2026-07-26):** the approved architecture, successor ADRs, Q-2 governance decisions, and implementation roadmap collectively constitute the governing baseline for implementation; WP-1 is the sole authorized slice; subsequent slices require their own acceptance; architectural modifications remain subject to the recorded reversal and governance conditions.
- **ARB Discriminator Principle (adopted 2026-07-25, reusable beyond this EPIC):** *if a single unresolved business question has been identified as the discriminator between multiple valid architectural realizations, the ARB avoids choosing among those realizations until that question is resolved, unless there is a compelling delivery reason to proceed.* Corollary (Vernon): some tactical decisions are blocked by strategic uncertainty — continuing tactical modeling past them produces consistent models on wrong assumptions.
- **Tactical DDD methodology FROZEN (ARB, 2026-07-25 — no process changes without operational evidence of a concrete weakness):** Strategic DDD → Entry Assessment → Tactical Work Package → Tactical Discovery → Aggregate Discovery (candidates only) → **Aggregate Evaluation** (per candidate: boundary sketch → 6 consistency criteria + **Criterion 7 Business Decision Authority** → confidence → alternatives preserved) → ARB decision → Responsibilities → Invariants → VOs → Domain Events → Commands → Repositories → Domain Services. Rhythm: **one artifact → ARB review → refine → freeze → next. Evaluation recommends; the ARB decides.** Architect-coined concepts (e.g. "AdjudicationProceeding") are hypotheses carrying a higher burden of proof, not discovered business language.
- **The four constitutional policies (ARB, 2026-07-25 — binding inputs to Tactical DDD; full text `EPIC-003_Tactical_DDD_Entry_Assessment.md` §THE FOUR DECISIONS):** (1) **Superseding Constitutional Publication** — published results NEVER modified/deleted; corrections are new publications that supersede (not replace). (2) **Evidence Preservation Window** — retention = contestation window + max adjudication duration + legal margin, per election instance; evidence for a permissible challenge never expires while the challenge can be initiated/resolved. (3) **Phase-1 Custodial Constraint** — codes→votes linkage is intentional (custodial investigation only, privileged+audited access, every lookup becomes evidence, reviewed at Self-Verifying Integrity); NEVER claim "the system is anonymous" — correct wording: *operational ballot secrecy, not cryptographic unlinkability*. (4) **Quarantine Pending Determination** — detection may be automatic, correction may not; automated verification never determines significance or alters constitutional records.

## AI/PublicDigit Engineering Platform (durable — Baseline v1.0, 2026-07-08)
- **Authority:** `engineering/architecture/adr/ADR-AIP-01` (Baseline + Construction Addendum) + `ADR-AIP-02` (AIP-14 Product Primacy) · corpus `engineering/architecture/baseline/Phase-0*.md` (FROZEN) · rulings register (Living, append-only): `engineering/architecture/adr/ADR-AIP-LOG-Platform-Rulings.md` (R-30..; R-1..R-29 historical in sealed Phase-02.5 §6).
- **Namespace (EM-001, 2026-07-10):** `docs/`+`architecture/` = Product · `engineering/` = Engineering Platform (entry: `engineering/README.md`; audit: `engineering/MIGRATION_REPORT.md`) · `.claude/` = runtime mount point (never moves). Create folders only when the first artifact arrives — no speculative namespaces.
- **Registry-first workflow is BINDING:** every `.claude` artifact: register in `.claude/platform/registry.yaml` (CMP/AST ids, five-question trace: capability→context→principle→decision→ADR) → review → implement → verify. Guide: `developer_guide/ai_platform/01_registry_first_workflow.md`.
- **Verification Engine = measuring instrument (R-26):** run → capture → PASS/FAIL → stop; no interpretation inside the instrument.
- **GOVERNANCE FREEZE (R-27):** no new platform principles/architecture docs until the PB-004 usage retrospective; amendments only (AIP-13); platform-only work needs ARB approval (AIP-14); retrospective includes a deletion goal.
- **STRUCTURAL FREEZE (R-37, 2026-07-10) — Engineering Platform Baseline v1.0 is frozen** (baselines freeze; architectures evolve — never say "the architecture is frozen"). Until C3 + PB-004 + retrospective: `engineering/` permits only bugfix/broken-link/typo; burden of proof reversed — *every architectural idea is guilty until proven necessary by implementation evidence*; expansion proposals without demonstrated insufficiency are rejected by default.
- Platform serves PublicDigit delivery — the standing question: *smallest platform change that unlocks the next PublicDigit capability?*
- **AIP observation stop (ARB, 2026-07-10):** unless implementation exposes a genuine deficiency in the Engineering Platform, do NOT add further AIP observations — record only implementation evidence required to complete the current ticket. Everything else waits for the retrospective.
- **Implementation-first default (ARB, 2026-07-10):** the architecture and engineering platform have demonstrated stability — every new ticket is an IMPLEMENTATION ticket by default, not an architecture ticket. The question is *"can I implement this capability within the approved architecture?"* — only a NO (with implementation evidence of insufficiency) opens an ADR/ARB discussion.

## Review taxonomy — TWO contracts, and a third was DECLINED (2026-07-30)
- **Artifact kinds → review contracts:** architecture knowledge → **Knowledge Contract Review** · decision record → **Authority Fidelity Review** (provisionally recognized, n=1). **A "Strategic Model Publication Review" was proposed for publication artifacts and RECOGNITION WAS DECLINED** — the Contract Differentiation Assessment found **ZERO of its eleven findings needed a capability the KCR lacks**. **Publication artifacts are reviewed under the KCR with a reading emphasis: objectives 7 (currency) · 1 (marker discipline) · 11 (publication responsibility).**
- **CONTRACT ADMISSION RULE (GOVERNED, n=4 — Discipline Amendment 3): *artifact kinds may define review EMPHASES, but review CONTRACTS are introduced only when operational evidence demonstrates a review capability that existing contracts cannot provide.*** Four separated concerns: **artifact kind** (knowledge · decision record · publication) ≠ **review contract** (KCR · AFR) ≠ **review emphasis** (a named weighting of an existing contract's objectives; **PROVISIONAL at n=1** — publication = obj 7 currency · 1 marker discipline · 11 responsibility). **PROMOTION REQUIRES CROSS-CONTEXT EVIDENCE, not bare n=2: at least one additional emphasis in a DIFFERENT artifact category or review situation that demonstrably improves review effectiveness without needing a new capability — because two publication emphases would be repetition within one lineage, which is the R-M6-6/T-2 problem (correlated agreement ≠ independent confirmation). Demotion if an emphasis changes no reviewer behaviour.** ≠ **the capability test**. **Artifact kinds are NOT one-to-one with contracts, and that asymmetry is the point.**
- ***Usefulness is evidence for an EMPHASIS. Unique capability is evidence for a CONTRACT.*** (Many review ideas are useful; very few justify becoming governance constructs.) The n=4 evidence base is **four FALSIFICATION ATTEMPTS with both positive and negative outcomes** — three declined, one recognized — not four confirmations; *a criterion that cannot fail is not a criterion.*
- **Reading order (NOT a dependency chain): artifact kind (HOW we read) → review emphasis (WHAT WE EMPHASIZE) → review contract (WHAT WE VERIFY) → review capability (WHETHER A NEW CONTRACT IS JUSTIFIED). The dependency runs emphasis → contract, upward against the reading order — an emphasis is *of* a contract.**
- **GOVERNED (n=4 across THREE artifact kinds — Integrity Model quality C): *an artifact's authority is determined not only by its stated purpose but also by the strength of the claims it makes. Disclaimers define INTENDED responsibility; wording determines EXERCISED responsibility.*** Instances: KC-1/KC-2 (knowledge artifact) · PUB-1/2/3 (publication) · RET-1/RET-2 (retrospective). **In every case the declaration was correct and the wording exceeded it.**
- **TWO REMEDY CLASSES, not interchangeable: EMPIRICAL over-generalization (claim beyond its evidence) → ADD SCOPE, and the claim survives at true size · NORMATIVE over-generalization (stops describing, starts prescribing) → CHANGE CLASS; scoping does NOT repair it ("in Phase II, a control is only proven by…" is still a prescription) — reclassify as Recommendation or route to MCA→CDR.** ***Scoping fixes an overreach of extent; only reclassification fixes an overreach of kind.***
- **REVIEW EMPHASIS is now GOVERNED (promoted 2026-07-30 on cross-context evidence). Governed emphases: publication (obj 7·1·11) · retrospective (obj 11·1·5·4). Contract count stays at TWO.** Demotion: an emphasis that changes no reviewer behaviour is not an emphasis.
- **META-LEARNING SEPARATION (durable): *a retrospective preserves what the PROGRAM learned; it must not accumulate what its REVIEWERS learned.*** When applying review remedies to an artifact, put the **correction** in the body and the **justification** in the Disposition History or the review record — a bare finding-pointer in the body is enough for auditability. *Generalizes: review-derived governance reasoning belongs to the framework, not to the reviewed artifact's historical record. Violating this is responsibility creep committed while remediating responsibility creep.*
- **Rule 13 already requires verifying/recording a changed finding — do NOT add a new rule for it.** AF-6's withdrawal and RET-3's narrowing are Rule 13 operating. ***A framework that adds a rule every time an existing rule performs well is accreting on its own successes.***
- **Verify a finding against the artifact's own supporting evidence BEFORE calling it a defect** — RET-3 shrank from a Minor defect to a one-word relabel on verification. *A review that will not do this produces the over-claiming it exists to catch.*
- ***A distinct lens is not a distinct capability.*** ***A named emphasis costs nothing; a named contract costs a governance construct.*** (Governance economy: contracts are scarce constructs, emphases are lightweight applications.)
- **THE TEST for any new review contract: not "is it useful?" and not "does it ask a new question?" but *could an existing contract have FOUND these defects?*** The AFR passed it on one named capability (register comparison; AF-3/4/5 reachable by nothing else). The Publication Review failed it. ***A distinct lens is not a distinct capability.*** **Record so far: 2 declined at n=0, 1 recognized at n=1, 1 declined despite eleven real findings.**
- ***"No existing contract asks my question" is a WEAKER claim than "no existing contract can find my defects."*** Do not mistake the first for the second.
- **GOVERNED publication principle (n=2, Integrity Model quality D): *an artifact may state where things STOOD; it must not state where things STAND unless it is maintained.*** Instances: AF-1 (decision record) · PUB-4/5/6 (publication artifact). **Remedy is a choice: maintain it, or date-mark it and point at the authoritative current state.**
- **A placement observation becomes a DEFECT only when the placement demonstrably misleads** (PUB-10's reclassification).
- **M8 is now READY for gate #2** — PUB-1..PUB-11 disposed and applied; **no model content was ever at issue and none was touched.**

- **Artifact kinds → review contracts:** architecture knowledge → **Knowledge Contract Review** · decision record → **Authority Fidelity Review** (provisionally recognized) · **publication artifact → Strategic Model Publication Review (n=1, UNRECOGNIZED — recognition precedes disposition).** **Each was admitted only when an artifact of that kind needed a question the existing contracts do not ask** — the same rule that DECLINED *Governance Integrity Review* and *Methodology Review* at n=0. **Do not add a contract without an artifact demanding it.**
- **General rule, now seen in TWO artifact kinds (AF-1 in a decision record, PUB-4/5/6 in a publication artifact): an artifact may state where things STOOD; it must not state where things STAND unless it is maintained.**
- **`Assembly:`-class discipline: a marker that means "asserts nothing beyond the sources" must never carry an evaluative superlative — an epistemic marker that is only sometimes accurate is worse than none, because it is trusted.**
- **M8 is NOT ready for gate #2** — 11 publication findings (PUB-1..PUB-11), **no model change required, no model drift found.** PUB-7/8/11 originate in the CCP-1 execution: **M8 has no 2026-07-30 Disposition History row because CCP-1 §11.6 specified history rows for AD-1 and C4-1 and none for M8.**

## Phase II REVIEW PROGRAMME COMPLETE (2026-07-30) — durable
- **All six reviewed, disposed, promoted, closed: AD-1 · C4-1 · M7 · M8 · RET-1 · AFV-1.** **"Closed" is not uniform: four by issued close-out; C4-1 and M7 by DISPOSITION AND EXECUTION (the close-out instrument was adopted mid-programme). Do NOT backfill close-outs for them — that manufactures a record of an act that never occurred.**
- **REOPENING CONDITIONS — no review reopens absent one of these four:** an approved change request against a promoted artifact · newly admitted governing evidence · an Authority disposition changing the baseline · **an independent assurance activity finding a new issue.** **Three are internal; the fourth requires a second lineage this programme cannot supply.**
- **TWO KINDS OF AUTHORITY (Integrity Model quality B): NORMATIVE = defines/constrains (AD-1, M6 dispositions, DAR-1) · EVIDENTIAL = attests, constrains nothing (AFV-1, C4-2, KBI-1, ERV-1). *PROMOTION DOES NOT IMPLY NORMATIVE AUTHORITY* — never cite a promoted assurance record as a constraint (`cited ≠ mandated`, one level up).**
- **✅ METADATA DEFECT CLOSED across all six promoted artifacts (2026-07-30): each carries exactly ONE `Status` row.** The pattern to reuse when an artifact's status changes: **rename the prior row to *"Status at execution/issuance (retained as history — TRUE WHEN WRITTEN, superseded …)"* — rename, never rewrite; never add a second row with the same key.**
- **(superseded) METADATA DEFECT once open in THREE promoted artifacts — C4-1, M8, RET-1** (M7 fixed 2026-07-30 as M7R-1; AD-1 and AFV-1 were always correct): each carries **two rows keyed `Status`** (execution + promotion) with no precedence. **Fix = rename the original to *"Status at execution (retained as history — TRUE WHEN WRITTEN, superseded …)"*. Content-neutral; one Authority instruction covers all three.**
- **OBSERVATION→FINDING discriminating tests (one per reclassification exercised): PUB-10 — *a placement observation becomes a defect only when the placement demonstrably misleads*; AIA-R2 — ***modal form vs operational semantics***: a directive modal is a finding only where the artifact's BEHAVIOUR actually constrains.** **AND: an OBSERVATION IS NEVER REMEDIATED, even when the fix is one word** — repairing it while calling it an observation blurs the boundary the rule exists to hold.
- **OBSERVATIONS vs FINDINGS (canonical spec, n=4): FINDING = materially affects authority · decision integrity · traceability · governance boundaries · historical correctness · review conclusions → REQUIRES DISPOSITION. OBSERVATION = characteristic / refinement opportunity / presentation inconsistency → CARRIED WITHOUT REMEDIATION. Do NOT elevate without governance impact.** ***The incentive runs one way — a review reporting findings looks more valuable than one reporting observations, and this distinction is the only defence against that pressure.***
- **TRUSTWORTHINESS TEST (canonical spec): REPLAY (reproduced solely from recorded evidence) · AUDIT (reconstructed without external explanation) · INSTITUTIONAL TRUST (trusted without knowing who drafted it — authority from commissions, evidence and ROLES, not personal credibility). FAILURE OF ANY ONE LIMB is a governance weakness; they are not a score to average.** **The third fails invisibly while the individuals remain available.**
- **STOPPING SIGNAL: when added review scope stops producing findings before it stops producing confidence, the artifact's defects have been found.** (M6 Authority Disposition: three issuances, three passes, one Minor finding + one Observation, both from pass one.)
- **KNOWLEDGE GOVERNANCE LIFECYCLE (canonical spec, 8 stages): Discovery → Review → IMPACT ASSESSMENT → Authority → EDITORIAL WORK → Consolidation → Strategic Modeling → Certification.** *Impact Assessment is a governance SAFETY MECHANISM between Review and Authority (it analyzes consequences so the Authority inherits no hidden assumptions); Editorial work follows Authority.* ***The separation both exist to hold: Authority changes GOVERNANCE state, editorial work changes REPRESENTATION state, never both in one commission — "Authority resolves uncertainty; editors implement resolved uncertainty."***
- ***A new LIFECYCLE STAGE does not imply a new review EMPHASIS*** (ARB threshold decision 2026-07-30). Lifecycle stages and emphases are counted independently — Execution Readiness Verification is a stage; it reuses the controlled-change emphasis. **CCP-1 designs execution, ERV-1 verifies the design: two artifact kinds, ONE responsibility family.**
- **ANNOTATION vs AMENDMENT: an amendment alters what an artifact SAYS; an annotation records what was later LEARNED about it.** **A correct finding needs no amendment where the artifact has already fulfilled its governance responsibility** — and where the artifact **explains a later act**, it must remain as it was when that act was taken (ERV-1 explains CCP-1 Amendment 1). *Prefer annotation.*
- **Acceptance of EVIDENCE is not acceptance of the LESSON** — disposing a finding ACCEPT gives its routed PMR candidate no new standing.
- **ENDURING GOVERNANCE EVIDENCE vs SUPERSEDED VERIFICATION: a superseded verification is one whose findings were OVERTURNED; one whose findings were RESOLVED into the thing it verified is part of the causal chain and stays live.** (ERV-1 → CCP-1 Amendment 1.)
- **A CORRECT finding with an UNDER-SCOPED remedy is a distinct failure mode from a wrong finding — and only execution distinguishes them.** (ERV-F6 specified history rows for C-09/C-15/C-16, not for M8's C-19..C-21 → PUB-7.)
- **TESTABLE determinism questions (ask both, separately, of any change plan): L1 — *"Did any editor author text?"* · L2 — *"Did any editor infer an unstated obligation?"*** **A plan can PASS L1 while FAILING L2 — CCP-1 did.**
- **FRAMING RULE (do not get this wrong in the MCA): CI-1 did NOT fail. *CI-1 completely protects Level 1 determinism; Level 2 was never its subject.*** ***A rule that fully achieves what it was written to achieve has not failed*** — faulting it would rewrite history.
- **TWO LEVELS OF EXECUTION DETERMINISM (vocabulary carried by PMR-7): L1 TEXTUAL — editors never invent wording · L2 OPERATIONAL — editors never infer obligations that are not explicitly specified.** ***Satisfying L1 does NOT guarantee L2.*** **CI-1 forbids editor-authored substance — it prevents INVENTION, not INFERENCE, which is why CI-1..CI-7 passed five Level-2 gaps.** **Test future change plans at Level 2 explicitly.**
- **PMR-7 (CANDIDATE, not adopted — raised 2026-07-30): *a change item must specify the OBLIGATIONS an edit incurs, not only the WORDS it writes.*** Evidence: five gaps in CCP-1's executed plan (C-06 a *form* not a wording · C-12's unfinished sentence · C-13's unspecified count · C-19..C-21's missing history row · the amendment record's missing supersession retention), **each producing a downstream defect, one Major and promotion-blocking.** ***Refines ERV-F1's "pre-supplied is not the same as determined" — CCP-1 answered the wording half and left the obligation half.*** n=5 but ONE lineage. **ROUTED TO MCA 2026-07-30** at the METHODOLOGY altitude: *define operational determinism*, NOT *add five clauses* — a clause list encodes one execution's gaps; a defined property generalizes.
- **A plan that reads as a plan after execution is NOT stale — it is a plan.** Do not raise currency findings against executed plans' forward-looking sections.
- **TEN emphases · FIVE families** (preservation 5 · interpretation 2 · decision 1 · consequence 1 · execution 1)
- **(superseded) NINE emphases · FOUR families** (preservation 5 · interpretation 2 · decision 1 · **consequence 1**) **· ONE contract · ZERO new review contracts.** Each stage **consumes the previous and produces the next**; **no stage may repeat or replace another.** *A stage consuming something other than its predecessor's output, or producing something other than its successor's input, has stepped out of position.* **The Authority stage does not create knowledge — it creates COMMITMENT.** Decision-layer artifacts must keep five concepts unblurred: **Evidence · Interpretation · Decision · Governance · Execution.**
- **DECISION MODEL INTEGRITY (review area for decision-layer artifacts): every decision needs 8 properties** — derived-not-preferred · explicit scope · explicit authority · explicit rationale · explicit confidence · explicit downstream consequences · explicit reopening conditions · **immutable except through additive governance** — **plus 4 aggregate: reproducible · auditable · bounded · historically stable.** ***A strategic decision must never depend on undocumented reasoning.***
- **AUTHORITY RESTRAINT, POSITIVE FORM (the negative form alone is insufficient): decide only what has reached governance MATURITY · preserve uncertainty · bind reopening conditions · commission rather than perform · record without rewriting · govern through explicit authority, not implicit interpretation.** ***A mature Authority record is characterized as much by the decisions it refuses to make as by those it records.***
- **CLOSING PRINCIPLE: the strongest record can be REPLAYED from its evidence, AUDITED from its history, and TRUSTED without knowing who produced it.** **The third limb is operative and easiest to fail — it requires attribution to ROLES not persons, and the creating act located in the COMMISSION not the drafter.**
- **THREE FAMILIES of review emphasis: PRESERVATION-oriented** (knowledge · retrospective · architecture definition · transformation · representation) **· INTERPRETATION-oriented** (discovery · relationship) **· DECISION-oriented** (authority disposition). *A decision review compares an act against **the authority that permitted it**; its characteristic finding is an act that exceeded, or a power used where restraint was required.*
- **AUTHORITY RESTRAINT is the hardest property to evidence in a governance record, because a refusal leaves no artifact behind — the substitute is leaving the REASONING behind.** The M6 Authority Disposition is the canonical instance: *"a demotion whose deciding probe is not auditable must not be fixed by authority act; that would convert a recording defect into settled strategic structure."*
- **Re-issued commissions: if a review has just been performed under a near-identical contract, EXTEND it with an addendum covering only the genuinely added scope — do NOT re-run.** A re-run creates two competing accounts of one assessment (the KC-4 condition).
- **TWO FAMILIES of review emphasis: PRESERVATION-oriented** (knowledge · retrospective · architecture definition · transformation · representation) **vs INTERPRETATION-oriented** (discovery · relationship). ***A preservation review diffs against an existing source; an interpretation review has none — it tests whether interpretation stayed subordinate to evidence, i.e. whether the artifact REFUSED to complete what its evidence left open.*** Canonical instances: **M7's ACL refusal** · **M6's demotion of its own strongest candidate** — in both, the correct output was a **refusal**, which no preservation check would register as a result.
- **⚠️ (superseded entry) METADATA DEFECT to fix across FOUR promoted artifacts (created 2026-07-30 by the promotion acts): C4-1, M7, M8 and RET-1 each carry TWO rows keyed `Status` (execution + promotion) with no precedence.** **The correct pattern is AD-1's / AFV-1's: rename the original to *"Status at execution (retained as history)"* / *"Status at issuance (retained as history — TRUE WHEN WRITTEN, superseded …)"*.** M7R-1 raised this for M7 only.
- **SEVEN review emphases, one contract, zero new capabilities. FIVE review a kind of PRESERVATION** (knowledge · historical · architectural · transformation · representational); **the SIXTH — DISCOVERY INTEGRITY — does not: it asks *was something discovered without being invented?*** *The contract extends past preservation.*
- **M6 is FROZEN under change control with BINDING enumerated reopening conditions — a STRICTER regime than promotion. Findings against it route through MCR-4's TWO FOLD CLASSES: evidence-neutral → appliable; assertion-changing → recommend + dispose separately. *Finding defects in a frozen artifact is not a reason to unfreeze it* — the Evidence Restatement stage exists so a correction can reach a frozen record without reopening discovery.**
- **AMENDING A FROZEN ARTIFACT — what an amendment standing actually protects (operational guidance from M6's five amendments, 2026-07-30):** a standing of the form *"nothing above this section has been edited"* protects **the substantive record (the body)**, **not the metadata block**. **Metadata describes the artifact's CURRENT governance state and has been maintained by every amendment; the body is historically preserved and edited by none.** So: **apply currency corrections in the metadata, record the correction statement in the amendment section, and leave the body untouched.** *(General form — "an amendment standing protects the substantive record, not the filing" — is a CANDIDATE beyond M6; n=1 artifact, not self-widened.)*
- **Three candidates sit with the Authority: the wider negative-claim rule · the DP-5 generalization (*representation conventions never justify represented knowledge*) · the general form of the metadata/body distinction. None self-admitted.**
- **GOVERNED review question (n=5, Method): *what is the artifact's honest failure direction?* — ask it of every artifact under every emphasis.** *An artifact that cannot say where it is most likely to be wrong has either not looked or is not saying — both are findings.*
- **M6's items are CLOSED (applied 2026-07-30 as MCR-4 class-1 folds at §14 Amendment 5; M6 remains FROZEN). Former text: M6R-1** (§14's correction names §7.5 only; the superseded *"cohesion FAIL"* still stands at §7's confidence table and §12's self-verification — §14 tallies **3 of 7, FAIL NOT met**; class-1 fold) · **M6R-2** (Status's programme-position clause stale; retain its commission-scope clause).
- **FIVE review emphases, one contract, zero new capabilities — each reviewing a different KIND OF PRESERVATION:** knowledge (publication) · historical (retrospective) · architectural (architecture definition) · transformation (transformation fidelity) · **representational (representation fidelity)**. Canonical spec: `PKS_Knowledge_Contract_Review_Method.md`.
- **REVIEWING A PROMOTED ARTIFACT = a POST-PROMOTION ASSURANCE REVIEW: its findings are CHANGE-CONTROL PROPOSALS, not remediations. A reviewer may not apply them.** (C4-1's review, 2026-07-30 — basis: reopening condition 2.)
- **CHANGE-CONTROL PATTERN (set by the first such act, C4-1 2026-07-30): before accepting an item against a promoted artifact, ask *does it touch architectural meaning?* If no → content-neutral application, no re-verification. If yes → re-verification against the governing artifact and a heavier instrument. *An act on a promoted artifact should be no heavier than the change requires.*
- **CANDIDATE, not admitted (ARB observation 2026-07-30, n=1 artifact): *representation conventions never justify represented knowledge — only governed evidence does.*** Generalizes DP-5/KO-1/KO-3 to UML, BPMN, ER, sequence diagrams, docs, ADRs. **Natural home if admitted: quality B (a convention is not an authority basis — the *cited ≠ mandated* family). NOT self-admitted: the ARB characterized, it did not direct, and one artifact's evidence is a candidate.**
- **C4-1's change-control items C4R-1/C4R-2/C4R-3 are CLOSED (applied 2026-07-30). Former text: C4R-1** (the third view is named *Knowledge Structure View* in §6 but *Responsibility Allocation View* in §1/§8/§10, and §1 reintroduces the repudiated *"substitute"* framing — **under-representation, a fidelity defect in the opposite direction from all others**) · **C4R-2** (ten omissions counted as nine — my C-13 insertion) · **C4R-3** (§10's stale *"promotion is not commissioned here"*).
- **IBC-1 revision is a BASELINE-CONFORMANCE activity, NOT a review** — align IBC-1 to the governing baseline; do not re-evaluate promoted artifacts.

## SIX Phase-II artifacts PROMOTED (2026-07-30) — durable
- **PROMOTED and GOVERNING: AD-1 · C4-1 · M7 · M8 · RET-1 · AFV-1** (six separate Authority acts; record `PKS_Phase_II_Execution_Record.md`). **None is freely editable — change control applies.**
- **AFV-1's promotion is DIFFERENT: it is NOT constraint-defining (ADR §3.1.1).** Promotion makes it the **authoritative assurance record of the Model→Architecture link** — **no implementation constraint, no architectural authority, and it does NOT convert fidelity into correctness** (the T-2 *"attests fidelity, not correctness"* ceiling stands). Its negative claims rest on **M6 §3.1–§3.4's (b) columns + M0–M8/DAR-1/RET-1, NOT exhaustive search**. Scope: **Model→Architecture only** (C4-1/C4-2/KBI-1 excluded). §13 item 6's insight is still a note, **not** an admitted methodology proposal.
- **GOVERNED (n=3, Integrity Model quality D corollary): *review records are amended for CURRENCY, never rewritten for OUTCOME.*** Date-mark; do not rewrite. ***A rewritten verdict erases the evidence that the review process worked*** — AFV-1's §13 *"AD-1 is not promotion-ready"* IS the record of why AD-1 was corrected.
- **NEGATIVE-CLAIM DISCIPLINE (Method, scoped to the transformation-fidelity emphasis): *negative claims require explicit evidence of SEARCH SUFFICIENCY.*** A positive claim is proved by exhibiting an instance; a negative one cannot be. **Disclosed and open for the Authority: the evidence supports a WIDER form — the reviewer's own AD-R1 (*"no governed rule forbids cycles"*) had the same defect. Not self-widened.**

## ALL FIVE Phase-II artifacts PROMOTED (2026-07-30) — superseded by the six-artifact entry above
- **PROMOTED and GOVERNING: AD-1 · C4-1 · M7 · M8 · RET-1** (five separate Authority acts; record `PKS_Phase_II_Execution_Record.md`). **NONE is freely editable any more — change control applies** (Consolidation §2.2 · Configuration Control).
- **Per-artifact non-changes that promotion did NOT alter — quote these when a promoted artifact is cited:** **C4-1: a promoted VIEW still holds NO AUTHORITY** (DP-7/AP-2; AD-1 governs on conflict, the view is corrected) · **M7: promotion restores NO withdrawn pattern name** (R-1 *pattern undetermined* · R-4 *no coordination pattern asserted*; cite the structural layer freely, pattern names only where they survived DAR-1) · **M8: promotion did NOT discharge Gate #2** (ARB output review outstanding; its findings would land against a promoted artifact) · **RET-1: promoting a retrospective did NOT promote its patterns** — P-1..P-11 are still CANDIDATES; pattern promotion needs MCA then CDR · **AD-1: AR-1/AR-2 still architecturally undefined, OQ-PKS-7 open, AP-3 UNDETERMINED, DR-7 descriptive not prescriptive.**
- **The promoted set is NOT certified, NOT independently confirmed, NOT complete, and NOT a closed question set. Operational Evidence certification dimension: ZERO, unmovable by this program alone.**
- **DR-7 reading guidance (binding, held OUTSIDE the promoted text): preserve *"triggers reopening the dependency model"*; do NOT strengthen to *"requires redesign"* or *"invalidates the architecture"* — reopening is a governance action whose outcome is deliberately open.**
- **U-2's translation obligation is still UNASSIGNED. Q-AD-1 (DR-5's "inward only" vs M7's "↔") is OPEN.**
- **CANONICAL KCR SPECIFICATION (`PKS_Knowledge_Contract_Review_Method.md`): invariant sections + the EMPHASIS as the ONLY interchangeable module.** Invariants: role · constitutional principle (*review the artifact as its own kind; do not redesign, improve, or optimize*) · artifact responsibility · the eleven objectives · **six review principles** · explicit non-scope · required output · success criterion. **Emphases: publication (7·1·11) · retrospective (11·1·5·4) · architecture definition (1·2·6·4) · full pass.** **Use this spec; do not author a new prompt structure per artifact kind.**
- **The six review principles: verify before criticizing · narrow findings when evidence requires · do not infer beyond the governed corpus · preserve historical correctness · separate observations from prescriptions · SEPARATE DESCRIPTIONS OF THE CURRENT STATE FROM CONSTRAINTS ON FUTURE STATES** *(the sixth is AD-R1 generalized — the temporal axis is where derivation most often overreaches; see also AP-3/C-04 and DR-7)*.
- **A PROMOTED artifact is not reviewable as a draft: a future concern is a CHANGE-CONTROL PROPOSAL, not a new review.** Q-AD-1 sits in that queue.
- **Next act: IBC-1 revision against the promoted baseline (13 recommendations, 9 mandatory-for-conformance; IBC-C1's precondition now met) → Implementation Handover Governance.**

- **`PKS_Phase_IIB_Architecture_Definition.md` is the PROMOTED, governing logical architecture of the PKS.** Promotion record: `PKS_Phase_II_Execution_Record.md`.
- **PROMOTION CARRIED NOTHING ELSE WITH IT — all still true of the promoted architecture:** Medium-High, one corpus, one lineage, **never citable as independently confirmed** · **OQ-PKS-7 OPEN and ARB-owned; AR-1's placement is CONTINGENT on it** · **AP-3's constitutional status UNDETERMINED** · **DR-7's acyclicity is a DESCRIPTION of the modelled graph, NOT a constraint on future elements** (an element closing a cycle is a *reopening trigger*) · **AR-1/AR-2 remain architecturally UNDEFINED — no component may be defined over them; DR-6 forbids assuming encapsulation, interface or internal structure** · §12's six questions + **Q-AD-1** open · Operational Evidence dimension **zero**. ***Promotion makes the architecture governing; it does not make it complete, certain, or closed.***
- **C4-1 · M7 · M8 · RET-1 are promotion-READY and NOT promoted** — per-artifact promotion is per-artifact. *Promoting the ready set because it happens to be ready substitutes readiness for decision.*
- **Q-AD-1 (open): DR-5 says the cross-edge dependency runs "inward only"; M7 records R-4 as "↔". Not resolvable from AD-1/M7/DAR-1 — needs a modelling or Authority reading.**
- **Emphasis result (exercised, not argued): THREE emphases — publication · retrospective · architecture-definition — on ONE contract, ZERO new capabilities. *Artifact kind determines the review EMPHASIS; the review CONTRACT remains stable.***

## Promotion chain — owners per stage (Discipline Amendment 2, 2026-07-30) — durable
- **Review Complete** *(reviewer)* → **Findings Dispositioned** *(Authority)* → **Remediation Verified** *(reviewer/verification commission)* → **Promotion Ready** *(**a DERIVED STATE, not a decision by anyone**)* → **Authority Promotion Decision** *(the act)* → **Promoted Baseline**. **No step may absorb the one after it.**
- ***VERIFIED-READY ≠ PROMOTED*** — the fifth member of the family: *frozen ≠ promoted* · *review outcome ≠ adoption* · *recommendation ≠ issuance ≠ adoption* · *closing a review ≠ adopting the artifact*. **Every member was added because the program conflated it once.**
- **Commission boundaries: a close-out must NOT absorb an adjacent commission's verification, even when the facts are true.** RET-1's close-out declined to claim PD-F1/PD-F2's discharge (that is Package D's record) — same rule that removed AFV-F4 from the ADR review ledger. ***That is how commission boundaries dissolve: quietly, and while everyone is being accurate.***
- **Closed review records are HISTORICAL and not to be extended** — a future review is a new commission with a new record. **Closing is right when remaining remedies only make claims smaller or labels more precise; beyond that point review starts correcting things that are not defects.**

## Package D CLOSED — §8 fully satisfied; promotion awaits the ACT, not a check (2026-07-30) — durable
- **PD-F1/PD-F2 discharged; Package D exit check re-run 17/17. §8 FULLY SATISFIED.** AD-1 **§11A** / C4-1 **§6B** retain superseded wordings in the **M6 §14 / Discipline Amendment 1 form — use that form for every future amendment.**
- **Disclosure that must travel with those sections: AD-1 and C4-1 are NOT version-controlled, so their superseded wordings are RECONSTRUCTED from the applied replacements, not recovered from a committed baseline** — faithful, but not independently attested. **And never invent a "before" for an item that only added content.**
- **PROMOTION READINESS: AD-1 · C4-1 · M7 · M8 · RET-1 · composite baseline ALL READY. KBI-1's "not yet promotion-ready" is DISCHARGED. No verification blocker remains.**
- **The next act is an Authority DECISION: per-artifact promotion. *Verified-ready ≠ promoted*** — the same family as *frozen ≠ promoted*, *review outcome ≠ adoption*, *closing a review ≠ adopting the artifact*. **Then IBC-1 revision against the promoted baseline → Implementation Handover Governance.**
- **Still true after promotion and not blockers: OQ-PKS-7 open and ARB-owned** (it stopped being a blocker *because* AD-1 qualifies its AR-1 placement instead of presupposing it) · **Medium-High, one corpus, one lineage — never cite as independently confirmed** · **Operational Evidence certification dimension at ZERO and unmovable by this program alone.**

- **§8 NOT fully satisfied. Failing criterion: X-4 / CI-6.** **PD-F1 (Major): AD-1's and C4-1's amendments did not retain their SUPERSEDED WORDINGS** — under forward-only supersession that makes the changes unauditable. **The established pattern that does it right: M6 §14 amendments and ARB-Discipline Amendment 1 both retain superseded text verbatim. Follow that form.** **PD-F2 (Minor): C-12's provenance note wrongly says the AR-negation list is "not in AD-1's derivations" — AD-1 §6.2 derives it.**
- **PROMOTION IS NOT AUTHORIZED and none has occurred.** Readiness: **M7 ✅ · M8 ✅ · AD-1 + C4-1 ready ON CONTENT, NOT ON RECORD · baseline NOT YET PROMOTION-READY.** ***The reason changed from "corrections unapplied" to "amendments unauditable" — nothing is wrong with the architecture; what is missing is the record of how it changed.***
- **Next act = ONE bounded authorized edit** (superseded-wording blocks for AD-1/C4-1; fix C-12's note), then re-run Package D's exit check.
- **Durable lesson: a verification package that could only ever confirm is not worth commissioning.** Package D found a blocker no individual change item was responsible for, after 20/21 items applied and every architectural check green.

## AFV-F4 disposed + CCP-1 executed (2026-07-30) — durable
- **AFV-F4 = ACCEPT.** AD-1's AR-1 placement is now **explicitly contingent on OQ-PKS-7's one-corpus reading**, which **REMAINS OPEN and ARB-owned** — the disposition qualified a placement, it did not answer a question. **ACCEPT was the narrow act; REJECT would have adopted the one-corpus reading architecturally (C-17, not applicable).**
- **CCP-1 executed 20/21** (C-17 REJECT-only n/a; H-1 held). **AD-1 was amended EXACTLY ONCE** per Ordering Rule 2. **C-18 bounded fidelity confirmation PASS 9/9.** C4-1 synchronized; M8's C-19/C-20/C-21 applied. **Bijective mapping preserved — no element added, removed, split, or merged.**
- **⛔ PACKAGE D (Promotion Readiness Verification) IS NOT DONE and is SEPARATELY COMMISSIONED. NO PROMOTION HAS OCCURRED.** Do not treat the completed edit queue as readiness — **CCP-1 made Package D a separate commission precisely so 20-of-21 could not be mistaken for done.** Untouched: promotion · IBC-1 revision against the promoted baseline · Implementation Handover Governance.
- **`PKS_Phase_I_ARB_Rulings.md` — review COMPLETE, document FROZEN; future changes require a NEW review commission.** No Authority Acceptance Commission was run (the issuer recommended against it).

## Governance-state vocabulary — CANONICAL (Discipline Amendment 2, ARB 2026-07-30)
- **TWO ORTHOGONAL AXES. Never flatten them into one list — that was the reconciliation the standardization required.**
- **Axis A — CONTROL ADMISSION** *(subject: rule · quality · objective · method · review contract)*: **Candidate → Provisional → Governed**; exits **Declined** (never admitted — insufficient evidence at the test) and **Withdrawn** (admitted, then removed by contrary evidence). **Declined ≠ Withdrawn** *(Rule 15 declined at n=1; AF-6 withdrawn on verification)*.
- **The verb varies BY SUBJECT and that is lawful, not drift: methodology → *certified* · rule/quality/objective → *adopted* · review contract → *recognized*. *Provisionally certified* / *provisionally adopted* / *provisionally recognized* are ONE state with correct subject–verb agreement — do not flatten to one verb, do not read as three states.**
- **Axis B — ARTIFACT LIFECYCLE** *(subject: a document)*: **Authored → Issued → Proposed → Adopted → Binding.** *Proposed* means no adoption record exists.
- **Axes are ORTHOGONAL:** a **Provisional** control may live in a **Proposed** document (the AFR does). Reading one axis's state as the other's is the error the amendment prevents.
- **Cross-cutting dispositions:** **Deferred** (unresolved, trigger named) · **Superseded** (replaced forward-only; original stands as history).
- **Coherence pass 2026-07-30 found NO conceptual conflicts** across the corpus (one editorial gap only: the Discipline lacks the explicit `Lifecycle status` field its siblings carry — recorded, deliberately not fixed). **PKS governance framework declared STABILIZED; primary effort returns to architectural work. Governance evolves only when operational evidence demands it.**

## PKS Phase I ARB rulings — fidelity-reviewed 2026-07-30; four items awaiting the Authority (durable)
- **`PKS_Phase_I_ARB_Rulings.md` now carries statement-class markers** (`[AUTHORITY ACT]` · `[RECORDED CONSEQUENCE]` · `[RECORDER DERIVATION]` · `[EXECUTION RECORD]`), a **per-ruling lifecycle table**, and `⚑ AF-n` flags. Review: `PKS_Phase_I_ARB_Rulings_Authority_Fidelity_Review.md`. **The six rulings are verbatim and faithful; every defect was in the consequence layer.**
- **Artifact-kind rule (durable):** a **decision record** is reviewed for **authority fidelity** (does it record the authority's rulings and only the authorized consequences?), **NOT** under the Knowledge Contract Review, whose criteria are built for architectural knowledge. **Different artifact kinds get different quality contracts.**
- **ROUTED TO THE DA/PA and still open (ARB accepted the findings but may not perform these acts):** **DR-2's Capabilities Pass was COMMISSIONED (non-gating) but NEVER EXECUTED** — no such artifact exists · **editorial items E-1..E-7 are UNDISPOSED and UNTRACKED** (they were foldable only under DR-1's *Accept with findings*; plain Accept was chosen, and the rulings record's own "did NOT dispose" list omits them) · **AF-2/AF-3/AF-4/AF-5 need Authority acts** a recorder may not perform.
- **Recorder discipline (durable, from AF-2/AF-3/AF-4):** never allocate a decision right in a record (*"scheduling rests with X"* is authorization) · when an authority rules **outside the offered option set**, say so and mark the consequence recorder-constructed — the menu is an aid, not a limit on authority · **never import content from an option the authority did NOT choose** (*"the precedent exists"* ≠ *"the exception remains available"*).
- **REVIEW TAXONOMY (principle recognized at n=2; do not grow it by symmetry):** architecture knowledge → **Knowledge Contract Review** · decision record → **Authority Fidelity Review — PROVISIONALLY RECOGNIZED 2026-07-30** (usable and authoritative for decision-record reviews; **NOT extracted into a framework document, NOT standardized/mandatory**). **Its ONE distinct capability: line-by-line comparison of each recorded consequence against the decision register — the options OFFERED and the option CHOSEN.** (AF-3/AF-4/AF-5 depend on it entirely; AF-1 and AF-7 were detectable by the KCR — the honest capability count is 1, not 5.) **Promotion to GOVERNED: a second independent decision-record review that again finds what no other contract would. DEMOTION if that review yields only KCR-detectable findings.**
- **Recorder-vs-authority boundary (durable, from the AF-2/3/4 dispositions): the ARB can settle what STATUS a recorder's statement has — *recorder derivation, conferring nothing* — but CANNOT convert an inference into an authority statement; that needs the authority who decided the matter. Dispositions of this class settle PROVENANCE, not content: repair the claim, never delete the content it mis-described.** **`Governance Integrity Review` and `Methodology Review` are DECLINED at n=0 — do NOT create them**; methodology is already governed through **MCA → CDR under Configuration Control**, so a new contract may be *unnecessary*, not merely unevidenced.
- **Sequencing rule (durable): recognize the review CONTRACT for an artifact kind BEFORE disposing findings produced under it** — otherwise dispositions rest on a contract with no standing. AF-1..AF-7 are therefore **HELD, not disposed**.
- **AF-6 was WITHDRAWN on verification** — the §4a fold is **aggregation, not expansion** (§4 claims scope over *"all open questions in the package"*; §4a is an erratum to that claim's enumeration). **Discovered en route: §4's table has 17 rows incl. the unnumbered Conformance entry, so DR-1's option text ("12 OQs and 4 collisions") undercounts the register — the rulings record is the more accurate of the two.** Do not re-raise AF-6.
- **Q-AF-1 open — the Authority's proposed model: Decision immutable · Decision RECORD immutable · Decision STATE mutable in a separate Decision Status Register** (event immutable, projection mutable). **Consequence pre-committed: if that register is introduced, the per-ruling lifecycle table currently inside `PKS_Phase_I_ARB_Rulings.md` MOVES there and only the date-marked pointer stays** — that table is knowingly misplaced (mutable state in an immutable record), retained only because removing it restores the stale-forward-statement defect.
- **A completeness register that is incomplete is worse than none** — it licenses the inference that anything absent from it was disposed.
- **Fixing a record is not the reviewer's act:** mark and flag in situ; do **not** delete, rewrite, or complete recorded content. Adding a true item to a record's own list is editing the record under cover of improving it — put it in a marked reviewer addendum.

## PKS-ADR-001 review commission — CLOSED 2026-07-30 (durable)
- **The Knowledge Contract Review Commission for `docs/adr/PKS_ADR_001_Implementation_Agnostic_Strategic_Baseline.md` is CLOSED** — formal close-out issued by the Authority in five clauses; record at `docs/implementation/PKS_ADR_001_Knowledge_Contract_Review.md` §14.8. **KC-1…KC-22 all disposed, zero rejections; F-3.2 and R-2 accepted and applied.**
- **The ADR is still PROPOSED. Closing a review is NOT adopting the artifact it reviewed** — `Authority Disposition` remains PENDING, and adoption is a separate act nobody has performed. *(Fourth instance of this defect class: review outcome ≠ adoption · approving M8 ≠ accepting a refinement to M8 · recommendation ≠ issuance ≠ adoption · closing a review ≠ adoption.)*
- **The review document is a HISTORICAL RECORD — do NOT extend it.** A future review of this ADR is a **new commission with a new record** (forward-only). No new finding may be added to the closed one.
- **Two governed terms to keep straight, both of which the closed review had to correct across 11 sites:** *"certified"* attaches to the **METHODOLOGY** (provisionally, two-dimensionally) — **never to the baseline, the constraints, the model, or the architecture**, which are **frozen / disposed** · IBC-1 is the **DESIGNED** semantic firewall — it is at verdict REVISE and is **not issued**, so never write that it *"is"* or *"provides"* the firewall.
- **Standing close-out shape (reusable): accept the record · charter fulfilled and closed · reviewed artifact's lifecycle status unchanged · execution work transferred out · framework questions unaffected.** Every clause exists because a real defect in this program made it necessary.

## Artifact ingestion rule (AI-sourced supporting material) — adopted 2026-07-28
- **Rule:** material the user feeds in — AI outputs (Gemini/ChatGPT/etc.), articles, research papers, notes, brainstorming — is **supporting input**, never a governed repository artifact by itself. Claude must read/evaluate it, extract evidence/findings/hypotheses, classify per the project's governance, and produce a **new** governed artifact with correct Kind/Authority/Status/Provenance metadata — never copy supporting material directly into a governed artifact. Provenance is preserved by reference (pointing at the raw file), not by embedding the source's review commentary inside the governed artifact. **Exception:** if the user explicitly states a document *is itself* the governed artifact, follow that instruction instead.
- **Why this exists:** surfaced when a raw Gemini-output file (`architecture/ai_architecture/documentation/what_gemini_suggested.md`) turned out to carry review commentary — including a simulated "if I were chairing the ARB, I would approve…" verdict — appended directly after the actual discovery content with no marker separating the two. Submitting it as-is to a real ARB would have let simulated-authority text ride along inside a candidate artifact (session log 2026-07-28, PKS track).
- **Placement scope note (same event):** for AI-engineering artifacts specifically, only `./engineering` and `./docs` are treated as official/governed locations — `./architecture` (repo root) is not, even though EM-001 groups `docs/`+`architecture/` as "Product" namespace generally. Narrow scoping, not an EM-001 override.
- **Wording discipline:** provenance/scope notes in a governed artifact must read as *descriptive* ("only X constitutes this artifact; Y is preserved as supporting material"), never *procedural* ("per [date] instruction, Y is excluded") — the latter reads as if content was altered after the fact rather than stating the artifact's scope.
- **How Claude uses this file:** read MEMORY.md + CONTEXT.md at the start of every task (existing Workflow rule below); precedence on conflict is **user instruction > repository governance (Constitution/Standards/ADRs) > this memory > default behavior** — memory is a hint layer, never authority (consistent with "MEMORY holds hints only," above). When a new stable convention like this one is established mid-work, record it here rather than letting it live only in a session log.
- **ENFORCED, not just stated (2026-07-28):** project knowledge may never go to global Claude storage. Two controls in `.claude/settings.json` — (1) `"autoMemoryEnabled": false` disables the auto-memory system entirely (read AND write); (2) `.claude/scripts/project-knowledge-guard.sh` is a **blocking** PreToolUse(Write|Edit) hook (exit 2) denying writes under `$HOME/.claude/{projects,plans,memory}/`. In-repo `.claude/` and the user's global `CLAUDE.md`/`settings*.json` stay writable. Verified live: the blocking path was triggered and confirmed. All project memory/context/plans/sessions live in-repo under `.claude/` (+ governed plans in `docs/plans/` per ES-004.2).

## User preferences (recurring)
- Chief-architect review loop: propose → review → apply refinements → freeze. Never start coding before the design artifact (IDD) is approved.
- TDD is mandatory and literal: RED confirmed before GREEN, every class.
- One micro-slice per session; stop at the ticket boundary; single NEXT ACTION always defined.
- **Developer guide per step is Definition of Done** (do NOT wait to be asked): one file per step under `developer_guide/<area>/` (+ `00_index.md`), snippets grounded in committed code, ending with a Traceability line. Standing rule in `.claude/CLAUDE.md`; reminder hook `.claude/scripts/dev-guide-reminder.sh`.
- **Development discipline is fixed: Business → DDD → Architecture → Tests → Implementation.** Never jump to a test/class before the DDD model + Architecture Decision that justifies it. Discovered gap: `Finding → Architecture Decision → RED → GREEN → Certification`. Architecture produces tickets; tickets never accrete into architecture (model the platform capability first). Standing rule in `.claude/CLAUDE.md`; tripwire hook `.claude/scripts/discipline-gate-reminder.sh`.
- **Engineering Standards are the constitutional truth — MEMORY holds hints only (consolidation 2026-07-11, PROPOSED):** canonical rule homes = `engineering/governance/STANDARDS_INDEX.md` → ES-001..ES-006. Quick hints (text lives in the ES docs, NOT here):
  · Rule parsimony + documents-record-governance → **ES-001**
  · Implementation-first default + observation stop (EEP/EP/ER registered) → **ES-002**
  · Qualification lifecycle (never fix in-run; PASS-AFTER-CORRECTION; F-/CR-/OQ- series) + score-persistence stop + measurement conventions → **ES-003**
  · Retrospectives-recommend + record conventions (append-only logs; ids not filenames; one responsibility per record type) + **artifact lifecycle consistency at slice closure (ES-004.3, R-41, role-based: Runtime describes today · Historical never rewritten · Reference tracks knowledge · Decision text immutable/status annotations evolve; sync touches ONLY the mutable portion)** → **ES-004**
  · Three-concern separation + folder rule + placement litmus → **ES-005**
  · Promotion ladder + research freeze (no Level 5) + harvest discipline + harvest question ("did this work REVEAL reusable engineering knowledge?" — No is the healthy default) → **ES-006 Engineering Knowledge Governance**
  · **Plans are TWO concepts (ADOPTED 2026-07-11):** Work Plan (Runtime, ephemeral, `.claude/plans/` = plansDirectory) vs Engineering Plan (governed EP-01 deliverable, `docs/plans/`, `YYYYMMDD-HHMM-<what>-plan.md`) — paper: `docs/implementation/Plan_Concept_Decision_Paper.md`; rulings register now R-30..R-40 *(hint corrected 2026-07-28: the stale "..R-38" nearly caused real ruling R-40 to be dismissed as fabricated — the register is the truth, this line is a hint)*
  · **Plans are TWO concepts (ADOPTED 2026-07-11):** Work Plan (Runtime, ephemeral, `.claude/plans/` = plansDirectory) vs Engineering Plan (governed EP-01 deliverable, `docs/plans/`, `YYYYMMDD-HHMM-<what>-plan.md`) — paper: `docs/implementation/Plan_Concept_Decision_Paper.md`; rulings register now R-30..R-38
- **Implementation execution protocol (OPERATIONAL PRACTICE, level 1 of 3 — NOT a governed standard; DA 2026-07-30):** 17 phases, `.claude/IMPLEMENTATION_PROTOCOL.md`. Status **OPERATIONAL — FROZEN for routine work (DA 2026-07-30)**: amendments ONLY on operational evidence from a completed work package (*"WP-X exposed a recurring problem"*), never on a good idea; design discussions are recorded as observations. Lifecycle Draft→Operational→Qualified→Governed→Retired. **Canonical precedence: Business Authority → Architecture (ADRs/ARB) → Governance (ES-00x) → Protocol → Implementation → Operational Evidence.** Governs implementation execution **unless superseded by explicit architectural authority or a later governance decision** — no protocol outranks authority. Promotion to Project Standard reviewed ~WP-3/WP-4 on operational evidence; amendments must themselves follow the protocol. Governing principle: *evidence + reasoning is not authority; a decision is an explicit act by an authority.*
- **EP-01-Light gate (hint):** below IDD weight, present the form and WAIT; record-only exempt; every implementation ends with the EP-02 report. Rule text: `Implementation_Process_v1.1_Draft.md` (registered in ES-002).
- Pending ARB (knowledge track): assembly-report review · EKP disposition · pilot authorization.
- Commits reference ticket IDs (`PB-003: ...`). Git identity: `=Dr. Nab Raj Roshyara`.
- Descriptive filenames always; renaming "Untitled" docs is expected housekeeping.

## PKS Phase II — knowledge-governance framework (durable, 2026-07-31)

**Four-layer framework, acyclic dependency `Integrity Model ← Method ← Discipline ← Governance Process`:** `docs/implementation/PKS_Knowledge_Integrity_Model.md` (criteria, qualities A–F) · `PKS_Knowledge_Contract_Review_Method.md` (workflow, 10 emphases, lifecycle) · `PKS_ARB_Review_Discipline.md` (conduct, Baseline v1.0 FROZEN + Amendments 1–4).

**Taxonomy — stable, and deliberately not grown:** **10 review emphases · 10 lifecycle stages · 5 review families · 5 governance domains · 1 review contract.** *Families and domains are ORTHOGONAL; the equal count is accidental (a family classifies what a review EXAMINES; a domain classifies what governance ACTIVITY is performed).*

**Three stabilizing principles — governance principles derived from this programme's evidence, falsifiable, WITHDRAWN as their lawful exit; NOT laws.** Form: *variation in X does not imply variation in Y*, with **different Y each time**, so they are three independent results:
1. a new lifecycle **stage** ⇏ a new review **emphasis**
2. a new analytical **scope** ⇏ a new governance **responsibility**
3. a new governed **artifact type** ⇏ a new governance **contract**

**Standing governance rules (canonical text in the framework documents; these are pointers):** recognition ≠ agreement ≠ adoption — *agreement does not possess legislative force* · an observation is never remediated · *statements about the WORLD age; statements about THIS ACT do not* · the load-bearing test applies at the level of the CLAIM, not the artifact · frozen artifact → **annotate**; continuing ledger → **maintain**, and *retention of the superseded text is the OBSERVABILITY CONDITION* · ledger identifiers are evidence — **clarify → alias → annotate, never renumber** · *a name, once issued into the record, cannot be withdrawn, only bounded* · negative claims require evidence of SEARCH SUFFICIENCY · verification before CLASSIFICATION, not merely before assertion (PMR-9, candidate) · reviews do not absorb adjacent governance work — *visibility is not jurisdiction*.

**Methodology baseline:** **SDM v1.1 / EOP v1.1 OPERATIVE** (`PKS_Phase_II_Methodology_Baseline_v1_1.md`) — a **REFERENCE-DEFINED** baseline, not a consolidated document. *Extraction into single documents is AUTHORIZED as administrative with zero new design content (CDR §4.2) but not yet commissioned.* **Methodology changes only via execution evidence → MCA-class assessment → CDR-class decision.** *Register: `PKS_Methodology_Refinement_Candidates.md`.*

**Certification:** Method Design **PROVISIONALLY CERTIFIED** · **Operational Evidence at ZERO-INDEPENDENT and correctly so** — *a certification dimension derives credibility from being outside the unilateral control of the programme it certifies.*

**⚠️ Two PERMANENT acronym collisions, bounded not renamable:** **AFV** = a promoted artifact / an activity class / PMR-2's discharge verification · **MCA** = the **Method Certification Assessment** (canonical), while **MCA-R1** is the register-scoped methodology-CHANGE assessment. *Always qualify both.*

**PKS Phase II GOVERNANCE BASELINE: COMPLETE (2026-07-31)** — `docs/implementation/PKS_Phase_II_Governance_Baseline_Complete.md`.
**Baseline: SDM v1.2 / EOP v1.2, REFERENCE-DEFINED (BRM-1 retained Model B).** Method Design provisionally certified; **Operational Evidence at zero-independent and unmovable by a governance programme**. Six promoted artifacts; sixteen reviews, none open, no finding open; register at 10 (5 adopted, 5 deferred).
**Completion standard:** *every question NECESSARY for the baseline has reached a lawful terminal state — **adopted · resolved · deferred · retired** — not that every possible question has been answered.* **⚠️ SIA-1 additionally reached *no present constitutional act is required*: a DISPOSITION USED ONCE, expressly NOT a fifth general terminal state (n=1).**
***Remaining Class B/C items and all deferrals are FUTURE-CYCLE work: future work begins because there is new evidence or a new need, not because Phase II was incomplete.***

**⭐ CLOSING META-PRINCIPLE, carried forward to any future cycle:** ***every governance act should establish only the STRONGEST CLAIM THAT ITS EVIDENCE PRESENTLY SUPPORTS.*** *A quality criterion, not a methodology step. It explains almost every Phase II refinement — confirmed vs strengthened · dissolved vs superseded · document vs rule vs binding · rationale vs ontology · readiness vs authorization · implementation vs adoption · observation vs methodology · disposition vs terminal state. **Every one narrowed a claim; none changed an outcome.***

**Scope of completion:** *this programme **exhausted the questions it set out to answer** — it did **not** finish governance.* **Any further substantial governance work begins as a NEW CYCLE with a NEW MANDATE, triggered by new evidence, new architectural needs or operational experience — never by reopening questions already at lawful terminal states.**

**PKS PHASE III CHARTERED (2026-07-31)** — `docs/implementation/PKS_Phase_III_Operational_Validation_Charter.md`. **Purpose: demonstrate whether SDM v1.2 / EOP v1.2 produces better engineering outcomes in real work.** *Internal soundness establishes nothing about whether the methodology works; only USE can move Operational Evidence off zero-independent.* **Success criteria are OBSERVATIONS not targets, and must admit NEGATIVE findings. Honest failure direction: the baseline is likelier to be TOO EXPENSIVE FOR ITS VALUE than to be wrong.** ⛔ ***A Phase III act that produces a governance artifact without producing operational evidence has FAILED THE MANDATE.***


---

## PB003 CLOSED (2026-08-05) — the certified-baseline closeout lifecycle is a STANDING CONVENTION

**PB003 Engineering Commission: CLOSED.** Baseline: tag `pb003-certified-baseline` (`228740ab`) · release record `engineering/verification/reports/2026-08-05-pb003-release-record.md` · certification `2026-08-05-pb003-arb-certification-report.md` (assets A/B + repository, ✅ CERTIFIED). Branch and worktree deleted; repository clean.

**Reuse this lifecycle for every major commission (ARB Chair direction, 2026-08-05):**
`Engineering → Merge Readiness → ARB Certification (per-asset → repository baseline) → ARB QA of the report → correction pass → Commission Completion Audit → Five-Dimension Completion Audit (incl. Commission→Verification traceability matrix) → PR merge (blocking composer merge-gate) → Programme State Assessment (reconstruct existing plans, never invent) → baseline tag → Release Record → ARB governance session → next commission on a fresh branch.`
Conventions proven in PB003: certification decisions are scope-qualified ("PB003 Commission: COMPLETE", never bare "COMPLETE") · deferred-by-governance ≠ incomplete · provenance before disposition (pre-existing vs introduced) · formal governance PR bodies carry no AI-attribution footer · engineering resumes only on an explicit authorization vote.

**Next per the prepared plan (nothing self-opens):** convene the five-vote ARB session (`2026-08-01-arb-session-decision-pack.md`: D1 · D2/A-1 · D3 · 4a · 4b) → on grant, WP-7 Slice 7A RED. Independent: Domain Owner answers Q1–Q4 · Delivery Governance re-syncs BACKLOG (F-2).

## WORKING AGREEMENT — Product Development phase (adopted 2026-08-05, post-PB003)

**Claude's role changes: AI Developer → Product Architect & Engineering Partner. The PRODUCT drives the work, not the engineering platform.** Standing question opening every commission: *"What business capability are we improving today?"*

**Four modes only:** **Reviewer** (read code+docs, explain what exists, find gaps) · **Architect** (refine business processes, PROTECT existing architecture, no redesign) · **Developer** (implement only approved backlog items, TDD, existing standards) · **QA** (verify implementation, tests, docs-match-code).

**Commission style: TINY.** One capability per commission (e.g. "Election Process Review → one review report"), staged with explicit stops: review → approval → doc update → approval → backlog (MVP only) → approval → implement item-by-item. **Review-first, always:** explain how it ACTUALLY works before discussing how it should work; classify findings as bug / missing functionality / technical debt / future idea.

**Forbidden without explicit commission:** discovering new bounded contexts · redesigning architecture · new governance frameworks · new verification frameworks · platform expansion. (Consistent with the frozen methodology, 2026-08-01.)

**First product commission:** Election Process Review — read all Election documentation, implementation, tests, UI, APIs; build one process map; compare documented vs implemented; deliver implemented/missing/inconsistent/recommendations; STOP for approval. No code.

**Working-agreement refinements (mentor review, 2026-08-05):**
- **Reviewer is the DEFAULT mode.** Every capability starts there: what exists · how it works · docs accurate? · missing? · duplicated? — before any other hat.
- Cycle per capability: Review → (approve) → docs-only commission → (approve) → MVP backlog → (approve) → implement ONE item (TDD) → QA → STOP. Same cycle for Election, then Membership, then each capability.
- **Product artifacts live under the EXISTING product root `docs/publicdigit/`** (processes/ · mvp/ · reviews/ — each directory appears with its first artifact, per the folder rule). A top-level `product/` root would need a placement-registry amendment + ADR; not done.
- Opening question of the product era: *"How does an election actually work in my software today, and what is the smallest set of improvements needed before customers can use it confidently?"*

**Promotion reflex (POINTER — canonical rule: ES-006.1 ladder + engineering/README placement litmus; this adds no rule):** after every commission ask *"did we learn something that improves software engineering in general?"* Mnemonic litmus: *"would this help almost any repository, regardless of business domain?"* Yes → PROPOSE promotion (ES-006.1 ladder — needs ≥2 independent evidences / "one work package is not a standard", R-90); No → it stays product-side. Never promote domain knowledge (elections, contestation, messaging schema choices) into KnowledgeOS. Applied to itself: the PB003 closeout lifecycle recorded above is ONE occurrence — it remains a project convention until a second commission exercises it.

**Daily loop (culture binding, 2026-08-05):** choose ONE business capability → review → understand → improve documentation → approve backlog → implement ONE thing → review → reflect → *did we learn something general?* (**most days: no — that is the healthy answer**) → done.

**Session header — five lines, every session:** Current Commission · Current Goal · Current Role (Reviewer default) · Current Constraints · Definition of Done.

**Culture order (do NOT invert):** experience → reflection → agreement → **automation** → habit. *Automate proven practice before documenting it* — automation is a MECHANISM (four-level model: engineering's own level, needs no governance act), whereas a new rule is not. Culture lives in hooks and gates, not in prose: `project-knowledge-guard.sh` · `discipline-gate-reminder.sh` · `dev-guide-reminder.sh` · `composer merge-gate` · `knowledge-lint`.

**Three-layer knowledge model — CANDIDATE, not in force** (`docs/pks/2026-08-05-knowledgeos-distillation-principle-candidate.md` §Addendum): product experience → observations (incubation) → proven candidates → KnowledgeOS standards, Decision Authority gating, no layer bypassing the one above. ⚠️ **Adoption is BLOCKED ON OQ-5** (is KnowledgeOS the Engineering Platform or a distinct product?) because `docs/pks/` is registered as the **PKS *product* root**, not an incubation namespace — the dual use exists in practice with no ruling. **The lifecycle-per-artifact-class principle is NOT new: canonical home is ES-004.3** (R-41); the candidate only proposes extending its role table. Promotion chain = ES-006.1; "no layer bypasses" = ES-006.3.

**⚠️ PLACEMENT FACTS (verified 2026-08-05, do not re-derive):** `docs/pks/` is the **PKS product domain's documentation root** (`docs/pks/README.md`: *scope: product-specific · domain: pks*) — **NOT** an engineering-idea incubation area. **PKS is a separate product domain, peer to PublicDigit** — product knowledge ABOUT PublicDigit goes to **`docs/publicdigit/`** (so the Election Process Review files there, never in `docs/pks/`). **A home for `cross-product · research` artifacts DOES NOT EXIST: resolver returns `PENDING (rule: cross-product-research, ref: ADR:OQ-2)`, exit 2 — record PENDING and escalate; never invent a folder** (ES-005.2). Engineering observations currently in `docs/pks/` are a de-facto non-compliant practice, already escalated (`2026-08-01-knowledgeos-maturity-structure-assessment.md`); today's candidate carries an explicit interim-exception note. **The exploration→standardization distinction the ARB Chair described already exists as the MATURITY AXIS** (research → qualified/adopted · ES-006.1 ladder · ES-005.3); what is missing is only its *home* — that is OQ-2/OQ-5, a governance question.

**ORIENTATION (not "bootstrap") — DEFERRED, gated on recurrence.** Diagnosis (2026-08-05, n=1): the repository does not lack information; a fresh session must *reconstruct the mental model*. 6 of 8 proposed bootstrap layers already exist; the two real gaps are **scattered vocabulary** and **`.claude/CONTEXT.md` not being scannable**. **Do NOT build a parallel onboarding architecture** (freeze · R-37 · ES-005.2). **Act only if the same friction recurs in a later commission**, then improve the smallest EXISTING mechanism.
**⚠️ Design constraint for the vocabulary page when it is built: it must map `term → canonical home` (a POINTER INDEX), never `term → definition`.** A page that restates definitions is a second home for rule text — ES-005.4 ("never a copy") and exactly the dual-canonical-home defect already carried as debt M-3/F-1.
**Onboarding-quality targets (measure, don't assert):** repository understood < 5 min · current commission understood < 2 min · wrong document placement 0 · wrong storage location 0 · missed workflow obligation 0. *(2026-08-05 actuals: hours · N/A · 1 · 1 · 1.)*
**Standing attitude:** *record the friction, see whether it repeats, then improve the smallest existing mechanism* — evidence accumulates at no architectural cost.

**Vocabulary index — agreed design for when recurrence unblocks it (still DEFERRED, build nothing yet):** a **generated** routing index, source of truth a small YAML (`term → canonical path`), rendered by a script beside `doc-placement.php`/`knowledge-lint.php`. **It owns no rule text — only routing.** **The generator MUST fail closed when a canonical path does not resolve** (same discipline as knowledge-lint): a hand-written index would silently ship dead pointers, and a validating generator turns "the canonical home moved" into a build failure. **Note the side effect: `Commission → EP-01` is currently UNROUTABLE — EP-01's rule text is the disputed dual home (debt M-3/F-1), so the index would mechanically surface that defect rather than paper over it.** Also: `engineering/governance/standards/` does not exist (fulfilled as `ES-001..006` files directly) — resolve real paths, never assume a tidy tree.
**Metric honesty:** of the orientation metrics, only **human corrections needed** is reliably observable by the human. Agent self-counts of "questions answered by tooling vs by searching" are self-reported and therefore weak evidence — keep them as soft signals, never as the acceptance measure.

**⚠️ The "canonical metadata → generated views → uniqueness validation" architecture ALREADY EXISTS — do not rebuild it.** EKP schema (`docs/knowledge/schema/`: knowledge-schema · authorities · statuses · knowledge-types · knowledge-relationships + 5 more) declares card fields (`knowledge_id`, `authority`, `status`, `owner`, `related_to`); `knowledge-lint.php` already enforces **`knowledge_id_unique: severity error`** (fails on duplicates, `knowledge-lint.php:107`); `knowledge-graph.php` already generates the derived graph. **The ENTIRE delta of the proposed vocabulary system is: add a `terms:` field to the card schema + a `term_owner_unique` rule** — one field, one lint rule, zero new tooling. That would also make M-3 (EP-rule dual home) a mechanical build failure.
**⛔ Blocked before that delta: EKP's own disposition is PENDING ARB (debt M-4).** Do not extend the schema of an artifact whose future is unruled — the ruling comes first.
**Metrics split (adopted):** **objective** (externally observable, the acceptance measure) = human corrections · wrong placement · wrong storage location · missed mandatory workflow step · doc-inconsistency build failures. **diagnostic only** (agent self-reported, never acceptance) = searches · tool invocations · files opened.

## Product phase — durable facts (2026-08-06)

**Vocabulary is settled: the term is "Working Organisation", never "Active Organisation".** *(The organisation within which the authenticated user is currently operating.)* "Active" collides with an organisation's **own** lifecycle states (active · suspended · archived); "Working" describes the **user's context**. **All stories, reviews and code use "Working Organisation".** Canonical home: `docs/publicdigit/backlog/PBDIGIT-30-*.md` §Terminology decision.

**Approved business rules of that concept (Product Owner, 2026-08-06) — the rule text lives in `PBDIGIT-30`; these are pointers:**

| Rule | What it settles |
|---|---|
| **B1** | identity + `0 → Create or Join` · `1 → enter it` · `2+ → ask`. **PublicDigit is a BOOTSTRAP organisation and is never a working context** — excluded from every count. Invariant: *every authenticated request executes inside exactly one Working Organisation*; with several, the platform **never guesses** |
| **B2** | it changes **only** by an authorised act — **never as a side effect** of navigation, rendering, routing, elections, caching or middleware |
| **B3** | authority: user (primary) · platform **only** on invalidity · administrator **No** · anything else **Never**. **No mechanism may *establish* one — only validate, consume or store it.** *The platform shall always be able to explain why the current Working Organisation was established* |
| **B10** | one eligible election today → enter it, **only** at login or platform root |

**B5–B7 need no additional business rules — they are derived consequences of B1–B3.** **B8/B9 need no further discovery *under B1–B3 as they stand*** (only the experience is open, which is design) — **but new business behaviour reopens them as discovery**, e.g. if a revoked member should meet an appeal flow rather than an immediate redirect. **B4 was withdrawn** as design-not-discovery → `PBDIGIT-34`, an unprioritised question that blocks nothing.

**Modelling vocabulary — adopted term: "derived consequence".** Not *"already answered"* (which only means somebody ruled earlier), but *"no longer an independent rule"*. A derived consequence has **no owner, no maintenance and cannot drift**, because it *is* its parents applied to a case. **Triage rule that goes with it:** *when a scenario question cannot be derived from the fundamental rules, it reveals either a **missing business rule** or a **new product decision** — distinguish the two rather than invent an answer.* B4 was the second answered as though it were the first.

**⚠️ Unresolved and load-bearing: two eligibility mechanisms exist and neither is authoritative** — `User::getActiveElection()` vs `VoterEligibilityService`/`EligibilityEvaluator`/`EligibilitySnapshot`. B10 forbids a second mechanism, so this must be settled inside `PBDIGIT-32` (`PBDIGIT-EPIC-02` MB-5 records the same concern).

**Routing mechanism: sound, not deficient.** `DashboardResolver`'s nine-priority model reviewed at **robustness 4/5** (`PBDIGIT-31`); it is **missing two destinations**, not wrong in kind. **The evidence does not justify a Working Organisation service, aggregate or value object** — `PBDIGIT-29` Q1 (field or concept?) and Q2 (which of four stores is authoritative?) are **undecided and are not prerequisites for implementation.** Extend; do not redesign.

**Standing product discipline (both at n=1, both FILED not adopted, both would extend an existing standard per ES-005.4):** *deliberate manual source edits, no `sed -i`* (`docs/pks/2026-08-05-manual-code-editing-observation.md`) · ***business discovery finds the rules a product already implies; it does not design the product's future*** (`docs/pks/2026-08-06-discovery-does-not-design-candidate.md`). **Diagnostic for the second: a discovered rule describes work the product already owes — a rule that *creates* work is a requirement wearing a discovery's clothes.**

**Nothing in the product is runtime-verified.** Every ✅ in `docs/publicdigit/backlog/` is a claim about code. `PBDIGIT-00` (one observed end-to-end election) remains the gate, and **verification precedes new features.**

## Shared-checkout commit hygiene (BINDING for all concurrent sessions — PO, 2026-08-13)
Multiple sessions share one working tree and one index. **Never assume the index contains only your own work.** Before EVERY commit: `git status` · `git diff --cached --stat` · `git diff --cached --name-only` (verify every path is yours) · then commit · then `git show --stat --oneline HEAD` to confirm what actually landed. Origin: Session 3's gate commit accidentally swept in another stream's three staged ADR renames (recorded in `.claude/sessions/2026-08-13.md`); history was deliberately NOT rewritten.

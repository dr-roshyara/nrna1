# Current Working State

**Updated:** 2026-07-25

## EPIC-002 track — CLOSED (2026-07-25); EPIC-003 Tactical DDD Entry Assessment IN PROGRESS
*(This section supersedes the "(7) NEXT: EPIC-002 Strategic Discovery Phase 1" pointer in the historical chain below — that phase ran 2026-07-11 → 2026-07-25 and is complete.)*

- **EPIC-002 Strategic DDD: COMPLETE + OFFICIALLY CLOSED** (ARB Resolution recorded in `docs/implementation/EPIC-002_Relationship_Pattern_Selection.md` §ARB RESOLUTION). Full chain: literature iterations 1–5 (falsification mode) → evidence-family audit (P2 corrected 11→8) → consolidation → domain discovery → candidate boundaries → decomposition evaluation → canonical decision → collaboration discovery → readiness assessment → Decision Gate ruling (**Scenario C Hybrid** custody + **keep CB-1/CB-4 separate**, with reversal condition) → **Canonical Context Map ACCEPTED (5 Bounded Contexts ratified: Collection & Aggregation · Contemporaneous Record-Fixing · Custodial Integrity · Self-Verifying Integrity · Adjudication)** → Relationship Pattern Selection + adversarial design review ACCEPTED (COL-3b revised to Published Language alone).
- **Strategic baseline is BINDING** (see `.claude/MEMORY.md` "Constraints that bite") — changes only via recorded falsifiability/reversal conditions or new domain evidence, never implicitly during implementation.
- **EPIC-003 Entry Assessment: ACCEPTED** (`docs/implementation/EPIC-003_Tactical_DDD_Entry_Assessment.md`) — implementation archaeology complete; correction loop = open arc (staged construction); Special Review №1: Adjudication = SUBSET → extend, no rename; Special Review №2: all-custodial baseline, Self-Verifying = greenfield. **The four constitutional policies RULED and binding** (Superseding Constitutional Publication · Evidence Preservation Window · Phase-1 Custodial Constraint · Quarantine Pending Determination — full text in the assessment §THE FOUR DECISIONS; compact summary in MEMORY). **Strategic architecture baseline complete and accepted; strategic-document refinement ENDED (chair instruction).**
- **TACTICAL DDD AUTHORIZED — ADJUDICATION (ARB, 2026-07-25), scope-limited:** the authorization covers *preparation and approval of the scoped Adjudication Tactical DDD work package only*. Aggregate discovery/VOs/invariants/repositories/commands/events begin only after that package is reviewed and FROZEN.
- **Adjudication Tactical Work Package ACCEPTED + FROZEN (ARB, 2026-07-25)** (`docs/implementation/EPIC-004_Adjudication_Tactical_Work_Package.md`) — Q-1 (authority model) and Q-2 (Contestation Window) remain pending ARB items per its §5b, carried forward.
- **EPIC-004A Tactical Discovery PRODUCED** (`docs/implementation/EPIC-004A_Adjudication_Tactical_Discovery.md`) — evidence document, not design: 9 deliverables (vocabulary incl. the **three-way Legitimacy collision** · decisions · invariant candidates · lifecycles · responsibilities · information ownership · code evidence · 8 modeling hotspots · findings). **NEW Q-3 discovered and reported: the loop's head crosses the package boundary** — the quarantine path ends in raising a Challenge (Contestation's job, explicitly not-in-scope for the Adjudication iteration); needs ARB routing (extend T-2's reach vs. a separate minimal Contestation package). Self-review passed; no tactical artifacts.
- **EPIC-004A ACCEPTED WITH RULINGS (7.8/10, the LAST preparatory document):** §§5/6/8 drifted into Tactical-DDD territory (recorded criticism); **Q-3 REJECTED as an ARB question** — reclassified Tactical Collaboration concern (cross-context processes are normal); discovery phase CLOSED; **binding execution rule: one Tactical DDD artifact → ARB review → refine → freeze → next** (sequence: Aggregate Discovery → Boundaries → Responsibilities → Invariants → VOs → Domain Events → Commands → Repositories → Domain Services).
- **TACTICAL DDD BEGUN — artifact №1 produced, then REFINED per ARB (8.2/10 review):** `docs/implementation/EPIC-004B_Adjudication_Aggregate_Discovery.md` — the original draft prematurely declared "discovered aggregates"; corrected throughout to **aggregate CANDIDATES** (consistency boundary is the decision, lifecycle only evidence — the corrected discovery rule). Two candidates (Determination — strong production evidence, verdict withheld; AdjudicationProceeding — may prove aggregate OR policy/process-manager/saga/service/workflow/part-of-Determination), five further candidates DEFERRED-not-rejected. The "proceeding never writes Determination" collaboration statement withdrawn (belongs to later artifacts).
- **Sequence AMENDED (ARB):** Aggregate Discovery → **Aggregate Evaluation & Selection** (NEW artifact №2: tests every candidate against six consistency criteria, declares which become aggregates, records accept/reject rationale, preserves alternatives) → Aggregate Boundaries → Responsibilities → Invariants → VOs → Domain Events → Commands → Repositories → Domain Services.
- **Artifact №1 FROZEN as refined (ARB re-review 9.6/10).** Process refinement adopted: Evaluation & Selection and Boundaries MERGE into one artifact — **Aggregate Evaluation** with boundary-sketch-first structure (you cannot evaluate a vacuum); evaluation **recommends**, the ARB decides.
- **Artifact №2 PRODUCED:** `docs/implementation/EPIC-004C_Adjudication_Aggregate_Evaluation.md` — per candidate: Section A tentative boundary sketch (hypothesis) + Section B six-criteria evaluation. **Recommendations (ARB decides):** Determination = aggregate, confirm existing unchanged (all six criteria met by production evidence) · **AdjudicationProceeding = aggregate** on conclude-time atomic fixation of the considered-evidence set, **with the Process-Manager alternative honestly preserved** (the §B6 tension stated, not smoothed) · declare-failure / evidence body / quarantine / superseding publication = not Adjudication aggregates (each with grounds) · authority = defer pending Q-1.
- **Artifact №2 REFINED per ARB (9.8–9.9/10): Criterion 7 (Natural Business Decision Ownership) added — and it CHANGED the Candidate-2 recommendation.** "Proceeding" was coined, not discovered — the recorded business language names Challenge/Determination/Authority; evidence leans toward the authority owning the decision. Recalibrated: **AdjudicationProceeding = Aggregate Candidate (Medium) · Process-Manager alternative viable (Medium) — both preserved; discriminator = Q-1.** Determination stays Aggregate/HIGH.
- **ARTIFACT №2 DECIDED + FROZEN (ARB, 2026-07-25):** Determination = **Approved as Aggregate (High)**, existing implementation confirmed unchanged · AdjudicationProceeding = **DEFERRED (Medium)** until Q-1 — both realizations preserved (Process Manager remains under consideration, Medium). Chair's deciding factor: "proceeding is an architectural hypothesis, not a discovered business concept" — the ARB approves aggregates the business recognizes, not ones that are technically elegant.
- **Q-1 RESOLVED (ARB, 2026-07-26; record: `docs/implementation/EPIC-004_Q1_Authority_Resolution.md`, FROZEN):** Governance owns the authority model (existing delegation machinery); **Adjudication depends on Governance's authority decision through a published contract** — never implements/duplicates authority rules (binding constraint recorded). Chair's confidence shift recorded: "Proceeding" still lacks business identity → leans Process Manager/orchestration, not yet decided.
- **CANDIDATE 2 RESOLVED (ARB, 2026-07-26): PROCESS MANAGER / orchestration** — "Proceeding" stays an architectural hypothesis without business identity; C1's conclude-time fixation survives as an obligation whose enforcement home is later tactical design; reversal condition recorded (business language may re-propose the aggregate). **Artifact №2 COMPLETE:** Determination = the context's single ratified Aggregate.
- **Artifact №3 PRODUCED (`docs/implementation/EPIC-004D_Determination_Aggregate_Responsibilities.md`):** 6 responsibilities accepted (lifecycle · instance-level exactly-once · act-time fixation · presence-refusal · attribution · anonymity-at-fixation) · **R-3 honestly rejected out of the aggregate** (set-level one-per-challenge → issuance boundary, where the implementation already puts it) · R-4-expanded deferred to the PM design · 7 exclusions with grounds. New **Responsibility Traceability Rule** in MEMORY (discovered-not-invented; mandatory criterion).
- **Artifact №3 FROZEN** (canonical epistemic loop: evidence → … → removal test → protected domain truth → responsibility accepted; Methodological Fitness Rule adopted → MEMORY). **Artifact №4 PRODUCED** (`EPIC-004E_Determination_Invariants.md`): 7 truths → 8 invariants (INV-1..7 aggregate-seated, INV-B1 at the issuance boundary per frozen R-3), all implemented+pinned with cited test evidence; merge finding: INV-4/INV-6 = one fact, two truths → keep truths distinct.
- **Artifact №4 FROZEN** (litmus-clean; merge finding: truths distinct, shared fact). **Artifact №5 PRODUCED** (`EPIC-004F_Determination_Value_Objects.md`): derivation-and-reconciliation audit — all 13 existing VOs confirmed with parent invariants + removal tests; entry condition discriminated (2 rejections: IssuedAt, RulingContent composite; 2 deferrals: AuthorityAssertion→Q-1 crossing, EvidenceSet→PM design); Jurisdiction semantic gap flagged (new open item, non-blocking).
- **Artifact №5 FROZEN; VODP adopted** (→ MEMORY). **Artifact №6 PRODUCED** (`EPIC-004G_Determination_Domain_Events.md`): `DeterminationIssued` confirmed (it IS the ruling's record per ADR-T19 — strongest removal test of the series); 3 rejections (Prepared · Finalized w/ reversal condition, Round 50-07 confirmed · IssuanceRefused→audit concern); 1 deferral (FailureDeclared→PM design). **Finding: the deferrals cluster around the PM design** (FailureDeclared + EvidenceSet + R-4-expanded).
- **Next action (this track, exactly one):** ARB reviews artifact №6 → refine → FREEZE → artifact №7 (Commands). Open: Q-2 · Jurisdiction semantics · the PM-design cluster.



## Active Work (structured — hooks parse these `Key:` lines; keep the format)
Ticket: PB-003
Plan: .claude/plans/PB-003-inbox.md
Branch: feature/pb003 (ACTIVE — baseline-release-1.1 tagged behind it)
Milestone: M1 — Messaging Infrastructure (49%, derived 17/35 WBS)
Priority: High

- **Detail:** PB-003 Inbox / Deduplication · Lifecycle: **VERIFIED / CERTIFIED (ARR, 2026-07-07)** · 18/18 WBS · ARR: `docs/implementation/PB-003_Architecture_Readiness_Report.md` · matrix: `docs/implementation/Messaging_Architecture_Verification.md`. **Next ticket: PB-004 (needs IDD).**

## What remains (EPIC-001)
PB-003 (Inbox) → **Platform Capability Certification (D-11 gate)** → PB-004 (Election Reaction, needs IDD) → PB-005 (Contestation Reaction, needs IDD) → PB-006 (IT-1..8) → PB-007 (Merge Gate incl. F-1 Deptrac, F-2 Infection)

## Blockers
None. PB-003 is Verified/Certified; PB-004 is unblocked (needs its IDD first).

## Next action (exactly one)
**Implementation (TDD) — prerequisite chain for PB-004. Step 1 ✔ DONE.**
- **✔ Step 1 (Contestation):** `TargetRef:string` → `ContestedOutcomeRef` VO (+ `ElectionId`/`TargetType`/`TargetId`); `Challenge::raise` + `ChallengeRaised` carry it (`ChallengeRaised` evolved in place — internal domain event, not published language; ADR-PL-01). RED→GREEN; ChallengeTest 12/12; Architecture 154✔/1 skip; greenfield PHPStan clean. ARB refinements folded (internal-event rationale; VO holds no transport — `fromParts` removed).
- **✔ Step 2 (Adjudication) — DONE (ARB Option A).** `DeterminationIssued` **payload schema version 2** (same event, additive optional `contestedOutcome`; NOT a new class). Adjudication LOCAL VOs (ADR-T16); `Determination` carries + emits the ref; hydrator dispatches vCurrent(2)+vPrevious(1); `OutboxEventAdapter` stamps schema_version 2; mapper/model + additive migration persist it (nullable). **TDD RED-first (corrected after ARB caught a lapse):** wrote failing behavior tests, confirmed RED with impl stashed, then GREEN. Adjudication 45✔; Architecture+Contestation+Adjudication 199✔/1 skip (2 risky pre-existing); greenfield PHPStan clean. Dev guide: `developer_guide/adjudication/`. Committed `f18d7ae37`.
- **✔ Step 3 (PB-004) — GREEN DONE (ARB round-3 APPROVED + refinements applied); AWAITING EP-02 review.** Minimal reaction slice `app/Contexts/Election/{Domain,Application}` turns all 13 unit tests green. No Infrastructure layer yet (deferred to next slice — RED is all unit/in-memory ports).
  - **Domain:** `ElectionId`/`DeterminationId` (local, ADR-T16) · `RulingOutcome`/`CorrectionType` enums (`isForwardOnly()`) · `ElectionCorrectionPolicy` (Dismissed→null D-02; Upheld→ContainedOnly ADR-T8) · `Election` aggregate (`applyDetermination()` decides; forward-only, no reverse/rescind; message + **semantic** idempotency via `reconstitute()`) · `ElectionCorrectionApplied` (Election-owned identity only; no ChallengeId) · `ElectionRepository` · exceptions `DeterminationLacksElectionScope` / `CannotApplyDeterminationToUnknownElection`.
  - **Application:** `DeterminationIssuedReactionHandler` (inbox consumer; reconstructs from schema v2; time received from event) · `Port\ReactionEventOutbox`.
  - **ARB refinements applied:** (1) repository asks a **pure domain question** `find(ElectionId): ?Election` — org scope moved OUT of the contract to the org-scoped implementation (domain stays tenant-free, ADR-T16); handler test double models an org-scoped repo. (2) Added **semantic-duplicate** GREEN test (reconstituted+reapplied ⇒ no event). Rulings A/B/C confirmed.
  - **Gates:** Election 13/13 · regression (Architecture+Contestation+Adjudication+Election) 212✔/1 skip · Architecture Fitness 142✔/1 skip · greenfield PHPStan clean (**Election added** to `phpstan-greenfield.neon`). Dev guide: `developer_guide/election/`.
  - **Deferred (finding, not silent):** extending `GreenfieldCoreArchitectureTest` CONTEXTS to Election is held until Election's **Infrastructure** layer lands — the fitness check asserts all three hexagonal layers, so scanning a partial context now would be a false RED. Anonymity for the emitted event is covered behaviorally by `ElectionTest`.
- **✔ EP-02 Completion Review — PASS (ARB approved 2026-07-08).** Conformance confirmed (matrix in session log). ARB rulings logged: **Q1 timestamp = APPLICATION TIME** (`appliedAt` = when Election applies, NOT inbound `DeterminationIssued.occurredAt`; carry determination time separately only if needed) — the ARB message had two contradictory Q1 rulings; the final block + explicit prompt both rule application time, followed as authoritative. **Q2 (exceptions package) = deferred** (revisit >3–4 files). Doc-only refinements applied (staged-qualification gate distinction in `phpstan-greenfield.neon` + fitness-test comment). **Recorded deviation:** handler currently sources `appliedAt` from inbound determination time — fix in next slice (inject `ClockInterface`, RED first).
- **✔ Step 4A.1 — Temporal Correction DONE (ARB approved w/ 3 refinements).** `DeterminationIssuedReactionHandler` now injects the existing bound `App\Domain\Shared\Clock\ClockInterface` (reuse, ER-03/04 — no new clock); `appliedAt = $clock->now()` (**application timestamp**, ARB Q1). RED→GREEN via `FrozenClock` at 10:04 vs payload `occurredAt` 10:00; both timestamps asserted to coexist unmutated. Determination `occurredAt` still parsed but intentionally never assigned to `appliedAt` (kept for future latency/audit/replay). **EP-02 deviation #1 RESOLVED.** `ER-08` added to process draft (reviews record, implementations repair). Gates: Election 13✔/33 assertions · regression 212✔/1 skip · greenfield PHPStan clean. Dev guide updated. Plan: `.claude/plans/PB-004-step4-election-infrastructure.md`. Commits `6677d738a` (ER-08) + Step 4A.1.
- **✔ Step 4A.2 — Election-existence design (IDD, ARB-approved).** Legacy `elections` behind a read-only **ACL** (Strangler); business-oriented `ElectionExistencePort.exists()`; ownership = Election BC owns the *concept*, legacy is the *current operational source of truth*; 9 Architectural Invariants (incl. **Aggregate Reconstruction Invariant**). No ADR (IDD only). Plus a **RED Test Matrix** + failure-ownership decision (business absence ≠ infrastructure failure). Editorial refinements folded (Domain Port wording; tenant = infra concern).
- **✔ Step 4A.3 — Infrastructure DONE (RED→GREEN).** `ElectionExistencePort` + `AppliedDeterminationStore` (Application ports); `CompositeElectionRepository` (existence + reaction state → `Election::reconstitute`); `LegacyElectionExistenceAdapter` (read-only, tenant-scoped, soft-delete aware, identity-only); `EloquentAppliedDeterminationStore` + `ElectionAppliedDeterminationModel` (BelongsToTenant) + migration `election_applied_determinations`; `ElectionServiceProvider` (bindings + migrations) registered in `config/app.php`. Aggregate accessors `id()`/`appliedDeterminationIds()`. **Gates:** Election 13✔ new (5 unit composite + 4 adapter + 4 store; +21 prior) · regression 217✔/1 skip · greenfield PHPStan clean (Election Infra scanned). Dev guide `developer_guide/election/02_*`. **Observation (retrospective, not a change):** 8 Election Feature tests flagged PHPUnit-risky ("removed error handlers") ONLY in the mixed suite; clean in isolation (8✔) — a pre-existing cross-suite/pgsql artifact, not this slice.
- **✔ Refinement (ARB): rename `AppliedDeterminationStore` → `AppliedDeterminationLedger`** (port + Eloquent impl + refs + test file; business-record naming, not mechanism). Green after rename.
- **✔ Step 4B — Messaging Integration DONE (RED→GREEN, ARB-approved w/ 6 refinements).** `ReactionOutboxAdapter` (implements `ReactionEventOutbox`; writes `ElectionCorrectionApplied` to shared `outbox_events`; `aggregate_type='Election'`; payload schema v1; `organisation_id`=`TenantContext::require()`); inbox-registry wiring (`DeterminationIssuedReactionHandler` registered by `(Election, DeterminationIssued)`) + `ElectionCorrectionAppliedHydrator` (payload↔domain event; unsupported schema_version rejected) — both in `ElectionServiceProvider::boot()`. **Atomicity inherited** from `Inbox::consume()` `DB::transaction` (no Election TransactionManager). IDD refinements folded: publication decision vs mechanism; transport `event_id` owned by adapter; composition-seam note; hydrator hydrates the domain event; matrix scenario #8. **Gates:** 4B 8✔ new (3 hydrator + 1 adapter unit + 4 messaging Feature incl. atomic write · duplicate→no dup · ledger-failure rollback · registry resolves) · regression 221✔/1 skip · Architecture 142✔/1 skip (EventRegistryCompletenessTest green with the new hydrator) · greenfield PHPStan clean. Dev guide `developer_guide/election/03_messaging_integration.md`. **Observation (retrospective):** 12 Election Feature tests flagged PHPUnit-risky in the mixed suite only; clean in isolation — the same pre-existing cross-suite/pgsql artifact.
- **✔ Step 4B — FORMALLY ACCEPTED by ARB.** (Closeout committed `d3d4e297a`: messaging ownership prose + candidate migration-pattern note + architecture-confidence.)
- **✔ Step 4C — Architecture Qualification DONE (PASSED, no production change).** `GreenfieldCoreArchitectureTest` extended to Election (`CONTEXTS` + event-ownership `Election`→`['Election']`); the Step-4A.3 staged-qualification deferral is now resolved (Election is a complete hexagonal context: Domain + Application + Infrastructure). Election qualifies against ALL existing greenfield rules — hexagonal completeness, domain-has-no-infra-imports, event ownership, readonly events, anonymity (AT-Q7). **Gates:** Architecture 142✔/1 skip (assertions 613→616) · regression 221✔/1 skip/12 risky/**0 failed**/805 assertions · greenfield PHPStan clean. No qualification failure → no implementation/architecture defect.
- **✔ PB-004 FORMALLY CLOSED by ARB (2026-07-08)** — all slices accepted (4A.1 · 4A.2 · 4A.3 · 4B · 4C). First backlog item deemed *architecturally complete*, not merely implemented.
- **✔ PB-004 Retrospective produced** — `docs/implementation/PB-004_Retrospective.md` (evidence capture; no ADR; candidate patterns stay candidates; includes Architectural Confidence Delta). ARB required this before PB-005.
- **▶ PB-005 (Contestation Reaction) — Phase 1 Discovery DONE (no code).** ARB authorized PB-005 (Phase 1 only). Discovery artifact: `docs/implementation/PB-005_Discovery_Ownership_Review.md`. **Key finding:** Contestation **owns** the Challenge → PB-004's ACL/Aggregate-Reconstruction/Strangler does **NOT** transfer (normal single-source `EloquentChallengeRepository`). Reaction consumes **both** `DeterminationIssued`→`adjudicate()` and `ElectionCorrectionApplied`→`resolve()` (+ Dismissed short-circuit); **new vs PB-004:** two reactions, **parking** (out-of-order), Dismissed short-circuit. **7 open architectural questions raised for ARB** (scope of raise→route path; park classification; idempotency classification; Dismissed short-circuit; timestamps; 2 frozen-catalog inconsistencies; ChallengeResolved `resolution` payload mismatch).
- **✔ PB-005 IDD (Phase 2) — ARB-approved + FROZEN** (`docs/implementation/backlog/PB-005_Contestation_Reaction_Implementation_Design.md`). 7 questions ruled: reaction+infra only (raise-path separate); park on out-of-order; app-timestamp; Dismissed short-circuit; catalog inconsistencies recorded (implement against ADRs). **F-1** business-condition→inbox-marker translation (Domain never names messaging outcomes). **F-2 ruling:** `ChallengeResolved` domain event stays MINIMAL; `resolution` enriches only the **Integration Event** (Application supplies it at publish time). `findByDeterminationId` = correlation only, not identity. Sub-sliced 5A/5B/5C/5D.
- **✔ PB-005 Step 5A — Domain+Application reaction ACCEPTED.** Two reactions (`ChallengeAdjudicationReaction` w/ Dismissed short-circuit; `ChallengeResolutionReaction` correlate-by-determinationId + premature→park); business exceptions (`ConflictingDetermination`/`DeterminationAlreadyApplied`/`AwaitingAdjudication`); `ChallengeReactionOutcomeTranslator` (sole messaging-boundary) + markers; thin handlers; `Port\ChallengeEventOutbox`; `Challenge::reconstitute`/`adjudicatedDeterminationId`; `DeterminationOutcome`. Unit 24✔. RED restructured to separate business from translation (ARB Issue 1).
- **✔ PB-005 Step 5B — Infrastructure persistence GREEN.** Single-source `EloquentChallengeRepository` (+ `findByDeterminationId`), `ChallengeModel` (BelongsToTenant), `ChallengeMapper` (sole translation; reaction-relevant columns only), **full-schema** `challenges` migration (raise-time columns nullable — ARB: repo persists the aggregate, not the backlog scope), `ContestationServiceProvider` (registered in `config/app.php`). Feature 4✔; regression 233✔/1 skip/0 failed; greenfield PHPStan clean. Aggregate persistence-ignorant; identity=ChallengeId.
- **✔ Release 2.0 Architecture Handover** — `docs/architecture/Architecture_Handover_Release_2.0.md` (authoritative onboarding; PB-004 CLOSED, Messaging proven by reuse, Aggregate Reconstruction VALIDATED, candidates validated-not-promoted).
- **✔ Step 5C — Messaging Integration GREEN (RED→GREEN; ARB: seam emergent, not prescribed).** `ChallengeOutboxAdapter` (`ChallengeAdjudicated`/`ChallengeResolved` → `outbox_events`), 2 hydrators (minimal domain-event reconstruction; unsupported schema_version rejected), inbox-registry wiring for both handlers + hydrator registration + adapter binding in `ContestationServiceProvider`. **F-2 seam emerged from RED:** `Resolution` enum + `ChallengeResolvedIntegration` carrier (minimal domain event + Application-supplied resolution: Upheld on correction path, Dismissed on short-circuit); adapter stamps `resolution` on the published payload; **domain event unchanged**; hydrators ignore resolution. Refactored 5A reactions' outbox interaction + updated 2 5A assertions (documented). Reused PB-004 test convention (ER-03). **Gates:** Contestation 34✔ (unit+feature); regression 236✔/1 skip/**0 failed** (7 Contestation Feature cross-suite-risky, clean in isolation); greenfield PHPStan clean. Dev guide `developer_guide/contestation/04_messaging.md`. Session log `.claude/sessions/2026-07-09.md`.
- **✔ Step 5D — Architecture + DDD Qualification DONE (no code; qualification only).** ARB Q1: `ChallengeResolvedIntegration` is a **temporary carrier, not a pattern** (documented). ARB Q2: `Resolution` is **Application, not Domain** (domain minimal; downstream needs it; derived at reaction time) — documented in `04_messaging.md`. **Architecture qualification:** `GreenfieldCoreArchitectureTest` 6✔ (Contestation complete hexagonal + event ownership); greenfield PHPStan clean; regression 236✔/1 skip/**0 failed**. Deptrac not run (binary not installed; F-1 Deptrac is the PB-007 merge gate; fitness `domain-has-no-infra-imports` covers direction meanwhile). **DDD qualification (all PASS):** Challenge owned solely by Contestation; ChallengeAdjudicated/ChallengeResolved Contestation-produced; domain infra-free; integration via events only; no new pattern; slice touched only `app/Contexts/Contestation`.
- **✔ PB-005 FORMALLY ACCEPTED + CLOSED by ARB (2026-07-09).** Contestation Reaction complete (5A·5B·5C·5D). The correction-loop **implementation is complete and architecturally qualified**; **end-to-end operational validation is PB-006** (implementation ≠ operational validation). Deptrac (F-1) **deferred by roadmap** to PB-007 (not skipped).
- **✔ Governance refinement — Trustworthiness Qualification adopted** (ARB): every future greenfield slice runs three qualification categories — **Architecture · DDD · Trustworthiness** (anonymity · forward-only · event immutability · replay safety · causal ordering · tenant isolation · constitutional invariants). Documented in `docs/architecture/Architecture_Handover_Release_2.0.md` §9a. PB-005 retroactively satisfies it (anonymity: no voter↔vote linkage in Challenge/events; forward-only: terminal states, no reversal; immutability: readonly events; replay: idempotency + inbox dedupe; causal ordering: parking; tenant: BelongsToTenant).
- **▶ PB-006 — Phase 1 Discovery DONE; ARCHITECTURAL FINDING F-PB006-1 raised (STOP before IDD).** Scope grounded: IT-1..IT-8 (Blueprint §9). **Finding:** the **outbox→inbox delivery bridge is ABSENT** — the relay dispatches to the Laravel event bus and stops; NO production code calls `Inbox::consume`; providers only register handlers; existing tests hand-stitch delivery. IT-1/IT-2's full relay chains are untestable end-to-end; production cannot run the loop across contexts. Classified: **missing platform capability** (not a defect) — the R-29 escape clause case ("feature demonstrates the platform is insufficient"). ARR gate → ARB ruling requested: **Option A (recommended)** build an ARB-authorized `InboxDeliveryBridge` (Shared Infra: relay output → registry lookup → `Inbox::consume` per consumer) with ADR + RED-first; B hand-stitch in tests (validates semantics, not operations); C defer to a new ticket. Artifact: `docs/implementation/PB-006_Discovery_Findings.md`.
- **✔ ARB RULED F-PB006-1: Option A approved with refinements** — B (hand-stitch) and C (defer) REJECTED. The capability is **Messaging-Platform-owned**, named **`IntegrationEventDispatcher`** (Inbox does NOT own delivery — it is one consumer); it joins the trusted computing base → own Trustworthiness Qualification.
- **✔ ADR-MP-06 recorded** (appended to the ADR-MP series + README index): the dispatcher's 4 explicit responsibilities (receive → consumer resolution → inbox-message creation → per-consumer delivery), invariants (deterministic routing · replay safety · tenant/correlation/causation/identity propagation · consumer isolation), R-29-escape evidence F-PB006-1.
- **✔ PB-006 IDD produced** — `docs/implementation/backlog/PB-006_Integration_Validation_Implementation_Design.md`: slicing **6A** dispatcher (RED matrix, 8 scenarios) · **6B** IT-1..8 over the REAL path · **6C** triple qualification + Completion Review. Open decisions **D-1** (additive correlation/causation on the `IntegrationEvent` envelope if absent) and **D-2** (listener wiring recommended — relay stays byte-identical).
- **✔ ARB approved ADR-MP-06 + IDD with 3 refinements (all folded):** (1) dispatcher depends on a **`ConsumerResolver`** port — registry is the implementation behind it; (2) **deterministic ORDERED routing defined** — identical event+registry state ⇒ identical ordered set, ordered by `consumerContext()` ascending; (3) **audit continuity** added to the dispatcher's Trustworthiness Qualification (every InboxMessage traceable to exactly ONE OutboxEvent via shared event_id). Plus: 'Consumer Discovery' wording; **Registration ≠ Delivery** recorded as a permanent design principle; **D-1 approved** (additive envelope CorrelationId/CausationId); **D-2 approved** (listener wiring; relay byte-identical).
- **✔ 6A RED WRITTEN + CONFIRMED (6 failed, expected RED from intentionally unimplemented collaborators):** `tests/Feature/Contexts/Shared/Messaging/IntegrationEventDispatcherTest.php` — single-consumer delivery + audit continuity + tenant/correlation/causation propagation · deterministic ascending order (registered in reverse) · consumer isolation under park AND under dead-letter · replay-safe redelivery (dedupe) · empty-consumer no-op. Failures: `Unknown named parameter $correlationId` (D-1 fields absent) + dispatcher/resolver classes not found.
- **✔ 6A GREEN DONE (ARB-authorized; 2 doc refinements folded first):** stable-consumer-identifier ordering wording + explicit **consumer atomicity** (own inbox txn per consumer; no partial-state leak) in ADR-MP-06. GREEN (minimal): D-1 nullable envelope `correlationId`/`causationId` · `ConsumerResolver` port + `RegistryConsumerResolver` (ordering contract lives in the resolver) · additive `InboxHandlerRegistry::handlersFor()` · `IntegrationEventDispatcher` (4 responsibilities; stateless; transient rethrown after all consumers → relay retry + dedupe) · D-2 listener wiring in `AppServiceProvider` (relay byte-identical). **Deviation documented:** NO `IntegrationEventBuilder` — `outbox_events` has no correlation/causation columns (**F-PB006-2**, open for 6B: additive outbox migration + producer stamping needed for IT-8's full chain), so a builder would copy nothing.
- **✔ Findings during 6A gates (repaired test-only + process fix):** **F-PB006-3** — PB-003 `InboxRedriveTest` registered fakes under the real `(Contestation, DeterminationIssued)` pair; boot-time real registrations (since 5C) collided → repaired with probe consumer context. Mixed-run failures in `InboxConsumeTest`/`InboxEventTest` = global counts/`firstOrFail()` under the no-rollback harness (now real deliveries accumulate rows) → all scoped by `event_id`. **F-PB006-4 (process):** the standard regression set MUST include `tests/Feature/Contexts/Shared` (the 5C collision went unseen because it didn't) — adopted.
- **✔ 6A gates:** dispatcher 6✔ · Shared directory 31✔/0 risky in isolation · ad-hoc PHPStan max (new/touched Shared files) OK · greenfield PHPStan OK · **widened regression 236✔/1 skip/0 failed** (50 cross-suite-risky = known artifact). Dev guide `developer_guide/audit_system/07_integration_event_dispatcher.md`.
- **✔ 6A ACCEPTED by ARB (9.8/10)** + 2 doc refinements folded: ordering is deterministic but **carries NO business meaning** (consumers never depend on execution order); transient-rethrow **rationale documented** (relay retry = eventual delivery for the failed consumer; completed consumers protected by inbox idempotency). **F-PB006-2 authorized**: additive outbox evolution (correlation/causation) without touching domain events.
- **▶ 6B-1 RED WRITTEN + CONFIRMED (correlation chain, F-PB006-2).** `tests/Feature/Contexts/Shared/Messaging/CorrelationChainTest.php` — REAL path end-to-end: Adjudication issues (mints correlation) → relay+dispatcher deliver → Election `ElectionCorrectionApplied` AND Contestation `ChallengeAdjudicated` outbox rows must carry the SAME correlation_id + causation_id = the DeterminationIssued event_id; inbox rows continue the chain. **Seam NOT prescribed — emerges in GREEN** (F-2 doctrine). RED failure: `Undefined property: stdClass::$correlation_id` (outbox columns absent — exactly the gap). IDD records 6B-1/6B-2 sub-slicing.
- **✔ 6B-1 GREEN DONE (chain test 1✔/11 assertions).** Emergent seam: **`EventProvenance`** (Shared Application Messaging) — the generalized invariant reified: `start(minted)` for chain-starting producers; `fromConsumed(incomingCorrelation, triggeringEventId)` for reactions (deterministic fallback: absent incoming correlation ⇒ correlation := triggering eventId — replay-safe, no random mint in handlers). Supplied EXPLICITLY at publish time: all three outbox ports now `enqueue(EventProvenance, object ...)`; adapters stamp `correlation_id`/`causation_id`; additive outbox migration (nullable, indexed); relay copies row→envelope (D-1 populated); Adjudication coordinator mints via injected `IdentityGenerator`. Domain events untouched.
- **✔ 6B-1 gates:** CorrelationChainTest 1✔ · widened regression **236✔/1 skip/0 failed** (51 known cross-suite risky) · greenfield PHPStan clean · ad-hoc max on new Messaging code clean (0 attributable errors; ~50 PRE-EXISTING max-level findings in the never-scanned relay/model files — recorded, not fixed, out of scope).
- **✔ 6B-1 ACCEPTED (ARB) + Constitutional Audit Invariant documented** (EventProvenance docblock · ADR-MP-06 · `developer_guide/audit_system/08_event_provenance.md`): one mint per conversation · propagate unchanged · causation = immediate parent · correlation = conversation not event.
- **✔ 6B-2 DONE — IT-1..IT-8 over the REAL path.** `CorrectionLoopIntegrationTest` 4✔/39 assertions: **IT-1** full Upheld loop (×2 relay runs; Resolved; ledger; resolution=upheld; ONE correlation across all rows; causation per hop) · **IT-2** Dismissed (Election silent; short-circuit) · **IT-3** duplicate redelivery deduped · **IT-4** out-of-order park→redrive→Resolved. IT-5/6/7 evidenced by existing suites (22✔); IT-8 folded into IT-1 + CorrelationChainTest. Mapping in the IDD.
- **✔ 6C gates:** widened regression **236✔/1 skip/0 failed** · greenfield PHPStan clean · fitness suites green. **PB-006 COMPLETE.**
- **✔ PB-006 FORMALLY CLOSED by ARB (2026-07-10)** after 3 documentation refinements (applied): **Constitutional Conversation** defined as an architectural term; **Architecture Fitness Rule — one CorrelationId mint per conversation** (enforced by start()/fromConsumed() design + IT-1; dedicated fitness test = retrospective candidate); **EventProvenance = Integration Events ONLY, never Domain Events** (invariant: no Domain Event may carry correlation/causation properties). ADR-MP-06 + guide 08 + VO docblock updated. Platform is now **auditable event-driven** (transport + delivery + lineage).
- **✔ PB-006 closure governance items applied (ARB):** DAG/acyclicity invariant recorded (conversations form a directed acyclic graph — future evolution, no implementation); minting fitness test **deferred to PB-007 or first architecture-quality milestone** (concrete slot, not indefinite); DoD warning resolved — PB-006's provider/migration changes ARE documented (guides 07/08 wiring+schema pointers added).
- **▶ PB-007 Phase 1 Discovery DONE** (`docs/implementation/PB-007_Discovery_Findings.md`): **gate classification** — MANDATORY architecture gates (fitness suites · greenfield PHPStan · **Deptrac** · widened regression) block merges; QUALITY gates (**Infection** MSI · coverage · pre-existing-findings cleanup) report/trend, ratcheted not retroactive. Tooling state: deptrac.yaml exists but binary NOT installed (composer change flagged); Infection installed but unconfigured; no greenfield merge-gate CI workflow. Open IDD questions: Deptrac scope widening (add Election/Shared) · MSI baseline · CI shape (new workflow recommended) · minting fitness test lands here.
- **✔ Discovery APPROVED w/ 4 ARB rulings — applied:** taxonomy = **Architecture / Behaviour / Engineering / Improvement / Reporting** (PHPStan = Engineering, still blocking); every gate classified Local/CI/Both; **single entry point** (`composer merge-gate` + `quality-gate`); DoD warning classified WITH EVIDENCE (overly broad area mapping, not a doc gap — messaging guides 07/08 are current; reminder refinement = retrospective).
- **✔ PB-007 IDD produced** (`docs/implementation/backlog/PB-007_Merge_Gate_Implementation_Design.md`): 7A Deptrac (install + widen to Election + Shared-layer proposal; report→fail; violations = classified ARB findings) · 7B minting fitness test (RED-first) · 7C Infection baseline MSI (ratchet, no enforcement) · 7D composer entry points + greenfield-merge-gate.yml · 7E triple qualification + Completion Review. **Approvals requested: A-1** composer require deptrac · **A-2** Shared layering · **A-3** ratchet policy.
- **✔ IDD APPROVED + 3 refinements folded (R1 rules-from-architecture · R2 minting-test ownership · R3 composer merge-gate = stable interface) + slice REORDER (CI last) + A-1/A-2/A-3 granted.** Plus 4 ARB 7A-refinements: distribution-not-hardcoded (documented substitution) · Shared = platform layer not dumping ground · representative debug inspection · findings table w/ Category+Action.
- **✔ 7A DONE — Deptrac REPORT MODE: 0 violations.** Install: canonical `deptrac/deptrac` unresolvable → supported shim `qossmic/deptrac-shim` (deptrac **1.0.2**) — documented substitution (A-1). Config re-derived from the approved architecture as **per-context hexagonal layers** (10 layers; Domain pure — not even Shared; Application→own Domain+Shared; Infra→own Domain+App+Shared; Shared→nothing; cross-context = violation by omission). **Findings:** F7A-1 initial dual-axis encoding → 2096 pair-artifacts + 83 warnings = **Configuration issue** → re-encoded (config-only, same model) → **0 violations / 0 warnings**; F7A-2 canonical package unresolvable = **Tool limitation** → shim; F7A-3 90 uncovered = **approved externals** (ClockInterface per PB-004 clock ruling · Illuminate in Infra · BelongsToTenant/TenantContext) → report-only. **Real architectural violations: 0.** Positive metric: **15 approved dependency directions encoded; 440 concrete dependencies verified; 15/15 clean.** `debug:layer ElectionDomain` confirms rule evaluation.
- **✔ 7A ACCEPTED (9.9/10) + 2 doc refinements applied** (deptrac.yaml): uncovered = **approved external platform dependencies** (uncovered ≠ approved; explicit `ExternalPlatform` layer = recorded future refinement) · **Shared admission rule** (business concepts enter Shared only via explicit ARB approval — never a Utils layer). ARB: fail-mode flip = **governance change**, lands in 7D as the blocking `composer merge-gate` step; DoD warning = tooling-reminder refinement, not a doc gap.
- **✔ 7B DONE — CorrelationId-minting fitness test GREEN** (`tests/Architecture/Messaging/CorrelationIdMintingTest.php`; Messaging OWNS the invariant, PB-007 hosts — ADR-MP-03/06): only allowlisted chain-origin producers may call `EventProvenance::start()` (today: Adjudication's coordinator; extending the allowlist = ARB decision); reacting handlers must use `fromConsumed()` and never mint; **falsifiability proven** (guard flags a synthetic mint site; ignores propagation AND prose). **Genuine RED caught a docblock mention** (EventOutbox port) → guard made comment-aware (strip comments before matching) — detector precision fix, not a weakening. Architecture suite now **145✔/1 skip**.
- **✔ PB-007 IDD APPROVED + FROZEN by ARB (2026-07-10).** Ruling: gate taxonomy clear · `composer merge-gate` = appropriate architectural abstraction · PHPUnit-11 coverage repair correctly classified as latent platform incompatibility (Infection revealed, didn't introduce). **Two freeze refinements applied:** §2c split into **2c-i platform compatibility repairs (PB-007-owned)** vs **2c-ii pre-existing repository debt (historical → retrospective)**; **exclusion-integrity statement** added (exclusions ONLY for non-executable artifacts; coverage policy for executable production code unchanged). Non-binding ARB recommendations recorded in IDD §2b: candidate principle *"quality gates reveal latent defects; they don't introduce them"* + future Engineering Trust Dashboard. No mutation thresholds yet (A-3 stands).
- **✔ 7C DONE — Infection baseline CAPTURED (greenfield core): MSI 75% · Mutation Code Coverage 78% · Covered Code MSI (Test Strength) 96% · 892 mutants = 638 killed + 48 timeout-detected (60s cap, caveat recorded) + 22 escaped + 184 uncovered · 0 errors. 7m16s, 8 threads.** Route there produced classified findings (IDD §2c): **repairs (2c-i)** — PHPUnit-11 coverage schema migration; Xdebug 3.5.3 per-invocation driver (phpdbg dead: PHPUnit 11 dropped it); **debt (2c-ii)** — F-7C-4 `ValidateVotingIpTest` fails standalone (legacy; Infection = first-ever full-6109-test pass), F-7C-5 Membership/Elections/Geography/Governance test debt (69E+5F, outside core), F-7C-6 57 risky greenfield Feature tests (framework-level teardown artifact); **decisions (2c-iii)** — additive `GreenfieldCore` testsuite (= mutant-killing set; Architecture file-scanners excluded, zero mutation signal); `AdjudicationServiceIntegrationTest` global→org-scoped counts (PB-006 convention; suite 179✔/0F); **two-step invocation** (phpunit coverage-xml+junit under project semantics → `infection --coverage --skip-initial-tests`) because Infection forces `stopOnDefect` and PHPUnit 11 stops on risky (verified in both sources) — 7D `quality-gate` encodes it (stable interface owns invocation semantics). Escaped-mutant hotspot: `ChallengeReactionOutcomeTranslator` (4) = first ratchet candidate. Ratchet: deliberate at milestones, never retroactive, NO enforcement in PB-007.
- **✔ 7C ACCEPTED by ARB (2026-07-10) + IDD §2d added (ARB clarification):** two explicit gate tiers — **blocking** (`composer merge-gate`: Architecture · Behaviour · Engineering) vs **non-blocking** (`composer quality-gate`: Improvement/Reporting; A-3, never blocks in PB-007). Escaped-mutant ratchet parked as **ENG-004 Mutation Ratchet 1** (BACKLOG, first target `ChallengeReactionOutcomeTranslator`).
- **✔ 7D DONE — stable interface implemented + VERIFIED (IDD §2e):** `composer merge-gate` **PASS** end-to-end (Architecture 146✔/626/1 skip → **Deptrac fail-mode flipped** (0 violations) → greenfield PHPStan clean → GreenfieldCore 179✔/477/0F) · `composer quality-gate` runs the two-step Infection through the interface · guides `developer_guide/merge_gate/00_index.md`+`01_stable_interface.md` (ARB-refined: stable contract, no time-dependent values). **F-7D-1** (Engineering Platform Repair, ARB-classified): Composer 300s process timeout killed Infection → `Composer\Config::disableProcessTimeout` on both scripts. **F-7D-2 (OPEN → ARB):** mutation measurement is THREAD-SENSITIVE — same coverage/mutants: 8 threads = MSI 75%/Covered 96%/20 escaped (reproduces baseline, 11m17s) vs 1 thread = 50%/65%/224 escaped; identical uncovered (184). Hypotheses: 8-thread false kills (concurrent `migrate:fresh` on shared nrna_test) vs 1-thread false escapes; discriminating experiment identified; candidate remedy = per-thread DBs (Infection `TEST_TOKEN`); interim: `--threads=8` pinned (explicit thread count = part of measurement semantics; `--threads=max` silently degrades to 1 in 0.29.10).
- **✔ F-7D-2 DECIDED (2026-07-10, experiment executed): 8-thread measurement REJECTED** — 8T escapes ⊂ 1T escapes (18/18), **202 mutants flip killed→escaped** without concurrency (65 = migration-file mutants; rest = messaging/persistence infra) ⇒ concurrent `migrate:fresh` races on shared `nrna_test` produced false kills. **Validated 1-thread baseline: MSI 50% · MCC 77% · Test Strength 65%** (reproducible: 416/418 killed, 224 escaped across two runs). §2c 8T table = history. ENG-004 baselines on validated numbers. PENDING EP-01-Light approval: quality-gate `--threads=8`→`--threads=1`; future fast+valid = TEST_TOKEN per-thread DBs (not PB-007). ARB-amended execution order (2026-07-10, binding): **(1) ✔** F-7D-2 evidence decision (8T REJECTED; validated 1T baseline; threads repair `bce4b6894`) → **(2) ✔ 7E DONE (`dc7027c37`)** — `greenfield-merge-gate.yml` (blocking, every PR, single `composer merge-gate` step) + `greenfield-quality-tier.yml` (weekly, validated 1-thread model, artifacts); NO gate logic in CI (R3); YAML validated; "CI green" evidence lands with the first real PR run (recorded, not claimed; IDD §2f) → **(3) ✔ TRIPLE QUALIFICATION + EP-02 DONE (`5b93c501f`, IDD §6):** Architecture = fresh merge-gate PASS exit 0 · DDD = ownership unchanged · Trustworthiness = product unchanged / engineering increased (qualification process rejected a measurement that failed evidence validation) · EP-02 = slice-for-slice conformance, 2 ruled findings, 0 silent deviations; open item = first real CI run (push-dependent) → **(4) ✔ PB-007 FORMALLY CLOSED by ARB (2026-07-10, ruling verbatim in IDD §6.5; `3f2fd5dd2`).** Wording refined: *confidence in the engineering qualification evidence increased*. EPIC-001 closes after board sync + retrospective. Post-closure: engineering platform = production subsystem. → **(5) ✔ BOARD SYNC DONE (`e51178f73`, 2026-07-11 session):** 4 boards re-derived from evidence (M1–M3 ✔ · EPIC-001 7/7 implementation-complete · epics renumbered EPIC-002 Evidence/003 Voting/004 Appointment/005 Read Models/006 legacy · ENG-004 → validated baseline · F-1/F-2 closed · Synchronization-record table). Retro input pack: `docs/implementation/EPIC-001_Retrospective_Input.md` (12 observations · 7 promotion candidates · 3 deletion candidates · EPIC-002 status-only). → **(6) ✔ RETROSPECTIVE CONDUCTED + ALL TEN CANDIDATES EXPLICITLY DECIDED + EPIC-001 FORMALLY CLOSED (explicit ARB decisions, 2026-07-11; record `docs/implementation/EPIC-001_Retrospective.md`; commits `83f6aff3a`..`1bfa4beab`).** Standing rules born here: documents record governance, decisions create it (continue ≠ approval); retrospectives recommend, ARB decides. AST-008 unwired (D-2 override). → **(7) → NEXT (single action, NEW SESSION, researcher mode): EPIC-002 Strategic Discovery Phase 1 — problem-space literature review** (charter `docs/implementation/EPIC-002_Problem_Statement.md`: primary trust-properties question · assumption-evidence table · NKM · FACT/INTERPRETATION/RECOMMENDATION/OPEN-QUESTION labels · no code/aggregates/APIs/IDD · STOP at session end) → **(4)** PB-007 / EPIC-001 closure → **(5)** re-derive + synchronize ALL program boards/status docs exactly ONCE → **(6)** retrospective → **(7)** primary focus shifts to remaining PublicDigit contexts (EPIC-002 onward). Boards deliberately NOT updated before closure.
- **✔ (2026-07-11) EPIC-002 pre-work — architecture-corpus audit + Strategic Dependency Map (Principal Architect directive, this session).** (a) **Corpus-vs-code audit** (4 parallel sweeps over all 443 `docs/architecture` docs; summary in session logs 07-10/07-11): greenfield core + messaging platform + ADR-001..005 capability system = IMPLEMENTED per certified design; genuinely open = operational-BC strangler migration (Evidence/Voting/Appointment — R49-07 order **stands**), Trust T-002/T-003 (+T-005/T-006), discovery debts D35/D36/D37 (HIGH) + ADH-1/ADG-2/ADGR-1/D39/D42B, AD-M2, G-1, ADR-005 C.2.8 residue (PhaseCompletionRules deprecated-not-deleted; CI codegen drift-check), dormant 5 security events + blank Evidence-ARB record, frontend arch-test case-sensitivity risk (`Composables/` vs `composables/`); stale-doc refresh (c4, v1.0 status tables) = Stream B at retrospective. (b) **Map artifact:** `docs/architecture/design/EPIC-002_Context_Dependency_Map_Draft.md` — **authority: generated, awaiting ARB ratification**; PA approved direction w/ 6 refinements FOLDED (R49-07 NOT reopened — map = evidence into discovery only; relationship-type taxonomy; dependency matrix w/ I/D/L status; Legitimacy = architectural HYPOTHESIS, not re-open; Identity/Eligibility = external upstream, GI-1/RQ-ID-01 visible). (c) **EPIC-002 shape ruled (PA):** Stream A = ONE BC (boards say Evidence), Strategic Discovery ONLY (literature → DDD → ownership → open questions → STOP, no IDD/implementation); Stream B = doc cleanup at retrospective, no redesign; **Architecture Gap Analysis = repeatable per-BC process** (retrospective promotion candidate; the 07-10 audit is the prototype run). (d) **PA follow-up review (9.5/10) — 2 further refinements FOLDED:** map §4 now rules **decomposition before ordering** (certified decomposition = discovery input, not immutable premise; challenges route through the BDR re-open trigger, same pattern as the Legitimacy hypothesis); NEW artifact `docs/architecture/design/EPIC-002_Problem_Statement_Draft.md` (authority: generated, awaiting ARB) — problem (evidence stored implicitly; dormant events; unresolvable `EvidenceEnvelopeRef`), Q0 decomposition + Q1–Q9, deliverables, explicit NOT-produced list + STOP rule, literature plan that REUSES Round 36A/B/C (new domains only: W3C PROV, chain of custody, administrative adjudication, constitutional-law workflows, event sourcing in regulated domains). PA sign-off guidance recorded: map = working discovery artifact not architecture · freeze EPIC-001 after retrospective · EPIC-002 = Problem Statement → literature → Strategic Discovery · no tactical models until discovery converges. Sequence (6) retrospective remains NEXT — map + problem statement are *inputs* to it and to EPIC-002 discovery.

<!--superseded-->
- **(superseded) → STOP. Next authorized — PB-006 (IT-1..8, end-to-end integration / operational validation of the constitutional correction loop): cross-context event flow · replay · parking/redrive · idempotency · operational correctness.** Reuse the qualified architecture exactly (no redesign of aggregates/repos/messaging/ownership/published language). PB-006 starts with **Discovery** (needs its scope/IDD) → workflow adds the three qualifications before Completion Review. THEN PB-007 (merge gate: F-1 Deptrac + F-2 Infection). Candidate-pattern promotion is weighed at the retrospective (practice → evidence → standards).

<!--old-->
- **→ STOP. Await ARB authorization to begin PB-005 (Contestation Reaction).** When authorized, PB-005 starts with **Phase 1 Discovery** (Strategic DDD Ownership Review — no code) → **IDD** → **ARB review** → RED → GREEN → Regression → Qualification → Completion Review → STOP. Governance: PB-004 gives *evidence*, not standards; reuse each pattern as an evidence-justified decision (state *why it fits*), not by precedent; the ARB decides candidate-pattern promotion only after reviewing PB-005.

**PROCESS NOTE (this turn):** TDD-first was breached on step 2's first attempt (production written before the failing test). Corrected via stash→RED→pop→GREEN. Lesson reinforced: RED **before** code, every step.

## Architecture debt (tracked)
- **AD-M1 — RESOLVED (2026-07-07) · category: Architecture Fitness Evolution** (no domain change — only *where* the fitness guard lives). Anonymity guard relocated to the constitutional suite (`GreenfieldCoreArchitectureTest::test_at_q7_001_no_voter_vote_linkage` now scans the Shared messaging surface incl. Outbox); property #11 removed from the Inbox test. RED→GREEN proven; Architecture suite 142✔/1 skip; greenfield PHPStan clean. First fitness change validating PGP-03.
- **AD-M2** — decide Outbox formal Application port / hexagonal symmetry (ADR-MP-05/Q5) — future ADR, only under business pressure.
- **G-1** (trivial, pending) — release numbering for the governance addition ("AKB 1.2" collides with roadmap §4); tag neutralized; recommend a non-colliding tag.

## Engineering Platform — constitutional consolidation COMPLETE (2026-07-11; supersedes older platform notes below)
- **Standards:** ES-001..ES-006 (`engineering/governance/STANDARDS_INDEX.md`) + Engineering Decision Model (7 decisions, `engineering/architecture/reference/`) + Decision Authority & Verification Matrix — all **PROPOSED, ratification batch awaiting ARB signature** (incl. ES-006.4 harvest question; riders: append-only-guard candidate, automation-principle candidate; F-04 EKP disposition on the same agenda).
- **Rulings:** register now R-30..**R-38** (R-38 = consolidation ruling: operational-validation phase; conceptual freeze — no new concepts without operational evidence).
- **Plan concept ADOPTED:** Work Plan (Runtime, `.claude/plans/`) ≠ Engineering Plan (governed, `docs/plans/`, ES-004.2) — `docs/implementation/Plan_Concept_Decision_Paper.md`.
- **Track next action:** ARB ratification → **fresh session runs C3 + OQ-ENG-003 together** (C3 plan: `.claude/plans/AIP-iteration-1-construction.md` · protocol: `engineering/verification/qualification/OQ-ENG-003-Execution-Loop-Verification-Protocol.md`) → STABLE → Project Knowledge pilot. Program board: `architecture/how_far_we_are/20260711_1750_three_track_program_board.md`.
- **2026-07-12 additions (platform UNCHANGED — understanding evolved through recorded observations awaiting future evidence):** verification reports (artifact lifecycle 60-artifact survey · execution verification TDD/Clean/Hexagonal/DDD, ARCHITECTURALLY APPROVED · AI-architecture reconstruction — placement litmus failed the docs/architecture/ai-architecture/ commission; Guide 03 runtime mechanics written instead) · db-safety hook CRITICAL FIX (exit 1→2: the gate was not actually blocking) · Project-State-Sync Observation Protocol ACTIVE (3–5 cycles, falsifiable) · ~10 recorded-not-built candidates parked for the retrospective (see session log 2026-07-12).
- **Pending ARB (new, 2026-07-12):** `docs/implementation/Strategic_DDD_Discovery_Engineering_Governance_Domain.md` (discovery only — no context map until evidence) · `docs/implementation/KnowledgeOS_Product_Discovery_Charter.md` (5-stage gated product discovery, third track, nothing executes until approved).

## Parallel tracks (not active this session)
- **AI Engineering Platform — Baseline v1.0, Reference Implementation Pending (ADR-AIP-01/02 signed; rulings R-1..R-27 in `Phase-02.5-Certification-Plan.md` §6):** Construction Iteration 1 under way — **C1 DONE+ACCEPTED** (`.claude/platform/registry.yaml`: registry-first workflow BINDING, CMP/AST ids, VERIFY-based adoption; dev guide `developer_guide/ai_platform/01_registry_first_workflow.md`). **GOVERNANCE FREEZE in force (R-27):** no new platform principles until the PB-004 retrospective. **Track next action: EXECUTE slice C3 (plan APPROVED AS WRITTEN by ARB 2026-07-08 — do not modify; instruction verbatim in the plan). C3 = the first operational qualification: fresh session boots via CONTEXT injection, follows EP-01, implements ONLY AST-010 `run-gates.sh` (measuring instrument: greenfield PHPStan → capture → PASS/FAIL, stop-on-fail, then Architecture suite; NO interpretation), verifies + falsifiability, EP-02 Completion Review, STOP. No other capabilities/refactorings/governance. Rulings now live in `engineering/architecture/adr/ADR-AIP-LOG-Platform-Rulings.md` (R-30..R-37; proposals sealed; EM-001 2026-07-10 moved the platform corpus to `engineering/` — see `engineering/MIGRATION_REPORT.md`). **STANDING INSTRUCTION (Principal Architect, 2026-07-10, in force until the retrospective): no architectural expansion — every change must either PRODUCE executable evidence or CONSUME executable evidence.** (R-37 structural freeze + burden of proof: expansion without demonstrated insufficiency is rejected by default.) Then PB-004 (Operational Readiness v1.0, R-33) → retrospective → C2.** After retrospective: Continuous Evolution (feature → observation → one amendment if justified). OI-1 (02.6 terminology review) still open. Plan: `.claude/plans/AIP-iteration-1-construction.md` · session log: `.claude/sessions/2026-07-08.md`. **✔ R-36 (2026-07-09): AI Architecture Promotion Review ADOPTED** — the R-27 freeze precondition (PB-004 retrospective) is met; via the AIP-13 amendment path, 6 one-line reasoning behaviours promoted into `OPERATING_INSTRUCTIONS.md` §Promoted Engineering Behaviours (ownership-determines-reuse · reuse-before-create · deferred ≠ skipped · epistemic labels · stop-at-uncertainty · evidence-over-unverified-theory); AST-008 deprecate→remove next release; Category-B follow-ups (Domain-Event ≠ Integration-Event → principles doc; workflow chain → v1.1 ratification) expressly NOT executed. Matrix: `claude/plans/swirling-jingling-blossom.md` · session log 2026-07-09.
- Governance: 38D-02 Capability Relationships (after ARB digests 38D-01)
- Docs candidates: Domain Model Catalogue · root CLAUDE.md tech-table refresh

## Automation (hooks, .claude/settings.json — see .claude/scripts/README.md)
- SessionStart auto-injects MEMORY.md + CONTEXT.md + the active plan THIS FILE declares (first `.claude/plans/*.md` path above) + today's log. Stop prints a sync report when state is stale. PostToolUse(Write|Edit) records modified files to `.claude/runtime/YYYY-MM-DD-files.log` + `-state.json` (gitignored) and refreshes plan `Last Updated:` headers.

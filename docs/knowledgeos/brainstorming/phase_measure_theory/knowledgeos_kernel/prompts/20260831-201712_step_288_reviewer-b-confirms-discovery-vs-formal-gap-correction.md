I’ve got the updated state. The important correction is now very clear:

**The project has not closed three formal gaps. It has recovered three corpus concepts that were missing from the working model.**

The current frontier is therefore:

1. **`≡` semantic equality** — named in the corpus, but its decision procedure is unresolved; additionally blocked by **Step 254 Decision 3** on whether `Π` belongs inside semantic equality.
2. **`≈` observational equality** — named, but requires a closed permitted-observation set.
3. **`≅_λ` provenance-sensitive equality** — named, but “relevant provenance” remains undefined.
4. **`Qualify`** — still the genuinely irreducible formal blocker for `π_K`; the existence of `Φ` narrows the discovery gap but does not provide `Qualify`'s missing observation-level semantics.
5. **`𝒪` vs the eight primitives** — still unresolved, so minimality cannot yet be claimed.
6. **`Ω`** — its referent layer is recovered (`W → Ω → O`), but executable semantics remain open.

And importantly, **D288 is scoped, not executed**.

### The architectural state I would freeze now

```text
DOMAIN REALITY
      W
      │
      ▼
   Sañjaya
      Ω
      │
      ▼
OBSERVATION / O
      │
      │  Qualify  ← G1 formal blocker
      ▼
KNOWLEDGE STATE / K
      │
      ▼
DECISION
      │
      ▼
ACTION / TRANSFORMATION
```

With the critical distinctions preserved:

```text
Reality ≠ Observation ≠ Knower

semantic state projection = ESTABLISHED
operational equivalence    = NOT ESTABLISHED
observational equivalence  = REFUTED
computable projection      = BLOCKED (Qualify)

Correspondence ⇏ Type ⇏ Primitive ⇏ Canonical Architecture
```

### The most important next decision

The project has now reached a **genuine governance/normative boundary**, rather than another research-discovery problem:

> **Does `Π` belong inside semantic equality `≡`?**

That decision determines whether the semantic quotient used in D285-5 is legitimate. Until it is decided, the statement about `δ` remains correctly weakened to:

> There exist distinct operations whose resulting states are equivalent **under a semantic projection that discards `Π`**.

It must **not** be promoted back to “`δ` is non-injective.”

And the corpus itself independently confirms that **equality ambiguity is a stop-gate**, so continuing to generate more Sanskrit hypotheses before resolving this would be moving in the wrong direction.

### One subtle but very important consequence

The finding about `≅_λ` is actually valuable because it demonstrates why the equality question cannot be hand-waved:

* under **structural equality**, the relevant antecedent can be false;
* under **semantic equality**, the witness becomes substantive, but only after choosing what semantics preserve;
* under **observational equality**, the result depends on the permitted observation set;
* under **provenance-sensitive equality**, the same witness can cease to establish non-injectivity.

So **“equality” is not one mathematical operation in this programme.** The corpus already recognizes multiple relations; the unresolved work is determining their operational decision procedures and normative scope.

I would therefore regard the current package as **ready to freeze and hand the `Π ∈ ≡?` decision to the appropriate human governance authority**, rather than launching another broad research pass.

# 10 — Next Legitimate Act (mandate §§10, 17, 24, 25)

## §10 AUDIT — *"N-4 is the earliest legitimate act"*

**§10 requires *"earlier"* be given a precise meaning, and lists five. Measured against meaning 1
(upstream in the dependency graph):**

**MEASURED** — source nodes (in-degree 0), all resolvable by a decision:
```
O   Qualify   assertion   evidence   id_context   policy   temporal      <- SEVEN
```
**Leverage** (nodes reachable):
```
O            reaches 20        evidence   reaches  2
id_context   reaches 17        Qualify    reaches  1
policy       reaches 16        temporal   reaches  1
                               assertion  reaches  1
```

$$\boxed{\begin{array}{l}\textbf{"N-4 is THE earliest legitimate act"} \rightarrow \textbf{NOT ESTABLISHED. DOWNGRADED.}\\ \textbf{"N-4 is the highest-LEVERAGE independent root"} \rightarrow \textbf{supported by measurement.}\end{array}}$$

**`𝒪` is *a* source node, not *the* source node.** Six others have no incoming edge either.

### Against the other four meanings of "earlier"

| Meaning | Is `N-4` earliest? |
|---|---|
| 2 prerequisite for **derivation** | 🟡 for the *extension* of `≈`/`≡` and for congruence — **yes**. For `≡`'s *definability* — **no** (`04 §3`) |
| 3 prerequisite for **governance** | 🟡 `N-1′` cannot be *evaluated* without `𝒪_K`. But `N-8`, `N-13`, `N-12`, `N-19`, `N-20`, `N-21` need nothing |
| 4 prerequisite for **implementation** | ✅ yes — nothing implements without operations |
| 5 prerequisite for **canonicalization** | 🔴 **NO** — canonicalization needs `evidence` (`38.85`), a **separate** root |

⚠️ **Meanings 4 and 5 disagree.** **They were being conflated.** *(That is exactly what §10 warned
against, and I had done it.)*

## §17 The competing legitimate first acts — the corpus selects none

| Act | Node | Leverage | Independent? | Blocked by |
|---|---|---:|---|---|
| **`N-4`** define the **mandatory-membership rule** for `𝒪`/`𝒯` | `O` | **20** | ✅ | nothing |
| **`N-8`** define **"identity context"** | `id_context` | **17** | ✅ | nothing |
| **`N-13`** the **canonicalization / evidence policy** | `evidence` | 2 | ✅ | nothing |
| *(policy semantics)* | `policy` | 16 | ✅ | nothing |
| **`N-3`** `𝒪_K`'s extension | `O_K` | — | 🔴 | `N-4` |
| **`N-1′`** ratify the `261.21` candidate | — | — | 🔴 | `N-3` ← `N-4` |

> ### The four DOCUMENTARY items require no decision at all (`07`)
> the `𝒪` glyph overload · `261.21`'s self-reference · registering `TraceOrigin`/`ExplainRevision` ·
> a type for observation operations.
> $$\boxed{\textbf{These can be repaired NOW, by nobody's authority, and three of them are what made } \mathcal O \textbf{ look worse than it is.}}$$

## §24 Stop condition reached

$$\boxed{\textbf{STOP-B, with a } \textbf{STOP-D} \textbf{ qualification.}}$$

**STOP-B** — a precise normative boundary is identified: **mandatory membership of `𝒪`/`𝒯`**, and no
derivation crosses it (8 routes tested).
**STOP-D** — ⚠️ **and a new dependency does invalidate the assumption that `N-4` is *the* earliest act:
seven independent roots exist, and meanings 4 and 5 of "earlier" disagree.**

## §25 THE REQUIRED FINAL STATEMENT

> **What is the smallest statement KnowledgeOS can honestly claim about `𝒪`/`𝒯` today, without
> assuming any unresolved equality, governance, observation, or operation semantics?**

$$\boxed{\begin{array}{l}\textbf{The corpus supplies a five-kind classification of operations, typed signatures for}\\ \textbf{eight of the twenty-two it names, and a subset relation } \mathcal T \subseteq \mathcal O.\ \textbf{It supplies no}\\ \textbf{rule saying which operations are MANDATORY, and no operation has a semantic}\\ \textbf{body, an identity, or a ratification. Nothing more can be claimed.}\end{array}}$$

⚠️ **Note what this does NOT say:** it does not say `𝒪` is open, closed, minimal, complete, or
derivable; it does not name a member; it does not say the classification is right. **It is the largest
statement the evidence supports and no larger.**

## STATUS
**MEASURED** 7 source nodes; leverage counts · **NOT ESTABLISHED / DOWNGRADED** *"N-4 is the earliest"*
· **DERIVED** meanings 4 and 5 of *"earlier"* disagree; 4 documentary items need no authority ·
**NORMATIVE** `N-4`, `N-8`, `N-13`, `N-3`, `N-1′`, `N-2` · **STOP-B + STOP-D**

---
artifact: 25 — THE GĪTĀ TRACK CLOSED (Ch 17 · 18 · 5) · four controlled comparisons · and the `Guṇa` fork
sources: 5 newly renamed prompts (1 byte-identical duplicate) vs their `external_research` counterparts
date: 2026-09-01
verdict: **FOUR controlled comparisons now, all the same shape. The `Guṇa` flag — open since Step 286 §4 — is not dissolved but SHARPENED into a two-branch fork. `Determination` RECOVERS a corpus object lost since Steps 157/165. And "equal treatment of all evidence" is REFUTED by the corpus's own statistics.**
---

# 25 — Chapters 17, 18, 5 · and the `Guṇa` Fork

## 0. Renaming
**5 stamped, 1 byte-identical duplicate.** The kernel lane has now covered Ch 5–18 — **the Gītā track is
complete on this side.**

## 1. FOUR controlled comparisons — the pattern is now established, not inferred

`24` compared Ch 16 head-to-head. **Three more chapters were read by both lanes:**

| Ch | `external_research` | kernel lane |
|---|---|---|
| **16** | *"Missing Architecture — **Solved**"*; Integrity/Bias/Character Solved | one inequality, *"an operator candidate"* |
| **17** | *"Trust Problem — **Solved**"* · *"Quality Classification — **Solved**"* · *"Epistemic Framework — **Solved**"* | guṇa as a **mode/state model**, *"not an assertion that KnowledgeOS literally implements the metaphysics"* |
| **18** | *"**Completeness ✅ Complete** — all gaps addressed"*; *"Architecture Gap ✅ Solved — **complete transition system**"* | five separations + **two candidate capabilities** |
| **5** | *"Non-Attachment ✅ Complete"* · *"Equal Vision ✅ Complete"* · *"Self-Satisfaction ✅ Complete"* | *"clarifies several kernel mechanisms that were previously implicit"* |

$$\boxed{\begin{array}{c}\textbf{4 chapters · 2 lanes · 13 "Solved/Complete" claims against 0 of 13 closure rows.}\\ \textbf{The kernel lane declares nothing solved in any of the four.}\end{array}}$$

⚠️ **`G-85` is now supported by four controlled comparisons, not by aggregate counting.**

## 2. ⭐⭐ THE `Guṇa` FORK — a flag open since Step 286 §4, now sharpened

**`REFINED-STEP-286 §4`:** *"`Guṇa` is flagged before it is ever opened: it touches the untyped-
`Threshold`/`θ`-overload measurement gap and **must declare a scale type first**."*

**`external_research` maps `Guṇa → Σ` directly** — and `19 E2` showed that produces a type error:
`Σ = (Supported, Strong)` uses values present in **no ratified axis**.

**The kernel lane does something different:**
$$\boxed{K_t = (\Sigma_t,\ G_t,\ \ldots)} \qquad \textbf{mode } G_t \textbf{ is a SEPARATE component, not a relabelling of } \Sigma$$
> *"Knowledge state and kernel mode must remain separate."*

### Does that dissolve the flag? **No — it forks it, and the fork is the useful part.**

| Branch | Consequence |
|---|---|
| **`G_t` is DERIVED from `Σ`** *(e.g. mode = goodness when support is strong)* | 🔴 **the threshold returns** — a derivation rule over `S`'s ordinal bands is exactly the `θ` gap `§4` flagged |
| **`G_t` is INDEPENDENT of `Σ`** | 🔴 **a new state component with no carrier** — and `K_t = (Σ_t, G_t, …)` is another **open-ended tuple**, which `258.35` warns against |

$$\boxed{\begin{array}{c}\textbf{Neither branch is free. The flag is not lifted —}\\ \textbf{it is converted from a vague caution into a two-branch question with named costs.}\end{array}}$$

✅ **That is real progress on an item that has sat untouched since Step 286.** ⚠️ **And `§4`'s gate still
binds: `Guṇa` must declare a scale type first, and neither branch declares one.**

## 3. ⭐ `Determination` — recovering a LOST corpus object

**Ch 18 proposes two distinct kernel capabilities:**
$$\boxed{Buddhi \rightarrow \text{discrimination}} \qquad\qquad \boxed{Dh\d{r}ti \rightarrow \text{persistence / stability of a chosen direction}}$$
with the chain `Knowledge → Buddhi → Determination → Action`.

### ⭐ `Determination` is not new — it is RECOVERED

**The programme's own record** (`01-STEP-272-PREMISE-AUDIT §3`, `PA-1`) lists **four concepts specified
and then dropped**, and `Determination` is #3:
> *"`Determination` — **a full aggregate root**, Steps **157/165**, absent from Steps 262–271."*
And `Determine` is among the **14 forced operations**; `D_t` appears in the canonical `K`.

$$\boxed{\begin{array}{c}\textbf{The G\={\i}t\={a} lens independently re-raises an object the corpus SPECIFIED and LOST.}\\ \textbf{That is a RECOVERY SIGNAL for a documented drop, not a new candidate.}\end{array}}$$

⚠️ **And it does not restore it** — `PA-1`'s finding stands: *"the corpus has no mechanism that notices a
commissioned step not being performed."* **Re-raising an object is not re-instating it.** **`R-Q1`,
registry.**

✅ **`Dhṛti` (persistence) converges with `H-G6-3` Epistemic Stability** — a second arrival.

## 4. Ch 18's five-way separation

$$\boxed{Knowledge \neq Understanding \neq Determination \neq Action \neq Result}$$

**Audited against the ratified eight primitives:**

| Term | Carrier |
|---|---|
| Knowledge | `K_t` — ⚠️ canonical `K` **OPEN** |
| **Understanding** | 🔴 **none** — this is `Vijñāna` again (`GK-Q2`), straddling **`Qualify`** |
| **Determination** | 🟡 `D_t` — **specified 157/165, lost** (§3) |
| **Action** | ✅ **ratified primitive** |
| Result | 🔴 none — ⚠️ `Karma-phala`; `R6` as *`Command ≠ Transformation`* |

⚠️ **Two of five have no carrier, one is a lost object, one is ratified.** ✅ **The separation is
consistent with `256.21`'s `Command ≠ Transformation`, which the source itself cites** — so it is
**corroboration**, not derivation.

## 5. 🔴 A `external_research` Ch 5 claim REFUTED by the corpus's own statistics

> **Ch 5 (`external_research`): *"Equal Vision ✅ Complete — **equal treatment of evidence**."***

**The corpus establishes the opposite, twice:**

| `25J.29`–`25J.32` | evidence **independence** must be modelled: if `E_2 = LLM(E_1)` then treating them as independent **double-counts** — *"a serious statistical error"* |
|---|---|
| `25J.34` | $\boxed{\text{Confidence aggregation requires a defined statistical model}}$ — *"never implement `Confidence = Average(SourceConfidences)`"* |
| `38.20` | $Cost(FalseMerge) \neq Cost(FalseSplit)$ — **asymmetric loss** |

$$\boxed{\begin{array}{c}\textbf{"Equal treatment of all evidence" is precisely the error the corpus names.}\\ \textbf{Samatva as EQUANIMITY is a stance; as EQUAL WEIGHTING it is a statistical mistake.}\end{array}}$$

⚠️ **This is a new T0 with a corpus refutation attached** — and it is the same shape as `E2`: a
philosophical term mapped onto a technical operation whose semantics contradict it.

## 6. Tally

| Tier | `24` | **`25`** | new |
|---|---:|---:|---|
| **T4 KERNEL** | 3 | **3** | **0** |
| T4-SHAPE | 3 | **3** | — |
| T3 | 13 | **14** | 0 |
| T2 | 18 | **21** | 0 |
| T0 | 16 | **18** | — |
| constraints | `C1`–`C10` | **`C1`–`C11`** | ⭐ `C11` |

$$\boxed{C11:\ \text{Knowledge state} \neq \text{kernel mode} \quad (K_t \text{ and } G_t \text{ must remain separate})}$$
**New T0s:** *"equal treatment of evidence"* (§5) · *"Chapter 18 completes the entire Gītā / complete
transition system"* (against `𝒪` membership open, 0 of 22 operations bodied).

$$\boxed{\textbf{The G\={\i}t\={a} track is now COMPLETE on the kernel-lane side: Ch 5–18 read. Still ZERO new primitives.}}$$

## 7. Register

| ID | Item | Class |
|---|---|---|
| **`P1′`** | ⭐ **The `Guṇa` fork** — `G_t` derived from `Σ` (θ returns) **or** independent (uncarried component + open-ended tuple). **Neither free; `§4`'s scale-type gate still binds** | **NORMATIVE** — sharpens `P1` |
| **`R-Q1`** | ⭐ **`Determination`** re-raised — specified `157`/`165`, **lost** per `PA-1`; `Determine` is in the forced 14; `D_t` is in canonical `K` | **registry / recovery** |
| **`K-Q6`** | `Buddhi` (discrimination) ≠ `Dhṛti` (persistence) — two kernel capabilities; `Dhṛti` converges with `H-G6-3` | **T2** |
| **`T0-new`** | *"equal treatment of all evidence"* — **REFUTED** by `25J.29–34` and `38.20` | **REFUTED** |

## 8. What this does not license
⚠️ **The `Guṇa` fork is not resolved, and `§4`'s gate is not lifted.** ⚠️ **`Determination` is
re-raised, not re-instated** — `PA-1`'s process defect stands. ⚠️ **No capability, mode or separation is
promoted.** 🔴 **Steps 288–291 unchanged; all `R6`.**
✅ **Discipline held on the kernel side in all four comparisons** — *"not an assertion that KnowledgeOS
literally implements the metaphysics of the Gītā"*, *"I would not simply append Chapter 5."*

# 09 — Normative Decision Register (mandate §14)

> **§14 is a hard constraint. NOTHING below is recommended, preferred, or ranked by desirability.**
> No `Π` branch · no `X` · no authoritative register · no `𝒪` · no `𝒯` · no kernel · no governance branch.

Each record carries the mandate's six required fields plus the ordering the graph forces.

| ID | Decision question | Alternatives | Consequences | Evidence AVAILABLE | Evidence UNAVAILABLE | Why research cannot decide it | Authority |
|---|---|---|---|---|---|---|---|
| **N-1** | Are `≡` and `≈` one relation or two? | (a) collapse `≈` into `≡` · (b) give `≡` a new non-observational definition · (c) notation clash — rename | (a) register → 5, violates *"not interchangeable"* · (b) `≡` currently **has no definition** · (c) cheapest, but must be established | `258.8`·`261.5`·`261.21`·`261.1`·`246` verbatim | ⚠️ **anything distinguishing a deliberate identification from an editorial slip** — `261` states both without noting tension | **The text supports no reading over another.** Choosing one would *repair the corpus by inference* — which §14 and Step 288's mandate both forbid | **corpus owner / ARB** |
| **N-2** | `Π ∈ ≡` ? | A: `Π ∈ ≡` · B: `Π ∉ ≡` | full 13-row table (`step-288/07 §6`): `≅_λ` collapses, `D285-5` vacuous, near-zero dedupe, replay-exactness — all flip | both branches corpus-stated (`261.8`); `258.15` shows `TraceOrigin` distinguishes and audit metadata does not | the **technical half** needs `𝒪`: *does any **mandatory** operation observe `Π`?* | The technical half is **decidable, not normative** (`258.31`) — but only after `𝒪`. The residue (which of `258.31`'s 3 repairs) is a meaning choice | **ARB + PO** |
| **N-3** | Which `𝒪_K`? | any closed observation set; **30 usable `Σ`-level candidates** if `≈` is read that way | determines `≈` **and** `≡_K` under `258.8` | `261.21` boxed *"not closed"*; 30 usable projections **measured** (`08`) | any corpus statement of *which* observations are permitted | membership is a declaration, not a derivation | **ARB** |
| **N-4** | Which `𝒪` / `𝒯`? | ⚠️ **not enumerable here** | gates congruence · minimality · `258.20/21` · all bindings · `δ` | `256.2`/`259.7` **candidate** set: 14 forced, ≤18 | a **membership rule** — and `03` found **no incoming derivation edge** | 6 candidate derivation routes tested; **all fail** (`03`) | **ARB** |
| **N-5** | Which `X` defines `≈`? — *if* the `Σ`-reading is adopted | **30** usable (32 minus 2 degenerate endpoints) | selects what counts as *the same* observationally | **exactly 32 partitions measured**; 2 degenerate | which question `≈` is meant to answer | a bounded choice over meanings | **ARB** |
| **N-6** | Which `λ` (provenance relevance)? | a **principle** exists; **no predicate** | `≅_P` undefined until fixed | `261.8`; `265` | any computable predicate | *"decision-relevant"* is not turned into an algorithm without evidence | **ARB** |
| **N-7** | Is `≡` required transitive? | yes · no · **within a context** | without it **no quotient exists** — `04 §13` | 5 loci flag it unsafe; `I_48` supplies the shape for `≅_I` | whether `≡` inherits `I_48`'s scoping | ⚠️ **partly derivable:** *if* canonicalization is required, transitivity is **forced**. The requirement is the choice | **ARB** |
| **N-8** | Define *"identity context"* | a scoping choice | unbinds `I_48`'s parameter — the programme's one narrowing | `38.6`·`38.7`·`38.81`·`195.16` | any definition — **none exists in the corpus** | a scope declaration | **ARB** |
| **N-9** | Ratify `I_48`–`I_51`? | ratify · reject · amend | `I_48` is the only equality-axis narrowing | the four invariants as stated | any adoption record | research→architecture promotion is always an act | **ARB** |
| **N-10** | Decision codomain | 4 corpus codomains + the `Relation × Status` product | flattening loses `012 §36`'s separation | all four enumerated | which sets | ⚠️ **the separation principle is derivable; the specific sets are not** | **ARB** |
| **N-11** | Per-operation bindings | 7 analysed · **6 unanalysed** | `261.19`: no single relation suffices | `261.19`'s matrix, all `UNRESOLVED` | 6 operations entirely | ⛔ **BLOCKED by N-4** — cannot start before `𝒪` | **ARB** |
| **N-12** | `Deduplicate`'s criterion | **4** (`261.17`) | near-zero dedupe under `N-2`(A) | 4 criteria; 8 counterexample shapes | which the domain needs | `261.19` classifies it **NORMATIVE/domain-dependent** | **PO + domain** |
| **N-13** | Which canonicalization? | ⚠️ **`38.85`: must FOLLOW evidence** | `=` and `≡_exact` are Level-5 **only relative to it** | `38.53`·`38.85`·`012 §27`; **EXECUTED** key-order and locale failures | any evidence policy | an evidence-policy choice, not a formal one | **ARB** |
| **N-14** | The `id`/mutable-`e.state` conflict | authorize repair · re-scope · accept dangling | **EXECUTED**: violates `StructuralValid`'s own clause | the executed transcript; `25I.29`·`25I.30`·`258.2`·`258.18` | — | ⚠️ **the repair is FORCED, not chosen** (`04 §14`). Governance **authorizes**; it must not **select** | **ARB** |
| **N-15** | Ratify `EntityID`/`KAID`/`RecordID`? | ratify · reject | 5 of 11 identity kinds absent | `25S.36`/`25S.43`, self-marked *provisional* | any adoption record | promotion act | **ARB** |
| **N-16** | Is `Continuity` a kernel relation? | yes · no | in **no** register; `195.24` identity ⊥ lineage | `195.21`·`195.51`·`195.52` | whether a mandatory op needs it — needs `258.21`, needs `𝒪` | ⛔ partly blocked by N-4 | **ARB** |
| **N-17** | Is `G_I` a kernel component or part of `ℛ`? | separate graph · inside `ℛ` | `38.87` *"must not be collapsed"*; `ℛ` is 3-field | `38.88`·`195.43` | same — needs `258.21` | ⛔ partly blocked by N-4 | **ARB** |
| **N-18** | `Merge` commutative? associative? | impose · do not | associativity is what distributed ingestion needs | `25J.46/47`: *"not yet imposed"* / *"a design goal"* | any test — needs `δ` | ⚠️ **these are LAWS TO BE TESTED, not chosen** (`04 §14`) — withhold | **not governance** |
| **N-19** | False-merge/false-split loss asymmetry | domain-specific (`38.20`) | sets the operating point | `38.18–38.23` qualitatively | loss function · threshold · calibration | ⛔ **also blocked by `G-22`: no `(Ω,𝓕,P)`**, so `P(Same)` has no referent | **PO + domain** |
| **N-20** | Register ownership / authority | name an owner | **no register is authoritative** (`06`) | 9 registers, none ratified | any ratification record | ownership is constitutive | **ARB** |
| **N-21** | The `287` identifier collision | equality-`287` · invariants-`287` · renumber | two artifacts share a number | both artifacts | — | registry hygiene | **registry** |

## Forced ordering (from the graph, not from preference)

```
N-1  ─── independent, and PREREQUISITE to every ≡-dependent record
N-4  ─── independent, and PREREQUISITE to N-3, N-11, N-16, N-17, N-2's technical half
N-8  ─── independent (unbinds I_48)
N-13 ─── independent (unbinds =)
   others follow
```

**Independent (can be taken in any order):** `N-1` · `N-4` · `N-8` · `N-13` · `N-12` · `N-19` · `N-20` · `N-21`
**Ordered (must follow):** `N-2`(technical) · `N-3` · `N-5` · `N-11` · `N-16` · `N-17` ← all after `N-4`
**WITHHELD from governance** (derivable or undecidable): congruence · `N-18` merge laws ·
`N-14`'s *selection* (authorize only) · `N-5`'s two degenerate endpoints · `≡`'s total Boolean procedure

## STATUS
**ESTABLISHED** 21 records, six fields each; the forced ordering; 8 independent · **BOUNDED** every
alternative set is finite and enumerated · **NORMATIVE** all 21 · **DERIVED** 5 items withheld from the
surface · **DEFERRED** every decision. **Nothing recommended.**

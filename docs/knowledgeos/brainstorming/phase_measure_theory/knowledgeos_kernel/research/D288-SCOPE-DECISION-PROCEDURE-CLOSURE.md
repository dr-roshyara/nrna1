---
artifact: D288 · SCOPE — DECISION-PROCEDURE CLOSURE
mandate: `prompts/20260831-195256_step_288_reviewer-b-…-d288-mandate.md`
date: 2026-08-31
status: **SCOPED, NOT EXECUTED — 3 of 4 targets are NORMATIVE and cannot close without a human act**
---

# D288 · Decision-Procedure Closure — Scope

**Mandate:** close `≡`, `≈`, `≅_λ` and `Qualify`, *"with no new Sanskrit expansion until those four are
resolved or explicitly classified as irreducible `G1` gaps."*
**Standing constraint:** *"Do not promote any of these into architecture merely because a plausible
definition can be written."*

> **This artifact SCOPES D288. It does not execute it.** Executing three of the four targets would
> require taking a normative decision, which this research may not do. **Per-target derivability is
> established below — that is the deliverable the constraint permits.**

## 1. Per-target derivability

### Target 1 · `≡` semantic equality

| | |
|---|---|
| **Corpus status** | `CORPUS` 246 §B — *"iff they encode the same knowledge semantics."* A **name**, no procedure |
| **What blocks it** | **Step 254 Decision 3, verbatim OPEN:** *"Is governance/authority part of semantic equality? Should `K₁ = K₂` require the same Authority/Policy/Governance, or only the same epistemic content?"* |
| **Derivable?** | 🔴 **NO.** The question *is* the decision. Both answers are formally consistent; the corpus declines to choose |
| **Classification** | **NORMATIVE.** Not `G1`-irreducible — `G1`-**pending-a-decision** |
| **Consequence of each branch** | `Π` **inside** `≡` ⇒ `D285-5`'s property becomes **vacuous** and `≅_λ` collapses into `≡`. `Π` **outside** ⇒ the property is substantive and provenance-blindness must be handled elsewhere. **The branch chosen decides whether `D285-5` says anything at all** |

### Target 2 · `≈` observational equality

| | |
|---|---|
| **Corpus status** | `CORPUS` 246 §C — *"iff every permitted observation produces the same result"* |
| **What blocks it** | **the permitted-observation set is undeclared** — and `CORPUS` **Step 261 §261.23** says so itself: *"the observation set is not fully closed."* Parameterisation is also open: by `Ω`? role? access level? purpose? *(all four appear in the Sañjaya principles: `Knowledge = f(Access_Level, Role, Purpose)`)* |
| **Derivable?** | 🟡 **PARTIALLY.** The **form** is derivable — `≈` is a **family** of relations indexed by an observation set, not one relation. That much follows from its own definition |
| **Classification** | **NORMATIVE** for the set · **DERIVED** for the form |
| **Sharpest available statement** | `≈_𝒬` for a declared query set `𝒬`; and `𝒬` must be declared per bounded context, because `K_A(Reality) ≠ K_B(Reality)` is a corpus principle |

### Target 3 · `≅_λ` provenance-sensitive equality

| | |
|---|---|
| **Corpus status** | `CORPUS` 246 §D — *"iff content and relevant provenance are equivalent"* |
| **What blocks it** | ***"relevant"*** is undefined. The nearest corpus material is `CORPUS` step-105: *"What matters is **decision-relevant provenance**, not private chain-of-thought"* — a **criterion of relevance**, not a definition |
| **Derivable?** | 🟡 **PARTIALLY** — *decision-relevance* is a corpus-supported **relevance principle**. Turning it into a predicate needs the decision context named |
| **Classification** | **NORMATIVE**, with a corpus-supported constraint |
| **Why it matters most** | ⚠️ **`≅_λ` is the relation under which `δ` is injective on `D285-5`'s witness.** Defining it therefore *changes an existing published result* |

### Target 4 · `Qualify`

| | |
|---|---|
| **Corpus status** | `Qualify : Observation × Policy ⇀ Evidence` — verification lane. Corpus has **the problem, formally posed**: the *Knowledge Qualification Problem* (`20260825-233107`) with `Φ : K̂_t → {Knowledge, Not-Knowledge}`, *"Problem A (Qualification): what makes `K̂_t` Knowledge? (necessary and sufficient characteristics)"* |
| **⚠️ Precision** | **`Φ` is NOT `Qualify`.** `Φ` qualifies an extracted **state**; `Qualify` qualifies an **observation**. **Different layers. Citing `Φ` as `Qualify`'s body would be a layer error** |
| **What blocks it** | the corpus poses qualification as an **open research problem**, and adds the constraint `P(k∣I)=0.97 ⇏ k ∈ K` — *probability is not qualification* |
| **Derivable?** | 🔴 **NO** — and unlike targets 1–3 this is **not** a pending decision. It is a genuine **`G1` formal gap**: no corpus passage states necessary-and-sufficient qualification criteria |
| **Classification** | **`G1` IRREDUCIBLE ON CURRENT EVIDENCE** |

## 2. Result — the mandate's own dichotomy, answered

The mandate asks for each target to be *"resolved or explicitly classified as irreducible `G1`."*

| Target | Verdict |
|---|---|
| `≡` semantic | 🔴 **NORMATIVE** — blocked on Decision 3. *Not* irreducible; **one human act closes it** |
| `≈` observational | 🟡 **form DERIVED** (`≈_𝒬`, a family) · **set NORMATIVE**, per bounded context |
| `≅_λ` provenance | 🟡 **principle CORPUS-SUPPORTED** (decision-relevance) · **predicate NORMATIVE** |
| **`Qualify`** | 🔴 **`G1` IRREDUCIBLE** on current evidence |

> ### **Three of four are one decision away; the fourth is a genuine formal gap.**
>
> **That is a materially better position than "four blockers."** The equality cluster is not a
> mathematical wall — it is **a single open governance question (Decision 3) plus two
> context-parameterisations.** Only `Qualify` is irreducible.

## 3. Why D288 is not executed here

| Constraint | Effect |
|---|---|
| *"no governance decision"* | targets 1–3 each **are** a decision |
| *"do not promote merely because a plausible definition can be written"* | I can write plausible definitions for all four. **That is precisely what is forbidden** |
| Step 261 §261.23 | **the corpus itself halted here** — *"final kernel selection must stop while equality remains ambiguous. We therefore obey that gate"* |

**Proceeding would break a gate the research lane set for itself before this programme existed.**

## 4. The one question that unlocks the most

> **Step 254 Decision 3: is governance/authority part of semantic equality — i.e. is `Π` inside `≡`?**

**It is a single yes/no, and it settles:** `≡`'s procedure · whether `≅_λ` is distinct from `≡` ·
whether `D285-5`'s property is substantive or vacuous · and whether `D285-6`'s `=_semantic` projection
is legitimate.

**Consequences, stated so the decision can be made on them:**

| | `Π` INSIDE `≡` | `Π` OUTSIDE `≡` |
|---|---|---|
| `D285-5` property | **VACUOUS** | **SUBSTANTIVE** |
| `≅_λ` vs `≡` | collapse into one | remain distinct |
| `D285-6` projection | **illegitimate as stated** | legitimate |
| provenance-blindness | impossible by construction | must be handled by another mechanism |
| audit strength | maximal | requires `≅_λ` alongside |

**No recommendation is offered.** Both branches are formally consistent; the choice is about what
KnowledgeOS wants equality to *mean*, which is exactly why the corpus left it open.

## 5. Discipline

No new relation invented · no target promoted · `Qualify` not solved by assumption · **no Sanskrit
expansion** (`GK-14 Guṇa`, `GK-15 Mokṣa` remain recorded and undeveloped) · `Sañjaya = Ω` not treated
as canonical · **`=_semantic` still not promoted.**

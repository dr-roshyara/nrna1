---
artifact: 28 — CH 1 · KERNEL FAILURE MODES · and the GĪTĀ TRACK CLOSED (Ch 1–18)
source: 1 newly renamed prompt — Ch 1 as the kernel's pathological state
date: 2026-09-01
verdict: **The track's LAST chapter addresses the one slot the track's OWN kernel proposal listed as open — failure semantics. It shapes that slot and fills none of it. Plus a FIFTH glyph collision (`M_t`), and corroboration of FOUR ratified primitives at once. Final tally: 3 T4, all corroborative. ZERO new primitives across 18 chapters.**
---

# 28 — Chapter 1, Failure Modes, and the Closed Track

## 1. The track is complete

**Ch 1–18, all read by the kernel lane.** The reading order was **5–18 → 4 → 3 → 2 → 1** — backwards
into the opening chapter, which turns out to be the right order: **Ch 1 is the failure state that
motivates everything after it.**

## 2. ⭐⭐ The finding — Ch 1 addresses the slot the track itself declared open

**Artifact `20` recorded the kernel proposal's own list of what must be *"determined, rather than
assumed"*:** primitive operators · types · pre/postconditions · composition rules · identity elements ·
invariants · **failure/uncertainty semantics** · minimality.

$$\boxed{\begin{array}{c}\textbf{Every other Gītā chapter addressed what the kernel should DO.}\\ \textbf{Ch 1 is the only one that addresses how it FAILS.}\end{array}}$$

**And the programme is thin here by measurement:** `REFINED-STEP-287-INVARIANTS` has **7 candidate
invariants, 0 ESTABLISHED** — invariants state what must *hold*; **nothing in the corpus states what
happens when they don't.** `291 §03` found **0 of 22 operations with failure semantics.**

### The four candidate failure modes

| # | Failure | Mechanism | Carrier? |
|---|---|---|---|
| **F1** | **observation collapses into interpretation** | entities are perceived and **semantic relations are attached at observation time** — `Entity_i --father--> Arjuna` | ⚠️ **`W --Ω--> O` exists** (`Sañjaya`, `R5`); the *collapse* has none |
| **F2** | **attachment contaminates discrimination** | bias enters before `Buddhi` operates | 🔴 **no carrier for "bias"** (`26`, `K-Q7`) |
| **F3** | **a Zero event misread as knowledge** | *"I see only bad outcomes"* — absence of a good option read as a **conclusion** | ⚠️ Zero exists; **`Zero ≠ Doubt` (`26`)**; the misreading has none |
| **F4** | **model inadequacy** — the *"wrong model"* problem | see §4 | 🔴 no `M_t` object |

$$\boxed{\textbf{Four failure modes named. Four carriers missing. The slot is SHAPED, not filled.}}$$

⚠️ **This is the fourth `T4-SHAPE` of the track** (with `𝕂=(K_t,Ω_K,ℐ)`, the 5-slot operation identity,
and `τ∘δ∘Z`). **The pattern is now unmistakable: the Gītā lens reliably produces well-formed empty
slots.**

## 3. ⭐ `Model adequacy` — a new candidate, and it completes `Reframe`

$$\boxed{\text{Does } M_t \text{ adequately explain } O_t\ ?} \qquad \text{if not:}\quad M_t \xrightarrow{Buddhi} M_{t+1}$$

⭐ **This supplies the TRIGGER `Reframe` was missing.** `27` established `Reframe(K_t) → K'_t` —
corpus-present, authority-ruled, registry-absent — **but nothing said *when* to reframe.**
**Model inadequacy is that condition.**

| | |
|---|---|
| `27` — **what** | `Reframe` — restructure the interpretive frame; **Sārathi proposes, Knower owns** |
| **`28` — when** | **when `M_t` fails to explain `O_t`** |

$$\boxed{\textbf{Two chapters, read five days apart, supply the operation and its trigger condition.}}$$

⚠️ **And both are missing from the registry.** ⚠️ **And `M_t` — the world model — is not a corpus
object.** `K_t` is the knowledge *state*; a *model that explains observations* is not the same thing.
**`M-Q6`. NOT promoted** — introducing a world model is an architecture change.

## 4. 🔴 A FIFTH glyph collision — `M`

| glyph | here | in the programme |
|---|---|---|
| **`M_t`** | the **world model** | **`𝓜`** — the **KERNEL** (`23`: *"the smallest persistent computational unit that evaluates and transforms knowledge states"*) |

**Collisions on record:** `𝒪` (operations/observations) · `≅`/`≡` (four relations per glyph) · `δ`
(discrimination/transition) · `ℐ` (intervention/invariants) · **`M` (world model/kernel)**.
$$\boxed{\textbf{FIVE glyph collisions, and every one arose within the last two days of this track.}}$$
⚠️ **`G-100`.** **This is now a systemic notation problem, not five incidents** — the Gītā track
generates symbols faster than the registry adjudicates them. ✅ **Consistent with `G-99`'s finding of a
broken intake path.**

## 5. ✅ Corroboration of FOUR ratified primitives at once

> *"The battlefield contains **entities**… but Arjuna immediately attaches **semantic relationships**…
> The same physical entity can participate in radically different semantic contexts."*
$$\boxed{Entity + Relation + Proposition + Context}$$

| `Entity` · `Relation` · `Proposition` | ✅ **three of the ratified eight** |
|---|---|
| `Context` | ⚠️ **`G-15` — untyped** |

**The densest single corroboration in the track** — and, characteristically, **it corroborates and adds
nothing.** ✅ **It also reinforces `25I.6`/`25S.34`:** the same entity under different contexts is not
the same knowledge object.

## 6. THE GĪTĀ TRACK — final tally, all 18 chapters

| Tier | count | new to KnowledgeOS |
|---|---:|---|
| **T4 — KERNEL** | **3** | **0** — `Kṣetrajña → 𝒩 ∉ K` · `Sañjaya → Ω` · `Event ≠ State`; **all corpus-native** |
| **T4-SHAPE** | **4** | well-formed, **all slots empty** |
| T3 | 17 | 0 |
| T2 | 26 | 0 |
| T1 | ≈32 | 0 |
| **T0 — refuted/`RX`** | **20** | — |
| **negative constraints** | **`C1`–`C14`** | ⭐ the track's real yield |

$$\boxed{\begin{array}{c}\textbf{18 chapters} \cdot \approx 60 \textbf{ correspondences} \cdot \textbf{ZERO new primitives} \cdot \textbf{ZERO new canonical relations.}\\ \textbf{The G\={\i}t\={a} CONSTRAINS the kernel; it does not POPULATE it.}\end{array}}$$

$$\boxed{C14:\ \text{Observation} \neq \text{Interpretation} \quad\text{— semantic relations must not be attached at observation time}}$$

### What the track actually produced, in order of value

| 1 | **14 negative constraints** — what the kernel must **not** be |
|---|---|
| 2 | **4 recoveries** of corpus content that had been dropped or unregistered — `Determination` · `I-V` · the ordering gap · **`Reframe`** |
| 3 | **6 independent arrivals** at *"no established order on `K`"* |
| 4 | **4 `T4-SHAPE`s** — well-formed empty slots that pose questions better than they were posed before |
| 5 | **5 glyph collisions** and a **broken registry intake path** — defects found *because* the lens generated candidates faster than the registry could absorb them |

## 7. Register

| ID | Item | Class |
|---|---|---|
| **`F-Q1`** | ⭐ **Four kernel failure modes** (`F1`–`F4`) for a slot the kernel proposal itself listed as open. **Four carriers missing** | **T4-SHAPE** |
| **`M-Q6`** | **Model adequacy** `Does M_t explain O_t?` — ⭐ **supplies `Reframe`'s trigger**; ⚠️ `M_t` is not a corpus object | **T2** |
| **`G-100`** | 🔴 **FIVE glyph collisions**, all arising within two days — **a systemic notation problem**, consistent with `G-99`'s broken intake path | **DOCUMENTARY — systemic** |

## 8. What this does not license
⚠️ **No failure mode is promoted** — four carriers are missing. ⚠️ **`M_t` is not introduced.**
⚠️ **No glyph is renamed** — `G-100` is for the registry owner. ⚠️ **The track's closure is a closure of
READING, not of the theory:** `261.23` remains at 0 of 6, the closure contract at 0 of 13.
🔴 **Steps 288–291 unchanged; all `R6`.**
✅ **And the last chapter kept the discipline:** *"Chapter 1 does **not** give us a new KnowledgeOS
primitive directly."*

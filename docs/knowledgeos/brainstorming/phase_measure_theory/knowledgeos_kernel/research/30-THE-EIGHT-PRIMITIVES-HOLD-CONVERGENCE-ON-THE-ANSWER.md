---
artifact: 30 — THE EIGHT PRIMITIVES HOLD · a convergence on the answer
sources: 2 newly renamed prompts — an 18-chapter formal kernel model · **a reconciliation that keeps `K_t` at the eight ratified primitives**
date: 2026-09-01
verdict: **The Gītā lane independently reaches this programme's tiering result: KEEP THE EIGHT RATIFIED PRIMITIVES; the Gītā is a LENS, not a source of primitives. Both files mark themselves `DERIVED / RESEARCH, not CANONICAL`. One real defect found: `Karma` is mapped to TWO distinct ratified primitives, and the disambiguation sits unused in the same table.**
---

# 30 — The Eight Primitives Hold

## 1. ⭐⭐⭐ THE CONVERGENCE — the lane reaches this programme's answer by itself

> *"I would keep the **canonical kernel state exactly at the eight ratified primitives**, while using
> the Gītā-derived concepts as a **philosophical interpretation/lens, not as additional primitives**."*

$$\boxed{K_t = \{Entity,\ State,\ Event,\ Observation,\ Proposition,\ Relation,\ Policy,\ Action\}}$$

> ⚠️ **NOTATION CORRECTED by `31 §1`.** Written as a **set**, this still reads as *"the state IS
> these eight things"* — which is compositional, and `258.35` requires `K` be defined **extensionally
> through behavioural sufficiency, "not compositionally through an arbitrary tuple."**
> **The eight are the VOCABULARY IN WHICH state is expressed — not its components, not its fields.**
> *(The source itself says "not eight equal fields" and then writes an 8-tuple, which is worse; mine
> was less wrong and still wrong.)*

**Artifacts `18`–`29` tiered ~60 correspondences across 18 chapters and concluded: 3 T4 candidates, all
corpus-native corroborations; ZERO new primitives; *the Gītā constrains the kernel, it does not populate
it*.**

$$\boxed{\begin{array}{c}\textbf{Two methods — systematic tiering, and the lane's own synthesis —}\\ \textbf{reach the same conclusion about the same corpus.}\end{array}}$$

⚠️ **This is corroboration of a NEGATIVE result, which is the only kind available here** — and it is
worth exactly as much as that: it raises confidence that *"0 new primitives"* is not an artifact of my
tiering criteria.

## 2. The lens mapping, audited primitive by primitive

| Ratified primitive | Gītā lens | Audit |
|---|---|---|
| **Entity** | **Kṣetra** — the field/object | ✅ **the disciplined form of Ch 13's mapping.** `19` put Ch 13's `Kṣetra → Field` at **T0** because it added a *new type*; **here it maps ONTO a ratified primitive.** Same correspondence, correct move |
| **State** | changing condition of the field | ✅ |
| **Event** | **Karma / Kriyā** | ⚠️ **see §3** |
| **Observation** | **Indriya → Manas** | ✅ consistent with `W --Ω--> O` (`Sañjaya`, `R5`) |
| **Proposition** | **Jñeya** — that which is to be known | ✅ matches Step 286's existing correction (*"`Jñātā → Knower`, `Jñeya → Proposition`"*) |
| **Relation** | **Sambandha** | 🆕 **not in the GK register** |
| **Policy** | **Dharma** | ✅ upgrades `Dharma` from `R4` *"emphasis only, no new formal object"* to **a lens on a ratified primitive** — a better placement, still not a new object |
| **Action** | **Karma** | ⚠️ **see §3** |

### ✅ And three explicit refusals of primitive status
$$\boxed{Buddhi \not\equiv Primitive} \quad\text{— }Buddhi = \text{a discriminative OPERATOR over } K_t$$
$$\boxed{K\d{s}etraj\tilde{n}a = \text{knower perspective, OUTSIDE } K_t} \quad\text{— i.e. the ratified } \mathcal N \notin K$$
$$\boxed{Gu\d{n}a = \text{mode influencing kernel operation}} \quad\text{— ✅ consistent with } C11:\ \text{state} \neq \text{mode}$$

⭐ **The `Kṣetrajña` line is the ratified `R5` restated from the philosophical side, five days after
`FA-4` recorded it.** ✅ **And `Guṇa`-as-mode takes branch (b) of `25`'s fork — separate from `Σ`, not
derived from it** — ⚠️ *which is the branch that leaves it without a carrier.*

## 3. 🔴 THE DEFECT — `Karma` is mapped to TWO distinct ratified primitives

| **Event** | *"something that happens / change occurs"* | **`Karma` / `Kriyā`** |
|---|---|---|
| **Action** | *"an operation/change **deliberately performed**"* | **`Karma`** |

$$\boxed{\begin{array}{c}\textbf{`Karma` is assigned to BOTH } Event \textbf{ and } Action \textbf{ — two primitives the ratified eight hold DISTINCT.}\end{array}}$$

⭐ **And the disambiguation is sitting unused in the same table:** `Kriyā` appears only in the `Event`
row. **The natural split — `Kriyā` = the happening, `Karma` = the deliberate act — matches the table's
own glosses** (*"change occurs"* vs *"deliberately performed"*) **and is never stated.**

⚠️ **This matters because `Event ≠ State` is one of the three `T4` corroborations** (`23`, Ch 11), and
`Event ≠ Action` is entailed by both being ratified. **A single Sanskrit term spanning two primitives
would collapse a distinction the corpus holds.**
✅ **Same shape as the `Jñāna` correction Step 286 already made** (`GK-Q2`): one Gītā term, two
KnowledgeOS objects, disambiguation required. **`GK-Q4`. Not resolved here — a register act.**

## 4. Two new register terms
**`Sambandha`** (→ `Relation`) and **`Kriyā`** (→ `Event`) — **neither is in the 20-term GK register**
(`07`). ⚠️ **The register grows again without adjudication** — the `G-101` pattern at the vocabulary
level. **`GK-Q5`.**

## 5. Both files mark themselves correctly

> *"the eight-primitives Kernel state is the **current ratified KnowledgeOS anchor**, but **equality
> semantics, the observation set, operation registry, `Qualify`, and the final canonical terminology are
> not all closed** yet. So this model should currently be marked **DERIVED / RESEARCH, not CANONICAL**."*

⭐ **That list is this programme's open register, stated from the other side:** `≡` · `𝒪_K` · `𝒪` ·
`Qualify` (`G1`) · terminology (`G-98`/`G-100`/`G-101`). **The lane and the research lane now agree on
what is open, item for item.**

**And the closing question:**
$$\boxed{\textbf{Can the Kernel be expressed as a small, CLOSED ALGEBRA of typed state transformations?}}$$
⭐ **That is `N-4` plus minimality, asked from the philosophical side** — the same question `291`
reached by graph analysis and `259` by congruence.

## 6. Where this leaves the track

| | |
|---|---:|
| chapters read | **18 of 18** |
| complete syntheses | **2** (one per lane, opposite verdicts) |
| **reconciliations to the ratified eight** | **1** — ⭐ **this one** |
| **T4 kernel candidates** | **3**, all corpus-native |
| **new primitives** | **0** — now **independently confirmed by the lane itself** |
| gaps filled | **0** |

$$\boxed{\begin{array}{c}\textbf{The G\={\i}t\={a} track ends where the tiering said it would:}\\ \textbf{the eight ratified primitives stand, and the G\={\i}t\={a} is a lens over them.}\end{array}}$$

## 7. Register

| ID | Item | Class |
|---|---|---|
| **`GK-Q4`** | 🔴 **`Karma` mapped to BOTH `Event` and `Action`** — two distinct ratified primitives. ⭐ **The disambiguation (`Kriyā` vs `Karma`) sits unused in the same table** | **registry** — same shape as `GK-Q2` |
| **`GK-Q5`** | **`Sambandha`** and **`Kriyā`** are new register terms, unadjudicated | **registry** — `G-101` pattern at vocabulary level |
| **`C-Q1`** | ⭐ The lane independently reaches **"keep the eight; Gītā is a lens"** — corroborating `18`–`29`'s tiering | **DERIVED — corroboration of a negative result** |

## 8. What this does not license
⚠️ **Nothing is promoted** — the reconciliation *confirms* the ratified eight rather than changing them,
which is precisely why it changes nothing. ⚠️ **`GK-Q4`/`GK-Q5` are recorded for the registry owner**
(`REFINED-STEP-286 §3`). ⚠️ **`Guṇa`-as-mode takes the fork branch that leaves it uncarried** (`25`).
🔴 **`261.23` at 0 of 6; closure contract at 0 of 13. Steps 288–291 unchanged; all `R6`.**

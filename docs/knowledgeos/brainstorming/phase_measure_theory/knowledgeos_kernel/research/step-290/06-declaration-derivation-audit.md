# 06 — Declaration vs Derivation Fork (mandate §7)

## Branch A — `≡` is normatively DECLARED

| **Would establish** | **Would NOT establish** |
|---|---|
| a usable `≡` — `δ`'s postconditions become statable | ⚠️ **that the declared `≡` is a CONGRUENCE** — `258.9` must still be *tested*, and a declaration cannot make it true |
| breaks **Z-2** and **Z-4** — the cycles clear | **decidability** — `012 §35`: `≡` is not fully decidable for arbitrary language. **No authority can ratify decidability into existence** |
| unblocks the 8 derivable prerequisites *conditionally* | **`𝒪_K`** — still open, so `≈` stays undefined and `261.23` cond. 1 stays |
| makes `N-1′` moot (the slot is filled directly) | **minimality** — `259` forbids the proof before congruence analysis |
| | **the representation block** — `261.23` conds. 4–5 are untouched |

⚠️ **The sharpest limit:** declaring `≡` and then *discovering* it is not a congruence would mean
`258.9`'s own verdict applies — *"`≡_K` is too coarse"* — i.e. **the declaration would be falsified by
the very test it was meant to unblock.** **Declaration is not self-validating.**

## Branch B — `≡` must be DERIVED

**Missing constructs that prevent it, each named:**

| Missing | Why it blocks |
|---|---|
| **`𝒯`** | `258.9` quantifies `∀T ∈ 𝒯`; no `𝒯`, no congruence test, no selection criterion |
| **`δ`** | congruence is a property *of* `δ`; commit case unspecifiable (`K₁ is K₀`, EXECUTED) |
| **`𝒪_K`** | the only offered candidates for `≡` are defined over `𝒪_K` (`02`) |
| **a minimality proof** | `259`: *"no valid minimality proof before transformation congruence analysis"* |

$$\boxed{\text{Branch B is blocked on } \mathcal T,\ \delta,\ \mathcal O_K \text{ — i.e. on } N\text{-}4.}$$

## Does the corpus close the fork?

$$\boxed{\textbf{NO.}}$$

| `261.21` | offers a candidate and **declines to ratify it** — *"not a completed theorem"* |
|---|---|
| `258.37` | *"we still do not have the final `=_X` or `≡_K`"* |
| `259` | blocks the derivation route on `𝒯` |
| `261.23` | *"selection must stop while equality remains ambiguous"* — **states the block, does not resolve it** |

**No branch is selected.** ⚠️ **And the fork is asymmetric in a way worth recording: Branch A is
*available now* and *not self-validating*; Branch B is *unavailable now* and *would be self-validating*.
That asymmetry is a fact about the fork, not an argument for either side.**

## STATUS
**ESTABLISHED** both branches' consequences; the corpus does not close the fork; Branch B is blocked on
`N-4`; a declaration cannot establish congruence or decidability · **BOUNDED** exactly two branches ·
**NORMATIVE** the choice · **DEFERRED** — no branch selected

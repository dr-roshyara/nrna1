# 04 — Observational Family Audit (mandate §5)

**The 2240-state audit is NOT re-run** (mandate §5). Results carried from Step 289.

| Question | Answer | Evidence |
|---|---|---|
| **1** Does `≈_X` instantiate the corpus's `≈`? | 🔴 **NO** | corpus `≈` is `∀O ∈ 𝒪_K` (`261.5`); `≈_X` is `π_X(Σ_1)=π_X(Σ_2)`. **Different index, different carrier** |
| **2** Is it merely one formalization of *"observational"*? | ✅ **YES — that is precisely its status** | it formalizes *"agree on observable labels"*; `258.11` separates that from behaviour |
| **3** Does any primary artifact specify which `X`? | 🔴 **NO** | swept; no locus names an `X` |
| **4** Is `≈_X` the same *kind* of relation as `≡_K`? | 🔴 **NO** | `≈_X` is `Σ`-level; `≡_K` is `K`-level, and **no `π: K → Σ` exists** (`289 §5`) — `Σ` is a *component* of `K` |
| **5** Do 30 survivors show under-specification or multiplicity? | ⭐ **UNDER-SPECIFICATION** | 30 survivors of one parameter is **one unfilled parameter**, not 30 canonical relations |

$$\boxed{\textbf{bounded candidate family} \neq \textbf{selected canonical relation}}$$

## Degeneracy, carried from Step 289 §6

| | blocks | verdict |
|---|---|---|
| `≈_∅` | **1** | **UNIVERSAL** — axiom-satisfying, information-free; **not a candidate** |
| `≈_{full}` | **2240** | **DISCRETE** — identical to `=` on `Σ`; **not an independent candidate** |
| the other 30 | 2…1120 | non-degenerate |

**30 non-degenerate candidates.** ⚠️ **This replaces all earlier *"32 usable choices"* language** —
locked per the reviewer's item 4.

## ⚠️ And a point of precision about what `≈_X` is FOR

`≈_X` was built in Step 287 to **bound** a candidate space, and it did: from *"an undefined mathematical
universe"* to **30 enumerated options**. That is real work. **What it is not is evidence about the
corpus's `≈`** — and every artifact citing it must carry that scope, because the number 30 is seductive:
it looks like an answer to *"which `≈`?"* when it is an answer to *"how many ways could one read
`≈` at `Σ`-level?"*

$$\boxed{\text{30 is a measure of UNDER-SPECIFICATION, not a menu.}}$$

## STATUS
**ESTABLISHED** `≈_X` is not the corpus's `≈`; no artifact specifies `X`; the 30 measure
under-specification · **BOUNDED** at 30 non-degenerate · **EMPIRICALLY VERIFIED** (289, not re-run) ·
**NORMATIVE** `N-5`, and only *if* the `Σ`-reading is ever adopted

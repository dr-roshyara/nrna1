# 09 — Gap Update (mandate §12 item 12)

## Change control — every modification stated

| | What changed | Why | Evidence | Class |
|---|---|---|---|---|
| **C-1** | *"`≡`/`≈` corpus contradiction"* → **WITHDRAWN** | both definitional loci self-label as candidates | `261.21` (*"candidate … not a completed theorem"*), `258.8`+`258.37` | **CORRECTION** — a false claim |
| **C-2** | **`G-67`** withdrawn; **`N-1′`** created | severity was mis-scored CRITICAL on the false claim | `08` | **CORRECTION** |
| **C-3** | *"N-1 is the earliest legitimate next act"* → **`N-4` is** | N-1 performed; and `N-1′` is *downstream* of `N-4` | `08` ordering | **SUPERSESSION** |
| **C-4** | Step 289's **4 cycles** → **3 necessary + 1 candidate-dependency** | Z-1's edge came from `261.21`'s candidate, mis-modelled as definitional | `05` | **REFINEMENT** — `{≡}` unchanged |
| **C-5** | *"nine registers"* → stands, **plus `261` carries one register in two notations** | `261.1` vs `261.20`/`261.25`, six-for-six | `01` | **NEW FINDING** |
| **C-6** | *"32 usable"* → **30 non-degenerate**, everywhere | `≈_∅` universal, `≈_{full}` discrete | `289 §6`, `04` | **CORRECTION** — locked |
| **C-7** | `(𝕂,merge,∅)` join-semilattice **REFUTED** → **NOT ESTABLISHED** | see below | `060 §60.71` | **CORRECTION — my own overclaim** |

### C-7 in full, because it is a correction to Step 289 itself

Step 289 §4 recorded `(𝕂, merge, ∅)` as a join-semilattice **REFUTED** for `𝕂`, on two grounds:
`060 §60.71` and retraction. **The second ground does not support REFUTED.**

- `060 §60.71` says *"**Not proven** … we cannot yet assert"* → that is **NOT ESTABLISHED**, not refuted.
- **Retraction refutes the *gloss*** — *"adding knowledge only grows the state"* is a claim about **state
  evolution**, and withdrawal shrinks. ✅ **That stays REFUTED.**
- But **`merge` may still be monotone even if `retract` is not.** Retraction is a **different operation**.
  **So retraction does not refute the algebraic claim about `merge`.**

$$\boxed{\begin{array}{ll}(\mathbb K, merge, \emptyset) \text{ as a join-semilattice} & \textbf{NOT ESTABLISHED (}060.71\text{); proven only for } PureClaimSetUnion\\ \text{"adding knowledge only grows the state"} & \textbf{REFUTED} \text{ — a claim about evolution; retraction shrinks}\end{array}}$$

⚠️ **Two claims were conflated into one verdict, and the stronger word was applied to both.** The
reviewer's lock item 5 — *"neither should it be upgraded to 'K is a lattice'"* — cuts in the other
direction too: **neither should it be downgraded to REFUTED when the corpus says NOT PROVEN.**
**The five verification-lane artifacts remain SUPERSEDED** (scope must be restored to
`PureClaimSetUnion`) — that part is unchanged.

## Register movements

### WITHDRAWN
`G-67` — the only gap this programme has ever withdrawn as **not a defect at all**.

### NEW
| ID | Gap | Class |
|---|---|---|
| **G-76** | `261` carries one relation register in **two notations**, and the second reuses the `≡` glyph for `≡_H`/`≡_P` — so `≡` appears to do four jobs. **A plausible cause of `G-67`'s misreading** | ⑦ documentation |
| **G-77** | `≡` has **no independent definition** — two candidates, both borrowing `≈`'s formula, both blocked on `𝒪_K` | ② formal *(a restatement of `G-77`'s true shape, replacing `G-67`)* |

### STANDING, unmoved
`𝒪`/`𝒯` · `𝒪_K` · `δ` commit case · `Qualify`/`G1` · `G-22` no `(Ω,𝓕,P)` · `G-55` `ℛ` 3-field ·
`G-57` `AuthorityAct` · `G-68`…`G-75` (Step 288/289) · `261.23` 0 of 6.

## Net position

| | |
|---|---|
| Gaps **withdrawn** | **1** (`G-67`) — first ever |
| Gaps **added** | 2 (`G-76`, `G-77`) |
| Gaps **closed** | 0 |
| Corrections applied source-first | **7**, of which **3 correct Step 289 or 288** |
| `261.23` conditions resolved | **0 of 6** |
| Governance surface | **one CRITICAL item removed**; root is now unambiguously `N-4` |

> **The most useful thing this step did was delete a finding.** Steps 288–289 turned a documented
> openness into a critical defect; `290` turns it back. **The urgency was manufactured, and removing it
> is as much a result as adding a gap would be.**

## Still not read
`025k`·`025l`·`025m`·`025o`·`025t`·**`025v`**·`025w`·`step-017`·`step-022`·**`step-039`**·`step-073`·`step_203`.
**Unchanged from Step 289 — N-1's scope did not require them, and none bears on the `≡`/`≈` register.**

# 08 — Decision Register (mandate §9)

## The N-1 classification

$$\boxed{\textbf{N-1A — DISTINCT}}$$

**Why not each of the others** — the mandate requires exactly one outcome, so the four rejections are recorded:

| Outcome | Rejected because |
|---|---|
| **`N-1B` — SAME** | five loci say otherwise, in two notations: `246`, `261.1`, `261.20`, `261.25`, `261.19` |
| **`N-1C` — DERIVABLE COLLAPSE** | no **established** corpus rule forces it. `261.21` is the only candidate collapse and **it self-labels as *"not a completed theorem"*** |
| **`N-1D` — GOVERNANCE-CHOICE** | ⚠️ **rejected for the distinction itself** — the corpus decides that. A residual `N-1D`-shaped question survives as **`N-1′`**, which is a *different question* |
| **`N-1E` — CORPUS-CONTRADICTION** | 🔴 **this is what Steps 288–289 claimed. WITHDRAWN.** Both definitional loci are candidates, not assertions; a register with an unfilled slot and an open proposal is not contradictory |

⚠️ **`OPEN` is not used as the answer**, per §9. The classification explains **why** `≡` lacks content:
**not conflict — an unratified candidate blocked on `𝒪_K`.**

## The one record this step creates

| Field | |
|---|---|
| **ID** | **`N-1′`** |
| **Question** | Should `261.21`'s candidate identification `≡_sem := (∀O ∈ 𝒪_K)` be **ratified**? |
| **Alternatives** | (a) ratify — `≡` becomes the observational relation, register effectively 5 · (b) reject — `≡` needs an independent definition · (c) defer until `𝒪_K` closes |
| **Consequences** | (a) `≅_λ`'s role changes (Decision 3 interacts); `≈` and `≡` cease to be independent constraints; **`261.19`'s per-operation plurality is unaffected** — it needs `≅_I`, `≅_H`, `≅_P` regardless · (b) the slot stays empty and `012 §35` bounds what can fill it · (c) status quo |
| **Evidence available** | `261.21` verbatim (candidate + boxed `𝒪_K` caveat) · `261.5` (the same formula for `≈`) · `258.8`/`258.37` · `261.1`/`261.20`/`261.25` (the distinction) |
| **Evidence unavailable** | ⚠️ **a closed `𝒪_K`** — so the candidate's *extension* cannot be computed, and neither alternative can be evaluated on its merits |
| **Why research cannot decide it** | Ratification is a promotion act. And **the candidate is defined over an unclosed set, so research cannot even determine what would be ratified** |
| **Authority** | **ARB** |
| **Priority** | ⚠️ **NOT critical. Downstream of `N-4`** — it cannot be evaluated before `𝒪_K` closes |

## Register corrections

| | Change |
|---|---|
| **`G-67`** *"`≡`/`≈` share one definition while listed as distinct — CRITICAL, adjudicate first"* | 🔴 **WITHDRAWN.** Replaced by `N-1′`: non-critical, downstream of `N-4`, and not evaluable yet |
| **`N-1`** *(Step 288/289 register)* | ✅ **PERFORMED — returns `N-1A`.** Closed as a research question |
| *"`N-1` is the earliest legitimate next act"* | **SUPERSEDED** — the earliest act is **`N-4`** (`𝒪`/`𝒯`), which now gates `N-1′` as well |

## Ordering, after this step

```
N-4  (O / T)  ─── the earliest act; no incoming derivation edge (289 §3)
  ├── N-3  (O_K)
  ├── N-1' (ratify 261.21's candidate)   <-- newly downstream, was "adjudicate first"
  ├── N-2  technical half (does a mandatory op observe provenance?)
  ├── N-11 per-operation bindings
  └── N-16 / N-17
N-8  (define "identity context")  ─── independent
N-13 (canonicalisation policy)    ─── independent
N-12, N-19, N-20, N-21            ─── independent
```

$$\boxed{\textbf{Net effect of Step 290 on the governance surface: one CRITICAL item removed, one non-critical item added, and the surface's single root is now unambiguously } N\text{-}4.}$$

## STATUS
**ESTABLISHED** `N-1A`; the four rejections; `N-1′`'s six fields; the revised ordering ·
**WITHDRAWN** `G-67` · **SUPERSEDED** *"N-1 first"* · **NORMATIVE** `N-1′` and the 20 carried records ·
**BLOCKED** `N-1′` behind `N-4` · **DEFERRED** all decisions

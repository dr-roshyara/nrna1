---
artifact: 34 — THE UNSCHEDULABLE PROGRAMME · and the dimension model
sources: 2 newly renamed prompts — a mathematical-foundation change · a Step-290 operation-semantics mandate
date: 2026-09-01
verdict: **The OPERATIONS programme has now been proposed FOUR times under THREE step numbers (289 ×2, 286, 290) and NEVER SCHEDULED — every number it claims is occupied. `G-99` + `G-101` have a concrete consequence: a whole research programme cannot enter the queue. And the proposed dimension model `𝒟_t ⊆ 𝒟_∞` needs no new membership rule — `258.21` already supplies it.**
---

# 34 — A Programme That Cannot Be Scheduled

## 1. ⭐⭐ THE OPERATIONS PROGRAMME — four proposals, three numbers, zero slots

| # | Artifact | claims step | that slot actually holds |
|---|---|---|---|
| 1 | `…225400_step_289_…operations-mandate…` | **289** | 🔴 dependency graph / bootstrap boundary |
| 2 | `…230300_step_289_…operations-and-transformation-semantics-tightened…` | **289** | 🔴 same |
| 3 | `…024209_step_286_…continue-into-the-operations-programme…` | **286** | 🔴 philosophical-source programme |
| 4 | `…025251_step_290_…operation-semantics…` — header: **`artifact: REFINED-STEP-290 — OPERATION SEMANTICS`** | **290** | 🔴 **N-1 adjudication** |

$$\boxed{\begin{array}{c}\textbf{FOUR proposals · THREE numbers · ZERO free slots · NEVER SCHEDULED.}\end{array}}$$

⚠️ **And #4 lists `depends on: Steps 261, 285, 286, 287, 288, 289`** — it **knows** 289 exists and still
claims 290 without checking. **`G-101` in its purest form.**

### ⭐ This is `G-99` and `G-101` producing a concrete cost

| `G-99` | an **adjudicated operation** (`Reframe`) never entered the registry — **a broken intake path** |
|---|---|
| `G-101` | **two lanes minting step numbers in parallel** with no shared registry |
| **`G-106`** | ⭐ **consequence: an entire research programme cannot be SCHEDULED.** Every number it claims is taken, and no mechanism assigns it a free one |

$$\boxed{\begin{array}{c}\textbf{The operations programme is the single most-blocked item in the estate }(N\text{-}4),\\ \textbf{and it is blocked TWICE — once on ratification, and once on a step number.}\end{array}}$$

⚠️ **The second block is trivially fixable and nobody owns it.** ✅ **`step-291/00`'s recommendation
stands and is now urgent: assign a free slot (292+) or explicitly retire the duplicates.**

## 2. The Step-290 proposal's own content — and it is disciplined

> *"Step 290 must define what it means for a KnowledgeOS operation to be **semantically valid**, without
> prematurely selecting the final operation set, kernel state, equality relation, or implementation."*
> **`authority: Research derives; Governance ratifies normative choices`**

⭐ **Three levels that "MUST remain separate":**
$$\textbf{Level 1 — operation IDENTITY} \quad\cdot\quad \textbf{Level 2 — operation SEMANTICS} \quad\cdot\quad \textbf{Level 3 — operation EXECUTION}$$

**These map onto three of `291 §Audit A`'s nine closure senses:**

| level | `291` measured |
|---|---|
| identity | **0 of 22** |
| semantics (bodies) | **0 of 22** |
| execution | implementation — not begun |

✅ **Same discipline `291` applied when it split *"closed"* into nine senses: refuse to let one word
carry several states.** ⚠️ **And the central question — *"can an operation be represented as
`δ : K × O → K'`, or does the corpus require a richer model?"* — is `259.7`'s finding restated**
(*"not every operation has the form `T : K → K`"*), **which the proposal does not cite.**

## 3. ⭐ The dimension model — and why it needs no new rule

**Proposed:** `K_t ⊆ 𝒦` is *"not rich enough"* — it cannot express dimensions appearing, disappearing,
or the dimensional structure evolving.
$$\boxed{\mathcal D_t \subseteq \mathcal D_\infty} \qquad d_t = |\mathcal D_t| \qquad \boxed{d_{t+1} \neq d_t}$$

**Third convergence on dimensional structure:** `21` (`D_t → D_{t+1}`, purification) · `27` (`Reframe`) ·
this. ✅ **And `29`'s `DiscoverDimension` was a fourth.**

### ⭐⭐ `258.21` already supplies `𝒟_t`'s membership criterion

**The proposal introduces `𝒟_t` and does not say what makes something a dimension. The corpus does:**
> **`258.21`:** *for a property `p`, `Relevant(p)` if there exists a mandatory operation `T` and states
> `K_1, K_2` differing only in `p` such that `T(K_1,c) ≢_K T(K_2,c)`.* — *"This gives us a principled way
> to discover the minimal kernel."*

$$\boxed{\mathcal D_t = \{\,p : Relevant(p)\,\} \qquad\textbf{— the criterion exists, and it quantifies over } \mathcal T.}$$

⚠️ **So the proposed new object needs no new rule — and inherits the same blocker as everything else.**
✅ **And this matters for `258.35`:** a dimension-indexed `K_t` is **compositional**, which `258.35`
forbids — **unless the dimensions are themselves derived from behavioural distinctions.** **`258.21` is
exactly that derivation.** $\boxed{\textbf{The dimension model escapes } 258.35 \textbf{ ONLY via } 258.21, \textbf{ and only once } \mathcal T \textbf{ closes.}}$

### ⚠️ Two defects
🔴 **`𝒟` is a THIRTEENTH glyph collision** — `D_t` already collides three ways (`G-105`: dimensional
structure · `Determination` · the `K_t,O_t,D_t,K_{t+1}` skeleton); `𝒟_t`/`𝒟_∞` add a fourth reading.
🔴 **`dim(𝒦_∞) = ∞` presupposes a notion of dimension — hence an INDEPENDENCE relation between
dimensions — which is undefined.** ⚠️ And `G-22` means there is no measure on `𝒦` either. **`M-Q8`.**

## 4. Tally

| Tier | `33` | **`34`** | new |
|---|---:|---:|---|
| **T4 KERNEL** | 3 | **3** | **0** |
| T4-SHAPE | 5 | **5** | — |
| T3 | 19 | **20** | 0 |
| T2 | 28 | **30** | 0 |
| T0 | 22 | **22** | — |
| constraints | `C1`–`C16` | **`C1`–`C17`** | ⭐ `C17` |

$$\boxed{C17:\ \text{operation IDENTITY} \neq \text{operation SEMANTICS} \neq \text{operation EXECUTION}}$$
$$\boxed{\textbf{Still ZERO new primitives. Still ZERO new canonical relations.}}$$

## 5. Register

| ID | Item | Class |
|---|---|---|
| **`G-106`** | ⭐⭐ **The OPERATIONS programme is UNSCHEDULABLE** — 4 proposals, 3 numbers, 0 free slots. **The estate's most-blocked item is blocked TWICE: on ratification, and on a step number.** ⚠️ **The second block is trivially fixable and unowned** | ① **process — urgent, and cheap to fix** |
| **`D-Q3`** | ⭐⭐ **`𝒟_t`'s membership criterion already exists** — `258.21`'s `Relevant(p)`; **the dimension model escapes `258.35` only via it, and only once `𝒯` closes** | **DERIVED** |
| **`G-105′`** | 🔴 **`𝒟` — thirteenth glyph collision**, fourth reading of `D` | **DOCUMENTARY** |
| **`M-Q8`** | `dim(𝒦_∞) = ∞` presupposes an **independence relation between dimensions** — undefined; and no measure on `𝒦` (`G-22`) | ② formal |

## 6. What this does not license
⚠️ **No step is assigned or renumbered** — `G-106` is for the registry owner; `291 §22`'s discipline
holds. ⚠️ **The dimension model is not adopted** — it is compositional unless routed through `258.21`,
which is blocked on `𝒯`. ⚠️ **The three-level operation separation is not adopted** — it is a
restatement of distinctions `291` already measured as **0 of 22** on two of the three levels.
🔴 **`261.23` at 0 of 6. Steps 288–291 unchanged; all `R6`.**

---
artifact: 35 — A CANDIDATE CARRIER FOR THE MISSINGNESS DEFECT (`A6`/`G-60`)
source: 1 newly renamed prompt (1 byte-identical duplicate) — a controlled mathematical + DDD refinement mandate
date: 2026-09-01
verdict: **The dimension model supplies a STRUCTURAL DISTINCTION for the one defect this programme has EMPIRICALLY CONFIRMED — Step 280's Critical Failure #7, where `not-asked` was indistinguishable from `absent`. `d_i ∉ 𝒟_t` vs `d_i ∈ 𝒟_t ∧ v_i = Unknown` separates exactly those two states. It is the most significant candidate the Gītā track has produced — and its population rule is blocked on `𝒯`.**
---

# 35 — A Candidate Carrier for `A6`/`G-60`

## 1. ⭐⭐⭐ THE FINDING

**The mandate asks for a formal distinction between:**

| | |
|---|---|
| **unknown VALUE** | $d_i \in \mathcal D_t \ \wedge\ v_i = Unknown$ — *the dimension is recognized; its value is not known* |
| **MISSING DIMENSION** | $d_i \notin \mathcal D_t$ — *the dimension is not even recognized* |

> *"Determine whether these represent **fundamentally different epistemic states**."*

### ⭐ They do — and the corpus already PROVED they are indistinguishable in `Σ`

**Step 280, Critical Failure #7 — the programme's one EMPIRICALLY CONFIRMED theory defect:**
```
unknown (no evidence) -> Σ = (Neutral, None)     distinguishable
absent                -> a ∉ 𝒜                    distinguishable
not-asked             -> INDISTINGUISHABLE from absent    *** FAILURE ***
```
**Classified `T` — theory defect. Not implementation, not data.** Registered as **`A6`/`G-60`**, with
Step 281's `M1`–`M7` as a **specified, unexecuted** repair.

$$\boxed{\begin{array}{c}\textbf{CF\#7 says: } \textit{not-asked} \textbf{ and } \textit{absent} \textbf{ collapse in } \Sigma.\\ \textbf{The dimension model separates them STRUCTURALLY:}\\ \textit{not-asked} \equiv d_i \notin \mathcal D_t \qquad \textit{recognized-but-unknown} \equiv d_i \in \mathcal D_t \wedge v_i = Unknown.\end{array}}$$

⭐ **This is the first Gītā-track candidate that addresses a MEASURED failure rather than a conceptual
gap.** Every other candidate across 18 chapters was a distinction, a shape, or a corroboration.

### Why it is a carrier and not another shape
`Σ` is a **flat 5-tuple of labels** — every axis always has a value, so *"this dimension was never
considered"* has nowhere to live. **The dimension model makes `𝒟_t` a varying SET**, and *membership in
a varying set is representable*. **Set membership vs value-at-a-member are structurally different
states.** ✅ **That is a carrier.**

## 2. ⚠️ The audit — how far it actually goes

| Claim | Verdict |
|---|---|
| separates `not-asked` from `recognized-but-unknown` | ✅ **YES — exactly CF#7's failure** |
| supplies all of Step 281's `M1`–`M7` | 🔴 **NO.** It gives `M1` (∉`𝒟_t`) vs `M3` (∈`𝒟_t`, `Unknown`). **`M2` *asked+absent* needs a THIRD value beyond `Unknown`; `M4`–`M6` are support states; `M7` orphan is structural (`deg_ℛ = 0`) and unrelated** |
| has a population rule | 🔴 **BLOCKED** — `34` established `𝒟_t = {p : Relevant(p)}` via **`258.21`**, which quantifies over **`𝒯`** |
| escapes `258.35`'s anti-compositional rule | ⚠️ **only via `258.21`** — i.e. only once `𝒯` closes |
| is executed | 🔴 **no** — Step 281's repair remains **specified and unexecuted**, and this adds a candidate structure, not an execution |

$$\boxed{\begin{array}{c}\textbf{It addresses the SPECIFIC measured failure (CF\#7), not the whole } M1\text{–}M7 \textbf{ repair.}\\ \textbf{The carrier's STRUCTURE exists; its POPULATION RULE is blocked on } \mathcal T.\end{array}}$$

⚠️ **So `A6`/`G-60` is NOT closed.** ✅ **But for the first time it has a candidate carrier with a named
blocker, rather than no carrier at all.** **`A6-Q1`.**

### ⭐ And the statistical reading sharpens it
`17 §3` established that *obscuration* is specifically **MNAR** — missing *because of* the unobserved
value. **The dimension model gives the mechanism taxonomy a home:**
$$d_i \notin \mathcal D_t \ \text{(never modelled — MCAR-like)} \qquad d_i \in \mathcal D_t,\ v_i = Unknown \ \text{(modelled, unobserved — MAR/MNAR)}$$
⚠️ **Distinguishing MAR from MNAR still requires the mechanism, which nothing supplies** — Manski's
bounds remain the honest response (`17 §3`).

## 3. The mandate's other content

| § | Content | Status |
|---|---|---|
| **1** | ⭐ $\boxed{\text{Gītā interpretation} \neq \text{mathematical proof} \neq \text{DDD domain fact}}$ — *"use the Gītā to generate hypotheses, mathematics to test consistency, DDD to determine legitimate domain meaning"* | ✅ **a THREE-way discipline** — sharper than the programme's two-way *correspondence ⇏ derivation* |
| **9** | *"Do not describe Zero merely **poetically**"* — investigate whether Zero can be **modelled** | ✅ sound; ⚠️ `26`'s `Zero ≠ Doubt` is the prior constraint |
| **11** | **separate discrimination from transformation** | ✅ **the `δ`/`τ` split** — `G-95`'s glyph collision, now stated as a requirement |
| **13** | a formal contract for every operator | ✅ = `O-Q5` (`31`) |
| **14** | *"particularly investigate **non-monotonic** knowledge"* | ⭐ **an EIGHTH arrival** at non-monotonicity |
| **16** | revisit the Mokṣa model | ⚠️ `C3`: already **REFUTED as too simplistic** (Ch 6) |

## 4. Tally

| Tier | `34` | **`35`** | new |
|---|---:|---:|---|
| **T4 KERNEL** | 3 | **3** | **0** |
| T4-SHAPE | 5 | **5** | — |
| T3 | 20 | **21** | 0 |
| **T2** | 30 | **32** | 0 |
| T0 | 22 | **22** | — |
| constraints | `C1`–`C17` | **`C1`–`C18`** | ⭐ `C18` |

$$\boxed{C18:\ \text{unknown VALUE } (d_i \in \mathcal D_t,\ v_i = Unknown) \ \neq\ \text{MISSING DIMENSION } (d_i \notin \mathcal D_t)}$$
$$\boxed{\textbf{Still ZERO new primitives. Still ZERO new canonical relations.}}$$

⚠️ **`A6-Q1` is `T2`, not `T4`** — a candidate carrier is not a primitive, and its population rule is
blocked.

## 5. Register

| ID | Item | Class |
|---|---|---|
| **`A6-Q1`** | ⭐⭐⭐ **A candidate CARRIER for `A6`/`G-60`** — `d_i ∉ 𝒟_t` vs `d_i ∈ 𝒟_t ∧ v_i = Unknown` separates exactly the two states **Step 280's CF#7 measured as collapsed**. **The first Gītā-track candidate addressing a MEASURED failure.** ⚠️ Covers `M1` vs `M3` only; population rule blocked on `𝒯` via `258.21`; unexecuted | **T2 — candidate carrier, named blocker** |
| **`P-Q1`** | ⭐ $\text{Gītā interpretation} \neq \text{mathematical proof} \neq \text{DDD domain fact}$ — **a three-way discipline**, sharper than the programme's two-way version | **DERIVED — methodological** |
| **`M-Q9`** | **Eighth arrival at non-monotonicity** | corroboration |

## 6. What this does not license
⚠️ **`A6`/`G-60` is NOT closed** — a candidate carrier with a blocked population rule is not a repair,
and **Step 281's `M1`–`M7` remains specified and unexecuted.** ⚠️ **The dimension model is not adopted**
— `34` established it is compositional except via `258.21`, which needs `𝒯`.
⚠️ **Nothing here is executed.** 🔴 **`261.23` at 0 of 6. Steps 288–291 unchanged; all `R6`.**

# 08 — Invariant Consolidation & Satisfiability Plan (stage V11)

**Established (TV-F-007/008):** 𝒱≠∅ under interpretation (M₀/M₁) · system is a liveness-free schema over ≥12 uninterpreted predicates · fresh IDs mandatory (5×`I_1`, 6 IDs for Unknown≠False, swapped registries).

## Step 1 — Fresh-ID consolidated catalogue (mechanical; first Track-B-adjacent task; no governance needed)
Mint `KI-nnn` for every distinct invariant statement across the ≥10 registries (~130 statements) with columns: KI-ID · canonical statement (verbatim-anchored) · all source IDs+locations · semantic-duplicate group · layer (next step) · modal strength (MUST/SHOULD as found — both recorded where dual, e.g. K5) · status (from A5/TV-F verdicts where touched). Dedup rule: semantic near-duplicates grouped, never merged — the group is the object.

## Step 2 — Layer classification (per 1600 §13's demand to distinguish)
Each KI classified: DEFINITIONAL (holds by construction of the event-log/state machinery — e.g., several 197 conservation laws) · NORMATIVE-EPISTEMIC (Unknown≠False family) · NORMATIVE-GOVERNANCE (K1–K7, authority family) · METHODOLOGICAL (I-8, no-assertion-without-evidence) · ARCHITECTURAL/OPERATIONAL (test-assertion shaped) · EMPIRICAL (calibration-type). The 162 four-constraint-kind scheme is the corpus-native template — reuse it, cite it.

## Step 3 — Per-layer satisfiability at intended semantics
For each layer, once its predicates receive interpretations (from V2/V3 outcomes + the governance devices): exhibit a model or a minimal unsatisfiable subset. Baseline models M₀/M₁ extend per layer. Expected hard spots: governance layer under multi-authority (needs the precedence device); epistemic layer's Conflicted-exit (needs adjudication signature); anything consuming `authoritative/material/governed` (needs definitions).

## Step 4 — Independence/redundancy analysis
Within each layer: which KIs follow from others (candidate: several 048 items follow from the event-log construction + provenance conservation); which are independent axioms. Output: a minimal generating subset per layer — **PROPOSED, never a deletion of the others**.

## Step 5 — The liveness question (governance)
The system's liveness-freeness is a theorem (TV-F-007.3). Whether KnowledgeOS *should* carry liveness obligations (e.g., 048 §48.9's example, K7's capability upgraded to occurrence) is a design decision — formulate the question precisely, decide nothing.

**PASS criteria:** Step 1–2 complete when every statement has exactly one KI row and one layer; Step 3 per-layer PASS = explicit model; FAIL = minimal unsatisfiable subset exhibited; OPEN = named missing interpretation. **Circularity check** (⭕-2 goal/policy recursion; ⭕-5 identity bootstrap) runs alongside Step 3.

# 06 — Measurement Verification Plan (stage V9)

**Audit complete (TV-F-017/AM):** 1 clean metric (CR), 1 conditional (abstention accuracy), rest scale-defective or estimand-less. Remaining work = upgrades and demands, never silent repairs:

| # | Task | PASS criterion |
|---|---|---|
| M-1 | **Estimand-first discipline**: for each SNF metric, either an estimand is *defined independently of the estimator* (population of expression-pairs + target quantity + sampling frame) or the metric is permanently classed DESIGN CHOICE/heuristic | estimand statement or final classification per metric |
| M-2 | **d-scale upgrade attempt**: determine whether any interval-scale justification for a structural distance is *derivable* (e.g., via an explicit generative edit model); if not, freeze d as ordinal and forbid means (thresholds/medians only) | derivation or freeze decision recorded |
| M-3 | **Entropy comparability**: fix the cross-case comparability problem (÷log n) — candidate: report (H, n) pairs, never H^norm alone; PROPOSED REFINEMENT | comparability rule stated + counterexample showing the current defect |
| M-4 | **CR empirical protocol**: gold-set construction rules, inter-rater reliability requirement (κ threshold), sample-size rationale — the one metric worth an execution path | protocol meeting S-2-style design standards |
| M-5 | **`Readiness` definition demand**: the 025a-1 inequality is load-bearing and its symbol UNDEFINED; state exactly what a definition must supply (contract-relative sufficiency ordering); no invention | requirement statement (G-CRITICAL if the inequality is retained in the final theory; else drop the claim) |
| M-6 | **Utility scale justification path**: either a measurement-theoretic elicitation basis for U(a,s) is declared (governance) or EU-optimization is classed policy machinery, not measurement | classification recorded |
| M-7 | **Projection hygiene**: verify every scalar surface in the reconstructed theory is labeled *projection* with its source structure (the corpus's own good practice, generalized) | zero unlabeled scalars |
| M-8 | **Vocabulary quartet**: enforce measurement-theory ≠ measure-theory ≠ metric ≠ score distinctions in the final theory's Part on measurement (measure theory: absent by recorded decision — restate, don't resurrect) | terminology section drafted |

**Standing rule:** no arithmetic without established scale; no estimator without independent estimand; no threshold without declared semantics (experimental vs calibrated vs governance-set).

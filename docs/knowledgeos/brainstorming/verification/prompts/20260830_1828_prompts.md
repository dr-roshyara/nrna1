# NEXT INVESTIGATION DIRECTIVE — FROM NORMATIVE IMPASSE TO THEORY CONSTRUCTION

You have reached an important point in the KnowledgeOS verification.

You have correctly identified that two questions currently appear normative:

1. the canonical epistemic-status vocabulary for `Σ`;
2. the shape of the Knowledge State `K_t`.

**Do NOT ask the user to choose between the alternatives yet.**

The user has explicitly stated that they do not know which alternative is mathematically or architecturally correct. Your responsibility now is to determine how much of these choices can still be derived from the corpus, mathematics, statistics, computability requirements, DDD semantics, and practical KnowledgeOS requirements.

The objective is no longer merely to verify existing documents.

The objective is:

> **Discover, reconstruct, formalize, test, and justify the smallest coherent KnowledgeOS theory that can actually be implemented — while preserving uncertainty wherever the evidence does not determine a unique answer.**

---

## 1. FIRST: FREEZE THE CURRENT STATE

Create a new supervisory artifact before making any substantive change.

Record:

* what the corpus establishes;
* what the verifier establishes;
* what remains merely proposed;
* what is genuinely underdetermined;
* what decisions appear normative;
* what decisions may still be derivable;
* what empirical validation is still missing.

Do not silently resolve anything.

Use explicit epistemic classifications such as:

* CORPUS ESTABLISHES
* FORMALLY PROVEN
* DERIVED
* CONDITIONALLY PROVEN
* EMPIRICALLY SUPPORTED
* SOURCE CLAIM
* DESIGN CHOICE
* VERIFIER RECOMMENDATION
* UNRESOLVED
* CONTRADICTED
* REFUTED
* INCOMPLETE

---

# 2. DO NOT CHOOSE `K` YET — DERIVE ITS REQUIREMENTS FIRST

The current question

> "What is the shape of `K_t`?"

is premature unless we first establish what a knowledge state MUST support.

Construct a formal **Knowledge-State Requirements Register**.

Derive every requirement from the historical corpus.

At minimum investigate:

* assertion representation;
* epistemic status;
* evidence association;
* provenance;
* context;
* temporal validity;
* uncertainty;
* contradiction;
* missingness;
* unknown;
* identity;
* equality;
* membership;
* state transition;
* supersession;
* merge;
* replay;
* validation;
* authority;
* governance;
* lineage;
* determinism;
* composability;
* computability.

For every capability answer:

1. Is it intrinsically a property of a knowledge state?
2. Is it a relation between knowledge states?
3. Is it a property of a transformation?
4. Is it a property of a history?
5. Is it an external assessment?
6. Is it governance metadata?
7. Does putting it inside `K` create circularity?
8. Does removing it from `K` lose information?

This distinction is crucial.

Do not assume that everything KnowledgeOS needs must therefore be a component of `K`.

---

# 3. RECONCILE THE TWO KERNEL LINEAGES

The previous reconciliation identified two historical lineages.

Do not simply select one.

Perform a **semantic reconciliation**.

For every primitive appearing in either lineage:

| Primitive | Historical role | Mathematical role | Required in K? | Required outside K? | Can be derived? | Evidence |
| --------- | --------------- | ----------------- | -------------- | ------------------- | --------------- | -------- |

Specifically investigate:

* artifact
* type
* event
* invariant
* provenance
* transformation
* policy
* state
* context
* evidence
* authority
* identity
* decision
* action
* observation
* assessment
* status
* lineage

Determine whether apparently different primitives are:

A. genuinely different concepts;

B. aliases;

C. different levels of abstraction;

D. projections of one structure;

E. historical alternatives;

F. orthogonal dimensions;

G. incorrectly conflated concepts.

Do not reconcile by chronology.

Do not assume that the later definition supersedes the earlier definition.

Prove or explicitly record the relationship.

---

# 4. INVESTIGATE `K` AS A TYPE, NOT JUST A TUPLE

Before choosing between:

* `K = Set(Assertion)`
* `K = (Assertions, Relations)`
* the six-tuple;
* another structure;

formalize the following:

### 4.1 What is an Assertion?

Define:

`Assertion = ?`

Identify its primitive fields and their types.

In particular investigate:

`q`
`s`
`e`
`c`
`t`
`u`

and determine whether each is:

* primitive;
* derived;
* external reference;
* relation;
* annotation;
* function;
* value object.

### 4.2 What is identity?

Define:

`id(a)`

and determine whether two assertions are equal when:

* proposition is equal;
* proposition + context are equal;
* all fields are equal;
* identity is externally assigned;
* some canonicalization function is required.

### 4.3 What is equality of knowledge states?

Define:

`K₁ = K₂`

without accidentally making equality depend on:

* history;
* provenance;
* insertion order;
* event IDs;
* storage representation.

Test whether the definition satisfies reflexivity, symmetry and transitivity.

### 4.4 What is membership?

Define:

`a ∈ K`

and ensure it is computable.

### 4.5 What is transition?

Define:

`δ(K,o)`

with all required preconditions and postconditions.

Do not merely name operations.

---

# 5. PERFORM THE "MINIMAL ADEQUATE K" TEST

Do not ask:

> Which model looks nicest?

Instead ask:

> What is the smallest mathematical structure that satisfies all non-negotiable requirements?

Construct a sequence:

`K₀ → K₁ → K₂ → ...`

where each added component must have a demonstrated necessity.

For each candidate component:

1. What requirement forces it?
2. Can that requirement be represented externally?
3. Can it be derived?
4. What breaks if it is removed?
5. Can the removed information be reconstructed?
6. Does the component introduce circularity?
7. Does it create unwanted coupling?

Perform actual removal tests wherever computationally possible.

The final result must distinguish:

**MINIMALITY PROVEN**

from

**MINIMALITY PLAUSIBLE**

from

**MINIMALITY NOT ESTABLISHED**.

Do not claim "smallest" merely because a model has fewer fields.

---

# 6. INVESTIGATE THE EPISTEMIC STATUS QUESTION WITHOUT FORCING A USER DECISION

The user does not know which of the four status vocabularies is correct.

Therefore investigate whether the vocabulary is actually a primitive of the theory.

Test these hypotheses:

### H1 — One canonical finite status set

Determine whether the corpus mathematically requires one.

### H2 — Status is multidimensional

Investigate whether:

`EpistemicStatus`

and

`GovernanceStatus`

must be separate dimensions.

Also investigate:

* evidential status;
* validity status;
* lifecycle status;
* governance status.

Determine whether the historical "11 competing vocabularies" are actually different dimensions that were incorrectly collapsed into one field.

### H3 — Status is context-dependent

Determine whether the theory requires a parameterized status algebra.

### H4 — Status is partly derived

For example:

`Supported`, `Refuted`, `Conflicted`, etc.

may potentially be derived from evidence and inference rather than stored as primitive state.

Do not assume this. Test it.

---

# 7. INVESTIGATE THE SEMANTICS OF STATUS TRANSITIONS

For every candidate status vocabulary, construct its transition relation:

`Σ × Event → Σ`

or the appropriate multidimensional equivalent.

Test:

* totality;
* determinism;
* reversibility where claimed;
* monotonicity where claimed;
* contradiction handling;
* supersession;
* invalidation;
* retraction;
* uncertainty;
* unknown.

Then ask:

> Which vocabulary permits the required transition algebra without artificial exceptions?

This is a mathematical criterion, not a preference.

---

# 8. USE DDD / UBIQUITOUS LANGUAGE AS A HARD CONSTRAINT

Now perform a dedicated DDD semantic audit.

Build:

### KnowledgeOS Ubiquitous Language Register

For every important term record:

| Term | Definition | Type | Bounded Context | Synonyms | Forbidden conflations | Source | Status |
| ---- | ---------- | ---- | --------------- | -------- | --------------------- | ------ | ------ |

At minimum audit:

* Knowledge
* Knowledge State
* Assertion
* Proposition
* Evidence
* Observation
* Provenance
* Lineage
* Transformation
* Event
* Assessment
* Validation
* Decision
* Authority
* Governance
* Policy
* Context
* Status
* Uncertainty
* Contradiction
* Supersession
* Resolution
* Replay
* History

Pay special attention to:

`observation ≠ evidence`

`evidence ≠ determination`

`determination ≠ decision`

`decision ≠ authorization`

`authorization ≠ execution`

`validation ≠ transformation`

`knowledge state ≠ transformation history`

`provenance ≠ lineage`

Do not merely document these distinctions.

Determine whether the mathematical model actually respects them.

If the mathematics collapses two DDD concepts into one object, record the contradiction.

---

# 9. BRIDGE THEORY TO IMPLEMENTATION

The theory is intended to become KnowledgeOS.

Therefore every proposed mathematical object must have an implementation interpretation.

Construct:

**Mathematical Object → KnowledgeOS Concept → Data Structure → Operation → Test**

For example:

`Assertion → ? → ? → ? → ?`

`KnowledgeState → ? → ? → ? → ?`

`Transformation → ? → ? → ? → ?`

`Evidence → ? → ? → ? → ?`

`Provenance → ? → ? → ? → ?`

Do not invent implementation details merely to make the mapping work.

If no implementation mapping exists, classify the concept as:

`THEORETICALLY DEFINED / IMPLEMENTATION MISSING`.

---

# 10. COMPUTABILITY MUST BECOME A GATE

For every central definition ask:

> Can KnowledgeOS actually compute this?

At minimum define computability for:

* membership;
* equality;
* assertion identity;
* validity;
* contradiction detection;
* status determination;
* transition;
* replay;
* provenance;
* lineage;
* evidence lookup;
* supersession;
* merge;
* validation.

For every operation provide:

`Input → Preconditions → Algorithm/Rule → Output → Failure cases`

If an operation cannot be computed from the available information, identify exactly what information is missing.

---

# 11. STATISTICAL / UNCERTAINTY AUDIT

Because the corpus contains probabilistic and uncertainty concepts, perform an independent statistical audit.

For every probability or uncertainty quantity determine:

* random variable;
* sample space;
* estimand;
* observation model;
* probability space;
* conditioning information;
* estimator;
* calibration semantics;
* uncertainty interpretation.

Reject any numerical uncertainty claim that lacks a valid statistical basis.

Distinguish:

* epistemic uncertainty;
* aleatory uncertainty;
* missingness;
* non-identifiability;
* contradiction;
* ignorance / unknown.

Do not let a probability number become a substitute for these distinct concepts.

---

# 12. FIND THE TRUE INCOMPLETENESS BOUNDARY

The key deliverable is NOT simply:

> "K is undefined."

Determine exactly what is missing between:

`Historical Corpus`

→ `Conceptual Theory`

→ `Formal Theory`

→ `Computable Theory`

→ `KnowledgeOS Model`

→ `Executable Implementation`

→ `Empirical Validation`

For each transition identify:

* missing definition;
* missing theorem;
* missing assumption;
* missing algorithm;
* missing data;
* missing experiment;
* missing architectural decision.

Create a **Theory Completion Gap Register**.

Each gap must have:

`GAP-ID`

`Question`

`Why it matters`

`Evidence`

`Current status`

`Blocking?`

`Can be derived?`

`Requires experiment?`

`Requires design decision?`

`Requires user decision?`

`Resolution path`

---

# 13. IMPORTANT: DISTINGUISH THEORY FROM DESIGN

Do not attempt to "complete the theory" by silently inserting engineering preferences.

Explicitly divide results into:

### Mathematical necessities

Things that must be true for the formal system to work.

### Empirical facts

Things demonstrated by experiments or the actual KnowledgeOS system.

### Historical facts

Things established by the corpus.

### Architectural consequences

Things that follow from the theory.

### Design choices

Things that are selected because several mathematically valid alternatives exist.

### User/business/governance decisions

Things that cannot be determined mathematically.

This separation is mandatory.

---

# 14. ONLY ASK THE USER WHEN THE DECISION IS GENUINELY IRREDUCIBLE

After completing the investigations above, produce:

## USER DECISIONS REQUIRED

Only include decisions for which:

* the corpus does not determine the answer;
* mathematics does not determine the answer;
* DDD semantics does not determine the answer;
* implementation requirements do not determine the answer;
* empirical testing cannot determine the answer.

For each remaining decision provide:

1. the precise question;
2. why it cannot be derived;
3. the valid alternatives;
4. consequences of each;
5. your expert recommendation;
6. whether choosing the recommendation would change the mathematical theory or merely the architecture.

Do not ask the user to make a choice simply because the corpus contains competing alternatives.

---

# 15. MOST IMPORTANT: DO NOT PREMATURELY DECLARE THE THEORY COMPLETE

The current corpus has substantial mathematical and architectural work, but the central theory is not yet complete merely because Steps 1–240+ exist.

Do not use:

* "verified";
* "complete";
* "canonical";
* "minimal";
* "correct";
* "production-ready"

unless the corresponding proof/evidence exists.

The completion criterion should be:

> A senior mathematician, statistician, DDD architect, and software architect can independently reconstruct the theory from the final specification, understand every symbol, compute every central operation, identify every assumption, reproduce every claimed result, and distinguish proven theory from design choice and unverified hypothesis.

---

# 16. NEXT EXECUTION ORDER

Execute in this order:

**A. Freeze current findings**

↓

**B. Build Knowledge-State Requirements Register**

↓

**C. Reconcile both kernel lineages**

↓

**D. Define Assertion**

↓

**E. Define identity/equality/membership**

↓

**F. Derive minimal adequate K**

↓

**G. Derive status algebra**

↓

**H. Perform DDD/Ubiquitous Language reconciliation**

↓

**I. Formalize transformations and assessments**

↓

**J. Establish computability**

↓

**K. Statistical/uncertainty audit**

↓

**L. Map theory to KnowledgeOS implementation**

↓

**M. Build Theory Completion Gap Register**

↓

**N. Identify genuinely irreducible user decisions**

↓

**O. STOP FOR SUPERVISION**

Do NOT proceed to final theory publication after O without supervision.

---

# 17. CRITICAL METHODOLOGICAL RULE

The purpose of this phase is not to make the existing corpus look coherent.

The purpose is to discover whether a coherent KnowledgeOS theory actually exists, and if it does not yet exist, to determine exactly what must be constructed.

Therefore:

> **Preserve what is true. Reject what is false. Prove what is provable. Test what is testable. Qualify what depends on assumptions. Separate mathematics from architecture. Separate architecture from governance. Expose every missing piece. Never repair silently.**

Begin with the Knowledge-State Requirements Register and do not choose `K` or `Σ` until that analysis is complete.

## NEXT MANDATE — POLICY RECONSTRUCTION AND FOUNDATIONAL CLOSURE

You are continuing the adversarial reconstruction of the KnowledgeOS theory.

The previous mandate has now reached a critical transition point.

The latest results establish:

* `K = (𝒜, ℛ)` as the canonical knowledge-state representation/model currently derived.
* `P = (E,D,V)` as the canonical proposition structure, subject to the recorded measurement correction.
* `T` has been fully instantiated and executed.
* State/history sufficiency has been tested.
* `Assurance` has been split because it was not a well-defined primitive.
* The theory has been executed end-to-end against concrete inputs.
* The running EKP has been used as an independent third evidence stream.
* The theory → EKP conformance matrix has identified both implemented and missing capabilities.
* Canonical Ubiquitous Language has been reconstructed after the mathematical stabilization.
* The current gate is **FORMALLY INCOMPLETE**.
* The remaining foundational blocker is `Policy`.
* `Assessment` is well-typed but cannot yet be computed canonically because `Policy` is undefined.
* The shape of `Σ` therefore remains unresolved.
* The current result must NOT be presented as a completed KnowledgeOS theory.

The objective of this mandate is therefore:

> **RECONSTRUCT `POLICY` FROM THE CORPUS, MATHEMATICS, AND RUNNING EKP BEFORE ASKING THE USER TO MAKE ANY NORMATIVE CHOICE.**

Do not invent a Policy merely because the theory needs one.

Do not choose a Policy vocabulary because it appears convenient.

Do not promote an implementation detail into theory without evidence.

Do not treat a later document as automatically correct merely because it is newer.

Do not treat verifier output consumed by later corpus steps as independent corroboration.

Maintain the existing evidence classes and feedback-loop discipline.

---

# 1. Exhaustive Policy archaeology

Search the entire KnowledgeOS corpus, including the 1–240+ step history and all verification artifacts, for every explicit or implicit occurrence of:

* Policy
* policy
* rule
* governance rule
* constraint
* admissibility
* validity criterion
* acceptance criterion
* decision criterion
* evaluation criterion
* threshold
* profile
* strict/default policy
* governance status
* epistemic status
* validation rule
* assessment rule
* assurance rule
* schema-as-policy
* configuration-as-policy
* invariant
* normative condition

Construct a complete Policy lineage.

For every occurrence record:

```text
source step
source document
term used
definition
inputs
outputs
purpose
whether normative or descriptive
whether executable
whether later superseded
whether contradicted
whether incorporated into current theory
```

Do not merely collect mentions.

Determine whether these fragments already define the same mathematical object under different names.

---

# 2. Reconstruct the dependency chain

Explicitly reconstruct:

```text
K
 ↓
Assertion / Proposition
 ↓
Evidence / Context / Time / Status
 ↓
Transformation T
 ↓
Policy ?
 ↓
Assessment ?
 ↓
Σ ?
 ↓
Validation / Governance decisions
```

For each arrow answer:

1. What is the source object?
2. What is the target object?
3. Is the relation mathematically defined?
4. Is it deterministic?
5. Is it partial or total?
6. What parameters are required?
7. What information is external?
8. Can the operation actually be computed?

Produce a dependency graph.

The graph must contain no hidden dependencies.

---

# 3. Determine what Policy is NOT

This is mandatory.

Test whether Policy is:

* part of `K`,
* part of `P`,
* part of `T`,
* part of evidence,
* part of authority,
* part of governance,
* part of history,
* part of `Σ`,
* merely configuration,
* merely implementation metadata,
* or an independent normative object.

Use counterexamples where possible.

In particular test:

```text
Policy ≠ Knowledge State
Policy ≠ Evidence
Policy ≠ Assertion Status
Policy ≠ Validation Result
Policy ≠ Governance Status
Policy ≠ Assessment
Policy ≠ Transformation History
```

Do not assert these distinctions merely conceptually.

Find corpus evidence and/or construct formal counterexamples.

---

# 4. Determine the mathematical type of Policy

Attempt to derive the smallest mathematically adequate signature.

Possible forms must be tested rather than selected by preference.

For example:

```text
Policy : K × Context → Criterion
```

or

```text
Policy : Observation → Decision
```

or

```text
Policy : Claim × Evidence × Context → Assessment
```

or

```text
Policy = set of predicates / constraints
```

or another structure if the evidence requires it.

For every candidate determine:

* domain,
* codomain,
* totality/partiality,
* determinism,
* composability,
* equality,
* identity,
* versioning,
* applicability,
* conflict handling,
* provenance,
* execution semantics.

Then eliminate inadequate candidates.

Do not choose a candidate merely because it makes the algebra convenient.

---

# 5. Perform the policy-dependence experiment properly

Repeat and strengthen the existing policy experiment.

Use at least two genuinely different policies:

```text
Policy_A
Policy_B
```

and the same knowledge state:

```text
K
```

Then demonstrate:

```text
Assessment(K, Policy_A) ≠ Assessment(K, Policy_B)
```

if that is actually the case.

Also test the converse:

```text
Assessment(K1, Policy) = Assessment(K2, Policy)
```

for distinct states where appropriate.

Record actual executable output.

No prose-only PASS.

No pseudo-execution.

Keep failed experiments and corrected experiments.

---

# 6. Test Policy identity and equality

A foundational theory cannot leave Policy equality implicit.

Determine:

```text
P1 = P2
```

when?

Is equality:

* structural?
* semantic?
* extensional?
* identifier-based?
* version-based?

Construct concrete examples.

Also determine whether:

```text
Policy_A ≠ Policy_B
```

can nevertheless produce the same assessment for a particular input.

This distinction is important:

```text
policy equality
≠
equal output
```

---

# 7. Test Policy composition

Determine whether policies can be composed.

For example:

```text
P1 ∘ P2
```

or:

```text
P = P1 ∧ P2
P = P1 ∨ P2
```

If composition is meaningful, define it.

If composition is not meaningful, prove why.

Do not introduce algebraic operations merely because they look elegant.

---

# 8. Investigate the existing EKP as a candidate Policy source

The previous audit found an important result:

> the EKP schema behaves partly as policy.

Now investigate this rigorously.

Inspect:

* `knowledge-schema.yaml`
* linter rules
* validation logic
* invariants
* admissibility rules
* relation constraints
* supersession constraints
* existing executable checks
* any configuration that changes assessment behaviour.

Determine which of these are:

```text
descriptive schema
structural constraint
normative policy
validation mechanism
implementation invariant
governance rule
```

Do NOT conclude:

> "schema = Policy"

unless the evidence supports the identity.

Instead determine whether the implementation contains:

```text
Policy representation
Policy interpretation
Policy enforcement
Policy evaluation
```

as separate concerns.

---

# 9. Reconcile Policy with governance

This is especially important for DDD architecture.

Determine the relationship:

```text
Policy
Governance
Authority
Validation
Assessment
```

Do not collapse them.

Ask:

```text
Who defines Policy?
Who owns Policy?
Who applies Policy?
Who evaluates Policy?
Who may change Policy?
What authorizes a Policy change?
```

These are partly normative questions.

First search the corpus and implementation exhaustively.

Only escalate to the user if no answer can be derived.

---

# 10. Reconstruct Σ only AFTER Policy

Do not decide the canonical `Σ` vocabulary yet.

Once Policy has been formally reconstructed, determine:

```text
Σ = ?
```

Then test whether a single epistemic-status value is sufficient.

Specifically test the distinction already identified in the corpus:

```text
EpistemicStrength ≠ GovernanceStatus
```

Determine whether these are:

* one dimension,
* two orthogonal dimensions,
* derived values,
* or context-specific assessments.

Do not choose a vocabulary simply because it has fewer values.

The canonical vocabulary must follow from the ontology and operations.

---

# 11. Re-run the complete dependency audit

After reconstructing Policy and Σ, rerun the dependency graph from first principles.

The final graph should have no undefined foundational object.

Check:

```text
K
P
E
D
V
R
T
Policy
Assessment
Σ
Validation
Governance
History
Lineage
```

For every object determine:

```text
definition
type
inputs
outputs
dependencies
equality
computability
evidence
implementation correspondence
```

Classify every result:

```text
CORPUS ESTABLISHES
FORMALLY DERIVED
EMPIRICALLY VERIFIED
IMPLEMENTED
PROPOSED
UNRESOLVED
REFUTED
```

Never collapse these classes.

---

# 12. Search for the NEXT hidden blocker

Do not stop after solving Policy.

Once Policy is reconstructed, ask:

> What is the next object required for the theory to become fully executable and internally closed?

Run a dependency-driven search.

Do not proceed merely in numerical step order.

Follow the mathematical dependency graph.

The objective is to discover the first genuinely foundational unresolved object.

---

# 13. Perform a canonical-theory falsification pass

Before declaring the theory complete, actively try to break it.

Attempt to find:

* circular definitions,
* ambiguous notation,
* overloaded terms,
* undefined domains/codomains,
* non-computable operations,
* hidden external parameters,
* contradictory status semantics,
* non-deterministic transformations,
* history-sensitive equality,
* policy-sensitive identity errors,
* impossible states,
* unhandled contradictions,
* unrepresented evidence,
* unrepresented authority,
* unrepresented governance,
* implementation/theory mismatches.

For every successful attack, create a recorded counterexample.

For every failed attack, record why it failed.

---

# 14. End-to-end practical test

After the mathematical closure candidate exists, execute the complete pipeline:

```text
input observation
→ proposition
→ evidence/context
→ knowledge state K
→ transformation T
→ policy
→ assessment
→ epistemic status Σ
→ validation/governance outcome
```

Use real EKP data wherever possible.

Every stage must have:

```text
concrete input
concrete output
executable operation
recorded result
```

No placeholders.

No "conceptually possible".

No prose PASS.

---

# 15. Do NOT ask the user yet unless necessary

You are explicitly authorized to continue research and reconstruction without asking me for a normative choice whenever the answer can reasonably be derived from:

* the corpus,
* the mathematical structure,
* the implementation,
* experiments,
* or existing governance definitions.

Only stop and ask the user if, after exhausting these sources, the remaining decision is genuinely normative and cannot be derived.

If such a decision remains, present:

```text
Question
Why mathematics cannot decide it
What the corpus says
What the implementation says
Available alternatives
Consequences of each
Your recommendation, clearly marked as recommendation
```

Do not ask a bare multiple-choice question.

---

# 16. Required artifacts

Produce:

1. `POLICY-LINEAGE-AND-ARCHAEOLOGY.md`
2. `POLICY-TYPE-RECONSTRUCTION.md`
3. `POLICY-COUNTEREXAMPLES.md`
4. `POLICY-EXECUTION-EXPERIMENT.md`
5. `POLICY-EQUALITY-AND-COMPOSITION.md`
6. `POLICY-EKP-CONFORMANCE.md`
7. `SIGMA-RECONSTRUCTION-AFTER-POLICY.md`
8. `FOUNDATIONAL-DEPENDENCY-CLOSURE.md`
9. `NEXT-FOUNDATIONAL-GAP-AUDIT.md`
10. `END-TO-END-CANONICAL-THEORY-TEST.md`

Do not create a final "Theory Complete" artifact unless the dependency audit actually demonstrates closure.

---

# 17. Final stopping condition

Stop only after answering:

### Mathematical closure

```text
What is K?
What is P?
What is T?
What is Policy?
What is Assessment?
What is Σ?
What is Validation?
What is Governance?
```

### Computability

```text
Can every foundational operation actually be computed?
```

### Empirical grounding

```text
Has the theory been executed against real inputs?
```

### Engineering correspondence

```text
What does EKP implement?
What does EKP not implement?
Where does implementation exceed the theory?
Where does theory constrain implementation?
```

### Semantic closure

```text
Does every canonical term have exactly one meaning?
```

### Falsifiability

```text
What observation would prove the reconstructed theory wrong?
```

### Remaining gap

```text
What is the smallest remaining unresolved foundational question?
```

The objective is NOT to make the theory look complete.

The objective is to determine whether KnowledgeOS has a mathematically coherent, computable, empirically testable and DDD-consistent theory — and, if not, to identify precisely what prevents that.

Continue from the current state. Do not restart the entire investigation.

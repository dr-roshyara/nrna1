# NEXT VERIFICATION MANDATE — IDENTITY → ROUND-TRIP → KERNEL RECONSTRUCTION

You have just completed `BLOCKER-ANALYSIS-AND-EXECUTABLE-KERNEL.md`.

Do NOT proceed as if the proposed repair

`Item = (id, q, s, e, c, t, u, integrity)`

is now the theory.

It is only a verifier proposal.

Your task now is to determine, adversarially and from evidence, whether this proposed repair is actually justified by the complete corpus and by the existing KnowledgeOS implementation.

The goal is NOT to make the theory look complete.

The goal is to discover the smallest remaining incompleteness and determine whether the theory can be made formally coherent, computable, empirically anchored, and expressible in stable DDD/Ubiquitous Language.

---

## 1. FIRST: FREEZE THE CURRENT STATE

Before changing or proposing anything, record:

1. What the corpus establishes.
2. What the verifier has formally derived.
3. What has actually been executed.
4. What has been observed in real KnowledgeOS code.
5. What remains only a proposal.
6. What remains explicitly underdetermined.

Maintain the evidence classes already used.

Do NOT convert:

* verifier reasoning → empirical evidence
* implementation evidence → theory
* source claim → established fact
* proposal → corpus conclusion

---

# 2. INVESTIGATE BLOCKER 1 — ITEM IDENTITY

The previous audit identifies `decisionId` as the likely missing primitive.

Do not simply add `id`.

Determine:

### A. Historical derivation

Search the entire theory corpus for every treatment of:

* identity
* identifier
* decisionId
* item identity
* entity identity
* proposition identity
* knowledge identity
* object identity
* equality
* deduplication
* supersession
* lineage
* replay
* merge

Determine:

> Where did the need for identity first arise?

Trace the evolution from the earliest occurrence to the current theory.

Produce an explicit chain:

`first problem → proposed concept → refinement → later formulation → current implementation`

If the concept changes meaning over time, preserve those changes.

Do NOT assume the latest formulation is correct merely because it is newer.

---

# 3. DETERMINE WHAT EXACTLY IS IDENTIFIED

This is the critical mathematical question.

Do NOT assume that the identifier belongs to an Item.

Test at least these possibilities:

A. identity of a proposition
B. identity of a knowledge item
C. identity of an evidence assertion
D. identity of a state element
E. identity of a decision
F. identity of a lineage node
G. identity of an observation
H. identity of an occurrence/version

For each, determine:

* what entity receives the identity;
* whether identity is persistent;
* whether identity survives supersession;
* whether identity survives re-ingestion;
* whether identity survives serialization;
* whether identity is semantic or merely technical;
* whether two independently created objects can legitimately have different IDs while representing the same knowledge.

This distinction is essential.

---

# 4. EQUALITY MUST BE RE-DERIVED

The previous audit discovered three equality relations:

`eq_struct`
`eq_sem`
`eq_obs`

Now determine whether the addition of identity changes their definitions.

Formally define and test:

1. identity equality
2. structural equality
3. semantic equality
4. observational equivalence

Do not collapse these.

Construct explicit counterexamples showing cases where:

* IDs equal but content differs;
* IDs differ but semantic content is equal;
* structure is equal but observation differs;
* observation is equal but underlying states differ.

Determine which equality each operation actually requires:

* Add
* Update
* Supersede
* Merge
* Replay
* Deduplicate
* Validate
* Query
* Compare

Create an operation → required equivalence relation matrix.

---

# 5. INVESTIGATE INTEGRITY

The previous audit found:

`integrityHash`

in real code but not in the proposed mathematical item.

Do NOT automatically add `integrity` to K.

Determine what integrity actually is.

Ask:

* Is integrity a property of an Item?
* a property of a serialized representation?
* a property of a state?
* a property of a lineage edge?
* a cryptographic commitment?
* a property of provenance?
* a verification result?

Trace `integrityHash` historically through the corpus and implementation.

Determine whether integrity belongs:

`inside K`
`inside T`
`inside E`
`inside A`
`inside lineage`
or as a derived function

For example, investigate whether:

`h = Hash(Serialize(K))`

is preferable to storing `h` as a primitive component.

Do not choose until the derivation and implementation evidence justify it.

---

# 6. ROUND-TRIP EXPERIMENT — REQUIRED

Now perform the experiment proposed in the previous audit.

Find one real:

`GovernanceLineageNode`

and its corresponding edge/related objects.

Read the actual implementation.

Construct the smallest mathematically corresponding representation.

Then perform:

`implementation → theory representation → implementation`

and, separately:

`theory representation → implementation representation`

Compare field by field.

The result must explicitly classify every field:

* exact correspondence
* derived correspondence
* lossy correspondence
* theory-only
* implementation-only
* semantic mismatch

Do not merely say "compatible."

Produce an actual executable assertion.

If round-trip fails, identify the exact first divergent field.

---

# 7. DO NOT STOP AT ONE EXAMPLE

After the first round-trip, determine whether the result is accidental.

Select at least three structurally different real artifacts if available:

1. a simple lineage node;
2. a node involved in a transition/supersession;
3. a node carrying integrity/provenance information.

Run the same mapping.

Record whether the proposed kernel survives all three.

---

# 8. REVISIT THE STATUS VOCABULARY

The previous audit correctly identified eleven competing epistemic-status vocabularies.

Do NOT choose one yet.

Instead:

1. enumerate all vocabularies;
2. identify their semantic dimensions;
3. determine which distinctions are genuinely different;
4. determine which are synonyms;
5. identify whether "status" is actually one dimension or several;
6. construct a crosswalk;
7. identify the minimum semantic distinctions required by the executable kernel.

Then determine:

> Is `s` actually one field, or is the theory conflating epistemic status, truth status, validation state, and governance state?

This may be a deeper blocker than vocabulary naming.

---

# 9. REVISIT THE COMPLETE ITEM TYPE

Only after Sections 2–8 should you propose a revised Item.

Potentially investigate forms such as:

`Item = (identity, proposition, epistemic-state, evidence-reference, context, temporal-validity, uncertainty, integrity)`

BUT:

Do not assume this is correct.

Derive the minimal structure.

For every component answer:

1. Why does it exist?
2. What problem requires it?
3. Where did the corpus derive it?
4. Where is it implemented?
5. Can it be computed?
6. Can it be serialized?
7. Can it be compared?
8. Can it participate in transitions?
9. Is it primitive or derived?
10. Can it be removed without breaking a required capability?

---

# 10. RUN A SECOND MINIMALITY TEST

For every proposed component `x` in K:

Calculate/test:

`K_without_x`

Then determine which formally defined capabilities fail.

Produce:

| Component | Remove it | Broken capability | Counterexample | Necessary? |
| --------- | --------- | ----------------- | -------------- | ---------- |

Do not call a component necessary merely because it is useful.

It must be necessary for a formally required capability.

---

# 11. REVISIT THE KERNEL BOUNDARY

The earlier audit identified a major conceptual distinction:

Some things are properties of knowledge states.

Others are properties of transformations or histories.

Now formally classify:

* K
* C
* T
* E
* A
* Policy
* Validation/Assessment
* History
* Governance
* Integrity
* Replay

into:

### State

### Transformation

### Evidence

### Actor/Authority

### Policy

### Assessment

### History

### Derived relation

Then test whether the kernel tuple

`𝒦 = (K,C,T,E,A)`

is still the correct decomposition.

If not, derive the smallest corrected decomposition.

Do not preserve the old tuple for historical reasons.

---

# 12. UBIQUITOUS LANGUAGE AUDIT

Perform a strict DDD semantic audit.

Create a glossary containing at minimum:

* Knowledge
* Knowledge State
* Item
* Proposition
* Evidence
* Provenance
* Lineage
* Identity
* Version
* Supersession
* Transformation
* Transition
* Authority
* Policy
* Governance
* Validation
* Assessment
* Integrity
* Uncertainty
* Context
* Observation
* Equality
* Equivalence

For every term identify:

* current definitions in the corpus;
* competing definitions;
* implementation meaning;
* mathematical meaning;
* whether the term is overloaded;
* whether it should remain in the Ubiquitous Language.

Explicitly flag:

`TERM COLLISION`
`SEMANTIC DRIFT`
`UNDEFINED`
`OVERLOADED`
`STABLE`

Do not solve terminology conflicts by silently selecting the newest definition.

---

# 13. RETURN TO THE 200+ STEP EVOLUTION

This work must remain connected to the historical derivation.

For every major correction discovered now, identify:

`original step → problem raised → attempted solution → later refinement → current evidence → current verdict`

Especially trace:

* identity
* equality
* integrity
* epistemic status
* uncertainty
* provenance
* lineage
* transition
* validation
* governance
* replay

The purpose is to determine whether the late kernel is actually the culmination of the theory's evolution or whether earlier insights were accidentally dropped.

---

# 14. CRITICAL QUESTION: WHAT IS ACTUALLY MISSING?

At the end, do NOT simply produce another candidate model.

Rank remaining blockers.

Use:

### BLOCKER

Required for formal coherence or computability and currently unresolved.

### GAP

Important but not blocking the central mathematical model.

### DESIGN CHOICE

Multiple mathematically valid alternatives remain.

### DOCUMENTATION

Concept exists but is scattered or poorly documented.

### IMPLEMENTATION GAP

Theory is adequate but implementation does not yet realize it.

### EMPIRICAL GAP

Theory is defined but insufficiently tested against real KnowledgeOS.

---

# 15. FINAL DECISION MATRIX

Answer these independently:

| Question                                     | YES / NO / PARTIAL / UNDERDETERMINED | Evidence |
| -------------------------------------------- | ------------------------------------ | -------- |
| Is Knowledge formally defined?               |                                      |          |
| Is K formally defined?                       |                                      |          |
| Is identity formally defined?                |                                      |          |
| Is equality formally defined?                |                                      |          |
| Is semantic equivalence defined?             |                                      |          |
| Is uncertainty formally defined?             |                                      |          |
| Is evidence formally defined?                |                                      |          |
| Is provenance formally defined?              |                                      |          |
| Is lineage formally defined?                 |                                      |          |
| Is transition formally defined?              |                                      |          |
| Is governance formally defined?              |                                      |          |
| Is validation formally defined?              |                                      |          |
| Is integrity formally defined?               |                                      |          |
| Is the model computable?                     |                                      |          |
| Is it executable?                            |                                      |          |
| Has it been tested against real KnowledgeOS? |                                      |          |
| Does it round-trip real artifacts?           |                                      |          |
| Is the Ubiquitous Language stable?           |                                      |          |
| Is minimality proven?                        |                                      |          |
| Is the theory complete?                      |                                      |          |

---

# 16. STOP CONDITION

STOP if any of the following occurs:

* identity remains ambiguous;
* equality cannot be formally defined;
* the proposed Item cannot round-trip a real artifact;
* status semantics remain materially contradictory;
* integrity has unclear ontological placement;
* the revised kernel introduces a new unsupported primitive;
* a real artifact exposes another missing primitive.

Do NOT patch around these conditions.

Record the blocker and explain exactly what evidence or derivation is needed next.

---

# 17. MOST IMPORTANT RULE

Do not try to finish the theory.

Try to BREAK it.

The desired outcome is not:

> "We now have a complete theory."

The desired outcome is:

> "We now know exactly which formal objects are established, which are derived, which are empirically supported, which remain underdetermined, and what smallest missing piece prevents completion."

Only after that should the theory be normalized into a final mathematical specification.

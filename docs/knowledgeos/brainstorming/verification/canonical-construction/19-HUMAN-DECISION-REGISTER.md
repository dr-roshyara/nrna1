---
artifact: 19-HUMAN-DECISION-REGISTER
date: 2026-08-30
status: **6 DECISIONS — 1 new and urgent (D-0) · 3 carried and NARROWED · 2 new**
rule: nothing here is decided; each entry states why the corpus, mathematics and implementation cannot decide it
---

# 19 · Human Decision Register

---

## D-0 · ⚠️ **URGENT — was `𝒪_core` actually decided?** *(new)*

**Question.** Step 273 works from *"the verified semantic operation universe `𝒪_core`"* and cites
*"the attached HPA response"* accepting the dependency order and classifying History/Lineage/Policy/
Authority as external. **Did you make that decision?**

**Why it cannot be resolved from the record** `EXECUTED`:
- `𝒪_core` occurs **5 times, all inside that single research file**. No declaring artifact exists.
- The *"attached HPA response"* is **not in the repository**.
- `governance-notes.md` runs to **GN-73** and contains **zero** entries for `𝒪_core`, the operation
  universe, or Steps 271/272/273.

> **A decision reported in a Stratum-1 research narrative with no authority record is not an
> authority act** — the exact `humanActRef`-without-binding pattern this estate's discipline forbids.

**Options.** **(a)** You did decide → it needs recording as an authority act, and D-1 closes.
**(b)** You did not → Step 273 is building on an unauthorised premise and should be flagged.
**(c)** It was a working assumption → say so, and it stays `HYPOTHESIS`.

**Consequence.** Everything in Step 273 downstream of `𝒪_core` inherits its status.
**Recommendation:** answer this first; it is cheap and it gates the rest.

---

## D-1 · What is the mandatory operation universe `𝒪`? *(carried — and now NARROWED)*

**What is no longer in question** `DERIVED`, `EXECUTED`:
- a **14-element lower bound is FORCED** by the corpus's own non-collapse laws
  (`Add, Assess, Authorize, Derive, Determine, Promote, Qualify, Reject, Remove, Replay, Revise,
  Supersede, Validate, Withdraw`);
- the **upper bound is 18**;
- the **five-class partition** and the congruence rule *"only state-transforming operations enter the
  primary congruence test"* are corpus-stated and independently re-derivable.

**What remains open — and it is only this:** are **`Transform`, `Merge`, `Split`, `Reintroduce`**
*mandatory* or *optional*? They are named in §256.2/§259.7 and forced by **no** non-collapse law.

**Why it cannot be derived.** *"Mandatory"* is a **normative predicate**. No corpus of descriptions
entails an obligation. The corpus states the blockage itself (§259.18).

**Options.** **(a)** all four mandatory — largest `𝒪`, hardest congruence proof.
**(b)** none mandatory — `𝒪` = the 14 forced; smallest provable core; but `Transform` is the corpus's
most-used operation (8 of 9 kernel phases), so this reads oddly.
**(c)** `Transform` mandatory, the rest optional — matches usage evidence.
**(d)** declare `𝒪` **open-ended with a mandatory core** — congruence proved over the core only.

**What this decision does NOT block** `EXECUTED`: **`K` is invariant across all 16 subsets.** It
blocks the **congruence proof** and **minimality relative to `𝒯`**, and nothing else.

**Recommendation:** **(c) or (d).** Both keep the 14-element core provable now.
**Also worth your attention:** `Qualify`, `Determine` and `Derive` are **forced but absent from the
corpus's own registry** — whichever option you take, the registry needs them added.

---

## D-2 · Where does authority enter? Is it exogenous? *(carried)*

**Question.** Is authority **exogenous** (recorded, never computed) with a two-tier constitutional
core — or is there an in-system authority object?

**Evidence available.** `IMPLEMENTATION` — **132/132 grants across 22 work items carry
`humanActRef`**; **zero typed authority acts exist inside the system**. `CORPUS` — *"The mechanism
records authority; it does not grant authority"*; `Evidence ≠ Authority` (45×); `Capability ≠
Authority` (16×).

**Why it cannot be derived.** The implementation shows a *practice*, not an *obligation*. And
`AuthorityAct` has **0 corpus occurrences** — so there is nothing to promote; any typed act is
**INNOVATION**, which mandate §22 permits only for an irreducible requirement.

**Options.** **(a)** exogenous, untyped — status quo; keeps acyclicity, but the estate has **already
lost an authority act** to a `grantId` collision. **(b)** exogenous **but typed** — an `AuthorityAct`
with identity, still recorded-not-computed; fixes revocation/version-drift/collision; **innovation,
minimal**. **(c)** in-system authority — **rejected by the corpus's own strongest rule**.

**Recommendation:** **(b)**, and note it is the **only** place in this entire register where
innovation appears genuinely necessary.

---

## D-3 · Is conditional determination in the canonical model? *(carried)*

**Question.** Must the model preserve *"this assertion is accepted **because rule R was satisfied
under condition C**"*?

**Evidence.** `CORPUS` — **`Determination ≠ Decision` is stated 19 times.** `DERIVED` — `Determine`
is **forced** by that law and is **absent from the corpus's registry and from the terminal theory**.

**Why it cannot be derived.** The law forces the *distinction*; it does not fix **how much** of the
justification is retained — the rule instance, the condition, both, or a reference.

**Options.** **(a)** determination as a bare outcome — loses the *because*. **(b)** determination +
**reference to the rule instance** — recoverable, cheap. **(c)** full conditional record — complete,
heaviest.

**Recommendation:** **(b)** — it satisfies `Determination ≠ Decision` without conflating evidence,
evaluation, authority, decision and policy, which is the mandate's §8 requirement.

---

## D-4 · Is `Σ` stored or derived? *(new)*

**Question.** Step 273 §273.12 poses Model A (single status) / B (structured) / C (derived
`Σ=f(A,R,E)`) and says *"do not decide beforehand — the operation tests decide."*

**Evidence.** `EXECUTED` — `Σ` is **policy-relative**: identical evidence yields `Supported` at
`min_support=2` and `Unknown` at `3`. So a **stored** `Σ` admits an update anomaly. But a **derived**
`Σ` cannot carry an accepted commitment not recoverable from current evidence — which is what
`Γ=Committed` *is*.

**Why it cannot be derived *yet*.** Step 273's own deletion/replacement tests are the designated
instrument and **are still running**. `CORPUS` §273.31: *"`Σ` required does not imply `Σ` stored."*

**Recommendation: DO NOT DECIDE — let Step 273 finish.** This is the one decision where the right
act is to wait for work already in flight.

---

## D-5 · Is `ℛ` restored to the corpus 8-tuple? *(new)*

**Question.** The corpus defines `r = (E₁,E₂,T,R,Q,E,Σ,τ)`, *"two **or more** entities"* (4 files).
The terminal model uses `ℛ ⊆ 𝒜×𝒜×RelationType` — **5 fields and the n-arity discarded, unrecorded.**

**Consequence, constructed:** under the terminal model *"A₁ contradicts A₂"* **cannot be evidenced,
dated, superseded or contested.**

**Why it cannot be derived.** `v0.2` is **AUTHORIZED and md5-pinned**; `ℛ` is in its object
inventory. Restoring it **reopens a ratified artifact** — a governance act, not a verification one.
**And note `Σ` inside `r` is ill-typed** (policy-relative, stored with no policy parameter), so a
naive restoration imports a defect.

**Options.** **(a)** restore fully — reopens `v0.2`. **(b)** restore `E`, `τ` only, leave `Σ` out —
closes contradiction-evidencing and relation-temporality, avoids the ill-typed field. **(c)** keep
the triple and **record the loss as a declared scope exclusion with its cost**.

**Recommendation:** **(b)** — but it depends on D-4, and it touches ratified architecture, so it is
yours.

---

## ⚠️ Standing item, not a decision of mine to make

`CORPUS` — **GN-73** records a **MATERIAL COLLISION WITH THE RATIFIED LANE**, raised by another
session and **awaiting you**: the policy-change loop was closed **twice, differently** — ratified
`I-11 + R-1` (v0.2, GN-19) and again by the 2026-08-30 audit — *"two unreconciled resolutions of one
problem (`ES-005.4`)"*. **This construction phase sits on top of that unresolved item.**

---
artifact: 14-INDEPENDENT-VERDICT
date: 2026-08-30
verdict: **STRUCTURALLY OPEN AND SEMANTICALLY OPEN — the prior "closed" hypothesis is FALSIFIED, and it is falsified by the corpus's own highest step**
---

# 14 · Independent Verdict

## 1. The hypothesis, and the one fact that settles it

**Hypothesis under test:** *"THEORY CLOSED AGAINST THE STATED CRITERIA — NOT COMPLETE IN EVERY
RESPECT."*

**The corpus's own highest step refutes it.**

`Step 271 — POLICY SEMANTIC MINIMALITY AND ASSESSMENT BOUNDARY` (`20260830-204917`) is the last
research artifact in the corpus. It says:

> *"Therefore **Step 271 does not declare closure.** It narrows the problem."*

Its own status table marks **🔴 open**: Policy internal structure · Policy conflict semantics ·
Numerical assessment · Statistical independence · Probability · **Epistemic status Σ**. It records
`H₂₇₁` as **`HYPOTHESIS`, not theorem**. And it commissions:

> **STEP 272 — *"Derive the minimum epistemic-status structure from distinguishability,
> contradiction, missingness, evidence assessment, supersession and inference."***

**Step 272 does not exist.** *(Verified: highest step = 271; 217, 229, 268 also absent.)*

> **The research track's final act was to declare `Σ` and Policy-internals open and commission the
> next step. The theory was not closed. It was interrupted.**

*Admissibility, stated honestly:* step 271 postdates the closure audit and has read it (`01` §3), so
it is **inadmissible as corroboration** of any verification finding. It is **admissible as
disagreement**: a downstream source that has read a closure claim and still records *"does not
declare closure"* is not echoing it.

## 2. Answer to mandate §19

> # **STRUCTURALLY CLOSED BUT SEMANTICALLY OPEN** — is the closest single label, and it is still
> too generous. The precise answer is:
>
> **NOT COMPLETE. Structurally open (5 dangling primitives, graph is not a DAG), semantically open
> (6 symbols with ≥2 global senses), computationally closed only above its oracles, and
> governance-closed at the regime level while open at the level of the individual act.**

| Closure | Verdict | Decisive evidence |
|---|---|---|
| **Structural** | 🔴 **OPEN** | `Qualify`, `Authorize`, `𝒩`, `δ`-commit, `Admissible` all dangle; **3 cycles**, one unterminated |
| **Semantic** | 🔴 **OPEN** | `Ω` (≥4 senses), `θ` (≥5, incl. threshold **and** sample mean), `E`, `V`, `Q`, `Assurance` (≥5, refuted, still in law 42.9) |
| **Computational** | 🟡 **CONDITIONAL** | computable **relative to per-requirement evaluators** — the artifact's own words. The evaluators are unestablished |
| **Governance** | ✅ **CLOSED at regime level** | `I-11` stratification + `humanActRef` 132/132. 🔴 **open at act level** — `AuthorityAct` has no type |

## 3. The six previously-claimed closures, re-verified on new evidence

| Gap | This pass |
|---|---|
| **qualification / sufficiency** | 🟡 **CATEGORY ERROR in the prior verdict.** `Q` = epistemic sufficiency **is typed** at 42.40 and conjoined at 42.41. It was **dropped in ratification**. → `G4`, not `G3`. *(Corrects both prior passes.)* |
| **authority → gate binding** | 🔴 **UNRESOLVED.** `Authorize` has **no body corpus-wide**; codomain `𝒞` while 3 of 4 outcomes are not commands; `𝒩` undefined; **no `gᵢ` is typed to consume an authority** |
| **overloaded terminology** | 🔴 **CONTRADICTED.** `Ω` and `θ` are worse than `E`/`V` and were never registered; step 263 records `E`/`V` as *live* ambiguities |
| **provenance** | ✅ **CLOSED.** Four objects, four names, each independently anchored; refined — not contradicted — by step 265 (`π ∈ K`, object outside) |
| **reflexivity / policy-change** | ✅ **CLOSED, twice, before the verification programme.** `v0.2 R-1`+`I-11` and externalisation. ⚠️ **but only for policy *change*; the `Qualify` cycle is policy *use* and is unterminated** |
| **missingness** | 🟡 **PARTIALLY CLOSED — 8 of 9 distinctions exist, 4 EXECUTE.** `zero_reference.py` discriminates `Missing` from `Unknown` in code and passes 8/8. The gap is one missing component (`D_t`) plus a vocabulary that hides the capability |

**Score: 2 CLOSED · 2 PARTIALLY/CATEGORY-ERROR · 2 UNRESOLVED-or-CONTRADICTED. Not six.**

## 4. What an independent researcher would conclude who had never seen the prior verdict

> **That this is an unusually strong research corpus whose formalisation was reduced too far, and
> then stopped mid-step.**

Three reductions did the damage, and each is measurable:

1. **`ℛ` reduced from an 8-tuple to a bare triple** — 5 fields and the n-arity discarded. Contradiction
   claims became unevidenced, undatable and uncontestable; `Σ` went blind to `ℛ`; lineage edges lost
   their evidence.
2. **Status reduced from ten executable values to `(dir, str)`** — `Missing`, `Stale`, `Prohibited`,
   `NotApplicable`, `Invalid`, `Insufficient` became inexpressible, **while code implementing all ten
   sat in the repository passing 8/8 tests.**
3. **`DC(d)` reduced from 7 components to a ratified 6** — `Q`, epistemic sufficiency, was removed,
   and then its absence was rediscovered as an open theoretical problem.

**In every case the corpus was richer than the model built from it.** The theory's deficit is
substantially a **transcription and ratification** deficit, not a discovery deficit.

## 5. The smallest missing things, in dependency order

This **corrects** the previous pass's single answer (`SELF-3`).

| # | Missing | Depends on | Corpus status | Closes |
|---|---|---|---|---|
| **1** | **`ℛ` restored to `(E₁,E₂,T,R,Q,E,Σ,τ)`, n-ary** | nothing | **CORPUS ESTABLISHES ×4** | contradiction-evidencing · lineage evidence · `Σ`↔`ℛ` · relation temporality — **four gaps, one restoration, zero invention** |
| **2** | **`D_t` as a component of `K`** | nothing | **CORPUS ESTABLISHES** (Zero) | *"not asked"* vs *"asked, nothing found"*; 8 of 9 missingness distinctions |
| **3** | **`AuthorityAct` with identity** | nothing | 🔴 **0 corpus occurrences — genuinely absent** | revocation · version drift · bypass · the `grantId` collision already on record |
| **4** | **`Qualify`'s body** | 3 (a constitution is adopted by an act) | 🔴 absent | the pipeline's first stop; and it is what makes the `Evidence→Policy→K→Assertion→Evidence` cycle undecidable |
| **5** | **`(W, Ω)`** | nothing | **CORPUS ESTABLISHES** (31.17–18) | non-identifiability; the referent `K` represents |
| **6** | `Σ`'s value set and `str` rule | 1, 2 | partly (ten-set executes) | **this is exactly Step 272** |

> **Three of the six require no invention — only restoration or adoption. Only #3 and #4 are
> genuinely absent from the corpus.**
>
> **And #1 is the smallest, cheapest and highest-yield of all: restoring one tuple closes four gaps
> and invents nothing.**

## 6. The one normative decision — put to you, unanswered

Everything above is a finding. **One question is genuinely normative and I will not answer it**
(mandate §20).

> ### **Do the corpus constructions that ratification dropped go back in?**
> Specifically: the **`ℛ` 8-tuple**, the **ten-value status set**, **`Q` epistemic sufficiency**,
> **`D_t`**, and **`(W, Ω)`**.

**Why the corpus cannot derive it.** Not because the mathematics is unclear — it is unusually clear,
and all five predate the verification programme. **Because an authority has already ruled the other
way and the ruling stands.** `v0.2` is **AUTHORIZED and md5-pinned** (GN-19); `FA-1…FA-9` are
**RATIFIED** (GN-31); the standing lines are *no architectural invention · no mathematics-as-
architecture without independent establishment · P4 CLOSED*. **Restoring any of the five reopens a
ratified artifact.** `Assessment → verdict → NEVER grants authority` — this verification cannot make
that call, and saying so is the rule working.

**Alternatives and consequences:**

| Option | Consequence | What depends on it |
|---|---|---|
| **A · Execute Step 272 as commissioned** | The corpus's own next step; derives `Σ` from distinguishability. **Requires no ratification change** — it is research, and 271 already commissioned it | `Σ`, missingness, contradiction — the three 🔴 rows of 271's own table |
| **B · Restore `ℛ` alone** | Smallest change; closes 4 gaps; invents nothing. **But `ℛ` is in `v0.2`'s object inventory ⇒ reopens a ratified artifact** | contradiction-evidencing, lineage, `Σ`↔`ℛ` |
| **C · Commission an evidence pass on all five** | Matches the precedent already set (GN-42 Decision 3: *"I.3 is NOT to be filled from inference; it waits for the evidence"*) | everything in §5 |
| **D · Ratify the reductions as deliberate** | Legitimate — but then **the losses must be recorded as declared scope exclusions with their costs**, and the claim *"the corpus cannot express X"* must be withdrawn, because it is false | the honesty of every future completeness verdict |

**Recommendation, offered as a recommendation only: A, then C.** Option **A costs nothing
governance-wise** — Step 272 is already commissioned by the corpus's own highest step, it is research
rather than ratification, and it is aimed precisely at the object (`Σ`) that every open row depends
on. **The single cheapest correct next act is to execute the step the research track already wrote
down and never ran.**

## 7. Stopping conditions (mandate §22)

| # | Condition | Met? |
|---|---|---|
| 1 | every primitive tested | ✅ 34 concepts scanned; 29 areas in the matrix |
| 2 | every dependency tested | ✅ 24-node graph, DFS-traversed, 3 cycles found |
| 3 | every transformation tested | ✅ `Qualify`, `Authorize`, `δ`, `Assessment`, `merge`, `Σ` — each executed or shown bodiless |
| 4 | competing definitions reconciled or left explicitly unresolved | ✅ 17-entry registry; **6 left explicitly unresolved**, none resolved by recency |
| 5 | executable claims executed | ✅ **5 suites run**; one witness found vacuous |
| 6 | implementation mismatches recorded | ✅ 6 recorded, **3 of them implementation-exceeds-theory** |
| 7 | normative decisions isolated | ✅ **exactly one**, §6 |
| 8 | remaining gaps demonstrably parametric or out of scope | 🔴 **NOT MET — and this is why the theory is not complete.** 11 `G3` and 6 `G6` are neither parametric nor out of scope |

**Condition 8 fails. The investigation stops here and reports, as instructed.**

---

> # FINAL
> **The prior hypothesis is FALSIFIED.** The theory is **not closed against the stated criteria** —
> and the strongest evidence is not adversarial analysis but the corpus's own highest step, which
> declares `Σ` and Policy-internals open and commissions a Step 272 that was never written.
>
> **The theory is also better than its own verdicts suggest.** Four of its hardest "gaps" are
> reductions of material the corpus already established, and two of those already **execute and
> pass**. What it needs first is not new mathematics. **It is to stop losing what it already had —
> and then to run Step 272.**

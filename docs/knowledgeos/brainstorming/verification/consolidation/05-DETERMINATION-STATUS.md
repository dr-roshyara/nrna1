---
artifact: 05-DETERMINATION-STATUS
date: 2026-08-30
status: **CORPUS-ESTABLISHED AND LOST — a complete 8-field aggregate exists; the canonical theory has none. RECONSTRUCTION.**
---

# 05 · Determination Archaeology

## 1. Independent verification of the second-order claims

| Claim | Verified? | Measured |
|---|---|---|
| ~1 165 occurrences | 🟡 **partly** — **836 occurrences across 172 files** (corpus, excluding `verification/`) | the order of magnitude holds; the exact figure does not |
| Step 157 provides a typed concept | ✅ **CONFIRMED** | §157.22, read in full |
| lifecycle exists | ✅ **CONFIRMED** | §157.23 |
| Step 165 gives explicit structure | 🟡 `step_165` exists (*aggregate and consistency-boundary derivation*); **the load-bearing structure is §157.22**, not 165 |
| distinct from Decision | ✅ **CONFIRMED** | `Determination ≠ Decision` stated **19×** |
| embedding into Assertion rejected | ✅ consistent with §157.22 making it its **own aggregate root** |

**Correction to the second-order pass: the count is 836, not ~1 165, and Step 157 (not 165) carries
the structure.** The substance of its finding survives.

## 2. What the corpus actually has `CORPUS` §157.22

> **`Determination` is a strong Aggregate Root candidate.** It owns:
```
DeterminationId · Proposition · Method · EvidenceReferences ·
Result · EpistemicStatus · Actor · ContextSnapshot
```
> **Aggregate invariant:** *"A determination cannot claim evidentiary support without referencing the
> evidence and method on which that support depends."*
>
> **§157.23 — immutable after finalization:** `DRAFT → PROPOSED → FINALIZED`, then
> `D1 --SupersededBy--> D2`. *"This gives us epistemic history."*

**Eight typed fields, an invariant, a lifecycle, and a supersession rule.** Dated 2026-08-28.

## 3. The four questions

1. **Is Determination part of the corpus ontology?** ✅ **YES** — fully, with structure.
2. **Is it part of the canonical formal theory?** 🔴 **NO.** Absent from `K=(𝒜,ℛ)`, from
   `K=(A,R,Σ,E_L)`, from 272A's `𝒪_sem`, and from 273.
3. **Where exactly was it lost?** In the **transition from the domain-model track (steps 156–165) to
   the mathematical-kernel track (steps 230+)**. The kernel track reconstructed from `230.x` and the
   Q-series and **never re-entered the 15x domain-model band**. *This is the same discontinuity that
   lost `ℛ`'s 8 fields and the ten-value status set.*
4. **Reconstruction, refinement, or new theory?** → **RECONSTRUCTION (category 4).**

## 4. Why it cannot be dismissed as historical

`DERIVED` — Independently of §157.22, **`Determine` is a FORCED operation**: the law
`Determination ≠ Decision` (19×) entails an operation that produces the former and not the latter.
**`Determine` appears in neither §256.2 nor §259.7** — the corpus's own registries omit an operation
its own laws force.

> **Two independent routes — the aggregate at §157.22, and the forcing argument from the
> non-collapse law — reach the same object. That is corroboration, not repetition.**

## 5. It answers D-3 from the corpus

D-3 asked whether *conditional* determination (*"accepted **because** rule R under condition C"*) is
in scope. **§157.22 already answers it:** `Method` + `EvidenceReferences` + `ContextSnapshot` retain
exactly the *because*, and the aggregate invariant **requires** it.

> **D-3 is substantially answered by corpus recovery rather than by decision.** What remains is
> narrow: whether `Method` references a **rule instance** or a **rule version**. That is a
> representation choice, and it is now the whole of D-3.

## 6. Not promoted

`NORMATIVE` — Recovery into the canonical theory is a governance act, not a verification one.
**Recorded, not promoted.**

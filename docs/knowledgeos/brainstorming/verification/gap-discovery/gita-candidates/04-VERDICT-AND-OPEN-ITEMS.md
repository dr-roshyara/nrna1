# 04 — Verdict, Live Residue, and Status

---

## 1. The verdict

> **Are there Gītā candidates for KnowledgeOS, and for the KnowledgeOS kernel?**

$$\boxed{\begin{array}{c}\textbf{For KnowledgeOS: YES — 8 at T3, 7 at T2. Four of the eight are corroborations of results}\\ \textbf{already derived; four are unexamined and could be examined cheaply.}\\[4pt] \textbf{For the KERNEL: NO. Three T4 hits, all corroborative, all corpus-native.}\\ \textbf{ZERO new primitives · ZERO new canonical relations · ZERO new kernel components.}\end{array}}$$

**Verified independently** (`01` §4), not accepted on citation.

## 2. The live residue — four items that are genuinely still open

These are the only places where the strand still has something at stake.

### `GR-1` — the `Guṇa` fork ⚠️ **the one open decision**

`Guṇa` was flagged before it was ever opened (needs a declared scale type) and has now been
**sharpened into a two-branch question with named costs:**

| branch | consequence |
|---|---|
| **`G_t` DERIVED from `Σ`** *(mode = goodness when support is strong)* | 🔴 **the threshold returns** — a derivation rule over `S`'s ordinal bands **is** the `θ` gap |
| **`G_t` INDEPENDENT of `Σ`** — `K_t = (Σ_t, G_t, …)` | 🔴 **a new state component with no carrier**, and another **open-ended tuple**, which `258.35` warns against |

$$\boxed{\textbf{Neither branch is free. The flag is not lifted — it is now a two-branch question with named costs.}}$$

**Status: `DECISION REQUIRED`, and it is small.** Note branch (b) was taken informally by artifact
`30` (*"`Guṇa` = mode"*) — **the branch that leaves it uncarried.**

### `GR-2` — the missingness carrier: structure yes, population rule no

`Māyā/Avidyā → MNAR` names the defect; the **dimension model** supplies a candidate carrier for the
one **empirically confirmed** theory defect, Critical Failure #7:

$$\boxed{\textit{not-asked} \equiv d_i \notin \mathcal D_t \qquad \textit{recognized-but-unknown} \equiv d_i \in \mathcal D_t \wedge v_i = Unknown}$$

**Why a carrier and not another shape:** `Σ` is flat — every axis always has a value, so *"never
considered"* has nowhere to live. **`𝒟_t` is a VARYING SET, and set membership is representable.**

🔴 **But:** it separates `M1` from `M3` only, **not all of `M1`–`M7`**; `M2` needs a third value;
`M7`'s orphan is structural; and the **population rule is BLOCKED** — `𝒟_t = {p : Relevant(p)}` runs
through `258.21`, which quantifies over the **unenumerated `𝒯`**.

$$\boxed{\textbf{The carrier's STRUCTURE exists; its POPULATION RULE is blocked. } A6/G\text{-}60 \textbf{ is NOT closed.}}$$

⭐ **Still the most significant thing the strand produced**, because it is the only candidate aimed
at a *measured* failure rather than a conceptual gap.

### `GR-3` — `Determination`: a recovery signal

The Ch 17/18 lens **re-raised an object the corpus specified (157/165) and then lost.** `Dhṛti →
persistence/stability of a chosen direction` gives it a behavioural reading; the layered model later
gave it a **position**. ⚠️ **Re-raised, not re-instated** — and `SPEC-DET-2026-v1`'s status remains
unverifiable from the governance register.

### `GR-4` — four unexamined T3 correspondences

`Yajña → evidence aggregation` · `Śraddhā/Śaṅkā → trust/abstention` · `Kṣamā → error tolerance` ·
`Dhyāna → epistemic regulation`. **All four have a real KnowledgeOS layer and none has been examined
against it.** `Dhyāna` is blocked (`δ ← 𝒯 ← 𝒪`); **the other three are not blocked by anything.**

`[REC]` **If any further Gītā work is authorized, it should be these three and nothing else** — they
are the only rows where an examination could still change a tier, and none of them touches the kernel.

## 3. ⚠️ The lane conflict — reported, not resolved

**Two Gītā lanes exist, and they hold opposite verdicts on every load-bearing question.**

| `external_research` declares | the research lane establishes |
|---|---|
| *"all gaps addressed"* ×8 · *"Missing Architecture — Solved"* ×5 | **0 of 13** closure-contract rows satisfied, in any of three senses |
| *"Architecture Gap ✅ Solved"* | **`261.23` ACTIVE — 0 of 6 conditions resolved**, 4 FAILED |
| *"Type System ✅ Complete — seven types"* | **`𝒪` membership OPEN; 0 of 22 operations has a body, an identity, or ratification** |
| *"Σ Theory ✅ Complete — three modes"* | **`Σ` is FIVE axes, 2 240 states; 0 of 5 ordered** |
| Ch 15: *"the complete theory of knowledge identity"* | ⚠️ **that is THE equality question** — established OPEN by five steps |

$$\boxed{\begin{array}{c}\textbf{Two lanes, one corpus, opposite verdicts on every load-bearing question.}\\ \textbf{Neither cites the other.}\end{array}}$$

⚠️ **This is more serious than the three-vocabulary gap (`G-68`)**: a vocabulary gap is visible on
inspection; **a disjoint closure-status gap is visible only if you read both lanes.**
**Adjudicating it is a governance act. This document records it and takes none.**

## 4. What the estate should NOT do — and the corpus says so itself

| Do not | Because |
|---|---|
| **promote any Gītā term to a primitive** | 18 chapters, ~60 correspondences, **0 reached primitive status**; every object-level attempt failed type-checking |
| **map `Guṇa → Σ` directly** | produces `Σ = (Supported, Strong)` — values in **no ratified axis** (`E2`) |
| **use `Θ_total`** | type-check failure across four carriers |
| **read the structural match quantitatively** | the probability **law** is deferred and 0 of 5 axes is ordered |
| **test the Gītā strand for kernel candidacy again** | `[REC]` **Theory 13 §4, verbatim:** *"five cycles, zero machinery — the candidacy question is answered"* |

## 5. Status

$$\boxed{\begin{array}{c}\textbf{The G\={\i}t\={a} candidacy question is CLOSED, and closed NEGATIVELY for the kernel.}\\ \textbf{Theory v1.2 FROZEN · kernel NOT SELECTED · } \mathcal O_{core} \textbf{ NOT FROZEN · nothing adjudicated.}\end{array}}$$

**This package promotes nothing, adopts nothing, ratifies nothing and reopens nothing.** Its one
`DECISION REQUIRED` is `GR-1`, the `Guṇa` fork, and its one recommendation is `GR-4` — three
unexamined T3 rows, none of which touches the kernel.

`[INF]` **And the closure is the useful part.** A strand that ran 18 chapters, ~60 correspondences,
five cycles and two lanes, and produced **three corroborations, ten refusals, four empty shapes and
zero primitives**, has told the estate something precise: **the kernel's remaining problems are
carrier problems, and no philosophical source supplies carriers.**

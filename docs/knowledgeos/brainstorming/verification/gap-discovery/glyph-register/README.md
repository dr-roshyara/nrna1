# `glyph-register/` — `V3`: the Glyph-Collision Inventory

**2026-09-07 · input to `H1`, the registry act.** Read-only. **This inventory proposes no renames and
takes no registry decision — `H1` is Governance's.**

---

## 0. The headline — the count was too low, and the worst case is the central term

I have been reporting **16 collisions**. **Measured by definitional context across the whole
`docs/knowledgeos/` tree, the picture is worse in one specific way:**

$$\boxed{\begin{array}{c}\textbf{The most-collided glyph is } \mathcal K / \mathbb K / K \textbf{ — the theory's CENTRAL term —}\\ \textbf{with SEVEN distinct tuple definitions, of three different arities.}\end{array}}$$

**And `Π` carries six meanings, not three.**

## 1. `𝒦 / 𝕂 / K` — **seven definitions** 🔴🔴🔴

| definition | arity | source |
|---|:--:|---|
| `K = (𝒜, ℛ)` | 2 | `verification/CLAUDE-CHATGPT-RECONCILIATION.md` |
| `𝒦 = (K, H)` | 2 | `gap-discovery/14-FALSIFICATION-RESULTS.md` |
| `𝒦 = (K, C, T, E, A)` | 5 | `verification/prompts/20260830_1021_prompts.md` |
| `𝕂 = (Σ, M, B, D, O, δ)` | 6 | `three_model_convergence/…/per-file/2283.yaml` |
| `𝒦 = (E, S, T, O, P, R, Π, A)` | 8 | `verification/findings/TV-F-034-039-steps-041-055-findings.md` |
| `𝕂 = {(G, σ, θ, λ, π)}` | set of 5-tuples | `verification/KERNEL-RECONCILIATION-230-236.md` |
| `𝕂 = { admissible knowledge states }` | a set, undefined members | `verification/prompts/20260830_0952_prompt.md` |
| *(plus)* ratified `K_t` over the 8 primitives · `K_t = (𝒜_t,ℰ_t,ℛ_t,𝒞_t,ℋ_t,Γ_t)` | 8 / 6 | as in `V1` |

⚠️ **Three of these are `𝒦` and two are `𝕂` — the script distinction is load-bearing and undeclared.**
`[INF]` **This is my *"two rival `K`"* finding, measured at the glyph level.** It is not two rival
definitions; **it is at least seven, and the reader has no rule for telling which is meant.**

## 2. `Π` — **six meanings**, and one is self-containing 🔴🔴

| meaning | source |
|---|---|
| **provenance** — `id = H(P,e,c,t,Π)`, `Lineage = Π ∘ ℛ_der*`, **Decision 3: `Π ∈ ≡?`** | `knowledgeos_kernel/research/38-THE-LAYERED-MODEL…` |
| **`Π = (Q, C, O)`** — the **inquiry frame** | `…20260903-093000_prompt-design-the-r5-r2-reduction-experiment.md` |
| **`Π = preservation contract`** | `…20260903-092003_clarification-what-raw-kr-zero-records-means.md` |
| **`Π : 𝓡 → O`** — the **observable** (Theory 01) | `…theory-01-primitives-and-signatures.md` |
| **`Π_t`** — a **probabilistic representation** layer | the layered model (`G-109`) |
| **Policy** | the state-vocabulary table (`G-102`) |
| ⚠️ **`Π = (X, 𝒯, ℰ, ~_Q, Q, Π)`** — **the tuple contains `Π` as its own last component** | `three_model_convergence/01_source-analysis/dimension-registry-mathematical.md` |

$$\boxed{\textbf{A reader importing } \Pi \textbf{ as Policy silently re-reads Decision 3, the programme's central normative question.}}$$

⚠️ **The self-containing definition is flagged as a defect, not interpreted.** It is either a
transcription error or a genuine recursion; **I do not know which, and guessing is not my act.**
*(The same shape appears at `Λ = (𝓗, ℛ_sem, Λ, Σ_Λ)` — `Λ` inside `Λ`.)*

## 3. `Θ` — **five meanings**, spanning three mathematical traditions 🔴

| meaning | tradition | source |
|---|---|---|
| **`Θ : 𝒮_t × Input_t → 𝒮_{t+1}`** — a transformation | automata | `…20260902-155427_the-research-model-and-the-optimized-model.md` |
| **`Θ_total`** five-operator algebra | — | **REJECTED**, `D285-4` (type-check failure, 4 carriers) |
| **`Θ = { φ, ¬φ }`** — a **frame of discernment** | Dempster–Shafer | `gap-discovery/theory-compatibility/03` ⚠️ **mine** |
| **`Θ → [0,1]`, `m(∅)=0`, `Σ_{A⊆Θ} m(A)=1`** — a **mass-function domain** | Dempster–Shafer | `verification/spec/STEP-TRACE-B1-20260825.md` |
| **`Θ = world states`, `U = utility`, `C = constraints`, `R = risk model`** | **Wald decision theory** | `verification/spec/STEP-VERIFY-056-066.md` |
| **`Θ = Y ∘ L`** | a composition | corpus |

### 🔴 A **third** correction to my own `Θ` claim

| when | I said | status |
|---|---|---|
| `theory-compatibility/06` | *"`Θ` has never been raised"* | 🔴 withdrawn (2026-09-02) |
| `gap-update/08` §3 | *"present as `𝒜_Q`, the alternative space"* | ⚠️ true but incomplete |
| **now** | **`Θ = world states, U = utility, C = constraints, R = risk model` is in `verification/spec/STEP-VERIFY-056-066.md` — Wald's `Θ`, literally, under its own glyph** | ✅ **the accurate statement** |

$$\boxed{\begin{array}{c}\textbf{I claimed } \Theta \textbf{ was absent; it was present under FIVE readings, one of them the exact one I said was missing.}\\ \textbf{The cause both times was searching for a symbol in one scope instead of a concept in the estate.}\end{array}}$$

## 4. `Σ` — **four** definitions

| | source |
|---|---|
| **`Σ = (A,S,R,V,C)`** — 5 axes, `7×5×4×4×4 = 2240` states | `knowledgeos_kernel/research/REFINED-STEP-287.md` |
| **`Σ = {Unknown, Supported, Refuted}`** — 3 values | `verification/DECISION-SIGMA-EPISTEMIC-STATUS.md` |
| **`Σ = (Neutral, None)`** — a pair | corpus |
| **`Σ ≅ 𝒫({Support, Refute})`** — 4 values, = FDE `(S⁺,S⁻)` | my lane · `KR-CONTR-FDE` |
| *(plus)* `Σ_Λ` inside `𝔥_Λ`; `Σ` as summation throughout | — |

⚠️ **`Σ` is the one fully-verified structure in the theory, and it is the second-most collided glyph.**

## 5. `δ` — a transition function **and** a numeric threshold 🔴

| meaning | source |
|---|---|
| **`δ : K_t → K_{t+1}`** — the state transition (`TODO Group E`) | `…per-file-mathematical/M0166.yaml` |
| **`δ = 0.3099`** — the **resolution threshold** on a score difference | `FR-001` / `…20260902-142901_copy-of-the-frozen-model-reference.md` |
| `δ = successor-state axioms` · `δ = situation calculus` | step-292 (Reiter) |

$$\boxed{\textbf{The estate's central OPEN definition shares its glyph with a measured constant in its one FROZEN result.}}$$

## 6. The rest, with counts

| glyph | meanings | note |
|---|:--:|---|
| **`Γ`** | 4 | governance/authority state · `Γ : E_t×Q×C → A_t` the attribution function · `Γ = Uncommitted` (a **value**) · `Γ → K'` |
| **`τ`** | 3 | a **stopping time** `{τ ≤ t} ∈ ℱ_t` · `τ = Y(1) − Y(0)` the **causal treatment effect** · `τ` the **threshold vector** in `SPEC-DET` |
| **`𝒪`** | 3 | operation universe · the **observation function** `O(K_1) = O(K_2)` · `𝒪 = 𝒪_S×𝒪_A×𝒪_C×𝒪_X` |
| **`Φ`** | 2 | `Φ : Π_t → K_t` (`G-109`) · `Φ : K̂_t → {Knowledge, Not-Knowledge}` — **one arrow apart in the same pipeline** |
| **`Λ`** | 2 | `Λ = (m, σ, α)` the evidence regime · `Λ` inside `𝔥_Λ` — **self-containing** |
| **`ℐ` / `𝓘`** | 2 | `ℐ = {I_1,…}` the invariant set · `ℐ → 𝒫_c` a different arrow · ⚠️ **two scripts for one register** |
| **`D_t`** | 3 | dimension set `{d_1,…,d_100}` · `Determination` · the kernel skeleton |
| **`AR_t` / `R_t`** | 2 | caught and corrected at source, 2026-09-07 |
| **`ℱ`** | 2 | σ-field of measurable knowledge regions · `ℱ_t` the filtration |
| **`𝒩`** | 1 + | the knower; `𝒩 → 𝒩` endomorphism questions open |

## 7. Tally, and what it is evidence for

| | |
|---|---|
| glyphs carrying **≥ 2** definitions | **15** |
| **total distinct definitions across them** | **≥ 45** |
| glyphs carrying **≥ 4** | **`𝒦/𝕂/K` (7) · `Π` (6) · `Θ` (5) · `Σ` (4) · `Γ` (4)** |
| **self-containing tuples** | **2** — `Π = (…, Π)` and `Λ = (…, Λ)` |
| collisions **spanning mathematical traditions** | **`Θ`** (automata / DS / Wald) · **`τ`** (stochastic processes / causal inference / decision thresholds) |
| collisions **inside one artifact** | 6 in one table (`G-102`) |

`[INF]` **`H1` is not tidying.** Two of the top five glyphs carry a **normative question** (`Π`'s
Decision 3) or the theory's **central object** (`𝒦`). **A reader cannot resolve either from the text,
and two lanes writing `𝒦` are not necessarily writing about the same thing.**

## 8. What `H1` needs, stated as requirements — not as a proposal

1. **one register**, glyph → meaning → owning context → citation;
2. a rule for **`𝒦` vs `𝕂` vs `K`** — the script distinction is load-bearing and undeclared;
3. a **disambiguation for `Π`** that keeps Decision 3 legible;
4. a ruling on the **two self-containing tuples** — transcription error, or recursion;
5. a rule for glyphs that **cross traditions** (`Θ`, `τ`) — most likely: *the tradition must be named at every use*;
6. **`ℐ` vs `𝓘`** — one script.

**I propose no renames.** Renaming is a registry act, and it would silently rewrite citations across
~3 000 files.

## 9. Reproduce

```bash
cd docs/knowledgeos
for g in 'Π' 'Θ' '𝒦' '𝕂' 'Σ' 'δ' 'Γ' 'τ' 'Λ' 'Φ'; do
  echo "## $g"
  grep -rhoE "$g *(:|=|∈|→|:=)[^.|]{0,70}" --include='*.md' . | sort | uniq -c | sort -rn | head -8
done
```

# 09 — Multiplicity Register: Every Absence Claim Re-Audited Under the Corpus Rule

**2026-09-06.** Applying `../CORPUS-SEARCH-RULE.md`. Search universe:
`docs/knowledgeos/brainstorming/` — **3 136+ files**, searched with exact terms, synonyms,
alternative terminology and **LaTeX** notation.

**Every item is classified A / B / C. Nothing is resolved by preference. Where the corpus does not
resolve a multiplicity, the outcome is `DECISION REQUIRED` and it is put to Governance in `10`.**

| | |
|---|---|
| **A · NOT FOUND** | a search result only — never evidence of non-existence |
| **B · DEFINED MULTIPLE WAYS** | a corpus finding — reported, never silently narrowed |
| **C · CANONICALLY RESOLVED** | the corpus itself shows precedence — only here may one be treated as canonical |

---

## 0. The sweep, measured

Every concept my package called *undefined · missing · a blocker*, counted over the whole tree.
"Definition-shaped" = the file also contains `Definition` / `:=` / `\boxed` / *is defined as* / *We define*.

| my blocker claim | files | definition-shaped | verdict |
|---|---:|---:|:--:|
| `Contr` / contradiction | 1 100 | **647** | **B** |
| ordering `⪰` / progress | 414 | **289** | **B** |
| `Qualify` | 770 | **442** | **B** |
| `δ` / transition | 440 | **308** | **B** |
| `Admissible` / `Assurance` | 552 | **331** | **B** |
| `≡_sem` / equality | 364 | **240** | **B** |
| `dedup` | 90 | 51 | **A** *(mentions, no definition)* |
| distance `d(K,I)` | 559 | **352** | **B** |
| `Φ : Π_t → K_t` | 39 | 27 | **B** *(and a glyph collision)* |
| `𝒪_core` / registry | 142 | 87 | **B** |
| invariants `ℐ` / `ℛ_req` | 178 | 102 | **B** |
| `Reject` | 415 | **217** | **B** |
| missingness population | 178 | 120 | **B** |
| `Zero` | 1 269 | **675** | **B** |
| provenance placement | 1 557 | **908** | **B** |
| composition rule | 329 | **211** | **B** |
| lifecycle / retirement | 932 | **548** | **B** |

$$\boxed{\textbf{16 of 17 are state B. One is A. Not one is an absence.}}$$

`[INF]` **My register was a multiplicity register mislabelled as a gap register.** The word *undefined*
appears in my package where the corpus holds **several** definitions and no precedence rule. That is a
different problem with a different owner: **not derivation — decision.**

---

## 1. ⭐ `C` — CANONICALLY RESOLVED: the Theory v1.3 closure claim

**This one the corpus decides, so I record it as decided.**

**Claim A** — `…182003_consolidated-architectural-audit…`, `…182005_final-architectural-review-…v13`,
`…182014/182015_ratification-assessment-final-closure-packages` (2026-09-02 18:20):

| CLOSURE-1 `EVal` | CLOSURE-2 `Det` | CLOSURE-3 `𝒪_core + δ` | CLOSURE-4 `≡_sem + ∘` | CLOSURE-5 `EA` + kernel `ABK-1` |
|---|---|---|---|---|
| 🟢 RATIFIED | 🟢 RATIFIED | 🟢 RATIFIED | 🟢 RATIFIED | 🟢 RATIFIED |

$$\text{"KnowledgeOS Kernel Theory v1.3 is Fully Ratified, Closed, and Complete."}$$

with `ℛ_req`, `Contr`, `𝒪_core` (**5 primitives**), `δ`, Composition, `≡_sem`, Kernel Selection all
`[CLOSED]`.

**Claim B** — four later or contemporaneous sources, none of which accept it:

| source | date | says |
|---|---|---|
| `…182009_review-as-senior-mathematician` §8 | 09-02 | *"CLOSURE-4 — the semantic equivalence claim is too strong"* |
| `…182010_review-consolidation-as-senior-statistician` §7 | 09-02 | *"CLOSURE-4 semantic equivalence is not closed… the claim that CLOSURE-4 is fully ratified **is not supported by the evidence**"* |
| `step-292/00_INDEX` | 09-02 **19:12** | *"Theory v1.2 unchanged · **no v1.3** · kernel **NOT SELECTABLE**"* |
| `…20260904-102000_consolidated-research-theory-draft-**not-v13-ratification**` | 09-04 | *"the current governance position is still: **Theory v1.2 remains frozen; no kernel law, algebra or Theory v1.3 is declared until audit/adjudication is complete**"* |
| **Theory 00–14 series** | 09-04/05 | *"**Not Theory v1.3. Not a ratification.** Theory v1.2 remains FROZEN · the kernel remains NOT SELECTED · **nothing here is adjudicated**"* |

$$\boxed{\textbf{RESOLVED. The closure package is a PROPOSAL that was reviewed and not adopted. v1.2 is frozen; there is no v1.3.}}$$

⚠️ **And this retires my own `06-AUTHORITY-CLAIMS-AUDIT` framing.** I reported four `[RATIFIED]`
stamps as *"not locatable in the governance register."* **The correct finding is stronger and
different: they were located, reviewed, and superseded — by the corpus, in writing, twice.** The
estate's self-correction worked. **My audit looked in the wrong place for an answer the corpus had
already given.**

✅ **And governance HAS gained an entry since I last measured:**
`docs/knowledgeos/governance/EPISTEMIC-STATUS-VOCABULARY.md` — **ADOPTED 2026-09-04**, defining
`[DEF] [THM] [COR] [PRP] [EXP] [CONJ] [NEG] [OPEN] [REC] [DEFECT]`, retiring `[PROP]` as ambiguous,
and ruling that **an untagged mathematical statement is a defect.** My *"governance grew by zero
entries"* is **withdrawn.**

---

## 2. `B` — `Contr`: seven signatures and two consequences

**The estate's own priority-1 item. It is not undefined; it is over-defined.**

### Signatures found

| form | source family |
|---|---|
| `Contr(p)` | `…155903_transcript-now-perform-the-contr-experiment`, Priest extraction |
| `Contr(p,K)` | `…155900_prompt-minimum-machinery-to-represent-contradiction` |
| `Contr(K_t,p)` | `…093546_satisfaction-by-requirement-class` (the `Sat` family) |
| **`Contr(K_t,σ)`** | same — **over a scope `σ`, not a proposition** |
| `Contr(p,C,t)` | `…155900_…` — context- and time-indexed |
| `Contr(p,K,t)` · `Contr(p,K_t)` | `…155900_…` |
| `Contr(P,¬P)` | Priest extraction — **over a proposition *pair*** |

### A semantic definition — and it is mine

$$Contr(p)\iff S^+(p)=1 \wedge S^-(p)=1 \qquad Contr(p)=Support^+(p)\wedge Support^-(p)$$

⭐ **That is `Σ = (1,1)`.** The corpus and my package derived the same object independently. **My
`Σ ≅ 𝒫({Support,Refute})` is not a competing model of `Contr` — it is one of the corpus's own.**

### Two incompatible consequences

$$Contr(p)\Rightarrow GlobalInvalidity \qquad\text{vs}\qquad Contr(p)\Rightarrow LocalConflict(p)$$

**This is the explosion question — classical vs paraconsistent — stated as a fork in one file.**

### Five non-collapse constraints, agreed across sources

$$Contr(p,K)\not\equiv\{Absent,\ InsufficientEvidence,\ NotAssessed,\ Unknown,\ Unobservable\}(p,K)$$

**Exactly the distinctions `KR-CONTR-FDE` measured FDE collapsing two of.**

**Difference that matters:** the signature choice decides *what contradiction is predicated of* —
a proposition, a proposition **pair**, a state-proposition pair, or a **scope**. The consequence
choice decides whether one contradiction invalidates a state. **They are independent choices.**

**Precedence evidence in the corpus:** none selects. `Contr` is `[OPEN]` in `The Frozen Model` §4.4,
the HPA-approved TODO register (Group B, **priority 1**) and step-292. **⇒ `DECISION REQUIRED`.**

---

## 3. `B` — `⪰`: three orderings the corpus says must stay separate

| reading | form | source |
|---|---|---|
| **state progress** | `K_{t+1} ⪰_EC K_t` | the `Sat`/closure family |
| **evidence strength** | `ES_{K_t}(p) ⪰ s_min` | same family, **different type** |
| **refinement / subsumption** | `⪰` as refinement (`25I.21`, `25J.8`); `C ⊑_S D` — *"subsumption gives the formal shape of the missing `⪰` lane, not its content"* | Description-Logic integration |

**The corpus states the reconciliation constraint itself:** *"`⪰` `OPEN`, **independent of `N_eff`** —
a definition; **two independent paths exist and must stay independent**."* And:
*"The previous experiment correctly identified `⪰` as missing; **it did not establish its semantics**."*

⚠️ **Note the type clash:** `K_{t+1} ⪰ K_t` orders **states**; `ES(p) ⪰ s_min` orders **scalars**.
These are not two notations for one relation.
`[CORPUS]` And the estate has measured the cost of leaving it open: **83 % of progress claims
(1 491 of 1 793) specify no ordering**, and `291` found **0 of 5 axes ordered.**

**⇒ `DECISION REQUIRED`** — and the corpus's own hint is that the answer may be *more than one
ordering, kept apart*, not a single `⪰`.

---

## 4. `B` — `δ`: two argument types, and a `Reject` branch I called missing

| form | second argument |
|---|---|
| `K_{t+1} = δ(K_t, e_t)` | an **event** |
| `δ(K, o) = K'` | an **operation** |
| **`δ(K, o) = Reject(r)`** | **operation, with a rejection branch** |

🔴 **`Reject` is in the corpus.** `…step_285_canonical-state-reconciliation-gita-informed-lens.md:1070`
and `…step_285_gita-informed-reading-canonical-state-research.md:867` both write
`δ(K,o) = Reject(r)`. **My register carries *"the `Reject` repair — untouched, open."* A rejection
branch of `δ` exists; whether it is *the* repair is a separate question I have not settled.**

**Also present:** composition `K_{t+2} = δ(δ(K_t,e_1),e_2)`, the discrimination question
`δ(K,o_1)=δ(K,o_2) ⇒ o_1=o_2?` marked **TBD**, and the invariant schema `P(K) ⇒ P(δ(K,o))`.

**Difference that matters:** event-indexed `δ` makes the kernel **reactive**; operation-indexed `δ`
makes it **commanded**. Whether `δ` is **total** or **partial with a `Reject` codomain** changes every
invariant statement.

**Precedence:** none. `δ` is `[OPEN]` in three registers. **⇒ `DECISION REQUIRED`.**

---

## 5. `B` — `≡_sem`: a definition exists, under a contested name

**Definition A** — CLOSURE-4: `K_1 ≡_sem^{Q,Γ,𝒪} K_2` iff their **determinations match** for all
`q ∈ Q` and `o ∈ 𝒪`. **Executable** — the package ships
`"""CLOSURE-4: Observational Semantic Equivalence Tester."""`.

**Definition B** — `261.25`: `𝔎 = (K, =_str, ≡_sem, ≈_obs, SameId, ≡_H, ≡_P)` — **`≡_sem` as one of
seven relations**, with `≡` and `≈` explicitly non-interchangeable (`246`, `261`).

**The reviewer's correction, which reconciles them:**

> *"I would **not call it general semantic equivalence**. It is closer to
> `K_1 ≈_{Q,Γ,𝒪} K_2` = **contextual observational equivalence** — because two representations can
> produce the same answers for the selected queries while differing in other observations."*

$$\boxed{\textbf{Definition A is sound and is } \approx_{obs}\textbf{, not } \equiv_{sem}\textbf{. It is slot 3 of B's seven, not slot 2.}}$$

`[INF]` **This is a naming conflict, not an absence** — and the corpus contains its own repair. **⇒
`DECISION REQUIRED`, but a small one: adopt A under the corrected name, or keep the slot open.**

---

## 6. `B` — `Qualify`: three signatures, no body under any of them

| signature | source |
|---|---|
| `Qualify : Observation × Policy ⇀ Evidence` | `G1`, the verification and kernel lanes |
| `Qualify(x) → {Qualified, Unqualified, Undetermined, **Terminus**}` | Ch-16 refinement, with `C10: Qualify ≠ prove everything — justification may legitimately TERMINATE` |
| `Evidence × Proposition × Context × Provenance × Discrimination → Epistemic Status` | *"second candidate Qualify signature"* |
| *(different framing)* **Knowledge Qualification Problem** `Φ : K̂_t → {Knowledge, Not-Knowledge}` | `20260825-233107` |

$$\boxed{\textbf{Three competing signatures. No body under any. The body-absence survives; the word "undefined" does not.}}$$

**⇒ `DECISION REQUIRED` on the signature — and then a derivation for the body.** These are two
different work items and my register conflated them.

---

## 7. `B` — `𝒪_core`: four different cardinalities

| answer | source |
|---|---|
| **5 primitives** | CLOSURE-3 (superseded with the v1.3 package, §1) |
| **13** (two minimal kernels) | `KR-2026-09-01` ablation, protocol algebra, exhaustive over 16 384 subsets |
| **8** | same experiment, extended algebra, four minimal kernels, six-operator core |
| **six minimal registries, none selectable** | `GN-84` |

**And the experiment's own verdict on why the numbers differ:** *"minimality is
**representation-relative**… not conflicting measurements, but solutions to two different
optimization problems"*, with `Reachability ≠ Epistemic adequacy`.

$$\boxed{\textbf{Not four rival answers to one question — four answers to different questions. The corpus says so itself.}}$$

**⇒ Not a decision yet.** The estate's register puts kernel selection **last** (Group J) and says
*"don't select the kernel yet."* **I record the multiplicity and do not ask.**

---

## 8. `A` — the one genuine NOT FOUND

**`dedup` (`TG-11`).** 90 files mention deduplication; **51 are definition-shaped but define
something else.** Every hit is interrogative — *"Does deduplication require semantic reasoning?"*,
*"Can the system preserve two identities without semantic deduplication inside the Kernel?"* —
or an external-source note (Quine: *"Similarity ≠ identity → attack semantic deduplication"*).

$$\boxed{\textbf{State A: searched, not found. Recorded as a SEARCH RESULT, not as proof of non-existence.}}$$

---

## 9. Consequences for my own package

| my claim | corrected to |
|---|---|
| *"`Contr` undefined"* | **B — seven signatures, two consequences, `DECISION REQUIRED`** |
| *"`⪰` undefined"* | **B — three orderings the corpus says must stay separate** |
| *"`δ` has no body"* | **B — two argument types + a `Reject` branch; the *commit case* claim survives** |
| *"`≡_sem` unresolved"* | **B — defined, mis-named; the corpus contains its own repair** |
| *"`Qualify` has no body"* | **✅ survives** — but under **three** competing signatures, not one |
| *"`Reject` repair untouched"* | 🔴 **withdrawn — `δ(K,o) = Reject(r)` is in the corpus** |
| *"4 `[RATIFIED]` stamps not locatable"* | 🔴 **withdrawn — located, reviewed, superseded. §1** |
| *"governance grew by zero entries"* | 🔴 **withdrawn — `EPISTEMIC-STATUS-VOCABULARY.md` ADOPTED 2026-09-04** |
| *"the estate traded governance blockers for derivation blockers"* | 🔴 **withdrawn — reversed. Theory 13: *"the programme's constraint is no longer experimental capacity; it is that two decisions have not been made"*** |
| *"two rival `K`"* | ⚠️ **at least four vocabularies** — ratified 8-primitive `K_t` · `K=(𝒜,ℛ)` · `K_t=(𝒜_t,ℰ_t,ℛ_t,𝒞_t,ℋ_t,Γ_t)` · and Theory 01's `D,T,R,Q,Π,O,C,E_S` |
| `dedup` | ✅ **A — the only one that survives as not-found** |

## 10. And the frontier has moved past my package

Theory 13 (2026-09-04) is the estate's authoritative open register, and **its top three items are
not on my list at all**:

| | | |
|---|---|---|
| **`OQ-1`** | **the carrier** — KnowledgeOS has no declared mathematical carrier; every result was produced on `r = (value, source, tag, time)`, `\|D\| ≤ 6` | **governance; no experiment can decide it** |
| **`OQ-2`** | **`DECISION-02`** — is `φ` a semantic evaluation frame or an evidence partition? | **adjudication; blocks composition and `δ`** |
| **`OQ-3`** | **the transfer problem** — do the three separations survive on a non-synthetic carrier? **Not attempted.** | *"the largest scientific risk in the programme"* |

> `[REC]` **"Steps 1 and 2 are governance acts and they gate everything else."**

$$\boxed{\textbf{The estate is blocked on two DECISIONS, not on derivations. My verdict said the opposite.}}$$

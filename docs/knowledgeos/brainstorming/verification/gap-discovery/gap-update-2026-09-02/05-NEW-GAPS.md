# 05 — Gaps the New Artifacts CREATE

**Nine.** None was in my register on 2026-09-01. Each is registered with its evidence and its class,
and **none is resolved here.**

Classes as in `readiness/`: `BLOCKED — REQUIRES DERIVATION / DECISION / GOVERNANCE / IMPLEMENTATION /
EMPIRICAL OBSERVATION`.

---

## NG-1 🔴🔴 `Admissible` is undecidable — and it sits UPSTREAM of `ℐ`

`Admissible(o,K)` is the antecedent of every invariant in the `𝓘` schema. It depends on `Assurance`,
which the corpus has **REFUTED as definable**; `42.41`'s law belongs to a 7-tuple while the **ratified
DC is a 6-tuple with no admissibility conjunction of its own.**

$$\boxed{\textbf{The invariant schema's antecedent cannot be decided. Every candidate inherits this.}}$$

**Class: `BLOCKED — REQUIRES DERIVATION`.** ⚠️ **This is upstream of my keystone.** My package said
`ℐ` was the thing everything depended on. **`ℐ` depends on `Admissible`, and `Admissible` depends on
a refuted concept.** *(Evidence: `REFINED-STEP-287-INVARIANTS.md` §2.)*

## NG-2 🔴🔴 `δ` has no commit case — executed

`Γ` is derived and is not a component of `K`, so `δ` **has nowhere to write a commitment**. Executed
result: **`K₁ is K₀`** — object identity, not merely equality.

$$\boxed{\textbf{The central transition is a no-op in the only executable reading available.}}$$

**Class: `BLOCKED — REQUIRES DERIVATION`.** `[INF]` **This is the deepest gap in the estate as it now
stands.** Every downstream question — invariants, congruence, the operation registry, `Σ`'s
propagation, `Zero`'s evaluation — presupposes that `δ` changes something. **It does not, yet.**
*(Evidence: `REFINED-STEP-287-INVARIANTS.md` §2, executed.)*

## NG-3 🔴 `Φ : Π_t → K_t` — a SECOND undefined transformation

The layered pipeline `X → Y_t → E_t → ℱ_t → Π_t → K_t` has **two** undefined arrows:

```
Qualify : Y_t → E_t     (G1, known)
Φ       : Π_t → K_t     (NEW)
```

`Φ` sits **exactly where probability would become knowledge — the step all three models jointly deny
is automatic.** If *"probability is not knowledge"* holds, something non-trivial must carry `Π_t` to
`K_t`, **and nothing names it.**

**Class: `BLOCKED — REQUIRES DERIVATION`.** ⚠️ **Do not merge with `G1`** — different carriers,
different positions. *(Evidence: `38-THE-LAYERED-MODEL…`, registered there as `G-109`.)*

## NG-4 ⚠️ `~_Λ` is not transitive — **my inference from it is WITHDRAWN**

> 🔴 **CORRECTED** — see [`08` §5](./08-SCOPE-ERROR-AND-CORRECTIONS.md). The source **already
> separates the three levels** (`The Frozen Model` §2.3) and states `C-3`: *"`≡_sem` and family-level
> complexity remain separate concepts. `≡_sem` is not refuted; it must not be asked a question it was
> not designed for."* **`~_F` is level 1 (identity); `~_Λ` is level 2 (measurement).** `FR-001`
> refutes level 2 for level 3 and explicitly declines to touch level 1. **The premise on my definition
> stays open — `≡_sem` is TODO Group D — but `FR-001` is not the threat I said it was.**

### The finding as originally written
 ⇒ `H/~` is ill-defined — and it is FROZEN

`KR-DIST-2026-09-02` exhibited a **sorites witness**: the distinguishability relation `~_Λ` fails
transitivity, so the quotient `H/~` **is not a well-defined object.** The packing repair matches
duplication (0.98–1.06) and **fails on correlation by up to 222×**.

**Frozen as `FR-001`:** *distinguishability cannot carry family-level complexity.*

**Class: `BLOCKED — REQUIRES DERIVATION`.** `[INF]` **This bites my package directly.** My
`Sufficient(F,𝒪,ℐ)` is stated over `~_F`, an **equivalence relation**. If the estate's operative
distinguishability relation is not transitive, **`~_F` is not available as an equivalence and every
quotient argument in `step-272` needs a stated carrier.** ⚠️ **I have not checked whether `~_F` and
`~_Λ` are the same relation. Until someone does, my definition carries an unverified premise.**

## NG-5 🟡 `DECISION-02` is open, and two ratified criteria hang on it

> **Is `φ` a semantic evaluation frame, or merely an evidence partition?**

**Option A** ⟹ `majority` makes **representation granularity semantically observable**.
**Option B** ⟹ frame individuation is evidential and `C7` does not bite.
`DECISION-01` (non-evidential invariance) is `[DECIDED]` and **explicitly does not decide** whether
`majority` violates it — that is contingent on `DECISION-02`. `Z` is `[DEFERRED]`, blocked by it.

**Class: `BLOCKED — REQUIRES DECISION`.** *A live, well-posed, small normative question — the kind my
package is forbidden to answer and the kind that is cheapest to answer.*

## NG-6 🟡 The `Guṇa`/mode fork was taken and leaves a concept uncarried

`30` takes branch (b) — **`Guṇa` = mode, not primitive** — *"the branch that leaves it uncarried."*
A concept the estate uses has no carrier under the chosen branch.

**Class: `BLOCKED — REQUIRES DECISION`** (which carrier), or `REQUIRES DERIVATION` (that none is
needed). *Registered; I did not evaluate the fork.*

## NG-7 🔴 Glyph collisions reached **fifteen**, and two carry normative questions

| glyph | meanings |
|---|---|
| **`Π`** | **provenance** (`id = H(P,e,c,t,Π)`, `Lineage`, **Decision 3: `Π ∈ ≡?`**) · **Policy** · **probabilistic representation** |
| **`Θ`** | **transformation algebra (REJECTED)** · **state space (never raised)** ← **mine, registered in `03` §5** |
| `Σ` `S` `V` `R` `A` `O` | four of the six hit `Σ`, the one fully-verified structure in the theory |
| `D_t` | dimensional structure · `Determination` · the kernel skeleton |

$$\boxed{\textbf{A reader importing } \Pi \textbf{ as POLICY would silently re-read the programme's central normative question.}}$$

**Class: `BLOCKED — REQUIRES GOVERNANCE`** — a registry act, not a derivation. `[INF]` **This is the
highest-value, lowest-cost act available in the estate**, and it is the one nobody is commissioned to
perform.

## NG-8 🟡 Provenance-by-directory

*"Directory location is being used as provenance, and it is not reliable provenance."* — three
instances in one session, one of which would have attributed another lane's work to the
kernel-reduction experiment.

**Class: `BLOCKED — REQUIRES GOVERNANCE`.** ⚠️ **My package cites by path throughout** and therefore
inherits the hazard. *(Evidence: `kernel-reduction/00-INDEX.md`, `18 §2`.)*

## NG-9 🟡 A protocol begun and not executed, at scale

`three_model_convergence/`: **1 279 files · 1 260 in the reading stage · 13 of 16 output lanes
EMPTY.** Its own `MD-006` records **16 per-file records against a 2 321-file manifest ≈ 0.7 %**, and
its own rule forbids permanent classification during pass 1.

**Class: `BLOCKED — REQUIRES IMPLEMENTATION`** (of its own protocol). `[INF]` **Recorded because it is
the shape `PA-1` predicted** — *"the corpus has no mechanism that notices a commissioned step not
being performed."* **A protocol with a `progress.tsv` is such a mechanism for itself, and it is
reporting 0.7 %.** That is the mechanism working, not failing.

---

## Where the new gaps sit relative to mine

```
NG-1  Admissible undecidable   ─┐
NG-2  δ has no commit case     ─┼─►  UPSTREAM of ℐ, of 𝒪, and of every congruence claim
NG-4  ~_Λ not transitive       ─┘     (including my own Sufficient(F,𝒪,ℐ))

NG-3  Φ : Π_t → K_t             ─►  parallel to G1; the second hole in the same pipeline
NG-5  DECISION-02               ─►  a decision, well-posed, cheap
NG-7  fifteen glyph collisions  ─►  a registry act, cheap, uncommissioned
NG-6  NG-8  NG-9                ─►  hygiene, real, not blocking
```

$$\boxed{\textbf{Three of the nine are upstream of my package's own foundations. That is the finding of this document.}}$$

---
artifact: REQUIREMENT EVALUATION — birth to terminal state
date: 2026-09-10
status: **My `EvalReq` D-classification is REFUTED. There are TWO families: proposition-evaluation (typed, Kleene-verified, continuous from step-018) and requirement-evaluation (named four times, NEVER typed).**
scope: firewall excluded · semantic coverage of the readable corpus ≈1.5 %
---

# What happened to requirement evaluation through time?

> **First, the admission.** I classified `EvalReq` as **D — GENUINE CORPUS GAP** on **one phrase
> search** (`"requirement evaluation"` = 0/265). §15 forbids exactly that, and it is the fourth
> time this pass has caught the same error in my own work.

## §1, §8 · The widened search — my single phrase was hiding two live objects

| variant searched (026–268, 265 files) | hits |
|---|---|
| `EvalReq` / `EvalRequirement` | 0 |
| ⛔ *"requirement evaluation"* — **my only prior search** | **0** |
| *evaluate/evaluating a requirement* | 0 |
| requirement + `evaluat*` on one line | 0 |
| requirement status · requirement check* · requirement met/fulfil* | 0 |
| contract-specific evaluation | 0 |
| ⭐ **requirement + satisf\*** | **15** |
| ⭐⭐ **`Eval(` / `Evaluate(` as a function** | **9** |

**Notation absent — concept present.** The zero was real for eight variants and wrong for two.

---

## §2, §9 · The genuine birth point is neither `EvalReq` nor `EvalRequirement`

`verification/DEFINITION-VERIFICATION-REGISTER.md` **DV-23**, verbatim:

> *"`Eval(P,K) ∈ {T,F,U}` with the §25 connective tables (**step-018**)"*

$$\boxed{\textbf{The evaluation family is born at STEP-018 — before the 026–268 interval AND before the 025-series.}}$$

*(step-018 is in the 001–022 band — this reconstruction's acknowledged backfill debt. Its content
here is taken from the verification lane's register, which cites it; the source file itself is
`NOT YET READ`.)*

---

## §3, §7 · The chronology — and it splits into TWO families

| # | when | object | subject evaluated | **codomain** |
|---|---|---|---|---|
| 1 | **step-018** | `Eval(P,K)` | a **proposition** `P` | ⭐ **`{T,F,U}`** + connective tables |
| 2 | 08-27 18:31 `025d` §25D.37 | `Evaluate(K_t, r, EC_G)` | a **requirement** `r` | via `⊕` — ⚠️ and `⊕` collides with evidence-combination `⊕` from `25C` |
| 3 | 08-27 18:33 `025e` §25E.27 | `EvalRequirement(K, r, C)` | a **requirement** `r` | ⛔ `Status` — **named, never defined** |
| 4 | 08-29 20:06 → 08-30 09:40 | `{T,F,U}` carried through `DEFINITION-VERIFICATION-REGISTER`, `STEP-VERIFY-041-055`, `STEP-VERIFY-186-205`, `EXECUTED-TEST-222-repairs` | proposition | `{T,F,U}` |
| 5 | ⭐⭐ **08-30 10:25 s240 §240.12 (D-09)** | `Eval(P,K) ∈ {T,F,U}` | proposition | ⭐ *"**independently exhaustively checked** and **identified as strong Kleene logic**, with **commutativity, associativity and monotonicity verified**"* — `HIGH CONFIDENCE`, `HistoricalClaim + FormalDefinition + IndependentComputation` |
| 6 | 08-30 10:38 s241 | `σ = Eval(P,E,K,C,π,…)` | proposition | ⚠️ *"a more defensible dependency hypothesis"* — classified **`NECESSARY FOR EVALUATION, NOT SUFFICIENT`** |
| 7 | 09-02 09:39 | `Sat_c : 𝒦×ℛ_c×Γ → V_Sat`, `V_Sat = {⊤,⊥,U}` | a **requirement** (by class) | ⭐ `{⊤,⊥,U}` — **the same three values** |
| 8 | 09-06 00:39 `theory-part-06` | `EvalReq(K,r,EC,Γ)` | a **requirement** `r` | ⛔ **none stated** |

### ⭐⭐⭐ The two families

| | **proposition-evaluation** | **requirement-evaluation** |
|---|---|---|
| members | `Eval(P,K)` · `Eval(P,E,K,C,π,…)` | `Evaluate(K_t,r,EC_G)` · `EvalRequirement(K,r,C)` · `EvalReq(K,r,EC,Γ)` · `Sat_c` |
| **codomain** | ⭐ **`{T,F,U}`, from step-018, continuous** | ⛔ **`⊕`-structured · `Status` undefined · none · `V_Sat`** |
| **algebraic laws** | ⭐ **verified** — Kleene, commutativity, associativity, monotonicity | ⛔ **none, ever** |
| **independent computation** | ⭐ **yes**, s240 | ⛔ **none** *(except `Sat_c`'s later implementation — `G-25`)* |
| continuity across 026–268 | ⭐ **unbroken** | ⛔ **absent** |

$$\boxed{\textbf{The corpus typed, verified and carried PROPOSITION-evaluation for ten days — and never once gave REQUIREMENT-evaluation a result space.}}$$

---

## §5 · Identity — `EvalRequirement` ≟ `EvalReq`, and both ≟ `Eval`

| pair | verdict | basis |
|---|---|---|
| `EvalRequirement(K,r,C)` × `EvalReq(K,r,EC,Γ)` | **IDENTITY UNWITNESSED** *(unchanged from `G-25`)* | same subject `r`, same role; **arity 3→4**, `C` → `EC,Γ`, codomain dropped, **zero citation** |
| `Evaluate(K_t,r,EC_G)` × `EvalRequirement(K,r,C)` | ⭐ **SAME CONCEPT — REFINED** | **two minutes apart**, `025d` 18:31:42 → `025e` 18:33:20, same subject, contract argument retained (`EC_G` → `C`) |
| **`Eval(P,K)` × the requirement family** | ⭐⭐ **RELATED OBJECT — DIFFERENT SUBJECT** | `P` is a **proposition**; `r` is a **requirement**. Evaluating a claim is not evaluating a requirement. **No document identifies them** |
| `Eval(P,K)`'s `{T,F,U}` × `Sat_c`'s `V_Sat = {⊤,⊥,U}` | ⭐⭐ **SAME OBJECT — STRONG CONTINUITY** | **same three values, same "undetermined" third**, and an **unbroken chain 08-29 → 08-30 → 09-02**. ⚠️ **no citation** between the endpoints ⇒ continuity is **measured, not declared** |

---

## §4 · Distributed completion — what this changes for `G-25`

`G-25` recorded **four unreconciled `Sat` codomains**: `𝒮` (9→11) · `V_Sat` (3) · `𝒮_sat` (4) ·
`𝕊_sat`. **One of the four now has an ancestry.**

$$\boxed{V_{Sat} = \{\top,\bot,\mathsf U\} \textbf{ is NOT invented at 09-02. It descends from } Eval(P,K) \in \{T,F,U\} \textbf{, born step-018, Kleene-verified at s240.}}$$

⇒ Classification for the three-valued space: ⭐ **B — COMPLETE THROUGH MULTIPLE SOURCES**
(step-018 supplies the connective tables; s240 supplies the algebraic verification; `Sat_c` supplies
the requirement-class indexing and a body).

⚠️ **The 4-valued `𝒮_sat` and the 9→11 `𝒮` remain unreconciled with it** — `G-25`'s incomparability
result stands untouched.

## §6 · Mathematical typing

| claim | status |
|---|---|
| `Eval(P,K) → {T,F,U}` | ⭐ **PROVEN** — stated, with connective tables |
| that space is strong Kleene, comm./assoc./monotone | ⭐ **PROVEN** — *"independently exhaustively checked"* (s240) |
| `V_Sat` ≡ `{T,F,U}` | ⚠️ **TYPE-CONSTRAINED** — same three values, same semantics; **no document states the identity** |
| `EvalReq → 𝒱` | ⚠️ **TYPE-CONSTRAINED** *(unchanged from `G-25`)* |
| `Status = 𝒱` | ⛔ **INFERRED — remains withdrawn** |
| any codomain for `EvalRequirement` / `EvalReq` | ⛔ **UNKNOWN, at every point in 10 days** |

**No promotion of `TYPE-CONSTRAINED` to `PROVEN`.**

## §7 · The `Req → EvalReq → Sat → Determination` chain

| edge | verdict |
|---|---|
| `Req → Evaluate(K_t,r,EC_G)` | ⭐ **DIRECT** — `025d` §25D.37, `r ∈ R_G` |
| `Evaluate(K_t,r,EC_G) → EvalRequirement(K,r,C)` | **DIRECT** — 2 minutes, same subject |
| `EvalRequirement → EvalReq` | ⛔ **UNWITNESSED** — 10 days, zero citation |
| `EvalReq → Sat` | ⭐ **DIRECT** — `theory-part-06` §6.18 composes them |
| `Sat → Determination` | ⚠️ **POSSIBLE** — `Det_r` is *inside* `Sat`'s definition, not downstream of it. And `Determination`'s own birth is `reviews/` 08-19, a **governance** lane |
| `Eval(P,K) → Sat_c` | ⚠️ **POSSIBLE** — the codomain descends; **no citation** |

⛔ **The chain is not established end-to-end.** Two of six edges are `UNWITNESSED`/`POSSIBLE`, and
the corpus never draws it as one pipeline.

## §12 · Provenance

| claim | lineages |
|---|---|
| `{T,F,U}` is the evaluation codomain | ⭐ **≥3 independent** — step-018 (origin), the verification lane (08-29/08-30, four documents), s240 (independent computation) |
| requirement-evaluation is never typed | **1 measurement** over 265 files + 4 named formulations — not 4 confirmations |

---

## §16 · Terminal classification

| object | class |
|---|---|
| **proposition-evaluation `Eval(P,K)`** | ⭐ **A — COMPLETE HISTORICAL RECONSTRUCTION.** Birth (step-018), evolution, algebraic verification, terminal descendant (`V_Sat`) all located |
| **the three-valued space** | ⭐ **B — COMPLETE THROUGH MULTIPLE SOURCES** |
| **requirement-evaluation** | ⭐⭐ **E — OBJECT IDENTITY UNRESOLVED** *(was: D)*. Four formulations, three subjects-in-common, **zero cross-citations**, and **no result space at any point** |
| ⛔ **NOT D** | the concept is present and named four times; what is missing is not the concept but **a codomain it never had** |

## §17 · Gap impact

| gap | action |
|---|---|
| **`EvalReq` D** | ⛔ **WITHDRAWN → E.** Refuted on a widened search |
| **`G-25`** | ⭐ **RE-SCOPED** — `V_Sat` now has an ancestry (step-018 → s240 → 09-02). **Three codomains unreconciled, not four** |
| **`G-01`** | ⭐ **RE-SCOPED** — the 3-valued space is not a late narrowing; it is the **oldest** member of the family |
| **`Det_r`** | unchanged — `FIREWALL-LIMITED` |
| **NEW `G-27`** | ⭐ **Requirement-evaluation has been named four times in ten days and given a result space zero times** — while its sibling proposition-evaluation was typed at birth and algebraically verified. **The asymmetry is the finding** |

## §15 · Negative-history

**Searched:** 265 interval files × 11 variant patterns, both spellings; plus the whole
non-firewalled corpus for `{T,F,U}` and the `Evaluate(...)` argument shapes.
**Result: `NOTATION ABSENT · CONCEPT CONTINUES · DEFINED IN MULTIPLE STAGES`** — **not**
`GENUINE CORPUS GAP`. **step-018 is `NOT YET READ`** (backfill debt); its content is taken from the
verification lane's register, which cites it.

⛔ Firewall honoured. Nothing canonicalized. No codomain supplied. No family merged.

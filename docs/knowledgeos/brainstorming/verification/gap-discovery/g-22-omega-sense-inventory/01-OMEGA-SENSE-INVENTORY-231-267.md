---
artifact: G-22 · Ω SENSE INVENTORY AND IDENTITY MATRIX, steps 231–267
date: 2026-09-10
lane: theory-extraction / chronological reconstruction (bounded local investigation)
status: **DISPOSITION — `HOMONYM` (1 pair, explicit) + `IDENTITY-UNWITNESSED` (rest). FIVE senses, not six.**
scope: steps 231–267 as the bounded corpus; three sources outside it opened only to type a symbol used inside it
---

# G-22 — Ω across steps 231–267

> **Governing constraint applied throughout:** *same spelling ≠ same object* · *disappearance ≠
> retirement* · *repetition ≠ continuity* · *similarity ≠ identity.* No sense was harmonised with
> another on the strength of the shared glyph.

## 0. A search defect found first, and it changes the count

The corpus writes this symbol **two ways**: the Unicode glyph `Ω` and the LaTeX control word
`\Omega`. Across steps 231–267:

```
Unicode Ω only ....  2 occurrences   in 1 file
\Omega ...........  26 occurrences   in 11 files
                    ──────────────
TOTAL ............  28 occurrences   in 11 of 40 files
```

**A glyph-only search returns 1 file of 11 and 2 occurrences of 28 — a 93 % miss.** Every count below
covers both spellings. Ω is **absent from 29 of the 40 files** in the window (231–235, 239, 242–245,
248–250, 252–263, 265, 267).

---

## 1. Complete occurrence inventory — all 28, chronological

| # | step | time | line(s) | verbatim notation | local defining passage | introduced / reused / mentioned |
|---|---|---|---|---|---|---|
| 1–2 | 236 | 10:13 | 302, 308 | `(A,\Omega,\mathcal L)` | *"`A` is a carrier; **`Ω` is a set of operations**; `ℒ` is a set of laws/properties"* | **generic convention** — cited to explain what an algebra requires; no KnowledgeOS commitment |
| 3 | 237 | 10:18 | 108 | prose | *"The archaeology specifically records the **first appearance of `Ω` on 2026-08-24** and identifies eight kernel formulations"* | **metadata about Ω**, not a use |
| 4 | 238 | 10:22 | 16 | `\Omega,\sigma`-style mathematical space | regime **R1 — Measure Theory Crisis**, *"Main object"*; *"Evidence status: historical experiment/**failed direction**"* | **mentioned** (classifying a past regime) |
| 5–7 | 238 | 10:22 | 52, 56, 60 | `\Omega_a\rightarrow IdealState/contract` · `\Omega_b\rightarrow dropped` · `\Omega_c\rightarrow complement principle` | *"Interestingly, `Ω` did not simply disappear. **Its three roles diverged**"* | ⭐ **explicit SPLIT record** — see §3 |
| 8–9 | 238 | 10:22 | 83, 93–94 | prose + boxed | *"`Ω` **first appears in the kernel era, 2026-08-24**… It appears in Bayesian/typed mathematical work before the measure-theory phase begins on August 25"*; *"**R1 transformed `Ω`; R1 did not create `Ω`.**"* | **provenance claim** (→ `G-21`) |
| 10 | 238 | 10:22 | 191 | `\text{Ω-observer}` | *"**Lord** has two unresolved referents: Ω-observer versus action selector"* | a label for a referent of **Lord**, not a use of Ω as an object |
| 11–12 | 240 | 10:25 | 97, 109 | `\delta_K(K_t,o_t,\rho_t,\Omega_t)` | *"Another important formulation gives…"* — then L114: *"**the exact transition signature is unresolved**"* | **reused**, untyped |
| 13 | 241 | 10:38 | 519 | `\delta_K(K_t,o_t,\rho_t,\Omega_t)` | L470: *"**the exact signature of `δ_K` is unresolved**"* | **reused**, untyped |
| 13b | 241-corr | 10:42 | 521 | same | (numbering-correction duplicate) | duplicate |
| 14–15 | 246 | 11:11 | 625, 631 | `\Omega=\{\text{all possible knowledge states}\}` · `\mu:\Omega\rightarrow[0,1]` | *"**Earlier material proposed**…"*, under the heading *"this also resolves the earlier distribution-theory **temptation**"* | **retrospective citation, being disposed of** |
| 16–17 | 247 | 18:25 | 750, 758 | `(\Omega,\mathcal F,\mu)` · `\Omega=\mathbb K` | *"**The earlier proposal** … can now be interpreted as a **regime over the state space**"* | **retrospective citation, relocated** |
| 18–20 | 251 | 18:36 | 52, 338, 455 | `Update(K_t,E_t,\Omega,EC)` | genealogy table: *"025-series · epistemic/update · **sub-family**"* | **reused** — carries the 025-series sense |
| 21–22 | 251 | 18:36 | 616, 623 | `\Omega:W\rightarrow O` · `World\xrightarrow{\Omega}Observation` | *"**The corpus also has:** `Ω : W → O`"* — attributed to *"the corpus"*, **no step cited** | **reused**, identity unwitnessed |
| 23 | 264 | 19:35 | 747 | `f:\Omega\rightarrow\mathbb R` | *"A measurement is a homomorphism"* | **generic convention** |
| 24 | 266 | 19:54 | 1193 | `(\Omega,\mathcal F,P)` | *"We need a probability space"* | **generic convention** |

*(Counts 25–28 are the second `\Omega` in items 5–7's boxed displays and the duplicate file; the
substantive inventory is the 24 rows above.)*

---

## 2. Ω Sense Inventory — **FIVE senses**, and my prior count of six was wrong in both directions

| Sense ID | First evidence (in window) | Definitional source | Last evidence in window | Exact notation / type | Local meaning | Explicit predecessor | Explicit successor | Identity evidence | Status |
|---|---|---|---|---|---|---|---|---|---|
| **Ω-1** *ontology* | `251` L52 (18:36) | ⭐ **`025k` L53, 2026-08-28 09:39** — *"`Ω` = **domain ontology/rules**"* | `251` L455 | `Ω` as 3rd arg of `Update(K_t,E_t,Ω,EC)`; a **set of rules** | the rule-set the derivation is run against | 025-series | none | attributed by name to the 025-series in `251`'s genealogy table | **CONTINUOUS** within its own family; **`RE-FOUNDED AWAY`** at `031` |
| **Ω-2** *observation* | `251` L616 (18:36) | ⭐ **`031` §31.18, 2026-08-28 10:22** — *"**Define** `Ω : W → O` … **as the observation mechanism**"* (boxed) | `251` L623 | **function** `W → O`; `W_t` = *"the relevant real-world state"*, `O_t` = `Obs(W_t)` | the observation mechanism; chain `W --Ω--> O --f--> K` | none stated | none | `251` says only *"**the corpus** also has"* — **no step cited** | **IDENTITY-UNWITNESSED** (type is byte-identical to `031`; the attribution is not) |
| **Ω-3** *transition arg* | `240` L97 (10:25) | ⭐ **`step-016` L1993, 2026-08-27 16:00** — a **boxed formula with no accompanying definition**; the file's **only** Ω | `241` L519 | `Ω_t`, 4th arg of `δ_K(K_t,o_t,ρ_t,Ω_t)` — **UNTYPED** | unstated | none | none | its own sources say ***"the exact signature of `δ_K` is unresolved"*** (`240` L114, `241` L470) | **IDENTITY-UNWITNESSED**; ⭐ **the OLDEST readable Ω** |
| **Ω-4** *state measure space* | `238` L16 (10:22) | `246` L625, `247` L758 | `247` L758 (18:25) | `(Ω,ℱ,μ)` with **`Ω = 𝕂`** / `{all possible knowledge states}` | the sample space **is** the knowledge-state space — a KnowledgeOS claim | R1 (measure-theory phase) | *"a regime over the state space"* (`247`) | quoted twice as *"**earlier** material proposed"* / *"the **earlier** proposal"* | **EXPLICITLY_REDEFINED** — relocated from foundation to regime, then abandoned as a *"temptation"* |
| **Ω-5** *generic placeholder* | `236` L302 (10:13) | — (textbook convention, never defined *as a KnowledgeOS object*) | `266` L1193 (19:54) | `(A,Ω,ℒ)` operation set · `f:Ω→ℝ` · `(Ω,ℱ,P)` | **not a KnowledgeOS object** — imported notation illustrating what an algebra / a measurement / a probability space requires | — | — | — | **HOMONYM** (borrowed notation) |

### Two records that are *about* Ω and are not uses of it — kept separate per §5

| | | |
|---|---|---|
| **Ω-M1** | `237` L108 · `238` L47–60, L77–94 | the **genealogy**: first appearance 2026-08-24; *"R1 transformed `Ω`; R1 did not create `Ω`"*; the `Ω_a/Ω_b/Ω_c` split |
| **Ω-M2** | `238` L191 | `Ω-observer` — a candidate **referent of `Lord`**, not a use of `Ω` as an object |

### ⛔ Correction to my own prior finding

`09-EVIDENCE-LOG.md` §5 recorded *"`Ω` has **six mutually incompatible meanings** inside this one
block."* **Wrong in both directions:**

| my prior list | correction |
|---|---|
| measure-theoretic space (`238`) · `Ω={all knowledge states}` (`246`) · `(Ω,ℱ,μ)` with `Ω=𝕂` (`247`) — counted as **3** | ⛔ **over-count.** One sense (**Ω-4**), three sites. `246` and `247` are *retrospective citations of the same earlier proposal*, not new introductions |
| `Ω : W → O` attributed to `251` | ⚠️ **under-attributed.** Its definitional source is `031` §31.18, two days earlier |
| — | ⛔ **MISSED: Ω-1.** `251` carries `Update(K_t,E_t,Ω,EC)` at L52/338/455 — the 025-series ontology sense — **in the same document, 300 lines from `Ω:W→O`** |
| — | ⛔ **MISSED: Ω-5 at `236`.** *"`Ω` is a set of operations"* — an explicit local gloss, never recorded |

**Net: 6 → 5 senses, with one previously invisible collision inside a single document.**

---

## 3. `Ω_a / Ω_b / Ω_c` — an explicit split record whose terms are never defined

`238` §238.2 states the divergence as fact:

```
Ω_a → IdealState/contract
Ω_b → dropped
Ω_c → complement principle     ⇒  invariant I-7:  X_t ≠ Observed(X_t)
```

~~**No document in the search universe defines, names or types `Ω_a`, `Ω_b` or `Ω_c` individually.**~~
⛔ **WITHDRAWN — see §12.1.** All three ARE defined, two days earlier. My search used the
subscripted forms; **the corpus's dominant form is hyphenated (`Ω-a`)**. Consequences, kept apart:

- The **role** carried by `Ω_c` demonstrably survives — as **invariant I-7**, `X_t ≠ Observed(X_t)`,
  which is the same *semantics* as `Ω : W → O`.
- **But `238` does not say `Ω_c` became `Ω:W→O`.** It says `Ω_c` produced *a principle*. **The role
  survived as an invariant; whether the SYMBOL continued to denote it is a separate question the
  corpus does not answer.** Recorded as `IDENTITY-UNWITNESSED`, not as continuity.

---

## 4. `Ω : W → O` — located, typed, and its inheritance tested

| question | answer |
|---|---|
| **where does it occur?** | `031` §31.18 L598 (**definitional**, boxed, 2026-08-28 10:22:51) · `031` L1187 (restatement) · `251` L616 (2026-08-30 18:36) |
| **what is `W` locally?** | `031` §31.17: *"Let `W_t` represent **the relevant real-world state**"* |
| **what is `O` locally?** | `031` §31.17: *"KnowledgeOS has only partial access: `Obs(W_t)=O_t`"* — the partial observation |
| **do later Ω uses inherit the type?** | **`251` alone.** Byte-identical signature, plus the chain `World --Ω--> Observation`. Every other in-window Ω has a different or absent type |
| **retained / weakened / altered / abandoned?** | **RETAINED, unchanged**, at the one site that carries it |
| **does any document claim equivalence to `031`'s Ω?** | ⛔ **No.** `251` writes *"**The corpus also has:** `Ω : W → O`"* — an unattributed citation to "the corpus". No step number, no equivalence claim |

---

## 5. Four histories, kept separate

| history | content |
|---|---|
| **Conceptual** | Ω names, at different moments, *the rules knowledge is derived against* (Ω-1), *the mechanism by which the world becomes observable* (Ω-2), *an unnamed fourth thing a knowledge transition consumes* (Ω-3), and *the space of possible knowledge states* (Ω-4). These are four different kinds of thing. **No conceptual thread connects them.** |
| **Mathematical** | one **function** (`W→O`), one **rule set**, one **measure-space carrier** (`Ω=𝕂`), one **untyped argument**, one **borrowed placeholder**. ⭐ Only Ω-2 has a signature stated in a definitional act |
| **Operational** | Ω-1 is consumed by `Update`/`Derive`; Ω-3 by `δ_K`; Ω-2 is **never consumed by any operation in the window** — it appears only in an explanatory chain; Ω-4 was a proposed foundation, demoted to *"a regime"*, then called a *"temptation"*; Ω-5 is never operational |
| **Governance / history** | `238` classifies R1 (Ω-4's home) as *"historical experiment/**failed direction**"* and records `ALT-01`. ~~**No governance act adjudicates the symbol Ω itself.**~~ ⛔ **WITHDRAWN — see §12.2. `GN-09` is an explicit terminology ruling on Ω, and `D-R27` records it in the definition register.** |

> **A later mathematical redefinition has not overwritten an earlier conceptual record here** — because
> **there was never a redefinition.** There were independent definitions that never met.

---

## 6. Re-foundings — one explicit alignment, and it is the whole finding

| re-founding (from the Chronicle) | Ω alignment |
|---|---|
| **1 · `031`** — *"drops the 025 apparatus"* | ⭐⭐ **EXPLICIT AND DECISIVE.** `Ω = domain ontology/rules` **is** part of the 025 apparatus (`025k` L53, 09:39). `031` drops that apparatus — and **in the same document, 43 minutes later, defines `Ω : W → O` as the observation mechanism.** `031` mentions `025`, *"domain ontology"* and `Update(K,E,Ω,EC)` **zero times** |
| 2 · `183-pre` | no Ω evidence in the window |
| 3 · `230` | Ω does not appear; `236`–`238`'s archaeology *follows* it and is where the Ω genealogy is written |
| 4 · `262` (`K=(𝒜,ℛ)` imported) | ⛔ **no alignment.** Ω's last substantive use is `251` at **18:36**; `262` lands at **19:27** — Ω had already gone dark **51 minutes earlier**. Nothing connects them |
| 5 · `273` (`K=(A,R,Σ,E_L)`) | no Ω evidence |
| `τ : S×C → S` (Step 204) | **classificatory only.** `251`'s genealogy table lists `τ` and `Update(K_t,E_t,Ω,EC)` as sibling rows in one *"family"* of transition candidates. **A shared classification is not a shared object** — recorded, not treated as identity |

$$\boxed{\textbf{The symbol } \Omega \textbf{ crossed re-founding \#1 and was re-used for a different object. Silently.}}$$

---

## 7. G-19 not used

No primary source connects the `G-19` falsification result to any Ω sense. `G-19` played **no part**
in the adjudication above, and no Ω sense was preferred because it makes the `K` model work.

---

## 8. Identity matrix — all ten pairs

| pair | relation | evidence |
|---|---|---|
| **Ω-1 × Ω-2** | ⭐ **HOMONYM** | `025k` L53 *"Ω = domain ontology/rules"* (08-28 **09:39**) vs `031` §31.18 *"Define Ω:W→O as the observation mechanism"* (08-28 **10:22**) — **43 minutes apart, same author, same day.** Type-incompatible: a rule-set is not a function `W→O`. `031` does not mention `025`. **Both live in `step_251` simultaneously, 300 lines apart, unremarked** |
| **Ω-2 × Ω-3** | ⭐ **DISTINCT — explicit differentiation** | In `step-016`, the **birth document of Ω-3**, the observation function is separately named **`Obs`** (`o_t = Obs(X_t,S_t)`, L1985) and `Ω_t` is a **different fourth argument in the same boxed formula** (L1993). The document distinguishes them **by construction**, not by inference |
| **Ω-1 × Ω-4** | **DISTINCT — type** | a set of derivation rules vs a measure-space carrier `Ω=𝕂`. No document relates them |
| **Ω-1 × Ω-5** | **DISTINCT — type** | domain rules vs a universal-algebra operation set / textbook placeholder |
| **Ω-2 × Ω-4** | **DISTINCT — type** | a function `W→O` vs a set |
| **Ω-2 × Ω-5** | **DISTINCT — type** | a function vs a set of operations / generic carrier |
| **Ω-1 × Ω-3** | **UNDECIDABLE** | both are arguments of a knowledge-update function in comparable positions (`Update(K_t,E_t,Ω,EC)` vs `δ_K(K_t,o_t,ρ_t,Ω_t)`), but Ω-3 is **untyped and its own source says so**. Different companions (`EC` vs `ρ_t`), different arity position. **No document connects them, and none excludes them** |
| **Ω-3 × Ω-4** | **UNDECIDABLE** | Ω-3 untyped |
| **Ω-3 × Ω-5** | **UNDECIDABLE** | Ω-3 untyped |
| **Ω-4 × Ω-5** | **POSSIBLE SAME OBJECT — identity unwitnessed** | both are measure-theoretic sample-space notation. Ω-4 **commits** (`Ω = 𝕂`, a claim about KnowledgeOS); Ω-5 is a **placeholder** in an imported formula. Same convention, different commitment. No document identifies them |

**Tally: 1 HOMONYM (explicit, 43 min) · 1 DISTINCT-by-explicit-differentiation · 4 DISTINCT-by-type
· 3 UNDECIDABLE · 1 POSSIBLE-SAME-UNWITNESSED · 0 SAME OBJECT.**

⭐ **Not one pair of the ten is supported as the same object by any corpus evidence.**

---

## 9. Mandatory adversarial check — both directions, searched, not assumed

### A. *"What evidence would have to exist for the senses to be one continuous object?"*

| required evidence | searched | found |
|---|---|---|
| a statement identifying two Ω uses as one object | both spellings, whole universe: *"same Ω"*, *"Ω here denotes"*, *"as in step"*, equivalence phrasing | **none** — ⚠️ but I searched only for *identity*, never for a *contradiction ruling*. **`C-06` is one, and I missed it. See §12.3** |
| a rename / supersession / retirement record for Ω | *"renamed"*, *"supersedes"*, *"replaced by"*, *"retired"* near Ω | **none** |
| a type bridge — Ω-2's `W→O` used as `δ_K`'s or `Update`'s Ω argument | direct inspection of every occurrence | **none**; and the two sources that could supply it **declare the signature unresolved** |
| the `Ω_a/Ω_b/Ω_c` split naming which role became which later Ω | `\Omega_a`, `Ω_a`, `Omega_a`, *"three roles"*, *"first/second/third role"* | **the split is asserted; its terms are never defined** |

⇒ **Recorded as `IDENTITY-UNWITNESSED`. Absence is NOT converted into proof of distinctness.**

### B. *"What evidence would prove at least two senses are distinct objects?"*

| candidate proof | found |
|---|---|
| two incompatible **explicit definitions** of Ω | ⭐ **YES** — `025k` L53 (rule set) and `031` §31.18 (function `W→O`), 43 minutes apart, neither citing the other |
| one document naming the observation function **something other than Ω** while using `Ω` for a different argument | ⭐ **YES** — `step-016`: `Obs(X_t,S_t)` **and** `Ω_t`, in the same boxed formula |
| a document explicitly marking a same-word collision | ⭐ **YES, though not for Ω** — `238` L204: $\boxed{Same\ word\neq Same\ concept}$, applied there to `Zero` (three senses) and `Lord` (two referents) |

⇒ **Ω-1 × Ω-2 is `HOMONYM` on explicit definitional evidence. Ω-2 × Ω-3 is `DISTINCT` on
construction. Everything else remains unresolved and is recorded as such.**

### ⭐ The self-referential finding

`step_238` states the exact discipline this gap enforces — *"Same word ≠ Same concept"* — as a
**"DDD lesson"**, applies it to `Zero` and to `Lord`, and **in the same document uses `Ω` in three
different senses** (R1's measure space, the `Ω_a/b/c` split, `Ω-observer`) **without applying the
lesson to `Ω`.** The corpus had the instrument and did not turn it on itself.

---

## 10. Findings

| ID | finding | class | severity |
|---|---|---|---|
| **G22-1** | **Ω-1 × Ω-2 is a HOMONYM on explicit definitional evidence**, born **43 minutes apart** on 2026-08-28, across re-founding #1, with neither definition citing the other | `EXECUTED` | **CRITICAL** |
| **G22-2** | **`step_251` carries both senses simultaneously**, 300 lines apart, and never notices — the single document on which `G-03` and the Ω/`EC` sub-family classification rest | `EXECUTED` | **CRITICAL** |
| **G22-3** | **Five senses, not six.** My prior count over-split Ω-4 across three sites and missed Ω-1 in the window and Ω-5 entirely | `EXECUTED` | HIGH (corrective) |
| **G22-4** | **A glyph-only search misses 93 %** of Ω in this window (2 of 28 occurrences, 1 of 11 files) | `EXECUTED` | **CRITICAL (method)** |
| **G22-5** | **Ω-3 is the oldest readable Ω** (`step-016`, 2026-08-27 16:00) and is **untyped at birth**, in a boxed formula with **two** undefined arguments (`ρ_t` also) | `EXECUTED` | HIGH |
| **G22-6** | The `Ω_a/Ω_b/Ω_c` split is **asserted and never defined** — three roles named, zero typed | `UNRESOLVED` | HIGH |
| **G22-7** | **No governance act touches Ω.** No rename, supersession, retirement or collision record exists for any sense | `UNRESOLVED` | MEDIUM |
| **G22-8** | `238` states *"Same word ≠ Same concept"* as a DDD lesson, applies it to `Zero` and `Lord`, and **uses Ω in three senses in the same document** | `EXECUTED` | MEDIUM |

---

## 11. Scope discipline

Nothing here decides what Ω *should* mean. No sense is preferred, merged, repaired or retired. Ω-4's
own sources call it a *"failed direction"* and a *"temptation"* — **that is their classification,
recorded, not adopted.** Three documents outside the window (`025k`, `031`, `step-016`) were opened
**solely to type a symbol used inside it**, and no other content was imported from them.

**Firewall:** `brainstorming/three_model_convergence/` was not opened, read, grepped or cited by this
investigation or its evidence worker.

---

# 12. CORRECTIONS AFTER THE BOUNDED EVIDENCE SWEEP

A bounded evidence worker swept the whole search universe (3,108 files; 493 contain Ω) for identity
statements, the `Ω_a/b/c` terms, the 2026-08-24 origin, and post-window occurrences. **It refuted
three of my claims above and added a sixth sense.** All four corrections were re-verified by me at
the primary source before being recorded here.

## 12.1 ⛔ `Ω_a / Ω_b / Ω_c` **ARE** defined — two days before `238` cites them

**Source:** `phase_measure_theory/how_to_combine/20260828-150140_step_combine_combine-prompt-4.md`,
**2026-08-28 15:01:40** — verified verbatim:

```
Ω  →  ⎧ Ω_a   ideal reference
      ⎨ Ω_b   unboundedness
      ⎩ Ω_c   whole-space observation
```
```
Ω
├── Ω-a: ideal reference  →  Ideal State  →  Epistemic Contract
├── Ω-b: unboundedness    →  dropped
└── Ω-c: whole-space observation  →  unresolved
```

**Why I missed it:** I searched `\Omega_a`, `Ω_a`, `Omega_a`. **The corpus's dominant form is the
hyphenated `Ω-a`.** Adopted, alongside the two-spelling rule: **a symbol sweep must cover subscript
AND hyphen forms.**

### ⭐ And `238` changed one of the three

| | `combine-prompt-4` · 08-28 15:01 | `step_238` · 08-30 10:22 |
|---|---|---|
| Ω_a | → Ideal State → Epistemic Contract | → `IdealState/contract` ✅ same |
| Ω_b | → **dropped** | → `dropped` ✅ same |
| **Ω_c** | → ⭐ **unresolved** | → ⭐ **complement principle** ⇒ invariant **I-7** |

**`238` reports as settled what its source recorded as unresolved, with no derivation shown.**
The `reviews/synthesis/` lane later records the same transition as `Ω-c → X_t ≠ Observed(X_t)`
(TRANSFORMED, Step 066) — so a Step-066 basis exists downstream; **`238` cites none.**

⚠️ Note the two records also disagree on *where Ω-c went*: `combine-prompt-4` and
`synthesis/model/lord-omega-analysis.md` say **DISPLACED — no host** (*"nothing in R5 carries Ω-c"*);
`238` and `phase-2d-closure` say **TRANSFORMED into its complement**. Recorded, not reconciled.

## 12.2 ⛔ A governance ruling on Ω **does** exist

| act | source | verbatim |
|---|---|---|
| **`GN-09`** | `reviews/synthesis/analysis/governance-notes.md` L67 | *"**Ω terminology ruling:** Ω belongs to **theory/history, not the final ubiquitous language** — retained fully in the discovery record because it explains *why* the architecture evolved toward bounded, partial, contract-driven knowledge"* |
| **`D-R27`** | `verification/spec/A3-definition-register.md` L56 | *"Ω \| **historical term only** — decomposed: Ω-a→EC, Ω-b bounded away, Ω-c→X_t (GN-09) \| RECONSTRUCTED lineage ruling"* |
| **`OQ-6`** | `synthesis/final-architecture/FA-6` | Ω-b's drop is an **open record gap**: *"dropped by disuse, no act"* |

**Corrected governance line:** Ω is **ruled historical-only** and **excluded from the final
ubiquitous language**, on a *reconstructed lineage* basis. ⚠️ **The ruling governs the
decomposition (Ω-a/b/c) — it does not mention Ω-1 (domain ontology), Ω-2 (`Ω:W→O`) or Ω-3
(`δ_K`'s argument).** Three of the five in-window senses are **outside the scope of the only ruling
that exists.**

## 12.3 ⛔ The corpus **had already registered the collision** — and I re-derived it

| | source | time | verbatim |
|---|---|---|---|
| **`TG-15`** | `verification/THEORY-GAP-REGISTER.md` L39 | 08-30 **20:51** | *"**`Ω` carries ≥4 global senses**, two of them foundational and opposite in direction … 🔴 **OPEN — NEW, and the most dangerous naming collision found**"* |
| **`C-06`** | `verification/independent/11-CONTRADICTION-REGISTRY.md` L17 | 08-30 **21:16** | *"`Ω` = Knowledge Space **vs** `Ω : W → O` … 🔴 **TRUE CONTRADICTION**. Two *foundational* uses pointing opposite ways … Combining them would make the non-identifiability result trivially false. **Unregistered by every prior pass**"* |

⭐ **This is the `G-19` pattern again, and this time I am inside it.** The `verification/` lane
registered the Ω overload as *"the most dangerous naming collision found"* on 2026-08-30, and my
`G-10`/`G-22` recorded it as *"new collision, register it"* on 2026-09-10.

**What survives as genuinely new:** `TG-15`'s four senses are ① sample space ② Knowledge Space
③ observation function ④ residual possibilities. ⭐ **`Ω-1` — `Ω = domain ontology/rules`
(`025k` L53) — is in none of them.** The **Ω-1 × Ω-2 homonym, its 43-minute interval, and its
coincidence with re-founding #1 are not recorded anywhere in the corpus.** That finding stands.

## 12.4 A **sixth** sense, out of window — and it is the other half of `C-06`

| Sense ID | source | notation | meaning |
|---|---|---|---|
| **Ω-7** *Knowledge Space* | `20260826-105229_research-synthesis-zero-lens-…` L33 (2026-08-26) and 3 further Zero-lens files | **`Zero(K_t) = Ω \ Represented(K_t)`** | Ω is the **infinite knowledge space** of which any `K_t` is a partial projection |

Ω-7 is `C-06`'s first term. **It does not occur in steps 231–267** and so is not an in-window sense —
but it is the sense the corpus itself judged **most dangerous**, and it is the direct ancestor of
`Ω-a` (*"ideal reference"*), which makes it upstream of the whole `Ω_a/b/c` decomposition.

## 12.5 An attribution in an independent verification that does not reproduce

`verification/findings/TV-F-022-024-…` L60 states:

> *"**Ω overloading confirmed at source:** step-007 ω (ontology version) · step-008 Ω_A (acceptance
> product state) · **step-016 §54 Ω (ontology in `F(K₀,H,ρ,Ω)`)** · **step-017 Ω (semantic model)**"*

Checked at source:

| claim | result |
|---|---|
| step-016 **§54** | ✅ exists — *"Step 16 mathematical core"*, L1969–2035 |
| the formula **`F(K₀,H,ρ,Ω)`** | ⛔ **does not occur** in step-016 (or anywhere in the universe). §54 contains **two adjacent boxed formulas** — `K(t)=Fold(δ_K,K_0,H_K^{≤t})` (L2001) and `δ_K(K_t,o_t,ρ_t,Ω_t)` (L1993). **`F(K₀,H,ρ,Ω)` is those two conflated** |
| **Ω = "ontology"** in step-016 | ⛔ **unsupported.** step-016 contains **exactly one Ω** and **no gloss for it** |
| step-017 Ω | ⛔ **step-017 contains ZERO Ω** (either spelling) |

⇒ **Two of the four Ω-origin attributions in that sentence do not reproduce.** The finding
*asserts* a type for Ω-3 that its own cited source does not carry.

> ⛔ **Consequence: `Ω-1 × Ω-3` stays `UNDECIDABLE`.** The one corpus document that types Ω-3 as
> *"ontology"* does so on a non-reproducible citation. **A wrong citation is not weak evidence for
> the identity — it is no evidence, and adopting it would have made Ω-1 × Ω-3 read as SAME OBJECT
> on nothing at all.**

## 12.6 Ω after the window — `G-11` is far larger than recorded

**171 files** for steps 268–292; **64 contain Ω**; **290 line-occurrences**. Ω does not merely
reappear at `step_282` — it becomes central to the Gītā lane (`step_286`), where it acquires **at
least three further readings**:

| reading | evidence |
|---|---|
| `Ω = Sañjaya = observation capability` | ⭐ **Ω-2 revived**, and *"corpus-native"* per the reviewer |
| `Ω = epistemic horizon / infinite Knowledge Space`, `K_t ⊊ Ω` | ⭐ **Ω-7 revived** |
| `Ω_K = the set of legitimate kernel operations` | ⭐ **Ω-5's operation-set reading, now a KnowledgeOS object** |
| `Kṛṣṇa = Ω` | ⛔ **explicitly REJECTED**: *"`Kṛṣṇa ≢ Ω`"*, *"Rejected metaphysical projection"* |

⭐ **The two senses `C-06` calls a TRUE CONTRADICTION are both live again after step 285, in one
lane, unreconciled.** `G-11` must be re-scoped from *"Ω and EC reappear at step 282"* to
**"Ω re-enters across 64 files in steps 269–291 in at least three senses, two of which the corpus
has ruled contradictory."**

## 12.7 Convergent corroboration of the 2026-08-24 origin

My independent identification of the two 2026-08-24 files matches the corpus's own archaeology
**exactly**:

> `reviews/synthesis/analysis/phase-archaeology-findings.md` **AF-003** (2026-08-28 17:35):
> *"Ω first appears 2026-08-24 in kernel lens docs (`20260824-121835`, `20260824-122855`) as the
> **ambient space of typed/Bayesian models** — one day before phase_measure_theory opens. **The
> measure-theory crisis inherited Ω; it did not coin it.**"*

Two independent passes, same two files of 92 dated that day, same characterisation. `AF-003` is the
archaeology `step_237` L108 refers to. ⭐ **The `G-21` gate verdict is unchanged and now
double-sourced.**

## 12.8 Net effect on the disposition

| | before the sweep | after |
|---|---|---|
| in-window senses | 5 | **5** — unchanged |
| out-of-window senses recorded | 1 (Ω-6) | **2** (+ **Ω-7** Knowledge Space) |
| `Ω_a/b/c` | *"never defined"* | ⛔ **defined 08-28 15:01**; `238` altered Ω-c's fate |
| governance | *"no act"* | ⛔ **`GN-09` + `D-R27`** — but they **do not cover Ω-1, Ω-2 or Ω-3** |
| prior art | none found | ⛔ **`TG-15` and `C-06`** registered the overload on 2026-08-30 |
| **Ω-1 × Ω-2 HOMONYM** | new | ⭐ **STILL NEW** — `Ω-1` appears in none of `TG-15`'s four senses |
| **Ω-1 × Ω-3** | UNDECIDABLE | **UNDECIDABLE**, now with a *refuted* attribution attached |
| `G-21` gate | open with constraint | **unchanged, double-sourced** |

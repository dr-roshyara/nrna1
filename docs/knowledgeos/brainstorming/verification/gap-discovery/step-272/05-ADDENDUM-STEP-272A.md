# 05 — Addendum: Step 272A

**Trigger.** Step 272 is now present in the corpus as **Step 272A**, written **2026-08-30 22:42:42** —
**nine minutes after** the premise audit in `01` was taken (22:33). This addendum records what
changed. **Where it conflicts with `01`–`03`, this file wins.**

Re-verified: `exec/premise_audit.py`.

---

## 1. What arrived

**File:** `20260830-224242_step_272a_core-operation-universe-derivation.md` (26,392 B)
**Title:** `STEP 272A — DERIVATION OF THE CORE OPERATION UNIVERSE`
**Header:** `Status: DERIVED — FOUNDATIONAL BASIS` · `Authority: HPA` ·
`Role: Prerequisite to K minimality` · `Predecessor: Step 271` · `Successor: Step 273`

Its opening sentence:

> *"Yes. You are correct. **Step 272A was intended to be derived first, and our later work jumped over
> that derivation.** The subsequent audits themselves explicitly identify the missing dependency: the
> operation universe must precede the proof of `K` minimality."*

### What it establishes

$$\boxed{\;\mathcal O_{core} \rightarrow Requirements(K) \rightarrow Candidate(K) \rightarrow Minimality(K) \rightarrow \Sigma\;}$$

- The previous treatment of `𝒪_core` was **too broad**: `Assert` is a state transition, `Query` an
  observation, `Authorize` a governance predicate, `Serialize` a representation function —
  *"They cannot simply be treated as one homogeneous algebra."* (The five-class partition of §259.7,
  re-derived independently.)
- Removal test restated: `K → K^(−x) → execute all mandatory operations → counterexample`.
- Closing rule: *"**Do not infer ontology from implementation method names.** Derive semantic
  operations from mandatory distinctions, classify their mathematical role, and only then derive the
  minimum state required to support them."*

---

## 2. Corrections to `01` — Premise Audit

| `01` said | Now |
|---|---|
| files in `phase_measure_theory`: **540** | **541** |
| missing steps: **217, 229, 268, 272** | **217, 229, 268** |
| *"step 272 file present? **NO**"* | **YES — as Step 272A** |
| `𝒪_core` in **7** files | **8** files |

**`01` §2.1's substance is unaffected:** Step 272's *conclusion* was cited and accepted by Step 273 at
21:44, and the derivation only reached disk at 22:42 — **58 minutes after the work that depended on
it.** The premise-audit gate still fails; it now fails for a sharper reason.

---

## 3. Correction to `02` — Gap Fulfilment

**Step 272A retracts Step 277's closure claim**, in its own words:

> *"the earlier claim that `O_core` was already **'closed'** was too strong: the corpus had
> reconstructed the operation inventory, but had not yet completed the **minimality and
> category-separation proof**."*

| Gap | `02` said | Corrected |
|---|---|---|
| **G-16** transition signature | **FULFILLED** (cited Step 277 *"Classification: CLOSED"*) | **NARROWED** — that closure is retracted; the inventory stands, the category-separation and minimality proofs do not |
| **G-01** `𝒪` unenumerated | NARROWED, on Step 277's closure | **NARROWED, on a better basis** — Step 272A supplies the *derivation* Step 277 asserted. Residual unchanged: **D-1** (4 operations' mandatory status) and **D-0** |

**Net effect on the count: FULFILLED 9 → 8; NARROWED 8 → 9.** Actual theoretical holes remain **11**.

---

## 4. D-0 — split in two, and only half closes

This is the consequential change, and it is a split rather than a resolution.

| | Before | After Step 272A |
|---|---|---|
| **Content gap** — does a derivation of `𝒪_core` exist? | ❌ no declaring artifact | ✅ **CLOSED** — Step 272A is that derivation |
| **Authority gap** — was it *decided*, by an authority act? | ❌ no record | ❌ **STANDS** |

Re-verified after Step 272A landed:

```
files containing 'O_core'                                   : 8   (was 7)
governance-notes.md, highest note                           : GN-73   (unchanged)
governance-notes entries for O_core / "operation universe"  : 0       (unchanged)
occurrences of "HPA RULING" / "Ruling" / "decided" / "DECISION" in Step 272A body : 0
```

**Step 272A carries `Authority: HPA` as a front-matter label and contains no ruling in its body.**
That is exactly the pattern D-0 names — *a decision reported in a Stratum-1 research narrative with no
authority record* — and it is the same shape as the estate's own `humanActRef`-without-binding
concern, against a standard of **132/132** grants carrying one.

**D-0 is therefore narrowed, not answered.** The revised question is one sentence shorter:

> **Is the `Authority: HPA` label on Step 272A an actual authority act? If so it needs a governance
> record; if not, Steps 273–278 remain `HYPOTHESIS`.**

---

## 5. The finding this addendum adds — and it is a positive one

**PA-4 (`EXECUTED`) — the first repaired band-transition loss.**

Five specified-then-lost concepts were documented (`04` §4). Step 272A is the **first one repaired**,
and it was repaired **by being pointed at**: its first words are *"Yes. You are correct."*

| # | Lost concept | Repaired? |
|---|---|---|
| 1 | repaired measure-theory model | ❌ |
| 2 | `Regime` | ❌ (reinvented wordlessly at Step 270) |
| 3 | `Determination` | ⚠️ partially — `Determine` is in the forced 14, `D_t` in canonical `K` |
| 4 | **`𝒪_core` derivation (Step 272)** | ✅ **REPAIRED — Step 272A** |
| 5 | `K`-sufficiency (Step 277 → 278) | ❌ still 0 mentions in Step 278 |
| 6 | congruence-vacuity / `Sufficient(K,𝒪,ℐ)` | ❌ still 0 occurrences |

**This is direct evidence for `04` §6 item 3.** A commission-closure check would have caught the
Step 272 gap automatically instead of requiring an external reader to notice it — and the estate
demonstrably *acts* on the finding once it is surfaced. **The mechanism is missing, not the will.**

**Two losses remain open, and both are K-sufficiency:** the commissioned step (#5) and its defining
criterion (#6). They are the same gap seen from two sides.

---

## 6. Does anything else in `01`–`04` change?

| Claim | Status |
|---|---|
| Σ is no longer the frontier (Step 275 derives `Σ=(D,S)`) | **unchanged** |
| PA-1 — Step 277 commissioned K-sufficiency; Step 278 did not perform it | **unchanged, 0 mentions** |
| PA-2 — `so_exp06` numbers adopted, vacuity criterion not | **unchanged, 0 occurrences** |
| G-55 `ℛ` 3-field · G-57 `AuthorityAct` 0 · G-15 `Context` untyped | **unchanged** |
| 238 verification documents across 9 packages | **unchanged** |
| The mandate should not be executed as written | **unchanged — and reinforced**: Step 272A now occupies the slot the mandate wanted to fill |
| Recommended re-issue as **STEP 279-PRE** | **unchanged**, with item 3 (commission-closure check) now empirically supported by PA-4 |

---

## 7. The standing recommendation, updated

**Answer D-0 in its narrowed form — one sentence:**

> *"The `Authority: HPA` on Step 272A is / is not an authority act."*

If **is** → record it as a governance note (GN-74) and D-1 largely closes.
If **is not** → mark Steps 272A–278 `HYPOTHESIS` and the estate's 100 %-coverage authority invariant
is restored rather than breached.

Everything else in `03`'s ordering is unchanged: **D-0 → D-5 → D-2 → D-1 → D-3 → (D-4: wait).**

---

*Snapshot taken 22:47. The research thread wrote at 22:42. This addendum may itself be superseded —
which is the sixth time this investigation has had to say so.*

# File Analysis — 0738

*(Ultra-concise record (MD-013) — see 0738.yaml.)*

## Source
`docs/knowledgeos/brainstorming/phase_measure_theory/20260828-131303_step-150-knowledgeos-runtime-architecture.md`
· canonical_source 0738.

## Classification
`c1` primary / `g1` secondary — PRIMARY_RESEARCH, importance **critical**.

## Tier 2
**TRIGGERED — delivers the clean three-layer runtime model and several sharp, currently and broadly
applicable AI-safety/systems-architecture principles, among the most valuable single-file contributions in
the entire Steps 120-150 arc.** Opens `KnowledgeOS Platform≠Agent Harness≠Engineering Environment`, with
precise per-layer responsibility statements — the most direct, concretely applicable separation given this
repository's own actual `.claude/`/`.codex/` split.

Delivers a full request-tracing walkthrough for the entire Golden Trace runtime sequence, with a worked
three-source context-authority-preservation example (Governance/Agent memory/Runtime observation each
retaining distinct semantic status labels rather than being flatly concatenated).

**Delivers "why instructions are insufficient," a sharp AI-safety principle**: "`AGENTS.md` says: never
deploy directly to production... but it is not a security boundary. A malicious, buggy or misconfigured
agent can ignore it. Therefore: **Instruction≠Enforcement**. The actual authorization boundary must enforce
the rule" — directly extending 0709's "governance below the LLM instruction layer" principle, grounded
precisely in this reconstruction's own governing `AGENTS.md`/`.claude/` structure.

**Delivers five named Runtime boundaries and a concrete three-zone Trust-zone model**: Zone A (Agent
Environment) explicitly untrusted for authority (`AgentOutput=Untrusted assertion` until governed); Zone B
(KnowledgeOS) is "the semantic control plane"; Zone C (Engineering Environment) is "operational authority"
— "this distinction prevents KnowledgeOS from inventing operational truth."

**Delivers the Control-Plane-vs-Data-Plane distinction, applying classical systems terminology precisely to
AI governance for the first time**: KnowledgeOS as `Semantic/Governance Control Plane`; engineering systems
as `Execution/Operational Data Plane`; the Agent as a `Reasoning Actor` — "the agent is not the control
plane."

**Delivers a risk-sensitive fail-open/fail-closed policy**: `ExecutionPolicy=f(ActionRisk)` — low-risk
reads may fail open, governance-sensitive actions must fail closed — with a four-level action-risk
classification. Delivers duplicate-execution protection ("same `ActionID`⇏second physical execution"),
runtime timeout semantics, and the Semantic-trace-vs-Technical-trace distinction.

**Delivers `RT-001` through `RT-006`**, six new runtime-security invariants (agent environment is not an
authority boundary; authorization enforced server-side; external systems remain authoritative; high-risk
execution fails closed; material actions idempotently identifiable; telemetry and traceability must be
correlatable).

Delivers a concrete initial deployment architecture directly operationalizing 0737's persistence
recommendation, reaffirming "modular monolith first" with five justifications. **Delivers a Conway's-Law-
grounded decomposition principle**: "we should derive the software topology from the domain/organizational
boundaries rather than inventing services first" — extending the corpus's non-premature-decomposition
discipline with real organizational-design theory.

Delivers "runtime architecture and current KnowledgeOS," directly and concretely mapping this
reconstruction's own governing repository mechanisms onto the new architecture without discarding them
(Claude/Codex→AgentAdapter, `AGENTS.md` as repository-level pointer, hooks as
`LocalFastCheck+GovernedAssurance`, session logger evolving toward `Session→Action→Evidence`) — a five-stage
staged Runtime evolution strategy, "much less risky than a replacement project."

**Delivers the file's most important runtime rule**: "agent harnesses are clients of KnowledgeOS, not
alternate KnowledgeOS implementations." Delivers the most complete single target-runtime diagram in the
reconstruction. Verdict restates the clean three-layer separation and controlled interaction chain.

**Closes by opening "Step 151 — The First Executable Architecture,"** explicitly declaring "we have reached
the point where continuing to add conceptual layers would become counterproductive," promising a genuinely
concrete first implementation slice (repository/module structure, first database schema, first API
contracts, first domain objects, first events, first adapter, first deterministic checker, first Golden
Trace test, minimum Claude/Codex integration, Definition of Done) — "**KnowledgeOS Vertical Slice v0.1**"
using Nexus, deliberately read-only — "where the architecture finally becomes something that can be handed
to an implementation team—or to Claude/Codex—to build without having to reinterpret the architecture
themselves." No actual code execution, repository inspection, or verified filesystem/architecture claim
reported anywhere in this file.

**Bridge candidates**: read 0739 (Step 151) with the HIGHEST priority — determine whether it delivers
genuine implementation-level artifacts (schema DDL, actual file/module names, code sketches) or remains at
the architecture-diagram level.

## Classification Revision History
| Date | Event | To | Reason |
|---|---|---|---|
| 2026-09-03 | initial | `c1`/`g1`, critical | Delivers a clean, precisely reasoned three-layer runtime separation directly relevant to this reconstruction's own governing `.claude`/`.codex` repository structure. Delivers Instruction≠Enforcement, the Control-Plane-vs-Data-Plane distinction, five named runtime boundaries, a concrete trust-zone model, and a risk-sensitive fail-open/fail-closed policy — among the most valuable single-file contributions in the entire Steps 120-150 arc. Delivers RT-001 through RT-006, a Conway's-Law-grounded decomposition principle, and a memorable capstone rule. Opens Step 151's decisive continuation, flagged maximum priority |

# File Analysis — 0591

*(Ultra-concise record (MD-013) — see 0591.yaml.)*

## Source
`docs/knowledgeos/brainstorming/phase_measure_theory/20260828-094115_step-025m-epistemic-error-refutation-retraction-correction-and-revision.md`
· canonical_source 0591.

## Classification
`c1` primary / `g1` secondary — PRIMARY_RESEARCH, importance **high**.

## Tier 2
**Not triggered** — a disciplined, solid continuation without a single landmark-level new architectural
discovery, though it carries an important corpus-integrity flag (see below).

Opens Step 25M directly executing 0590's close: what should happen when previously accepted knowledge is
later discovered wrong — "wrong" has several distinct meanings a single Boolean would lose. Delivers
`K_{t+1}=Revise(K_t,E_{new})` with additive `H_{t+1}⊇H_t`.

**Delivers a formal six-kind taxonomy of "wrong"**, each independently motivated and formally signed:
**Evolution** (`Evolution≠Correction`, old knowledge remains historically valid); **Correction**
(`Correction(E_1,E_2)`, `Status(E_1)=Corrected`, a new event not destructive modification); **Retraction**
(`Retraction≠Refutation` — losing support ≠ proving false, "especially important for statistical
reasoning"); **Refutation** (a conclusion derived from evidence+domain semantics, not merely a second
assertion's existence); **Reinterpretation** (worked wrong-target example, `ObservationValid∧
AssertionInvalid`, explicitly citing 25I/0587 as necessary foundation); **ModelRevision** (`Ω_1→Ω_2`
producing `K^{Ω_1}≠K^{Ω_2}` on unchanged observations, delivering **`Interpretation is versioned`**).

**Delivers Revision as event addition, never destructive mutation**: `H'=H∪{E_{revision}}`,
`K'=Derive(H',Ω,EC,M)` — **`Revision=EventAddition+StateReDerivation`**, with an auditability
demonstration that the pre-correction state remains fully reconstructable.

Delivers **Assertion≠Assessment** as a distinct revision dimension (support can weaken while the
proposition stays unchanged), a worked Bayesian revision example (`P(H|E_1)=0.95→P(H|E_1,E_2)=0.40`)
formalizing **`Probability revision≠Logical refutation`**, and a reconfirmation that deterministic and
probabilistic knowledge must coexist without reducing KnowledgeState to a probability distribution.

**Delivers a disciplined revision-propagation treatment** via a worked six-node dependency chain
(`E_1→A_1→K_1→R_1→Zero→Lord→D_1`): retraction must not cascade-delete, especially given independent
support — **`Revision propagation=Support recomputation, not cascading deletion`**, an Affected Revision
Frontier `RF(E)` for incremental recomputation, scoped by semantic (DDD) dependency.

**Delivers a significant extension of Zero**, directly building on 0582's/0584's formal Zero algebra:
**`Zero is dynamic`** — a satisfied requirement can become unresolved again once supporting evidence is
invalidated. **Delivers an equally significant extension of Sārathi's decisions**, directly building on
0586: `DecisionAffected(D_1)=True` when a prerequisite is invalidated, but automatic reversal is explicitly
NOT permitted unless governance policy allows it — preserving computation/authority separation, with five
named governance responses; delivers a six-value Decision status lifecycle
(`Valid,Superseded,Questioned,Invalidated,Executed,Reversed`), reconfirming
**`Historical existence≠Current validity`**.

**Delivers seven labeled revision falsification tests (1-7), ALL PASS**: genuine evolution; wrong target;
source withdrawal; genuine refutation; model revision; redundant support (survives retraction of one of
two independent supports); decision dependency (`DecisionAffected=True`, reversal governed not assumed).

Reconfirms normal-PC computational feasibility for revision machinery, with large-scale performance a
scalability concern, not a theoretical obstacle.

**Delivers Step 25M's central formal invariant** (`H_{t+1}=H_t∪{RevisionEvent}`,
`K_{t+1}=Derive(H_{t+1},Ω,EC,M)`) and **verdict "25M — PASS,"** with the file's headline distillation: the
model now distinguishes **four fundamentally different dimensions of change** — World Change, Knowledge
Change, Assessment Change, Model Change — "related, but not the same event."

**Closes by reconfirming 0588's rejection of naive confidence-averaging**, opening **"Step 25N — Evidence
Aggregation Algebra,"** separating six distinct concepts
(`Confidence≠Probability≠Reliability≠Authority≠Support≠Independence`), posed as "the next major
mathematical test." No actual code execution reported.

**Corpus-integrity finding (priority bridge candidate)**: "Step 25N — Evidence Aggregation Algebra" opened
at this file's close carries the SAME TITLE as Step 25C (0578, "Evidence Aggregation Algebra," already
PASSed). Flagged for the eventual full-corpus reconciliation (§29) — trace whether 25N is executed as its
own file and whether it genuinely extends/supersedes 25C or duplicates already-covered ground.

## Classification Revision History
| Date | Event | To | Reason |
|---|---|---|---|
| 2026-09-02 | initial | `c1`/`g1`, high, tier2 not triggered | Disciplined six-kind taxonomy of epistemic "wrongness" (Evolution/Correction/Retraction/Refutation/Reinterpretation/ModelRevision), each formally motivated and tested; concrete extensions to Zero (now dynamic) and Sārathi (governed, non-automatic decision reversal); revision-propagation principle (support recomputation, not cascading deletion); verdict "25M — PASS," four-dimension change distinction; opens Step 25N — flagged for a title collision with 0578's Step 25C, both named "Evidence Aggregation Algebra" |

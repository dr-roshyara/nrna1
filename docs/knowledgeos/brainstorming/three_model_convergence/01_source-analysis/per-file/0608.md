# File Analysis — 0608

*(Ultra-concise record (MD-013) — see 0608.yaml.)*

## Source
`docs/knowledgeos/brainstorming/phase_measure_theory/20260828-101614_step-029-knowledge-consistency-constraints-invariants-satisfiability-and-dependency.md`
· canonical_source 0608.

## Classification
`c1` primary / `g1` secondary — PRIMARY_RESEARCH, importance **critical**.

## Tier 2
**TRIGGERED.** Opens Step 29 directly executing 0607's close, delivering the central motivating
distinction: **`Pairwise consistency≠Global consistency`**. Delivers a formal knowledge-state consistency
definition and DDD invariants formalized as executable constraints.

**Delivers the file's key clarifying insight**: worked `Change=Approved∧ApprovalBoard=NotConvened`
example violating an invariant despite the two assertions not being logically opposite — precisely
distinguishing invariant violation from simple contradiction (0607's central topic). Delivers formal
Constraint provenance with the explicit governance statement: **`a generated LLM rule must not silently
become an authoritative domain invariant`**. Delivers Constraint authority, scope, and versioning (traced
to 0601's temporal knowledge model).

Delivers Temporal and Causal consistency, formal Dependency closure, a dependency graph, and a precise
distinction between `CircularDependency` (not automatically invalid) and `CircularJustification`
(problematic).

**Delivers the file's central formal deliverable — formal satisfiability** (`SAT(K∧C)?`, extended toward
SMT). **Delivers an important architectural discipline explicitly restraining over-formalization**: "do we
need an SMT solver everywhere? No," directly consistent with the corpus's recurring resistance to
unnecessary machinery (0599/0601/0602/0603/0605/0606).

Delivers an eight-class constraint taxonomy, referential/provenance integrity, a five-value Severity
taxonomy, and a notable clarification: **deterministic binding constraints should not normally carry a
RuleConfidence figure**. Delivers Constraint precedence explicitly as a governance concern the system must
never invent — undefined precedence yields `Escalate`, never an arbitrary pick.

**Delivers a formal seven-dimensional Consistency vector** `(Logical,Temporal,Semantic,Referential,
Provenance,Causal,Policy)`, extending the corpus's anti-scalar discipline (0592/0599/0603/0606).

**Delivers a substantial statistical-consistency treatment**: `Variance≥0`, probability-sum constraints
(worked `0.7+0.6=1.3>1` violation), conditional-probability consistency, and distribution consistency.
**Delivers a formal treatment of unit and dimensional consistency, explicitly demonstrating errors caught
before execution**: worked `10m` mistaken as `10km`, and a dimensional-analysis contrast
(`Speed=Distance+Time` invalid vs. `Speed=Distance/Time` valid). Delivers a formal Semantic type system
reconnecting to DDD value objects — **`TypedKnowledge` preferred over `GenericKeyValueKnowledge`**.

Delivers Constraint propagation and a formal `ProofObligation` structure, explicitly connected to 0604's
(25Z) action-precondition unification. **Delivers the file's second central formal deliverable — a three-
layer formal-verification boundary** (Generative reasoning→Probabilistic inference→Deterministic
constraint validation→Execution). Delivers KnowledgeOS reframed as a proof-carrying system, a formal
`Proof(C)` object, and the sharpened principle: **`AI generates; deterministic machinery verifies`**.
**Delivers the file's headline architectural preference**: **`Generate→Verify→Accept` is preferable to
`Generate→Trust`**.

**Delivers nine labeled falsification experiments (A-I), ALL PASS**: approval without evidence; mutually
exclusive same-context assertions (`UNSAT`); historical assertion valid under its historical constraint
version; deleted referenced evidence (`BrokenProvenance`); circular justification detected; probability-
sum violation; unit/dimensional violation; unresolved proof obligation blocks execution; unprecedented
constraint conflict escalates.

**Delivers Step 29's verdict**: **"STEP 29 — PASS,"** with seven boxed conclusions plus the headline
`Generate→Verify→Accept` preference. **Delivers the corpus's most complete layered-assurance architecture
diagram to date**: an eight-stage pipeline explicitly introducing a NEW named **`DETERMINISTIC ASSURANCE
GATE`** stage interposed between Decision and Action, refining 0604's (25Z) earlier control-loop diagram.

**Closes with the file's central, self-named "subtle problem" — internal consistency does NOT guarantee
external correctness**: worked example (`A=True, B=True, C=True` internally consistent while `A=False` in
reality), formalizing **`Internally consistent while being Externally incorrect`**. **Opens "Step 30 —
Epistemic Calibration, Reality Alignment, Validation, Ground Truth and Model Drift,"** posing "how does
KnowledgeOS determine whether its internally coherent knowledge corresponds to reality?" Delivers the
file's central distinction for the next step: **`Consistency≠Correctness`**. **Delivers the file's
profound, self-aware closing challenge**: "a knowledge system must be able to discover that its own
internally coherent model is wrong" — "genuinely epistemic rather than merely a sophisticated consistency
engine." No actual code execution reported.

**Bridge candidates (priority)**: trace whether Step 30 is executed as its own file and whether it
succeeds in addressing the Consistency≠Correctness gap.

## Classification Revision History
| Date | Event | To | Reason |
|---|---|---|---|
| 2026-09-02 | initial | `c1`/`g1`, critical | The corpus's formal SAT/SMT/constraint-satisfaction and proof-carrying-knowledge layer. Precisely distinguishes invariant violation from simple contradiction; "LLM rule must not silently become an authoritative invariant"; disciplined "not everywhere" SAT/SMT restraint; formal statistical/unit/dimensional consistency checking with concrete error-catching examples; Generate→Verify→Accept over Generate→Trust. New DETERMINISTIC ASSURANCE GATE architecture stage. Verdict "STEP 29 — PASS"; opens Step 30 with the profound closing challenge Consistency≠Correctness — "genuinely epistemic rather than merely a sophisticated consistency engine" |

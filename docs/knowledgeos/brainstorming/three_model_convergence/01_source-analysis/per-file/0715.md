# File Analysis — 0715

*(Ultra-concise record (MD-013) — see 0715.yaml.)*

## Source
`docs/knowledgeos/brainstorming/phase_measure_theory/20260828-125345_step-129-knowledgeos-architecture-fitness-model.md`
· canonical_source 0715.

## Classification
`c1` primary / `g1` secondary — PRIMARY_RESEARCH, importance **critical**.

## Tier 2
**TRIGGERED — formalizes the fitness-rule mechanism and substantially extends 0714's AFR catalog to a
full, systematically numbered AFR-01..AFR-26.** Formalizes `F_i(S,C)→{PASS,FAIL,WARN,UNKNOWN}`, with the
framing: "a principle describes what should be true, a fitness rule makes it testable, an execution
produces evidence." Delivers the `FitnessRule` object schema and rule-provenance chain
(`FitnessRule→Principle→Authority`), plus a precisely reasoned Rule-applicability governance requirement
rejecting `if inconvenient: rule.not_applicable = true`. Delivers eight fitness-rule categories
(Structural/Semantic/Governance/Security/Evidence/Behavioral/Assurance/Agent).

**Delivers sixteen additional numbered rules (AFR-11 through AFR-26)**, the most systematically complete
catalog in the reconstruction to date: AFR-11 (agents may consume authoritative knowledge through approved
interfaces, must not establish independent authoritative stores — operationalizes Claude/Codex symmetry);
AFR-13 (Ubiquitous-Language rule: "Approved" must have one governed meaning per bounded context); AFR-14
(only the owning context may perform invariant-sensitive aggregate transitions — "a classic DDD fitness
rule"); AFR-15 (authority is temporal, `ValidAuthority(a,t)`, worked through a concrete failing delegation-
expiry test); AFR-16 (every exception needs authority+scope+expiry, with the explicit "never use 'temporary'
without expiry" anti-pattern — `Expiry=null` → FAIL, "prevents temporary architecture from becoming
permanent through neglect"); AFR-18 (current retrieval must exclude superseded knowledge, worked stale-
knowledge test); AFR-19 (verification results must identify rule+checker version, motivated by a
reproducibility test `V_1=f(R,I)=V_2`); AFR-20 through AFR-24 (the Agent-specific rules, including AFR-24
directly re-formalizing the pointer-layer principle within the fitness-rule system); AFR-25 (material agent
actions require referenced authorization, worked negative-test example: unauthorized production change →
BLOCK); AFR-26 (material governed changes require post-action verification — `CommandSucceeded≠
MigrationCorrect`, `exitCode=0` proves the command ran, not correctness).

**Delivers a genuinely valuable refinement of the `.claude/memory/` pointer-layer test beyond 0714's binary
FAIL**: the checker's real question is whether a file is "an authoritative source or merely a local
pointer/cache" — independently asserting "ADR-42 is authoritative" fails, but "See KnowledgeOS decision
ADR-42" passes — a content/framing-sensitive refinement of the test.

Delivers Agent-identity attribution (`Agent+Session+Task`, concrete example "Agent: codex, Session: S-1842,
Task: migrate Nexus configuration"), Agent-recommendation fitness (`Recommendation→Evidence/Producer/
ContextHash`), and a worked Context-integrity example directly extending 0711's `H(ContextPackage)`
proposal: a Codex recommendation made under context `C_1` must remain bound to `C_1` even as the
architecture evolves to `C_2`.

**Delivers a genuinely important recursive self-governance requirement on fitness rules themselves**
(directly extending 0709's "Constitutional self-protection" into the fitness-rule layer): "what if someone
changes AFR-19 to make a failing system pass? Then the rule change itself requires governance" — a rule has
its own governed lifecycle (`Proposed→Reviewed→Approved→Effective→Superseded`) and `RuleVersion`.

Delivers the `FitnessResult` object schema and the precise principle `FitnessResult≠AuthoritativeKnowledge`
("a result says: at time T, checker C found condition X. Governance may subsequently interpret that
result"), plus **the precise Fitness-result→Finding disposition rule**: `FAIL→FindingCreated`, but
`UNKNOWN` must NOT automatically become a `Violation` — instead `Finding(type=InsufficientEvidence)` —
extending the corpus's Unknown-preservation discipline (0693/0699/0708/AFR-10) into result-handling logic
specifically.

Delivers a three-plus-valued fitness state machine (`PASS|FAIL|UNKNOWN` plus `NOT_APPLICABLE`/`EXCEPTION`),
a worked fitness dashboard mockup, and Fitness trends over releases as an architectural-drift signal.

**Delivers a precisely reasoned "no single score" caution**: architecture health must never be reduced to
one aggregate percentage — "a single failed authority invariant could be more important than twenty
cosmetic successes" — paired with a Severity taxonomy (BLOCKER/CRITICAL/HIGH/MEDIUM/LOW, itself governed)
and an Enforcement-policy mapping (`Verdict+Severity→PipelineDisposition`).

**Delivers a precise, genuinely valuable Fitness-Rule-vs-Test-vs-Policy-vs-Decision taxonomy**:
`UnitTest⊂EngineeringAssurance` vs. `FitnessRule⊂ArchitectureAssurance` as complementary disciplines;
`Policy→FitnessRule`; and the complete governance-to-assurance path `Decision→Knowledge→FitnessRule→
Verification`.

Delivers the file's central, appropriately calibrated thesis: **"Narrative Architecture → Executable
Architecture... not by eliminating documents, but by attaching deterministic verification mechanisms to
the parts that can be objectively tested"** — worked through three concrete translation examples (DDD
boundary, Agent boundary, Evidence).

Verdict: "architecture fitness is the executable boundary between architecture governance and engineering
reality," with the complete pipeline `Principle→Rule→Checker→Evidence→Verdict→Finding→Governance`.

**Closes by opening "Step 130 — The KnowledgeOS Assurance Graph,"** promising to combine every object
derived across the entire preceding arc (knowledge/claims/evidence/authority/decisions/policies/
observations/findings/recommendations/actions/verification/fitness rules/agents/runtime/governance) into
one machine-traversable graph, explicitly reframing the central question from "what objects exist?" to "can
we traverse the complete lifecycle of an engineering decision from organizational intent all the way to
runtime evidence and back?" — target graph `Intent→Decision→Knowledge→Rule→Implementation→Runtime→
Evidence→Verification→Finding→Governance`, named a candidate "central architectural construct." No actual
code execution, repository inspection, or verified filesystem/rule-execution claim reported anywhere — all
worked examples remain hypothetical illustrative applications.

**Bridge candidates**: read 0716 (Step 130) with continued high attention for whether it delivers a
genuinely traversable Assurance Graph and whether it finally reconciles the three-way open candidate-
fundamental-unit question (0703/0710/0711).

## Classification Revision History
| Date | Event | To | Reason |
|---|---|---|---|
| 2026-09-03 | initial | `c1`/`g1`, critical | Formalizes the fitness-rule mechanism and substantially extends 0714's AFR catalog to AFR-01..AFR-26 (sixteen new rules) — the most systematically complete rule catalog in the reconstruction. Refines the .claude/memory/ test into a content-sensitive check. Delivers a recursive self-governance requirement on fitness rules themselves, a precise fitness-result disposition rule (UNKNOWN never automatically Violation), a "no single score" caution, and a Fitness-Rule-vs-Test-vs-Policy-vs-Decision taxonomy. Crystallizes "Narrative Architecture → Executable Architecture." Opens Step 130's major promised Assurance Graph |

# File Analysis — 0658

*(Ultra-concise record (MD-013) — see 0658.yaml.)*

## Source
`docs/knowledgeos/brainstorming/phase_measure_theory/20260828-120906_step-074-causal-knowledge-from-correlation-to-intervention.md`
· canonical_source 0658.

## Classification
`c1` primary / `g1` secondary — PRIMARY_RESEARCH, importance **critical**.

## Tier 2
**TRIGGERED — CONFIRMS, AT THE HIGHEST SEVERITY YET FOUND, THE COLLISION FLAGGED AT 0657.** Step 74
re-derives the corpus's already-extensive causal-inference program (0594, 0624, 0645/0646): temporal-
precedence-is-not-causation, confounding, `do(X=x)` intervention, `P(Y|X=x)≠P(Y|do(X=x))`, counterfactual
potential outcomes (`τ=Y(1)-Y(0)`), and **Identification formally distinguished from Estimation
(`Identifiable≠PreciselyEstimated`, `CausalEffect=NonIdentified`)** — substantively the same distinction
this reconstruction's 0646 record already confirms was delivered as `NotIdentified` at Step 63, zero
cross-reference.

**Delivers the single most severe confirmed naming-and-content duplication in this entire reconstruction**:
this file's closing invariants include **`I_Causal`** and **`I_Intervention`** — **both under the EXACT
SAME NAMES as two of 0646's own four invariants** (`I_Causal, I_Intervention, I_Counterfactual,
I_Identification`), substantively overlapping content, zero cross-reference. This exceeds the `I_Concurrency`
duplication confirmed at 0656 (against 0641): here TWO invariant names repeat simultaneously from the same
four-invariant set.

**Delivers a second, independent "Experiment: graph collapse" appearing exactly one file after 0657's own
identically-titled experiment** (0657's §73.54 collapsed Knowledge/Provenance/Dependency/Authority/Trust
into one generic `relation()` → FAIL `SemanticAmbiguity`; this file's §74.26 collapses dependency/
provenance/temporal-order/causality into one generic `A→B` → FAIL `SemanticFailure`) — **the same
experiment name, same anti-pattern, same verdict, confirmed independently one file apart, zero
cross-reference**. The most concrete evidence yet that the corpus fails to maintain memory even across
immediately adjacent files in this span.

Despite this, delivers genuinely new synthesis material: **Causal transportability** (`Effect(C_1)⇏
Effect(C_2)`, cross-department causal-effect non-transfer, PASS), extending 0652's context-sensitivity
discipline. A `CausalClaim` lifecycle (`Hypothesis→Identified→Estimated→Validated→Challenged→Refuted→
Retired`, PASS). **Most valuably: the "governed causal learning loop"**
(`Observe→Explain→Decide→Intervene→ObserveOutcome→EvaluateCause→ReviseKnowledge`), explicitly "perhaps the
most important loop in the entire architecture," paired with **`Recommendation≠
EvidenceOfRecommendationCorrectness`** — an AI's recommendation being executed and followed by a good
outcome does not validate its causal reasoning (confirmed via a self-validation experiment, Rejected, PASS)
— a genuinely important, well-motivated safety principle protecting against self-reinforcing AI
recommendation bias, extending 0647's circular-self-validation discipline into the causal-recommendation
domain specifically. Delivers Intervention provenance and a formal `Intervention=(Target, Treatment, Time,
Scope, Protocol)` object (PASS). Delivers `Outcome≠CausalEffect` (PASS), paralleling 0646's `CausalEffect≠
DecisionUtility` under new terminology, again without cross-reference.

Delivers Causal model versioning (PASS) and Causal contradiction reframed via context
(`ContextualCausalDifference`, not automatically error, PASS) — extending 0652's `ApparentContradiction≠
LogicalContradiction` into the causal domain. Delivers a consolidated `CausalKnowledge` object (12 fields).

**Delivers Step 74's theorem candidate**: "temporal precedence, dependency, and correlation are
insufficient to establish causality." **Delivers four invariants**: the confirmed-duplicate `I_Causal` and
`I_Intervention`, plus `I_CausalUncertainty` and `I_CausalModelVersion` (these two not confirmed exact-name
duplicates, though territorially adjacent to 0646's `I_Counterfactual`/`I_Identification`). Verdict
**"STEP 74 — PASS,"** distinguishing four questions: "what happened? what do we believe? what caused it?
what would happen if we changed it?"

**Closes by opening "Step 75 — Decision Theory Under Uncertainty"** — **a SIXTH flagged collision
candidate**: this reconstruction's records confirm the corpus already substantially addressed Decision
Theory at 0643 and again at 0645 (`CausalEffect≠DecisionUtility`). Step 75's preview (utility, cost, risk,
constraints, reversibility, stakeholder preferences, worst-case loss) should be directly compared against
0643's content once 0659 is read. No actual code execution reported.

**Bridge candidates (HIGH PRIORITY)**: read 0659 (Step 75) and directly compare against 0643/0645. For
§29 synthesis: the Steps 65-75 span has now accumulated a dense, well-evidenced cluster of confirmed
content-repetition instances (kernel divergence, triple overlap, Concurrency duplicate, this file's
Causal/Intervention duplicate plus the cross-adjacent-file "graph collapse" repeat) warranting explicit
treatment as a discrete methodological finding.

## Classification Revision History
| Date | Event | To | Reason |
|---|---|---|---|
| 2026-09-02 | initial | `c1`/`g1`, critical | CONFIRMS, at the highest severity yet found, the collision flagged at 0657: re-derives 0645/0646's causal-inference program with TWO invariants (I_Causal, I_Intervention) under the exact same names as two of 0646's four invariants, zero cross-reference. Confirms a second "graph collapse" experiment appearing one file after 0657's own identical version — clearest evidence yet of memory failure across adjacent files. Delivers genuinely new, valuable material (governed causal learning loop, Recommendation≠EvidenceOfRecommendationCorrectness, causal transportability). Flags a sixth collision candidate (Step 75 vs decision theory). Verdict "STEP 74 — PASS" |

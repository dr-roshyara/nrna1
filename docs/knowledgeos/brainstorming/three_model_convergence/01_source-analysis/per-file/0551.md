# File Analysis — 0551

*(Ultra-concise record (MD-013) — see 0551.yaml.)*

## Source
`docs/knowledgeos/brainstorming/phase_measure_theory/20260827-152413_step-011-uncertainty-propagation-and-derived-knowledge.md`
· canonical_source 0551.

## Classification
`c1` primary / `g1` secondary — PRIMARY_RESEARCH, importance **critical**.

## Tier 2
**TRIGGERED — LANDMARK: uncertainty propagation formalized as a contract, not a formula.** **CORPUS-
INTEGRITY NOTE**: opens as "Step 11," though 0550 explicitly named "Step 10 — Inference and Derivation" as
next — no dedicated Step-10 file appears in the manifest; this file's own content (deterministic/
statistical/abductive inference typing, inference sufficiency, missing-premise gap discovery)
substantially absorbs what Step 10 promised.

Opens with a statistician's warning: `A_1,A_2⇒A_3` does NOT mean A_3 inherits its premises' confidence.
Delivers **six formally distinct uncertainty sources** (Premise/Inference/Model/Measurement/Semantic/
Context), reformulating `U` as a vector rather than scalar. Delivers the finding that
**`PropagationFunction depends on InferenceMethod`** (deterministic arithmetic error-propagation cannot
apply to abductive inference). Distinguishes deterministic derivation (`Deterministic rule⇏uncertain
premises`) from statistical inference (Estimate+Uncertainty both preserved, never collapsed).

**Delivers the file's single most important statistical result**: `P(A)=0.9`, `P(B)=0.9` does NOT
determine `P(A∧B)` (0.81 if independent, 0.9 if perfectly dependent, other values otherwise) —
**`Marginal confidence does not determine joint confidence`** — "a major reason KnowledgeOS cannot use
naive confidence multiplication." Grounds this via `Var(Y)≈JΣ_XJ^T` — "covariance matters."

**Delivers the file's central architectural consequence**: preserve DEPENDENCY structure, not merely
confidence scores — `Dependency(A_1,A_2)∈{Independent,Dependent,Unknown}` must accompany every related
pair. Confirms derived knowledge retains its full dependency graph (`Basis(A_3)={A_1,A_2}`; retraction
triggers `Reassess`, never automatic deletion). Formalizes deliberately non-universal Support/Uncertainty
propagation functions — "the kernel stores uncertainty semantics; the calculus performs uncertainty
propagation." Introduces the standard aleatory/epistemic uncertainty distinction.

**Delivers inference sufficiency** (`Complete(R,Γ)`) distinguishing deductively-derivable conclusions from
LLM hypotheses. **Delivers the file's most elegant new capability**: partial derivation as GAP-DISCOVERY —
a missing premise generates `Δ_missing_premise` that Lord can search to resolve, yielding
`Inference→MissingPremise→Discrepancy→EvidenceAcquisition` — "KnowledgeOS doesn't simply infer knowledge.
It can infer what it needs to know in order to infer the desired knowledge."

**Delivers the file's key theoretical result on chains**: **provenance propagation is universal; numerical
uncertainty propagation is calculus-dependent** — worked via an LLM chain (Document→extraction→
interpretation→synthesis) — "this does not mean its uncertainty must be numerically multiplied. It means
its epistemic lineage must be preserved."

Addresses validity/temporal/conflict propagation (each with careful non-automatic rules: `Propagation of
dependency≠automatic propagation of status`), formalizes a transitive dependency closure operator `D*(A)`
for acyclic graphs, and requires explicit fixpoint/cycle policy for cyclic inference. Delivers a
computational conclusion directly answering the corpus's foundational computability concern: computable
when finite/explicit/represented/defined, but "there is no universal algorithm for arbitrary
natural-language inference" — distinguishing computable kernel operations from AI-assisted interpretation.
Delivers a formal **KnowledgeOS computational contract**: every derived assertion must declare its method,
premises, rule/model, and version.

**Delivers ten canonical Step-11 invariants (U1-U10)**: Preserve uncertainty; Method dependence; Dependency
dependence; Provenance is universal; Unknown dependency remains unknown; Revision triggers reassessment;
Missing premises remain gaps; Statistical semantics preserved; LLM inference is typed
(`LLMOutput≠DeterministicDerivation`); No artificial confidence amplification.

**Step 11 verdict**: THEORETICALLY RESOLVED AT THE FRAMEWORK LEVEL — "KnowledgeOS does not need one
universal uncertainty formula. It needs a universal uncertainty CONTRACT." Consolidates the fullest single
pipeline yet: `Input→Evidence→Relations→Assessment→Acceptance→KnowledgeState→Discrepancy→
CandidateAction→NewEvidence→Inference→DerivedKnowledge→UncertaintyPropagation→RevisedKnowledgeState`.

**Closes opening "Step 12 — Identity, Entity Resolution, and Semantic Equivalence"**: motivated by concrete
puzzles (does "Nexus"/"Nexus Repository"/"Nexus3"/"nexus3.dgverlag.de"/"the artifact repository"/"the old
Nexus server" denote 1 entity or 6? are three differently-phrased Nexus-version statements the same
proposition?) — "deceptively simple, but... fundamental."

**Bridge candidates**: confirm whether a genuine "Step 10" file exists elsewhere; trace whether Step 12 is
executed.

## Classification Revision History
| Date | Event | To | Reason |
|---|---|---|---|
| 2026-09-02 | initial | `c1`/`g1`, critical | landmark: formalizes uncertainty propagation as a contract not a formula, delivers a rigorous statistical proof that marginal confidence cannot determine joint confidence (forbidding naive confidence multiplication), separates universal provenance propagation from calculus-dependent numerical propagation, delivers inference-as-gap-discovery unifying the derivation layer with Lord's evidence-acquisition machinery, and ten canonical U1-U10 invariants; notes a corpus-integrity gap where "Step 10" was never produced as its own file |

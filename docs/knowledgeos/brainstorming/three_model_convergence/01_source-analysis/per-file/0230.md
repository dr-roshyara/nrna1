# File Analysis — 0230

*(Ultra-concise record (MD-013) — see 0230.yaml.)*

## Source
`docs/knowledgeos/brainstorming/kernel/20260824-141231-bishop-prml-adaptive-inference-fabric-beyond-hmm-hsmm-hybrid.md`
· canonical_source 0230 (self — explicitly supersedes 0226's EG-HTI with a larger target architecture).

## Classification
`c1` primary, `g1` secondary (26-lens correspondence table).

## Tier 2
**TRIGGERED (critical).** Bishop's *PRML*, mined against the full lens system. Explicitly downgrades
0226's EG-HTI from "ultimate architecture" to one branch of a larger **Evidence-Gated Adaptive Inference
Fabric (EGAIF)**: deterministic + probabilistic + temporal + graphical + approximate + sampling +
model-combination, routed by a Risk/Cost/Assurance Router. Strongest extraction: Bishop's formal
**Inference ≠ Decision** separation — the computational counterpart of the Gödel-lens `proof != truth`
invariant — with a first-class **reject option** (`ABSTAIN`) replacing ad hoc confidence thresholds via
`Decision=argmin_a E[L(a,θ)|evidence]`. Refines the escalation formula to `ExpectedTotalCost=ComputeCost+
ExpectedDecisionLoss`, adds Bayesian model comparison/averaging (distinguishing state uncertainty from
model uncertainty), mixture-of-experts as a computational mechanism for DDD bounded contexts, and elevates
**graphical models/factor graphs** (sum-product vs. max-sum kept explicitly distinct) above HSMM as
potentially the more important extraction — "can we factorize the problem?" (Structural Tractability
Principle). Makes approximation itself auditable (variational lower-bound monitoring), elevates numerical
stability to an assurance concern, and produces the strongest lens-to-mechanism correspondence table in the
corpus (17 rows) plus eight named principles P1-P8. Explicit non-adoption list keeps probability from ever
becoming truth or governance authority.

## Classification Revision History
| Date | Event | To | Reason |
|---|---|---|---|
| 2026-09-01 | initial provisional | `c1`/`g1` | explicitly supersedes EG-HTI with the larger EGAIF architecture; formalizes Inference!=Decision with a first-class reject/abstain outcome; strongest lens-to-mechanism correspondence table in the corpus |

# File Analysis — 0536

*(Ultra-concise record (MD-013) — see 0536.yaml.)*

## Source
`docs/knowledgeos/brainstorming/phase_measure_theory/20260827-134245_evidence-algebra-step-01-define-the-universe.md`
· canonical_source 0536.

## Classification
`c1` primary / `g1` secondary — PRIMARY_RESEARCH, importance **critical**.

## Tier 2
**TRIGGERED — LANDMARK: the corpus's first documented EXECUTED computational experiment.** Steps 1-7
rebuild Evidence Assessment cleanly: universe 𝒪/𝒫/𝒞/ℛ/ℰ with `e=(o,p,r,c,π,τ)`; a ternary
`Polarity(e)∈{+,-,0}`; a quality vector `Q(e)=(q_R,q_Re,q_F,q_I,q_C)` explicitly NOT required to be
numeric (`Quality≠Score`); a dependency graph `G_E` with typed edges yielding `Independent(e_i,e_j,G_E)`;
a normalization operator `N:ℰ→ℰ*`; and a non-collapsing output `EA=(S^+,S^-,U,D,Q,Γ)`.

**Step 9-10 deliver the file's sharpest new mathematical nuance**: individual polarity contributions obey
directional (positive/negative) monotonicity, but the OVERALL Evidence Assessment may legitimately be
**non-monotonic** — adding contradicting evidence can decrease aggregate `Support^+(P)` under some
aggregation policies ("the system has learned something contradictory"). **Step 11 formally rejects**
`KnowledgeConfidence=ΣEvidenceScores`. **Step 12 poses the central open question** — what algebra combines
independent evidence? — surveying six candidates (Ordinal, Weighted vector, Numeric+⊕, Probability,
Belief/plausibility/Dempster-Shafer, Hybrid) and resolves it architecturally: **"Structured Evidence
Calculus + Pluggable Inference Mathematics"** — the domain defines invariants (Evidence/Provenance/
Dependency/Polarity/Quality/Conflict/Assessment); policies (ρ_deterministic/ρ_bayesian/ρ_statistical/
ρ_ruleBased) implement domain-specific decisions — "analogous to DDD: the domain defines the invariants;
policies implement domain-specific decisions."

**Then the file EXECUTES an actual computational experiment** ("I ran the experiment as a small executable
epistemic model... [Download the experiment results]") against a ten-case evidence universe, with a
downloadable CSV artifact — the corpus's first documented shift from discursive to executed mathematics.
**Six empirical findings**: (1) duplicate evidence confirmed NOT to manufacture support
(`Assess({e_1})=Assess({e_1,e_2})`); (2) independent corroboration genuinely increases support
(`Corroboration≠Duplication`); (3) an LLM restatement explicitly recorded as derived from e_1 is correctly
excluded from independent corroboration — "an LLM can generate another representation of knowledge without
generating another independent observation"; (4) stale evidence remains in the graph with diminished
weight (`Stale≠Deleted`); (5) irrelevant evidence contributes negligibly (`Evidence existence≠Evidence
relevance`); (6) contradictory evidence causes BOTH `Support(P)` and `Support(¬P)` to be preserved
simultaneously with an explicit Conflict:ACTIVE/Resolution:OPEN structure, rather than picking a winner.

**Delivers the file's central conclusion**: reject one universal `f(E)→[0,1]`; the fundamental object
remains structured `A_ρ(E,P,C)→EA`, with any scalar a secondary, policy-derived quantity. **Delivers a
genuinely important empirical discovery**: a necessary pipeline ordering
`Normalize≺DependencyAnalysis≺QualityAssessment≺Aggregation≺Inference`, vividly illustrated via a
Database→LLM→Report→ManagerPresentation chain that looks like four sources but is epistemically ONE
underlying observation with three transformations. Delivers "the laws that survived the experiment" — an
eight-item E-1..E-8 list (yet a THIRD independent numbering scheme within this single file, alongside its
own Step-8 Laws 1-5 and 0535's E1-E12).

**Closes with an honest admission**: the executed experiment's specific formula
(`s(e)=R×Relevance×Currency×Independence` with saturating aggregation `1-∏(1-s_i)` — a third distinct
aggregation formula in the corpus) is a reasonable engineering choice, "not derived from our theory," and
must not be frozen — proposing the next step: systematically test the six candidate algebras against the
full accumulated law set to move from "a plausible evidence model" to "a formally tested evidence calculus
suitable for implementation in KnowledgeOS."

**Bridge candidates**: reconcile four independent axiom/law-numbering conventions now in the corpus's
Evidence theory; trace whether the six-algebra comparison is executed next; locate the referenced CSV
artifact.

## Classification Revision History
| Date | Event | To | Reason |
|---|---|---|---|
| 2026-09-02 | initial | `c1`/`g1`, critical | landmark: the corpus's first documented executed computational experiment (a ten-case evidence-assessment simulation with a CSV artifact), empirically confirming several evidence-assessment laws, sharpening monotonicity into a two-level distinction, rejecting a naive additive confidence model, exposing a necessary five-stage pipeline ordering, and resolving the central open evidence-algebra question via a DDD-analogous "domain defines invariants, policy implements decisions" architecture |

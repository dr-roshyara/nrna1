# File Analysis — 0229

*(Ultra-concise record (MD-013) — see 0229.yaml.)*

## Source
`docs/knowledgeos/brainstorming/kernel/20260824-140628-zero-and-leonardo-lenses-on-the-adaptive-inference-kernel.md`
· canonical_source 0229 (self — formalizes a mandatory pre-inference readiness gate).

## Classification
`g1` primary (Zero/Leonardo lenses), `c1` secondary (formalized into the escalation-cost architecture).

## Tier 2
**TRIGGERED (critical).** Applies Zero + Leonardo (only) to 0227's AI-Efficiency Kernel. Zero: no
computation without sufficient basis. Leonardo: no decision from an incomplete bounded context. Produces
**Z-KOS Computational Gate** (`NoBasis` ≠ `False` ≠ `UnknownState`, an O(1) fail-before-inference check) and
a two-dimensional readiness model: binary prerequisite basis `B` (product of per-item booleans — any zero
means `NO_BASIS` regardless of statistical confidence, a direct anti-hallucination mechanism) vs. a
context-completeness coverage **vector** `C` (domain/authority/temporal/dependencies/operational/security),
enabling a targeted **Evidence Acquisition Planner** instead of "retrieve everything." Introduces
`KnowledgeKey=(Context,Concept,Meaning,Version)` — same expression, different bounded context, different
knowledge object (worked "approved" example) — independently reinforcing the registry's Identity-invariant
fault line (0214/0219) via a third route. Formalizes a four-state readiness taxonomy (`NO_BASIS`/
`INSUFFICIENT_CONTEXT`/`CONTEXT_CONFLICT`/`READY`) via truth table `R=(B,C,K)`, and unifies with 0226's
cost formalism: `Ready(Q,E)=1` gates `M*=argmin[Compute(M)+ExpectedLoss(M)]`. Closes with the reframed
question — not "make the LLM faster" but "determine whether an LLM is needed at all" — and a formal
**Epistemic Readiness Principle**.

## Classification Revision History
| Date | Event | To | Reason |
|---|---|---|---|
| 2026-09-01 | initial provisional | `g1`/`c1` | formalizes a mandatory Zero+Leonardo readiness gate unifying with 0226's cost architecture; KnowledgeKey independently reinforces the Identity-invariant fault line via a third route |

# File Analysis — 0578

*(Ultra-concise record (MD-013) — see 0578.yaml.)*

## Source
`docs/knowledgeos/brainstorming/phase_measure_theory/20260827-182534_step-025c-evidence-aggregation-algebra.md`
· canonical_source 0578.

## Classification
`c1` primary / `g1` secondary — PRIMARY_RESEARCH, importance **critical**.

## Tier 2
**TRIGGERED — LANDMARK: requirements-first evidence-algebra derivation, culminating in a rigorous rejection
of four candidate mathematical frameworks.** Opens Step 25C: "one of the most important mathematical
points in the entire KnowledgeOS experiment." Governing question: how to combine multiple evidence items
"without double-counting, losing uncertainty, or manufacturing certainty?" Reaffirms 0577's discipline:
derive required properties BEFORE choosing Bayesian/Dempster-Shafer/fuzzy-logic/weighted-scoring.

Delivers **`No numerical value without defined semantics`** (a bare `0.8` is meaningless without knowing
which of several distinct quantities it represents). Delivers an experimental seven-dimension Assessment
decomposition and a four-value qualitative support relation.

**Delivers the file's central formal deliverable — eight labeled experiments (A-H), each deriving a
specific required algebraic property**: (A) duplicate evidence → **Idempotency** (`x⊕x=x`); (B)
independent corroboration → **`Evidence identity and evidence independence are different`**; (C) dependent
(copied) evidence requires explicit `Dep(E_2,E_1)`; (D) contradiction requires an explicit `Conflict(H)`
state rather than a naive averaged `0.5` — **`Conflict≠Uncertainty`**; (E) stale evidence requires
`TemporalFit(E,H,t)`; (F) **`Reliability≠Relevance`**; (G) **`Authenticity≠Correctness`** (extends
0562's/0573's Authenticity≠Truth); (H) AI evidence explicitly rejects the naive `Reliability=0` shortcut —
an LLM can produce useful extraction, transformation, interpretation, or hypothesis, not only
hallucination, requiring the system to represent HOW output was produced.

**Delivers a major new result**: aggregation must operate over evidence **LINEAGE**, not evidence
**COUNT** (worked three-hop Source→LLM-summary→Human-report chain). Delivers three further required
properties, each carefully qualified: **Commutativity** and **Associativity**, both explicitly qualified
by **contextuality** — the true aggregation object is `Aggregate(E_1,E_2|QueryContext)`, not a bare
`E_1⊕E_2`.

Delivers a first non-scalar aggregated-evidence structure `ℰ(H)=(S_H,C_H,U_H,L_H)`, worked through a
Rollback example contrasting a bare confidence figure against a much more useful qualitative statement.
Resolves the eventual-need-for-numbers concession via a clean pipeline: **`Evidence Algebra→Assessment
Representation→Decision Model`**, rejecting a naive `Evidence→OneUniversalScore` collapse.

**Delivers the file's central formal deliverable — a rigorous, individually justified evaluation of four
candidate frameworks, each explicitly "Candidate, not selected"**: Bayesian updating (credited for
sequential evidence; criticized because naive conditional-independence assumptions risk double-counting
correlated evidence — "makes the dependency model explicit," doesn't eliminate the problem);
Dempster-Shafer (credited for `Bel(H)≠Pl(H)` epistemic-uncertainty representation; flagged for weak
conflict/dependence handling); Fuzzy logic (credited for linguistic concepts; explicitly warned
`FuzzyMembership≠Probability`); Weighted scoring (credited only for computational ease; five named failure
points — arbitrary weights, dependence difficulty, conflict-erasing averaging, unclear calibration, no
intrinsic epistemic meaning) — **`Useful engineering heuristic≠Universal epistemic algebra`**.

**Delivers the file's central architectural conclusion**: a five-layer stack (Evidence Representation→
Evidence Dependency/Lineage→Assessment Algebra→Statistical/Logical Inference→Decision Model), directly
extending the corpus's recurring "no universal formula" pattern, now applied concretely to this specific
imminent decision. Delivers a corresponding four-context DDD proposal (Evidence/Assessment/Statistical/
Decision Context), rejecting a monolithic `KnowledgeConfidenceService`.

**Delivers seven concrete falsification tests**, each with an explicit expected result and failure
criterion: order-independence, duplication (explicit `FAIL` if triplicated identical evidence strengthens
support), dependency, contradiction (forbidding a bare `0.5` with conflict discarded), irrelevant evidence,
stale evidence (query-relativity), and the climactic test — **"stronger evidence does not necessarily mean
higher readiness"** (Rollback Unknown→Failed lowering readiness), formalizing
**`EvidenceStrength≠GoalUtility`**, reconfirming the corpus's central MoreKnowledge⇏MoreReadiness finding
once more as a required algebraic property.

**Delivers a culminating provisional Assessment-state structure** `𝒜(H|C,t)=(Support,Challenge,
Uncertainty,Dependency,TemporalFit,Provenance)`, paired with a separate inference function
`Inference(𝒜,M)→Result`.

**Delivers the file's single most significant architectural refinement, explicitly self-named as such —
the formal three-layer separation**: Evidence state `ℰ` / Epistemic assessment `𝒜` / Inference `ℐ` —
**`Evidence≠Assessment≠Inference`**, "one of the strongest refinements produced by 25C." Delivers the
resulting clean LLM architectural home: `Extract→CandidateEvidence` or `Interpret→CandidateAssertion`, but
the kernel mandates `Candidate→Assessment→Inference→Commitment` — "the LLM does not own the epistemic
algebra."

**Delivers Step 25C's verdict**: **"25C — CONDITIONAL PASS,"** with the "key achievement" explicitly
identified as NOT selecting a probability framework but discovering that KnowledgeOS requires a layered
evidence algebra: `Evidence→Evidence Graph→Assessment→Inference→Decision`.

**Delivers six named unresolved sub-problems (C1-C6)**: Evidence dependency (precise independent-
contribution calculation); Support aggregation; Conflict (mathematical coexistence of support/challenge);
Uncertainty (choosing among Probability/Belief-Plausibility/Intervals/Sets/Qualitative states); Calibration
(does a stated `P(H)=0.8` behave like a genuine 80% probability?); Decision conversion.

**Closes by proposing "Step 25C.1 — Compare Candidate Evidence Algebras,"** rejecting intuition-based
selection in favor of running the SAME scenarios through all four frameworks against an eleven-item
requirements checklist. Delivers the closing methodological principle: "the winner should not be 'the
mathematically fanciest framework'... the fewest unacceptable assumptions," plus an explicit **advance
falsifiable prediction**: "I suspect the final answer will not be one universal algebra. It may be a
composable epistemic mathematics in which qualitative state, provenance/dependency graphs, statistical
models and decision mathematics each do the job they are actually suited for." As with the whole 25A/25B/
25C sub-arc so far, no actual code execution is reported.

**Bridge candidates (priority)**: trace whether Step 25C.1 is executed as its own file, and whether the
advance prediction (composable, not universal) is confirmed or refuted; trace whether C1-C6 are addressed
sequentially.

## Classification Revision History
| Date | Event | To | Reason |
|---|---|---|---|
| 2026-09-02 | initial | `c1`/`g1`, critical | LANDMARK: rigorous requirements-first evidence-algebra derivation via eight labeled experiments (A-H), individually justified rejection of Bayesian/Dempster-Shafer/Fuzzy-logic/Weighted-scoring frameworks, seven concrete falsification tests, and the corpus's single most significant refinement of evidence representation — the three-layer Evidence≠Assessment≠Inference separation, self-named "one of the strongest refinements produced by 25C"; reconfirms MoreKnowledge⇏MoreReadiness as an explicit algebraic requirement; verdict "25C — CONDITIONAL PASS" with six named C1-C6 sub-problems; opens Step 25C.1 with a falsifiable advance prediction of a composable (not universal) final answer |

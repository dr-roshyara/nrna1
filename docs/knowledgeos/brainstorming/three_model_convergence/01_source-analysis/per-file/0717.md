# File Analysis — 0717

*(Ultra-concise record (MD-013) — see 0717.yaml.)*

## Source
`docs/knowledgeos/brainstorming/phase_measure_theory/20260828-125526_step-131-from-assurance-graph-to-knowledgeos-logical-architecture.md`
· canonical_source 0717.

## Classification
`c1` primary / `g1` secondary — PRIMARY_RESEARCH, importance **critical**.

## Tier 2
**TRIGGERED — delivers the first concrete, six-layer KnowledgeOS Logical Architecture, the most directly
actionable software-architecture deliverable in the reconstruction to date.** Opens with
`SemanticObject≠SoftwareComponent` / `BoundedContext≠Microservice`, then delivers the full target diagram:
Actors → Agent/Application → Application Services → Domain (Governance/Knowledge/Evidence/Assurance,
containing the Assurance Graph) → Integration → Infrastructure.

**Delivers the explicit dependency rule `Infrastructure→Application→Domain`, never the reverse — which
strikingly and independently converges with this very repository's own governing Laravel architecture
discipline** (CLAUDE.md Rule 1: Domain layer zero Laravel dependencies, Infrastructure Laravel allowed
freely) — a meaningful independent-convergence data point. Delivers per-domain candidate responsibilities
for Governance/Knowledge/Evidence/Assurance, with `CloseFinding`'s placement (Assurance vs. Governance)
flagged as an unresolved boundary.

**Delivers the Agent Port/Adapter pattern** — `KnowledgeOS→AgentPort→{ClaudeAdapter,CodexAdapter}` — the
first concrete architectural implementation of the long-tracked Claude/Codex symmetry principle
(0709/0714/0716), explicitly motivated to prevent vendor-specific coupling. Delivers a parallel Tool Port
(`Agent→ToolPort→{Git,CI,Kubernetes,Nexus,Shell}`) with Tool-execution-becomes-evidence
(`GitCommand→ExecutionRecord`), directly extending 0711's `ToolExecution` proposal into concrete placement.

Delivers named Integration-layer adapters (GitAdapter/NexusAdapter/KubernetesAdapter/CIAdapter/IAMAdapter/
MonitoringAdapter) reconfirming the Anti-Corruption-Layer discipline, and Search placement as an
infrastructure/application capability (`Keyword+Semantic+GraphTraversal`) directly operationalizing 0716's
hybrid-retrieval synthesis. **Delivers a Query-vs-Command distinction directly matching this repository's
own CQRS-Light Rule 5** — a second independent structural convergence.

**Delivers the Capability ladder (Level 0-5: Read/Analyze/Recommend/Prepare/Execute-sandbox/
Execute-production)**, extending 0703's L0-L4 and 0709's A0-A6 taxonomies, and the precise
`AgentCapability≠AgentAuthority` principle. **Delivers the Policy Enforcement Point (PEP), explicitly named
for the first time**: `Agent→ActionRequest→PolicyEnforcement→{DENY,ALLOW→ToolAdapter}`. Delivers the
precedence invariant `Authorize≺Execute≺Verify` and the complete action pipeline diagram — "the executable
control loop," the most complete single action-lifecycle diagram in the reconstruction to date.

**Delivers a genuinely valuable Human-in-the-loop worked classification table**
(`Risk+Authority+ActionType→RequiredApproval`: read/test/branch/PR → automatic; merge/deploy → approval;
change governance rule → governance authority), plus Approval as a domain event (`DecisionApproved`).

**Delivers the Assurance dual-nature distinction**: `AssuranceDomain` (semantics: Rule/Verification/
Finding) vs. `AssuranceExecutionInfrastructure` (mechanisms: RepositoryScanner/RuntimeAPI/StaticAnalyzer),
worked through a classic hexagonal diagram. **Names Hexagonal/Ports-and-Adapters architecture explicitly,
paired with an appropriately non-dogmatic "avoid architecture-by-pattern" caution**: "we should not declare
'KnowledgeOS is hexagonal' merely because hexagonal architecture is fashionable" — justified specifically
because the domain must remain independent of volatile external systems (Claude, Codex, Kubernetes, Nexus).

**Delivers a concrete fifteen-item Current-state inspection checklist** (repository/package structure,
`.claude/`, `.codex/`, `AGENTS.md`, registry, governance artifacts, hooks, deterministic assurance, memory,
persistence, APIs, integration boundaries, existing context/knowledge model, evidence model, current
deployment), the Architecture-delta formalization (`ΔArchitecture=Target-Current`, explicitly NOT "rewrite
everything," classified `Retain/Refactor/Introduce/Remove/Integrate`), and **the mature Migration principle:
"strengthen existing architecture before replacing it"** — directly consonant with this repository's own
actual engineering discipline against gratuitous rewrites.

**Delivers AFR-27 through AFR-30**, bringing the fitness-rule catalog to thirty rules total: AFR-27 (domain
must not depend on agent-specific infrastructure); AFR-28 (domain must not depend directly on
external-system SDKs); AFR-29 (agent execution must pass through governed application boundaries); AFR-30
(material actions must pass through authorization enforcement).

**Delivers the file's central, memorable capstone statement of "the architectural center"**: explicitly NOT
Claude, Codex, Postgres, a VectorDB, or Nexus — "**the center is: Governed Engineering Knowledge + Evidence
+ Assurance**" — with AI framed as "a replaceable reasoning mechanism around this core," crystallized into
the final principle: **"AI is probabilistic; governance authority must be deterministic and explicit."**

Verdict: the first KnowledgeOS Logical Architecture established
(`Actor→Agent/Application→Domain→Ports/Adapters→ExternalSystems/Infrastructure`), with seven explicit
design guarantees enumerated.

**Closes by opening "Step 132 — Current-State Reconstruction," explicitly declaring a MODE CHANGE from
conceptual design to actual architecture assessment** — the sharpest, most concretely itemized promise of
genuine empirical engagement in the entire reconstruction to date: "further conceptual design without
inspecting the actual system would become dangerous," promising a `Current→Target Architecture Delta
Matrix` with a five-way outcome classification (`Already Exists|Partially Exists|Missing|Architecturally
Wrong|Unknown`) — "this is the point at which the KnowledgeOS reconstruction becomes an actual architecture
assessment rather than a conceptual design exercise." Per the 0694 calibrating meta-finding this is recorded
without escalating framing, but its structural specificity genuinely exceeds every prior such promise
(0692/0693/0709's), making Step 132 (0718) an especially important file to check for fulfillment versus
continued deferral. No actual code execution, repository inspection, or verified filesystem/architecture
claim reported anywhere in this file.

**Bridge candidates**: read 0718 (Step 132) with the HIGHEST priority attention paid to any file since the
0694 calibrating meta-finding — determine definitively whether it fulfills this file's mode-change promise
with real repository inspection, or extends the promise-then-defer pattern once more.

## Classification Revision History
| Date | Event | To | Reason |
|---|---|---|---|
| 2026-09-03 | initial | `c1`/`g1`, critical | Delivers the first concrete six-layer KnowledgeOS Logical Architecture (hexagonal design, named ports/adapters, capability ladder, explicitly named Policy Enforcement Point, complete action pipeline, mature migration principle). Independently and strikingly converges with this repository's own governing Laravel architecture discipline on two separate structural points (dependency-inversion rule, Query-vs-Command/CQRS distinction). Extends the AFR catalog to thirty rules. Crystallizes "the architectural center" and "AI is probabilistic; governance authority must be deterministic." Closes with the sharpest, most concretely itemized promise of genuine empirical engagement in the reconstruction to date, declaring an explicit mode change |

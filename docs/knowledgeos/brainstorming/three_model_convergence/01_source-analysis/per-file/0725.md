# File Analysis — 0725

*(Ultra-concise record (MD-013) — see 0725.yaml.)*

## Source
`docs/knowledgeos/brainstorming/phase_measure_theory/20260828-125937_step-138-aggregates-commands-and-domain-events.md`
· canonical_source 0725.

## Classification
`c1` primary / `g1` secondary — PRIMARY_RESEARCH, importance **critical**.

## Tier 2
**TRIGGERED — delivers thorough tactical-DDD elaboration for the eighteen-object model, with two genuinely
significant architectural clarifications.** Opens with "aggregate boundaries follow invariants, not nouns,"
then delivers lifecycle state machines, commands, and events for each object (Decision, Policy, Exception,
KnowledgeClaim, Evidence, Observation, FitnessRule, Verification, Finding, AgentSession, Recommendation,
Authorization, Action), largely consolidating prior-established distinctions with a few valuable
refinements: `VerificationFailedToExecute≠FAIL` (a crashing checker produces UNKNOWN, not FAIL); `FAIL⇏
Finding universally` (`Policy(FAIL)→Disposition`); `Authorization Context=TO BE CONFIRMED` (an honest,
appropriately deferred boundary decision rather than a premature new bounded context).

**Delivers the file's most significant architectural clarification** — "the Assurance Graph may therefore
be a projection of bounded-context state, rather than the transactional source of truth for every context...
this is a very important refinement" — "bounded contexts own state; Assurance Graph connects it" — resolving
a latent ambiguity from 0716 by cleanly assigning the graph CQRS read-side/projection status rather than
"one giant GraphAggregate."

Delivers Transaction-boundary discipline (each context transacts internally, cross-context propagation via
events) and **the Source-of-truth rule, "one of the most important architectural invariants"**: exactly one
authoritative owner per semantic concept, everything else a projection/cache/index/reference — worked
through concrete reaffirmations (`Decision_Governance` authoritative, `Decision_AgentMemory` not;
`RuntimeState_Kubernetes` authoritative, KnowledgeOS's observation not).

Delivers per-command authorization scoped to its semantic owner and Risk-based authorization
(`RequiredApproval=f(ActionType,Scope,Environment,Risk,Authority)`). **Delivers the nine-step Agent
execution contract, the most concrete, sequential agent-execution specification in the reconstruction,
directly operationalizing 0717's Agent Port**: request context→receive governed context→reason→produce
recommendation→request action→receive authorization→execute through tool boundary→return evidence→receive
verification — "the common contract for Claude and Codex," giving KnowledgeOS "model-level independence
from a specific AI vendor."

**Delivers the file's second most significant contribution — event authenticity and "this prevents
semantic spoofing"**, a sharp, directly implementable AI-governance principle: an event must be emitted by
the component that owns the transition; a Claude transcript stating "I approve this decision" is explicitly
an `AgentStatement`, categorically not the domain event `DecisionApproved` — crystallized as "**natural
language assertion≠domain state transition**... a foundational principle for AI-native systems," among the
most directly implementable, broadly applicable AI-safety mechanisms in the reconstruction.

Delivers three clean cross-context integration patterns (synchronous query/command/domain event) and
"avoid event-driven everything" (a rules-lookup query should never become a `RuleQueryRequestedEvent`).
Delivers the candidate domain-event catalog organized by owning context and **"the most important event
chain" — the complete KnowledgeOS assurance event chain**: `DecisionApproved→ContextBuilt→
RecommendationProduced→ActionAuthorized→ActionExecuted→EvidenceCaptured→VerificationCompleted`, with
`VerificationCompleted(FAIL)→FindingRaised`, worked through a concrete graph-reconstruction example.

**Delivers an appropriately cautious non-conclusion on event sourcing**, directly extending the
reconstruction's technology-independence discipline: "we should NOT yet conclude that KnowledgeOS must be
event-sourced" — the persistence strategy remains open.

Verdict: six boxed principles (one authoritative owner per concept; commands request state changes; only
the owning context performs authoritative transitions; events record completed transitions; evidence
records what happened; the Assurance Graph connects independently-owned states) and the overall flow
`Governance→Knowledge→Agent→Authorization→Execution→Evidence→Assurance→Governance`.

**Closes by opening "Step 139 — Context Map & Integration Contracts,"** promising the first formal
KnowledgeOS Context Map (upstream/downstream, query/command/event classification, translation points, ACL
placement, the Assurance Graph's role as projection) — posing directly: "how can KnowledgeOS connect
Governance, Knowledge, Evidence, Assurance, Agents, Git, CI/CD, Kubernetes and Nexus without allowing any
of them to become the accidental owner of another context's semantics?" No actual code execution,
repository inspection, or verified filesystem/architecture claim reported anywhere in this file.

**Bridge candidates**: read 0726 (Step 139) with standard attention — check for genuinely new
context-mapping content versus further consolidation of already-established Anti-Corruption-Layer
discipline.

## Classification Revision History
| Date | Event | To | Reason |
|---|---|---|---|
| 2026-09-03 | initial | `c1`/`g1`, critical | Delivers thorough tactical-DDD elaboration for the eighteen-object model, largely consolidating prior distinctions. Contributes two genuinely significant clarifications: the Assurance Graph's precise reframing as a read-side projection (resolving latent ambiguity from 0716), and the semantic-spoofing-prevention principle (natural language assertion≠domain state transition) — among the most directly implementable AI-governance mechanisms in the reconstruction. Delivers the Source-of-truth rule, the nine-step Agent execution contract, the complete assurance event chain, and a deferred event-sourcing decision. Opens Step 139's well-motivated Context Map continuation |

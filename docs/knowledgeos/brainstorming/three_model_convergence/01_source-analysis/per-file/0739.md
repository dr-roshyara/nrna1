# File Analysis — 0739

*(Ultra-concise record (MD-013) — see 0739.yaml.)*

## Source
`docs/knowledgeos/brainstorming/phase_measure_theory/20260828-131421_step-151-knowledgeos-vertical-slice-v0-1.md`
· canonical_source 0739.

## Classification
`c1` primary / `g1` secondary — PRIMARY_RESEARCH, importance **critical** (MAJOR SYNTHESIS MILESTONE).

## Tier 2
**TRIGGERED — decisively confirms and exceeds the maximum-priority concreteness test.** Delivers the
objective "to prove one complete, deterministic, auditable path" using Nexus, deliberately read-only, with
epistemic discipline maintained even here ("the actual rule must come from the existing governance/
assurance material rather than being invented merely to satisfy the demo").

**Delivers a genuinely concrete repository structure sketch — the first actual repository-directory-level
artifact in the entire reconstruction**: `knowledgeos/{AGENTS.md, docs/, src/{governance,knowledge,
evidence,assurance,agent,action,authorization,context}, adapters/{nexus,git,ci}, infrastructure/, tests/,
.claude/, .codex/}`, distinguishing stable architectural ownership from technology-specific layout.
Delivers minimal per-context "v0.1" schemas for Governance/Knowledge/Evidence/Assurance/Context, each
deliberately smaller than the full target model.

**Delivers an illustrative API contract with worked JSON request/response examples — the first concrete
payloads in the reconstruction**: `GET /context, POST /verifications, POST /evidence, POST
/recommendations, POST /actions, POST /authorizations/evaluate`, explicitly caveated as illustrative
protocols, not the architectural contract itself.

**Delivers "checker must not use the LLM," an important assurance boundary applied at the implementation
level for the first time**: `f(input)=same output` — "the LLM may explain the result. It should not decide
the authoritative verdict" — sharply operationalizing 0705's/0709's/0735's deterministic-assurance
discipline into an actual component constraint.

Delivers a full worked assurance chain (Evidence E42→Verification V42 with explicit immutability→Finding
F42, "the minimal assurance chain") and a first-Action restriction to `READ_ONLY`. **Delivers the numbered
17-step first Golden Trace, the most operationally concrete Golden Trace walkthrough in the reconstruction**
— directly executable as an integration-test outline — mapped to four ID types (TraceID/CausationID/
EvidenceID/RuleID+RuleVersion).

**Delivers an actual concrete relational persistence table list** (`agents, tasks, contexts; decisions,
policies, rules; claims, evidence, verifications, findings; recommendations, actions, authorizations,
executions; domain_events, outbox, audit_records`) — the first genuinely concrete schema artifact in the
reconstruction. **Delivers "do not create generic EAV,"** a valuable, precisely reasoned database-design
anti-pattern warning. Delivers a concrete graph-edge list matching the Golden Trace exactly and **"do not
make graph mandatory for context v0.1"** — "the first context path should work even if the graph is
unavailable. That proves: `Graph≠Authority`" — the most concrete implementation-level test of the
graph-as-projection principle established across 0725/0727/0733/0734/0737. Delivers a minimal ten-event
vocabulary and the Harness boundary test — the first genuinely codeable architectural-compliance test for
the pointer-layer principle (`.claude/`/`.codex/`/`AGENTS.md` must not own Governance state).

**Delivers the first Definition of Done, a genuinely concrete, checkbox-based DoD across seven categories**
(Domain/API/Nexus/Assurance/Traceability/Agent/Persistence), matching this repository's own actual governing
DoD discipline. **Delivers the Golden Trace acceptance criterion**: "delete all projections. Then rebuild
them from authoritative state/events... this proves that the projections are not secretly authoritative" —
the strongest possible rebuildability test. **Delivers five named failure acceptance tests (Test A-E)** and
**a Determinism acceptance test** (`Verify(E,R)=V` must hold under repetition, "the result should not change
because a different LLM happened to process it"), paired with the Agent-nondeterminism boundary (agents may
reason differently; Authorization and Verification must remain governed/deterministic).

**Delivers "the critical separation" capstone**: "the LLM does not disappear. It is simply placed in the
correct architectural position" — crystallizing 0738's Control-Plane-vs-Data-Plane distinction. **Delivers
"what we should NOT build yet," an explicit ten-item scope-exclusion list**, the most concrete itemized
scope boundary in the reconstruction (❌ autonomous production deployment, ❌ automatic migration, ❌
universal enterprise ontology, ❌ multi-agent swarm, etc.).

Delivers a new capstone definition of KnowledgeOS ("a governed engineering control and evidence platform
that provides agents with authoritative context and constrains consequential actions. The AI layer sits on
top"), **"the crucial distinction"** — a clean three-tier truth taxonomy (Agent assertion=LOW authority;
Engineering observation=OBSERVATIONAL authority; Governed decision=GOVERNANCE authority, connected by
Assurance's `Expectation+Observation→Verification`) — and **the KnowledgeOS core equation**, the most
compact single formula in the reconstruction:
`KnowledgeOS=Governance+Knowledge+Evidence+Assurance+Context+Authorization+Traceability`, with
`AI=Consumer+Reasoning Actor`.

Verdict: "do not implement the whole platform. Implement one complete Golden Trace" — named "Nexus
Read-Only Assurance Trace."

**Closes by opening "Step 152 — Implementation Architecture Constitution v1.0,"** promising a
machine-checkable constitution (dependency rules, module ownership, allowed APIs, legal state transitions,
explicit Claude/Codex constraints, determinism/auditability requirements, compliance tests) —
`Architecture→Rules→Automated Verification` — explicitly invoking "the existing KnowledgeOS governance
approach — registry, hooks, deterministic assurance, session/change logging, `.claude`/`.codex` symmetry —
as the enforcement mechanism." **This is a THIRD constitution-naming attempt** (after 0705's ratified C1-C7
and 0730's WP20 seven-rule set, neither cross-referenced here) — flagged for careful tracking. No actual
code execution, repository inspection, or verified filesystem/architecture claim reported anywhere in this
file, though its content is the most directly translatable into actual code of any file in the entire
reconstruction to date.

**Bridge candidates**: read 0740 (Step 152) with the HIGHEST priority — determine whether this third
constitution attempt reconciles with 0705's C1-C7 and 0730's WP20, and whether it delivers genuine
machine-checkable rule specifications beyond boxed-formula invariants.

## Classification Revision History
| Date | Event | To | Reason |
|---|---|---|---|
| 2026-09-03 | initial | `c1`/`g1`, critical (major-synthesis) | Decisively confirms and exceeds the maximum-priority promise of implementation-level concreteness. Delivers the reconstruction's first genuinely concrete repository structure, minimal per-context schemas, illustrative API endpoints with worked JSON payloads, an actual relational table list, and the implementation-level "checker must not use the LLM" constraint. Delivers a directly executable 17-step Golden Trace, a checkbox-based Definition of Done, a rigorous delete-and-rebuild acceptance criterion, five named failure tests, a determinism test, and an explicit ten-item scope-exclusion list. Delivers the compact KnowledgeOS core equation and a clean three-tier truth taxonomy. Opens a third constitution-naming attempt requiring careful tracking |

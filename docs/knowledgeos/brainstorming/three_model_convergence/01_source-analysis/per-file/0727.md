# File Analysis — 0727

*(Ultra-concise record (MD-013) — see 0727.yaml.)*

## Source
`docs/knowledgeos/brainstorming/phase_measure_theory/20260828-130050_step-140-knowledgeos-logical-component-architecture.md`
· canonical_source 0727.

## Classification
`c1` primary / `g1` secondary — PRIMARY_RESEARCH, importance **critical**.

## Tier 2
**TRIGGERED — adds component-level granularity to 0717's Logical Architecture, largely consolidating prior
distinctions, and delivers the reconstruction's first proposed formal Architecture Decision Records.**
Delivers a refined five-layer diagram (Application/Domain/Cross-Context Graph/Ports-Adapters/external
systems), reconfirming `BoundedContext≠Microservice`. Delivers per-domain object/responsibility lists for
Governance/Knowledge/Evidence/Assurance/Agent Execution (the latter explicitly "deliberately subordinate" —
consumes Knowledge/Governance/Assurance, produces Recommendation/Action, never owns Decision/Policy/
Authority).

**Delivers Graph rebuildability, a genuinely valuable operational-resilience property**: "projection can be
reconstructed from authoritative state... this significantly reduces architectural risk" — if the graph is
lost but domain state remains intact, it can be rebuilt. **Delivers `Registry≠Graph`** (Registry answers
"what exists?", Graph answers "how are things related?"), extending 0719's/0721's registry-classification
discipline.

Delivers the Deterministic assurance engine as a separated capability with **a named seven-item Checker
Plugin taxonomy** (ArchitectureChecker/DependencyChecker/RepositoryChecker/SecurityChecker/RuntimeChecker/
NexusChecker/KubernetesChecker) implementing a common `Checker` port. Reconfirms the Agent Port/adapter
pattern and pointer-layer diagram (Claude/Codex via a shared Agent Application Port).

Delivers a five-category Storage-architecture taxonomy (Transactional domain state/Evidence store/Graph
projection/Artifact storage/Agent operational state), the persistence decision left `Undecided`, and the
recommended sequence `Modules→Contracts→Persistence boundaries→Deployment boundaries` (never
`Microservices→discover domains`).

**Delivers "one important correction"**: KnowledgeOS owns only the platform representation of an agent
(`AgentIdentity`/`AgentSession`), never the actual implementation — `ClaudeImplementation∉
KnowledgeOSDomain` — with the same principle applied to Nexus (`NexusReference` only, not
`NexusRepositoryLifecycle`).

**Delivers `LA-01`** (core domain modules must not depend directly on external infrastructure SDKs) and a
worked Read-side composition example ("what do I need to know before migrating Nexus?") — "this is the key
AI architecture": the agent receives `TaskRelevantContext`, formalized as `Context=Filter(Graph,Task,Actor,
Time,Scope)`, explicitly rejecting "`SELECT * FROM knowledge`."

**Delivers Context integrity and Context-to-action traceability** — `ContextID/ContextVersion/ContextHash`
plus `Action→ContextID`, enabling **`Reconstruct(Action,t)`**: "reconstruct the information and authority
available to the agent when it acted... a major capability for trustworthy AI engineering," directly
operationalizing 0711's/0715's/0716's prior context-hashing and audit-reconstruction proposals into concrete
fields.

**Delivers the reconstruction's FIRST proposed formal Architecture Decision Record series (ADR-KOS-01
through ADR-KOS-05), a genuinely significant step toward institutionalizing the accumulated synthesis**:
ADR-KOS-01 (modular bounded-context platform); ADR-KOS-02 (Assurance Graph as cross-context projection);
ADR-KOS-03 (common agent semantic contract); ADR-KOS-04 (agent-local files are pointer/operational layers,
not authoritative knowledge — the pointer-layer principle recast for the first time as a candidate formal
ADR rather than a numbered invariant); ADR-KOS-05 (deterministic assurance authoritative wherever
mechanically verifiable) — explicitly "should remain proposed until validated." This is a meaningfully
different kind of artifact than the AFR/CM/IR/LA numbered-invariant series produced throughout Steps
128-140: it packages the synthesis into named governance decisions this repository's own actual ADR
discipline would recognize.

Verdict gives the logical component architecture "a coherent shape," restating "do not equate bounded
contexts with deployable services."

**Closes by opening "Step 141 — Deployment & Runtime Architecture,"** distinguishing Logical from Deployment
architecture, posing an eleven-item runtime-inspection checklist (what runs where, where does deterministic
assurance execute, where should the Assurance Graph be materialized) — "this is where the KnowledgeOS
architecture becomes an actual runtime architecture, rather than only a DDD model." Given the established
0717-0721 mode-change pattern, tracked with calibrated (not escalating) attention per the 0694 meta-finding.
No actual code execution, repository inspection, or verified filesystem/architecture claim reported anywhere
in this file.

**Bridge candidates**: read 0728 (Step 141) checking whether it delivers genuine runtime-inspection
findings or continues the promise-then-defer pattern, and whether the proposed ADR-KOS series is ever picked
up later.

## Classification Revision History
| Date | Event | To | Reason |
|---|---|---|---|
| 2026-09-03 | initial | `c1`/`g1`, critical | Adds component-level granularity to 0717's Logical Architecture, largely consolidating prior distinctions. Delivers Graph rebuildability, Registry≠Graph, a named Checker Plugin taxonomy, LA-01, and a precise agent-ownership-boundary correction. Delivers Context-integrity fields enabling Reconstruct(Action,t). Most significantly, delivers the reconstruction's FIRST proposed formal Architecture Decision Record series (ADR-KOS-01..05), recasting the pointer-layer principle as a candidate ADR for the first time. Opens Step 141's runtime-architecture continuation, flagged for calibrated tracking |

# File Analysis — 0728

*(Ultra-concise record (MD-013) — see 0728.yaml.)*

## Source
`docs/knowledgeos/brainstorming/phase_measure_theory/20260828-130122_step-141-deployment-and-runtime-architecture.md`
· canonical_source 0728.

## Classification
`c1` primary / `g1` secondary — PRIMARY_RESEARCH, importance **critical**.

## Tier 2
**TRIGGERED — delivers genuinely substantial new deployment/runtime architecture content, markedly more
novel than the immediately preceding 0725-0727.** Reframes Deployment architecture around "where does each
responsibility actually execute, and where does its authoritative state live?" **Delivers a five-zone
runtime topology** (Agent workstation/KnowledgeOS/Engineering/Runtime infrastructure/Evidence-persistence),
with the local repository framed precisely as `EdgeContext`, never authoritative.

**Delivers Offline-behavior semantics, a genuinely valuable operational consideration not previously
addressed**: three connectivity states (`ONLINE/DEGRADED/OFFLINE`) with policy-driven disposition — low-risk
actions may proceed offline, high-risk governed actions require `NoExecution`. Delivers Stateless-vs-Stateful
separation and graph-database justification explicitly conditioned on workload evidence, not assumed.

**Delivers the transactional-outbox pattern for reliable cross-context event propagation**, a mature,
directly implementable mechanism: domain-state write and outbox record commit atomically before a separate
publisher propagates the event — "reliable graph/event propagation without requiring Kafka from day one."
Reconfirms `EventuallyConsistent` graph freshness and "never use `StaleGraph` as proof of `CurrentAuthority`."

Delivers Local-vs-CI-vs-Production assurance distinctions and the full nine-stage deployment pipeline. **Delivers
Execution isolation and credential architecture, with the file's sharpest, most broadly applicable security
contribution — "why shared credentials are dangerous"**: `Agent→Tool→Authorization→ScopedCredential`
limits blast radius; if Claude and Codex share a credential, the system can observe a change but cannot
attribute it to a specific agent/action/session — `SharedCredential→WeakAccountability`. Delivers the Agent
identity chain (`AgentID/SessionID/ActionID`) and Human delegation (`actingFor≠automatically authorized`).

**Delivers a four-tier Trust-zone classification, the most concrete security-architecture trust taxonomy in
the reconstruction** (Lower Trust: agent workstation/local files/memory/generated context; Medium Trust:
agent gateway/context service/tool adapters; Higher Trust: governance/authoritative knowledge/authorization/
evidence; External Authority: Git/Kubernetes/Nexus/IAM/production) — giving the pointer-layer principle its
most concrete security-architectural grounding to date by explicitly placing `.claude/memory/` and
`AGENTS.md` in the lowest trust tier.

**Delivers a concrete Disaster-recovery data-criticality framework**: Critical (governance state/
authorization/evidence/verification history)/Important (knowledge/graph projections/registry)/Rebuildable
(context packages/caches/search indexes), with the sharp distinction: "if the graph disappears: Rebuild. If
the authoritative state disappears: `CriticalIncident`." Delivers **Platform self-assurance**, extending
0709's self-verification-vs-self-governance discipline into operational terms: KnowledgeOS must eventually
verify its own health (GraphProjectionLag/OutboxBacklog/EvidenceIntegrity/AuthorizationService checks) —
"KnowledgeOS assures engineering systems... KnowledgeOS also assures itself."

Delivers a three-stage runtime-maturity model and **"do not centralize everything immediately"** — an
explicit anti-pattern warning against moving every script/hook/memory file to a central server — plus
`LocalPASS≠FinalPASS` (local verification is supporting evidence, not the final authoritative result for
high-risk operations).

**Delivers `RA-01` through `RA-05`**, five new runtime-architecture invariants (RA-01: authoritative state
must not depend on agent-local filesystem; RA-02: high-risk actions require an authorization-aware boundary;
RA-03: external runtime systems remain authoritative for their own state; RA-04: derived projections must be
rebuildable — reconfirming 0727; RA-05: local tooling must not silently bypass central governance).

Verdict: "KnowledgeOS should be centralized where authority/evidence/cross-context coordination matter;
local where speed/developer workflow/agent integration matter" — Local Agent Edge + Central Governed
Platform + External System Authority.

**Closes by opening "Step 142 — Current-State Runtime Archaeology,"** the fifth iteration of the
reconstruction's recurring empirical-engagement promise cycle (after the unfulfilled 0717-0721 arc), now
targeting runtime/deployment topology specifically — an eight-question per-mechanism checklist, explicitly
"stop saying 'KnowledgeOS should...' and start producing CURRENT/TARGET/DELTA statements." Per the 0694
calibrating meta-finding, recorded without escalating framing. No actual code execution, repository
inspection, or verified filesystem/architecture claim reported anywhere in this file.

**Bridge candidates**: read 0729 (Step 142) with calibrated attention — determine whether this fifth
iteration is fulfilled or continues the promise-then-defer pattern.

## Classification Revision History
| Date | Event | To | Reason |
|---|---|---|---|
| 2026-09-03 | initial | `c1`/`g1`, critical | Delivers substantially new, genuinely valuable operational/deployment content not previously treated this concretely: five execution zones, offline-behavior semantics, the transactional-outbox pattern, credential architecture with "why shared credentials are dangerous," a four-tier trust-zone classification (the pointer-layer principle's most concrete security grounding to date), a disaster-recovery framework, and platform self-assurance. Delivers RA-01 through RA-05. Markedly more novel than 0725-0727. Opens a fifth iteration of the recurring empirical-engagement promise cycle, flagged for calibrated tracking |

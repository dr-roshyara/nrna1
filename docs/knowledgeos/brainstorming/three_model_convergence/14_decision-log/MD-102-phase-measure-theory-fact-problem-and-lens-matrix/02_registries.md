# MD-102 §02 — Duplicate/Content-Recombination Register, External-Theory Register, Self-Correction
Ledger, Cross-Lane Transfer Register

## Duplicate/Content-Recombination Register

- **Confirmed**: position 20 (`233107_knowledge-qualification-problem-research-framework.md`), a large
  multi-part file, contains — as its own internal tail section — a passage byte-identical (verified by
  direct text comparison) to the entirety of position 21's own file
  (`233112_evidence-connects-observation-to-facts.md`). This is **not** counted as two independent
  evidence events: the "Evidence connects observation to facts" content is logged once
  (`01_objects-and-theorystates.md`). The **reuse event itself** — the same passage appearing as both an
  embedded tail section and later as its own standalone file — is recorded here as a further, confirmed
  instance of the content-recombination pattern MD-101 first named, extending it from a
  cross-file phenomenon to one that also occurs **within** a single large cumulative file.
- No exact byte-for-byte duplicates (per MD-101's own 33-group register) appear among positions 18–24.

## External-Theory Register

No new external, independently-citable theory sources were introduced in positions 18–24 beyond what
MD-101 already registered (Doignon & Falmagne KST; Pritchard). One named external synthesis is
referenced and directly engaged: an AI system called "Perplexity" is quoted and critiqued at length in
position 20 and again in position 23 — this is **not** an independent scholarly source; it is a
same-programme, model-generated synthesis being reviewed by the corpus's own dialogue, analogous in
kind (though not in name) to the "critique of a critique" recombination pattern already seen in
MD-100's own segment. Classified: **RECONSTRUCTION-ADJACENT COMMENTARY, not EXTERNAL SOURCE** — its
content is treated exactly as the corpus's own text treats it: several claims explicitly downgraded
from "Strong Mathematical Evidence" to "candidate mathematical representation requiring investigation"
by the very next document in the dialogue (position 20's own internal self-correction, and again in
position 23).

## Self-Correction Ledger

1. **Position 19 self-corrects position 18's own "elements overlap" framing**: position 18 leaves open
   whether overlap is intrinsic to the Knowledge Space or merely an artifact of observational limits;
   position 19 resolves this (within the same dialogue, by direct author clarification) toward the
   observational/inverse-problem reading, without discarding the intrinsic-overlap question entirely
   (both are recorded).
2. **Position 20's own internal correction of the "Perplexity" synthesis**: `Φ` is downgraded from
   binary to a structured `Status(k,t,C)`; several claims are explicitly relabeled from "Strong
   Mathematical Evidence" to "candidate mathematical representation requiring investigation" — a
   genuine, source-internal self-correction, not this reconstruction's own inference.
3. **Position 23 repeats and reinforces position 20's own correction** ("we should not yet conclude
   Fact = probabilistic object... these aren't yet mathematical facts established by research") —
   recorded as **recurrence/reinforcement of an already-logged self-correction**, not a new one.

## Cross-Lane Transfer Register

**Inbound**: none new this phase.

**Outbound / forward echoes, flagged not resolved**:
- Position 24's own cross-lens convergence table (testing whether "Confidence ≠ truth," "History ≠
  current state," "boundary cannot prove external truth" recur across DDD/Nyāya/Viveka/Logic/Relations/
  Temporal/Information lenses) closely parallels, in method though not in content, this
  reconstruction's own repeated finding (across `kernel/`, the Session-1/Session-2 cluster, and now this
  lane) that independently-built classification instruments keep reappearing for the same underlying
  problem (mistyped comparisons, homonym collisions, premature ontology-from-representation moves).
  Recorded as a further structural echo, not merged, not adopted as evidence of anything beyond the
  corpus's own recurring research habits.
- The `Fact`-as-missing-bridge finding and the `K_t^*=Cn_{R_t}(S_t∪F_{≤t})` closure definition remain
  entirely non-cross-referenced (so far as read) with `kernel/`'s own parallel Kernel-formulation work
  or the math lane's `EC_t`/`Sat` chain.

**Firewall discipline maintained**: `theory-extraction/` and `verification/zero-algebra/` never
accessed.

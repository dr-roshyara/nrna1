# MD-075 §02 — Kernel ↔ Mathematical-Lane Definition Resolution Register

Per the mission's D1–D5 discipline. `kernel/` = `docs/knowledgeos/brainstorming/kernel/` (plus the
adjacent `brainstorming/synthesis/`+`00_INDEX.md` material GAP-008 found actually houses the register
— see `01_gap008-closure-report.md`'s location correction). Math lane =
`mathematical_ideas_that_can_be_implemented/`, known to this session in detail from MD-057–074 (the
canonical source `[00-47]`, Theory-00-21 Parts I/II/V/VI `[05-36]`/`[05-37]`/`[05-40]`/`[05-41]`, and
the full 876-file traversal MD-067 performed).

| Concept | Status | Kernel-lane source | Math-lane source | Note |
|---|---|---|---|---|
| **Kernel** (as a single, unified architectural-boundary object) | **D5** | 11+ competing candidate specifications (K-1..K-11 territory, plus the separate Kernel-boundary falsification chain, MD-072 §01/§02), none chosen | **Not a math-lane term at all** — the math lane works directly with `K_t`/`EC_t`/`Sat`/`Δ_t`/`Zero` as its own typed objects; it never wraps them in a formal "Kernel" boundary object | Genuinely undefined after both lanes checked — but this is expected, not alarming: the two lanes are asking structurally different questions (architectural minimality vs. formal satisfaction semantics) |
| **Knowledge** (as a philosophical primitive, "what is Knowledge") | **D5** | K-1..K-11, "all preserved, none chosen" | Math lane never asks this question directly — it works with `K_t` as a formal, time-indexed state, never defining "Knowledge" as a philosophical category | Same as above — different question, not a shared gap |
| **`K_t`** (as a time-indexed formal state object) | **D2** (with a major caveat, see `04_gap006-adjudication.md`) | Not defined in `kernel/` at all in this exact time-indexed form; `phase_measure_theory/` (a third lane, not `kernel/`) develops its own extensive `K_t` proliferation | **DEFINED** — `[00-47]` `[DEF-19]`ff, T5, later `K_t∈𝕂` (deliberately abstract) | The concept that DOES have a real math-lane definition is `phase_measure_theory`'s own `K_t`, not `kernel/`'s — `kernel/` itself barely uses time-indexed `K_t` notation at all |
| **Zero** [kernel/'s own sense: "Zero Lens," a research methodology] | **D1** | `20260824-152415` ("Zero Lens: remove prior structure, observe what is recoverable from source alone") | Math lane's own `Zero(K,EC)` (`[00-47]` `[DEF-22]`, T5) is a **formal predicate**, not a methodology | **D4/HOMONYM** relative to the math lane's `Zero` — same word, unrelated kind of object (a research heuristic vs. a formal predicate). A newly-noticed fourth instance of the corpus's own recurring "Zero" name-collision pattern (alongside `ZeroLens`/T11, `Zero_{T,Π}`/Zero-Algebra, canonical `Zero`) — flagged, not merged; see `03_...md` |
| **Decision** [kernel/'s own sense: ordinary governance English — "who may decide," decision rights] | **D4** | Extensive informal use, `dependency-map.md`/`contradiction-map.md`, no formal specification | F4's own `Decision` (T20–T22) is *tracked* but also never given its own internal formal structure (`[THM 16.38]` proves only what it is NOT — not determined by `Determination` alone) | Both sides are under-specified — this is a genuine shared gap in the corpus, not a resolvable-by-cross-checking case; recorded `D4` for both |
| **Determination** [as a capitalized, formally-intended term] | **not load-bearing in kernel/** | Zero hits found anywhere in the GAP-008 read population | **DEFINED** — F4's `Determination` (T20/T21, `[Def 3.5]`/`[Def 6.2]`, `[THM 3.1]`/`[THM 6.1]`/`[THM 6.2]`) | `kernel/` simply does not engage this concept at all — not a gap, an absence |
| **`Req`/`r`** [kernel/'s own sense: not present in `kernel/` itself — this is a `phase_measure_theory/` term, see GAP-007] | n/a to `kernel/` directly | — | **DEFINED** — F4's `Req(EC_t)`/`r` (`[00-47]` T5, T7) | Listed here for completeness; the actual comparison is `phase_measure_theory` vs. F4, adjudicated in `05_gap007-adjudication.md`, not a `kernel/`-vs-math-lane question |
| **Admission** (candidate Kernel core act) | **D1** | Extensively developed, `20260823-110950` onward, four competing hypotheses (A–D) | Not a math-lane term | No math-lane counterpart exists to check — `kernel/`'s own proliferation stands alone |
| **Authority** (Capability/Permission/Authority/Responsibility split) | **D1** (kernel/-local) / **D3, partial** | `20260823-113410` (kernel/'s own four-way split) | **`phase_measure_theory/`'s own `step_187`** independently derives the *same* four-way split (`Cap`/`Perm`/`Auth`/`Resp`, `AuthReq(r,a)`) — see MD-072 §01 | This is a genuine cross-lane structural echo — between `kernel/` and `phase_measure_theory/` (two sibling lanes), **not** between either and F4's own math-lane chain. No citation found either direction; recorded `RECONSTRUCTED CONNECTION`, not merged |

## What this register changes about GAP-008/006/007

Confirms, with evidence rather than assumption, that the "critical definition-resolution rule" the
mission opened with does **not** rescue any of `kernel/`'s own Kernel/Knowledge/Zero/Decision
vocabulary from `D5`/`D4` status by appeal to the math lane — the math lane simply does not address
these same questions in `kernel/`'s own terms. The one place the rule *does* matter is `K_t` itself:
`kernel/` barely uses time-indexed `K_t`; the real proliferation-and-possible-math-lane-relation
question belongs entirely to `phase_measure_theory/` (`GAP-006`), not to `kernel/` (`GAP-008`) — a
scope clarification this register makes explicit.

# MD-075 §03 — Kernel ↔ F4 Object Correspondence Register

Six-level adjudication ladder applied per the mission's §4: lexical → conceptual → functional →
structural → formal equivalence → demonstrated identity. Default `UNRESOLVED` unless the evidence
affirmatively supports a stronger or a weaker (unrelated) call.

| `kernel/` object | F4 object | Lexical | Conceptual | Functional | Structural | Formal equiv. | Demonstrated identity | **Verdict** |
|---|---|:---:|:---:|:---:|:---:|:---:|:---:|---|
| "Zero Lens" (research methodology) | `Zero(K,EC)` | partial (word "Zero") | weak (both touch "absence/removal") | none | none | not established | no | **HOMONYM/UNRELATED** — a method vs. a predicate |
| Kernel (architectural-boundary candidates, K-1..K-11 adjacent) | (no single F4 object — F4 never proposes a wrapping "Kernel") | n/a | n/a | n/a | n/a | n/a | n/a | **N/A — no F4-side object exists to compare against** |
| "Decision" (governance English) | `Decision` (T20–T22, tracked, unformalized) | yes | weak-moderate (both are "the thing authority acts on") | none demonstrated | none (neither side has internal structure) | not established | no | **UNRESOLVED** — both sides genuinely under-specified, not a resolvable comparison from current evidence |
| "Determination" | `Determination` (`[Def 3.5]`/`[Def 6.2]`) | n/a — term not used in `kernel/` | n/a | n/a | n/a | n/a | n/a | **N/A — not referenced in `kernel/`** |
| Admission (candidate core act) | (no F4 counterpart — closest adjacent concept is F4's own `Sat`/`Determination` boundary, but no shared vocabulary or formula) | no | weak (both are "gatekeeping" concepts in a loose sense) | none | none | not established | no | **UNRELATED** — no lexical match even, purely a loose thematic echo |
| Capability/Permission/Authority/Responsibility split | (no F4 counterpart directly; echoes `phase_measure_theory`'s own `step_187` `Cap/Perm/Auth/Resp`, itself not F4) | n/a to F4 | n/a to F4 | n/a to F4 | n/a to F4 | n/a to F4 | n/a to F4 | **N/A to F4** — this is a `kernel/`↔`phase_measure_theory/` echo, recorded in §02, not a `kernel/`↔F4 relation |

## Summary verdict for GAP-008's own F4-impact question

**No `kernel/`-originated object reaches even `FUNCTIONAL CORRESPONDENCE` with any F4-tracked object.**
The strongest finding is a same-word, different-kind-of-object collision ("Zero"), already the
corpus's fourth instance of that specific pattern (canonical `Zero`; `ZeroLens`/T11; `Zero_{T,Π}`/
Zero-Algebra; now `kernel/`'s own "Zero Lens" methodology) — recorded as `HOMONYM`, consistent with
every other instance of this pattern this reconstruction has found, never merged.

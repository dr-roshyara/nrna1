# MD-075 §06 — Updated Gap Register (delta only)

Only the three gaps this phase's mission named. MD-072's own `07_gap-register.md` is not edited — its
`GAP-006`/`007`/`008` rows stand as historical record of the state before this closure; this is the
forward-recorded update.

| Gap | Prior status (MD-072) | New status (MD-075) | Disposition |
|---|---|---|---|
| `GAP-008` | UNRESOLVED, genuinely load-bearing | **CLOSED WITH QUALIFICATION** | The classification pipeline (relocated: actually `brainstorming/synthesis/`+`00_INDEX.md`, not `kernel/`-confined) has zero occurrences of any F4-tracked symbol and no formal/structural bridge to F4 — vocabulary overlap only (see `01_gap008-closure-report.md`). Qualification: the pipeline's own *source* 256-document population remains at Level-1 census depth (MD-072), not re-verified at full depth here — only its own terminal conclusion artifacts were read to full depth. |
| `GAP-006` | UNRESOLVED, non-blocking | **UNRESOLVED, sharpened to a positive `HOMONYM` finding** | Not merely "no citation found" — the comparison is structurally ill-posed given F4's own deliberate abstraction of `K_t`'s type (see `04_gap006-adjudication.md`). Downgraded from "candidate open question" to "answered: unrelated, for a stated structural reason," though technically still `UNRESOLVED` in the sense that no document anywhere states the two are unrelated — this reconstruction's own adjudication supplies that reasoning, labeled as such. |
| `GAP-007` | UNRESOLVED | **`HOMONYM`, evidence-grounded** | Direct reading of `step_186` shows `r`/`Req(r)` occupy structurally different roles in the two formalisms (transition-subject vs. requirement-argument) — see `05_gap007-adjudication.md`. The strongest, most confidently-resolved of the three gaps this phase addressed. |

## No new gap opened

This phase's own investigation did not surface a new gap of the same load-bearing character as
`GAP-006`/`007`/`008` themselves — see `07_unchanged-frozen-conclusions-and-next-action.md` for the
one narrower, genuinely new finding that did warrant a backlog ticket (not a Gap Register entry, since
it is not blocking any part of the F4 chain's own reconstruction).

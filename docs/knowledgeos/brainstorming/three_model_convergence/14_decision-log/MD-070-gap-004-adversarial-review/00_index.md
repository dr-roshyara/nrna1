# MD-070 — Independent Adversarial Review of GAP-004 (the Theory-00-21 `Sat` Definition)

## Authorization

User's direct instruction: "Investigate GAP-004 next." MD-068 characterized GAP-004 as
UNRESOLVED/UNRECORDABLE from the corpus *as traversed*, requiring an actual independent adversarial
review — new investigative work, not further reading of already-traversed summaries. This phase
conducts that review, mirroring the discipline this corpus's own reviewers applied to every other
major claim (11+ documented instances across MD-057–069).

## What this phase is

A **CONSTRUCTED** activity, explicitly labeled as such — this reconstruction's own review, not a
corpus-native artifact. It does not retroactively change what any `TheoryState(T_n)` in MD-069
records as having existed at that historical point; it evaluates whether one specific historical
artifact (`[05-41]`'s `Sat(K,r,Γ)=Det_r(EvalReq(K,r,EC,Γ),EC)`, Def 6.18, T21 in MD-069) withstands
scrutiny under this corpus's own demonstrated standards.

## Method

Primary source files reopened and read in full (not summarized from ledgers): Theory-00-21 Parts I,
II, V, VI (`[05-36]`, `[05-37]`, `[05-40]`, `[05-41]`), plus the worked example (`[05-57]`/`[05-58]`
equivalents, the "Part XXI-A rev2" file). Every "PROVED" theorem in the core chain was checked
directly against its own stated proof, not assumed valid from a summary.

## Verdict, stated up front

**GAP-004 disposition: DOWNGRADED, not REFUTED.** The `Sat(K,r,Γ)=Det_r(EvalReq(K,r,EC,Γ),EC)`
definition is not mathematically wrong. But it does **not** do the work MD-067/068/069 credited it
with doing. It is a genuine **structural/type-level advance** (a cleaner separation of evidence-
evaluation from acceptance-rule than any prior corpus attempt) that supplies a **name and a type
signature** for the missing computation, not a **computed body** — `Det_r` remains exactly as
unspecified as `standard` (GAP-001) always was. The theory's own flagship worked example confirms
this directly: it never once invokes `Det_r`/`EvalReq`; it stipulates `Sat` values by fiat, the same
move every earlier, explicitly-retired `Sat` attempt made.

## Artifacts

- `01_findings.md` — the five findings, each with exact source location and severity.
- `02_verdict-and-corrections.md` — the calibrated verdict, and explicit corrections to MD-067/068/069
  (their text unedited, weight corrected forward per standing discipline).

## MD-070 STATUS: EXECUTED. HARD STOP — no further phase automatically opened; MD-057–069 preserved
unchanged throughout.

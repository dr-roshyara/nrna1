# Decision Context

## MD-031's finding, restated as the factual basis for this decision (not re-derived here)

`docs/knowledgeos/research/kernel-reduction/06-composition-rules.md` — cited by section number
("§06") in the already-admitted `04-operator-contracts.md`, but not itself read by this
reconstruction until MD-031 — states, in prose, the identical derivation rule MD-030 found only in
the (inadmissible) executable lane: `Claim,Evidence | Hypothesis,Evidence → (warrant-assessment) →
Verdict`. Chained through `04`'s own already-admitted atom assignment (`Validate`'s atom =
`warrant-assessment`), this closes the B `Validate` input-carrier field MD-029 found `NOT SPECIFIED
BY SOURCE`, using narrative-lane text only.

MD-031's own verdict: **E — PARTIAL CONVERGENCE**. Open items MD-031 explicitly left unresolved:
the executable lane's own tested `V6` alternative rule (a different input set for the same
derivation step); `Validate`'s preconditions, postconditions, and failure semantics (none stated by
`06`); and the relationship between `06` and the executable rule, classified `CONVERGENCE WITH
COMMON-CAUSE PROVENANCE`, explicitly not independent confirmation.

## What this decision is, and is not, about

**Is about**: whether `06-composition-rules.md` may be used as an evidence source in future,
separately-authorized research — the same narrow admissibility question MD-028-DQ-1 already
answered for `03-capability-model.md` and `04-operator-contracts.md`.

**Is not about**: whether `06`'s content is mathematically correct, canonical, or sufficient on its
own to resolve `Validate`'s full specification; whether `V6` should be adopted, rejected, or
resolved; whether the executable lane should also be admitted; whether Model B is correct; whether
a composition test should run. Each of those is a separate question, unaffected by this decision
either way.

## Provenance summary carried into this decision

`06` carries the identical Git-provenance status to the two already-admitted files — same
2026-09-06 bulk commit (MD-026's own original finding, re-confirmed in MD-031 `02`/`06`) — not a
weaker status. This is offered as relevant context for treating it consistently with `03`/`04`; it
is not, by itself, sufficient reason to admit it (a decision requires the human act recorded in
`02`, not an automatic consequence of matching provenance).

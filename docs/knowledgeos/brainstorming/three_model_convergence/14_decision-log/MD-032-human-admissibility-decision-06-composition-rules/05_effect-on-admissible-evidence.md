# Effect on Admissible Evidence

## What changes, mechanically

The admissible-evidence set now available for future, separately-authorized `Validate`
specification/composition research is: `03-capability-model.md` + `04-operator-contracts.md`
(MD-028-DQ-1) + `06-composition-rules.md` (this decision) — three narrative-lane files, all under
`docs/knowledgeos/research/kernel-reduction/`, all admitted for the same narrow purpose, all held at
the same `RECONSTRUCTED PROVENANCE` ceiling.

## What does NOT change

Following MD-028-DQ-1's own exact precedent: **`classification-register.tsv` is not touched.** The
newly-admitted file remains outside the `docs/knowledgeos/brainstorming/` corpus root and is not
added to it. Its admissibility is recorded only in this governance-log entry
(`14_decision-log/model-boundary-decisions.md`) and in this decision's own artifacts
(`14_decision-log/MD-032-.../`) — exactly the mechanism already used for `03`/`04`. No file was
copied, moved, or rewritten; `06-composition-rules.md` remains exactly where and as it was
(re-confirmed unmodified in `07`).

## Executable lane — explicitly unaffected

`nrna1/research/kernel-reduction/` (including `kr/carriers.py`) remains exactly as MD-030 found it:
**not admitted**, provenance-blocked (zero Git history, self-declared `[EXP]`, internally contested
via `V6`). This decision does not touch, upgrade, or otherwise affect its status in any way.

## `V6` — explicitly unaffected

Per the authorization's §8: admission of `06` does not adjudicate the competing executable rule
(`variants.py`'s `V6`). `06` remains silent on it (MD-031 `07`); this decision does not resolve,
narrow, or otherwise change that silence's status.

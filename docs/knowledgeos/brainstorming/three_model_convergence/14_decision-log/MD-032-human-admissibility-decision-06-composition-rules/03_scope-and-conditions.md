# Scope and Conditions

## What admission unlocks (per the authorization's §6, exact)

The smallest future scientific use this admission enables: **controlled specification/composition
research using the narrative `Validate` derivation rule** (i.e., `06`'s own content becomes usable
as evidence, alongside the two already-admitted files, in a future, separately-authorized study).

## What admission does NOT automatically authorize (per §6, exact — each requires its own separate
authorization if reached)

- Execution of a composition test (Pair 1 repeat, or any pair).
- Admission of `kr/carriers.py` or any other executable-lane artifact.
- Resolution of `V6`.
- Creation of a canonical operator contract.
- Model selection.
- Stage 07.
- Schema design.
- Architecture work.
- Implementation of any kind.

## Conditions attached (Option A carried no additional human-specified conditions)

The user selected Option A as presented, without adding further conditions beyond what Option A's
own text already specifies (the narrow-purpose scope stated in `02`). No condition was invented on
the user's behalf, per the authorization's own explicit instruction (§3, Option C guidance, applied
here by not fabricating conditions where none were given for Option A).

## The user's own explicit exclusions, carried forward from their authorizing message (not part of
the formal decision text, but recorded here as directly relevant context for any future study)

The user's own commentary accompanying this authorization stated two things explicitly, worth
preserving alongside the formal decision even though they are not conditions of Option A itself:
"I would not admit `kr/carriers.py` at the same time" and "I would not resolve `V6` before admitting
06." Both match what Option A's own text already excludes (`03`, non-effects list) — recorded here
as confirmation of intent, not as additional binding conditions beyond what the option itself
already states.

# Provenance Status

## The distinction the authorization required be preserved (§4)

`06-composition-rules.md` has the same repository-history provenance as the already-admitted
`03-capability-model.md` and `04-operator-contracts.md` — all three were first tracked in the
identical bulk commit `70fee73c8bcce04b18606adb007fa45ab5297787` (2026-09-06), per MD-026's original
finding and MD-031's own re-confirmation (`02`/`06` of that study). This supports treating `06`
consistently with those two sources **for the purposes of this admission decision only**.

## What this does NOT mean

**Same provenance does not mean independent confirmation.** MD-031's own classification —
`CONVERGENCE WITH COMMON-CAUSE PROVENANCE` (the agreement between `06`'s stated rule and the
executable lane's own `kr/carriers.py` rule is explained by a shared authoring process, not by two
independently-arrived-at confirmations; MD-031 `06`: five-millisecond mtime gap between `04` and
`06`, no cross-citation in either direction between `06` and the code) — **is not upgraded by this
decision**. It remains `CONVERGENCE WITH COMMON-CAUSE PROVENANCE`, unchanged, in this record and in
MD-031's own frozen text.

## Ceiling unchanged

Provenance for `06`, exactly as for `03`/`04`, remains at `RECONSTRUCTED PROVENANCE` — the same
ceiling MD-026 established for this entire narrative lane and MD-028-DQ-1 preserved for the first
two admitted files. This decision does not upgrade that ceiling for `06` or for `03`/`04`.

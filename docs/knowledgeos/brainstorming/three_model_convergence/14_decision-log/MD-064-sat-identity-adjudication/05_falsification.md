# MD-064 §05 — Q6: Falsification (Actively Searching for Distinctness), and Q5: Boundary Transfer

## Q6 — searching for evidence the two `Sat`s are different constructs

| Target | Found? |
|---|---|
| Different signatures | **Cannot compare — M0125 gives none** (`02`). Absence of a stated signature is not itself a difference, but it is not identity either. |
| Different evaluated objects | **Suggestive, not conclusive** — M0125's own table context leans toward a proposition/claim-evaluation object, not `K_t`/`r` (`04`). |
| Different domains | Same limitation — M0125 states no domain to compare. |
| Different purposes | M0043's `Sat` is explicitly purpose-relative via `EC_t`; M0125's is never connected to `EC_t` at all (`02`) — a real, if not decisive, asymmetry. |
| Explicit redefinition | **NOT FOUND** — M0125 never redefines `Sat` with a new formula. |
| Conflicting semantics | **NOT FOUND** — nothing in M0125 contradicts what `Sat(K_t,r)` does in M0043; M0125 simply does not restate it. |
| Different lifecycle/transition roles | **NOT FOUND** — M0125's own `Sat` occurrences are not framed in transition terms at all. |
| Separate, independent definitions of `Sat` | **NOT FOUND** — no `[DEF]`-tagged second definition of `Sat` exists anywhere checked. |

**No direct contradiction found.** Per the authorizing prompt's own explicit instruction, **this
absence is not treated as evidence of identity** — it is exactly as inconclusive as the positive
search in `01`–`04`.

## Q5 — does M0125's boundary diagnosis bear on F4 `Sat(K_t,r)`?

Since Q1–Q4 do **not** establish a sufficiently strong identity relationship (verdict: `C`, `06`):

$$\boxed{\textbf{STRUCTURAL ANALOGY ONLY}}$$

`Reason`/`Provenance`/`Context`/`Condition` are **not transferred** into F4's own `Sat(K_t,r)`
semantics by this phase. The relationship these constructs bear to F4 remains exactly what MD-062/063
already established for M0127 itself — a same-programme, well-evidenced, but not F4-object-level
corroboration.

# Cross-Source Conflict Register

No direct conflict was found between any two sources for the same target structure, within this
study's own search frame:

- Operator contracts: only one source found (`research/kernel-reduction/04-operator-contracts.md`),
  no competing definition elsewhere.
- `S^epi`/`Context`: the admissible-frame signature (`E,C,Q`→`A`) and the external type table are
  consistent, not conflicting — the external table elaborates, does not contradict, the admissible
  signature.
- `ConflictRecord`, `Θ`: no second source found to conflict with the single naming occurrence.

**One noted internal-to-external tension, not a conflict between two equal sources**: `research/
kernel-reduction/19-directive-adoption-and-research-restructure.md` states it *"SUPERSEDES THE
HEADLINE"* result of the same directory's own earlier files (per its own `00-INDEX.md`). This is the
external material's own internal revision discipline, not a cross-source conflict this study
adjudicates — flagged for any future phase that might bring this directory into scope, since it means
even the "operator contracts" file (`04`) sits inside a lane whose own headline claim was later
qualified.

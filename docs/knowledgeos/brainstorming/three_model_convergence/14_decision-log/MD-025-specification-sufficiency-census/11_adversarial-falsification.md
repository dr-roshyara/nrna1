# Adversarial Falsification

| Claim | Falsifier |
|---|---|
| `research/kernel-reduction/04-operator-contracts.md` genuinely elaborates C0's own 13 operators | A close reading finding the atom vocabulary describes different operators than C0's own — checked directly, all 13 named operators plus `Qualify` appear in the contract table by exact name; not falsified |
| M0030 genuinely cites this directory as its own output | A reading of lines 1443/1488 in a different sense (e.g. as an example, not an instruction) — checked directly in context; both citations are directive/output-path statements, not falsified |
| This directory was never classified by this reconstruction | A `classification-register.tsv` row this study's `grep -c` missed due to a path-formatting difference — possible, not exhaustively ruled out beyond the exact-string search performed |
| No external elaboration exists for `ConflictRecord`/`Θ` | A directory this study did not search (`docs/knowledgeos/governance/`, `backlog/`, `reviews/`, `developer_guide/`, `how_far_we_are/` were not checked) — **this is a genuine, disclosed limitation, not a settled negative**; this census covered `research/` and `architecture/` only, not all 8 sibling directories of `docs/knowledgeos/` |
| A's Context material is genuinely 4-way internally unresolved | A later A-evidence-base file (outside the 84 checked in Phase 1) that reconciles the 4 treatments — not checked here, would require reopening Phase 1's own frozen evidence base, out of scope |

## Disclosed limitation, not silently omitted

This census's own search frame for the C1/C2 targets (`ConflictRecord`, `Θ`) covered only `docs/
knowledgeos/research/` and `docs/knowledgeos/architecture/` — not the full `docs/knowledgeos/` tree
(`governance/`, `backlog/`, `reviews/`, `developer_guide/`, `how_far_we_are/` remain unchecked). The
operator-contract and `S^epi` findings (`03`/`04`) are correspondingly stronger (direct citation
chain from admissible evidence) than the `ConflictRecord`/`Θ` negative findings (`05`/`06`), which
rest on a narrower search and are reported with that narrower confidence.

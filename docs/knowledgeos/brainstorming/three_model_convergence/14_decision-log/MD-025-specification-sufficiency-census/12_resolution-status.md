# Resolution Status — the 13 Required Decision-Tree Questions

**Q1. Does Model B contain enough source-attested operator contracts to permit genuine composition
testing?** Not within the admissible frame. **Yes, if `docs/knowledgeos/research/kernel-reduction/`
is brought into scope by a separate governance decision.**

**Q2. If yes, which operators?** All 13 C0 operators plus `Qualify` — the contract table (`03`) covers
every one.

**Q3. If no (within scope), exactly which contract elements are missing?** Input/output types,
preconditions, postconditions, per-operator invariants — all absent from the 151-file admissible
population itself.

**Q4. Are those missing elements actually present elsewhere in B's admissible corpus?** No — they are
present outside the admissible corpus, in a directory an admissible file (M0030) directly cites.

**Q5. Does A genuinely define a `Context` structure usable by `S^epi`?** No — A's own Context material
is itself 4-way internally unresolved (CT-1); using any one of the 4 would be a forced, unjustified
selection.

**Q6. Does C1 genuinely define the semantics and invariants of `ConflictRecord`?** No, within any frame
this census checked (with the disclosed limitation that not all of `docs/knowledgeos/` was searched).

**Q7. Does C2 genuinely define `Θ` as a transition structure?** No, beyond naming it as a component.

**Q8. Does any source explicitly connect the supposedly complementary structures (B's operators and
A/C1/C2's aggregates)?** No direct connecting source was found for any pair.

**Q9. Which MD-024 findings survive unchanged?** The core negative results (Pairs 1, 2, 3 as
`NOT FORMALLY SPECIFIED ENOUGH TO TEST`/`PARTIALLY TESTABLE` within the admissible frame) survive
exactly as stated. The `Δ_t`/GA-038 findings (MD-023) are untouched by this study.

**Q10. Which MD-024 findings must be downgraded or narrowed?** Two, per this authorization's own
required corrections (§2): "well-definedness fails by default" → "not defined by the source / not
testable"; the `ConflictRecord` "functional correspondence" claim → "adequacy constraint identified,
correspondence not established."

**Q11. Is a genuine composition experiment now possible without inventing semantics?** **Not within
the current admissible corpus root.** **Potentially yes, without inventing anything, if
`docs/knowledgeos/research/kernel-reduction/` and `theory-v1.1-simulation/` are brought into the
admissible evidence base by a separate, explicit governance decision** — the specification already
exists; nothing would need to be invented, only the corpus-boundary decision to admit it.

**Q12. If yes, what is the smallest separately authorized composition experiment?** *(Reported as a
future proposal only, not begun here, per the authorization's own §24.)* A narrow, single-pair
retest of MD-024's own Pair 1 or Pair 2, using the now-located operator contracts, conditional on a
prior, separate corpus-boundary decision admitting `docs/knowledgeos/research/` — nothing more.

**Q13. If no, what exact missing evidence would be required?** For `ConflictRecord`/`Θ`: any source
document, inside or outside the currently-searched frame, that defines their own internal structure —
genuinely not located by this study, and not recoverable by interpretation of what exists.

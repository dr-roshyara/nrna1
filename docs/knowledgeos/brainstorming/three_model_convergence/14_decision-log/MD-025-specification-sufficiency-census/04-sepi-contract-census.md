# `S^epi` Contract Census

## Within the admissible B population

M0032/M0033/M0036 (already known from MD-023/024) state the signature `S^epi(E,C,Q)→A` and the
Assessment output `A_t=⟨attitude,strength,warrant,status⟩`. No further typing of `E`, `C`, or `Q`
individually was found within the 151-file admissible population by this census's own Pass 1 search
(`Domain:` = 0 hits; no dedicated `S^epi` type table found inside the admissible frame).

## Outside the admissible frame

`docs/knowledgeos/research/theory-v1.1-simulation/C-type-system.md` (never classified by this
reconstruction, same status as `03`'s finding) contains a formal type-dependency table:

```
EpistemicStandard | — | S^epi | Authorization, Policy, RealityState
```

and separately types `Context` (`C`) as *"Conflict axis of Σ · semantic core 𝒞"* — a real, if
terse, formal definition, not a bare name. The same document explicitly flags its own notational risk
(`E` used for both "epistemic state" and "Evidence," `[OPEN]`, unresolved even within this external
source) — disclosed here rather than smoothed over.

## Status

`E1-OUT-OF-SCOPE` for `Context`'s own typing (parallel to `03`'s finding); `E2-PARTIALLY-SPECIFIED`
within the admissible frame for `S^epi`'s own signature overall (the function shape is known, argument
types are not, within scope).

# Cross-Source and Temporal Conflict Register

## No cross-source conflict found between `research/` and admissible Model B evidence

No admissible B document contradicts anything in `kernel-reduction/`'s own content — they address the
same subject (kernel minimality/operator reduction) without stating incompatible claims, consistent
with `07`'s "contemporaneous, separate lane" reading.

## One internal-to-external temporal note

`04`'s own finding: `kernel-reduction/19-directive-adoption-and-research-restructure.md` supersedes
that same directory's own earlier headline. This is an internal revision within the external material,
not a conflict between the external material and the admissible corpus — recorded for completeness,
not adjudicated further (out of this study's own scope, since it would require reading `research/` as
if it were admissible evidence to adjudicate).

## The one genuine provenance conflict found

`reviews/kernel/session1/S1-F028` vs. its own `session2/S2-R-F028` — already fully treated in `06`.
Status: `session2` supersedes `session1` **by that lane's own explicit act**, not by this study's
inference — the clearest, most explicit self-correction found anywhere in this census.

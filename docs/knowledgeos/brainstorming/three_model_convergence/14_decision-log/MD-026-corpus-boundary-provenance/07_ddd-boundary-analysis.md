# DDD Boundary Analysis

## The directory boundary treated as a hypothesis

Four candidate relationships between `brainstorming/` and `research/kernel-reduction/`
(per the authorization's own list, §10):

1. **Same bounded context as Model B?** No source-attested evidence supports this — no admissible
   B document declares `research/` to be "the same corpus," and the human check-in commit itself
   (`03`) linguistically separates them.
2. **A downstream research context?** **Best-supported reading.** `kernel-reduction/00-INDEX.md`
   self-labels its own lane explicitly: *"research experiment — NOT canonical, NOT architecture, NOT
   governance."* This is a self-declared research lane, produced (per `04`'s dating) contemporaneously
   with M0030 but maintained as its own separately-indexed, separately-versioned artifact set (its own
   §19 supersession discipline, its own falsification/negative-results sections) — the structure of a
   distinct, self-contained research programme that happens to share a subject with Model B's own
   evidence, not a fragment of Model B's own corpus.
3. **A reconstruction context?** Partially — `18-audit-response-and-protocol-audit.md`'s own listed
   contents (*"corpus-boundary contamination finding... audit of the protocol itself"*) suggest this
   lane conducted its own internal boundary self-audits, structurally similar to this reconstruction's
   own discipline, independently.
4. **An unresolved relationship?** For `theory-v1.1-simulation/` specifically — yes, given `05`'s own
   weaker provenance finding (no direct citation from admissible evidence).

## Ubiquitous-language check

`kernel-reduction/`'s own vocabulary (`Reach(S)`, atom-based operator contracts, `Pareto` kernel
search, `SMUGGLING` detection) is more elaborate and more rigorously operationalized than anything
found in Model B's own admissible 151-file population — using overlapping but not identical
terminology (e.g. "atoms" as a formal device does not appear inside the admissible frame's own concept
register).

## Verdict

`kernel-reduction/` and `theory-v1.1-simulation/` are best characterized as **a separate,
self-governing research context that investigated the same subject matter as Model B's own admissible
evidence, contemporaneously, but was never merged into it** — Outcome C (separate research context),
not Outcome A (incorrectly omitted). This is a DDD interpretation, explicitly labeled as such, not a
source-attested fact.

# Adversarial Falsification

| Claim | Falsifier |
|---|---|
| The operator-contract file is genuine Model-B-adjacent output, not unrelated material | Content mismatch between the contract table's own 13+1 operators and C0's own named set — checked directly, exact match; not falsified |
| M0030's citation is an actual output/provenance reference, not illustrative text | Reading the surrounding prompt template as purely hypothetical/instructional with no bearing on real execution — the two citations are directive statements ("write output to..."), consistent with a real instruction, but this study does not have a document stating execution actually wrote there; **this remains the weakest link in the chain**, explicitly flagged as `[RECONSTRUCTED PROVENANCE]` throughout, not upgraded |
| The directory was genuinely outside the original evidence frame for chronological reasons, not policy | A hypothetical later document showing MD-010/MD-011's authors were aware of `research/` and chose to exclude it anyway — not found; the git-timing evidence (`03`) is the strongest available counter-evidence to any "deliberate exclusion" reading |
| The external type system belongs to the same semantic model as `S^epi` | No citation link was found (`05`) — this is itself evidence against strong identity, already reflected in the weaker confidence rating |
| No `ConflictRecord` elaboration exists in the newly searched directories | The one apparent counter-example (`S1-F028`) was found and directly disqualified by its own source (`06`) — not a case this study overlooked |
| No C2 `Θ` elaboration exists in the newly searched directories | A directory outside the 6 searched (`docs/knowledgeos/` has no further siblings beyond the 8 already enumerated in this whole reconstruction's history) — genuinely exhaustive within `docs/knowledgeos/`; **not exhaustive of the wider `/home/.../nrna1/` filesystem**, which was never in scope for any phase of this reconstruction and remains outside this study's own search frame by design |

## Self-check against the study's own prohibited inference (§16)

This study never asserts: "M0030 cites directory X → X belongs to Model B → X is admissible → composition
can proceed." Every step in `03`–`05` is kept as its own separately-confidence-rated claim. No
specification was declared canonical merely because it is detailed (`04`'s own "qualified, not
canonical" framing). No absence was declared corpus-wide merely because it was absent from the searched
frame (`06`'s own explicit scope disclosure).

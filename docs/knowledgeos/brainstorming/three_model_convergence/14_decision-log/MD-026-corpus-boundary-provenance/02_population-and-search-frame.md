# Population and Search Frame

## Population for the provenance/chronology questions

`docs/knowledgeos/research/kernel-reduction/` (22 files), `docs/knowledgeos/research/
theory-v1.1-simulation/` (13 files), `docs/knowledgeos/research/theory-v1.2-simulation/` (further
files, not individually enumerated here — out of this study's own direct target scope, noted where
relevant to `Θ`). Git history for the whole `docs/knowledgeos/` tree.

## Population for the completed ConflictRecord/Θ census

The 6 previously-unsearched sibling directories named by the authorization: `governance/`, `backlog/`,
`reviews/`, `developer_guide/`, `how_far_we_are/`, plus `architecture/` (already partially checked by
MD-025, re-confirmed here). Exhaustive `grep -rli` for `ConflictRecord` and `transition`/`Θ`-adjacent
terms across each directory's full contents — a census of these 6 directories, not a sample.

## Duplicate/derived-document policy applied throughout

Per the authorization's own statistical discipline (§11): multiple files within `docs/knowledgeos/
research/kernel-reduction/` that are internal sections of one experiment (e.g. `10`–`12`, the ablation/
pairwise/randomized results) are treated as **one research-generating event**, not independent
observations, when this study counts evidentiary weight. The `reviews/kernel/session1/` and `session2/`
material is treated as **one reviewing lane across two sessions**, not two independent sources — its
own `session2` material explicitly reviews and, in the `ConflictRecord` case, disqualifies `session1`'s
own content.

# `theory-v1.1-simulation/` Provenance

## Same check-in evidence as `03`

Tracked in the identical 2026-09-06 commit, named explicitly alongside `kernel-reduction/` in the same
commit body, distinct from the enumerated `brainstorming/` subdirectories — `[DIRECT EVIDENCE]`,
identical reasoning to `03`.

## Does an admissible source directly point to it?

Unlike M0030's own explicit citation of `kernel-reduction/`, **no admissible B-tagged file
(M0032/M0033/M0036, the `S^epi`-defining documents) was found to cite `theory-v1.1-simulation/` by
path.** This is a materially weaker provenance link than the operator-contract case — `[HYPOTHESIS]`
that the two are related, based only on subject-matter overlap (`S^epi`'s own signature appearing in
both), not a stated citation.

## Chronology

`theory-v1.1-simulation/00-INDEX.md` and its own file set do not carry the same explicit same-day
date-stamp discipline `kernel-reduction/`'s own index does (no "Experiment ID `KR-2026-09-01`"-style
self-dating found in this study's own reading). **Status: `TEMPORAL ORDER NOT ESTABLISHED`** for this
directory specifically — a materially weaker finding than `04`'s own dated result for
`kernel-reduction/`.

## Answer to Q7

**`C-type-system.md` cannot currently be established as genuine Model-B evidence** — it is simulation-
specific elaboration in a directory with no direct citation link from any admissible B-tagged source,
and no established chronology. Its relevance to `S^epi` is a content-level resemblance only, exactly
the kind of finding this study's own §16 (prohibited-inference rule) warns against over-reading.

# Operator-Contract Temporal Audit

## Content-internal dating

`docs/knowledgeos/research/kernel-reduction/00-INDEX.md` self-dates its own experiment: *"Experiment ID
`KR-2026-09-01`... 2026-09-01."* M0030's own filename encodes the same date
(`20260901-222111_prompt-kernel-reduction-and-minimality-experiment.md`). **Content-internal evidence
is consistent with same-day authorship** — the experiment M0030 requested and `kernel-reduction/`'s own
index claims to report appear to be the same research act, dated the same day, independent of git
check-in timing. `[RECONSTRUCTED PROVENANCE]`, not stronger — no single document states "this directory
is the execution of that prompt" in so many words; the inference rests on matching dates, matching
trial counts (already noted in MD-025), and the directory path M0030 itself names.

## CB-6 — what M0030's citation actually establishes

Re-read directly: M0030's own text (lines 1443, 1488) instructs the executing process to write its
output to `docs/knowledgeos/research/kernel-reduction/` — **this is an intended/instructed output
location, stated within a prompt document, not a documentary record of actual execution.** M0035 (the
"confirmed execution" record) narrates that the protocol was run (~150,000 trials) but does not itself
state, in its own words, "results were written to `research/kernel-reduction/`." **CB-6 answer: M0030's
citation establishes *intended output location*, not *documented actual output* — the connection
between the two remains `[RECONSTRUCTED PROVENANCE]`, matching dates and trial counts, not a stated
fact.**

## The internal supersession chain, examined

`research/kernel-reduction/00-INDEX.md` itself states: *"The headline result has been superseded — read
[§19] first"* — `19-directive-adoption-and-research-restructure.md` **supersedes** the directory's own
original headline (`|K_min|=13` vs. `8`), replacing a first-to-seventh-priority ordering
(`Representation → Capability → Reachability → Minimality`, kernel question now *seventh* of seven).
**The operator-contract file (`04-operator-contracts.md`) is not itself named as superseded** by `19` —
its own atom-vocabulary/contract-table content is presented as foundational apparatus the later
restructuring builds on, not as part of the retracted headline. **Status: `QUALIFIED, NOT
SUPERSEDED`** — the contracts remain the directory's own standing apparatus; the *conclusion drawn from
them* (minimality claims) is what was revised.

## Answer to Q5/Q6

`04-operator-contracts.md` is chronologically consistent with being Model-B-adjacent research output
(same date, same subject, cited output path); its own later-directory supersession affects the
minimality conclusions, not the contract definitions themselves.

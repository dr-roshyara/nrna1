# Claim and Provenance Audit

## MD-040's own smallest-next-step framing ("whether the corpus elaborates the standard-is-policy
claim elsewhere")

**Confirmed and substantially extended.** It is elaborated — but not in a way that confirms
kernel-reduction's own claim; the elaboration (seq 0583, corroborated by `M0032`) points toward
`Validation` and `Governance` being distinct, not the same. MD-040's own text is not rewritten; this
finding is recorded as a new, connected study.

## MD-040's own B verdict ("`Warrant` is partially specified")

**Unaffected — the missing computation rule is still missing**, now confirmed across a third,
independent-ish thread. If anything, `B` is reinforced: more formal *structure* (the `EC` container,
the `Validated(r)`/`Authorized(r)` split) exists than MD-040 alone found, while the *content*
(the actual threshold) remains equally absent.

## MD-036's `STRUCTURAL CORRESPONDENCE` verdict for V0/V6

**Unaffected**, per this study's own explicit scope boundary (`00`) — not revisited, not addressed.

## No frozen artifact rewritten

MD-031, MD-033 through MD-040 are not edited by this study.

# Adversarial Hypotheses

## H1 — Seq 0583 supplies a computable warrant threshold

**Fails.** `05` establishes directly: `Validated(r)` and `Γ` are named slots, not computed values;
the document's own examples never state how a satisfaction rule is evaluated.

## H2 — Seq 0583's Validation/Governance distinction resolves the three-way tension in kernel-
reduction's favor or against it

**Fails, as stated.** It does not *resolve* the tension — it *documents* a third position, making
the tension three-way rather than settling it. Kernel-reduction's own frozen text is not edited or
retracted by this finding (`10`/`11`).

## H3 — Seq 0583's `EC` framework is the direct source kernel-reduction's own "epistemic contract
`EC`" references draw on

**Plausible but not proven.** `07` establishes strong circumstantial support (chronological
precedence with genuine Git evidence; kernel-reduction's own text treats "EC" as pre-established,
never defining it fresh) but finds no direct citation in either direction. Recorded as "plausible
common ancestor," not confirmed.

## H4 — The three threads (kernel-reduction, `M0032`, seq 0583) are independent statistical
confirmations of a `Validation ≠ Governance` position

**Fails explicitly**, per this session's own corrected discipline (`07`) — this is corroboration
within a shared corpus vocabulary/lineage, not independent replication.

## H5 — Seq 0583 resolves the conflict-authority question needed to adjudicate between kernel-
reduction's and `M0032`'s differing positions

**Fails.** The document's own closing verdict (`03`, §25E.38/§25E.41) states directly that exactly
this kind of conflict-resolution ("a rigorous conflict/authority algebra for deriving the contract
when governing sources disagree") remains its own next, unexecuted research step.

## H6 — This finding changes MD-036's own V0/V6 verdict

**Fails, and is not addressed by this study at all**, per its own explicit scope boundary — nothing
in `EC`'s own formal apparatus bears on `V0`/`V6` specifically; no attempt is made to connect them.

## Summary

| Hypothesis | Result |
|---|---|
| H1 (computable threshold found) | Fails |
| H2 (tension resolved) | Fails |
| H3 (direct source of kernel-reduction's EC term) | Plausible, not proven |
| H4 (independent statistical confirmation) | Fails |
| H5 (conflict-resolution supplied) | Fails |
| H6 (changes V0/V6 verdict) | Fails / not addressed |

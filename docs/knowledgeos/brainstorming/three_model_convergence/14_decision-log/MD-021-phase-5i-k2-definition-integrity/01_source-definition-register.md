# Phase 5I — Source Definition Register (every Assertion definition, un-merged)

| # | Source | Seq | Date | Exact formulation | Conceptual fields | Technical fields | Constructor | Identifier | Role of Observation | Role of Evidence | Role of Context | Role of Time | Role of Provenance | Role of Policy | Normative/Descriptive/Executable/Hypothetical | Confidence |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| D1 | D285-1 §2 (prose) | 1006 | 2026-08-31 | `Assertion → {Proposition,Entity,Evidence,Context,Time,Provenance}` | Proposition, Entity | Evidence, Context, Time, Provenance | none (prose only) | none stated | Absent (revised elsewhere in same doc, §"the lanes agree" retraction) | Named field | Named field | Named field | Named field | Not a field (declared external elsewhere) | Descriptive, research | High (direct quote) |
| D2 | D285-6 §3 (prose) | 1007 | 2026-08-31 | `Assertion → {Proposition,Entity,Observation}+{id,c,t,Π}` | Proposition, Entity, Observation | id, c, t, Π | none (prose only) | `id` named as a field | **Named field, present** | Not named (absent from this list) | `c` (unexpanded) | `t` (unexpanded) | Not named | `Π` (unexpanded) | Descriptive, research | High (direct quote) |
| D3 | `exec/t285_reconcile.py`, `ASSERTION_CONTAINS` | — | undated (co-located with D285-1/6/7) | `{"Proposition","Entity","Evidence","Context","Time","Provenance"}` | Proposition, Entity | Evidence, Context, Time, Provenance | none — a set literal, not a data constructor | none | **Absent** — computed into the script's own "ABSENT" bucket alongside `State` (`03`) | Named field | Named field | Named field | Named field | Not a field | Executable (set literal) | High (direct code read) |
| D4 | `exec/t285_equality.py`, `UNPACK` | — | undated | `{"Proposition","Entity","Observation"}` (comment: full form `(id,P,e,c,t,Pi)`) | Proposition, Entity, Observation | id, c, t, Pi (named in comment, not in the `UNPACK` set itself) | none — a set literal | `id` named in comment only | **Named, included in `UNPACK`** | Comment glosses `e` as "(evidence)" but `e` is not in `UNPACK` | `c` (comment only) | `t` (comment only) | `Pi` (comment only, glossed as policy not provenance) | `Pi` (comment glosses as "policy") | Executable (set literal + comment) | High (direct code read) |
| D5 | `exec/e_equality.py`, `A(P,e,c,t,Pi)` | — | undated | A 5-parameter constructor, `dict(P=P,e=e,c=c,t=t,Pi=Pi,id=H(P,e,c,t,Pi))` | `P` (a nested tuple, itself carrying entity-shaped content) | `e`, `c`, `t`, `Pi` | **Yes — an actual executed constructor**, not a set literal | `id` = SHA-256 hash of the other 5 fields, **derived, not an independent input** | **Not a named parameter** — Observation does not appear; `e` is populated directly by the caller with evidence-shaped content in the worked example | `e` (by inference from content shape, not by explicit label) | `c` (a plain string, e.g. `"prod"`) | `t` (a tuple, e.g. `("2026-08-01",None)`) | `Pi` (a plain string, e.g. `"origin:scan"`, glossed in comments as provenance-like, not policy) | Executable (a real constructor, exercised with worked examples) | High (direct code read, re-executed) |
| D6 | seq 0630 §49.76 | 630 | 2026-08-28 | `Evidence = QualifiedObservation` (a defining equation, not an Assertion schema) | — | — | none | none | Domain of the implicit `Qualify` function | Codomain of `Qualify` | not mentioned | not mentioned | not mentioned | not mentioned | Descriptive/definitional, research | High (direct quote) |
| D7 | seq 0795 §170.4–170.5 | 795 | 2026-08-29 | `O=⟨source,time,value,context⟩`; `E=⟨O,provenance,integrity,classification,scope⟩` (Observation/Evidence tuples, not an Assertion schema at all) | — (this is a different object than `Assertion`) | time, context (inside O); provenance, integrity, classification, scope (inside E) | Formal tuple notation, not code | none | `O`'s own top-level structure | `E`'s own top-level structure, explicitly `E ⊃ O` | Named field of `O` | Named field of `O` | Named field of `E` | Not present (Evidence has no policy field here) | Descriptive/definitional, research, predates the D285 package by 2 days | High (direct quote) |

## Discipline note

**Seven materially distinct records, not merged during collection**, per the authorization's §4. D6
and D7 are not `Assertion`-schema definitions at all — they define `Evidence`/`Observation` directly,
and are included because they bear on the same conceptual territory (what `Qualify`/`e`/`Evidence`
mean) without being forced into the `Assertion`-shaped rows above.

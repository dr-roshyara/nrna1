# Scientific Relevance Audit — Roles Kept Separate

## A. Evidence about V6 itself (its stated definition, domain, structure)

- `12` line 144 (already admitted): the derivation rule and definition text.
- `18` §3.3: clarifies what V6 does and does not establish structurally — **this is genuinely
  category A**, not C, because it is a statement about V6's own logical role in the argument
  ("contrast case"), not a claim about whether V6 is epistemically good.
- **Nothing in the 17 files adds a new formal statement of V6 itself** beyond what `12` (already
  admitted) already gives.

## B. Evidence about the meaning of "surviving a defeater"

- **None found.** This is the category MD-037's own search was built to fill, and it remains empty
  across all 17 files (MD-037 `04`, re-confirmed here). `18`'s `I9` invariant is explicitly **not**
  placed in this category — it is a different, weaker condition ("consideration"), and treating it
  as category B evidence would be exactly the conflation the authorization warns against.

## C. Evidence about the epistemic motivation for V6

- `12`'s own causal-criticism/OLS-analogy section (already admitted).
- `18` §1 A-7 ("V6 is close to definitional") and §3.3's broader framing.
- `13`'s aphorism ("unchallenged ≠ validated").
- **These are motivation, not definition or structural fact** — kept explicitly out of categories A
  and B. None of them states what "surviving" operationally requires; they state *why* the research
  programme thought the requirement mattered.

## D. Evidence about provenance/common authorship

- `18` §0's provenance ledger — the single clearest example, and explicitly kept in its own category:
  it says nothing about V6's semantics, only about who wrote what. **Never used, in this or any prior
  study, to support a claim about V6's own correctness or meaning** — it supports only the
  independence/corroboration classification in `03`.

## E. A proposed future formalization method

- `19` §7's "Semantic Kernel Equivalence" framework — characterized in `05`, never treated as
  evidence for or against V6's own semantics. It is a *method*, not a *finding*.

## Cross-check: does any candidate file's content get miscategorized if handled carelessly?

**`18` is the one file that could plausibly slide across categories if not handled carefully** — it
contains A (§3.3), C (§1 A-7, framing), D (§0), and touches on B only by declaring it absent (F-9/
F-10 style framing, shared with `15`/`17`). This study's own admission matrix (`02`) and this
section keep these four roles explicitly separate within that one file, rather than treating "18 is
relevant" as a single undifferentiated fact.

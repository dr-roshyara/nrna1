# MD-038 — Verification and Completion

## Verification suite

1. `resume.py`/`resume_mathematical.py` → both `CONSISTENT`, unchanged.
2. MD-024 through MD-037 — `git status --porcelain` confirms no modification.
3. `docs/knowledgeos/research/kernel-reduction/` (all 21 files) — confirmed unmodified.
4. `classification-register.tsv` — 2377 lines, unchanged.
5. No code inspected or executed. No composition test. No model selected. No canonicalization. No
   Stage 07. No K-1/K-2 change.
6. Backlog collision resolved a third time (`EKS-14` → `EKS-15`); `EKS-07` corroboration note
   updated to reflect the recurrence count.
7. Only the new `14_decision-log/MD-038-defeater-evidence-admission/` directory (9 files) written,
   plus the backlog housekeeping.

## WHAT MD-037 ESTABLISHED

No formal definition of "surviving a defeater" exists anywhere in the 17 characterized files. The
gap is confirmed as one the original kernel-reduction research programme itself knowingly left open
(`17` Q-6/Q-7, `15` F-9/F-10), not an oversight of this reconstruction's own reading. `18`'s own
audit-response document independently reaches the same separation MD-036 constructed (V6 as
"contrast case," not evidence) — corroboration within a common provenance lineage, not independent
replication (corrected per this study's own `03`). `18`'s own provenance ledger source-confirms
MD-031's common-cause-provenance finding. A named but explicitly weaker invariant, `I9` ("Defeater
consideration"), was found and correctly distinguished from "surviving a defeater."

## WHAT REMAINS UNRESOLVED

What predicate or transition distinguishes a merely-considered defeater from a survived one. Whether
the series' own proposed Semantic Kernel Equivalence framework (`19` §7) could formalize this — the
framework itself is under-specified (its own authors say so) and would still require the missing
definition as an input, not supply it. MD-036's own `STRUCTURAL CORRESPONDENCE` verdict for V0/V6.

## HUMAN DECISION REQUIRED — RESOLVED

**Decision recorded (Option C)**: `15-falsification.md`, `17-open-questions.md`, `18-audit-response-
and-protocol-audit.md`, and `19-directive-adoption-and-research-restructure.md` are admitted, narrow
scope, per `08`. Thirteen other characterized files remain outside the admissible population. No
formal definition was admitted because none exists to admit; the admission enables future,
separately-authorized work with the framework and the open-question record, nothing more.

## HARD STOP

No downstream scientific work is authorized by this task. AWAITING SEPARATE AUTHORIZATION for: any
formalization attempt, any V0/V6 selection, any composition test, or any executable-lane admission.

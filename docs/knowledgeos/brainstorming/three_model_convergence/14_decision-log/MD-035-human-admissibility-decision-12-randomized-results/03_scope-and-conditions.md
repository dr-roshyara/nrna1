# Scope and Conditions

## What admission unlocks

The smallest future scientific use this admission enables: **controlled research into `Validate`
specification, the V1/V4/V5/V6 variants, the stated baseline limitation, and precondition/
postcondition/failure semantics** — i.e., `12`'s own content becomes usable as evidence, alongside
the three already-admitted files, in a future, separately-authorized study.

## What admission does NOT automatically authorize (each requires its own separate authorization
if reached)

- Adjudication of whether `V6` is a genuine semantic alternative or a representational variant.
- Selection of `V0` or `V6` as the operative rule.
- A composition test (Pair 1 repeat, or any pair).
- Admission of `kr/carriers.py` or any other executable-lane artifact.
- Creation of a canonical operator contract.
- Model selection.
- Stage 07.
- Schema design.
- Architecture work.
- Implementation of any kind.

## Conditions attached (Option A carried no additional human-specified conditions)

The user selected Option A as presented, without adding further conditions beyond what Option A's
own text already specifies. No condition was invented on the user's behalf.

## The user's own explicit framing, carried forward as context (not a formal condition)

The user's own commentary stated: *"do not adopt V6 yet... admission gives us the evidence needed to
investigate V6; it does not answer whether V6 is mathematically or semantically superior."* This
matches what Option A's own text already excludes — recorded here as confirmation of intent, not as
an additional binding condition beyond what the option itself already states.

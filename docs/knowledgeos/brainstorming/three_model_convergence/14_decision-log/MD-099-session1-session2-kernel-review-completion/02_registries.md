# MD-099 §02 — Self-Correction Ledger, Negative-History Register, Duplicate/Provenance Findings,
Cross-Lane Transfer Register

## Self-Correction Ledger (native to the reviewed programme itself)

1. **Session 1 self-audited its own coverage** (`S1-F024`'s second-pass depth audit, 2026-08-26):
   found one material missed finding (a second, incompatible Zero-lens definition) among five
   under-treated documents — recorded as a genuine, disclosed self-correction, not a silent fix.
2. **Session 1's own coverage report retracted its own "160/160 processed" framing**: disclosed that
   "processed" meant substantial-read (~20 docs) + thesis-extraction (~135 docs, 2–6 quoted lines each)
   + depth re-read (5 docs) against a 189,132-line corpus — "text actually read: on the order of 3–5%."
   Session 1's own words: "That overstatement is corrected here rather than left standing."
3. **`S1-F036`'s full re-read (2026-08-26) overturned its own first-pass thesis extraction**: the first
   pass captured "roughly 15%" of the document; the full read recovered ten material findings, three
   among the strongest in the whole 112-file record. Session 1 itself states this "invalidates" the
   0.6%-miss-rate figure its own earlier audit had reported for K3/K4-class documents.
4. **Session 2 corrected its own confidence-scoring practice mid-review** (`X-004`, `X-005`): having
   recorded "strengthens the hypothesis" in five earlier per-artifact reviews, Session 2 recognized its
   own standing hypothesis (a preservation-and-reconstruction reading of KnowledgeOS) might itself
   descend from the very corpus material used to test it — reclassified all subsequent hypothesis-tests
   as CONSISTENT/INCONSISTENT only, explicitly declining to treat the five earlier entries as
   retroactively wrong, per that programme's own "synchronize the mutable part, never rewrite delivered
   analysis" discipline.
5. **Session 2 corrected an "arrival count" it had itself endorsed** (`S2-R-F017`): Session 1's
   consolidation document asserted both "this document's agreement with earlier findings is dependent,
   not independent" and, two paragraphs later, "it is also the fourth independent statement of
   KCON-012" — Session 2 flagged this as the document refuting Session 1's own prior six-arrival count
   from inside the corpus.

## Negative-History Register (claims explicitly tested and found not to hold, within the reviewed
programme)

| Claim | Disposition |
|---|---|
| "Six independent arrivals" at atomicity-not-relatedness (`S1-F016`) | reduced to ~2–3: one origin, one adversarial confirmation; the rest are a refused-conclusion critique, a consolidation, and two downstream acceptances of the falsification — not independent arrivals |
| "Fourteen independent framings" converge 13–1 (`S1-F019`) | one reasoner, fourteen lenses it selected, applied to a hypothesis stated in the prompt — "one opinion, fourteen times," not a vote |
| "Four independent sources" at preserve-not-perform (`S1-F039`) | reduced to ~2 — two KR&R textbooks share their field's founding architecture |
| "Seven formalisms, one verdict — strongest convergence in the corpus" (`S1-F032`) | one disposition (take-the-distinction-refuse-the-ontology) applied seven times to a pre-loaded question shape |
| "Extent vs contents — the most clarifying single distinction extracted from the corpus" (`S1-F028`) | **disqualified entirely**: not a corpus arrival at all, but Session 2's own adjudication-track output laundered back through a saved file (`X-006`) |
| Ten "unresolved contradictions" (`S1-COVERAGE-REPORT.md`) | seven dissolve under the thirteen-mechanism taxonomy; the list itself omits the one genuine same-question same-altitude conflict found (ziran vs the constitutional identity invariant) |
| "Evidence: Entity or Value Object — the central unresolved question that determines the Kernel's shape" (`S1-F018`) | reported (not independently verified by this reconstruction) as a false dichotomy — a third route via `EvidenceLinks`-as-Collection |

## Duplicate/Provenance Findings

- **The `X-006` laundering loop** (§00_index §3): the single most consequential provenance finding in
  this 112-file cluster — a full round trip from adjudication-track output → saved research-corpus file
  → Session-1 extraction → near-consumption as Session-2 corroboration, caught before consumption.
- Multiple within-cluster duplicate files already known to this reconstruction from MD-093–097's own
  chronological pass are independently re-flagged by Session 1/Session 2 (e.g. the `192351`/`192948`
  measure-theory-resolution byte-identical pair) — consistent with, not contradicting, this
  reconstruction's own duplicate register.
- Session 1's own coverage report discloses 14 byte-identical duplicates, 1 raw-source-material file
  (a Medium article pasted verbatim), 2 scrollback/capture-origin files (one of which is the `X-006`
  laundered file), and 1 misfiled architecture-baseline document within its own 160-document population.

## Cross-Lane Transfer Register

**Inbound — a further, richer cross-validation event** (extending MD-098's own inbound finding): this
112-file Session-1/Session-2 cluster is a third independently-conducted layer of analysis over
substantially the same primary corpus this reconstruction has already read directly and chronologically
(`kernel/`, MD-093–097). Where Session 1's own extracted Kernel-formulation content overlaps this
reconstruction's own Kernel Identity Ledger, it is recorded as corroborating context under the standing
wording correction ("methodologically cross-validated under a separate reconstruction procedure, with
common corpus provenance"), never as new primary evidence and never merged into this reconstruction's
own ledger entries.

**Outbound / forward echoes, flagged not resolved**:
- The `EvidenceLinks`-resolves-Entity/VO-dilemma finding and the "`ConflictRecord`/Verification-Port are
  pre-existing v1.1 objects" finding (MD-098) both point toward the same firewalled v1.1/Constitution
  document as their common, still-unread source — reinforcing MD-098's own named priority target for a
  future, separately-authorized K-1/K2 adjudication phase.
- The K-1 structure candidate's own content (`KnowledgeAggregate`+`ConflictRecord`, Verification Port as
  sole inbound gate) reappears, unresolved, inside the `X-006` laundered material — a fourth sighting of
  the same candidate structure, still held at `IDENTITY UNRESOLVED`.
- `Standing(p)` (Session 1's own unelaborated, distinct usage) vs this reconstruction's own tested
  `Standing(p)=(S⁺,S⁻,R,P,Ctx,Cond)` (MD-080) — a fifth-plus homonym instance in the recurring
  bare-symbol-collision pattern this reconstruction has tracked since MD-078/MD-089/MD-092 — flagged,
  not merged, not investigated further this phase.

**Firewall discipline maintained**: `theory-extraction/` and `verification/zero-algebra/` were never
accessed throughout this 112-file read.

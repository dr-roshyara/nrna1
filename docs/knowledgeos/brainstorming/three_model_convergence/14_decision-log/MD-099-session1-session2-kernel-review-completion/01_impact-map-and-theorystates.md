# MD-099 §01 — Document→Object Impact Map, TheoryState Updates, New Objects, Competing Formulations,
Resolved/Unresolved Relationships

## Document→Object Impact Map (representative, not exhaustive — 112 files)

| Document cluster | Objects touched |
|---|---|
| `S1-F001`–`F005` | `Authority` (temporal binding), `Rule`/`Knowledge` vocabulary instability, Kernel member-list divergence (lists A/B/C), determinism, mathematics-as-regime separation rule |
| `S1-F006`–`F013` | semantic entropy, god-object (six conflations), nine-capability map, equilibrium criterion, admission-vs-lifecycle, transformation boundary + 16 exclusions, epistemic accountability core, eightfold UNRESOLVED verdict |
| `S1-F014`–`F020` | states-as-projections + six contradictions, twelve-question sequence + content mutability, aggregate falsification (atomicity), phase consolidation, Evidence Entity-or-VO dilemma, fourteen-lens vote, `ADR-KOS-KERNEL-001` |
| `S1-F021`–`F027` | four-layer lens stratification + boring Kernel, externally-cited lens commission (352 citations), Tarka nine-family instrument + absence typology, book-extraction register, anti-reasoner constraint, phase closure ("books prove nothing"), inference-licensing constraints |
| `S1-F028`–`F040` | governance-artifact-in-corpus anomaly (later revealed as `X-006`'s laundering loop), abstention + evidence-role constraints, evidence acquisition as domain, ziran identity challenge, formal regimes as regime-not-ontology, EIR + cost argument, sufficient state, counterfactual robustness, minimum preservation unit (ten recovered findings), modelling prohibitions, substrate/measure + 13:47 test, preserve-not-perform convergence, measure-theory cascade (falsified in-corpus) |
| `S2-F001`–`F024` (thematic + per-artifact reviews of `S1-F001`–`F009`) | provenance-inflation mechanisms, type errors, altitude conflations, the `F007`↔`F008` "too large/too small" dissolution |
| `S2-R-F010`–`F040` | thirteen dissolution mechanisms applied across the whole Session-1 record; `EvidenceLinks`-as-third-route finding; six `IMPLEMENTATION QUESTION` cluster; `X-001`–`X-006` cross-track register |
| `S2-00`/`S2-01`/`S2-R-COVERAGE`/`S2-FINAL-KERNEL-REVIEW` | full synthesis: implementation tally, architectural-effect classification (A–E), final recommendation (DEFER) |

## New TheoryState entries opened this phase (second-order review apparatus; not merged with this
reconstruction's own primary Kernel Identity Ledger)

1. **The thirteen-dissolution-mechanism taxonomy** (Session 2's own instrument): modality · orthogonality
   · question-scope · disjoint sets · claim-vs-question · different referent (object vs slot) ·
   different predicate · target internally inconsistent (holds both positions) · purpose-count vs
   capability-count · different scope level (bounded context vs Kernel extent) · vocabulary collision ·
   different questions · set mismatch. Applied to all ten of Session 1's own listed "unresolved
   contradictions" — **seven dissolve**, one (projection-divergence) survives and is doubly contested,
   one (justification-type: invariant vs economics) survives and is flagged as verdict-flipping, one
   (four incompatible lifecycle-relation classifications) partly dissolves with a genuine residue
   surviving (`S2-R-F015.2`: generalization-vs-counterexample, "is withdrawal an epistemic state or an
   assertion-status axis" — the single strongest surviving tension of this kind).
2. **`X-001`–`X-006`, the cross-track observation register** (Session 2's own instrument, distinct from
   corpus findings): `X-001` (research-hygiene "preserve distinctions" vs adjudication "sufficiency
   test" are principles of different tracks, never evidence for each other); `X-002` (scope exclusions
   porous to secondary citation); `X-003` (a research finding proposing a merge of three adjudication
   items — `W:C-7`/`C-14`/`C-15` — reshapes adjudication topology without ruling on content); `X-004`
   (Session 2's own standing hypothesis, tested against every artifact, may itself descend from
   corpus material it is supposedly testing — recorded as a live, unresolved reflexivity question, not
   a proven defect); `X-005` (the corpus's own "Zero lens" carries at least three incompatible senses —
   subtractive-structural, statistical-residual, and Session 2's own absence-typology sense — and was
   used undefined for ~17 hours before either corpus definition existed); `X-006` (the self-referential
   laundering loop, see `00_index.md` §3).
3. **Method observations `M-1`–`M-8`** (about the Session-1 extraction method itself, not about
   KnowledgeOS): arrival-counting without independence tests (M-1, later self-remediated by Session 1);
   no cross-artifact consistency check (M-2, later remediated); caution applied to conclusions but not
   measurements (M-3); evidence-class inflation — description promoted to evidence (M-4); reasoning-base
   unrecorded — provenance captures *who*, not *what law the author had* (M-5); counts reported as
   results (M-6); project decisions superseded without recorded reversal (M-7); "the corpus attacked its
   mathematics and defended its architecture" — six refutations of the measure-theory proposal in 78
   minutes vs zero adversarial passes over eight Kernel formulations in four days (M-8).
4. **`EvidenceLinks`-as-third-route finding** (Session 2's own reported reading of v1.1, never
   independently verified by this reconstruction against the primary architecture document, which
   remains firewalled from direct K-1/K2 adjudication per the standing freeze): Session 2 reports that
   v1.1's `EvidenceLinks` member (a Collection of references + acquisition method + reliability
   conditions, with content held external under an annotation Session 2 calls `⟨C-3⟩`) resolves the
   corpus's own self-declared "central unresolved aggregate boundary question" (`S1-F018`'s
   Entity-or-Value-Object dilemma for Evidence) as a **false dichotomy** — a third route neither horn
   contemplates. **Recorded as reported content of a document consulted by the reviewed programme, not
   as this reconstruction's own verified fact about v1.1** — this reconstruction has not independently
   opened the v1.1/Constitution text to confirm this reading, per the standing K-1/K2 firewall.

## New objects discovered (native to the 112-file cluster, distinct from this reconstruction's own
tracked families)

- `Standing(p)` glossed differently within Session 1's own material (not this reconstruction's own
  MD-080 `Standing(p)=(S⁺,S⁻,R,P,Ctx,Cond)` — a distinct homonym, native to a different lane entirely;
  no merge).
- The nine (not eight) Kernel formulations: the original eight (`S1-F009`–`F019`'s admission/
  consistency/decision/transformation/accountability/coherence/traceable-relationship boundaries) plus
  a ninth from `S1-F036` — a ten-item "invariant surface" (stable identities, immutable historical
  records, typed relationships, explicit context, temporal distinctions, provenance chains, epistemic
  and lifecycle histories, contradiction and plurality, cross-context references, reconstruction
  capability) — the only formulation framed as guarantees rather than capabilities or boundaries.
- The six-way temporal role split (`S1-F036`, Full-read additions §A): what-was-said / what-was-observed
  / what-was-valid / what-became-effective / what-was-evaluated / what-remains-disputed — argued to bear
  directly on this same lane's own `S1-F001` temporal-gap measurement (0/20 authority grants carry
  validity information).
- The 24-row decidable Kernel-membership removal test (`S1-F036` §C) — the only place in the 112-file
  cluster where a membership criterion is actually executed against a concrete list, rather than merely
  proposed.

## New competing formulations recorded, not merged

- Nine Kernel-boundary formulations (above), explicitly not adjudicated, not compared against this
  reconstruction's own Kernel Identity Ledger (33+ entries, MD-093–097) beyond the cross-validation
  statement in `00_index.md`.
- Three-to-four incompatible senses of "Zero lens" (subtractive-structural / statistical-residual /
  absence-typology), a fourth vocabulary-collision instance in a family this reconstruction has
  independently tracked since MD-095 (Invariant Zero / Boundary Zero split) — **explicitly a different,
  fourth homonym, no merge attempted.**
- Seven-plus unreconciled Knowledge genera (understanding, relationship, product-with-members, justified
  state evolution, capacity, ratified domain commitment, inter-level phenomenon) — closely parallel to,
  but not verified identical with, this reconstruction's own Knowledge-State tuple-proliferation family
  (MD-095/096); recorded as a further, richer instance of the same general proliferation pattern, not
  merged.

## Newly resolved relationships (within the 112-file cluster's own internal logic; not adopted as this
reconstruction's own findings)

- `F009`↔`F010` ("admission is the core act" vs "admission is the lifecycle's first transition") —
  dissolves as different question ("during admission" vs "independently of the Kernel").
- `F016`↔`F019` (aggregate falsification vs re-assertion) — clarified as vocabulary collision on
  "aggregate" (preservation unit ≠ consistency boundary ≠ projection view), not a reversal.
- `F015` mutability table vs `F013` Model A immutability — dissolves as different referent (value vs
  slot).
- Three identity positions (primitive-and-indefinable / self-so-not-assigned / administrative) —
  dissolve as three different questions (what kind of thing / how it comes to be / whether required).

## Relationships that remain UNRESOLVED (explicitly, per this cluster's own final synthesis)

- ziran's "identity is self-so" vs the constitutional invariant "assigned, never derived" — same
  subject, same question, genuinely incompatible, constitutional-altitude, **not adjudicated by this
  reconstruction and not routed**.
- Projection-divergence: "must never contain contradictory propositions" (`S1-F038`) vs "may differ in
  commitment while answerable to correctness" (`S1-F039`, Brandom) — survives, doubly contested.
- Justification-type tension: invariant-protection framing vs economic/cost framing (`S1-F033`) — the
  cluster's own flagged verdict-flipping tension, carrying no measurements, unresolved.
- The K-1 structure candidate itself — untouched by this phase, held exactly at `IDENTITY UNRESOLVED`
  per MD-094–098.

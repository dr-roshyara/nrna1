# MD-084 §03 — Three-Dimensional Classification, Responsibility Evolution Ledger, Terminal Verdict

## Three-dimensional classification, every candidate, never collapsed

| Candidate | A. Object identity | B. Semantic responsibility | C. Computational completeness |
|---|---|---|---|
| `Sat(K,EC_t)`/`Sat(K,r)` (birth, `[DEF-19]`/`[DEF-21]`) | anchor | anchor — the responsibility itself | `DEFINED` (named, used), `NOT COMPUTABLE` (no body from birth) |
| `Adequate(K_t,EC_t)` (`[DEF-20]`) | `SAME OBJECT` (defined `⟺ Sat`) | `SAME` | `DEFINED` (as an alias), `NOT COMPUTABLE` (inherits `Sat`'s own openness); its own six-item enumeration is `PARTIALLY DEFINED` at best — named, not specified |
| `Warrant` | `UNRESOLVED` (no internal structure to compare) | `RELATED` (defeater-survival is plausibly part of the same broader justification question, never demonstrated identical) | `NOT COMPUTABLE` — MD-037: no formal definition, invariant, or derivation rule found anywhere |
| `Assessment` | `UNRELATED_HOMONYM` in most instances (external-literature-extraction, borrowed per-paper vocabulary) | `RELATED`, possibly `SAME_LINEAGE_AS` `Warrant` via a shared Titelbaum root (§02, unverified to certainty) | not evaluated as a KnowledgeOS-native object — `NOT PRESENT CORPUS-WIDE` as a native formal object |
| `Admissible=Pre∧Inv∧Assurance∧Auth` | `RELATED OBJECT` (different bounded context — action-admissibility) | `RELATED` (a neighboring, not identical, responsibility) | `DEFINED`, `NOT COMPUTABLE` — `Assurance` "REFUTED as definable" (`NG-1`, reused) |
| qualification rule | `RELATED OBJECT` | `SAME` (closest verification-lane match to the actual "what counts" question) | `DEFINED` ("well-formed"), `NOT COMPUTABLE` — `EG-2`: "no decision procedure" |
| `Policy_Det` | `RELATED OBJECT` to `EC.Rules` (explicitly, textually connected) | `SAME, ILLUSTRATED BUT NOT DISCHARGED` | `DEFINED` (case structure), `NOT COMPUTABLE` (own conditions never defined; confirmed unique to its own section, MD-082) |
| `Standing(p)` | `RELATED OBJECT` to `Eval`'s own output (MD-080) | `RELATED` — a per-*proposition* evaluator, not a per-*requirement* acceptance criterion; performs a neighboring, not identical, job | `DEFINED`, `COMPUTABLE`, `EMPIRICALLY VALIDATED` (12/14 scenarios) — but for a different responsibility than `EC.Rules`'s own |
| `Suff`/`Accept`/`𝔸` (external proposal, MD-083) | `RELATED OBJECT, CONSTRUCTED CANDIDATE` | `SAME, PROPOSED, NOT ADOPTED` | `DEFINED` (architecture only); `Suff` itself explicitly left `NOT COMPUTABLE` by the proposal's own admission |

## Responsibility Evolution Ledger — extension

| Time | Responsibility | Carrier | Status (3-way) | Predecessor | Successor |
|---|---|---|---|---|---|
| 2026-08-27, `step-023` | acceptance, named at birth | `Sat(K,r_i)` | `defined`, not `computable` | — | `[DEF-19/20/21]` |
| 2026-08-30, `gap-discovery/08-EVIDENCE-GAP.md` | the "qualification rule" | `EG-2` | `defined`, not `computable` (adversarially confirmed) | `step-023`'s own `Sat` | none demonstrated |
| 2026-09-01/02 | acceptance, aliased | `Adequate(K_t,EC_t)` | `defined` (as alias), not `computable` | `Sat` (`SAME OBJECT`) | `[DEF-21]`'s own `Δ_t` |
| 2026-09-02 22:40, `gap-update-2026-09-02/05-NEW-GAPS.md` | action-admissibility (neighboring) | `Admissible`/`NG-1` | `defined`, not `computable` (adversarially confirmed) | common-lineage with `EG-2`, not independent | none demonstrated |
| 2026-09-06, T21 Part VI §6.43 | acceptance, illustrated | `Policy_Det` | `defined`, not `computable` | `EC.Rules`'s own slot (explicitly named) | none — dies in the same section (MD-082) |
| 2026-09-10, external session (MD-083) | acceptance, proposed architecture | `Accept`/`Suff`/`𝔸` | `defined` (architecture), `Suff` explicitly left open | `Policy_Det`'s own vocabulary (partially reused) | `PROPOSED CANDIDATE, NOT ADOPTED` |

## TERMINAL verdict

**TERMINAL D — the responsibility cannot be reconstructed beyond what is already known, and the corpus
provides positive evidence that the remaining openness is intentional/design-level.**

This is not chosen merely because repeated searches failed (the mission's own explicit prohibition) —
it rests on two direct, first-person textual disclosures, not absence alone:

- `Policy_Det` (T21 Part VI §6.43): *"The exact policy belongs to the epistemic contract. This
  prevents KnowledgeOS from encoding one universal philosophy of evidence."*
- `Threshold` (T21 Part VI §6.44): *"A threshold without semantics is not a mathematical epistemic
  rule... It becomes meaningful only through a declared calibration or decision framework."*

Both are the theory's own explicit statements of *why* the acceptance criterion is left unspecified —
a deliberate architectural stance (keep the acceptance policy external and contract-pluggable, so the
theory does not encode "one universal philosophy of evidence"), not silence. This is corroborated,
though less directly, by `Adequate`'s own six-item enumeration ("sufficiency, completeness... evidence
requirements...") which *names* the dimensions without *closing* any of them — consistent with a
deliberately left-open parameter space, not an oversight.

**What keeps this from being `TERMINAL C` instead**: `TERMINAL C` ("the responsibility is clearly
present, but its specification is genuinely incomplete") would fit an evidence picture of *pure
absence* — searches that found nothing. This investigation instead found *explicit, first-person design
commitments to leaving the parameter open* — a qualitatively different and stronger kind of evidence,
which is exactly what the mission's own §12 requires to choose `D` over `C`.

**What is not claimed**: this does not mean construction is now authorized, or that `D` forecloses a
future, separately-authorized construction phase — `TERMINAL D` is a historical-reconstruction finding
about the corpus's own stance, not a governance decision about what to do next (`EKS-48` remains that
separate, still-open decision).

## Backlog assessment

No new ticket. Every finding this phase either sharpens an already-tracked result (`EKS-48`'s own
evidentiary basis) or corrects this reconstruction's own prior overclaim (the "five rediscoveries"
count) — recorded forward, not requiring a governance-facing ticket of its own.

## Verification

- No construction, mapping invention, canonicalization, or adoption performed anywhere in this phase.
- `Adequate` not treated as closing `Sat`'s own gap — recorded precisely as an alias contributing no
  new content.
- The "five rediscoveries" correction recorded forward; MD-082's own text not edited.
- No frozen artifact (MD-024–083) modified.
- `resume.py`/`resume_mathematical.py`: run below, both must report `CONSISTENT`.
- `theory-extraction/` never accessed.

## MD-084 status: EXECUTED. HARD STOP.

`SAT-OPERATIONAL-CLOSURE-v1` remains unauthorized. No F3↔F4 bridging, no implementation, no governance
decision made. Next action, named, not authorized: `EKS-48`'s own three-way decision, now resting on a
historical-reconstruction finding (`TERMINAL D`, with explicit textual grounds) rather than an
absence-based inference — the strongest evidentiary basis this reconstruction has produced for treating
the acceptance/sufficiency parameter as a deliberate design choice rather than an unfinished derivation.

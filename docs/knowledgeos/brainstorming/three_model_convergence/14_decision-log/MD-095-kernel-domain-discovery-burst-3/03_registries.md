# MD-095 §03 — Self-Correction Ledger, Negative-History Register, Cross-Lane Transfer Register,
Corpus-Hygiene Findings

## Self-Correction Ledger (representative, chronological)

1. **File 3 (Batch A, Ziran) → unseen prior document**: explicit retraction, antecedent text not in
   this segment — *"the first mapping was useful, but it made one mistake: it treated the
   consciousness traditions as if they described one vertical stack..."* (mirrored again independently
   in Batch F, File 3 — same retraction pattern, different subject matter, same author-voice).
2. **File 7 (Batch A) → the "God KnowledgeAggregate"**: prior claim (one oversized aggregate) →
   correction (split into `KnowledgeClaim`/`Evidence`/`Determination`/`Authority`) → reason (DDD
   atomicity test: "which of those must change atomically... shared evidence, independent
   determinations, authority grants and historical records do not automatically belong to one
   transaction boundary") → surviving claim (the four-way split). See §02 for its adjudication
   against MD-094's own K-1 structure.
3. **Batch C, File 4 (Bishop PRML)**: explicit supersession — "Evidence-Gated Hybrid Temporal
   Inference (EG-HTI)," treated elsewhere as an established prior artifact but undefined within this
   segment, is demoted from "ultimate architecture" to "one component" and replaced by a named
   superset, "Evidence-Gated Adaptive Inference Fabric (EGAIF)."
4. **Batch E, File 3 ("inverse approach")**: `K=X\N` proposed, then retracted within the same
   paragraph — *"but not literally as a simple set complement"* — because "not knowledge" is
   heterogeneous (a 14-item list, "these should not be collapsed into one category").
5. **Batch F, File 3 → unseen prior mapping (consciousness model)**: same retraction pattern as #1,
   applied to a chakra/tattva-to-layer correspondence; the demoted material is then partially
   elaborated (not fully avoided) in the very next file (Batch F, File 4) — an unresolved tension
   recorded, not repaired, by this extraction.
6. **Batch G, File 6 (Catuṣkoṭi "tightening")**: three explicitly staged self-corrections of "Knowledge
   is..." (v1 "too narrow" → v2 "better" → v3 adopted), the clearest single-file staged-correction
   sequence found this phase.
7. **Batch F, File 5 → File 6 (Zero split)**: `Zero(K)` defined once, then immediately split into two
   named sub-senses with the source's own text explicitly declining to merge them back — recorded in
   §02 as a genuine, source-native non-identification event (not a correction of an error, a
   deliberate preserved distinction).

## Negative-History Register

| Claim | Disposition | Where |
|---|---|---|
| "Bayesian Kernel" / "Semantic Kernel" as architectural components | Explicitly rejected, listed among "What NOT to Build" (10 items) | Batch A, Files 5/6/7 |
| Pan-Bayesianism ("everything in KnowledgeOS should be Bayesian") | Explicitly rejected | Batch A, File 1 |
| Attractor theory attributed to Åström's source book | Explicitly rejected as an over-attribution, filed as "research hypothesis" not "source fact" | Batch D, File 2 |
| "Dhātu extraction" as the actual content of the *Swar Gyan* source | Explicitly rejected, stated at both the opening and closing of the same document | Batch D, File 5 |
| Kalman filtering / dynamic programming as Åström's own content | Explicitly declined to import — the source volume does not contain a large modern treatment of either | Batch C, File 7 |
| Named ML algorithms (SVM, neural nets, random forests, PCA, etc.) as KnowledgeOS domain concepts | Explicitly rejected, twice, independently within the same session (recurrence, not independent replication) | Batch C, File 2; Batch C, File 5 |
| "Worst-case is always the right criterion" / "Iteration guarantees truth" (carried forward from prior phases' own tracked pattern; not re-encountered verbatim this phase, no new instance found) | N/A this phase | — |
| Rule of Three (biased under variation/measurement error) | Explicitly rejected, quoted from Stigler | Batch A, File 2 |
| Purusha as a literal importable KnowledgeOS entity | Explicitly rejected — "We should not literally introduce Purusha into KnowledgeOS" | Batch F, File 3 |

## Cross-Lane Transfer Register

**Inbound**: none — `kernel/` remains the earliest-surveyed material in the corpus; no reference to
`three_model_convergence/`, `phase_measure_theory/`, or `verification/` (beyond the standing firewall
around `verification/zero-algebra/`) was found in any of the 52 files.

**Outbound / forward echoes, flagged not resolved**:

- The `KnowledgeAggregate` recurrence (Batch A, Files 5/6/7) against MD-094's File-44 K-1 structure —
  adjudicated `IDENTITY UNRESOLVED` this phase (§02); named as the single most consequential
  cross-lane question this phase re-examined, without upgrading it.
- The Knowledge-State tuple-proliferation family (§02) — a standing pattern any later phase reading
  further into `kernel/`, `phase_measure_theory/`, or the math lane's own `K_t` family should be
  alert to; this reconstruction's own MD-078–088 (`Det_r`/`EvalReq`/`EC`/`r`/`Γ`/`Sat` reconciliation
  work) already documented an analogous, later-stage proliferation pattern for a structurally
  different family — the two are not asserted identical, but the *shape* of the pathology (repeated,
  uncoordinated re-invention of a central tuple, never reconciled) recurs across this corpus at
  multiple points in its own history.
- Batch B's Shieber "EPISTEMIC KERNEL" boundary document (K-B, entry #25) is the most rigorous
  Kernel-boundary artifact this reconstruction has found anywhere in `kernel/` to date (39+20+52=111
  files read) — flagged as a strong candidate reference point for any future Kernel-family
  adjudication phase, without pre-judging that phase's own scope.

**Firewall discipline maintained**: `theory-extraction/` and `verification/zero-algebra/` were never
accessed by any of the 7 extraction agents or by this adjudication.

## Corpus-hygiene findings

- **Confirmed byte-identical (direct `diff -q`, this phase)**: master files 5/6 (Batch A); master
  files 33/34, "Fraser HMM" (Batch E); master files 35/36, "knowledge as measurable construct" (Batch
  E).
- **Presumed duplicate by filename convention, NOT independently diff-verified this phase** (the two
  halves fell in different batches with no shared file access): master files 8/9 ("seven works
  integrative summary"); master files 37/38 ("knowledge as a continuously changing body of facts").
  Recorded as an open, low-priority verification item, not resolved.
- **`EKI-08` labeling collision** (Batch G, Files 3/4) — recorded in §02, not repaired.
- **File corruption, reported verbatim, not silently repaired**: Batch C's File 1 contains an
  unexplained topically-unrelated tail (lines ~1218–1338, discussing an unrelated "V-3 grant"/
  `KOS-CONTRACT-NEUTRALITY-001` governance decision, disconnected from the file's own Kernel/
  AI-efficiency subject matter) — either a concatenation or corruption artifact, excluded from the
  file's own object table, flagged here rather than resolved. Batch G's File 3 (cosmology article)
  ends with a single unexplained stray line of German ("Ich bevorzuge diese Antwort") with no
  surrounding context. Batch F's File 7 (raw Vedic-source reproduction) contains one garbled
  Sanskrit/Latin mixed-script fragment (`"...तमा॒हुः प॑र॒मे वYaमन् ॥3॥"`), reproduced exactly as found.
- **Batch C's File 6 ("session-handover-package")** is confirmed, by its own explicit header fields,
  **not** an authoritative governance handover — `Status: Research / extraction only`,
  `Decision authority: None assigned by this document`. Recorded as a corrected expectation (the
  filename alone overstates the document's governance weight), consistent with the master mission's
  own explicit caution against assuming "handover" implies an authoritative transition.

# MD-082 §01 — `Policy_Det`: Birth and Evolution

## Birth classification

`Policy_Det` — **FORMAL OBJECT BIRTH**, and, on direct grep across the full math lane and the
`kernel`/`verification` lanes, **death in the same breath**: the symbol `Policy_{Det}` occurs only at
§6.43 (lines 1539–1562) of `theory-part-06-evidence-evaluation-determination-calculus.md`. No earlier
occurrence, no later occurrence, no reappearance in any of the remaining 20 T21 parts, no reappearance
anywhere in the post-T22 corpus this reconstruction has read (MD-077's own exhaustive post-T22 scan).
This is a genuine `TERM BIRTH = CONCEPT BIRTH = FORMAL OBJECT BIRTH = DEATH`, all in one section.

## The exact case conditions, searched directly

`"sufficient independent support"` and `"sufficient challenge"` — the exact phrases `Policy_Det`'s own
case structure uses — were searched across the full math lane, `kernel/`, and `verification/`: **zero
occurrences anywhere outside `Policy_Det`'s own defining section.** These specific conditions were never
operationalized, referenced, or even repeated verbatim anywhere else in the corpus. A broader search for
"sufficient support"/"sufficient evidence" (without the exact `Policy_Det` phrasing) returns ~55 files,
overwhelmingly from the 2026-09-01/02 general research burst — not independently opened this phase
(budget-bounded; per the mission's own §11 vocabulary, this is `NOT YET LOCATED` at the individual-file
level, not `GENUINELY ABSENT`, since the broad phrase is common enough that a formal object could in
principle be hiding among the hits; the *exact*, distinctive `Policy_Det`-specific phrasing search is
what was exhausted, and it is exhausted).

## Two further, closely-adjacent status-set variants — a third instance of the corpus's own recurring proliferation pattern, now at fine grain

Within the same ~70-line span (§6.41–6.44), three different but visibly related status vocabularies
appear, none cross-referenced to either of the others:

| Location | Status set | Note |
|---|---|---|
| §6.42, lines 1515–1533 (unnamed "determination rule") | `{Established(p), Rejected(p), Conflicted(p), Undetermined(p)}` | "A determination rule then decides whether the contract permits..." — no name given to the rule itself |
| §6.43, `Policy_Det` | `{Established, Rejected, Conflicted, Unknown}` | "Undetermined" from §6.42 becomes "Unknown" here, one section later, same file |
| Part V §5.6 (already known, MD-078), `𝕊_sat` | `{Satisfied, Partial, Unsatisfied, Unknown, Conflicted}` | a fifth value ("Partial") absent from both §6.42 and §6.43's own sets |

**Object identity**: `RELATED OBJECT` across all three — visibly the same general idea (a small,
enumerated outcome space for evidence-resolution), never reconciled. **Responsibility relation**:
`SAME RESPONSIBILITY, REFINED (undisclosed) THREE TIMES IN ONE SECTION SPAN`, precisely the same
proliferation pattern this reconstruction has found at every other grain of this theory (`r`, `Γ`,
`Zero`, `Det` — MD-078/079) — now confirmed to recur even *within* a single ~70-line, single-topic
passage, not only across widely-separated Parts.

## Also found in this span, not previously isolated: a second, incompatible `Eval` signature

Line 1500–1513, immediately preceding §6.42's own status set:

```
Evaluation may produce:
Eval(p) = ⟨E⁺(p), E⁻(p), Dependency, Quality, Uncertainty, Conflict⟩.
```

This is a **6-field, single-argument** `Eval(p)`, distinct in both arity and field content from §6.15's
own `Eval:K×E×P×EC×Γ→𝒱` (5-argument, with the worked 7-field example `⟨Support,CounterSupport,
Uncertainty,Conflict,Dependencies,Assumptions,Justification⟩`) — a **third** internal inconsistency in
`Eval`'s own signature, within the *same Part*, in addition to the already-known Part VI/Part 17
homonym (MD-078 §01). Recorded here as part of the same proliferation pattern, not pursued further as
an independent thread (already sufficiently characterized by MD-078's own `E — object identity
unresolved` classification for `Eval`).

## Conclusion for `Policy_Det`'s own evolution

**Not carried forward, not completed, not abandoned in the sense of a retraction — simply never
returned to.** It is best classified `RECONSTRUCTION ONLY` in one narrow, important sense: it is not
that the corpus fails to state the responsibility exists (it explicitly does: "the exact policy belongs
to the epistemic contract") — it is that the corpus states the responsibility's *location* while never
supplying its *content*, and no later or earlier passage anywhere in the corpus returns to fill that
content in. This is the `SEMANTICALLY PRESENT BUT COMPUTATIONALLY OPEN` case the mission's own §11
vocabulary specifically names, and it is the correct classification here — sharper and more accurate
than MD-081's own "genuine gap" language, which did not distinguish the two.

# Phase 5G — K-1 Mathematical Reconstruction (independent re-verification)

$$K_1 = (S_1, \mathcal{O}_1, I_1, C_1)$$

| Component | Value | Evidence |
|---|---|---|
| $S_1$ (carrier) | $\{E,S,T,O,P,R,\Pi,A\}$ = Entity, State, (temporal/)Event, Observation, Proposition, Relation, Policy, Action | seq 0630 §49.29/49.30, re-confirmed verbatim this phase |
| $\mathcal{O}_1$ (operations) | **NOT EVIDENCED** — explicitly, not merely by omission: D285-7 states outright *"`𝒪` never enumerated against the ratified 8 primitives"* | Direct evidence of absence, seq 1008 |
| $I_1$ (invariants) | Named only: `I-1…I-12`, "ratified." Content **NOT EVIDENCED** (not located in any document read this phase or Phase 5F) | seq 1006 |
| $C_1$ (constraints) | **NOT EVIDENCED** | — |

## Derived structures (seq 0630 §49.31 — re-verified verbatim this phase, unchanged from Phase 5F)

$$Evidence \subseteq O \times Context \qquad Claim \subseteq P \qquad Prov \subseteq R \qquad
Cause \subseteq R \qquad Identity: E \rightarrow ID \qquad L: K_t \rightarrow K_{t+1} \qquad
D: (K,\Pi) \rightarrow A$$

## Adversarial check: does K-1 itself carry an internal ambiguity?

**One found, not previously flagged in Phase 5F**: seq 0630's own boxed result at §49.30 writes the
tuple as $\mathcal{K}=(E,S,T,O,P,R,\Pi,A)$ — using **$T$ for "temporal/event structure"**, not $S$ for
State-as-a-separate-slot and a distinct symbol for Event. D285-1/D285-7's own prose list
(`Entity, State, Event, Observation, Proposition, Relation, Policy, Action`) names **8 distinct English
words**, but seq 0630's own **symbolic** tuple collapses "State" and "Event" into a single slot
labeled $T$ ("temporal/event structure") while introducing $S$ for "states" separately — re-reading
§49.30 exactly: *"S = states; T = temporal/event structure."* **This means seq 0630's own symbolic
8-tuple already has $S$ (states) as its own slot AND $T$ (temporal/event) as a further, separate slot**
— so the symbolic tuple is in fact consistent with the 8-word list after all (E,S,T,O,P,R,Π,A ↔
Entity,State,Event/temporal,Observation,Proposition,Relation,Policy,Action) — **re-checked and
confirmed consistent, not contradictory**, correcting an initial adversarial suspicion raised during
this audit's own re-reading. Disclosed here as a check performed and resolved, not silently skipped.

## Verdict

**K-1's mathematical reconstruction is confirmed, unchanged from Phase 5F's own `01`**, with one
additional adversarial check performed (the $S$/$T$ symbol-vs-word mapping) and resolved as
consistent, not contradictory.

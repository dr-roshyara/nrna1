# Phase 5G — Open Questions and Required Evidence

## Which Phase-5F claims survived

- Observational equivalence FALSE.
- Observation = layering gap (H1, qualified).
- Entity/Proposition/Relation correspondences (all three, unchanged).
- K-1 through K-7 dispositions (all seven, unchanged).

## Which were weakened

- "K-1/K-2 semantic equality TRUE" → weakened to "partial correspondence over a declared, unverified
  subset."
- "π_K definable, not computable" (as a blanket property) → weakened/precised to "non-computable
  specifically for the Observation component; State's own target under-specified."
- DDD "Customer/Supplier, ACL-attempted" → weakened to "a mathematical projection is evidenced; a DDD
  architectural context mapping is not independently established."

## Which were strengthened

- "K-1 ↔ K-1-B: FORMAL EQUIVALENCE" → strengthened to "DEMONSTRATED IDENTITY (qualified)" — the one
  case where the adversarial audit's own sharper logic (per the user's own mathematical point) moved a
  verdict *up*, not down, disclosed with equal rigor as every downgrade.

## Which were falsified

- None outright falsified. The closest is the DDD "Customer/Supplier" framing, which is downgraded to
  "not independently established" rather than shown to be actively wrong.

## Which remain unresolved

- `State`'s status in the verification lane (genuinely open, no evidence either way).
- The D285-1/D285-6 `Assertion`-unpacking tension (disclosed, not reconciled — a frozen-source finding).
- `claim-registry`/`concern-map.md` (still not located as standalone files).
- Whether K-1's own $S/T$ symbol usage in seq 0630 (re-checked and found consistent this phase) would
  survive a fully independent, from-scratch re-derivation — this phase only checked internal
  consistency, not external correctness.

## Evidence required for the next phase, precisely

1. **For K-1↔K-2 to move past partial correspondence**: an implemented `Qualify` function; independent
   verification (not mere declaration) that `{Event,Policy,Action}` are genuinely irrelevant to $K_2$'s
   own stated purpose; a reconciliation of the D285-1/D285-6 `Assertion`-unpacking tension.
2. **For a genuine DDD context-mapping finding**: an explicit governance document assigning
   upstream/downstream roles, or a specified/implemented anti-corruption-layer artifact.
3. **For `State`'s status to move past UNRESOLVED**: any corpus document proposing or rejecting a
   State-recovery construction.
4. **For K-1↔K-1-B's "qualified" flag to be removed**: an explicit corpus (or reconstruction-external)
   statement identifying the two citations as literally the same referent, beyond the textual-reuse
   inference this audit relied on.

## This phase's own epistemic discipline, restated

Per the authorization's own final rule: *"A rigorous downgrade is a successful scientific result."*
This phase downgraded two claims, strengthened one, and confirmed the remainder — treating all three
outcomes as equally legitimate results of the same disciplined process, not as a scorecard to be
balanced.

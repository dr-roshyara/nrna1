# Phase 5G — Phase 5F Claim Register (verbatim, under audit)

Each claim is quoted or precisely paraphrased from Phase 5F's own artifacts, with its source file
named, so the audit below (`02`–`12`) can test each independently.

1. **"K-1 (seq 1006) and Phase-5C's own K-1-B (seq 1008) are the same object... promoted from
   Phase 5E's 'structural correspondence' to FORMAL EQUIVALENCE."** — Phase 5F `09`.
2. **"K-1 and K-2 stand in a precise, corpus-native lossy semantic projection relationship...
   semantic equality holds after unpacking Assertion... structural and observational equality do
   not."** — Phase 5F `00`, `05`.
3. **"Observational equality: FALSE."** — Phase 5F `05` (reused from D285-6 §5).
4. **"`Observation`'s absence from K-2 traces to a documented recovery construction (the Sañjaya
   layer)... a genuine, traceable layering gap, not an omission."** — Phase 5F `04`.
5. **"`State` has no analogous recovery construction — genuinely UNRESOLVED."** — Phase 5F `04`.
6. **"Entity/Proposition/Relation each resolve to partial or structural correspondence, independently
   — none to formal equivalence, none to bare lexical coincidence."** — Phase 5F `03`.
7. **"K-1 (ratified) is upstream; K-2 (verification lane) is downstream... a DDD Customer/Supplier
   relationship... The `π_K` projection functions as a partial anti-corruption layer."** — Phase 5F
   `06`.
8. **The K-1 through K-7 dispositions**: K-1 LIVE OBJECT; K-2/K-3/K-6/K-7 RESEARCH HYPOTHESIS; K-4
   HISTORICAL CANDIDATE; K-5 REJECTED FORMULATION. — Phase 5F `08`.
9. **The implicit assumption that D285-1/D285-6/D285-7 form "one authored package"** based on shared
   header/template and same-day dating — Phase 5F `01`, `07`, `10` (never stated as a numbered claim
   in Phase 5F, but load-bearing throughout its provenance work; audited explicitly here since Phase
   5G's own authorization singles out provenance-type discipline, §15).

**Every one of these 9 claims is tested independently in `02`–`12` below; none is assumed correct
because Phase 5F asserted it.**

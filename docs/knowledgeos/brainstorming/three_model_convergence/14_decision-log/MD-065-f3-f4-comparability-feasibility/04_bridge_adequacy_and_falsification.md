# MD-065 §04 — Q4: Bridge Adequacy (N/A), and Q5: Falsification

## Q4 — bridge adequacy

**Not applicable.** No candidate bridge exists to test for adequacy (`03`). Per the authorizing
prompt's own conditional framing ("if a candidate bridge exists, test whether..."), this section is
correctly empty rather than filled with an invented test.

## Q5 — falsification: searching for evidence the correspondence CANNOT hold

Since no bridge was found (`03`), the six listed falsification patterns are tested at the level of
**type compatibility** — the weakest, most defensible thing that can be said without inventing a
mapping first:

| Pattern | Status | Basis |
|---|---|---|
| One F3 atom ↔ multiple F4 requirements | **UNRESOLVED** — no mapping exists to test against |
| Multiple F3 atoms ↔ one F4 requirement | **UNRESOLVED**, same reason |
| An F3 observation exists without an F4 requirement | **COMPATIBLE, trivially** — F3's own source never mentions requirements at all, so every F3 observation is, vacuously, "without" an F4 requirement — not evidence of incompatibility, only of the base apparatuses' independence |
| An F4 requirement exists without an F3 observation | **COMPATIBLE, trivially**, same reasoning in reverse |
| Same F3 behaviour → different F4 satisfaction outcomes | **UNRESOLVED** — untestable without a mapping |
| Same F4 satisfaction outcome ← different F3 behaviours | **UNRESOLVED**, same reason |

**No mismatch is repaired, and none is escalated to `INCOMPATIBLE`** — the honest classification for
every genuinely testable row is `COMPATIBLE` only in the trivial, vacuous sense that two unconnected
apparatuses cannot conflict; the remaining rows are `UNRESOLVED` for lack of a mapping to test.

## Why this does not reach Gate D (BRIDGE REFUTED)

Nothing in either source base **contradicts** a possible future bridge — the finding throughout this
phase is **absence of a bridge**, never evidence that one is impossible. This is the precise
distinction separating Gate C from Gate D, and it is the correct classification here.

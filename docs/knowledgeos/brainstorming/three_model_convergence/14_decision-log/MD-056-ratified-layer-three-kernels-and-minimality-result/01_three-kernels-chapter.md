# MD-056 §01 — `III.9 · Three Kernels` (Ratified Book Chapter)

## Provenance

`docs/knowledgeos/reviews/synthesis/` was first tracked in git commit `10bda5d7a` (2026-09-06
08:01:30) — a **separate, deliberate commit** with a specific, descriptive message ("implementation-
readiness analysis, its falsification, and the O_core gap record"), **not** the generic bulk-import
commit (`70fee73c8`, 38 seconds later) used for `brainstorming/`. This is a stronger provenance
signal than the brainstorming material's own tier — a specifically-authored commit, not an
undifferentiated pile. File mtime for the chapter cluster: 2026-08-28 18:23, genuine, pre-dating the
commit by more than a week.

**Ratification tier, self-declared throughout**: `canonical-architecture-v0.2.md` is `AUTHORIZED`
(GN-19); `FA-1…FA-9` are `RATIFIED` (GN-31); the book itself is built under a governed production
process (`BA-1…BA-7`, `GN-34`/`GN-42`). This is categorically different material from anything
admitted so far in this reconstruction (K0, the VERIFY SESSION thread) — those are unratified
research; this is the corpus's own **accepted, governed output**.

## The chapter, verbatim structure

Four files (`chapter.md`, `evidence-map.md`, `claims.md`, `unresolved.md`), read in full. The
chapter states the architecture **deliberately layers three senses of "kernel" rather than crowning
one** (citing ruling `D-FA-4`, `GN-31`):

1. **The constitutional kernel** `[FA←E]` — eleven articles selected by explicit human ruling from
   twenty-nine registered rows, each failure-tested. Governs; does not compute.
2. **The formal kernel candidate** `[FA]` — the v0.2 object model, *"the tradition the corpus called
   M₄₉: Knower, contract, state, gap, ladder, decision contract, governance, loop."* Computes; is a
   **candidate**, not established — *"object-level realization was never established, and the
   ratified architecture says so rather than assuming it."*
3. **The historical formulations** `[H]` — eight competing kernel formulations from the discovery
   corpus's kernel era, *"none ever selected within that corpus."*

**What remains open** `[U — OQ-2]`: *"Kernel membership at the object level: which formal objects
are irreducible — the kernel of the kernel — was never decided by any act."*

## Confirmed link to this reconstruction's own frozen record

Direct grep against `14_decision-log/model-boundary-decisions.md` confirms: this reconstruction's
own **Phase 5N** (K-1 ratification adjudication, executed 2026-09-08, independently, via primary-
source archaeology over `seq 0764`'s D-FA-6/D-FA-4 rulings) states, verbatim:

> *"the one ruling that addresses it (D-FA-4) explicitly classifies the 8-primitive kernel candidate
> ('M₄₉') as an 'L2 candidate' and explicitly states: 'Membership at the object level remains open
> (OQ-2).'"*

**This is the identical `OQ-2`, the identical `D-FA-4` citation, and the identical `M₄₉` label.**
Phase 5N reached this independently, from the primary ruling text (`seq 0764`) inside the
three-model-convergence corpus's own sequential pass. This ratified book chapter is a **second,
independent confirmation that the same open question is stated, in the same terms, in the corpus's
own accepted output** — not a new discovery of the underlying fact (Phase 5N already established it
authoritatively), but strong corroboration that the finding is not an artifact of this
reconstruction's own reading of a single ruling document — the governance layer's own book says the
same thing.

## What this adds, precisely

- **F1 (frozen governance K-1, 8-primitive tuple) is confirmed, again, to be "the formal kernel
  candidate"/M₄₉ named in §2 of this chapter** — the same object Phase 5N adjudicated as N2/"L2
  candidate"/OQ-2-open. No new fact about F1's own status; a second, independent-source confirmation
  of the same fact.
- **The "eight competing kernel formulations" `[H]`** are named as historical, coverage-caveated
  material — likely overlapping with, but not confirmed identical to, this reconstruction's own F7
  (the 7–11-way Knowledge-definition family) or the nine-kernel census the VERIFY SESSION thread
  independently built (MD-054 §01, `KERNEL-RECONCILIATION-230-236.md`). **Not cross-checked this
  phase** — named as an open question, not chased.
- **The chapter's own disposition is exactly the disposition this reconstruction has already
  reached**: layering (not selecting) three senses, with the object-level question named and left
  open, closeable only by *"the two acts"* the architecture names (not read this phase — the
  `unresolved.md` file does not name them explicitly; `FA-6`'s own open-questions register would need
  to be read for that, not admitted this phase).

## Evidence-level tags, per the chapter's own vocabulary (reused, not invented)

`[FA]` final-architecture-sourced · `[E]` executed/evidenced · `[H]` historical · `[U]` unresolved.
These are the book's own native grades (`BA-3`), not this reconstruction's own tag set — recorded as
such, not translated.

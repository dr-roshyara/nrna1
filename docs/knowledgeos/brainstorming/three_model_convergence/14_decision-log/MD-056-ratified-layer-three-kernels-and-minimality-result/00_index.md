# MD-056 — The Ratified Layer: "Three Kernels" and the Operation-Registry Commission

## Trigger

Continuing the standing forward-read methodology, applied this turn to the directories not yet
covered by MD-052/054 (`brainstorming/kernel/`, `brainstorming/synthesis/`,
`mathematical_ideas_that_can_be_implemented/`, `reviews/kernel/`, other `reviews/` subfolders, the
nrna1-top-level `verification/` and `research/`). A keyword/filename discovery sweep across these
surfaced two extraordinary finds inside `docs/knowledgeos/reviews/synthesis/` — the **ratified**
canonical-architecture layer (Stratum 2, per the VERIFY SESSION's own V0 map, MD-054 — a different
kind of material from the raw brainstorming corpus swept so far):

1. `book/part-3-architecture/03-09-three-kernels/` — a **ratified book chapter** stating the
   architecture deliberately layers three senses of "kernel," and naming an open item, **`OQ-2`**,
   confirmed by direct grep to be the exact same `OQ-2` already in this reconstruction's own frozen
   decision log (Phase 5N, K-1 ratification adjudication — verbatim matching language).
2. `commission-operation-registry/` — a real, dated (2026-08-31), governance-authorized commission
   (HPA ruling **GN-79**, commission **GN-80**, independent falsification **GN-83/86**, decision
   procedure **GN-85**) that executes a rigorous minimality test over the **operation** registry (a
   sibling question to the state/object kernel), reaches a first verdict, is then independently
   falsified and corrected (not merely repeated), and terminates in a formal **§17 HARD STOP**.

## What this phase does

1. Characterizes the "Three Kernels" chapter and its confirmed link to this reconstruction's own
   Phase 5N / OQ-2 finding.
2. Characterizes the full commission-operation-registry chain — derivation, contracts, minimality
   result, independent falsification, decision package, ten-dimension verdict, and the step-284/285
   continuation to its own terminal document — following the forward-read methodology through the
   whole cluster (16 files read in mtime order, from `MINIMALITY-RESULT.md` at 14:23 through
   `step-285/06-STEP-285-VERDICT.md` at 16:05, plus the four `03-09-three-kernels/` files at
   08-28 18:23).
3. Relates both finds to the existing F1–F8 inventory, K0 (MD-052), the VERIFY SESSION `K=(𝒜,ℛ)`
   thread (MD-054/055), and GA-001/GA-038.
4. Presents an admissibility decision for this specific material.

## Scope disclosed

`reviews/synthesis/` contains 285 files; this phase reads 20 of them (the two clusters named above),
chosen because they were the two highest-value discovery-signal hits in a directory-wide keyword
sweep, and because both threads are now confirmed to terminate at explicit hard-stop documents
reached by the forward-read methodology. **The remaining ~265 files in `reviews/synthesis/`, all of
`brainstorming/kernel/` (172 files), `reviews/kernel/` (106, partially spot-checked in MD-046/047 and
in the earlier turn that found K0), `brainstorming/synthesis/` (4), the math lane
(`mathematical_ideas_that_can_be_implemented/`, 402, already substantially covered by the Model B
reconstruction and the MinKer chain), the nrna1-top-level `verification/` (50, only `zero-algebra/`
touched) and `research/` (4, only `kernel-reduction/` touched) remain unswept beyond the initial
filename/keyword discovery pass.** This is disclosed as an explicit, honest scope limit, not a
silent omission — the methodology's own instruction is to follow a discovery signal to its
conclusion before moving to the next, not to read every file in every directory unconditionally.

## Artifacts

- `00_index.md` — this file.
- `01_three-kernels-chapter.md`
- `02_operation-registry-commission.md`
- `03_relation-to-inventory-and-admissibility.md`
- `04_verification-and-completion.md`

## Scope boundary

No composition test, no kernel selection, no modification of MD-024–055, no reclassification of any
register row, no merge into F1–F8/K0 without an earned label, no resolution of GA-001/GA-038, no
independent code execution (this material's own executed scripts are cited, not re-run).

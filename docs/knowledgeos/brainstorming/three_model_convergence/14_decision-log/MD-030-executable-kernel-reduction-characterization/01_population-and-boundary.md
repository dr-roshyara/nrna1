# Population and Boundary

## Correction to the prior turn's own count

The prior (pre-MD-030) reconnaissance reported "39 files" for this directory. A full `find`
census, run for this artifact, shows: **27 genuine files + 12 `__pycache__/*.pyc` (compiled Python
bytecode, auto-generated, not source)** = 39 raw filesystem entries. The population this study
characterizes is **27 files**; the 12 `.pyc` files are noted and excluded (build artifacts of the
10 `.py` modules under `kr/`, not independent evidence).

## Complete population (27 files)

### Top level (2)
| Path | Role |
|---|---|
| `README.md` | directory-level documentation, purpose statement, run instructions |
| `run_all.py` | orchestration script — imports the `kr/` modules, runs 7 experiment groups, writes `results/*.json` |

### `kr/` — the implementation package (11 files)
| Path | Lines | Role (from docstring + read) |
|---|---:|---|
| `__init__.py` | 7 | package marker |
| `atoms.py` | 49 | the 14-atom semantic vocabulary (13 "productive" atoms + `Qualify`'s atom) |
| `operators.py` | 49 | the 13 `C0` operator contracts (atom-set only, no I/O typing) + `Qualify` |
| `carriers.py` | 87 | carrier (epistemic-artifact) kinds + the `DERIVATION_RULES` table — the concrete, computed input→output rules |
| `reach.py` | 41 | `Reach(S)`: least-fixpoint composition engine consuming `carriers.derive_kinds` |
| `capabilities.py` | 72 | the C1–C25 capability requirements (kinds/atoms needed), `evaluate()` scoring |
| `scenarios.py` | 58 | 16 deterministic scenarios (not found to mention Validate/Verdict by name) |
| `properties.py` | 97 | P1–P10 randomized property checks + vacuity audit (not found to mention Validate/Verdict by name) |
| `variants.py` | 100 | 7 alternative models V0–V7 for robustness, incl. V6 (an alternative Verdict-derivation rule) and V7 (raw C0, no `Qualify`) |
| `ablate.py` | 90 | LOO / pairwise / triples / atom-level / smuggling-probe ablation logic |
| `audit_variants.py` | 55 | an extended-search audit over the variant space |
| `infotheory.py` | 54 | data-processing-inequality / confounding experiments |

### `results/` — pre-computed output (12 files)
`ablation_atoms.json` (2,842 B) · `ablation_loo.json` (8,932 B) · `ablation_pairwise.json`
(29,142 B) · `audit_extended_search.json` (6,220 B) · `baseline.json` (12,517 B) ·
`information_causal.json` (620 B) · `minimal_kernels.json` (766 B) · `paired_analysis.json`
(716 B) · `randomized.json` (25,699 B) · `smuggling.json` (3,862 B) · `smuggling_probes.json`
(883 B) · `variant_summary.json` (3,508 B). Total directory size: 276 KB.

The README itself flags `variant_summary.json` and `smuggling_probes.json` as "scratch outputs
from interactive runs... subsets of `variants.json` and `smuggling.json`... not regenerated by
`run_all.py`" — i.e. the README's own text distinguishes a "regenerate-by-running-`run_all.py`"
sub-population (10 files) from a "manual/interactive, not reproduced by the pinned script"
sub-population (2 files). Recorded here, not further investigated (no execution was performed).

## No sampling performed

Every one of the 27 files was either fully read (all 10 `kr/*.py` modules, `run_all.py`, `README.md`,
`minimal_kernels.json`, `paired_analysis.json`) or grep-confirmed in full across its complete text
(the remaining 10 `results/*.json` files, searched for `Validate`/`Verdict`/`warrant` and structurally
sampled via their JSON keys) and cross-checked against the module read for consistency. No file in
the 27 was skipped.

## Boundary confirmation

`nrna1/research/knowledgeos-sim/` (a sibling directory under `nrna1/research/`) and
`nrna1/verification/` (including `zero-algebra/`) were **not opened, not read, not grepped** in
this study — confirmed by the tool-call record for this session. This matches the user's explicit
scope instruction.

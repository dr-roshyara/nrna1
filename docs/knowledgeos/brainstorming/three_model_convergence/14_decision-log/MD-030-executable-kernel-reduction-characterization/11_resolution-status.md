# Resolution Status

## Completion-gate outcome

**D — ADMISSIBILITY/PROVENANCE BLOCK.**

*"The executable lane may be scientifically relevant, but provenance/admissibility is insufficient
for use."*

## Why D, and not the other four

- **Not A (irrelevant)** — Attack 1 (`09`) falsified the "merely implementation detail" claim.
  `kr/carriers.py` contains a concrete, on-point candidate rule for the exact question MD-029 left
  open, and reveals a further connection (`Verdict`'s load-bearing role for `C16`/`C17`) not
  previously surfaced anywhere in MD-024–029.
- **Not B (relevant but not necessary)** — this undersells the finding. The rule is not merely
  corroborative context; it is the most concrete candidate answer found anywhere, in any lane, for
  the specific gap MD-029 identified. Calling it "unnecessary" would misstate `08`'s own analysis.
- **Not C (minimum additional evidence identified, ready for a future admission proposal)** — this
  would require the candidate to be at least *provisionally admissible*, pending only a human
  decision on scope. It is not: three independent, source-grounded qualifications (`08`) — zero git
  history, explicit self-declared non-canonical status, and internal contestation via `variants.py`'s
  own `V6` — mean that even a narrowly-scoped admission proposal would be proposing something with a
  materially different (weaker) evidentiary character than what MD-028-DQ-1 already, cautiously,
  admitted for the narrative lane. This study does not prepare a ready-to-approve admission package
  for that reason.
- **Not E (conflict requiring governance decision)** — no contradiction was found anywhere between
  the executable lane and the two already-admitted narrative files (`06`'s crosswalk: every checked
  relationship is CORROBORATED or EXPLICITLY LINKED, never CONTRADICTED).

## What would move this from D toward C

A future, separately authorized step that either (a) establishes stronger provenance for this
specific directory (e.g., if it is later committed to git with an authorial/governance record,
or if an independent, corpus-internal citation to it is found — none was found by this study), or
(b) reads the still-unadmitted `06-composition-rules.md` and finds it states the same rule
independently (which would let a future decision weigh two converging sources instead of one weak
one) — either of these would change the qualifications in `08`, not this study's own conclusion.

## Governance question this raises, stated but not decided

Should a research-governance decision of this programme's kind (a SESSION-LEVEL HUMAN
RESEARCH-GOVERNANCE DECISION, per MD-028's own precedent) ever admit an artifact with **zero**
version-control history at all — as opposed to the narrative lane's own already-cautious case,
which at least had a dated commit? This study surfaces the question; it does not answer it.

# MD-080 §03 — `TheoryState(t)` Timeline Extension and Co-Evolution Matrix

MD-069's own `T0`–`T23` series and MD-078's own `TP-1`–`TP-4` series are not renumbered or edited. This
phase adds one new turning point, `TP-5`, chronologically located between `TP-1` (2026-08-27) and `TP-3`
(2026-09-06).

## `TP-5` — `Standing(p)`, a complete, tested per-proposition evaluator, independently developed and
## independently abandoned

- **Chronology**: 2026-09-02 (`M0127`), four days after `TP-1`, four days before `TP-3`.
- **Reading order**: encountered this phase via a lead already flagged, but never fully chased, in this
  same reconstruction's own much earlier work (MD-063: "M0125's own `Sat`... M0127... evaluates
  `Standing(p)`, not `Sat(K_t,r)`").
- **Trigger**: `M0127`'s own stated purpose — completing the "confirmed priority-1 Contr/Zero research
  line," specifically refuting a "fourth-value magic" hypothesis about contradiction handling by
  demonstrating that a *structured*, six-component representation outperforms simpler (Boolean/K3/FDE)
  collapses on preserved scenario count.
- **Newly introduced object**: `Standing(p)=(S⁺(p),S⁻(p),R(p),P(p),Ctx(p),Cond(p))`, with a typed
  `R(p)∈{Direct,Source,Rule,Observation,Interpretation}` and an accompanying `Contr(p)`,
  `ZeroLens(K_t)`, `Adequate(R,Q)`, `Custody(I)`, and a "deliberately strong" six-component semantic-
  equivalence relation `≡_sem`.
- **Semantic effect**: a genuinely operational, empirically-validated (12/14 scenarios) answer to
  essentially the same question `Eval`'s own output vector would later gesture at.
- **Mathematical effect**: a real, tested equivalence relation over structured evaluations — richer
  formal content than anything `Eval`/`EvalReq` ever received.
- **Governance status**: none — same brainstorming-lane, self-rated `critical`/`high confidence` but
  never ratified.
- **Semantic lineage to `TP-3` (T21)**: **`NOT_CARRIED_FORWARD`, not `SAME_LINEAGE_AS`** — confirmed by
  direct grep, zero occurrences of `Standing(` anywhere in the 21-part rewrite. This is the phase's own
  central, disclosed finding: not absence of a chain, but a demonstrated *break* in one — the clearest,
  most concrete instance in this entire investigation of a "responsibility discharged, then
  independently rediscovered-and-not-reused" pattern, rather than either a completion or a genuine gap.
- **Evidence basis**: `DIRECTLY EVIDENCED` (primary source read, this phase, plus reuse of this
  reconstruction's own earlier MD-061–063 empirical-result characterization).
- **Falsification status**: tested against the full 21-part rewrite (zero citations found) and against
  every later post-T21 file this reconstruction has read across MD-077/078/079/080 (no reappearance
  anywhere) — genuinely, not merely presumptively, abandoned.

## Co-evolution matrix (this family, extended)

| | `EC`/contract | requirement | evaluator (per-instance) | aggregator (per-proposition) | separation (Decision) |
|---|---|---|---|---|---|
| **2026-08-27** (`TP-1`) | `EC` 7-field, born | `Sat(K,r_i)`, gloss only | — | — | — |
| **2026-08-27→09-01** (`TP-2`) | `EC` drifts 2→4→9→8 fields (verification lane's own prior finding) | — | — | — | — |
| **2026-09-01/02** (`T5`) | `EC_t` 4-field | `Sat(K_t,r)∈{0,1}`, `Req(EC_t)`, `Δ_t`, `Zero` all co-born | — | — | — |
| **2026-09-02** (`TP-5`, new) | (not directly engaged) | — | **`Standing(p)`, complete, tested** | — | — |
| **2026-09-06, Parts I–III** (`TP-3`) | `EC` 6-field (T21-native) | `r` abstract (Def 2.18); `𝒮_sat`/`𝕊_sat` codomains | `Eval`/`EvalReq` introduced, no body, **does not reuse `Standing(p)`** | `Det(K,p,EC,Γ)`, proven (Thm 3.1) | `Decision(d,K,Q,P,A,Γ)`, proven separation (Thm 3.2) |
| **2026-09-06, Part VI** | `EC.Rules` never structured | | `Det_r`, signature only, "may be defined," never supplied | | |
| **2026-09-06, T22** | | `Sat(K,r_i)=Satisfied`, fiat — reverts to `TP-1`'s own original pattern | `Eval`/`EvalReq`/`Det_r`: zero invocations | | |
| **2026-09-06, Parts 16–21** | further `EC`-shaped variants (`RC`,`DC`,`AC`,`PC`,`RCog`,`FC`) | | | re-derived independently (Part 21 Def. 21.4) | re-proven independently ≥3 more times |
| **post-T21** (`T23`, MD-069; MD-077's own confirmation) | | | no engagement with `Det_r`/`EvalReq`/`Sat(K,r,Γ)` anywhere in the genuine post-T22 corpus | | |
| **`research/knowledgeos-sim/`**, self-dated ~09-02, within the `T5`–`T21` window | own 4-field `EpistemicContract` | own `Requirement(id,prop,kind,note)` | working `Sat`/`Sat_c`/`Eval_c` — related, unmapped | working `Gap`/`Zero`/`Adequate` | — |

The matrix makes visible what the earlier, per-object registries could only state separately: **the
"evaluator" column is the only one with a real discontinuity** — every other column (contract, gap/zero,
aggregation, separation) shows either stable reuse or independent-but-convergent re-derivation within
T21 itself. The evaluator column alone shows a completed, tested result (`Standing(p)`) sitting
un-cited four days before an attempted, incomplete replacement (`Eval`/`EvalReq`/`Det_r`), itself
abandoned hours after introduction (T22's own fiat reversion).

# Provenance Status

**The M0030 → `kernel-reduction/` relationship is, and remains, `RECONSTRUCTED PROVENANCE`.** This
package does not upgrade it, and no option in `04` upgrades it either — even Option C (admit the
entire lane) proposes admission *under* this same provenance label, not a promotion of it to `DIRECT
EVIDENCE` or `MACHINE-OBSERVABLE FACT`.

## Provenance ledger (carried forward from MD-026 `09`, MD-027's corrections applied)

| Element | Status |
|---|---|
| M0030 cites the directory | `DIRECT EVIDENCE` |
| The directory is the actual execution output of M0030's protocol | `RECONSTRUCTED PROVENANCE` |
| `kernel-reduction/` is genuine Model-B research output | `RECONSTRUCTED PROVENANCE`, corrected from MD-026's own slightly stronger original framing |
| `kernel-reduction/` is a self-governing bounded context relative to Model B | `NOT ESTABLISHED` (MD-027 correction) |
| `kernel-reduction/` is a separately organized research lane | `RESEARCH INTERPRETATION`, well-supported |

**If governance answers YES to DQ-1, the admitted material's own provenance field must be recorded
as `RECONSTRUCTED PROVENANCE` in whatever registry receives it — never silently upgraded to `DIRECT
EVIDENCE` merely because admission occurred.** This is the single most important discipline this
package protects.

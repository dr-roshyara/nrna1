# Phase 5N — K-1 Object Reconstruction

## Exact identity, from raw sources (re-confirmed this phase)

- **Origin**: seq 0630 (`step-049`), §49.29/49.30/49.75: $\mathcal{K}=(E,S,T,O,P,R,\Pi,A)$, the 8
  primitives `{Entity, State, Event, Observation, Proposition, Relation, Policy, Action}`.
- **Aliases found, and their evidentiary status**:
  - **"`K_t`"** — used by D285-1/D285-6/D285-7 (seq 1006/1007/1008) as the label for the ratified state.
  - **"M₄₉"** — used by seq 0757 and seq 0764 (both dated 2026-08-28) — "M" plausibly abbreviating
    "Model," "49" matching step-049's own step number.
  - **"K-1"** — this reconstruction's own label (Phase 5C onward), not a corpus-native term.

## Is M₄₉ = K_t = K-1 the same object? Tested, not assumed

- **seq 0757's own text**: "the 8-primitive M₄₉ kernel" — **explicitly ties "M₄₉" to the 8-primitive
  cardinality**, matching seq 0630's own tuple exactly.
- **seq 0764's own text (D-FA-4)**: "M₄₉ = L2 candidate" — reuses the identical label, in a document
  dated the same day.
- **No document anywhere writes an explicit identity statement** ("M₄₉ IS K_t" or "M₄₉ = the 8
  primitives of step-049"). **The identity is supported by matching cardinality (8), matching label
  fragment ("49" = step-049's own number), and same-day co-occurrence — strong circumstantial evidence,
  not a stated equation.**

## Verdict on identity

**RECONSTRUCTED, not directly stated (evidence level E3/E4)**: M₄₉, K_t, and this reconstruction's own
"K-1" label are best read as **the same object**, on the strength of the matching primitive count and
label-fragment correspondence — but this phase does **not** treat this as E1 (direct source evidence),
since no document states the equation explicitly. **Per the authorization's own §4 instruction ("do
not use later labels to retroactively establish earlier identity without evidence"), this
reconstruction is disclosed as inferential, not certain.**

## Consequence for the rest of this phase

Every subsequent finding about "D-FA-4's own ruling on M₄₉" is understood to bear on K-1 **via this
reconstructed identity**, not via a directly-stated equation — a caveat carried forward explicitly in
`04`/`06`/`16`.

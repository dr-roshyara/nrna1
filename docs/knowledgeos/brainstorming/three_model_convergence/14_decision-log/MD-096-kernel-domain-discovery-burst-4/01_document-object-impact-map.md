# MD-096 §01 — Document→Object Impact Map (50 files, `kernel/`, 2026-08-25 10:18–19:41)

Seven parallel extraction agents (batches A–G) read all 50 primary-content files in full, in strict
chronological order. This map records what was covered per batch and the headline findings; full
per-file tables live inside each batch's own extraction report and are drawn on directly in §02/§03.

## Batch A — files 1–8 (10:18–11:46)

Shannon-Weaver → **"principal architect review"** (a large multi-voice chain: a primary architect
voice plus embedded Perplexity/Kimi critique-and-response, Kernel definition restated/refined four
times within one file) → the corpus's first formal PlantUML class model (containing an apparent
verbatim self-duplicate paste, and a "Shared Kernel doctrine" naming a minimal candidate: identifier,
version, temporal interval, provenance pointer, context reference, correlation ID) → "what are we
actually building" → **preservation-unit-versus-consistency-boundary** (pre-registered flag: confirmed
**not** to contain any `KnowledgeAggregate→KnowledgeClaim/Evidence/Determination/Authority` split —
a negative finding against the task's own advance hypothesis) → Merricks "Objects and Persons"
(`KnowledgeAggregate` recurs a third time in the corpus, again as a **rejected** anti-pattern) →
ontological-reduction (an unexecuted scoring-model proposal) → Merricks "Truth and Ontology"
(rejects the universal Truthmaker thesis; refines the prior file's scalar model into a 7-dimension
vector).

## Batch B — files 9–15 (11:52–12:14)

McGinn "logical properties" (two concatenated sub-documents) → O'Connor "Persons and Causes" (two
concatenated sub-documents) → information theory as a lens (its content is later found duplicated
wholesale inside File 5 of this same batch) → McGinn "structural primitives of Knowledge Space" →
**information-theory-extracting-the-knowledge-space-architecture** — **`KnowledgeAggregate` and
`ConflictRecord` co-occur together for the first time since MD-094's File 44 birth**, as sibling
leaf-nodes in a "Kernel as the Boundary" diagram (see §02 for full adjudication) → **"Zero Lens on
McGinn: the Kernel is not knowledge"** (the first explicit naming of a Kernel≠Knowledge distinction;
a new six-part falsifiability battery; a `K-01` principle label, not `K-1`) → **"recommended next
research set before freezing knowledge-space kernel"** (confirmed NOT an actual freeze — a reading-
list proposal explicitly deferring any freeze, with zero governance-ratification vocabulary anywhere
in the file).

## Batch C — files 16–22 (12:27–13:23), the Fagin/Halpern cluster

Floridi → Dretske → **Fagin & Halpern's formal possible-worlds/Kripke model, proposed as a Kernel
candidate** → a genuinely corrupted terminal-scrollback critique fragment → the polished, explicitly
advisory critical review (nine findings F-1–F-9, five retained points, five ranked directions, five
open questions) → **"fagin regime not kernel, acceptance of critique"** — the cluster's own explicit
self-correction event, retracting the possible-worlds tuple's Kernel-candidacy and installing "Kernel
preserves; regimes reason" → a final re-verification against the full source text, adding a
World-Truth/Epistemic-State/Computational-State split and an "Epistemic Regime" vocabulary. This is
the most formally rigorous propose→critique→correct arc found anywhere in `kernel/` to date.

## Batch D — files 23–29 (13:34–15:00)

Gärdenfors "Knowledge in Flux" (claims AGM-belief-revision grounding but never actually reproduces
the AGM formalism) → "evolving knowledge space" (births a genuinely formal-looking `S_t` tuple and a
projection equation) → per-agent projection divergence (×2, **confirmed byte-identical by direct
diff**) → Shannon-Weaver duplicate (self-contained, standalone content extracted) → consciousness-
architecture duplicate (self-contained; births a **third**, homonymously-lettered `K(t)` tuple) →
Searle "Construction of Social Reality" (institutional facts, `StatusFunction(X,Y,C)`).

## Batch E — files 30–36 (15:55–18:07)

"Knowledge as capacity" (`K=(B,L,R,C,P)` capacity tuple) → **Williamson, "stop reductive modelling"**
(explicitly rejects the immediately preceding file's own proposal same-day: "Capacity is not
knowledge") → Brandom "inferential commitment" (references "our earlier Fagin mistake," confirming
Batch C's own correction was carried forward) → Gelfond-Kahl (Input/Derivation-History/Current-
Projection separation) → **"C15 reframed: knowledge at a time in evolving knowledge space"** — the
pre-registered flag: "C-15" found 15 times, used consistently as a pre-existing adjudication-item
label, but **no explicit bridge to the `C-14`–`C-18` register** anywhere in the text; classified
`IDENTITY UNRESOLVED` (§02) → "knowledge element versus state" (two internally-drifting tuple
definitions within one file; the corpus's **only** occurrence of `Γ`, meaning "preserved
histories/provenance" — unrelated to any tracked F4 sense) → Brachman-Levesque (`R_t^A⊂K_t^A⊂Ω`).

## Batch F — files 37–43 (18:08–18:46), the "Knowledge Measure Theory" arc

"Knowledge as discriminate/select/decide" → **"Knowledge Measure Theory v0.1: projection as
measure"** (proposes a measure `μ` with no additivity axiom ever stated) → **"challenge... projection
is not a measure"** (asserts, but does not numerically demonstrate, an additivity failure; renames the
document to v0.2) → **"critique of v0.2"** (confirms genuine responsive revision; contains two
concatenated documents, the second reacting to an **unseen "uploaded synthesis"** — a documentary
gap) → Roberts (measurement-theory scale types; also references an unseen "uploaded table" — a
second documentary gap) → Aggoun-Elliott (asserts, unqualified, "knowledge = conditional expectation,"
"first complete mathematical framework for KnowledgeOS") → **"knowledge is not conditional
expectation, correction"** (systematically rejects nearly every one of the prior file's claims,
including a concrete Brownian-motion counterexample against "martingale = stable knowledge"). One
further isolated `C-15` hit ("τ_retraction... C-15 lifecycle").

## Batch G — files 44–50 (18:47–19:29), the burst's own closing segment

Two interleaved threads, confirmed by direct reading (not by title):
- **Thread A (mathematics)**: "six category errors in the mathematical synthesis" (title claims six,
  text delivers only four explicitly-numbered errors plus five further unlabeled corrections — a
  genuine count discrepancy) → "rejection of complete mathematical framework claim" (an independent
  second review reaching matching conclusions — genuine replication, plus a large embedded artifact,
  see Thread B) → `refinement_phase/` "much better than the previous version" → "relational logical
  structure as core, mathematics as regimes" (a genuine, if informal, core+regime formalism,
  `𝔎=(P,C,T,X,I,E,R,Λ)`, confirmed byte-identical to its own duplicate via direct diff) →
  `refinement_phase/` "this is exactly the behaviour we want" → "critical analysis of the senior
  mathematical judgment" (confirms the core+regime correction as the programme's most important
  result; the file's own body is duplicated internally, once under a `# deepseek :` divider).
- **Thread B (Knowledge concept-extraction), embedded inside the "rejection..." file**: a genuine
  corpus-wide "What is Knowledge?" survey (99+151 documents), producing its **own native `K-1`–`K-11`
  model registry** for eleven competing *Knowledge* (not Kernel) definitions — a third, independent
  `K-`-prefixed labeling system now found in this corpus (see §02 for the homonym adjudication). A
  further, unseen document (referenced only by a `refinement_phase/` file) uses yet a **fourth**
  scheme, `K-M0`/`K-M1`/`C-K1`, never cross-mapped to the `K-1`–`K-11` registry in any text read.

## Cross-batch findings not local to one batch

- **The tracked F4 formal family remains almost entirely absent.** `Sat`, `Det_r`, `EvalReq`, `EC_t`,
  standalone `r`-as-requirement, `Δ_t`, `≡_sem`, `⪯_cap`, `MinKer`, `v1.3` — zero occurrences across
  all 50 files. `Γ` appears exactly once (Batch E), with an unrelated meaning. This extends the
  continuous absence boundary established across the first 111 files of `kernel/` through file 161.
- **The Kernel Identity Ledger** grows substantially again this phase — dozens of new senses, several
  self-corrected within their own document, none merged across files without explicit textual
  evidence (full extension in §02).
- **The Knowledge-State/Knowledge-Space tuple-proliferation pattern already found in MD-095 continues
  and intensifies** — at least a dozen further distinct, non-reconciled tuple/vector forms this phase
  alone (§02), several explicitly self-corrected within a single document.
- **`KnowledgeAggregate`/`ConflictRecord` re-adjudication** (§02) — the most consequential single
  finding of this phase for the K-1 historical trajectory.
- **Corpus hygiene**: two duplicate pairs confirmed byte-identical via direct `diff`/`md5sum` this
  phase (files 25/26, "per-agent-projection-divergence"; files 47/50, "relational-logical-structure-
  as-core"). Multiple internal self-duplications found within single files (a verbatim paste inside
  the PlantUML file, Batch A; an entire critique repeated under a `# deepseek :` divider, Batch G).
  Two documentary gaps flagged (Batch F: two files react to an "uploaded synthesis"/"uploaded table"
  not present anywhere in this 50-file set).

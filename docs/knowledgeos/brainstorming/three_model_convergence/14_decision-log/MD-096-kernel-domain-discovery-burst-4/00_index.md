# MD-096 — Kernel Domain-Discovery Burst 4 (50 files, `kernel/`, 2026-08-25 10:18–19:41)

## Frontier determination

Precise `mtime`-gap analysis of `kernel/`'s remaining files beyond MD-095's own first 111 finds a
single continuous research day (2026-08-25 10:18–19:41, 58 filesystem entries, no gap over ~57
minutes anywhere inside it), then a genuine ~38-hour break before a small 2026-08-27 cluster (2
files), then a ~134.8-hour break before the corpus's final `kernel/` file (2026-09-02, already
identified in this reconstruction's own MD-021 Phase 4 record as the sole `epistemic_knowledgeos`/C2
candidate, `review-yes12345.md`). This phase scopes to the entire 2026-08-25 cluster, bounded by the
first genuine multi-hour break — the same discipline used to bound every prior `kernel/` phase.

Of the 58 filesystem entries in this window, **8 are corpus-produced control/classification
artifacts**, not primary brainstorming documents: `classification/dependency-map.md`,
`classification/README.md`, `corpus/README.md`, `synthesis/README.md`, `falsification/README.md`,
`classification/cluster-map.md`, `00_CENSUS.md`, `classification/contradiction-map.md`. These are
noted in the impact map (their names — "dependency map," "cluster map," "contradiction map" — suggest
the corpus's own attempt to classify the very Knowledge-State/Kernel proliferation this reconstruction
is independently tracking) but are **not** dispatched to this phase's extraction agents; a future
phase may read them directly as a distinct, self-referential artifact class. **This phase's own
primary-content scope is therefore 50 files**, all read in full by seven parallel extraction agents.

## Special-attention flags (identified in advance from filenames only, not pre-judged)

- Batch B's own 6th file, `zero-lens-on-mcginn-the-kernel-is-not-knowledge` (12:05), and 7th file,
  `recommended-next-research-set-before-freezing-knowledge-space-kernel` (12:14) — both engage
  "Kernel" directly by title; flagged for careful, non-presumptive extraction.
- Batch C's own 6th file, `fagin-regime-not-kernel-acceptance-of-critique` (13:09) — an explicit
  correction/rejection event by title; flagged for the Self-Correction Ledger.
- Batch E's own 4th file, `c15-reframed-knowledge-at-a-time-in-evolving-knowledge-space` (17:18) — the
  label "C15" is structurally adjacent to MD-094's own still-unresolved `C-14`–`C-18` governance
  vocabulary (File 44, `wave-1-kernel-extent-versus-contents-adjudication-status`). Flagged for
  extraction agents' explicit attention: is this the same `C-14`–`C-18` register, a coincidental
  numbering, or an unrelated homonym? Default per the master mission: `IDENTITY UNRESOLVED` absent an
  explicit bridge.
- Batch F (files 37–43) traces an apparent propose→challenge→critique→correction arc: "knowledge
  measure theory v0.1: projection as measure" → "challenge to knowledge measure theory: projection is
  not a measure" → "critique of knowledge measurement theory v0.2" → "knowledge is not conditional
  expectation (correction)". Flagged for careful chronological self-correction tracking — do not
  assume the arc's own ordering equals its own logical resolution without reading each step.
- Batch G (files 44–50) contains "six category errors in the mathematical synthesis" and "rejection
  of complete mathematical framework claim" — apparent major adversarial/rejection events — plus two
  `refinement_phase/` files ("much better than the previous version" / "this is exactly the behaviour
  we want") that may represent a subsequent acceptance cycle. Flagged for careful self-correction and
  negative-history tracking; do not assume rejection is final or that refinement supersedes it without
  reading the texts directly.

## Method

Seven parallel extraction agents (batch sizes 8/7/7/7/7/7/7). Each agent applies the master mission's
explicit unit of investigation (**DOCUMENT → ALL OBJECTS → CHRONOLOGICAL THEORY STATES →
RELATIONSHIPS → RECONCILIATION**) and the four-lens discipline (Senior Statistician / Mathematician /
DDD Architect / Principal Knowledge Engineer), with special attention to:
- the Knowledge-State family (fourteen forms already found in MD-095; every new formal
  "knowledge-state"/"knowledge space"/"epistemic state" construction in this batch gets its own full
  reconstruction — exact notation, birth time, fields, type, purpose, bounded context, dependencies,
  predecessor/successor evidence, computational status, later reuse — never collapsed into the
  existing fourteen without explicit textual bridge);
- the K-1 structure candidate (MD-094/095) — as a historical candidate only, never assumed connected
  merely by token recurrence;
- the `Zero(K)` → Invariant Zero / Boundary Zero split (MD-095) — tracked as a deliberate
  non-identification unless this batch's own evidence says otherwise;
- the `KnowledgeAggregate`→`KnowledgeClaim`/`Evidence`/`Determination`/`Authority` DDD split
  (MD-095) — watch for any later document that revisits, extends, or contradicts this split.

## Artifact map

- `00_index.md` — this file.
- `01_document-object-impact-map.md`
- `02_theorystate-updates-and-new-objects.md` — including the Knowledge-State Identity/Evolution
  Ledger, the continued Kernel Identity Ledger, the K-1 historical trajectory update, and the Zero-
  family trajectory update.
- `03_registries.md` — Self-Correction Ledger, Negative-History Register, Cross-Lane Transfer
  Register, corpus-hygiene findings.
- `04_checkpoint.md`

## What this phase does NOT do

Does not identify the K-1 structure with the governance-frozen K-1/K2 track. Does not canonicalize,
ratify, implement, or merge identities. Does not modify any frozen artifact (MD-024–095). Does not
access `theory-extraction/` or `verification/zero-algebra/`.

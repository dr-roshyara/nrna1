# Phase 5B — Non-Lineages and Unresolved Relationships

Per the authorization's own instruction: "negative evidence is first-class evidence." Every item
below restates a finding already established in `02`–`04`, organized here specifically as a negative/
unresolved-relationship register, not re-derived.

## Objects with no demonstrated predecessor

- **KERNEL-OBJ-03** (`ADR-KOS-KERNEL-001`, seq 0167): no document in this phase's own investigation
  establishes what, if anything, it itself derives from — it is treated here as the earliest content-
  origin point found for the K-1 construct, not as having no predecessor absolutely (a stronger claim
  this phase's own bounded scope cannot support).
- **KERNEL-OBJ-09** (state-transition algebra `(𝕂,𝒪)`, seq 0858): no explicit predecessor identified
  within this phase's own bounded check.

## Objects with no demonstrated successor

- **KERNEL-OBJ-03** (`ADR-KOS-KERNEL-001`): PROPOSED status, no later file in the 22-file K-1
  population or the 116-file marker population records formal ACCEPTANCE.
- **KERNEL-OBJ-04** (the falsified six-part aggregate): falsified at seq 0157; no named replacement
  construct is identified as its direct successor within this phase's own investigation (Phase 4's own
  concept register recorded the falsification itself, not a specific successor).
- **KERNEL-OBJ-09**: no successor identified.

## Identical names with no demonstrated identity (homonym candidates)

- **"K-1"** (KERNEL-OBJ-01/02): the clearest case in this phase's evidence — two occurrences,
  structurally dissimilar content, no connecting document found. Recorded `UNRESOLVED`
  (Level 4 research interpretation: likely homonym, not certain).
- **"Kernel" itself**, corpus-wide: already established by the Phase-5 planning analysis and Phase 5A
  as a confirmed homonym across at least the four categories this phase's own Object-graph
  (`02_kernel-lineage.md`) independently reproduces (DDD aggregate design / formal governance artifact
  / mathematical tuple-algebra / conceptual-identity notion) — restated here, not re-derived, as the
  single most load-bearing homonym finding across all of Phase 4/5A/5B.

## Chronological succession with no documented causation

- **EKS → PKS**: chronologically adjacent in the corpus's own early sequence; no document states or
  implies a derivation between them (`03_c1-c2-lineage-evidence.md`, Part 1).
- **`kernel/`-directory Kernel-Boundary-Discovery cycle → `phase_measure_theory/`-directory Knowledge-
  state formalization arc**: the single most consequential finding of this entire phase. Both regions
  discuss "Kernel"/"Knowledge state" extensively; both are internally well-refined (each shows genuine
  `refines` chains within itself); **no document was found, across the 12-object provenance graph or
  the transition-language sample, connecting the two regions by an evidenced derivation.** They are
  chronologically adjacent (the `phase_measure_theory/` material is generally later in sequence) but
  this phase records that adjacency as Category C, not causation.

## Apparent C1/C2 bridges lacking documentary transition

- Seq 2322/2323 (`cross_model`) explicitly name and frame the C1/C2/M/G four-model structure, but
  neither document is itself a *transition* record — they describe an already-adopted taxonomy
  (MD-006's own), not a derivation of C2 from C1.
- Seq 2330's own text (KERNEL-OBJ-12) proposes that its DDD Kernel definition be "reconciled against"
  C1-tagged Kernel candidates (seq 2322/2323's own material) — a **self-declared** possible
  relationship, `POSSIBLE_RELATIONSHIP`, not a demonstrated bridge; the reconciliation itself is not
  found to have been carried out anywhere in the evidence this phase examined.

## Kernel candidates that cannot presently be connected

KERNEL-OBJ-07, KERNEL-OBJ-08, and KERNEL-OBJ-09 (all `meta_research`-tagged, all within
`phase_measure_theory/`-family subdirectories) are **not connected by any explicit document** to
KERNEL-OBJ-01 through KERNEL-OBJ-06 (the `kernel/`-directory family) — consistent with, and a specific
instance of, the chronological-only finding above.

## Kernel candidates that appear in separate lineages

Restated from `02_kernel-lineage.md`'s own closing summary: the twelve objects graphed span at least
five distinct subdirectories and four object categories, with the `kernel/`-family and `phase_measure_
theory/`-family objects showing **no cross-family `REFINES`/`DERIVES_FROM`/`SUPERSEDES` relationship**
anywhere in this phase's own evidence.

## Documents whose classification cannot explain their object lineage

- **Corrected during this phase's own verification** (see `03_c1-c2-lineage-evidence.md`'s own
  disclosed correction): seq 0080/0086 do **not**, on inspection, carry genuine occurrences of
  "Buddhi"/"purification"/"Moksha" — the keyword census matched a classifier's own forward-looking
  "bridge candidate" note (0080) and an explicit absence statement (0086), neither a positive content
  occurrence. This item is removed from this negative-finding register for those two files
  specifically; the genuinely verified early occurrences are seq 0143 (`admissibility`) and seq 0266
  (`Knowledge Space`), both `engineering_knowledgeos` — a narrower, but still standing, instance of
  "classification cannot explain object lineage," restated accurately: neither file's
  `engineering_knowledgeos` classification is itself in tension with its content (both are
  substantively engineering-architecture documents that happen to use vocabulary protocol.md later
  reserves for C2) — so this specific example is better read as **vocabulary overlap without a
  classification anomaly**, not as a documents-whose-classification-cannot-explain-lineage case. Kept
  here only to record the correction explicitly, not as a surviving example of this category.
- The 237-file `phase_measure_theory/knowledgeos_kernel/` population as a whole: classified
  predominantly `meta_research`, a label whose own definition ("methodology, process, research
  governance," per MD-006's table) does not obviously explain a 237-file population containing
  extensive formal Kernel-tuple proposals and Gita-lens Kernel formalizations — the classification
  and the actual object lineage this phase reconstructed for a subset of it (§`02_kernel-
  lineage.md`, Objects 7–9) do not obviously correspond.

## Summary

**Every mandatory negative-finding category the authorization named (§19) is populated with at least
one concrete, sourced instance.** No category was left empty by assumption; each is grounded in a
specific finding already established in `02`–`04`.

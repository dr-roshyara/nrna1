# V0/V6 Semantic Impact — Does This New Material Change MD-036's Own Finding?

## The formal comparison, re-checked against the wider series

- **Does `V6`'s additional requirement have a formally defined semantics?** No — confirmed again,
  more broadly, by `04`.
- **Can `V6`'s domain legitimately be written as `V0`'s domain plus `Defeater`?** Yes — restated,
  not newly established, by `11`'s own input-requirement passage; unchanged from MD-036 `03`.
- **Does the matching-rule ambiguity MD-036 identified (`06`'s own "available inputs" language)
  remain?** Yes — nothing in the 17 newly-read files resolves whether matching is exact-set or
  superset-based; the ambiguity is untouched.
- **Does `V6` change only reachability, or also the meaning of `Verdict`?** Only reachability —
  `13`'s own DDD table types `Validate`'s domain formula using the baseline (`Claim × Evidence →
  Verdict`), never mentioning `V6` at all, which is itself evidence that even the series' dedicated
  DDD analysis treats the baseline as the operative domain formula.
- **Can any semantic-preservation theorem actually be stated?** No — nothing found states or proves
  that `V6`'s `Verdict` preserves any property of `V0`'s `Verdict`, or vice versa.
- **Does the relationship remain `STRUCTURAL CORRESPONDENCE`, or must it be downgraded/upgraded?**
  **Unchanged: `STRUCTURAL CORRESPONDENCE`.** Nothing found in the wider series contradicts the
  domain-extension relationship MD-036 established; nothing found strengthens it to formal
  equivalence or weakens it to incompatibility. `18` §3.3's own characterization of `V6` as a
  "contrast case" (`03`) is, if anything, corroborating context for `STRUCTURAL CORRESPONDENCE`
  specifically — a contrast case is exactly what a structural variant (not an independent
  confirmation, not a contradiction) would be expected to be.

## What genuinely IS new, and how it's handled

The research programme's own later self-audit (`17` Q-6/Q-7, `18` §3.3, F-9/F-10 in `15`) **directly
corroborates** MD-036's own careful separation of the reachability fact from the interpretive
"epistemic safeguard" claim — the corpus itself, independently of this reconstruction's own work,
reaches the same separation (`18`: "V6 was never the evidence... V6 is the contrast case").

**This corroboration is recorded, but it does not retroactively upgrade MD-036's own verdict.** Per
this study's own scope discipline (`01`), the source of this corroboration (`17`, `18`) is not
currently admitted evidence — MD-036's verdict rests, and continues to rest, only on the four
already-admitted files. What this study adds is a *characterization finding*: were `17`/`18` ever
admitted, they would not change MD-036's own conclusion — they would strengthen it, by showing the
same distinction independently drawn by the corpus's own later audit process.

## Direct answer to this study's own central question

**No formal definition exists anywhere in the searched material.** What exists instead is the
corpus's own explicit acknowledgment, in its own open-questions register, that this is unresolved —
which is itself a meaningful, if negative, finding: the gap MD-036 found is not an oversight of this
reconstruction's own reading; it is a gap the original research programme knew about and left open.

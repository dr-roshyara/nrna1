# Resolution Status

## ESTABLISHED

Phase-0 boundary was explicit, directory-location-based (MD-010/MD-011). `research/` was not
Git-tracked at the boundary date. `research/` was not intentionally excluded by name (no evidence of
deliberate exclusion). M0030 directly cites `kernel-reduction/` as its own intended output path. No
admission mechanism was found in `protocol.md`, MD-010, or MD-011. `ConflictRecord` is not evidenced,
admissibly, anywhere searched — with the one apparent lead explicitly self-disqualified by its own
source as a "provenance loop." `Θ` is not evidenced anywhere searched. The commit message that checked
in `research/` is archival/descriptive language, not governance language (no "ratify"/"authorize"/
"admit"/"exclude" vocabulary present).

## NARROWED

"Did not exist to omit" → "was not Git-tracked; prior filesystem existence cannot be established."
"Two independent, decisive pieces of evidence" → "two convergent, evidentially-dependent observations
from one repository-history chain." "A separate, self-governing research context" → "a separately
organized research lane; DDD bounded-context authority not established." "`kernel-reduction/` is Model-
B output" → "a Model-B-adjacent candidate with reconstructed, not documented, provenance." "No
admission mechanism exists" → "none found in the documents actually read."

## NOT ESTABLISHED

Whether `research/` existed, untracked, on disk before 2026-09-01. Whether M0030's execution actually
wrote to the cited path (vs. only being instructed to). Whether `kernel-reduction/` has any DDD-
evidenced authority or ownership relationship to Model B. Whether `theory-v1.1-simulation/` is
genuinely Model-B-adjacent material at all (no citation link found). Whether the wider repository,
beyond the documents this audit and MD-026 actually read, contains an admission mechanism.

## GOVERNANCE-READY?

**YES**, with the corrections in `06` applied when the decision is presented. The evidence package,
corrected, distinguishes clearly what is direct evidence (MD-010/MD-011's own text, the M0030 citation,
the commit message's own wording), what is reconstructed provenance (the same-day authorship inference,
the Model-B-adjacency claim), what is interpretation (the DDD research-lane characterization, now
correctly downgraded from "self-governing"), and what remains genuinely unknown (pre-2026-09-01
filesystem existence, actual execution). A decision-maker presented with the corrected package would
not be misled about the strength of any claim.

## SMALLEST NEXT ACTION

**Prepare a human corpus-boundary decision record** — structurally analogous to MD-010/MD-011
themselves — presenting the corrected MD-026 evidence (per `06`'s register) and asking the real
organization: should `docs/knowledgeos/research/kernel-reduction/` be admitted as Model-B-adjacent
evidence for this reconstruction, given (a) its content directly supplies the operator contracts
MD-024 found missing, (b) its provenance link to Model B rests on reconstructed, not documented,
evidence, and (c) no existing protocol mechanism governs such an admission? This is an organizational
decision, not a further research task — this reconstruction cannot make it on its own authority, per
the same discipline already governing the K-1/K-2 GK-5K track (MD-022).

---

```
MD-027 COMPLETE — ADVERSARIAL AUDIT OF MD-026. TWO CORRECTIONS REQUIRED AND RECORDED (NOT EDITED INTO
MD-026'S OWN TEXT). GOVERNANCE-READY: YES, WITH CORRECTIONS APPLIED. NO DIRECTORY ADMITTED. NO
COMPOSITION. NO MODEL SELECTED. K-1/K-2 UNTOUCHED. STAGE 07 NOT OPENED.
```

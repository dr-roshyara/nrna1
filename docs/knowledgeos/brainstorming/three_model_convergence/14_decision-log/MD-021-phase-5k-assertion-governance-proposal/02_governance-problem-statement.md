# Phase 5K — Governance Problem Statement (Decisions D1–D7)

Applying seq 0927's own $Prepare$ discipline (*"Preparation does not create authority"*) — this file
only *names* what must be decided; it decides nothing.

### Decision D1 — What is the authoritative structure of `Assertion`?

Currently 3 materially distinct, mutually unreconciled characterizations exist (D1/D3, D2/D4, D5 —
Phase 5I/5J `01`). No mathematical, documentary, governance, or executable authority marker resolves
among them (Phase 5J `07`).

### Decision D2 — What does `Π` denote?

Policy (2 of 3 sources, weighted toward but not proven) or provenance-shaped data (1 executable,
worked-example source) — `UNRESOLVED` (Phase 5J `09`).

### Decision D3 — What is the authoritative arity and semantics of `Qualify`?

Four candidate arities exist (1-arg `CaptureAndQualify(O)`; 2-arg `Observation×Policy→Evidence`; 3-arg
`Qualify(o,c,π)→e`; the conceptual equation `Evidence=QualifiedObservation`) — none unified (Phase 5J
`11`).

### Decision D4 — How is `State` represented or mapped?

"The carrier" (D285-6's own phrase) has no independent definition anywhere in the corpus (Phase 5H
`06`, reconfirmed Phase 5I/5J).

### Decision D5 — Which K-1→K-2 projection, if any, is authoritative?

Two competing projections exist ($\pi_1$ via D1/D3's own field set, $\pi_2$ via D2/D4's own field
set), not shown equivalent or compatible (Phase 5I `09`).

### Decision D6 — What is the lifecycle status of the competing historical formulations?

Currently: all `COMPETING`/`RESEARCH`/`EXECUTABLE`, none `SUPERSEDED`, `DEPRECATED`, or `RATIFIED`
(Phase 5J `15`).

### Decision D7 — Who/what has authority to make these decisions?

**Investigated in full in `03`.** Preliminary finding: `GOVERNANCE AUTHORITY NOT EVIDENCED` as an
independently-established organizational fact, notwithstanding "HPA"'s own recurring functional role
as a reviewing/ruling actor elsewhere in the corpus.

## What this phase does NOT do with these seven questions

Does not answer D1–D6 by selecting a winner. Does not answer D7 by inventing an authority the corpus
does not independently establish. Every subsequent file in this phase either narrows the *option
space* for D1–D6 (`04`–`09`) or investigates D7 directly (`03`) — none of them decides.

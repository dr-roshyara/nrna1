# Phase 5K — Open Questions and Required Decisions

## What a governance authority must still decide

The 5 entries in `15`'s own Governance Decision Register (GK-5K-1 through GK-5K-5), plus the
prerequisite question the register itself surfaces: **whether a decision-authority mechanism must be
established before any of the substantive questions can be legitimately ratified at all** (this
phase's own finding, `03`, is that none currently exists).

## What must NOT yet be changed

Per the authorization's own absolute boundary: no Assertion schema is ratified; no Assertion schema is
implemented; no historical artifact is repaired; no DDD pattern is adopted; K-2 is not canonicalized.

## What future implementation work would follow only after ratification

If a governance authority ever ratifies one of Options A–D (or a variant), the following would become
newly authorized (but are **not** authorized by this phase): updating the two conflicting executable
scripts to converge on the ratified schema (or formally retiring one); implementing `Qualify`'s own
chosen arity as a real algorithm; defining `State`'s own carrier concretely; propagating the ratified
schema into any future four-model-convergence or Kernel-unification work (itself still separately
unauthorized).

## Residual reconstruction gaps, restated (not resolved by this phase, since resolving them was out of scope)

Whether any document outside the now-extensively-read set addresses the Assertion conflict
(Phase 5J's own disclosed gap, unchanged); `claim-registry`'s own location; HPA's own charter, if one
exists elsewhere in the corpus and was not found by this phase's own targeted search.

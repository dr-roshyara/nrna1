# MD-104 §01 — Document→Object Impact Map and TheoryState(t) Updates

## Document→Object Impact Map

| Pos | File | Objects touched (born/refined/challenged) |
|---|---|---|
| 31 | kernel-problem-minimum-substrate... | `S_Kernel` (B, the Kernel-substrate hypothesis), conditional determination regime `ℛ=(M,U,P)` (B), `S_min` (B, intersection of per-regime requirements), KST substrate `S_KST=(Q,𝒦,O≤t,r,p,u)` (reinforced, external), "Kernel preserves substrate; regime interprets" principle (reinforced, now with a concrete proposed tuple) |
| 32 | determination-is-the-missing-mathematical-object | `Determination` (R, third signature `D_t(p)`), `Fact` (competing-framing event relative to MD-102), `ℰ_t(p)` (R, first candidate internal structure, 6 fields), a research-question ordering (B: object→operation→representation→equivalence→survival→THEN invariance) |
| 34 | conditional-evidence-and-reasoning-combine-v1 | abstract epistemic state `S` (B, two competing 7-tuple formulations within one file), `U_R` (B, abstract update operator), `Answer_R`/`Update_R`/`Explain_R` (B, three generic regime operations), "Epistemic Representation Invariance" candidate theorem (B) |
| 35 | ...-v2-expanded | CONFIRMED (via `diff`) to be position 34's full content + position 31's full content, concatenated — zero net-new objects; a recombination event only |

(B) = born this segment. (R) = refined, prior formulation(s) preserved, not overwritten.

## TheoryState(t) updates, per object

### `S_Kernel` (new this segment)
- **t(pos31)**: NAMED, TYPED, DEFINED (informally). `S_Kernel = (D, 𝓔, 𝓢, 𝓣, 𝓤)` — Domain elements,
  Evidence items, Sources/agents, Temporal structure, Uncertainty metadata. Explicitly derived as
  "the minimum common substrate across regimes" via `S_min = ∩_{ℛ∈Regimes} Requirements(ℛ)`, tested
  informally against KST/Bayesian/Dempster-Shafer/Non-Monotonic-Logic/Argumentation-Theory's own
  stated requirements. Explicitly labeled "a research hypothesis, not an established fact." Not
  DERIVED (no proof this is actually the intersection, only an informal survey of five regimes), not
  COMPUTABLE, not EXECUTED, not VALIDATED.
- **Relationship to other tracked Kernel-family objects**: `K_t^A=Extract(H≤t,F_t^A,R_t)` (MD-100),
  `κ(K_t)` (MD-101, minimal-representation function), `K_t^*` (MD-102/103, two competing glosses),
  `MinKer`, `K-1`/`K-2` (frozen governance boundary) — **`S_Kernel` is recorded as a DISTINCT,
  UNRESOLVED-IDENTITY candidate relative to every one of these.** It differs structurally from all
  (a 5-tuple over Domain/Evidence/Sources/Time/Uncertainty, not a closure operator, not a minimality
  function, not a frozen governance pair) — same overall "Kernel preserves substrate" narrative
  thread as this lane's own recurring principle (MD-103 §01), now given its first concrete tuple.

### `Determination` (continuing MD-103's own trajectory: two signatures already on record —
`D(O,P,M,A,H)` at pos25, `D(p|E,R,C,t)` at pos28)
- **t(pos32)**: a THIRD signature — `D_t(p)`, embedded in the explicit pipeline
  `O_t → E_t(p|C,M) → R_t(p|E,F,C,M) → D_t(p) → F_t`. The file's own text states directly:
  "I think Determination is the missing mathematical object in the current framework." Not merged
  with the pos25/pos28 signatures — recorded as a third, co-existing formulation within the same
  short conversation (apparent continuation, not a declared identity).

### `Fact` (continuing MD-102/103's own trajectory: at least eight distinct formulations already on
record, ten qualifying-criteria sub-questions per the mission)
- **A genuine competing "missing X" framing recorded**: MD-102 (positions 20-23) established "Fact"
  as "the missing bridge" between Observation/Evidence and Knowledge; position 32 (this segment)
  instead names "Determination" as "the missing mathematical object in the current framework." Both
  claims occur within the same overall conversation/session, at different points. **Neither is
  treated as superseding the other** — per the standing discipline against collapsing distinct
  framings, this is recorded as two parallel, unreconciled "the missing X" claims about two different
  objects, both preserved as historical evidence events. No explicit textual statement anywhere in
  positions 25-35 declares one claim to replace, correct, or subsume the other.

### `ℰ_t(p)` (born MD-103 as a bare notation)
- **t(pos32)**: receives its FIRST candidate internal structure — a six-field tuple:
  `ℰ_t(p) = (proposition, support, justification, status, uncertainty, time)`. Explicitly offered as
  "a hypothesis that needs testing," NOT proposed as the Kernel model. The `status` field's possible
  resonance with MD-102's `Status(k,t,C)` object is noted as a POSSIBLE homonym-risk point, NOT
  merged — same standing discipline applied to the whole "epistemic evaluation function" cluster
  (`Φ`/`Status(k,t,C)`/`E_t(p)`/`ℰ_t(p)`/`K_t^*`), which now numbers at least five unreconciled
  members.

### Abstract epistemic state `S` (new this segment, position 34)
- **t(pos34), formulation 1**: `S = (K, Γ, Π, Ω, Θ, C, t)` — `K`=current commitments/belief or
  knowledge-attribution structure, `Γ`=support/inference/incompatibility/justification relations,
  `Π`=provenance/evidence history, `Ω`=remaining possibilities/alternatives, `Θ`=transition/update
  history, `C`=context/boundary, `t`=temporal position.
- **t(pos34), formulation 2 (SAME FILE, later section)**: `S = (𝒫, 𝒞, 𝒜, 𝒯, 𝒦, 𝒢, ℋ)` —
  `𝒫`=propositions/content, `𝒞`=context, `𝒜`=participants, `𝒯`=time, `𝒦`=epistemic
  commitments/knowledge-attributions, `𝒢`=support/inference/incompatibility graph,
  `ℋ`=history/provenance. **Both are 7-tuples describing broadly the same seven conceptual roles
  (commitment/support-graph/provenance/possibilities-or-participants/transition-or-time/context/time)
  under DIFFERENT symbol sets, within the SAME single file, never explicitly reconciled with each
  other by the source's own text.** Recorded as an internal, within-document multiplicity — neither
  formulation is treated as canonical; both preserved as separate TheoryState entries for the same
  named object `S`.
- Status for both: NAMED, TYPED (7-tuples), DEFINED only informally (component roles glossed in
  prose, no operational test given for any component). Not DERIVED/COMPUTABLE/EXECUTED/VALIDATED.

### `U_R`, `Answer_R`, `Update_R`, `Explain_R` (new this segment)
- **t(pos34)**: `U_R(S_t, E_{t+1}, C_{t+1}) → S_{t+1}` — an abstract, regime-parameterized epistemic
  update operator, with Bayesian/Dempster-Shafer/Fuzzy/Non-Monotonic/Argumentation/Epistemic-Logic/
  Temporal-Logic/Measurement-Theory each offered as a distinct realization `U_{Bayes}`, `U_{DS}`,
  etc. (all EXTERNAL THEORY, not adopted). Later in the same file, `U_R` is refined into three
  generic regime operations: `Update_R(S,E)`, `Answer_R(S,Q)`, `Explain_R(S,Q,A)` — NAMED, TYPED,
  DEFINED only informally. Not DERIVED/COMPUTABLE/EXECUTED/VALIDATED.

### "Epistemic Representation Invariance" (new candidate theorem, position 34)
- **t(pos34)**: proposed as — two regimes `R1,R2` are "task-equivalent over domain D" if there exists
  a semantics-preserving mapping `φ` such that, for all relevant states/evidence/questions:
  `Answer_{R1}(S,Q) ≡ Answer_{R2}(φ(S),Q)` and `φ(Update_{R1}(S,E)) ≡ Update_{R2}(φ(S),E)`.
  **Explicitly self-labeled by the source: "This is not a theorem we have proved yet. It is a
  candidate mathematical definition for our next research phase."** Status: NAMED, TYPED, DEFINED
  (informally, as a candidate definition) — explicitly NOT DERIVED (no proof attempted), NOT
  COMPUTABLE, NOT EXECUTED, NOT VALIDATED. A companion 10-field candidate invariant is also proposed:
  `Invariant(S) = (Content, Participant, Context, Time, Commitment, Support, Incompatibility,
  Consequence, Provenance, Transition)` — same status ladder position.

### Position 35's own content
- Confirmed via direct `diff` (see `02_registries.md`) to be position 34's full text followed by
  position 31's full text, concatenated verbatim, separated by a bare `#`. **No object trajectory is
  updated by position 35 itself** — every object it contains was already logged from positions 31 and
  34 directly. This is recorded strictly as a duplication/recombination event (see `02_registries.md`),
  not as an independent evidentiary document.

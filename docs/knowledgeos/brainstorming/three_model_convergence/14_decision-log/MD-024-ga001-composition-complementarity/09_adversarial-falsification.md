# Adversarial Falsification

| Claim | Falsifier |
|---|---|
| B's operators lack a stated input/output type in its own legitimate evidence base (`00`) | A document within the 151 `b`-tagged files, not the KR-SIM-tagged `ASSERT`/`LINK`/etc. apparatus, that this study's search missed |
| Pair 2's field-name alignment (`S^epi`'s `E`/`Q`/`A` vs. A's `Evidence`/`Question`/`Assessment`) is genuinely diagnostic, not coincidental | A's own tuple could have been built independently of any awareness of B's `S^epi` signature — this study did not check for a citation link between A's Kernel Candidate v0.2/v0.3 document and B's M0032/M0033/M0036 documents; absence of such a check is a real limitation, not a settled negative |
| Pair 4's finding (B's tested result constrains `ConflictRecord`'s adequacy) is a genuine functional correspondence, not overreach | A demonstration that C1's own `ConflictRecord` field, despite its simplicity, is not actually required to preserve the same distinctions B's own M0120 criterion tests — this study assumed the same functional requirement applies to both without confirming C1's own source states that requirement explicitly |
| H4 is the correct predominant verdict, not premature abandonment | A future raw-source pass through additional math-lane files beyond M0030/M0035 that this study did not read, which might contain the missing operator specifications |
| No composition was manufactured to preserve the "everything is unresolved" narrative | Pair 2 and Pair 4 both received genuinely positive, if narrow, findings — this study did not suppress them to keep a uniformly negative result |
| No composition was manufactured to prove complementarity | Pairs 1 and 3 were reported at their true, more negative status (`PARTIALLY TESTABLE` / `NOT FORMALLY SPECIFIED ENOUGH TO TEST`) rather than stretched toward Pair 2/4's more favorable findings |

## Self-check against the study's own absolute prohibitions

No identity, formal equivalence, or canonical Kernel claim was made from any successful partial finding
(Pairs 2, 4). No statistical replication language was used for the 4-pair sample. No missing
specification was reconstructed to enable a test (`Context` for Pair 2, `ConflictRecord`'s internal
structure for Pair 4, both explicitly left unsupplied). GA-038 was not reopened.

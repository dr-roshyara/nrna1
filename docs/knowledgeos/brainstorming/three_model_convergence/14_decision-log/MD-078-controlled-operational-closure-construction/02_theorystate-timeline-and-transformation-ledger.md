# MD-078 §02 — `TheoryState(t)` Timeline Extension and Transformation Ledger

MD-069's own `T0`–`T23` timeline is not renumbered, edited, or reordered. This phase's own new turning
points are recorded as an independent, cross-referenced `TP-n` series, chronologically located relative
to MD-069's own T-series but never merged into it. Per the mission's own §5 discipline, four axes are
kept separate throughout: **chronology** (when written), **reading order** (when this phase encountered
it — the concurrent census document first, then the primary Theory-00-21 series, then the executable
code), **argument order** (what the source itself derives first), and **semantic lineage** (whether a
later object actually descends from an earlier one — never inferred from chronology alone).

## `TP-1` — `Sat`/`EpistemicContract` genuine birth, predates `T5` by five days

- **Chronology**: 2026-08-27 16:25:45 (`phase_measure_theory/step-023`), five days before `T5`
  (2026-09-01/02).
- **Predecessor**: none found earlier in this phase's own search (not a claim of absolute earliest
  occurrence — only that this is the earliest **definitional** use located).
- **Trigger**: the source's own stated motivation — distinguishing "readiness" from "confidence"
  (§12: `HighConfidenceInOneAssertion ≠ DecisionReadiness`), requiring a formal notion of per-requirement
  satisfaction.
- **Newly introduced objects**: `Sat(K,r_i)` (gloss only, no computation rule — genuinely open from
  birth); `EpistemicContract`/`EC=(Purpose,Requirements,EvidenceRules,UncertaintyLimits,ConflictRules,
  TemporalRules,AuthorityRules)` (7-field), explicitly introduced as "a major architectural concept."
- **Semantic lineage to `T5`**: `RECONSTRUCTED`, not `SOURCE-STATED` — MD-067 itself already checked
  and confirmed the math lane's own birth point (`M0001`, Sep 1) does **not** cite
  `phase_measure_theory/`'s Aug-26/27 precursor thread. This phase's own finding does not overturn that
  citation-independence result; it establishes that the same *idea* (contract-indexed requirement
  satisfaction) was independently arrived at twice, five days apart, in two different lanes, without a
  demonstrated citation link — `SAME_LINEAGE_AS` at the conceptual level, `UNWITNESSED` at the
  documentary level. Already partially flagged by MD-065 ("a genuinely new, earlier Step-013/Step-023
  requirement/EC lineage... real, recorded in F4's type ledger, not chased further") — this phase
  chases it, confirms it, and supplies the exact source text MD-065 left unopened.
- **Evidence basis**: direct primary-source read, this phase.
- **Falsification status**: the fiat-stipulation pattern (`Sat(K,r_i)=Satisfied`, asserted not derived)
  is present from this very first use — genuinely tested against every later instance found in this
  phase's own census (T5, Parts 3/5/6/12–21, T22): **never falsified, never broken except by T21's own
  Part VI `Det_r`/`EvalReq` attempt, which is itself abandoned within the same writing session (T22).**

## `TP-2` — `EC` signature drift, a pre-existing, already-named finding in the verification lane

- **Chronology**: 2026-08-27 (same day as TP-1) through 2026-09-01, within `phase_measure_theory/`'s own
  `step-025` sub-series.
- **Reading order**: encountered by this phase via the cross-lane sweep, in
  `verification/spec/STEP-VERIFY-025a-025g.md`, which contains its own prior, explicitly-named "EC
  SIGNATURE DRIFT" table — i.e., **this finding was already made, by a different lane's own governed
  work, before this phase existed.**
- **Transformation chain** (per that document's own table, not re-derived here): `EC_G=(R_G,Γ_G)`
  (2-field) → `EC=(R,Γ,A,V)` (4-field) → `EC=(Requirements,…,Version)` (9-field) → `EC=(R,Γ,A,S,T,D,X,V)`
  (8-field, Provenance dropped).
- **Semantic lineage**: `SOURCE-STATED` as one continuous drifting redefinition within
  `phase_measure_theory/`'s own `step-025` sub-series — the verification lane's own document treats
  these as evolving states of one object, not homonyms, though it does not resolve which (if any) is
  final.
- **Relationship to `TP-1`, `T5`, and `T21`**: none of these four intermediate forms is `SOURCE-STATED`
  identical to TP-1's own 7-field birth form, `T5`'s 4-field `[00-47]` form, or `T21`'s 6-field Part
  I/II form. Six-to-eight mutually distinct `EC` formulations now stand on the record across the full
  corpus history — see §01's own `EC` entry.

## `TP-3` — the T21 rewrite's own internal notational drift (Parts I–21)

- **Chronology**: 2026-09-06 00:23:01 through 07:51:53, one continuous ~7.5-hour writing session.
- **Reading order**: this phase's own direct read (5 parts) plus three parallel extraction agents
  (16 parts), covering every one of the 21 parts plus both worked-example files.
- **Trigger**: none stated by the source — no part explicitly announces a notational reset; each
  simply introduces its own local variant of `Sat`/`Zero`/`Det`/`Δ`/`Req`/`r`/`Γ` while narratively
  continuing ("Recall: `Det(K,p,EC,Γ)`...") as if unbroken.
- **Newly introduced objects, by Part** (selective — full detail in §01): Part I `EC` 6-field promise;
  Part II `Γ` 7-tuple, `r∈Req` abstract, generic `Sat:𝕂×Req→𝒮`; Part III `𝒮_sat` 4-value, `π_EC`
  projection, `Det(K,p,EC,Γ)` proposition-level with proven Theorem 3.1, candidate bounded contexts,
  `Decision(d,K,Q,P,A,Γ)` with proven separation Theorem 3.2; Part IV `Zero(K,EC)` 2-arg, `r` as
  relation-type; Part V `𝕊_sat` 5-value (claims to formalize Part III, actually changes it), `χ_EC`
  projection, `Δ_t` fully composed through the 3-arg `Sat(K,r,Γ)`; Part VI `Det_r`, `EvalReq`, the
  decisive composition; Part VII `Zero(K,EC,Γ)` 3-arg, `Det(p|A)` and `Det(K,p,EC,Γ)` two arities in one
  file; Part VIII `Γ` 4-field+ellipsis, distinct from Part II; Part IX `r` as two different relation-
  instance tuples; Part X `r` as two different inference-rule tuples, `Γ_I` 6-field; Parts 11–15 each
  their own `Δ_X`/`Zero_X`; Part 16 `Decision` 11-field tuple + `DC` 14-field tuple; Part 18 seven
  candidate bounded contexts (no `EpistemicContract`/`AcceptanceCondition` named); Part 21 `Γ_R` 8-field,
  `Req_q(EC,Γ)` 2-arg subscripted-by-proposition, Definition 21.4 (closest full restatement of Part
  III's own Definition 3.5).
- **Semantic effect**: the rewrite reads as one coherent theory but is, on direct evidence, a sequence
  of loosely-coupled Part-local sketches, each reusing familiar symbols in a new local sense.
- **Mathematical effect**: no single Part's formula can be composed with another Part's formula for the
  same symbol without first resolving which of several incompatible local definitions is intended —
  this is the direct, evidenced cause of `Det_r`/`EvalReq`'s own disconnection from the rest of the
  chain (MD-076's finding), now shown to be a special case of a corpus-wide pattern, not an isolated
  defect.
- **Governance status**: none of Parts I–21 carries any ratification marker; all are same-session
  brainstorming-lane drafts.
- **Falsification status**: `SOURCE-STATED` in the sense that the raw text genuinely contains these
  variants — verified directly by primary-source read for every Part, not inferred from summary.

## `TP-4` — independently executable, adversarially-tested `Sat_c`/`Eval_c` and `Sat` implementations

- **Chronology**: `research/knowledgeos-sim/` — exact commit/file dates not independently re-verified
  this phase (flagged as an open item, not fabricated); the `kos12/` module's own docstring self-dates
  "KR-SIM-2026-09-02," placing it within the T5–T21 window, not before or after it.
- **Reading order**: discovered via the cross-lane sweep, verified directly by this phase (`inquiry.py`
  read in full; `satc_spec.py` verified via the extraction agent's own verbatim quotes, corroborated
  in style and citation accuracy by this phase's own independent read of the sibling `inquiry.py`).
- **Newly introduced objects**: a genuinely computable `Sat(K,r,E=None)` (kind-dispatched over five
  requirement kinds, `kos/inquiry.py`) with correctly-derived `Gap`/`Zero`/`Adequate`, citing
  `DEF-20`/`21`/`22` — the same citation IDs MD-067 tracked for `T5`; a genuinely computable,
  three-valued `Sat_c(K,r;Γ)`/`Eval_c(K_t,r,Γ_t)` (`kos12/satc_spec.py`+`evalc.py`), adversarially
  tested with real recorded results.
- **Semantic lineage to `T5`/`T21`**: `RECONSTRUCTED`, not `SOURCE-STATED` — `kos/inquiry.py`'s own
  citation of `DEF-20/21/22` strongly suggests deliberate implementation of the `T5` lineage
  specifically (not `T21`'s `Det_r`/`EvalReq` apparatus, which it never mentions or uses). `kos12`'s
  `Sat_c`/`Eval_c` uses a genuinely different signature (semicolon-separated `Γ`, `_c`-suffixed names)
  and is nowhere cross-referenced by either `T5` or `T21`'s own text.
  **Classified `RELATED OBJECT, CONSTRUCTED CANDIDATE`** relative to both `T5`'s and `T21`'s own `Sat` —
  never `SAME OBJECT`, since neither implementation's own author claims identity, and both are
  self-labeled non-canonical/research-only.
- **Falsification status**: `DIRECTLY EVIDENCED` — this is executable code with recorded test outputs,
  a stronger evidence class than any narrative document in this entire family.

## Transformation Ledger (consolidated, chronological)

| Old state | New state | Reason / evidence | Evidence status |
|---|---|---|---|
| (none) | `Sat(K,r_i)`, `EC` 7-field (`TP-1`) | new architectural concept, source-stated | SOURCE-STATED |
| `EC` 7-field | `EC_G` 2-field → 4-field → 9-field → 8-field (`TP-2`) | same-lineage redefinition, no reconciliation | SOURCE-STATED (verification lane's own prior finding) |
| `EC` (various) | `T5`'s `EC_t` 4-field (`[00-47]`) | independent/uncited re-derivation, `SAME_LINEAGE_AS` conceptually, `UNWITNESSED` documentarily | RECONSTRUCTED |
| `Sat(K,r)` 2-arg, `𝒮_sat` 4-value (Part III) | `Sat(K,r,Γ)` 3-arg, `𝕊_sat` 5-value (Part V) | self-described as "formalizing" Part III; actually changes arity, value-count, projection name | SOURCE-STATED claim, RECONSTRUCTED actual relationship |
| `Sat(K,r,Γ)` 3-arg (Part V) | `Sat(K,r,Γ)=Det_r(EvalReq(...),EC)` (Part VI) | composes the 3-arg predicate with a new determination-function layer | SOURCE-STATED |
| `Det_r`/`EvalReq` apparatus (Part VI) | reverted to `Sat(K,r_i)=Satisfied` fiat (T22, same-day worked example) | never invoked; the theory's own oldest pattern reasserts itself hours later | DIRECTLY EVIDENCED (zero invocations, confirmed by grep) |
| T21's own `Sat`/`EC`/`Γ`/`r` (Parts I–21) | 6+ mutually incompatible Part-local variants each (`TP-3`) | no reconciling document found anywhere in the 21 parts | DIRECTLY EVIDENCED |
| `T5`'s own `Sat`/`EC`/`Δ`/`Zero` shape | `kos/inquiry.py`'s executable `Sat`/`Gap`/`Zero`/`Adequate` (`TP-4`) | independent implementation, same `DEF-20/21/22` citation IDs, different computation strategy (kind-dispatch, not Eval/EvalReq/Det_r composition) | RECONSTRUCTED lineage, DIRECTLY EVIDENCED implementation |

# MD-078 §03 — Cross-Lane Transfer Register and Negative-History Register

## Cross-Lane Transfer Register

| Concept | From lane | To lane | Evidence of transfer | Status |
|---|---|---|---|---|
| `Sat`/`EpistemicContract` (7-field) | `phase_measure_theory/step-023` (Aug 27) | main math lane `T5` `[00-47]` (Sep 1–2) | none direct — `SAME_LINEAGE_AS` conceptually, `UNWITNESSED` documentarily (MD-067's own citation check already confirmed no direct citation) | RECONSTRUCTED, not proven |
| `EC` (various step-025 forms) | `phase_measure_theory` | `verification/DEFINITION-VERIFICATION-REGISTER.md` (DV-27) | direct — the verification lane's own record quotes the 7-field form verbatim, matching `step-023`'s own birth formulation exactly | DIRECTLY EVIDENCED |
| `Det_r`/`EvalReq`/`Sat(K,r,Γ)` (T21, Part VI) | `mathematical_ideas_that_can_be_implemented/` | `verification/gap-discovery/concept-family-birth-census/01-BIRTH-CENSUS-DET-R-FAMILY.md` | direct — that document quotes Part VI §6.18 verbatim, in full, as "the one readable `Det_r`" | DIRECTLY EVIDENCED |
| `T5`'s own `DEF-20/21/22` citation apparatus (`Sat`/`Adequate`/`Zero`) | main math lane | `research/knowledgeos-sim/kos/inquiry.py` | strong circumstantial — identical citation IDs reused in the code's own docstrings, no explicit narrative citation found | RECONSTRUCTED |
| this reconstruction's own `Det_r`/`EvalReq` analysis (MD-067–077) | `three_model_convergence/14_decision-log/` | (outward, to the concurrent census document, inferred) | the concurrent document's own 97%/90%-firewalled finding is best explained by this reconstruction's own extensive analysis text existing in the same repository — **not** verified by opening that document's own underlying grep output, since this reconstruction cannot and does not read its own artifact's effect on another session's search without circularity; recorded as a plausible inference, not a proven transfer | INFERRED |

## The concurrent-session cross-lane finding, in full

`verification/gap-discovery/concept-family-birth-census/01-BIRTH-CENSUS-DET-R-FAMILY.md` (dated
2026-09-10, i.e. today) is a **different, concurrent session's own work product**, operating under the
same overall mission this phase received, but under a self-imposed firewall **excluding
`three_model_convergence/`** — the mirror image of this reconstruction's own standing firewall against
`theory-extraction/`. Its own explicit, disclosed terminal state for `Det_r`:

> "Correct status: **NOT YET SEARCHED** — and not searchable by this lane."

and its own explicitly named smallest remaining gap:

> "A ruling on whether this reconstruction may read `three_model_convergence/14_decision-log/` for this
> concept family. Everything else in the mission is downstream of it."

**This phase is positioned to resolve exactly that gap**, because this phase's own mission explicitly
authorizes reading `verification/` (among other lanes) without a `three_model_convergence/` firewall —
the standing prohibition this reconstruction operates under is specifically against
`theory-extraction/`, not against its own prior work or the `verification/` lane's own K-1/K2 census
material. Reading `three_model_convergence/14_decision-log/`'s own MD-067–077 content is not "opening
K-1/K2 governance" in the sense MD-076/077's own §10 prohibits (that prohibition concerns re-litigating
K-1/K2's own governance *decisions*, not reading this reconstruction's own frozen prior findings about
`Det_r`/`EvalReq` — which this phase has done throughout, as required by its own mission's explicit
instruction not to treat MD-076/077 as final).

**Resolution, stated for the record**: `Det_r`/`EvalReq` genuinely have exactly one occurrence in the
combined corpus (Part VI §6.18) — confirmed by two independent methodologies (this reconstruction's own
876-file traversal plus this phase's own 21-part full-text census; the concurrent census's own
mechanical mtime sweep). The "97%/90% firewalled" figures the concurrent document reports are not
evidence of a hidden, richer history — they are evidence that **this reconstruction's own extensive
analysis of a single sparse primary-source fact dominates the raw occurrence count**, a distinct and
disclosable finding in its own right (a mild, indirect cousin of MD-077's own corpus-hygiene finding
about the 14 self-referential files, though of a different, benign kind — this reconstruction's own
governed decision-log, not misplaced primary-source-directory content).

**One genuine correction owed to the concurrent document**, recorded here forward, its own file not
edited: its claimed 2026-08-27 birth date for `EvalReq` (sourced to `step-025e`) is a substring false
positive on `EvalRequirement(K,r,C)`, a different 3-argument symbol — see §01's own `EvalReq` entry for
the direct verification.

## Negative-History Register — what was searched, what was genuinely absent

| Term | Lanes searched (this phase) | Result |
|---|---|---|
| `AcceptanceCondition` | all 23 Theory-00-21 files; `kernel/`, `verification/`, `synthesis/`, `reviews/kernel/`, `reviews/` (excl. `synthesis/`), top-level `verification/`, `research/` | **GENUINELY UNDEFINED AFTER CENSUS** — zero occurrences anywhere searched |
| `EC.Rules` (internal structure) | same full sweep | **GENUINELY UNDEFINED AFTER CENSUS** — the field name recurs, its internal structure is never given anywhere; Part I's own "will be refined later" promise is never honored in any of the remaining 20 parts |
| `Det_r`/`EvalReq` (beyond the one occurrence) | same full sweep, plus the concurrent census's own independent mtime-based sweep | **GENUINELY UNDEFINED AFTER CENSUS** — one occurrence, corroborated twice, independently |
| `𝕊_sat` (as a set, beyond Part 5's own) | full sweep | **PARTIALLY EVIDENCED** — Part 3's `𝒮_sat` (4-value) is a plausible but `RECONSTRUCTED`, not `SOURCE-STATED`, predecessor; no third source defines it |
| bare `Γ` matching the tracked 7-tuple `⟨D,P,T,U,C,V,A⟩` outside Part II | Parts IV, VII, VIII, IX, X, 11–21 (all checked directly by the three extraction agents plus this process) | **COMPETING DEFINITIONS**, not absence — see §01 |

This register does not claim exhaustiveness beyond the eight directories named in the mission and the
21-part rewrite itself — `docs/knowledgeos/theory-extraction/` was never searched (standing
prohibition), and the concurrent census's own firewalled 97%/90% figures for `Det_r`/`EvalReq` were not
independently re-derived cell-by-cell (their own underlying grep output was not re-run by this phase);
this phase's own independent 21-part full-text census is treated as sufficient corroboration for the
"one occurrence" finding, not as proof the concurrent document's own percentage arithmetic is exactly
right.

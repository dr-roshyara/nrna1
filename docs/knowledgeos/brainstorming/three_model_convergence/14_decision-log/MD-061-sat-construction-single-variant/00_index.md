# MD-061 — Controlled Construction of `Sat(K_t,r)` for One F4 Variant

**Date**: 2026-09-09. **Status: EXECUTED. HARD STOP per the authorizing prompt's own final section.**

## 1 · Objective

Construct and rigorously evaluate a concrete candidate body for `Sat(K_t,r)` for **one** F4 `K_t`
variant, selected by explicit criteria — not to reconcile the family, select a canonical `K_t`, or
build the F3↔F4 bridge.

## 2 · Correction to MD-059/060's own framing, found during variant selection (their text NOT edited)

While re-examining M0048's own `Sat(K,r)` proposal to score it against the selection criteria, its
own text was found to repeatedly say **"the document already defines"** a typed core, an epistemic
state, an Ideal State, `Δ_t`, Zero, transitions — language that only makes sense if M0048 is
*reviewing a specific prior document*, not proposing independently. **M0048 never itself defines
`EC_t`** — it uses the symbol as already-established. This is strong internal evidence that M0048 is
a **same-day review/extension of M0043** (dated 8 hours earlier, same day, 2026-09-02), not an
independently-invented second `Sat` proposal. **This corrects MD-059 §01a and MD-060's own "two
independent sources, neither citing the other" characterization** of the `Sat(K,r)` finding — the
correct classification is **same-provenance-lineage, same-day continuation**, not independent
proposal. Recorded here; MD-059/060's own text not edited.

## 3 · Variant selection

**Selected: V7** (`M0125`/`M0126`'s 11-component tuple, specifically its one typed sub-structure
`Σ_t=(A,S,R,V,C)`) — scored against V4b (M0043's own 10-component decomposition, the closest
competitor by textual/provenance proximity to `Sat`'s own definition) and the rest of MD-060's
12-variant census. **The decisive criterion: typing completeness.** Every other variant in the
census, V4b included, names components without typing any of them — `Σ_t` is the *only* place in
the entire 12-variant population where a component has an actual, corpus-stated value domain to
consult. Full scoring: `01`.

## 4 · Central result

**Gate: C — CONDITIONAL CANDIDATE.** A mathematically coherent `Sat*` was constructed, restricted to
`Σ_t`-shaped requirements over `K_t` instances that possess a `Σ_t` component (i.e., V7 specifically)
— set-membership over `Σ_t`'s own enumerated fields, deliberately avoiding an invented ordinal
structure the source never states (`03`). It passes 3 of 8 falsification tests cleanly, 1 is flagged
as an inherent (not defective) property, 1 finds no contradiction but cannot be tested for transition-
interaction, 1 finds no counterexample (not proof), and 2 expose real representation-dependence
limits (`04`). **`Δ_t` restricted to this same narrow requirement subtype is genuinely computable** —
the first concretely computable slice of `Δ_t` anywhere in this reconstruction's own F4 work (`05`).

## 5 · Artifact map

`01_variant_selection.md` · `02_sat_evidence_ledger.md` · `03_sat_candidate.md` ·
`04_sat_falsification.md` · `05_delta_computability.md` · `06_final_determination.md`.

## 6 · Response to Lane T's cross-lane question (`EKS-34`, found in the shared session log)

Lane T (theory-extraction, firewalled from this lane both ways) filed `EKS-34`: a naming collision
where "`F1`…`F20`" denotes twenty **ablation failure classes** (Representation/Semantic-
interpretation/Relation/Discrimination/… failure) in a corpus source
(`mathematical_ideas_that_can_be_implemented/20260901-222111_prompt-kernel-reduction-and-minimality-
experiment.md`, §§1299–1319 — **the same prompt document the F3 kernel-reduction experiment,
M0030/M0035/M0037, itself executes**) — while this reconstruction's own "`F1`…`F8`" denotes six
kernel *candidate families*, an organizing label this reconstruction assigned itself, never sourced
from the corpus. Lane T explicitly asked, via the shared session log: has a failure-class `F_i` ever
been read as a candidate `F_i` in this lane's own evidence, or the reverse?

**Checked directly**: grepped every MD-052–061 artifact and the cumulative decision log for the
failure-class's own defining phrases ("Representation failure," "Semantic interpretation failure,"
"Relation failure," "Discrimination failure") — **zero hits**. This reconstruction's own `F1`–`F8`
labels have never been drawn from a corpus search for the bare string "`F3`"/"`F4`" — they were
always assigned externally to specific, independently-identified corpus objects (K-1, C0/C0_plus,
`K_t`/`Δ_t`, etc.), never to a label found by searching the corpus for a numbered `F`-tag. **No
contamination found.** Reported back to Lane T via the shared session log, not answered *for* Lane T
(their own reachable evidence is firewalled from this lane in both directions).

## 7 · Explicit non-scope

Does not reconcile all 12 variants. Does not select a canonical `K_t`. Does not claim V4b or V7 is
the true F4 state. No F3↔F4 bridge. No `Beh`/`Trace` construction. No GA-001/GA-038 resolution. No
`≡_sem` adoption. No kernel selection. No Stage 07. No external literature. No code. MD-058/060 not
modified.

# MD-081 — Responsibility Reconstruction: `EC.Rules`/`standard`/`Acceptance`, `r`, `Γ`, `EC` Evolution

## Purpose

User accepted MD-080 as a bounded historical finding, not terminal, and redirected to the remaining
unresolved responsibilities in the family: `EC.Rules`/`standard`/`Acceptance`/`AcceptanceCondition`,
`r`, `Γ`, `EC`, and `Sat`'s own surrounding ecosystem (`Sat_c`/`Eval`/`Eval_c`/`Standing`/
`Determination`). Governing rule unchanged: reconstruct → reconcile → canonicalize; object identity and
semantic-responsibility identity tracked as two separate dimensions throughout.

## Scoping clarification, agreed before execution

A large share of what this mission requests (object-identity work for `r`, `Γ`, `EC`'s own evolution)
was already established with direct primary-source evidence in MD-078/079/080. This phase does not
re-read those same sources — it (1) performs genuinely new searching only where a genuinely new
question is posed (the broader `EC.Rules`-successor vocabulary: `threshold`, `qualification`,
`admissibility`, `evidence standard`, `decision rule`, `evaluation rule`, `contract rule`,
`epistemic rule`, `criterion`, `policy` — none of these were searched by MD-080's own narrower
`Warrant`/`Assessment`/`Verdict`/`AcceptanceCondition` sweep), and (2) adds the new
**responsibility-relation** dimension to the already-established object-identity matrices, and
reorganizes already-verified facts into the newly-requested registry formats (the EC evolution ledger,
the Responsibility Disposition Register). This is disclosed explicitly per artifact below, so no reader
mistakes a reorganization for a fresh independent confirmation.

## Method

New search dispatched: a targeted, grep-first, read-on-hit sweep for the ten `EC.Rules`-successor
candidate terms across all seven designated lanes (math lane, `kernel/`, `verification/`, `synthesis/`,
`reviews/`, top-level `verification/`, top-level `research/`). Everything else in this phase is
adjudication over MD-078/079/080's own already-verified evidence.

## Artifact map

- `00_index.md` — this file.
- `01_ec-rules-successor-search-and-responsibility-disposition.md` — the new search results, plus the
  Responsibility Disposition Register (§12 of the mission) for `EC.Rules`/`standard`/`Acceptance`.
- `02_r-and-gamma-responsibility-relation.md` — object-identity (reused from MD-079) plus the new
  responsibility-relation classification for `r` and `Γ`.
- `03_ec-evolution-ledger.md` — the requested chronological `EC₀→EC₁→...→ECₙ` ledger, built from
  MD-078/080's own already-verified `EC` history.
- `04_sat-ecosystem-roles-and-final-registers.md` — `Sat`/`Sat_c`/`Eval`/`Eval_c`/`Standing`/
  `Determination` classified by role (evaluator/aggregator/acceptance-mechanism/consumer); consolidated
  Responsibility Disposition Register; terminal classification per component; closure.

## What this phase does NOT do

Does not construct `Det_r`/`EvalReq`/a new `Sat`/`Γ`. Does not canonicalize `Standing(p)` or adopt
`Sat_c`/`Eval_c`. Does not invent any mapping. Does not perform F3↔F4 bridging. Does not make a
governance choice between construction alternatives. Does not reopen or rewrite MD-080. Does not modify
any frozen artifact (MD-024–080). Does not access `theory-extraction/`.

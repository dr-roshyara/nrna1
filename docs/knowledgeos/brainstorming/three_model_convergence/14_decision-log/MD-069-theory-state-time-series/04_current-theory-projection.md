# MD-069 §04 — Current Theory: A Projection, Not a Rewrite

Per the mission's §24: this is derived FROM the historical graph in `01_...md`/`02_...md`, not a
cleaned-up canonical statement. Every branch, contradiction, unresolved definition, retired
formulation, and governance gap below is preserved explicitly — none smoothed away.

## Surviving branches (all kept, none selected as "the" theory)

1. **The canonical/Theory-00-21 lineage** (T5→T21→T23): the only branch with a computed `Sat` body.
   Terminal state: CORPUS-SUPPORTED, DEFINED, PROVED, **DEFINED-BUT-UNREVIEWED (GAP-004)**.
2. **ZeroLens branch** (T11): `Boundary_t`-based alternative to `Δ_t=∅`, alive only within its own
   branch, never reconciled with (1).
3. **`ℛ_req`/ABK-1 branch** (T14): the corpus's only genuinely governance-ratified apparatus
   (THEORY-CLOSURE-GATE-2026-v1.0), structurally unrelated to (1) despite sharing English words
   ("Adequate," "Zero," "Requirement").
4. **Zero-Algebra branch** (T15/T17): richly tested, one major hypothesis (Zero predicts Adequacy)
   definitively FALSIFIED within its own scope; unrelated to (1).

## Unresolved definitions (preserved, not forced to a single answer)

- **`EC_t`** — two structurally different definitions (4-field canonical, 6-field Theory-00-21),
  GAP-002, genuinely UNRESOLVED, non-blocking.
- **`K_t`** — 7 historical versions across the traversal, never merged; the one governance rename
  found (`K_t→A_t`, T4-adjacent/`[01-56]`) was never shown propagated into either the canonical or
  Theory-00-21 lineage's own text.
- **`Δ_t`** — two permanently distinct senses (Sat-gap, transition-residue), UNRELATED_HOMONYM,
  GAP-005, closed with qualification (a corpus-hygiene fact, not a live research question).

## Known contradictions (preserved as historical events, not repaired)

- T5's own `[DEF-20]` (contract-wide `Sat`) vs. `[DEF-21]` (per-requirement `Sat`) — an internal arity
  ambiguity within the canonical source itself, never flagged or resolved in-corpus.
- T9's `Sat_c` obstruction (CE-1 factivity) — found, never repaired, the branch simply reclassified
  `Sat_c` as derived and moved on (T10).
- T12's FOL-entailment `Sat` — proposed and retired within the same research session.

## Retired formulations (explicit retirement only, per the mission's own RETIRED-vs-NO_LATER_EVIDENCE
distinction)

- `Sat(K_t,r)⟺K_t⊨Content(r)` (T12) — **RETIRED**, explicit HPA Supervisory Advisory action, not mere
  disappearance.
- Everything else that stopped being cited (T9's `App`, T7's structured `r`, T5's original `EC_t`
  fields as consumed by Theory-00-21) is recorded **NO_LATER_EVIDENCE**, per the mission's own
  distinction — never upgraded to "retired" without an explicit corpus statement.

## Unproven relationships (explicitly marked, never silently assumed)

- `App` → `EvalReq` subsumption: **RECONSTRUCTED** (functional outcome matches), bridge **UNWITNESSED**.
- `standard` (r-field) → `EC.Rules`/`Det_r`: **RECONSTRUCTED**, bridge **UNWITNESSED**.
- `Eval_c` (ZeroLens sense) → `Eval` (Theory-00-21 sense): **SAME_LINEAGE_AS** (shared role), specific
  field-mapping **UNWITNESSED**.
- Theory-00-21's own re-derivation → the canonical T5 source: **SAME_LINEAGE_AS** (identical target
  formulas), no direct citation of `[00-47]` found anywhere in the Theory-00-21 text.

## Governance status (kept separate from research/testing status throughout)

- **Ratified**: only the `ℛ_req`/ABK-1 branch (T14) — structurally unrelated to the canonical chain.
- **Self-declared "frozen," ratification event not independently located**: the "Theory v1.2 FROZEN"
  15-document set (T17) — also a branch unrelated to the canonical chain.
- **The canonical chain and the Theory-00-21 re-derivation (the two branches this reconstruction has
  actually been tracking since MD-057) have received NO governance-adoption event of any kind,
  anywhere in the 876-file traversal.** This is stated plainly because the mission's own completion
  criteria require an explicit answer to "which concepts were actually adopted or ratified," and the
  honest answer, for the object this whole reconstruction cares about, is **none**.

## Mathematical limitations (preserved, not hidden behind the proved theorems)

Every theorem proved in T19–T22 (`[THM 24.1]`, `[THM 25.1]`, `[THM 3.1]`, `[THM 5.1]`, `[THM 5.2]`,
`[THM 6.1]`, `[THM 6.2]`, `[THM 16.38]`, `[THM 21.8]`) is a **conditional** proof: each holds *given*
`Sat`'s own value as an input, never proving `Sat` itself is correctly computed for any real
requirement. `Det_r`'s own body remains, by the source's own explicit design choice, an externally
supplied parameter — not a corpus gap, but a genuine mathematical limitation on how much the proved
theorems can actually guarantee about any concrete application.

## Answering the mission's own nine completion questions, directly

1. **Historical** — when did each major object emerge? Answered in full in `01_...md` (T0–T21, per
   object, with the mission's own 7-stage birth taxonomy applied where the evidence supports it: e.g.
   `EC_t` has separable lexical (T1), conceptual (T1), definitional (T5/T19), and formal (T5/T19)
   birth moments; no object in this graph reaches the mission's own "validation"/"governance adoption"
   stages except the unrelated `ℛ_req`/ABK-1 branch).
2. **Evolutionary** — how did each object change? `01_...md`, per-`T_n` transformation types.
3. **Relational** — how did objects co-evolve? `01_...md`'s cross-object fields; `02_...md`'s three
   graph states.
4. **Branching** — where did competing formulations emerge? Four branches listed above, none merged.
5. **Negative** — what was rejected/falsified/retired/abandoned? T6, T9, T12, T15's KR-BRIDGE-01
   falsification — all preserved as historical events.
6. **Formal** — how did concepts become mathematical structures? T5 (first formalization of the whole
   chain), T7 (structured `r`), T19–T21 (the fullest formalization, with proofs).
7. **Operational** — how did formal structures become computable? T8 (KR-SIM commissioning), T21
   (`Sat` finally computable given a `Det_r` instance) — **still not computable in general**, since
   `Det_r` itself is a designed-open parameter.
8. **Governance** — which concepts were actually adopted/ratified? Only the unrelated `ℛ_req`/ABK-1
   branch (T14). The canonical/Theory-00-21 chain: **none.**
9. **Current** — what theory state does the corpus currently support? T23's terminal state: the
   Theory-00-21 chain is CORPUS-SUPPORTED, DEFINED, and internally PROVED, but **DEFINED-BUT-
   UNREVIEWED** — GAP-004 remains the sole load-bearing open question for the main objective.

**Can the current theory be reconstructed without destroying the historical sequence that produced
it?** Yes — this document and `01_...md`/`02_...md`/`03_...md` together are that reconstruction, and
nothing in MD-057–068's own frozen text was edited to produce it.

## MD-069 STATUS: COMPLETE. HARD STOP.

No canonical theory declared. No `Sat` declared solved. No K-1/K-2 work performed. `theory-extraction/`
untouched. MD-057–068 preserved unchanged throughout — this phase restructures, corrects weight where
warranted, never rewrites.

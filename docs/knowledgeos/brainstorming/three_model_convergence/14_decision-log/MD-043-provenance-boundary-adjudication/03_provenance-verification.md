# MD-043 §3 — Provenance Verification (Steps 3–9)

Method: git history (`git log --diff-filter=A --follow`, `git show --stat`, full commit-message
reading), cross-referenced against `model-boundary-decisions.md`. No inference from vocabulary,
folder names, matching step numbers, or style alone stands unsupported below — each classification
states its actual evidentiary basis.

## `docs/knowledgeos/reviews/synthesis/` (Step 7)

**Git evidence**: first tracked `10bda5d7a` (2026-09-06), commit message: *"implementation-readiness
analysis, its falsification, and the O_core gap record."* The message itself states directly:
*"carrier K_t over 8 primitives is RATIFIED; (A,R) = pi_K(K_t) is a projection; K-4 superseded, K-5
rejected."*

**Classification of the lineage-overlap claim itself**: **ESTABLISHED** (not merely "potential," as
the user's own prior-session wording preserved it) that this document's own subject matter is
directly, textually about the K_t/8-primitive-tuple/`pi_K(K_t)`-projection constructs — the same
constructs Phase 5F's own frozen finding already names (*"K1 and the verification-lane ontology (K-2)
stand in a precise, already-executed corpus-native 'lossy semantic projection' relationship"*).

**What remains unresolved**: whether this specific document IS the Phase 5F artifact, a restatement
of it, an independent assessment of the same ratified fact, or a distinct later document — the commit
message does not settle document-identity, only subject-matter overlap. **Distinguish, per Step 5's
own instruction: same filename (n/a here) ≠ same document ≠ same content ≠ same provenance ≠ same
research object.** Document-identity remains UNRESOLVED; subject-matter overlap is ESTABLISHED.

**Disposition**: unchanged from the user's own prior ruling — characterization-only, not admitted,
not adjudicated. This MD-043 only sharpens the wording of *why*, per its own instruction not to
silently upgrade.

## `docs/knowledgeos/reviews/kernel/` (Step 4)

**Git evidence**: two commits. `f24edf316` (2026-08-28, message: *"knoweledge os modelling"* — a
large, generic early bulk-add commit with no descriptive content); `57d93b0ee` (2026-08-29, message:
*"Session-2 independent review of the closed Session-1 Kernel research — 41/41 reviewed,
recommendation DEFER, nothing adjudicated"*), which states directly: *"Independent adversarial review
of the closed Session-1 corpus extraction (S1-F001..S1-F040 + S1-COVERAGE-REPORT). Session 1
discovers; Session 2 challenges and assesses implementation relevance; neither adjudicates. Read-only
on session1/ throughout."*

**No mention of `theory-extraction/`, "Lane T," "P-series," or "persistence" anywhere in either
commit message.** MD-042's hypothesis that this reads as theory-extraction-adjacent by naming style
is **not supported** by the one piece of first-party evidence (the commit's own author-written
description) that actually exists for it.

**Contrary, positive evidence found**: `session1/`'s own corpus-cutoff marker file
(`.corpus-cutoff-20260825.txt`, a filename inspected only for what it names, not read as scientific
content beyond the listing itself) enumerates source files whose own names and dates
(`20260822-...`, `20260823-...`) match the naming convention and date range of `brainstorming/
kernel/`'s own already-admissible corpus (dated 2026-08-22 through 2026-08-27, first tracked in git
the same day, 2026-08-28, as `reviews/kernel/session1/`). Session1's own filenames (`S1-F003-kernel-
member-candidates-diverge`, `S1-F007-kernel-as-god-object-six-conflations`, `S1-F016-aggregate-
hypothesis-falsified-atomicity-fails-on-every-pair`) closely track the same DDD/kernel-candidate
themes already found admissible in `brainstorming/kernel/` (Fagin-regime rejection, aggregate-vs-
value-object questions).

**Classification**: **STRONGLY INDICATED** (same-day first commit, matching date range, matching
subject-matter vocabulary, explicit "Session 1 discovers / Session 2 reviews" self-description in
the commit message) to be a **structured discovery-and-review layer built over the already-admissible
`brainstorming/kernel/` corpus** — not raw independent corpus, and not theory-extraction material.
**MD-042's specific hypothesis (theory-extraction-adjacent) is withdrawn as unsupported.**

**What remains unresolved**: whether this review layer's own 19+8 findings should ever be admitted as
evidence *for this reconstruction's own* kernel-candidate inventory — that is a separate admission
question (same pattern as MD-032/035/038), not decided here, and not made any more or less likely by
this provenance finding alone. The `sy/` entry is an empty directory (created 2026-08-28, no content)
— negligible.

**Disposition**: precautionary firewall retained, but for the corrected reason: a derived
review/synthesis layer over already-admissible corpus requires its own admission decision before use,
exactly like `reviews/synthesis/` and `reviews/exec/` — not because of a suspected theory-extraction
connection, which this phase does not support.

## `docs/knowledgeos/brainstorming/verification/` (Step 5)

**Git evidence**: `step-272/` first tracked `70fee73c8` (2026-09-06) — the single largest commit in
this repository's history (4,000 files), whose own message states: *"Brings the research corpus under
version control for the first time... checked in because the readiness analyses in
docs/knowledgeos/reviews/ cite these paths as evidence, and a citation to an untracked file is not a
citation,"* enumerating as **separate** bullets: `phase_measure_theory/` ("the D285 state-ontology
work"), `verification/` ("gap-discovery, the readiness matrices, the multiplicity register and the
conflict records"), and **`three_model_convergence/` ("per-file source analyses")** — this
reconstruction's own home directory, named as its own distinct bullet.

**Classification, whole-tree question**: **ESTABLISHED** that `verification/` is not part of `three_
model_convergence/`'s own product — the commit's own first-party description separates them
explicitly. This directly corroborates (not merely repeats) MD-026's own prior, separately-reached
finding about a structurally similar bulk commit.

**Classification, K-1/K2-lineage question for the specific cluster**: `step-272/`, `step-280/`,
`step-281/`, `step-282/`, `handoff/`, `witnesses/`, `canonical-construction/`, `consolidation/` all
share the **identical** first-commit hash `70fee73c8` — genuine git-level co-commitment, not a
naming coincidence. Combined with the commit message's own description of `verification/`'s contents
("conflict records," "readiness matrices," "multiplicity register" — vocabulary matching the K-2/
Assertion track's own `ConflictRecord` apparatus already investigated in the frozen Phase 5H/5I/5J),
and the independently-confirmed literal filename identity of `step-272/`'s own two files
(`05-ADDENDUM-STEP-272A.md`, `06-STEP-272B-REVIEW.md`) against the frozen decision log's own repeated
citation of "Step 272A"/"Step 272B" (`model-boundary-decisions.md` lines 2248–2597) — this cluster is
**STRONGLY INDICATED** to be K-1/K2-track-adjacent material. Document-identity for `step-272/`
specifically is addressed separately below.

**Classification, the remaining ~445 files** (`gita-candidates/`, `carrier-options/`, `glyph-
register/`, `theory-inventory-assessment/`, `registry-extension-proposal/`, `oq4-witness/`, and the
rest of `gap-discovery/`): **PLAUSIBLE at best** — same bulk commit, same broad "verification lane,"
but no specific K-1/K2-vocabulary match confirmed for these subdirectories by name alone, and no
further content was inspected. **UNRESOLVED whether these deal with K-1/K2 specifically.**

**Untracked subset**: `gap-discovery/lane-conflict/` and `gap-discovery/oq1-decision-brief/` have
**zero git history** — not part of `70fee73c8` or any other commit; current, uncommitted working-tree
content. Given the parallel Lane-T session's own same-day P-56 log entry discusses an open "lane
conflict"-adjacent question, this is circumstantially consistent with very recent Lane-T activity,
but this is explicitly **not established** — only PLAUSIBLE, and not pursued further (would require
content inspection beyond minimal provenance checking).

**Step 5's specific question — same filename vs. same document vs. same content vs. same provenance
vs. same research object**: the frozen decision log's own Phase 5J citations (*"read Step 272A and
Step 272B — read in full for the first time,"* `model-boundary-decisions.md` line 2261) do not, in
the text visible to this reconstruction, give an explicit file path for the "Step 272A"/"Step 272B"
they read. **This means: filename match between `docs/knowledgeos/brainstorming/verification/
gap-discovery/step-272/05-ADDENDUM-STEP-272A.md` and Phase 5J's own citation is STRONGLY INDICATED,
not fully ESTABLISHED as document-identity**, absent an explicit path citation in the frozen record
itself. This narrows MD-042's own "confirmed... identical material" claim by one level, per the
correction required in `01_md042-claim-audit.md`. **The underlying Phase 5J adjudication is not
reopened either way** — this finding only concerns whether MD-042 was entitled to call the match
"confirmed," and the honest answer is "strongly indicated, not fully confirmed by an explicit path
citation."

**Disposition**: whole-tree firewall retained, but the justification is now precise and
per-subdirectory rather than uniform — the cluster sharing `70fee73c8` and K-2 vocabulary is
STRONGLY INDICATED K-1/K2-adjacent; the remainder is PLAUSIBLE/UNRESOLVED; the two untracked
subdirectories are UNRESOLVED. All remain excluded from content-level search.

## `docs/knowledgeos/brainstorming/synthesis/` (Step 6)

**Git evidence**: first tracked in the **same** `f24edf316` (2026-08-28) commit as `reviews/kernel/
session1/` — i.e., contemporaneous with the same early kernel-discovery-and-review effort, not a
separate later event.

**MD-042's own F7 finding is directly relevant here and was already on record**: `KNOWLEDGE-CONCEPT-
EXTRACTION-001.md`'s own content pointers (read in MD-042, not re-read here) resolve into files
already inside the admissible `brainstorming/kernel/` corpus — i.e., this ledger is a synthesis
*of* already-admissible kernel/ corpus content, not an off-limits import from elsewhere. This is
inconsistent with treating the ledger itself as theory-extraction-track output.

**Classification**: **PLAUSIBLE-to-STRONGLY-INDICATED** that "EXTRACTION" in these filenames is a
generic research-methodology term ("concept extraction from the brainstorming corpus"), not a
reference to the separate `docs/knowledgeos/theory-extraction/` initiative. MD-042's theory-
extraction hypothesis for this specific naming is **weakened**, not confirmed.

**Disposition**: the 3 files remain unopened this phase (no new content read), but the *reason* is
now "derived-synthesis material requiring its own admission decision, contemporaneous with the same
early kernel-review effort as `reviews/kernel/`" — not "suspected theory-extraction ownership."

## `docs/knowledgeos/reviews/exec/` (Step 7)

**Git evidence**: first tracked `590043f42` (2026-09-06), full commit message: *"Closure(K_9)
computed as a CONDITIONAL DEPENDENCY COMPUTATION — the two capability bases are INCOMPARABLE, O_core
is mapping-fragile, and only 5 blocked constructs survive all 14 worlds."* States directly: computes
the closure of "the DERIVED capability basis K_9 = {C-1..C-9} (GN-77)" against "the STIPULATED basis
K_4," references `O_core`, `minimum_implementable.py`, "Theory v1.2 stays FROZEN, kernel NOT
SELECTED."

**Classification**: **ESTABLISHED, and MD-042's hypothesis WITHDRAWN.** There is zero mention of
theory-extraction, Lane T, or P-series anywhere in this commit message — the "K_9 naming matches this
repo's own recent Lane-T-style commits" hypothesis from MD-042 is **not supported and is withdrawn**.
Instead, this is first-party evidence of the **same K-1/K2/GN-ruling governance-research family**
already known to this reconstruction (GN-31/GN-19 already appear in the frozen decision log; `GN-77`
does **not** — checked directly, zero hits) — meaning this is **later or parallel material within the
same governance-ruling programme, not yet incorporated into the frozen Phase 5A–5N adjudication.**

**Disposition**: firewall retained (still executable code; still subject to the standing "no code
execution" rule; still requires its own admission decision), but for the corrected reason: same
K-1/K2-adjacent governance-research family, a ruling (`GN-77`) not yet covered by any frozen phase —
not "Lane-T style."

## `nrna1/research/knowledgeos-sim/` (Step 8)

**Git evidence**: **entirely untracked** — `git ls-files` returns nothing; `git status --porcelain`
shows the whole `research/` directory as `??` (untracked). Zero commit history exists to check.

**Verification of MD-042's own claim**: MD-042 said this was "previously excluded from MD-030's own
scope and not explicitly re-included" — **accurate as far as it went**, but MD-042 did not check (and
this phase now finds) that the directory additionally has **no git history at all**, meaning it
cannot be dated, attributed, or cross-referenced by any git-level method. This is a **stronger**
reason for caution than MD-042 gave, not a weaker one.

**Classification**: **PROVENANCE UNRESOLVED — PRECAUTIONARY FIREWALL RETAINED.** No further inference
attempted; determining ownership would require content-level inspection beyond minimal provenance
checking, which this phase does not perform.

## Statistical discipline applied throughout this section (Step 9)

No claim above is stated as "independent confirmation" or "replication." Where multiple documents
share vocabulary or a commit, this is recorded as **co-commitment** or **shared-lineage corroboration**
— evidence of common origin, not of two independent observers reaching the same conclusion. The
`step-272`/`step-280`/`281`/`282` filename match is treated as **one** piece of evidence (a git-level
co-commitment fact), not multiplied into several by counting each sibling directory separately.

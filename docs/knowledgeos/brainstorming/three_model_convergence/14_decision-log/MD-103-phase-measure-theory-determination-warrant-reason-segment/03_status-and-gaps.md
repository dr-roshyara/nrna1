# MD-103 §03 — Mathematical / DDD / Statistical Status, Negative Boundaries, Genuine Corpus Gaps

## Mathematical status (status levels never collapsed: NAMED / TYPED / DEFINED / DERIVED / COMPUTABLE
/ EXECUTED / VALIDATED)

- **`Determination`**: NAMED, TYPED, DEFINED (informally, two distinct signatures —
  `D(O,P,M,A,H)` and `D(p|E,R,C,t)` — not reconciled into one). Not DERIVED/COMPUTABLE/EXECUTED/
  VALIDATED at either signature.
- **`Warrant`**: NAMED, TYPED, DEFINED (informally, `Warrant(p|R,C,t)`). Not DERIVED/COMPUTABLE/
  EXECUTED/VALIDATED — no rule given for when the relation actually holds.
- **`Reason`**: NAMED, TYPED (a seven-field tuple), DEFINED (informally, with a seven-way sub-
  typology). Not DERIVED/COMPUTABLE/EXECUTED/VALIDATED.
- **`Justification`**: NAMED, TYPED, DEFINED (informally, `J(R,C)⇒Valid(R)`). Not DERIVED/
  COMPUTABLE/EXECUTED/VALIDATED.
- **`Extraction`**: NAMED only, not independently TYPED this segment.
- **`Fact`**: NAMED only, at least eight mutually distinct structural formulations now on record
  (MD-102 + this segment), none TYPED/DEFINED with consensus. Ten qualifying-criteria sub-questions
  (per the user's mission) remain UNRESOLVED except for sharpened relations to Observation/Evidence
  (see `01_objects-and-theorystates.md`).
- **`Evidence`**: reclassified this segment from object to RELATION (`Supports(O,p,C,t)`) — NAMED,
  TYPED (as a four-argument relation), DEFINED only informally (no satisfaction rule given). Not
  DERIVED/COMPUTABLE/EXECUTED/VALIDATED.
- **`K_t^*`**: two competing glosses now on record (MD-102's `Cn_{R_t}(S_t∪F_{≤t})` closure operator;
  this segment's `K_t^*(p|C_t)` conditional/possibly-non-crisp assessment). Both remain
  NAMED/TYPED/DEFINED only; neither DERIVED/COMPUTABLE/EXECUTED/VALIDATED; the two glosses are not
  shown equivalent or in conflict — simply unreconciled.
- **`E_t(p)`/`ℰ_t(p)`**: NAMED, informally TYPED (functions of a proposition and time), not DEFINED
  with a fixed computation rule (probabilistic instantiation offered as one option among several, not
  adopted as canonical).
- **`Acceptance`/`Rule`/`Criterion`/`Material`**: NAMED at most (component-role only, or absent
  entirely for `Acceptance`).
- The tracked F4 formal family (`Sat`/`Det_r`/`EvalReq`/`EC`/`EC_t`/`Γ`/`Δ_t`/`≡_sem`/`⪯_cap`/
  `MinKer`/`v1.3`): **confirmed absent** across all six files this phase.

## DDD status

- **New bounded-context-relevant distinction**: Evidence-as-Entity vs. Evidence-as-Relation
  (`Supports(O,p,C,t)`) — a genuine architectural fork in this lane's own text, unresolved, not
  present in MD-100-102's own DDD findings.
- **`Determination`/`Extraction`/`Warrant`/`Reason`/`Justification`/`Admission` form a candidate
  pipeline of distinct domain concepts** (`Extraction`→material; `Determination`→process consuming
  Reason/Evidence/Context; `Warrant`→the licensing relation; `Fact`→the resulting proposition-plus-
  status; `Admission`→the recorded epistemic status) — each kept SEPARATE per the user's explicit
  instruction, none collapsed into a single "epistemic evaluation" concept.
- **Homonym watch extended**: `E_t(p)` and `ℰ_t(p)` join `Φ`/`Status(k,t,C)`/`K_t^*` in an expanding,
  unreconciled "epistemic evaluation function" cluster — same spelling/role convergence across
  consecutive files in one author's own conversation is explicitly NOT treated as proof of identity.
- **`Acceptance`/`Admission` terminology-variant flag**: recorded, not resolved — the mission's own
  named object (`Acceptance`) was not found; a role-similar term (`Admission`) was found instead.

## Statistical status

- **Population addressed**: 30 of 566 `phase_measure_theory/` root files now read across MD-100-103
  (5.3%); positions 25-30 (6 files) newly addressed this phase.
- **Duplicate discipline maintained**: position 25's own internal self-duplication logged once (its
  content counted as a single evidentiary source, not doubled) — consistent with the "byte-identical
  counts once" rule, now extended to within-file self-duplication as well as cross-file duplication.
- **Common-provenance discipline maintained**: position 30's synthesis and position 29's critique of
  it are both same-programme, common-provenance material (the critique explicitly responds to the
  synthesis) — neither counted as independent corroboration of the other.
- **No new all-pairs comparison performed**, per the standing instruction; duplication/recombination
  detected only as encountered during ordinary sequential reading.

## Negative boundaries established

- F4 formal family: absent across all six files this phase, extending the boundary now spanning 321
  files' worth of genuinely distinct content across `kernel/` (172), the coordination layer (9), the
  Session-1/Session-2 cluster (112), and `phase_measure_theory/`'s own root population (28 net-new-
  content positions logged across MD-100-103, out of 30 addressed).
- `Acceptance` (as a distinct lexical/formal object): absent across all six files this phase (bounded
  negative finding, this segment only — not a corpus-wide claim).

## Genuine corpus gaps (named, not filled)

- 536 of 566 root-level `phase_measure_theory/` files remain unread by content.
- Whether `Acceptance` appears later in this same root population, and under what relationship to
  `Admission`, remains unknown.
- Whether `K_t^*`'s two competing glosses (closure operator vs. conditional/non-crisp assessment) are
  ever reconciled, declared distinct, or one is abandoned, remains unknown.
- Whether `E_t(p)`/`ℰ_t(p)` are ever explicitly identified with `Φ`/`Status(k,t,C)` or shown distinct
  from them, remains unknown.
- `reviews/synthesis/` (305), `verification/` excl. `zero-algebra/` (496), `research/` (69) remain
  entirely unread.

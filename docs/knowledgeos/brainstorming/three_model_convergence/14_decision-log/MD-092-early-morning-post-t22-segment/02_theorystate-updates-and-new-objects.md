# MD-092 §02 — TheoryState Updates and New Object Entries

## Corrections to already-tracked findings (recorded forward; prior text not edited)

### `Zero_{T,Π}(S;D)` — provenance sharpened
MD-089 §02 recorded this as the already-known "ZeroLens" branch, confirmed extended by an executed
Zero-algebra experiment (MD-089 Batch 3). This phase localizes its **direct textual construction
sequence** one session earlier: the canonical criterion `Zero_{T,Π}(S;D)⟺Π(T(D))=Π(T(E_S(D)))` is first
fully stated in this segment's File 13 (batch B), refined through Files 14–16, and handed off, complete
with a four-level carrier hierarchy and five Witness Generators (`W1`–`W5`), to the 13:14-onward
cluster MD-089 already read. Not a new construct — a dateable predecessor of an already-tracked one.

## New `TheoryState` entries opened this phase

### `Δ_t = D(K_t,I_t)` — cybernetic error-signal function (Files 9–10, batch B)
- Born as a direct structural translation of a psycho-cybernetics goal/feedback/correction metaphor:
  `K_t→I_t→Δ_t→investigation→determination→K_{t+1}`, `I_t=` Ideal State, `Δ_t=D(K_t,I_t)`.
- Explicitly, repeatedly distinguished from the cybernetic error signal itself (`Δ_t≠e(t)`, `Zero≠
  cybernetic error`) — the file's own authors are careful to keep this candidate construct from being
  conflated with the *metaphor's* own error term, but never test it against the tracked `Δ_t`.
- **Identity relative to the tracked `Δ_t={r∈Req(EC_t):¬Sat(K_t,r)}`**: `UNRELATED_HOMONYM`. Positive
  evidence of distinctness — different domain (a distance function between two whole knowledge states,
  not a set of unsatisfied per-requirement predicates), different arity, no shared vocabulary
  (`I_t`/`D(·,·)` vs. `Req`/`Sat`), explicitly self-contained derivation with zero citation either
  direction. This is the fourth confirmed bare-symbol collision on `Δ_t` specifically found across this
  reconstruction (after the post-T22 `Δ_Q(K_t)` gap notion, MD-089; T21's own within-part `Δ_t`
  signature drift, MD-090; and now this cybernetic error-signal sense).
- Status: `[PROP] inspirational metaphor / research hypothesis only`, per the source's own governance
  tag — never promoted, explicitly marked non-canonical throughout.

### Non-monotonic dynamics taxonomy — Stable/Convergent/Oscillatory/Expansive/Revisionary (File 10)
- Five formally defined transition-dynamics classes, `[PROP][OPEN]`, built to replace an initially
  assumed (then explicitly rejected) monotonic-convergence hypothesis for `Δ_t`-minimization.
- No relationship tested against any tracked object — an internally self-contained candidate typology
  for this segment's own cybernetic-metaphor research line specifically.

### `C1`–`C7` cybernetic candidate capabilities and the capability-reduction test (File 10)
- Seven candidate kernel capabilities (State/Reference/Compare/DetectDeviation/SelectCorrection/
  TransformState/Feedback) derived from the cybernetics metaphor, tested against the corpus's own
  existing capability-reduction machinery (`⪯_cap`). Result: `C7` (Feedback) shown reducible to
  `{Observe,Assess,Revise,Θ}`; `C5` (SelectCorrection) left explicitly `[OPEN] responsibility
  placement` after an initial over-hasty `[DERIVED/EXTERNAL]` classification was corrected in the same
  file.
- **Cross-check against this reconstruction's own MinKer/kernel-minimality thread** (MD-043–058): not
  tested this phase — the capability-reduction *method* used here (`⪯_cap`, semantic-contract→
  capability→simulation→irreducibility→minimality pipeline) is the same one MD-043–050 exercised on a
  different candidate set. Recorded as a candidate cross-lane comparison, not investigated further —
  out of this phase's own bounded scope.

### The 4-layer biological/software research architecture — `B→A(B)→S(A)→KOS(S)` (File 14)
- A formally named methodological discipline: biological observation → abstract communication
  structure → software analogue → KnowledgeOS test, with an explicit non-implication rule `B⇏KOS`
  ("There must always be an explicit structural mapping in between").
- This is the corpus's own explicit governance answer to a question this reconstruction has itself
  asked repeatedly about external-lens material (how much weight does a biological/metaphorical analogy
  carry) — recorded as a genuinely reusable methodological finding, not a formal theory object.

## Objects considered and explicitly not opened as new `TheoryState`s

The `W1`–`W5` Witness Generators, the four-level carrier hierarchy (Element/Relation/Sequence/History),
and the preservation-contract triple `Π_1`/`Π_2`/`Π_3` are all direct predecessors of objects MD-089
already opened `TheoryState`s for (or confirmed as the already-known ZeroLens branch) — not reopened
here to avoid double-counting one continuous construction as two discoveries.

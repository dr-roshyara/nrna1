# MD-092 §03 — Registries

## Transformation Ledger (chronological, this 16-file span)

1. **File 2 → File 3**: a proposed monotonicity hypothesis for `Δ_t`-minimization ("does iteratively
   minimizing `Δ_t` monotonically increase determination?") is explicitly rejected in the very next
   document of the same thread — replaced by the `D_{t+1}>D_t`/`=`/`<` trichotomy.
2. **File 3, internal**: an "IF AND ONLY IF" kernel-promotion rule is proposed, then weakened to "only
   if" within the same file; cross-lens agreement is asserted as a near-requirement, then explicitly
   retracted ("Multi-Lens Agreement ≠ Necessary for Kernel Membership").
3. **File 5 → File 6**: a software test case's own Zero-verdict methodology (`Zero_Q(M_A^1)=0` because
   an intermediate state changed) is explicitly rejected in the next file — "Zero is defined by
   preservation of the specified result, not by whether an event changes an intermediate state."
4. **File 7**: the single largest self-correction event in this segment — an entire prior "Derivations"
   section is downgraded from claimed mathematical proof to a reclassified table (Formal definition /
   Existential hypothesis ×2 / Conditional proposition / Candidate definition), with the explicit
   verdict "the mathematical section is not yet a proof."
5. **File 8, internal**: `W4` (Action Stability) is reclassified out of the Zero-algebra family
   entirely into "Determination → Decision boundary" territory; cross-lens agreement as a promotion
   criterion is rejected a *second* time (recurrence of item 2's correction, in a later document).
6. **File 8's own header** ("`[DESIGN] FROZEN — BASELINE PRE-REGISTRATION READY`") is contradicted by
   the same file's own closing line ("I would *not* execute it yet until the actual witness-generation
   carrier... are specified") — the same overreaching-header/correcting-body pattern found repeatedly
   elsewhere in this corpus (Files 3, 6, 7 all show the identical shape).

## Negative-History Register (explicit rejections/withdrawals, this 16-file span)

| Claim rejected | File | Disposition |
|---|---|---|
| Zoom-in-as-deletion (`Zoom(D,d_i)=` delete all but one dimension) | 2 | REJECTED, redefined as focus-preserving-context |
| The Convergence Theorem (path-graph reachability ⇒ determination) | 3 | REJECTED in the same file it was proposed, "almost tautological" |
| `K_t⊆K_{t+1}` as the Knowledge-State carrier relation | 3 | WITHDRAWN, replaced by `K⪯_𝒪K'` with explicit `Avail` predicate |
| `n<60` sparse-cell dropout rule | 5 | WITHDRAWN as incoherent/data-dependent |
| "M0 survived the selection audit" (implying validated causal effect) | 5 | EXPLICITLY RETRACTED as overclaiming |
| Cancellation-loop "derivation" (`Zero(M1)=0∧Zero(M2)=0⇒Zero(M1∘M2)=1`) | 7 | REJECTED, "generally false" |
| Epistemic closure = entropy/cardinality reduction to one cause | 7 | REJECTED |
| `Π` as "equivalence relation or projection operator" (conflated) | 7 | REJECTED, split into two distinct formalizations |

## Cross-Lane Transfer Register

**Inbound**: File 3's capability-reduction test explicitly reuses the `⪯_cap`/semantic-contract→
capability→simulation→irreducibility→minimality pipeline this reconstruction's own MD-043–050 phases
exercised — a candidate cross-lane comparison, not investigated this phase. File 4's own "not a complete
theory yet" self-assessment names "Semantic equivalence `≡_sem` ↔ capability `⪯_cap` bridge" and "Kernel
minimality" among six unresolved critical-path items — directly adjacent to, but not cross-cited by,
this reconstruction's own MD-043–058 work on exactly that bridge.

**Outbound / continuity confirmed**: this segment's own `KR-BIOCOMM-ZERO-01` protocol, `Zero_{T,Π}(S;D)`
criterion, and `W1`–`W5` witness generators are the direct, traceable predecessor of the material
MD-089's Batch 1 read starting at 13:14 the same day — a confirmed same-session continuation, not a
homonym or independent lineage.

**Firewall discipline maintained**: `theory-extraction/` never accessed.

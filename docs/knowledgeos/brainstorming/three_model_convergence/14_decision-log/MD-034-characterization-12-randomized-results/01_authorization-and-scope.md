# Authorization and Scope

## Pre-execution disagreement check

No blocking disagreement. The prompt is sound and matches MD-033's own recommended next step exactly
— a characterization/admissibility-recommendation study, not an admission decision or composition
test.

## Scope

**Read in full**: `docs/knowledgeos/research/kernel-reduction/12-randomized-results.md` (206 lines).

**Read for cross-reference only, not as new evidentiary content**: `03-capability-model.md` and
`04-operator-contracts.md` — re-grepped for their own "§12" citations (already known from MD-033),
to confirm the exact wording being characterized.

**Not read, per this study's own scope**: any other file in `docs/knowledgeos/research/kernel-
reduction/` (e.g. `07-ablation-design.md`, `10-ablation-results.md`, `11-pairwise-results.md`,
`14-alternative-kernels.md`), the executable lane, `theory-v1.1-simulation/`, or
`docs/knowledgeos/theory-extraction/`. `12` itself does not cite any of these by name (confirmed,
`02`), so no further scope expansion was triggered by its own content.

## What changed this study's expected shape, and how that is handled

MD-033 anticipated (correctly, for the population it examined) that `V6` would likely be absent from
whatever this file contained, based on the pattern already found in `03`/`04`/`06`. This study finds
the opposite for *this specific file*: `V6` is extensively present. This is reported as the finding
the evidence actually supports, not forced into the shape the prior study's own (correctly scoped)
expectation suggested.

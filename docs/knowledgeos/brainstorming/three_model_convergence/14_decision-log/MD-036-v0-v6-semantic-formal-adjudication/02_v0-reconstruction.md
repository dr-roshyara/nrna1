# V0 Reconstruction (Baseline)

Restated from MD-033's own `03_validate-contract-reconstruction.md` (no new content), which remains
the authoritative reconstruction — this study does not re-derive it, only re-states it for
side-by-side comparison with `06_mathematical-comparison.md`.

| Field | Status | Value / evidence |
|---|---|---|
| Atom | SOURCE-CLOSED | `warrant-assessment` (`04` line 32, 59) |
| Output carrier | SOURCE-CLOSED | `Verdict` (`03` line 49; `06` line 41) |
| Input carriers | SOURCE-CLOSED | `{Claim, Evidence}` or `{Hypothesis, Evidence}` (`06` line 41, chained through `04` line 59) |
| Non-reducible responsibility | SOURCE-CLOSED | "assign warrant given evidence + assumptions" (`04` line 59) |
| Corpus support rating | SOURCE-CLOSED | `STRONG` (`04` line 59) |
| State effects | SOURCE-CLOSED (negative) | "none, except `Revise`" (`04` lines 44–45) — `Validate ≠ Revise`, so no state mutation |
| Information effects | PARTIALLY SPECIFIED | "governed by the data processing inequality" (`04` lines 45–46) — a general principle, not a `Validate`-specific bound |
| Dependencies | PARTIALLY SPECIFIED | "whatever the type rules demand" (`04` line 46) — a pointer to the same derivation mechanism, not new information |
| Preconditions (beyond input availability) | NOT SPECIFIED | zero hits, MD-033's own exhaustive census |
| Postconditions (beyond output-carrier type) | NOT SPECIFIED | zero hits |
| Failure/error semantics | NOT SPECIFIED | zero hits |
| Determinism/non-determinism | NOT SPECIFIED | zero hits |
| Assumptions | PARTIALLY SPECIFIED | the responsibility text itself names "assumptions" as an input to the warrant-assessment, but no further definition of what an assumption *is* as a carrier or how it enters the derivation table is given anywhere in `03`/`04`/`06` |
| Epistemic meaning | PARTIALLY SPECIFIED | "assign warrant/verdict given evidence + assumptions" (`04` line 32) is the closest thing to a stated meaning; no further gloss is given |
| Explicit limitation (stated by the source itself, about `V0` specifically) | **NOW SOURCE-CLOSED, newly relevant given `12`'s own admission** | `12`'s causal-criticism section states directly that `V0` "permits" the failure mode `fit ⇒ validation` — see `04` for the epistemic status of this claim |

## What changed since MD-033's own reconstruction

Only one row: "Explicit limitation," which MD-033 could not populate (that content lived in the
then-unadmitted `12`). Every other row is unchanged — `12`'s own admission adds no new information
about `V0`'s own contract beyond this one explicit, source-stated critique.

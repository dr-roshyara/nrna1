# V6 Reconstruction

Independently reconstructed from `12-randomized-results.md`, now admissible (MD-035).

| Field | Status | Value / evidence |
|---|---|---|
| Definition | SOURCE-CLOSED | "a `Verdict` requires a surviving-defeater step" (`12` line 144) |
| Purpose (as stated) | SOURCE-CLOSED | to block the failure mode the source calls "`fit ⇒ validation`" — see `04` for the epistemic status of this framing |
| Input requirements | SOURCE-CLOSED (by cross-reference to the causal-criticism table) | `12` does not restate the exact carrier set for V6 in table form the way `06` does for `V0`; the only explicit input-shaped statement is the causal table's own framing ("verdict requires surviving a defeater") plus the operators-made-derivable column ("`Discriminate`, `DetectGap`" — line 144, unchanged from V0's own column) |
| Output requirement | SOURCE-CLOSED | `Verdict` — unchanged from `V0` |
| Additional steps/conditions | SOURCE-CLOSED, at the level of description | a defeater must be produced (via `Challenge`) and must *survive* — "survive" is not itself further defined (does it mean the defeater exists and is unrebutted? is not itself defeated? no further operational definition is given) |
| Changed semantics | SOURCE-CLOSED (directly stated consequence) | "Is a `Verdict` reachable without `Challenge`? V0: yes. V6: no." (`12` lines 196–199) |
| Claimed failure mode addressed | SOURCE-CLOSED (as a claim) | "`fit ⇒ validation`" — a system that "fits" (produces internally consistent output) can be "validated" without ever facing a defeater |
| Relationship to V0 | SOURCE-CLOSED | presented as one of "seven alternative reasonable models" (`12` line 132), i.e. an alternative to, not a refinement or successor of, the baseline — though see `06_mathematical-comparison.md` for the precise formal relationship this study finds beyond that framing |
| Described candidate vs. adopted design | **Described candidate only — NOT adopted.** | `12`'s own failure-rate table (line 38 onward, "pooled") is computed using the baseline/leave-one-out arms, not V6; the causal-criticism section itself frames V6 as a demonstration of what *would* change, not a redesign actually carried through the rest of the document. No source text states V6 replaces V0 as the design in use. |

## What "surviving a defeater" is NOT stated to mean, precisely (avoiding over-specification)

The source does not define: how many defeaters must be attempted; whether a defeater must be
attempted by a specific operator (`Challenge`) or could arise otherwise; what "surviving" means
formally (unrebutted? logically consistent despite the defeater? something else); whether a
`Verdict` produced under `V6` differs *internally* from one produced under `V0` (same carrier kind,
`Verdict`, in both) or only differs in *when* it is reachable at all. This is recorded as `NOT
SPECIFIED BY SOURCE`, not inferred.

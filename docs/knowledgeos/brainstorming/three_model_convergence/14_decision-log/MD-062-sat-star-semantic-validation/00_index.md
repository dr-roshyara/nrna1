# MD-062 — Controlled Validation of the F4 `Sat*` Semantic Slice

**Date**: 2026-09-09. **Status: EXECUTED. HARD STOP per the authorizing prompt's own final section.**

## 1 · Objective

Determine whether MD-061's `Sat*` is a semantically defensible foundation for F4 or merely a
formally computable predicate produced by MD-061's own narrowing choices — **before** extending it
to V7's other ten components.

## 2 · Method note (same standing clarification as MD-058–061, reapplied briefly)

Per this phase's own tightly-scoped research boundary ("reconstruct MD-061 exactly," "use only
transformations already justified by the corpus"), this phase validates against evidence already
assembled in MD-057–061, plus one genuinely new primary source — `20260902-175306_kr-contr-fde-
2026-09-external-writeup.md` (M0127) — which the user pointed to directly between MD-061 and this
authorization, and which turns out to be decisive evidence for Phase B/E below. No fresh 8-directory
sweep performed.

## 3 · Central result

**Final Determination: C — FORMALLY COMPUTABLE SURROGATE.** `Sat*` is mathematically coherent
(reconfirmed, `01`) but: **(a)** it never takes `EC_t` as an argument at all — the corpus's own
stated purpose-relativity of satisfaction (`[DEF-19]`, M0043) is not merely simplified, it is
**silently absent** from the construction (`02`/`03`); **(b)** it consults only 1 of `K_t`'s 11
named components; **(c)** by direct structural analogy to a same-day, directly relevant, previously
unread primary experiment (M0127, `KR-CONTR-FDE-2026-09`) that specifically tested whether value-only
representations preserve required epistemic distinctions — and found they do not, collapsing 6–12 of
14 required scenarios — `Sat*`'s own bare value-membership shape plausibly reproduces the same
collapse (`04`, E2/E3/E5). **Not D (REFUTED)**: no counterexample shows `Sat*` gives a *wrong* answer
against a stated corpus ground truth — the failures found are inadequacy (missing distinctions), not
incorrectness.

## 4 · A genuinely new primary finding, folded in from between MD-061 and this phase

M0127 (`KR-CONTR-FDE-2026-09`) is dated the same day as M0043/M0125/M0132 (2026-09-02), addresses a
directly adjacent question (structured evaluation of propositions against evidence), and is **never
cited by, or citing, the K_t/`Δ_t`/`Sat` lineage** — despite M0127 itself proposing (§14.2,
explicitly labeled a candidate, not an architectural decision) that its own `C_t` structure populate
**`C_t`, one of V7's own eleven top-level components** — the *second* typed component this
reconstruction has now found in V7, beyond `Σ_t` alone (MD-061's own finding). Backlog implication:
`05`.

## 5 · Artifact map

`01_sat_reconstruction.md` · `02_requirement_semantics.md` · `03_ddd_semantic_test.md` ·
`04_sat_falsification.md` · `05_delta_semantic_test.md` · `06_representation_test.md` ·
`07_final_determination.md`.

## 6 · Explicit non-scope

Does not extend `Σ_t` to V7's other components (including `C_t`, despite `04`'s finding it also has
a typed candidate). No F3↔F4 bridge. No GA-001/GA-038. No canonical `K_t`. `Sat*` never called the
true F4 `Sat`. No external literature. No code. MD-061 not modified.

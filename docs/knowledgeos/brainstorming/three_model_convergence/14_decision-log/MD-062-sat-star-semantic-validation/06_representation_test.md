# MD-062 §06 — Representation Independence (Phase G), Corpus-Justified Transformations Only

| Transformation | Result |
|---|---|
| Field-name reordering within `Σ_t` | **PROVEN INVARIANT** — reused from MD-061 T4, not re-derived here. |
| Alternative encoding of the same enumerated value | **UNTESTABLE FROM CURRENT CORPUS** — no source states an alternate encoding for `Σ_t`'s own fields to test against. |
| Projection from the larger V7 state into `Σ_t` | **UNTESTABLE FROM CURRENT CORPUS** — no source states that `Σ_t` alone is an adequate summary/projection of `K_t` for satisfaction purposes; this is precisely the adequacy question `02`/`04` already found unaddressed, not a separate representation-independence question with its own independent answer. |

**No equivalence relation manufactured to force a cleaner result**, per the authorizing prompt's own
explicit instruction. Where "same semantic state" is undefined, that is reported as
`UNTESTABLE FROM CURRENT CORPUS`, not resolved by assumption.

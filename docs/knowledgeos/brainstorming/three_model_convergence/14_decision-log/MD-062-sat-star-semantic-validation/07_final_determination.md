# MD-062 §07 — Final Determination

## Statistical discipline (Phase H), stated once, applied throughout

M0127 (`KR-CONTR-FDE-2026-09`) is classified precisely: **same-day (2026-09-02), separately-authored,
sibling-question corroboration** — not independent replication (it tests a different but
structurally analogous representational question, never mentioning `Sat`/`Σ_t`/`Δ_t^Σ`), not common-
provenance lineage (unlike M0048's own relationship to M0043, MD-061's own finding, which remains in
force and is not re-litigated here). Its countermodels are used as **structural analogy**, explicitly
never as direct evidence about `Sat*` itself.

## Final Determination

### **C — FORMALLY COMPUTABLE SURROGATE**

`Sat*` works mathematically (T1/T2/T3/T7, MD-061, reconfirmed `01`) but cannot yet legitimately be
identified with corpus `Sat`. Reasoning, at the strength the evidence supports:

- **Not A (SEMANTICALLY CORPUS-GROUNDED)**: `Sat*` is not merely under-specified relative to the
  corpus — it structurally omits `EC_t`, the corpus's own stated source of satisfaction's purpose-
  relative meaning (`02` Phase B(4)/(5)/(6), `03`).
- **Not B (MATHEMATICALLY VALID BUT SEMANTICALLY CONDITIONAL)**: this label would fit if the open
  items were merely unresolved parameters within an otherwise-adequate shape. Here, four
  demonstrated counterexamples (E1/E2/E3/E5, `04`) — three of them directly echoing collapse patterns
  a same-day, sibling in-corpus experiment (M0127) specifically found and fixed by adding boundary
  metadata — show the *shape itself* likely under-preserves required distinctions, not merely that
  some parameters remain open.
- **Not D (REFUTED)**: no counterexample shows `Sat*` returning an *incorrect* answer against a
  stated corpus ground truth — every failure found is a missing distinction, not a wrong one, and no
  stated corpus ground-truth case exists to test against in the first place (T8, MD-061, still true).

**C is the honest fit**: mathematically real, corpus-structure-grounded in its raw material (`Σ_t`'s
own typed fields), but not yet a defensible claim to represent corpus `Sat`.

## Consequence for the recommended next step

**Do not extend `Σ_t`'s typing approach to the other ten V7 components yet** (MD-061's own suggested
next step, now superseded by this finding — MD-061's own text not edited). Extending a shape already
shown to under-preserve required distinctions would produce a larger surrogate, not a better one.
**The more consequential next input, precisely named**: incorporate `EC_t` — or, following M0127's
own adequacy-tested pattern, a Reason/Provenance/Context-style boundary channel — into the
requirement/`Sat` construction *before* any further component-typing work. This is a research act,
not attempted here (out of scope, per §00 §6).

## MD-062 STATUS: COMPLETE / HARD STOP

No MD-063 opened by this completion. No component extension. No F3↔F4 bridge. No GA-001/GA-038. No
canonical `K_t`. `Sat*` not called the true F4 `Sat`.

# MD-089 §02 — TheoryState Entries for Newly-Discovered Load-Bearing Objects

Objects opened here are new *within this 41-file segment*. None is merged with, or assumed identical
to, any already-tracked F4-chain object merely because it shares a bare symbol — every case of
symbol-sharing is flagged as `UNRELATED_HOMONYM` (pending further evidence) rather than resolved.

## `Standing` (new instance — FactFinding pipeline stage)

- **Birth**: Batch-1 file 13 (`...zoom-in-discovery-vs-zoom-out-integration...`), 2026-09-07.
- **Definition**: `FactFinding:(D^cand,E,H_Q,M,S)→Standing→Determination` — an unelaborated
  intermediate stage between evidence assessment and determination.
- **Identity relative to the already-tracked `Standing(p)=(S⁺,S⁻,R,P,Ctx,Cond)`** (MD-080's own
  tested predecessor, born `M0127`, 2026-09-02): **`UNRELATED_HOMONYM`, not further resolved this
  phase.** The two objects sit in structurally different architectures (this one is an unelaborated
  waypoint in a Zoom-In/FactFinding pipeline with no stated internal structure; MD-080's is a fully
  specified, empirically-tested 6-field evaluator) and neither cites the other. Recorded as a
  collision, per this reconstruction's own standing `EKS-41`/`EKS-45` pattern (bare-symbol reuse
  across non-cross-citing lineages) — extends that pattern to a fourth instance, not filed as a new
  ticket.
- **Status**: `defined, not elaborated` (object) / not applicable (no responsibility claim made for
  it beyond the one-arrow diagram).

## `Δ_Q(K_t)` / `Δ_t` (new instance — inquiry-relative epistemic gap)

- **Birth**: Batch-1 file 8 (`...next-direction-capability-traceability...`), 2026-09-07, as an
  explicit *replacement* for an abandoned `Zero(K_t,Q)≠0` formulation in the same document (line
  174–183: "or, using the newer language, `Δ_Q(K_t)≠∅`").
- **Definition**: gap between what an inquiry `Q` requires and what `K_t` currently supplies;
  typed further in file 9 as `Δ_{Q,K}`, consumed by `NextEpistemicAct`.
- **Identity relative to the already-tracked `Δ_t={r∈Req(EC_t):¬Sat(K_t,r)}`** (T5's own canonical
  freeze, `[00-47]`): **`UNRELATED_HOMONYM`.** This segment's `Δ_t` is a generic inquiry-vs-knowledge
  gap with no `Req`/`Sat` machinery anywhere near it; the tracked `Δ_t` is specifically the
  unsatisfied-requirement set. Zero cross-citation either direction. This is the *same* bare-notation-
  collision shape already logged for `Req`/`r` (`EKS-45`, extended to a third pair there) — a further,
  fourth confirmed instance of the pattern, not a new ticket.
- **Status**: `defined`, feeds `NextEpistemicAct` (which is itself `[PROP][OPEN]`, never adopted).

## `EC`/`EC_t`/`E_C` (new instance — "evidence channel" parameter)

- **Birth**: Batch-1 file 10 (`...adequacy-replaces-entailment...`), 2026-09-07: `Adeq(K_t,Q,C,EC,
  S,R)`, `Determine(K_t,Q,C,E_C,S,R)`.
- **Reused**, not redefined, in Batch-1 file 13 (`Q,C,E_C,S,R` held constant, ZF4), Batch-2 file 9
  (`Adequacy(R,Q,C,EC)`; `Preserve(Packaging(R,Q,C,E_C))`), and Batch-3 file 2
  (`Θ(K_t,e_{t+1},Q,C,S,M,H,EC)`) — always as an already-known parameter, never re-derived, never
  given a field structure.
- **Identity relative to the already-tracked `EC`/`EC_t`** (T5's `[Def 5.1]` 4-field original, T21's
  6-field final, and the five further competing formulations already in the Mathematical Closure
  Matrix, MD-085): **`UNRELATED_HOMONYM`, most likely.** This segment's `EC` is never given field
  structure anywhere it appears — it functions purely as a placeholder argument name ("the epistemic
  contract"), never instantiated, never cited against T21. Cannot be ruled `SAME OBJECT` (no positive
  evidence either) nor confidently `UNRELATED` (the informal gloss — "epistemic contract" — is
  consistent with the tracked object's own general role) — recorded as `UNRESOLVED, WEAK STRUCTURAL
  ECHO ONLY`, the most honest available classification, not forced to either pole.
- **Status**: `used as an unelaborated parameter`, never `defined` within this 41-file segment.

## `Γ_i` / `Γ` (new instance — knowledge-tuple and inquiry-tuple component)

- **Birth**: Batch-1 file 7 (`...what-knowledgeos-makes-possible...`), 2026-09-07:
  `K_t={(p_i,S_i,E_i,W_i,Γ_i,t_i)}`; inquiry tuple `q=⟨X,Y,Γ,τ,Π,Λ⟩`; `Retrieve(E|q,Γ,t)`.
- **No further elaboration anywhere else in the 41-file segment** — a single-document appearance,
  never picked up by any of the other 40 files, never given a field structure.
- **Identity relative to the already-tracked `Γ`** (four competing structured forms already
  catalogued in MD-085/086/087/088, none reaching `SAME OBJECT`): **`UNRELATED_HOMONYM`.** A bare,
  single-use tuple slot with no stated internal structure cannot be meaningfully compared to any of
  the four already-tracked structured `Γ` forms — there is nothing to compare beyond the shared
  letter.
- **Status**: `named, not defined`.

## `𝓘_t` — Inquiry State (new, internally significant within this segment's own Thread 3)

- **Birth**: Batch-2/3, Sher/Minică cluster — formalized progressively across ~8 files, converging
  (Batch-2 file 12) on `𝓘_t=⟨𝒬_t,≈_t,D^{cand}_t,𝒮^{source}_t,𝒜_t⟩`.
- **Central architectural claim of this whole sub-thread**: `K_t≠𝓘_t` — Knowledge State and Inquiry
  State are separate, orthogonal objects; `ZoomIn` transforms `𝓘_t`, not `K_t` directly.
- **No relationship to the F4 chain's own tracked objects tested or claimed anywhere** — this is a
  purely internal architectural distinction inside Thread 3, not offered as, or compared against,
  any `EC`/`r`/`Sat` formulation.
- **Status**: `[EXT][PROP]`, not Theory v1.2, self-disclosed throughout as research, not canon.

## `Zero_{T,Π,I,C,R}(S;D)` — generalized preservation-relative Zero (ZeroLens branch)

- **Birth/refinement**: Batch-1 files 1–6 (typed distinction: algebraic zero / representational
  omission / preservation-relative Zero) and Batch-3 files 12–13 (executed generalization to
  group-level `Zero(S,D)`, empirically shown non-equivalent to element-level `Zero(x,D)` in both
  directions).
- **Confirmed, across every file in the batch that discusses it, to be the already-known "ZeroLens"
  branch** this reconstruction has encountered before (distinct from any `Zero(K,EC)` construction
  tied to `Sat`) — not a new lineage, an extension of an existing one. No `TheoryState` opened as
  "new"; recorded here only to confirm the identification and note the genuine new empirical content
  (the group-vs-individual non-equivalence result, 1,200 cases/family, `research/knowledgeos-sim/`-
  external to this reconstruction's own prior tracking of Zero).

## Objects considered and explicitly NOT opened as TheoryStates

`ActionRationale`/`AR_t`/`W_t` (Warrant) chain, the five-way epistemic-value separation, the GoF
pattern-vs-primitive falsification methodology — all extensively developed across Thread 3, all
self-disclosed `[EXT][PROP]`/`[PROP][OPEN]` throughout, none engaging the F4 chain by name or by
structural echo strong enough to warrant a homonym flag. Recorded in §01's impact map; not elevated
to individual `TheoryState` entries here, consistent with this reconstruction's own scope (the F4
`Sat`/`EC`/`r`/`Γ`/`Det_r`/`EvalReq` family and directly-adjacent homonym risks — not a general
theory-evolution history of every external lens in the corpus).

# Phase 5J — Open Questions, With the Four Categories Kept Explicitly Distinct (per the authorization's §22)

## Reconstruction gaps (this reconstruction could still investigate)

- Whether any document *outside* the set now read (Step 049, Step 170, Step 272A, Step 272B, D285-1,
  D285-6, D285-7, the 3 executable scripts) addresses the Assertion conflict — not exhaustively ruled
  out, since the corpus is large and this phase's search, while targeted and thorough within its named
  scope, was not a full corpus-wide census for this specific question.
- The full content of "step-049"'s own "C-022 in `claim-registry`" citation — `claim-registry` itself
  was never located as a standalone file (Phase 5H, restated).

## Source-research gaps (the corpus itself lacks the evidence, confirmed by this phase's own reading)

- **A reconciling `Assertion` definition** — confirmed absent from the two documents (Step 272A/272B)
  most plausibly expected to supply one.
- **`Qualify`'s single, algorithmic definition** — confirmed absent across all 4 variants now found.
- **`State`'s own carrier definition** — confirmed absent (Phase 5H, unchanged).
- **Repository-history evidence for creation/modification order** — confirmed absent; the bulk-import
  commit structurally cannot supply it (`05`).

## Source contradictions (the corpus contains incompatible claims — confirmed, not merely gaps)

- D1/D3 vs. D2/D4's own Assertion field sets.
- `t285_reconcile.py`'s own T-A vs. T-C sections.
- `t285_reconcile.py` vs. D285-1's own current (post-revision) text.
- `Qualify`'s 4 incompatible arities.

## Governance gaps (the corpus lacks an authority decision needed to choose between competing definitions)

- **No ratification act exists for `Assertion`, `Qualify`, or K-2 as a whole** (`07`) — in contrast to
  K-1, which has one. This is the specific, positive governance gap this phase identifies: not merely
  "unresolved," but "resolvable only by a governance act that has not yet occurred."

## These four are not collapsed

Per the authorization's own explicit instruction: the *contradiction* findings (source contradictions)
are not restated as mere *absence* (source-research gaps), and the *governance* gap (an authority
decision is needed and has not been made) is not conflated with a *reconstruction* gap (more reading
would help) — each of the four categories above contains only the findings that actually belong to it.

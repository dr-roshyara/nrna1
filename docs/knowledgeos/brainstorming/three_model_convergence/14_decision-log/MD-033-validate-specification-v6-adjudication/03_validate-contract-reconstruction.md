# Validate Contract Reconstruction — Smallest Source-Grounded Object

Built from the three admissible files only, chaining source-stated facts exactly as MD-031 first
demonstrated is legitimate for this exact purpose (input carriers), extended here to every field the
authorization requires.

| Field | Status | Value / evidence |
|---|---|---|
| Atom | **CLOSED BY SOURCE** | `warrant-assessment` — `04` line 32 (vocabulary) and line 59 (contract row: `Validate` → `warrant-assessment`) |
| Output carrier | **CLOSED BY SOURCE** | `Verdict` — `03` line 49 (C10 → `Verdict`) and `06` line 41 (derivation table) |
| Input carriers | **CLOSED BY SOURCE** | `{Claim, Evidence}` or `{Hypothesis, Evidence}` — `06` line 41, chained through `04`'s atom assignment (line 59) exactly as MD-031 `03` established |
| Non-reducible responsibility | **CLOSED BY SOURCE** | "assign warrant given evidence + assumptions" — `04` line 59, direct quote |
| Corpus support rating | **CLOSED BY SOURCE** | `STRONG` — `04` line 59 |
| State effects | **CLOSED BY SOURCE (negative)** | "none, except `Revise`" — `04` lines 44–45, "Common to all" contract; since `Validate ≠ Revise`, this directly establishes `Validate` has no state effects |
| Information effects | **PARTIALLY SPECIFIED** | "governed by the data processing inequality" — `04` lines 45–46, a general principle stated for all operators, not a `Validate`-specific quantitative bound; the cited "§ information_causal" is not itself an admitted, numbered document in this population and was not resolved |
| Dependencies | **PARTIALLY SPECIFIED** | "whatever the type rules demand" — `04` line 46, a pointer to the same derivation-rule mechanism already captured under "Input carriers" above, not additional information |
| Preconditions | **NOT SPECIFIED BY SOURCE** | zero hits, `02`'s exhaustive census |
| Postconditions (beyond the output-carrier fact) | **NOT SPECIFIED BY SOURCE** | zero hits, `02` |
| Failure/error/rejection semantics | **NOT SPECIFIED BY SOURCE** | zero hits, `02` |
| Determinism / non-determinism | **NOT SPECIFIED BY SOURCE** | zero hits, `02` |
| Domain exhaustiveness (is `{Claim,Evidence}`/`{Hypothesis,Evidence}` the *only* admissible input, or could a third rule exist that the admissible sources simply don't mention) | **NOT TESTABLE FROM CURRENT ADMISSIBLE EVIDENCE** | the admissible population states the rule as given (no hedge, no "at least," no "for example") but this study does not infer *exhaustiveness* as a further, stronger claim beyond what is literally written — see `06` for why this specific caution matters for the V6 question |

## What this table shows as a whole

Every field concerning *what `Validate` does when it succeeds* (its atom, its inputs, its output, its
non-reducible responsibility, and — negatively — its lack of state effects) is `CLOSED BY SOURCE`.
Every field concerning *what happens when it does not, or under what conditions it may not, succeed*
is `NOT SPECIFIED BY SOURCE`. This is not a uniform gap across the whole contract — it is a clean,
sharp split between the "happy path" (fully specified) and everything else (unspecified), which is
itself a useful, precise finding rather than a single blanket "insufficient."

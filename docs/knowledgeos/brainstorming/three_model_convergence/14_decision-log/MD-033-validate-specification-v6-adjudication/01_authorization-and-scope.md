# Authorization and Scope

## Pre-execution disagreement check

The prompt was sound; no blocking disagreement was found. One expectation was flagged before
execution and is confirmed, not contradicted, by this study's own findings: §4 instructs
"reconstruct V6 directly from admissible evidence," which presumes V6 is present in that evidence.
MD-031 had already found `06` never mentions `Validate` by name; this study extends that check to
all three admitted files and finds `V6` absent from every one of them (`04`). The prompt itself
anticipated this possible outcome by listing "V6 NOT ACTUALLY ESTABLISHED BY ADMISSIBLE EVIDENCE" as
one of its six required classification options — so this is a faithful execution of the prompt's own
design, not a deviation from it.

## Admissible evidence population (exactly three files, per MD-028-DQ-1 + MD-032)

1. `docs/knowledgeos/research/kernel-reduction/03-capability-model.md` (98 lines) — **read in full,
   cold**, for this study. Previously known to this reconstruction only through MD-029's/MD-030's own
   citations of specific rows (C10, C25) and through the executable `capabilities.py`'s own
   cross-reference — not read directly, in full, until this study.
2. `docs/knowledgeos/research/kernel-reduction/04-operator-contracts.md` (77 lines) — **read in full**
   for this study. A 20-line excerpt was read once before (MD-031's provenance check); the remaining
   57 lines are read here for the first time.
3. `docs/knowledgeos/research/kernel-reduction/06-composition-rules.md` (79 lines) — already read in
   full, cold, during MD-031, and quoted verbatim in that study's own `03_validate-narrative-
   contract.md`. **Not re-read from disk for this study** — treated as an already-settled, frozen
   prior finding, per this session's own discipline of not re-litigating a completed study's own
   direct-source work. Its content is used here exactly as MD-031 recorded it.

## What was explicitly NOT read as evidence

`12-randomized-results.md` (the file both `03` and `04` cite as "§12," where variant/robustness
results including V1/V4/V5 are presumably elaborated) — **not admitted, not read**. Its existence and
the fact that it is the cited location for variant detail is recorded (`04`) as a provenance/
navigation fact, not used as evidentiary content. `kr/carriers.py`, `variants.py`, or any other
executable-lane artifact — not re-read (MD-030's own characterization is treated as settled prior
work, referenced only as research-history context per the authorization's §3). `theory-v1.1-
simulation/`, any other file under `docs/knowledgeos/research/`, and `docs/knowledgeos/theory-
extraction/` — untouched.

## MD-029–032 consulted as provenance/research history only, per the authorization's own instruction

Re-read at the level of their own frozen final-status text (not re-executed, not re-derived) to
confirm what has and has not already been established, avoiding restating settled findings as new.

# Scope and Firewall — the Pre-Execution Exchange

## What happened

The user's MD-031 authorization prompt, as pasted, contained a §2 "FIREWALL — THREE_MODEL_CONVERGENCE"
instructing this study never to inspect, list, or acknowledge the existence of
`three_model_convergence/`, plus a §21 verification checklist referencing "P-07–P-40 artifacts" and
"K1–K11 cardinality." Both are the vocabulary of a *different* research track (the Lane-T/theory-
extraction/P-series line this session has been instructed, since its own start, never to inspect or
use — `docs/knowledgeos/theory-extraction/`), not this session's own. `three_model_convergence/` is
this session's own home directory — every MD-0xx artifact, including this one, lives there; a
firewall against inspecting it is incoherent as an instruction to this session, and the required
output path (`14_decision-log/MD-031-.../`) sits inside the very directory the firewall named.

This is the same shape of mismatch already encountered once before in this session (a mistaken
"P-38-flavored MD-028" prompt, corrected on the spot by the user with "ingore the last prompt...
this is the corret"). Before executing anything, this was flagged back to the user with the specific
evidence (the self-contradictory firewall, the mismatched §21 vocabulary, the user's own closing
commentary describing `three_model_convergence` as *someone else's* independent-check track), and a
concrete proposed resolution offered: execute MD-031's substance, drop the incoherent firewall, and
verify against this session's own MD-024–030/K-1/K-2 state instead of the P-series vocabulary.

## Resolution

The user confirmed: "I follow and accept this suggestion" — proceed with the adapted version. This
study therefore:

- **Does** treat `three_model_convergence/` as its own home directory (reads and writes to it
  normally, exactly as every prior MD-0xx study has).
- **Does not** inspect, cite, or depend on anything under `docs/knowledgeos/theory-extraction/` —
  that prohibition is this session's own standing rule, independent of the mismatched prompt, and is
  honored here without needing the (incoherent) §2 wording to enforce it.
- **Verifies** against this session's own governance state (MD-024–030 unchanged, `classification-
  register.tsv` unchanged, K-1/K-2/GA-038 untouched) rather than the mismatched "P-07–P-40"/
  "K1–K11 cardinality" checklist items, which name a different track's own artifacts.
- **Executes every other part of the original prompt as given** — §§1, 3–20, 22–23 (renumbered to
  this study's own artifact scheme) required no substantive change; they are sound, self-consistent
  instructions for a source-comparison audit and match exactly where MD-030 left off.

## Scope actually executed

- **Primary source**: `docs/knowledgeos/research/kernel-reduction/06-composition-rules.md` — read in
  full, cold, before re-consulting the executable code.
- **Comparison sources**: MD-030's own 12 artifacts and its underlying reads of `kr/carriers.py`
  (not re-read from scratch; MD-030's own characterization is treated as settled, per this
  programme's own frozen-artifact discipline); the two MD-028-admitted files
  (`03-capability-model.md`, `04-operator-contracts.md`).
- **No scope expansion**: no other file under `docs/knowledgeos/research/kernel-reduction/` was
  used as evidence beyond what the population census (`02`) records as read for context (the
  directory's own `00-INDEX.md`, if consulted, is noted there) — none was found necessary.
- **No admission, no composition test, no code execution, no Stage 07, no model selection** — all
  confirmed in `15`.

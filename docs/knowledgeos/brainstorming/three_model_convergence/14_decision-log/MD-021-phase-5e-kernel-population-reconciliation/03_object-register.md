# Phase 5E — Object Register (new candidates found in P2/P3)

20 documents were dispositioned `OBJECT-BEARING` (§`02`). Many belong to already-known objects
(Phase 5C's 13, Phase 5D's 6) or to each other within one research arc. This register lists the
**genuinely new, distinct constructs** this phase found, each with the 19-attribute taxonomy where
evidenced, `NOT EVIDENCED` elsewhere. None is merged with an existing object without a specific
equivalence test (`06`).

## NEW-OBJ-5E-01 through -07 — The K-1 through K-7 family (seq 1006, raw-source confirmed)

**Source**: `phase_measure_theory/knowledgeos_kernel/research/D285-1-STATE-ONTOLOGY-MATRIX.md` (seq
1006). This is the single most important document found in this phase — a definitive, raw-source
verified comparison table of **seven** distinct state/Kernel formulations, each with explicit
authority/ratification status:

| Label | Formulation | Source | Authority | Cardinality |
|---|---|---|---|---|
| K-1 | `K_t` = state over 8 primitives `{Entity, State, Event, Observation, Proposition, Relation, Policy, Action}` | `step-049`; C-022 in `claim-registry` | **RATIFIED** — "50 attack classes, no counterexample," computationally tested | 8 |
| K-2 | `K = (𝒜, ℛ)` | verification lane | **NOT RATIFIED** — 0 occurrences in any ratified artifact (GN-75) | 2 |
| K-3 | `K = (A, R, Σ, E_L)` | Step 272B/273 | research; self-attested authority, no ledger entry | 4 |
| K-4 | `K_t` 10-tuple | "…complete-mathematical-model" | non-authoritative; superseded by ~120 steps | 10 |
| K-5 | `K(t) = M(t) + iA(t)` (complex-number formulation) | ALT-09 | **REJECTED** | — |
| K-6 | `𝒦 = (K, H)` | provenance track | research | 2 layers |
| K-7 | `Zero(K_t) = Ω \ Represented(K_t)` | Zero lens | research; presupposes `Ω`, `D_t`, `Z_t` | — |

**This resolves Phase 5D's own open finding** (`07_k1-and-homonym-audit.md`, "seq 1008's own K-2/K-3/
K-6/K-7 siblings, named as an open coverage gap") — the siblings are not equal-status alternatives to
K-1; they are **explicitly non-ratified, rejected, or research-only candidates that were considered
and NOT adopted**, with K-1 uniquely carrying `RATIFIED` status. K-1 itself is consistent with — very
likely identical in content to — the "eight ratified primitives" state referenced throughout the
Gītā-lens thread (seq 2295 etc.) and plausibly the same object Phase 5C registered from seq 1008 as
K-1-B. **This equivalence (K-1 [seq 1006] ≡ K-1-B [seq 1008/Phase-5C]) is plausible but not
independently re-derived here at full attribute level** — flagged, not asserted.

**K-2 through K-7 are registered as five genuinely distinct, non-equivalent candidate objects**
(K-4's 10-tuple cardinality is later echoed, though not confirmed identical, by seq 0224's own
10-component `K=(S,E,C,U,P,R,T,A,V,D)` — see `06`). Object type: formal tuple (K-2, K-3, K-4, K-6),
complex-valued function (K-5), set-difference construction (K-7). Testedness: K-1 `RATIFIED`/tested;
K-2 `PROPOSED → UNTESTED-BUT-EXPLICITLY-NOT-RATIFIED`; K-3 `PROPOSED → UNTESTED, self-attested only`;
K-4 `PROPOSED → SUPERSEDED`; K-5 `TESTED → REJECTED`; K-6 `PROPOSED → UNTESTED`; K-7 `PROPOSED →
UNTESTED, presupposes undefined terms`.

## NEW-OBJ-5E-08 — Models A-F (seq 1007, raw-source partially confirmed)

**Source**: `D285-6-STATE-CANDIDATE-EVALUATION.md` (seq 1007). A **separate** six-model (A through F)
state-candidate comparison, distinct from the K-1..K-7 table above. Raw-source confirms at least
"Model F (separate products)" is explicitly refuted by inspection ("both mention `Relation` and
`Proposition`, so Model F is refuted"). **Full A-E content not raw-verified this phase** (time/scope
boundary — flagged as an open item for a future phase, not fabricated here). Object type: `NOT FULLY
EVIDENCED` pending further inspection; recorded as a known-to-exist, partially-evidenced comparison
set, not a specific tuple.

## NEW-OBJ-5E-09 — Knowledge Atom `KA=(p,d,v,c,e,t,pi)` (seq 2266)

A seven-field tuple explicitly proposed as "the smallest structural unit beneath 'sentence' and
beneath the eight ratified primitives" — i.e., a sub-primitive-level construct, not a competing Kernel
definition. Object type: formal tuple, distinct scope (finer-grained than any Kernel candidate).
Testedness: `PROPOSED → UNTESTED`.

## NEW-OBJ-5E-10 — Operator Contract `Op=⟨Name,Input,Pre,Transform,Post,Inv,Evidence⟩` (seq 2298)

A seven-field operator-contract schema, explicitly framed as "the thread's first general-purpose
schema" for defining Kernel operators — a distinct object *type* (operator schema, not state tuple).
Testedness: `PROPOSED → UNTESTED`.

## NEW-OBJ-5E-11 — Knowledge-Space foundation family (seq 2309→2315→2318, plus seq 0327/0340)

A **sequential-replacement family**, not five independent objects — each document explicitly replaces
its predecessor as the proposed mathematical foundation for the Knowledge Space 𝒦 (not directly "the
Kernel," but load-bearing for any Kernel state defined over it): vector space (implicit prior
position) → evolving-dimension-space `𝒦_∞` (seq 2309) → topological space `𝒦=(X,τ)` (seq 2315) →
measure/probability space `𝒦⊆(Ω,ℱ,ℙ)` (seq 2318). A parallel, earlier such replacement chain exists at
seq 0327 ("Knowledge Measure Theory v0.1: Projection as Measure") → seq 0340 ("Relational/Logical
Structure as the Core; Metric-Topology-Probability-Measure as Regimes") — **seq 0340's own title is
near-identical to seq 2330's** (the sole C2 file), already flagged as a candidate lineage lead in
Phase 5D's `06_kernel-family-and-provenance-closure.md`; this phase's finding **reinforces, not newly
establishes**, that lead (a second, independent instance of the same "Relational/Logical Structure as
Core" formulation recurring in the corpus). **Registered as one adjacent, evolving family — not
individually equivalence-tested against each other**, since each document already explicitly declares
itself a replacement of the prior one (a `SUPERSEDES` relationship, not a `POSSIBLE_RELATIONSHIP`).

## Summary

**11 new-candidate entries registered** (7 from the K-1..K-7 family + 1 partial from Models A-F + 3
independent new constructs). **0 merged with an existing object without a specific test.** The K-1
family finding (NEW-OBJ-5E-01 through -07) is this phase's single most consequential result — it
supplies the authoritative ratification-status data that both Phase 5C and Phase 5D lacked.

# MD-059 §07 — Final Judgment

## Note on the two prerequisite checks performed mid-phase

Before finalizing this judgment, a targeted primary-source extraction (`01a`) was performed against
M0043/M0132/M0125 directly, and cross-checked against an externally-authored analysis the user
supplied (dated today, explicitly citing this reconstruction's own MD-058 as its input — classified
**same-day, MD-058-consuming re-analysis, not prior/independent corpus evidence**, per this
reconstruction's own provenance discipline). Both converge on the same decision-gate answer
(**B — not fully instantiable**); the primary-source pass additionally sharpens the smallest-missing-
primitive finding (from "R_t closure" to "a decomposition-independent `Sat` body") and surfaces one
genuinely new corpus fact neither prior pass had (`[DEF-15]`, M0043 — a primary precedent for MD-058's
own derived relation shape). The judgment below reflects the sharpened, primary-source-grounded
picture.

## Success condition (§13 of the authorizing prompt)

**Condition B** (F4 obtains a partial semantic instantiation and the exact missing primitives are
isolated) — **MET**, precisely: `Obs_F4`/`Sat_F4` instantiated in form, blocked on `R_t`'s closure;
`Beh_F4`/`Trace_F4` isolated as UNAVAILABLE, blocked on the composition-rule choice.

**Condition C** (F4 cannot be instantiated without an ungrounded modelling choice, and that
obstruction is formally demonstrated) — **PARTIALLY MET**, for `Beh_F4`/`Trace_F4` specifically, not
for `Obs_F4`/`Sat_F4`.

**Condition D** (a genuine F3 ↔ F4 correspondence is established at some level of the adjudication
ladder) — **NOT MET** — the honest verdict is UNRESOLVED, with the obstruction (a type mismatch
between atom-space and requirement-space) precisely named, which is itself the useful result, per
the prompt's own instruction that equivalence is not the success criterion.

**Reported as the honest conjunction B ∧ (partial C) — not forced to one letter**, consistent with
this reconstruction's own standing discipline (MD-058 §06) of reporting the true shape of a mixed
result rather than picking the best-sounding single label.

## Required final table

| Question | Result | Epistemic status |
|---|---|---|
| F4 semantic instantiation possible? | **Partial** — `Obs_F4`/`Sat_F4` yes (in form); `Beh_F4`/`Trace_F4` no | MATHEMATICALLY DERIVED (form) / UNAVAILABLE |
| `Obs_F4` available? | **In form (corpus-convergent with `[DEF-15]`); not computable** | MATHEMATICALLY DERIVED / CORPUS-DERIVED (form), blocked (`Sat`'s body open) |
| `Beh_F4` available? | **No** | UNAVAILABLE — blocked on composition-rule choice |
| `Trace_F4` available? | **No** | UNAVAILABLE — same root cause |
| `Sat_F4` available? | **Yes as a frozen shape** (`Δ_t={r∈Req(EC_t):¬Sat(K_t,r)}`, M0132 ratifying M0043/M0047); **no, as a computable body** — the corpus's own text (M0132) states this requires `K_t`'s unresolved component semantics | CORPUS FACT (shape) / OPEN (body — source-stated, not inferred) |
| Representation-independent? | **Conditionally — 4 of 8 tests pass, but only under a disclosed, non-corpus-sourced assumption (`Sat` evaluated as a black box); 3 of 8 undecidable (no two agreed `K_t` encodings exist to test); 1 flagged near-circular** | HYPOTHESIS (conditional, downgraded from the phase's own first-pass framing) / UNDECIDABLE FROM CURRENT CORPUS (partial) |
| F3 ↔ F4 comparable? | **No — UNRESOLVED, precisely: a type mismatch (atoms vs. requirements), no corpus-given bridge** | UNRESOLVED (adjudication-ladder default, with a named reason) |
| MinKer improved? | **Sharpened, not resolved** — a second, independent instance of the open-registry blocker found (`R_t`, alongside `𝒪_K`); quotienting still not meaningful across F3/F4 | NECESSARY CONSEQUENCE (of the above) |
| GA-001 changed? | **UNCHANGED** — still no comparable pair; the reason is now sharper (type mismatch, not mere unavailability) | — |
| GA-038 changed? | **UNCHANGED** — corroborated a second time, independently, by a different candidate family's own registry-closure gap | — |

## MD-059 COMPLETE / HARD STOP

No MD-060 opened by this completion. Two named, unauthorized next actions (not silently chosen),
sharpened by the primary-source check: **(a)** a research act reconciling `K_t`'s own 9+ mutually
unresolved variants (UE-1/UE-2) enough to supply a decomposition-independent body for `Sat(K_t,r)` —
the corpus's own named blocker (M0132), not an invention of this reconstruction; **(b)** a research
act building the missing atoms↔requirements bridge between F3 and F4 — the precisely-named smallest
input that would make the F3↔F4 comparison well-posed for the first time in this reconstruction.
Neither is a governance-act-only item in the way `𝒪`/`𝒪_K`'s `N-4` was for the general MinKer
apparatus (MD-057/058) — both are named research obstructions specific to F4.

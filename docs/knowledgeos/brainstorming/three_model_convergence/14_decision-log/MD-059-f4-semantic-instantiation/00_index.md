# MD-059 — Controlled Semantic Instantiation of F4 (Model B `K_t`/`Δ_t`)

**Date**: 2026-09-09. **Status: EXECUTED. HARD STOP per the authorizing prompt's own §14/§16.**

## 1 · Objective

Determine whether the corpus contains enough formally grounded F4 material to construct a genuine
`Obs`/`Beh` instantiation without inventing a decomposition or importing semantics from another
model, and — if possible — compare F3 ↔ F4 under MD-058's derived relation `K₁≈_{Q,𝒪}K₂ ⟺
Obs_{Q,𝒪}(K₁)=Obs_{Q,𝒪}(K₂)`.

## 2 · Correction to MD-058's own language, adopted here (MD-058's own text NOT edited)

The user corrected MD-058's claim that `Obs` is "the only candidate mathematically satisfying the
requirements." The precise statement, adopted going forward: **given the currently established
requirements AND the currently available corpus-grounded semantic primitives, `Obs_{Q,𝒪}`-equality
is the only primitive presently constructible without an additional modelling choice** — not a
mathematical uniqueness proof. This distinction is honored throughout this phase.

## 3 · Method note (the same disagreement/clarification as MD-058, reapplied — accepted by the user
last time as a standing method note, restated briefly rather than re-argued)

The F4 Semantic Evidence Ledger (`01`) is built from Model B's own already-synthesized Phase-2
register (`03_model-b_mathematical/02_concept-register.md`, `03_contradictions-and-open-questions.md`
— themselves "already-admissible F4/Model-B material," exactly what the authorizing prompt's §3
asks for), spot-checked against the underlying `.md`/`.yaml` source only where a claim is
consequential, rather than a fresh re-sweep of the eight named directories. This is presented as
already-agreed method, not a fresh disagreement to be settled.

## 4 · Prerequisite primary-source check (performed mid-phase, before finalizing — see `01a`)

Before finalizing the analysis below (drafted first from Model B's Phase-2 *register*, a secondary
synthesis), the user asked for a targeted primary-source recovery pass against M0043/M0047/M0132/
M0125/M0126 directly, plus a cross-check against an externally-authored analysis the user supplied
(a file dropped into the corpus directory today, dated after and explicitly citing this
reconstruction's own MD-058 — its provenance is therefore **not** prior/independent corpus evidence;
it is a same-day, MD-058-consuming re-analysis, treated as a cross-check, not a source). Both passes
converge on **decision gate B — F4 still cannot be fully instantiated** — but the primary-source
pass sharpens *why*, and surfaces one genuinely new finding neither the register nor the external
cross-check had: `[DEF-15]` (M0043, primary, 2026-09-02) is a **named, primary corpus definition** of
exactly the shape MD-058 derived independently (`r₁≡_sem r₂ ⟺ B_{r₁}=B_{r₂}`) — a correction to
MD-058's own provenance framing, recorded in `01a`, MD-058's own text not edited. Full detail: `01a`.

## 5 · Central result (revised after the primary-source check)

**F4 achieves a genuine PARTIAL instantiation — the first real gain in the F1–F6/K0 population beyond
F3, but it does not reach comparability with F3, and the blocking primitive is sharper than an
initial register-level pass found.** `Δ_t={r∈R_t:Sat(K_t,r)=0}` and `Sat`'s own shape are corpus-
native and frozen (M0132, ratifying M0043/M0047) — but the corpus's **own primary text states
directly** that `Sat(K_t,r)`'s computation requires `K_t`'s own component semantics, and only one
component (`Σ_t=(A,S,R,V,C)`, M0125) has ever been given a concrete typed definition, within only one
of 9+ mutually unreconciled `K_t` variants (UE-1/UE-2) never shown consistent with each other. **The
smallest missing primitive is therefore not "close `R_t`" (an earlier, less precise framing) but "a
decomposition-independent body for `Sat`"** — which the corpus itself, not this reconstruction,
identifies as blocked on the unresolved `K_t`-variant family. `Beh_F4`/`Trace_F4` via the corpus-
native transition (`K_{t+1}=δ(K_t,e_t)`, M0125; `Orgasm_t`, §K of the register) are **BLOCKED**, not
by missing material, but by an *unresolved choice among candidate composition/transition rules*
(`P-12`/`P-13`, "survive testing, not established"). **F3 ↔ F4 comparison: UNRESOLVED**, for a
precisely named reason distinct from mere unavailability — F3's `Obs` outputs *reached atoms*, F4's
`Obs` outputs *satisfied requirements*; no corpus document bridges these two output types.

## 6 · GA-001/GA-038 impact

**Both UNCHANGED.** GA-001: still no comparable pair (F3↔F4 now attempted, genuinely blocked by a
type mismatch, not merely by absence — a sharper negative than MD-058's own "5/6 unavailable"). GA-038:
still no canonicalization criterion; `Sat`'s own decomposition-independent body is now a second,
independently-found instance of the same shape of blocker as `𝒪_K`/R10 — a different candidate
family's own registry/component-resolution gap, reinforcing, not resolving.

## 7 · Artifact map

`01_f4-semantic-evidence-ledger.md` · `01a_targeted-primary-source-extraction.md` ·
`02_f4-semantic-construction.md` · `03_f4-representation-independence-test.md` ·
`04_f3-f4-comparison.md` · `05_minker-consequence.md` · `06_adversarial-hypotheses.md` ·
`07_final-judgment.md`.

## 8 · Explicit non-scope

No F1/F3/F4/F5/F6 selection. No GA-001/GA-038 resolution. No CLOSURE-4/`≡_sem` adoption. No invented
`Obs`/`Beh`/`Trace`/`Sat` semantics beyond what is corpus-native. No closure of `𝒪`/`𝒪_K`/`R_t` by
assumption. No Stage 07. No canonical theory. MD-024–058 unmodified. MD-050 not reopened. Lane-T
`K3`/`Ω`/`T-K1`/`T-K2` untouched.

# MD-058 §01 — Requirement Ledger `R`

Each `Rᵢ` carries: statement · status tag · provenance (source MD, tracing to corpus evidence).

| ID | Statement | Status | Provenance |
|---|---|---|---|
| **R1** | Minimality claims are meaningless unless stated relative to a fixed representation; comparing minimality across representations without first fixing or abstracting representation is not well-posed. | **CORPUS FACT** | Model B Phase 2 concept register §F/§P: FR-001 (non-transitivity of a naive induced equivalence), Hilbert-space rejection, "minimality is representation-relative... solutions to two different optimization problems," `Reachability ≠ Epistemic adequacy` — executed, adversarially tested (MD-024/045/050 series; corroborated again in MD-057 Cluster 1 §7). |
| **R2** | A semantic-equivalence definition must not depend on the arbitrary naming or decomposition of a candidate's internal capabilities/operators — a definition that does is circular. | **CORPUS FACT (a stated rule, not yet a satisfied one)** | The `KR-KERNEL-MINIMALITY-2026-09` chain's own explicit instruction, quoted directly in MD-045: *"If any definition depends on the arbitrary naming or decomposition of the candidate capabilities, stop and expose the circularity rather than proceeding."* Corroborated by MD-046's own finding of zero corpus-grounded decomposition-independent capability-identity criterion. |
| **R3** | Congruence: `K₁≡K₂ ⇒ o(K₁)≡o(K₂)` for every admissible operation `o` — stated as "the critical mathematical requirement," with an explicit warning that failing it makes the relation too coarse. | **CORPUS FACT** | MD-057 Cluster 2 (`REFINED-STEP-290` §6, citing source `258.9`), confirmed independent of the `≡`/`≈` conflict itself. |
| **R4** | Any relation used as `≡_sem` inside `MinKer`'s own formula must actually BE an equivalence relation (reflexive/symmetric/transitive), since `MinKer` quotients `𝔎_adm` by it. | **MATHEMATICALLY NECESSARY** | Definitional requirement of `MinKer(𝔠_KOS)=Min_⪯sem{K∈𝔎_adm\|K⊨𝔠_KOS}` itself (MD-043/049) — quotienting presupposes an equivalence relation; not separately stated by any corpus source, but forced by the formula's own well-formedness. |
| **R5** | The corpus intends `≡` (semantic identity) to be a *stronger*, separately-typed relation than `≈` (observational equivalence) — not interchangeable, even where their current candidate formulas coincide. | **CORPUS FACT** | MD-057 Cluster 2, `N-1A` — 5 loci, 2 notations, source-stated non-interchangeability (`246`, `261.1`, `261.20`, `261.25`). |
| **R6** | No corpus-ratified formula currently fills `≡`'s own content; the one execution-tested candidate is self-labeled "a candidate, not a completed theorem," and is itself later re-typed as `≈_obs` rather than `≡_sem`. | **CORPUS FACT** | MD-057, both clusters. |
| **R7** | A candidate reformulation must not lose or fabricate externally observable capability relative to its original ("no capability laundering"). | **CORPUS-DERIVED** | MD-045's own DDD conservation principle, derived from the `𝔎_adm`/`𝔎_sat` split and counterfactual capability-removal construction (`𝔎_adm^{-c}`). |
| **R8** | A satisfaction relation `K⊨C_KOS` must exist and be computable for `MinKer`'s own formula to typecheck against a real constraint set. | **MATHEMATICALLY NECESSARY, CORPUS STATUS: NOT SPECIFIED** | `MinKer`'s own formula (MD-043/049); `𝔠_KOS`/`⊨` confirmed `NECESSARY BUT UNSPECIFIED` throughout MD-043–050, unchanged by MD-057. |
| **R9** | Among the symbols this apparatus depends on (`≡`, `𝒪`, `𝒯`, `𝒪_K`, congruence), `{≡}` is the unique minimal (size-1) cut breaking every dependency cycle in the corpus's own stated dependency graph — i.e. resolving `≡` first has maximal leverage. | **CORPUS FACT, EXECUTED** | MD-057 Cluster 2 (`REFINED-STEP-291` §7, 29-node/44-edge graph, `exec/t291_omega_tau_audit.py`, cited). |
| **R10** | An operation/observation registry (`𝒪`/`𝒯`/`𝒪_K`) must have a closed, mandatory-membership rule before any operation-preserving congruence (R3) or any observation-parameterized relation (R6's candidate) can be fully evaluated — its absence is a named, unresolved governance item (`N-4`), not a search gap. | **CORPUS FACT** | MD-057 Cluster 2 (`REFINED-STEP-291` §5/§15/§17); independently corroborated (common-provenance-lineage, not independent replication — see MD-057 §00) by MD-056's own operation-registry commission finding (registry membership NOT ESTABLISHED, blocked on P-1…P-11). |

**Explicitly not included as a requirement**: any statement resembling "the kernel is the set of 8/13
primitives" — this is a *representation* claim, precisely the kind R1/R2 forbid taking as a starting
premise. Excluded deliberately, not overlooked.

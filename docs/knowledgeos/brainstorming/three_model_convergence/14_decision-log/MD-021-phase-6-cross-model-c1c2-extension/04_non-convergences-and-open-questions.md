# Phase 6 — Non-Convergences and Open Questions (mandatory section)

## 1. Model-specific structures with no demonstrated counterpart

| Target | Missing from | What exists on the other side |
|---|---|---|
| Operator/reduction/composition/closure (Model B's own formal apparatus) | C1 and C2 | B §B/§N/§F: ablation-tested operator sets, composition-rule exclusions, closure-sense catalog — C1/C2's own constructs are exclusively aggregate/tuple-shaped, never operator-shaped |
| Sustained, explicit "knowledge"-definition content | A and B | C1 §D–H, C2 §M: repeated explicit definitions of "knowledge" as a term (see `02` Row 8) |
| A tested/executed representation-adequacy apparatus | C1 | B §F/§I (tested); C2 §M has a *proposed*, untested analogue (regimes/Shani) |
| Formal persistent-identity (Ātma-style) construct | B | C1 §G (untested, internally unreconciled); A's own Gita-sourced material discusses the same vocabulary without formalizing it |
| Measurement-governance gate (Roberts's Representation+Uniqueness+Meaningfulness) | A, B, C1 | C2 §M only, n=1, no comparison possible |

**Each of these is a genuine gap, not a defect.** C1/C2 were built from a different source population
(engineering/DDD proposal documents and one epistemic synthesis) than A (philosophical/narrative) or B
(mathematics/statistics research) — independent reconstruction producing non-overlapping formal
apparatus is the expected outcome, not a shortfall.

## 2. Rejected or low-confidence correspondence proposals

- **Kernel** (Rows 1–3): PARTIAL CORRESPONDENCE at the categorical level only; the one specific,
  naming-based candidate correspondence (A's 0219 vs. C1's K-1) was checked directly against raw
  source and does **not** hold.
- **State** (Rows 4–6): PARTIAL CORRESPONDENCE at the notation/process level for C1↔B and C1↔A;
  UNRESOLVED at the structural level for all pairs; C2 provides no evidence either way given n=1.
- **Representation** (Row 7): FUNCTIONAL ANALOGY only, thinly evidenced (n=1 on the C2 side).
- **Persistent identity/Ātma** (Row 9): UNRESOLVED, low ceiling — shared vocabulary, no formal
  apparatus to compare on A's side, unreconciled even within C1's own evidence.

## 3. Methodological parallels explicitly NOT domain correspondences

**Repeated "Kernel" category-confusion diagnosis.** C1's own internal proliferation (8 candidates,
1 tested) and Model B's own internal diagnosis (the four-way conflation, §L) independently notice the
same failure mode — repeated redefinition of "Kernel" relative to adjacent concepts (Knowledge Product/
Evidence/Governance in C1's case; Knowledge-Space/Epistemic-State/History in B's). This is a shared
property of how ambitious, under-constrained architecture proposals tend to fail, likely reflecting a
general research hazard rather than a shared domain theory. Recorded here, excluded from
`02_extended-correspondence-matrix.md`, so it is never cited as evidence of a domain-level
correspondence.

**Independently-executed self-correction discipline, now a third instance.** Phase 3 already recorded
this parallel between A's 0808 and B's M0045 (self-audit catching and correcting overreach the same
research period it was produced). C1's own `phase_measure_theory/` arc shows the identical behavior at
least five times (seq 0470/0479/0480/0481/0486, each independently catching and repairing a circularity
in the immediately preceding file). A third independent instance of the same research-discipline
pattern — still a fact about how this whole reconstruction's underlying corpus was produced, not about
any model's theory content.

## 4. New cross-model observations (labeled per the non-negotiable requirement)

> **PROPOSED CROSS-MODEL HYPOTHESIS** (not part of any original model; not independently supported by
> any single model's own source content; requires further, separately-authorized testing before any
> stronger status is considered):
>
> **The `K_t`/`Δ_t` proliferation-and-self-repair pattern Phase 3 first noticed between Model A and
> Model B (its own PROPOSED CROSS-MODEL HYPOTHESIS, `05_cross-model/04_non-convergences-and-open-
> questions.md`) recurs a third time, independently, in Model C1's own `phase_measure_theory/` arc —
> and this third instance uses the identical variable names (`K_t`, `Δ_t`) as Model B's own math-lane
> thread, with a verified absence of any citation link between the two lineages.** This strengthens,
> without proving, the underlying hypothesis that under-constrained attempts to formalize "epistemic
> state" tend to proliferate non-convergent tuple variants largely independent of which corpus lineage,
> content domain, or even which specific numbering scheme produces them. It does **not** establish that
> the two `K_t` threads are the same formal object — no component-level mapping has been attempted —
> only that the *instability signature itself* recurs with matching notation and zero citation, which
> is a stronger form of independent replication than Phase 3's own original A↔B finding (which had no
> shared vocabulary at all).

> **PROPOSED CROSS-MODEL HYPOTHESIS:**
>
> **A guarding principle against representation-induced semantic corruption — "a representation/regime
> must not silently redefine or destroy what it represents" — appears independently in Model B's own
> tested Representation Adequacy Principle and in Model C2's own single, untested Shani-invariant.**
> Given C2's evidence population is exactly one file, this is offered as a hypothesis worth testing
> against a larger future C2 population (should one ever be found, per the standing guardrail that
> resemblance must never itself expand C2's membership), not as an established cross-model finding.

No other candidate abstraction was found with sufficient evidentiary support to warrant even a
hypothesis-level label. In particular: **no evidence in this adjudication pass supports a "universal
kernel" spanning all four models** — the Kernel rows (1–3) settled at PARTIAL CORRESPONDENCE
(categorical) / UNRESOLVED (specific), exactly mirroring Phase 3's own A↔B Kernel finding, now
confirmed a third and fourth time rather than overturned.

## 5. The C2 evidence-scarcity constraint, recorded without predicting outcomes

Model C2's population is exactly one file, corpus-wide, unchanged since Phase 4. This constrains what
this phase could adjudicate — most C2 rows in `02_extended-correspondence-matrix.md` read UNRESOLVED or
carry low confidence **because that is what the available evidence independently supports for each
specific target tested**, not because scarcity was assumed in advance to produce that result. Two C2
rows (Representation/Row 7, Kernel-categorical/Row 3) reached a positive classification (FUNCTIONAL
ANALOGY, PARTIAL CORRESPONDENCE) despite n=1 — demonstrating the population size did not mechanically
determine the outcome in either direction.

## Naming-collision finding, carried forward without adjudication

The label "K-1" is now confirmed used for **at least two, and on this evidence possibly three,**
unrelated structures across this corpus's own history: Model C1's `KnowledgeAggregate+ConflictRecord+
VerificationPort` (seq 0165/0167, `kernel/`); the Phase 5A–5N sub-sequence's own 8-primitive tuple
`𝒦=(E,S,T,O,P,R,Π,A)` (seq 0630, `phase_measure_theory/`) — explicitly not touched, reopened, or
compared here, per this phase's own boundary; and Model A's own per-file synthesis at seq 0219, which
appears to conflate the two without a raw-source basis for either. **This is recorded as an open
question for a future, separately-authorized phase** — not resolved, not adjudicated, and not used
anywhere in this phase's own matrix beyond the single raw-source check already reported (Row 1).

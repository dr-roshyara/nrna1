# Authorization and Scope

## What MD-028-DQ-1 permits

Use of exactly two files — `04-operator-contracts.md`, `03-capability-model.md` — for
"specification-sufficiency analysis arising from MD-024/MD-025." This study exercises exactly that
permission, for exactly the purpose named, on exactly one diagnostic pair.

## What remains explicitly out of scope

No other `kernel-reduction/` file (in particular, not `06-composition-rules.md`, which would supply
the missing derivation table `02` needs but was not admitted). No `theory-v1.1-simulation/`. No
P-series. No model selection. No common-Kernel claim. No GA-038 reopening. No K-1/K-2. No Stage 07.

## The single question

Does the newly available B `Validate` contract (`04`, `03`) make MD-024's own Pair 1 — B `Validate` ↔
C1 P-3 — scientifically testable, and if so, what does the evidence show?

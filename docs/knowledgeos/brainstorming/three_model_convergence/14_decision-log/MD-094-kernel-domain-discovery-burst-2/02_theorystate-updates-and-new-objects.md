# MD-094 §02 — TheoryState Updates, Kernel Identity Ledger (extended), New Objects

## The "K-1 structure" candidate — the phase's own central finding

- **Birth**: File 44 (batch A), `wave-1-kernel-extent-versus-contents-adjudication-status`, 2026-08-24
  02:06. Defined in one sentence, without derivation: *"That is the K-1 structure: KnowledgeAggregate +
  ConflictRecord, Verification Port as sole inbound gate."*
- **Nature of the document it appears in**: not an adjudication despite its title — a live status/
  handoff note, explicitly incomplete, deferring its own ruling, containing an "extent vs. contents"
  distinction applied to six otherwise-undefined governance items (`C-14`–`C-18`, `F-CM-1a`) and naming
  forward dependencies (`F-CM-1b`, `C-3`, `C-10`, `C-4`) not defined anywhere in this 39-file window.
- **Identity relative to this reconstruction's own governance-frozen "K-1/K2" track** (cited in every
  phase closure since MD-067 as permanently untouched): `IDENTITY UNRESOLVED — INSUFFICIENT EVIDENCE`.
  No explicit predecessor/successor, refinement, or formal-equivalence statement connects them within
  this batch. The match is strong on symbol and rough conceptual shape (a minimal, gate-protected
  aggregate structure) but this is exactly the kind of resemblance the master mission's own §3/§9
  explicitly warns against treating as identity. **Recorded as the single highest-priority candidate
  cross-lane connection this reconstruction has yet found for its own governance-frozen track's actual
  origin — a named target for a future adjudication-only phase (in the style of MD-091), not resolved
  here.**
- **Corpus-boundary observation**: this file's own vocabulary (`C-14`–`C-18`, `F-CM-1a/1b`, "the second
  corpus... sealed") runs as a wholly separate track from the surrounding book-extraction files, with
  zero cross-reference in either direction within this window — confirming a genuine parallel research
  lane operating inside `kernel/` itself, distinct from the multi-lens philosophical-brainstorming lane
  MD-093/094 have otherwise been reading.

## Kernel Identity Ledger — extension (new entries this phase, none merged with MD-093's sixteen)

| # | Sense | Birth (this batch) | Note |
|---|---|---|---|
| 17 | Kernel-D: "eventual coherent core" (lowercase, generic) | File 50 (batch B), second pass | Register/capitalization shift from the protective proper-noun usage found elsewhere — itself evidence of possible sense-drift, not confirmed |
| 18 | Kernel-E: "Layer 1 — Knowledge Kernel" (`Claim`/`Evidence`/`Context`/`Authority`/`Perspective`/`Justification`/`Lineage`/`Transformation`/`Assessment`/`Withdrawal`) | File 51 (batch B) | First genuinely content-bearing candidate in this batch — a real ten-field list, not a bare reference |
| 19 | Kernel-F: protected boundary object in the Human-AI-collaboration boundary claim | File 52 (batch B) | "Do not put Human-AI Interaction into the KnowledgeOS kernel" — closing ownership statement: KnowledgeOS owns knowledge authority; Retrieval owns access; AI Platform owns execution; Human-AI Collaboration owns the human/machine boundary |
| 20 | "K-1 structure" (`KnowledgeAggregate`+`ConflictRecord`+Verification Port) | File 44 (batch A) | See above — the phase's own central finding |

All twenty entries (sixteen from MD-093, four from this phase) remain mutually `IDENTITY UNRESOLVED`.
No merge performed anywhere.

## New `TheoryState` entries opened this phase

### `KnowledgeAssertion` vs. `KnowledgeClaim` — a partially-propagated correction
- File 50 (batch B) explicitly corrects its own prior terminology: *"Not `KnowledgeClaim` but:
  `KnowledgeAssertion{proposition, referent, context, situation, perspective, role, authority_relation,
  epistemic_basis, evidence, temporal_scope, practical_scope, lineage}`"* — reasoning that a bare claim
  record cannot carry context/perspective/referent.
- **The correction is not fully propagated**: File 51 (the very next file), in its own "Layer 1 —
  Knowledge Kernel," still lists "Claim," and continues using both `KnowledgeClaim` and `Claim`
  interchangeably alongside the newer framing. Recorded as genuine, still-open terminological drift
  within the corpus itself — not resolved by this reconstruction, not silently merged.

### `TheoryEvaluation`, `KnowledgeBridge`, `EquivalenceClaim`, `Invariant` (batch A/B candidate objects)
- A cluster of well-typed (field-list) but never formally defined candidate objects, born across the
  Williamson and Chalmers extractions, explicitly self-labeled "research vocabulary, not the Kernel."
  Per the mathematical-closure discipline (master mission §6): all are classified `NAMED`/`well-TYPED`
  at most — none reaches `DEFINED`, `COMPUTABLE`, `EXECUTED`, or `VALIDATED`. Recorded for completeness,
  not opened as individually tracked trajectories (they are candidate vocabulary, explicitly non-
  promoted, per the source's own repeated self-restraint statements).

## Objects considered and explicitly not opened as new trajectories

The twelve-technique statistical taxonomy, the causal-inference primitive family (`CausalQuestion`,
`CausalGraph`, `Estimand`, etc.), and the Evidence-Acquisition-as-its-own-domain apparatus (batch C) are
recorded in the impact map (§01) as a confirmed independent research fork, not opened as individual
KnowledgeOS `TheoryState`s — every object in that fork is explicitly self-labeled "candidate," "not yet
certified," or "research hypothesis, not architecture," and the fork itself never engages the Kernel
question this reconstruction is specifically tracking.

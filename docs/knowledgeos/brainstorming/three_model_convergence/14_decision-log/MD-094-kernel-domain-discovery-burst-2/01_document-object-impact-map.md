# MD-094 §01 — Document→Object Impact Map (20 files, `kernel/` Wave 1, 01:08–03:36)

## Headline finding: a candidate origin for the "K-1" governance label

File 44 of the burst (batch A, `wave-1-kernel-extent-versus-contents-adjudication-status`) introduces a
**"K-1 structure"**: `KnowledgeAggregate` + `ConflictRecord`, with the Verification Port as the sole
inbound admission gate. This is the exact token `K-1` that this reconstruction has treated, in every
phase closure since MD-067, as an already-frozen, permanently-untouched governance track ("K-1/K2
untouched") — without this reconstruction ever having read its own origin point. **No explicit identity
statement connects this file's `K-1` to the governance-frozen `K-1` this reconstruction has cited
throughout its own history** — per the master mission's own identity-ladder discipline, this is recorded
as `IDENTITY UNRESOLVED — INSUFFICIENT EVIDENCE`, a strong candidate origin, not an established one.

The same file confirms it is **not an adjudication despite its own title**: it is a live status/handoff
note that explicitly defers its own ruling to an unnamed addressee ("Waiting... until you've ruled on
Wave 1"), contains two apparent source-text corruption artifacts, and names a separate, never-elsewhere-
referenced governance-item vocabulary (`C-14`–`C-18`, `F-CM-1a`/`1b`, "Wisdom," "the second corpus...
sealed"). This entire apparatus runs as a genuinely separate track from the book-extraction files
surrounding it in time, with zero cross-reference in either direction within this 39-file window.

## Two divergent sub-threads within this Wave

**Batch A + most of B (files 40–58)** continue the book-extraction/Kernel-discovery thread from MD-093:
Logitica, Philosophy of Complex Systems, Chalmers' *Conscious Mind* (with an explicit protocol
self-correction — see §03), Williamson's *Modal Logic as Metaphysics*, Nyāya-Vaiśeṣika, *Architecture
Patterns with Python* (the first purely technical/software-pattern source in the whole burst), RAG-
boundary architecture, Human-AI-collaboration boundary-drawing, and Freedman's statistical-inference
discipline.

**Batch C (files 54–59)** is a genuine fork: **zero occurrences of "Kernel" as a KnowledgeOS concept**
anywhere — the word appears only four times, all as the statistical term "Kernel density estimation."
This sub-thread instead develops statistical techniques, causal inference, and "evidence acquisition is
its own domain" as an independent research line, converging on its own Zero-Lens/Chinese-lens synthesis
with no reference back to the Kernel-discovery question.

## Kernel-identity proliferation continues

Batch A alone surfaces three further unreconciled Kernel candidate-content lists (from the Complex-
Systems file, the first Chalmers pass, and its corrected rerun) — none agreeing on membership (Status/
Context vs. Claim/Scope/State are added and dropped without reconciliation). Batch B's own extraction
agent independently identified **six further distinct textual occasions of "Kernel"** (labeled Kernel-A
through Kernel-F in its own report), none explicitly identified with one another by the source text,
despite several sharing near-identical protective phrasing ("research vocabulary, not the Kernel").
File 51's own "Layer 1 — Knowledge Kernel" (`Claim`/`Evidence`/`Context`/`Authority`/`Perspective`/
`Justification`/`Lineage`/`Transformation`/`Assessment`/`Withdrawal`) is a genuinely new, content-bearing
candidate, distinct from every sense found in MD-093. Combined with MD-093's own sixteen-sense ledger,
this reconstruction now tracks well over twenty distinct, non-identified "Kernel" candidates across the
burst's own first ~30 hours — confirming this proliferation is structural to the corpus's own research
process, not sampling noise.

## "EKS" resolved (batch C)

File 55 explicitly self-defines: **EKS = "Engineering Knowledge System"** — confirmed a distinct usage
from this reconstruction's own backlog-ticket "EKS-nn" prefix by direct in-text definition, though the
extraction agent correctly declined to declare `UNRELATED_HOMONYM` outright absent a positive cross-
reference disproving connection. The same file also uses "PKS" as "RELATED ARCHITECTURAL SYSTEM /
BOUNDARY CANDIDATE" — a third acronym worth tracking for future collision risk.

## A third independent site for the RAG-boundary claim

File 51 (`rag-as-controlled-knowledge-access-layer-not-knowledge-model`) independently derives
`Retrieved≠Relevant≠Supported≠True≠Authoritative` and "KnowledgeOS is not a RAG system" — structurally
the same claim family this reconstruction already found twice elsewhere: MD-089's Batch 1 (the "what
KnowledgeOS makes possible beyond conventional RAG" file) and T21 Part 20's own RAG-boundary theorems
(MD-090). This file predates both by roughly two weeks. No citation links any of the three sites.
Recorded as a confirmed recurring theme, not investigated for identity this phase.

## What remains

`kernel/`'s own next segment (2026-08-24 09:44 onward, past the ~6-hour session break) and the
remaining ~110 files beyond it are named, not begun.

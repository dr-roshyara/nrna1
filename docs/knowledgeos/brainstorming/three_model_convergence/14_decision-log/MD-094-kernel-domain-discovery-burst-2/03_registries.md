# MD-094 §03 — Self-Correction Ledger, Negative-History, Cross-Lane Transfer, Corpus Hygiene

## Self-Correction Ledger (representative)

1. **Protocol self-correction (batch A, files 43→45/46)**: File 43 (first Chalmers pass) is a
   "thematic DDD analysis," not the File-40-established extraction protocol. File 45/46 explicitly:
   *"Your correction is important. The previous answer was a thematic DDD analysis. What you are asking
   for is the same research-extraction method we established before."* Prior claim → correction → reason
   (protocol compliance) → surviving claim (full CH-001..019 re-extraction in the compliant format,
   substantive content largely preserved, method corrected).

2. **Architectural-conclusion self-correction (batch B, File 50's own two-pass structure)**: *"Previously
   I would have said: KnowledgeOS should be a DDD + event-driven + CQRS knowledge system. I would now
   say: that is insufficient as an architectural definition."* Reason: the Chinese-philosophy lens
   exposes unaddressed identity/context/authority/perspective/transformation dimensions. Surviving claim:
   a transactional domain kernel is still needed, but is not sufficient on its own.

3. **Terminology self-correction, partially propagated** (batch B, File 50→51): `KnowledgeClaim` →
   `KnowledgeAssertion`, reasoned but not carried through the very next file — see §02.

4. **Confidence downgrade, not a reversal** (batch B, File 50): CQRS moves from "Strongly required" to
   "likely yes, but that should still be demonstrated" under the newly adopted Zero-lens discipline.

## Negative-History Register

| Claim | Disposition | Where |
|---|---|---|
| Bayesian inference ranked very high (implicit prior architectural ranking) | Re-derived down to one sibling implementation among several (frequentist, bootstrap, confidence intervals) under "explicit uncertainty management" as the actual requirement | Batch C, File 54 |
| "Worst-case is always the right criterion" | Explicitly rejected as a universal rule — scoped to one specific problem class only | Batch A, File 41 |
| "Iteration guarantees truth" | Explicitly rejected | Batch A, File 41 |
| "The two 'adjudication' claims in File 44" (the file's own title) | Confirmed NOT an adjudication by direct textual analysis — a deferred status note | Batch A, File 44 |
| A commit-hook historical implementation defect (test-presence bypassing the unified ObservationRuntime) | Explicitly recorded as corrected, with the surviving canonical pipeline stated | Batch C, File 55 |

## Cross-Lane Transfer Register

**Inbound**: none — this remains the earliest-surveyed material in the corpus.

**Outbound / forward echoes, flagged not resolved**:
- The "K-1 structure" candidate (§02) — the phase's own central open question for a future adjudication-
  only phase.
- The RAG-boundary claim family, now confirmed independently arrived at a third time across this
  reconstruction's own survey (§01).
- File 2 of batch B (`research-direction-formal-model-before-technical-scenario-testing`) is a genuine
  methodological pivot: *"Technical first, with a formal/mathematical layer underneath it"* — the
  corpus's own explicit decision to move toward formalization, though no formal notation (`Sat`, `EC`,
  equivalence-relation symbols) appears anywhere in this 20-file window itself. This is recorded as the
  clearest evidence yet of *when* this research programme decided to formalize, ahead of *where* it
  actually did so — a genuine chronological marker for a future frontier.

**Firewall discipline maintained**: `theory-extraction/` and `verification/zero-algebra/` (the
concurrent session's own active lane) were never accessed by any of the three extraction agents or by
this adjudication.

## Corpus-hygiene findings

- Files 45/46 (batch A, Chalmers rerun and its `-duplicate`) confirmed byte-identical (MD5 match).
- File 44 (batch A) contains two apparent source-text corruption artifacts (dropped words/characters,
  missing spaces) — reported verbatim, not silently repaired, per the master mission's own discipline.
- No new duplicate-content instances found in batch B or C beyond what MD-093 already characterized.

# Phase 5H — Open Questions

## What was closed

- `Qualify`'s type signature: `Observation → Evidence`, triangulated across 2 independent documents
  (seq 0630, seq 0795) — previously cited from only 1 source.
- `e`'s plausible identity as the evidence-payload field of `Assertion`, traced through executable
  code.
- K-1's operator census: 9 named, typed transformations identified and tabulated (previously
  uncatalogued).
- The information-loss classification (D1–D7) applied concretely, per primitive, for the first time.

## What is a reconstruction gap (this reconstruction could, in principle, still find more)

- `Qualify`'s own two-vs-one-argument signature conflict — not investigated further this phase for
  which is more authoritative; a targeted search of Step 272A/272B's own full content (only their
  existence was confirmed, not their content read) might resolve this.
- Model A's own Sañjaya/Arjuna lineage cross-check (named but not adjudicated, per Phase 5F/5G's own
  scope boundary) remains a reconstruction task for a future, explicitly cross-model-authorized phase.

## What is a genuine source-research gap (no further reading closes it)

- The complete K-1 operator register (Gap 1).
- `Qualify`'s computable algorithm (Gap 2).
- `State`'s own projection target (Gap 3).
- The Assertion-unpacking conflict, now confirmed at the code level (Gap 4) — **this is the clearest
  case of a genuine source gap in the whole reconstruction to date**, since it is not merely an
  absence but an active, machine-observable contradiction between two of the corpus's own artifacts.

## What remains mathematically undefined

`State`'s carrier; a formal precondition predicate for `Qualify`; a unified `Assertion` schema; a
governed operator register for K-1.

## What new research would be required from the corpus itself

Restated from `11`: a governed operator register; a formalized `Qualify` predicate; an explicit
`State` carrier definition; a single reconciled `Assertion` schema (retiring one of the two conflicting
scripts, or unifying them with a stated justification); a single reconciled `Qualify` arity.

## This phase's own epistemic discipline, restated

Per the authorization's own final rule: *"The purpose of Phase 5H is not to make the model complete.
Its purpose is to determine whether completeness is supported by the existing evidence at all."* This
phase found the answer is **no, not yet, and precisely why** — five named gaps, three of them
confirmed as genuine source-research gaps rather than reconstruction failures, with the fourth (the
Assertion-unpacking conflict) elevated from a prose-level curiosity (Phase 5G) to a code-level,
machine-observable contradiction (this phase) — a strengthening of the finding's evidentiary weight,
not merely a restatement.

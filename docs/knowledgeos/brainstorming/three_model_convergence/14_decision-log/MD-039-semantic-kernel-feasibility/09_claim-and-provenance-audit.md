# Claim and Provenance Audit

## MD-036's own `STRUCTURAL CORRESPONDENCE` verdict

**Unchanged, and this study establishes it cannot be strengthened using the newly-admitted
framework** (`05`) — the framework is inoperable without an externally-supplied definition, so it
cannot itself upgrade or downgrade MD-036's classification.

## MD-037's own "no formal definition found" finding

**Unchanged and extended**: this study adds that even the one candidate *instrument* the corpus
itself proposes for eventually closing that gap presupposes the definition as an input (`03`), a
more specific and consequential finding than mere absence.

## MD-038's own framework characterization ("under-specified by its own authors' admission")

**Confirmed and sharpened.** MD-038 `05` already found the framework under-specified; this study,
reading `19` §7 fresh rather than reusing that reconstruction, confirms it component-by-component
(`02`) and adds the precise classification of *which* outcome (2, not 1 or 3) that under-specification
corresponds to.

## The corrected independence/corroboration language, applied here too

Nothing in this study treats `18`'s agreement with `12`, or the framework's own stated preconditions
matching `19`'s own restructure table, as independent confirmation — both are within the same
author-layer/lineage (MD-031, re-confirmed MD-037), consistent with the correction accepted at the
start of this study (`00`).

## No frozen artifact rewritten

MD-031, MD-033 through MD-038 are not edited by this study.

# Phase 5F — Open Questions and Non-Convergences

## What was established

- K-1 (ratified `K_t`, 8 primitives) and Phase-5C's own "K-1-B" are the same object, cited identically
  within one authored package — **formal equivalence**, per this phase's own justified promotion.
- K-1 and K-2 (verification lane) stand in a precise, corpus-native **lossy semantic projection**
  relationship — not a homonym, not equivalence, not incompatibility.
- `Observation`'s absence from K-2 is a documented, traceable **layering gap** (Sañjaya recovery,
  computability blocked on `Qualify`) — not an omission or incompatibility.
- Entity/Proposition/Relation each resolve to partial or structural correspondence, independently —
  none to formal equivalence, none to bare lexical coincidence.

## What was falsified

- The earlier (pre-2026-08-31, within-corpus) claim *"the lanes do not disagree, they agree"* — the
  corpus's own D285-1 already falsified/narrowed this; this phase's own analysis independently confirms
  the narrowing was correct.
- Any implicit assumption that K-2 (`(𝒜,ℛ)`) is minimal — explicitly refuted twice (272A §27; D285-7).
- Any assumption that K-1's ratification settles its semantic identity with any other object — refuted
  by this phase's own explicit separation of the two claim types (`07`).

## What remains unresolved

1. **`State`'s status in the verification lane** — no recovery construction analogous to Sañjaya was
   found; genuinely open (`04`).
2. **The `Assertion`-unpacking variance between D285-1 and D285-6** (`10`, item 5) — not reconciled.
3. **Whether K-1 (seq 1006/1008) and K-1-B are the "same object" in the strongest sense (demonstrated
   identity)** — this phase reaches formal equivalence, one level short; an independent proof beyond
   shared citation would be needed to close this fully.
4. **`claim-registry` and `concern-map.md`** — cited by D285-1 as evidence sources but not located as
   standalone files within the searched scope; their content was not independently verified.
5. **The origin of K-2's own `(𝒜,ℛ)` construction as a single named document** — consistently referred
   to as "the verification lane" across three documents, but no single originating file was
   independently located this phase (distinct from K-1's own clean three-document chain).
6. **`𝒪` (operations) has never been enumerated against K-1's own 8 primitives anywhere in the
   corpus** — the corpus's own research (D285-7) names this as the actual blocking reason `𝒪_core`
   cannot close. This is a **gap in the corpus's own research programme**, not a question this phase
   is positioned to answer (doing so would require new mathematical derivation, which is out of scope
   for an adjudication phase).
7. **Whether the Sañjaya/Arjuna Observer/Knower split is genuinely the same construction Model A's own
   reconstruction tracked** — a plausible but unadjudicated cross-model lineage lead, explicitly not
   resolved here (cross-model work is out of scope for Phase 5F).

## What evidence would be required to advance further

- For K-1 ↔ K-1-B to reach demonstrated identity: an explicit statement, anywhere in the corpus, that
  the two citations refer to "the same object" (rather than this phase's own inference from shared
  package/label) — or a constructed isomorphism proof.
- For K-1 ↔ K-2 to advance beyond partial correspondence: an implemented `Qualify: Observation ×
  Policy → Evidence` function, and an enumeration of `𝒪` against K-1's own 8 primitives (both
  explicitly named as missing by the corpus's own D285-6/D285-7).
- For `State`'s verification-lane status to move past `UNRESOLVED`: a corpus document proposing or
  rejecting a `State` recovery construction, analogous to Sañjaya for `Observation` — none currently
  exists.

## Non-convergences (per the authorization's own closure discipline — a rigorous UNRESOLVED is valid)

This phase does **not** report full convergence. K-1↔K-2 remains partial; `State` remains unresolved;
K-1↔K-1-B reaches formal equivalence but stops short of demonstrated identity by design, not by
oversight.

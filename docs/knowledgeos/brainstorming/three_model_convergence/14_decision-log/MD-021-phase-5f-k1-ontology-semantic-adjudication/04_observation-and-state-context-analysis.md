# Phase 5F — Observation and State Context Analysis

Per the authorization's §8: neither primitive is treated as merely "absent from the corpus." Both are
investigated as first-class, context-bound questions.

## Observation

**Raw-source finding (this phase, corroborating and extending D285-1/D285-6's own already-executed
analysis)**: `Observation` is a top-level primitive of K-1 (ratified `K_t`). It does **not** appear as
a top-level primitive of K-2 (`(𝒜,ℛ)`), but D285-6 §4(b) explicitly states this is **not an omission**:
*"The observation layer is NOT missing (revised 2026-08-31). `W` = Domain reality, `Ω` = Sañjaya, the
state-observation capability — corpus-formalised 2026-08-26 with six typed principles. The structural
gap is closed; only the function body remains."*

**This phase's own independent trace to the origin document** (seq 0979,
`phase_measure_theory/knowledgeos_kernel/research/01-GAP-UPDATE-FROM-REVISED-286.md`, raw-read this
phase): the Sañjaya construction is dated **2026-08-26**, in `20260826-151534_right-architectural-
interpretation-now.md`, and carries:

- `Knower ≠ Observer` — *"the foundation of the Sañjaya/Arjuna split"*
- `Sañjaya_K = (Observed, Inferred, Reported, Unknown, Conflicting, Unresolved)` — a six-value
  observation-status vocabulary, explicitly **at the observation layer**
- `Arjuna_K ≠ Sañjaya_K` — "built on top of, never replacing"
- An explicit note that `Σ₀` (an assertion-level object, i.e., K-2's own territory) and `Sañjaya_K`
  (observation-layer) *"are at different layers and were never rivals"*

**Cross-thread connection (disclosed, not asserted as identity)**: this "Sañjaya layer" construction is
plausibly the same Gītā-derived thread Model A's own reconstruction independently tracked (Phase 1's
`02_model-a_gita/02_concept-register.md` §I, "Sañjaya Layer... state knowledge") — **this is a
candidate cross-model lineage lead, not adjudicated here** (adjudicating it would be cross-model work
against Model A, explicitly outside Phase 5F's own authorized scope).

**Classification of the relationship (per the authorization's §8 options)**: **(3) context boundary,
combined with (6) semantic transformation not yet computable.** The `Observation` primitive is present
in both models' *conceptual reach* but sits at a different *layer* (K-1 treats it as a co-equal
top-level primitive; the verification lane treats it as recoverable only via the Sañjaya
sub-construction, projected through an unimplemented `Qualify` function — see `05`). It is **not**
omission, and **not** a genuine incompatibility — it is a documented, corpus-native layering with one
remaining gap (`Qualify`'s missing function body).

## State

**Raw-source finding**: `State` is a top-level primitive of K-1. D285-1 §2 records: *"`State`-as-
primitive remains unimported"* by the verification lane — unlike `Observation`, no corpus-native
recovery construction (comparable to Sañjaya) was found or cited by D285-1/D285-6 for `State`.

**This phase's own check**: no additional primary source resolving `State`'s status in the
verification lane was found this phase (a targeted search for a "State"-recovery construction
analogous to Sañjaya, inside `phase_measure_theory/knowledgeos_kernel/`, returned nothing). **This is
disclosed as a genuine, currently-unresolved gap** — not assumed closed by analogy with `Observation`.

**Classification**: **(7) unresolved relationship.** Unlike `Observation`, `State` has no documented
recovery path in the corpus's own research to date.

## The asymmetry, restated correctly

D285-1's own ¶43 explicitly narrows an earlier over-strong claim ("the lanes do not disagree… they
agree" → too strong) to: *"they do differ on `Observation`... that asymmetry is the load-bearing one."*
**This phase's own reading confirms this narrowed claim remains accurate**: `Observation` has a
documented (if not-yet-computable) recovery path; `State` does not. The two primitives should **not**
be treated identically merely because both are "ratified-only, not imported."

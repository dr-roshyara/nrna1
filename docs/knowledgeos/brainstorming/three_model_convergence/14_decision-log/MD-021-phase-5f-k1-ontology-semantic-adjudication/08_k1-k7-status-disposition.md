# Phase 5F — K-1 Through K-7 Status Disposition

Required vocabulary: `LIVE OBJECT / HISTORICAL CANDIDATE / REJECTED FORMULATION / RESEARCH HYPOTHESIS
/ REPRODUCTION / GOVERNANCE RECORD / UNRESOLVED`. This is an object/provenance determination, not
global classification — no `classification-register.tsv` row is touched.

| Label | Formulation | Disposition | Reasoning |
|---|---|---|---|
| **K-1** | `𝒦=(E,S,T,O,P,R,Π,A)`, 8 primitives | **LIVE OBJECT** | Ratified, computationally tested, actively cited/traced across at least 3 documents (0630, 1006, 1008) in this reconstruction's own reading |
| **K-2** | `(𝒜,ℛ)`, verification lane | **RESEARCH HYPOTHESIS** | Explicitly `NOT RATIFIED` (0 occurrences in ratified artifacts); an active, still-open research construction (D285-6's "OUTCOME B" is itself provisional — "definable, not computable") |
| **K-3** | `(A,R,Σ,E_L)` | **RESEARCH HYPOTHESIS, COLLAPSING TOWARD K-2** | D285-7: "`Σ` stored contradicts the retract/OR-merge proof ⇒ `Σ` must be derived ⇒ collapses toward K-2" — an explicit, source-stated near-reduction, not full equivalence |
| **K-4** | 10-tuple, "complete-mathematical-model" | **HISTORICAL CANDIDATE** | Non-authoritative; "superseded by ~120 steps" per D285-1's own table — the corpus's own research has already moved past it |
| **K-5** | `K(t)=M(t)+iA(t)`, complex-valued | **REJECTED FORMULATION** | Explicitly `REJECTED` (FA-4) |
| **K-6** | `𝒦=(K,H)` | **RESEARCH HYPOTHESIS** | D285-7: "restores replay — the minimal repair to K-2" — an active proposed extension, not itself tested to closure |
| **K-7** | `Zero(K_t)=Ω\Represented(K_t)` | **RESEARCH HYPOTHESIS, INCOMPLETE** | "Presupposes `Ω`, `D_t`, `Z_t`, undefined" per D285-1's own table — the formulation itself is not self-contained |

## Cross-check against Phase 5C/5D/5E's own prior labels

- Phase 5C's "K-1-A" (seq 0167, `KnowledgeAggregate+ConflictRecord+VerificationPort`) is **not** any
  of K-1 through K-7 above — it belongs to a wholly separate `kernel/`-directory research thread (the
  DeepSeek/Kimi/Perplexity DDD dialectic, seq 0150–0168) that this D285-x package (dated 2026-08-31,
  weeks later) does not reference. **This confirms, rather than resolves, Phase 5C's own "likely
  homonym" finding for K-1-A vs. this phase's K-1** — they remain independently-evidenced, structurally
  dissimilar objects sharing only a label.
- Phase 5C's "K-1-B" (seq 1008) = this phase's **K-1** (Reading 1, `02`/`09`).
- Phase 5D's flagged "K-2/K-3/K-6/K-7 unregistered siblings" (from seq 1008) are exactly the K-2/K-3/
  K-6/K-7 rows above — now fully dispositioned, closing that specific open item.

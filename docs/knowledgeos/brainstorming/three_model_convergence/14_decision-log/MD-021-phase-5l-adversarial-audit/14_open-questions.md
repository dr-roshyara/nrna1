# Phase 5L — Open Questions

## What survived adversarial audit, unchanged

The Assertion conflict itself; the option space (A–E, with one named minor gap); the mathematical
discipline (no unearned equivalence claims); the DDD-implications-only framing; the
`GOVERNANCE AUTHORITY NOT EVIDENCED` finding (strengthened, not weakened); the core recommendation of
Option E as a non-binding interim position.

## What was narrowed

The "3 independent sources" evidentiary framing (`04`); the "lowest governance complexity" claim
(`06`); Option C's own "strongest candidate" framing (`05`); Option E's own 2-variant naming scheme
(`07`).

## What was newly discovered, outside this phase's own direct scope

The `C-022`/`claim-registry` citation gap (`08`), bearing on K-1's own ratification citation — named,
not adjudicated.

## Candidate F-prime, named but not added to the option set

"Observation and Evidence as separate knowledge objects, not fields of Assertion at all" (`03`) — a
legitimate, minor completeness gap for a future phase to consider, not added here since doing so would
exceed this phase's own audit-only authorization.

## What a future phase would need to investigate

- Whether K-1's own ratification citation (`C-022`/`claim-registry`) can be resolved elsewhere in the
  corpus, or whether K-1's own "RATIFIED" status itself requires the same governance-unresolved
  treatment this reconstruction has given K-2 — **explicitly out of scope for Phase 5L**, named for a
  future, separately-authorized phase only.
- Whether Candidate F-prime should be added as a sixth option, should governance ever move past the
  interim Option E position.
- The corrections in `13`, should any future phase build further on Phase 5K's own specific claims.

## What remains genuinely unresolved (unchanged from Phase 5J/5K)

`Π`'s denotation; `Qualify`'s authoritative arity/algorithm; `State`'s own projection target;
legitimate governance authority for any K-2 decision.

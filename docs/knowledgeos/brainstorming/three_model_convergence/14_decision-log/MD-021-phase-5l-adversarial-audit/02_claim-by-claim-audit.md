# Phase 5L — Claim-by-Claim Audit

Distinguishing, per the authorization's §2, seven categories throughout: empirical finding /
reconstruction / inference / methodological preference / architectural preference / governance
recommendation / legitimate organizational decision.

| Claim (5K source) | Category | Audit finding |
|---|---|---|
| "K-2's `(𝒜,ℛ)` top-level structure is stable" (`01`) | Empirical finding | **CONFIRMED** — re-verified across all 7 register records this phase, no counter-example found |
| "No authority marker exists for Assertion, Qualify, or K-2 as a whole" (`03`) | Empirical finding (search-scoped) | **CONFIRMED WITH QUALIFICATION** — true within the searched scope; `08` extends the search further and still finds none, but the search is not, and cannot be shown to be, corpus-exhaustive |
| "'HPA is the ratification authority' is a negative example, not a fact" (`03`) | Empirical finding (direct quote) | **CONFIRMED** — re-read verbatim this phase |
| Options A–E as constructed (`04`–`09`) | Reconstruction + one genuine construction (D) | **CONFIRMED WITH QUALIFICATION** — see `03` of this phase for completeness testing |
| "3 independent sources" for Observation→Qualify→Evidence (`07`/`10`) | Stated as empirical finding; actually partly inference | **OVERSTATED**, corrected in `04` |
| "Option C is the strongest content-level candidate" (`14`) | Governance recommendation, partly resting on the overstated claim above | **UNDER-SUPPORTED**, narrowed in `05`/`11` |
| "Option E has zero information loss" (`13`/`14`) | Empirical/mathematical finding | **CONFIRMED** — a factual claim about what is and isn't discarded, independently re-verified this phase (`05`) |
| "Option E has lowest governance complexity" (`13`/`14`) | Governance recommendation, presented adjacent to the empirical claim above without clearly separating the two | **OVERSTATED as stated; CONFIRMED WITH NARROWING** — true of the *ratification act* itself, not shown true of overall system complexity (`06`) |
| "Option E creates a Shared-Kernel-style joint-ownership arrangement... if ever formally co-maintained" (`11` of 5K) | Explicitly hedged architectural hypothesis | **CONFIRMED AS HEDGED** — Phase 5K's own language already carries the "if ever" qualifier; re-checked, this was not overstated |
| Recommendation is `NON-BINDING`/`NOT MATHEMATICALLY DERIVED`/`HUMAN DECISION REQUIRED` (`14`) | Correctly labeled governance recommendation | **CONFIRMED** — the labeling itself is accurate; what required correction was the *content* feeding into it (rows above), not the label |
| Governance Decision Record, 5 entries, all `Pending` (`15`) | Legitimate proposal artifact (not itself a decision) | **CONFIRMED** — no entry is marked as decided; `10` of this phase tests only the *sequencing* among them |
| "D285-1... `C-022` in `claim-registry`" as the basis for treating K-1 as genuinely ratified, used repeatedly across Phase 5F–5K as a contrast case for K-2's own lack of authority | Assumed-established fact, inherited across many phases | **UNDER-SUPPORTED — new finding this phase** (`08`) |

## Summary count

CONFIRMED: 6. CONFIRMED WITH QUALIFICATION: 3. OVERSTATED (corrected): 2. UNDER-SUPPORTED: 2 (one
newly discovered, outside 5K's own direct scope). REFUTED: 0. UNRESOLVED: 0.

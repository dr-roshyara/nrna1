# Phase 5L — Corrections Required (per the authorization's §17 format: `5K-FINDING-ID → 5L-AUDIT-FINDING → REQUIRED CORRECTION`)

**Phase 5K's own frozen artifacts are NOT modified anywhere.** Every correction below is recorded only
in this Phase-5L artifact set, per the authorization's own explicit instruction.

## Correction 1

`5K-FINDING: "07"/"10"/"14" — "3 independent sources" for Observation→Qualify→Evidence`
→ `5L-AUDIT-FINDING: no citation chain exists, but all 3 sources sit within one continuously-numbered
research programme — "independent" overstates the evidentiary framing (04)`
→ `REQUIRED CORRECTION: any future citation of this evidence should read "three separate, uncited
restatements within one continuous research programme," not "3 independent sources."`

## Correction 2

`5K-FINDING: "13"/"14" — Option E has "lowest governance complexity"`
→ `5L-AUDIT-FINDING: true only for ratification-time complexity; likely highest for consumption-time
complexity; Phase 5K's own comparison matrix does not distinguish these (06)`
→ `REQUIRED CORRECTION: any future use of this claim should read "lowest ratification-time governance
complexity; does not reduce, and arguably increases, consumption-time and architectural complexity."`

## Correction 3

`5K-FINDING: "14" — Option C called "the strongest content-level candidate" without repeating the
A/B-vs-E/F distinction already correctly drawn in "07"`
→ `5L-AUDIT-FINDING: presentational gap, not a logical error — the underlying reasoning in 07 is sound
(05)`
→ `REQUIRED CORRECTION: any future summary of Option C should explicitly restate that only the
motivating relationship (Observation precedes Evidence, via Qualify) is evidenced — the structural
embedding into Assertion itself remains unevidenced, equal in evidentiary status to Option D's own
synthesis.`

## Correction 4

`5K-FINDING: "09" — Option E proposes 2 named variants, "K-2-Variant-Alpha/Beta"`
→ `5L-AUDIT-FINDING: the actual register contains 3 materially distinct records (D1/D3, D2/D4, D5);
D5 is silently dropped from Option E's own naming scheme (07)`
→ `REQUIRED CORRECTION: any future adoption of Option E should register 3 variants
(Alpha/Beta/Gamma), or explicitly justify folding D5 into one of the other two.`

## Correction 5

`5K-FINDING (inherited from Phase 5H) — "claim-registry itself was never located as a standalone
file"`
→ `5L-AUDIT-FINDING: a file named "claim-registry" WAS located this phase; its content does not match
D285-1's own cited "C-022... 50 attack classes" content; a "C-022" heading exists elsewhere (step-152)
but concerns an unrelated topic (trace identity) (08)`
→ `REQUIRED CORRECTION: any future statement of this finding should read "a file named 'claim-registry'
exists, but its content does not correspond to D285-1's own citation; the specific 'C-022' D285-1
cites cannot be verified anywhere in the corpus." This bears on K-1's own citation, not K-2 — flagged
for a future phase's own consideration, not adjudicated here.`

## No further corrections found

All other Phase-5K claims are `CONFIRMED` or `CONFIRMED WITH QUALIFICATION` per `12`'s own matrix, with
no required correction beyond the five above.

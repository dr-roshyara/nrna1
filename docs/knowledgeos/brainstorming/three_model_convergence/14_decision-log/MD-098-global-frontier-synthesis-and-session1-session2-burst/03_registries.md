# MD-098 §03 — Self-Correction Ledger, Negative-History Register, Cross-Lane Transfer Register,
Corpus-Hygiene Findings

## Self-Correction Ledger

1. **`synthesis/KNOWLEDGE-CONCEPT-EXTRACTION-001.md`, in-line**: retracts its own prior claim that
   "Knowledge Space first appears at `kernel/20260825-103542`," restated as "same term, different
   concept, not term absent" after direct corpus verification — a clean, explicitly-labeled
   evidence-preserving correction.
2. **`kernel/classification/contradiction-map.md`, T-1**: amended in real time — *"a fourth, earlier
   arrival found during full-corpus extraction"* (citing `synthesis/EXTRACTION-LEDGER.md` DOC-6
   directly), upgrading "three independent arrivals" to "four across six days."
3. **`kernel/classification/contradiction-map.md`, T-8/S-4**: *"This pair falsifies my own finding
   S-4... S-4 is hereby narrowed: it described the corpus up to 2026-08-25 18:10, and no longer holds
   as a general property."* — a self-correction recorded within minutes of the finding it corrects.
4. **`kernel/classification/cluster-map.md`, §1**: *"(Update 2026-08-25 18:17: +8 documents since this
   table...)"* — an explicit, timestamped addendum rather than a silent edit.

## Negative-History Register

| Claim | Disposition | Where |
|---|---|---|
| "Knowledge Space" (Phase 2 sense) is recoverable from Phase 1 material | Tested directly against all 13 Phase-1 "original region" documents: 0 occurrences of the phrase — explicitly rejected as a "cross-generation hypothesis," not a recovery | `synthesis/KNOWLEDGE-CONCEPT-EXTRACTION-001.md` §5; `kernel/classification/contradiction-map.md` §4 |
| "Every genuine falsification is late or external" (S-4) | Falsified by its own author within the same classification programme | `kernel/classification/dependency-map.md` S-4 vs. `contradiction-map.md` T-8 |
| Renaming `kernel/` → `corpus/` | Considered and explicitly rejected by the "HPA" | `kernel/00_CENSUS.md` §7 |
| A single, unified `KnowledgeProduct` aggregate | Challenged in the very same document that proposed it ("this may be too large") | `synthesis/EXTRACTION-LEDGER.md` DOC-6, KCON-010 |

## Cross-Lane Transfer Register

**Inbound — a major, newly confirmed cross-validation event**: this reconstruction's own MD-093–096
chronological, full-text extraction work independently reproduces — via an entirely different
method — nearly every headline finding this pre-existing classification apparatus (Phase A/B census,
dependency-map, cluster-map, contradiction-map) already recorded: the Fagin/Halpern propose-critique-
correct chain, the aggregate-too-large recurrence (T-1/KCON-012), the measure-theory propose-attack
pair (T-8), the Daoist-*ziran*-vs-McGinn identity dispute (T-4), the ADR-KOS-KERNEL-001 draft, and the
Wave-1 adjudication status report (File 44). This is recorded as strong, independent corroboration of
this reconstruction's own method and findings — not as evidence this reconstruction's own prior work
should be revised.

**Outbound / forward echoes, flagged not resolved**:

- The `ConflictRecord`/Verification-Port/`W:C-n`-workbook finding (§02) — the single highest-priority
  item for a future, separately-authorized K-1/K2 adjudication phase, likely requiring direct access
  to the v1.1/Constitution formal architecture this reconstruction has never read.
- A cross-folder duplicate newly found this phase: `kernel/20260824-151311` (topological Åström) =
  `../20260824-145631` (a file in `brainstorming/` root, outside `kernel/` — never previously
  identified by this reconstruction, and outside `kernel/`'s own scope, so not itself re-verified here).
- The 99 root-level `brainstorming/` documents remain, per `00_CENSUS.md`'s own explicit admission,
  "never censused" by this classification programme — a gap this reconstruction's own earlier
  (pre-MD-089) work may partially cover, not reconciled here.

**Firewall discipline maintained**: `theory-extraction/` and `verification/zero-algebra/` were never
accessed.

## Corpus-hygiene findings

- **Full duplicate-register cross-check performed**: of the 12 duplicate pairs listed in
  `kernel/00_CENSUS.md` §2, **nine were already independently confirmed by this reconstruction across
  MD-093–096** (via direct `diff`/`md5sum` in several cases), all matching exactly. **Two pairs are
  newly identified this phase, entirely within `kernel/`'s already-closed scope, requiring no
  correction to prior phases**: `20260824-020936`/`021011` (Chalmers rerun) and `20260824-003307`/
  `004125` (managing-memory). **One is a genuinely new, cross-folder finding**: `kernel/
  20260824-151311` = `../20260824-145631` (outside `kernel/` entirely) — flagged, not chased down
  further this phase.
- **Internal scope-count discrepancy**: `kernel/classification/dependency-map.md` and `cluster-map.md`
  both state the kernel corpus as "141 source documents," while `contradiction-map.md` (written
  later, per its own contents) states "151 source documents." `00_CENSUS.md` itself states "158
  documents" as of its own cutoff (19:23), later reconciled to "142 documents / 130 distinct" in its
  own §6 traceability line. **At least three different population counts appear across four
  documents from the same authoring programme, never reconciled with each other in text.** Reported
  verbatim, not resolved.
- **`kernel/classification/dependency-map.md`'s own mtime (16:14) is earlier than its own referenced
  content** (it discusses documents timestamped through `20260825-132329`, i.e., 13:23) — a
  filesystem-mtime-vs-content-chronology inversion, consistent with the same "mtime is a save time,
  not an authoring order" caveat this file itself states in its own §3. Recorded as a further instance
  of the general chronology-inversion risk this reconstruction has flagged since MD-093.

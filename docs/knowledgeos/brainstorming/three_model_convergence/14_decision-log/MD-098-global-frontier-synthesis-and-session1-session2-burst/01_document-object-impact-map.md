# MD-098 §01 — Document→Object Impact Map (9 files)

## `brainstorming/synthesis/README.md` (16:15)

States the directory's own purpose (corpus-wide synthesis, not kernel-scoped) and a governance
principle: *"A document never acquires a later status because of the directory it sits in. Status
advances only by a recorded act."* — `SOURCE → CLASSIFIED → SYNTHESIZED → ADVERSARIAL → ARCHITECTURAL
AUTHORITY` pipeline stated explicitly. This exact pipeline recurs, verbatim, in `kernel/
classification/README.md` — confirming both are outputs of one coordinating authority.

## `brainstorming/synthesis/KNOWLEDGE-CONCEPT-EXTRACTION-001.md` (18:55) — the K-1–K-11 registry's
own origin

A governed research extraction ("Concept extracted: What is Knowledge?"), scope 99+151 documents
across `brainstorming/` (Phase 1) and `brainstorming/kernel/` (Phase 2), explicitly `RESEARCH ·
NON-AUTHORITATIVE`, "no model chosen." Introduces the **K-1 through K-11 registry of competing
Knowledge (not Kernel) definitions** — this is the confirmed *original* of the content MD-096 Batch G
found embedded (copy-pasted, ~8 minutes later) inside `kernel/20260825-190319-rejection-of-complete-
mathematical-framework-claim.md`. K-1 = "stored content/a thing" (rejected by corpus and law alike);
K-11 = "primitive and unanalysable" (Williamson) — "the deepest tension in the corpus... does not
compete within the field of definitions — it denies the field." A self-correction is recorded in-line
(§5): *"I previously reported that 'Knowledge Space' first appears at `kernel/20260825-103542`. That
was kernel-scoped and wrong corpus-wide... same term, different concept, not term absent."*

## `brainstorming/synthesis/KNOWLEDGE-RECONSTRUCTION-LEDGER.md` (19:21) — births `K-M0`/`K-M1`

A parallel, differently-labeled model-tracking ledger (Session 1's own): `K-M0` (inherited baseline,
Knowledge as relationship/state/evolution) → `K-M1` (an M2-extend triggered by the corpus's own
oldest document, `20260801-1231`, introducing `Observation→Recommendation→Decision→Outcome→
Assessment→KNOWLEDGE`). Opens `C-K1` ("Is knowledge understanding or a relationship?") as a
recorded-not-resolved contradiction. **Never cross-references the K-1–K-11 registry from the
sibling document 26 minutes earlier by name** — a genuine, confirmed parallel-labeling event, not
yet reconciled anywhere in this corpus.

## `brainstorming/synthesis/EXTRACTION-LEDGER.md` (20:08) — the coordination hub

States the objective was "revised 2026-08-25," corpus in scope "256 documents" (98 P1 root + 158 P2
kernel), explicitly excludes `_misc/` (7 documents, HPA ruling). Introduces provenance codes
`P1`–`P9`. Its own Kernel candidate matrix contains the phase's single most consequential line:
*"**Rule Port** | kernel holds the protocol; EKS/PKS supply rules | ... | **`⟦L⟧` CONSISTENT with
Verification Port**"* — treating "Verification Port" as an already-established formal-law (`⟦L⟧`)
concept, not a term this extraction is coining. Confirms `S1-F001`–`S1-F004` already exist by exact
filename, cross-referencing `docs/knowledgeos/reviews/kernel/session1/` directly.

## `kernel/classification/README.md` (16:14) and `00_CENSUS.md` (mtime 19:26, scope stated through
19:23) — Phase A of the kernel corpus's own classification layer

Declares itself "the authoritative classification layer for the kernel corpus" (158 documents,
2026-08-22 16:19 → 2026-08-25 19:23). A **duplicate register** (§2) lists 12 redundant copies —
**cross-checked directly against this reconstruction's own independently-found duplicates across
MD-093–097 and found to match on every one already verified** (111317/111611; 112051/113404;
154256/155130; 160023/160339; 160637/160645; 140251/145918; 101801/145948; 162039/150010;
130514/130339 as "lossy, not identical" — exactly matching MD-096 Batch C's own finding). **Two
duplicate pairs not previously found by this reconstruction**: `020936`/`021011` (Chalmers rerun) and
`003307`/`004125` (managing-memory) — both purely within `kernel/`, not requiring any correction to
prior phases. **One genuinely new finding**: a **cross-folder** duplicate, `kernel/20260824-151311`
(topological Åström) = `../20260824-145631` (a file in `brainstorming/` root, outside `kernel/`
entirely) — never before identified by this reconstruction. A K3/K4 "Kernel relevance" tagging
(39 K3/K4 documents, "30% of the corpus") is recorded as a prior, independent relevance-ranking
exercise, cross-checkable against this reconstruction's own Kernel Identity Ledger but not merged
with it.

## `kernel/classification/dependency-map.md` (Phase B, mtime 16:14 — text implies later authorship
than its mtime, an internal inconsistency)

Headline finding: "the corpus is almost entirely unlinked" — 1 of 141 documents cites another by ID
(`20260825-130514`, the Fagin critique — **exactly** the chain MD-096 Batch C independently traced in
full: `125708→130514→130932→132329`, "the only fully-linked chain in the corpus," confirmed here by
an entirely independent, non-full-text-reading classification method). Names the aggregate-size
recurrence (T-1, `110248→111730→113243`) matching MD-095's own "God Aggregate" finding, and several
chronology-inversion anomalies (`161931`/`162039` — a response timestamped 68 seconds *before* what
it answers).

## `kernel/classification/cluster-map.md` (Phase B, mtime 18:18)

**The central discovery of this phase** (§00, §02): defines the `W:C-1`…`W:C-19` "adjudication
workbook" namespace, explicitly distinct from `⟨C-1⟩`…`⟨C-5⟩` (v1.1 architecture annotations).
`W:C-7` = "`CONFLICTED`↔`ConflictRecord` cardinality"; `W:C-15` = "retraction/withdrawal
representation." A 14-cluster conceptual matrix (Identity, Knowledge, Knowledge Space, Projection,
Time, Epistemic state, Admission, Evidence, Justification, Boundary/aggregate, Change/supersession,
Retraction, Decision/authority, History/lifecycle) records competing positions without adjudicating
them. A provenance-mode breakdown (§1) finds only 10 of 141 documents carry "deliberate project
architectural thinking" (6 original-brainstorming + 4 response-to-prior-hypothesis); 50 are book
extractions (research input, not project position).

## `kernel/classification/contradiction-map.md` (Phase B, mtime 19:40, scope stated as "151
documents" — an internal scope-count discrepancy against the other three files' own "141")

Full T-1 through T-8 contradiction register with a nine-field schema per row. **T-8** ("Is a
knowledge projection a measure?", `181038→181719`) is **exactly** the measure-theory propose-attack
pair MD-096 Batch F independently traced from primary text — this classification artifact's own §2
records it happened "3 minutes" before the map entry was written, self-correcting its own prior
finding S-4 ("every genuine falsification is late or external") in real time: *"This pair falsifies
my own finding S-4... S-4 is hereby narrowed."* §4 directly tests and rejects the claim that
`20260825-171853`'s "Knowledge Space + projection" framing recovers something from the corpus's
original region: *"'knowledge space' — 0 [occurrences in the 13 original-region documents]... The
Knowledge-Space-projection reading of C-15 is therefore a cross-generation hypothesis... not a
recovery of something the original architecture already contained."*

## `kernel/00_CENSUS.md` (Phase A, scope stated as "158 documents")

The foundational document underlying all three Phase-B artifacts above. Full per-cluster document
tables (C1–C12) largely corroborate this reconstruction's own MD-093–096 findings independently (the
ADR-KOS-KERNEL-001 draft at `232146`, matching MD-093's own finding exactly; the Wave-1 adjudication
status report at `020611`, matching MD-094's File 44 exactly — confirmed here under a second,
independent classification method). Records the HPA's own governance rulings on directory structure
(`kernel/` keeps its name; `classification/`, `corpus/`, `synthesis/`, `falsification/` each declared
a distinct function; "an empty directory is not evidence that its phase has run"). Explicitly notes:
"the 99 root-level documents have never been censused" (a gap this reconstruction's own Phase-1-era
work may already partially cover, not reconciled here). A "corpus growth log" (§8) records the
measure-theory thread as it happened, live, matching MD-096 Batch F's own reconstruction closely.

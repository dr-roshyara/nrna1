# Lexical Census — Every Meaningful Occurrence

Classification per occurrence: does it **define**, **constrain**, **compute/derive**, or **merely
name** `Warrant`?

## Thread 1 — kernel-reduction

| Source | Context (paraphrased/quoted) | Classification |
|---|---|---|
| `04` (admitted) | `warrant-assessment` atom: "assign a warrant/verdict given evidence + assumptions" | **Names and constrains lightly** (an atom-level responsibility statement) — not a computation rule |
| `06` (admitted) | derivation rule `{Claim,Evidence}\|{Hypothesis,Evidence} → warrant-assessment → Verdict` | **Constrains** (input/output typing) — does not define what "assessment" computes |
| `10` (unadmitted) | restates the atom/capability mapping | **Merely names**, redundant with `04` |
| `11` (unadmitted) | "warrant kind" (DEDUCTIVE vs. generated-and-tested), non-substitutable | **Constrains via typology** — introduces a two-valued classification of warrant *kind*, still no computation rule for either kind |
| `13` (unadmitted) | *"warrant requires evidence + assumptions"*; *"warrant standard changes"*; **"the standard is policy"** | **Constrains, and adds a structural boundary claim** — see `04`/`05` for why this is the single most consequential passage found |
| `15` (admitted) | F-7, F-10, F-14 — three falsifiers, all treating warrant computability as open | **Merely names**, explicitly flagged open |
| `17` (admitted) | Q-7, Q-8 — catalogues the same as open | **Merely names**, explicitly flagged open |
| `18` (admitted) | *"epistemic standards were folded into `Policy` and `warrant-assessment` and never varied"* | **Constrains** — confirms warrant-assessment was never itself varied/tested as an experimental dimension |
| `19` (admitted) | `P`'s own ten-dimension list names `Warrant`; the directive-divergence table: *"semantics of warrant need expansion"... "accepted... level-2/level-4 work"* | **Merely names** (in `P`); **explicitly defers** definition to a different research phase |
| `FINAL` (unadmitted) | *"the standard (warrant threshold, epistemic contract `EC`, admission policy) is governance"*; *"`Verdict` is warrant, not truth"* | **Constrains, and restates the policy-boundary claim independently** |

## Thread 2 — Titelbaum/Epistemic-Assessment (M0032/33/36, Model-B primary evidence)

| Source | Context (quoted) | Classification |
|---|---|---|
| `M0036` line 116 | `A_t(h) = ⟨attitude, strength, warrant, status⟩` — `[PROP]`, not `[DF]` | **Merely names** — a proposed tuple component, evidence status Hypothesis, no computation |
| `M0036` line 1047 | `K_t = Commit(E_t,A_t,W_t,P_t,H_t)` where *"`W_t` represents warrant/validation"* | **Merely names** — a function-argument label, no computation rule |
| `M0036` line 1227 | `Validate(A,E,S)` — a proposed relation, different signature from kernel-reduction's own `Claim×Evidence→Verdict` | **Constrains, differently from Thread 1** — see `06` for the discrepancy this raises |
| `M0036` line 1428 (pipeline diagram) | `ASSESSMENT → SUPPORT / WARRANT → VALIDATION → KNOWLEDGE STATE` | **Merely names**, and conflates "Support" and "Warrant" as one pipeline stage without distinguishing them |
| `M0032` line 1745 | *"Knowledge as justified/epistemically warranted state evolution"* | **Merely names**, incidental adjectival use |

## Net census result

**Across both threads, every occurrence either merely names `Warrant`, or constrains it lightly
(atom/input-output typing, a two-valued "kind" classification, or the standard-vs-operation policy
boundary). No occurrence, in either thread, computes or derives `Warrant`.**

# V0/V6 Discipline Check

Per the authorization's own instruction, V0/V6 is revisited only insofar as the `Warrant` census
requires it — this section confirms nothing has changed, it does not reopen the question.

| Established finding | Status after this study |
|---|---|
| `V6`'s own representation is source-grounded | **Unchanged** |
| `V6` adds a `Defeater` requirement | **Unchanged** |
| The reachability difference is established | **Unchanged** |
| Epistemic superiority is **not** established | **Unchanged** — this study's own new finding (the "standard is policy" boundary, `04`) does not bear on whether `V6` is superior; if anything it raises a *further* question (does `V6` violate the standard-outside-the-kernel boundary by hard-coding a specific requirement into the derivation rule itself?) that this study does not adjudicate |
| The OLS/confounding example is motivation/analogy, not proof | **Unchanged**, re-confirmed MD-039 `06` |
| MD-036's `STRUCTURAL CORRESPONDENCE` classification | **Unchanged** |
| MD-039's verdict B (framework requires external definition) | **Unchanged** — this study's own finding sharpens *why* (the definition may be architecturally excluded from the kernel by design, not merely unwritten) but does not alter the verdict |

## The Warrant investigation is not used to silently upgrade V6

No finding in this study is treated as evidence for or against `V6`'s own preferability. The
"standard is policy" boundary applies equally to `V0` and `V6` — neither is stated to resolve it, and
this study does not construct an argument that either does.

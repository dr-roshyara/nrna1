# Defeater–Warrant Relation Audit

## Tested relations, per the authorization's own list

**`Defeater → Warrant`**: not stated as a direct functional relation anywhere. What *is* source-
grounded (already established, MD-036) is that `Defeater` becomes, under `V6` specifically, one of
the *inputs* to the same derivation step whose introduced atom is `warrant-assessment` (`06`'s own
table, chained with `12`'s `V6` row). This is a **structural input relationship**, not a claim that
`Warrant` is computed *from* `Defeater` or defined in terms of it — the atom's own output remains
`Verdict`, not a `Warrant` value in its own right.

**`Defeater → survival → Warrant`**: not stated anywhere. "Survival" itself remains undefined
(MD-037/038); no source chains it onward to a `Warrant` computation.

**`Evidence + Defeater → Warrant`**: not stated as a rule. The closest adjacent statement is `15`'s
own F-10: *"Show warrant is computable as `Discriminate` over `{claim, defeaters}` given evidence"*
— explicitly framed as an **open, untested falsifier**, not an established relation.

**Any other explicit relationship**: none found in either thread. Thread 2 (`M0036`) does not use
the term `Defeater` at all — its own vocabulary (`Assessment`, `Standard`, `Evidence`) does not
intersect with Thread 1's `Challenge`/`Defeater` machinery anywhere in the document read.

## Required conclusion, stated exactly as the authorization anticipated

> `Warrant` is the designated framework slot closest to the missing semantic question (per MD-039),
> but the corpus does not establish its semantic content — and, specifically, does not establish any
> computational relation between `Defeater` and `Warrant`, only a structural input relationship
> between `Defeater` and the *step* whose atom is named `warrant-assessment`.

This is not assumed; it follows directly from the census (`03`) and the sufficiency test (`04`), and
is stated here as its own explicit finding, per the authorization's own required framing.

# Phase 5M — Content-Match Analysis (field-by-field, per the authorization's §7)

| D285-1 attribution | Candidate artifact | Exact match? | Evidence type |
|---|---|---|---|
| 8-primitive `K_t` tuple | seq 0630 §49.29/49.75 | **YES, exact** | E1 (direct source evidence) |
| "`step-049`" as source | seq 0630 (the filename literally is "step-049-...") | **YES, exact** | E2 (machine-observable, filename match) |
| "50 attack classes" | seq 0757: "survived 50 attack classes" | **YES for the number**; **NO for the epistemic framing** — seq 0757 adds "no counterexample found in tested scenarios, explicitly NOT a proof," which D285-1's own citation omits | E1 for the number; the *omission* of the hedge is itself E2 (a directly observable textual difference) |
| "no counterexample" | seq 0757: same phrase, "no counterexample found in tested scenarios" | **YES, exact phrase**, but embedded in a stronger epistemic hedge D285-1 drops | E1 |
| "COMPUTATIONALLY TESTED" | seq 0757 does not use this exact phrase; it says "EXP-02's scoped result" | **NO exact match** — "computationally tested" is D285-1's own paraphrase/strengthening, not a direct quote from seq 0757 | E4 (research interpretation on D285-1's own part, not directly evidenced) |
| "FA-4" | seq 0764 discusses `D-FA-1` through `D-FA-7` (not literally "FA-4" as a standalone identifier); the *document itself* is described elsewhere in the corpus as ratifying an "FA-9 Ratification Packet" | **NO exact match for "FA-4" as written**; a plausible but unconfirmed reading is that D285-1 means "the `D-FA` determination series" loosely | E4 |
| "D-FA-6 'qualified naming'" | seq 0764: *"D-FA-6 — K_t naming register... Reasoning: Qualified naming... is adopted"* | **YES, close match** — "qualified naming" is a genuine, near-verbatim echo of seq 0764's own text | E1 |
| "`C-022`" | step-152 §152.26: "C-022: Trace identity" | **NO** — same identifier string, unrelated subject | E2 (a confirmed, exact non-match) |
| "`claim-registry`" | Step 239 (only file with this in its own filename) | **NO** — same phrase, unrelated subject (a different kernel object) | E2 (confirmed non-match) |
| "RATIFIED" (K-1's own overall status) | seq 0764's own D-FA-6 ruling is a genuine ratification act, but of *naming*, not explicitly of "the 8-primitive tuple survives 50 attack classes" as one combined, ratified fact | **PARTIAL** — a ratification act exists and is genuine, but its own scope (naming) is narrower than what D285-1's row implies (the whole K-1 row, including the attack-class result) | E3 (reconstructed provenance — combining two genuine but separately-scoped facts) |

## Summary

**5 of 11 fields: exact match. 2 of 11: partial/close match. 2 of 11: confirmed exact non-match
(the two literal identifiers). 2 of 11: no direct match, D285-1's own apparent paraphrase/
strengthening.**

# MD-063 §01 — Q1: Corpus-Native Satisfaction Semantics

## Every primary occurrence giving semantic meaning to `Sat(K_t,r)`

| Occurrence | Source | What it establishes |
|---|---|---|
| `𝕀(EC_t)={K∈𝕂:Sat(K,EC_t)}` | M0043 `[DEF-19]` | `Sat` is the membership test for the admissible-ideal region |
| `Adequate(K_t,EC_t)⟺Sat(K_t,EC_t)` | M0043 `[DEF-20]` | `Sat` defines adequacy directly |
| `Δ_t=Gap(K_t,EC_t)={r∈Req(EC_t):¬Sat(K_t,r)}` | M0043 `[DEF-21]`, ratified `[FROZEN]` M0132 | `Sat`'s Boolean codomain and its role in the frozen `Δ_t` shape |
| *"`Sat` collapse `\| value∘Eval_c \|` Reason for U (9→1)"* | **M0125 §3.1, new to this phase** | A **corpus-native**, not merely analogous, diagnosis that evaluating `Sat` through a bare-value projection (`value∘Eval_c`) loses a distinction ("Reason for U," a 9-to-1 collapse) |

## What the corpus does NOT give

No source, anywhere checked (M0043, M0047, M0048, M0125, M0126, M0132), supplies `Sat`'s own
computational body — this is unchanged from MD-059–062's own repeated finding, now confirmed a
further time across the newly-checked sections of M0125.

## The one genuine new nuance this phase surfaces

M0125's own "`Sat` collapse" line (§3.1) never writes `Sat` as an applied function `Sat(...)` — it
occurs only as an informal table label, in a document whose **Part 1/3/8** (Contr/Zero evaluation,
the Projection Framework, and the KR-CONTR-FDE commissioning) sit alongside, but never formally
cross-reference, that same document's own **Part 2** (`K_t=(A_t,...,M_t)`, `Σ_t=(A,S,R,V,C)`).
**Whether M0125's own "`Sat`" here denotes the same predicate as M0043's own `Sat(K_t,r)` is not
established by any formula in either document** — addressed as a DDD question in `03`, not assumed.

## Classification

`[DEF-19]`–`[DEF-21]` — **CORPUS FACT** (shape). `Sat`'s own body — **OPEN**, unchanged. M0125's own
"`Sat` collapse" diagnosis — **CORPUS FACT** (the statement exists, in this exact wording, in a
primary source) applied to an **UNRESOLVED** referent (which `Sat`, precisely, it diagnoses).

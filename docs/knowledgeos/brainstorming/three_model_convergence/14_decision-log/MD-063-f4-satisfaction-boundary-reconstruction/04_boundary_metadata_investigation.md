# MD-063 §04 — Q4: Boundary Metadata — Is `Reason`/`Provenance`/`Context`/`Condition`/`Evidence`
Actually Part of F4 Satisfaction Semantics?

## The central finding: M0125 commissions M0127

`20260902-175304_...knowledge-transfer-for-next-session.md` (M0125 — the source of V7's own `K_t`/
`Σ_t` tuple, Part 2) also contains, in the same document:

- **§1.2, "The Immediate Next Action"**: names `KR-CONTR-FDE-2026-09`, status *"APPROVED — READY FOR
  EXECUTION"*, with the exact core candidate `Standing(p)=(S⁺,S⁻)` and `Boundary=(Facet,Condition,
  Context,Provenance)` M0127 itself later reports on.
- **§8.2–8.5, "The Immediate Action"**: a full, detailed commissioning protocol — the exact four
  models to test (Classical/K3/FDE/Structured), the exact 14 scenarios, a **17-section required report
  format** (`# 1. Executive Summary` through `# 17. Classification Register`), and a **six-way final-
  verdict vocabulary** (`A` through `F`, including *"C. STRUCTURED EVALUATION REQUIRED"*).

**M0127's own actual report** (read in full for MD-062) uses this **exact** 17-section structure and
reports **exactly** *"Status: C. STRUCTURED EVALUATION REQUIRED"* and *"Kernel Verdict: K2."* File
timestamps: M0125 at `175304`, M0127 at `175306` — **two seconds apart.**

**Correction to MD-062's own provenance classification (MD-062's own text NOT edited)**: MD-062
classified M0127 as *"same-day, separately-authored, sibling-question corroboration... never
mentioning `Sat`/`Σ_t`/`Δ_t^Σ`"* and used it only as *"structural analogy, never as direct evidence
about `Sat*` itself."* **This was too cautious in one specific respect**: M0127 is not a merely
adjacent, separately-motivated experiment — it is the **direct, literal execution of a protocol
specified inside the same primary document that defines V7.** The correct provenance classification
is **common-authorship, commissioned execution**, not "separately-authored, sibling-question."

**What this correction does, and does not, change**: it does **not** mean M0127's own findings are
now *F4-native by default* — M0127 still evaluates `Standing(p)` for a **proposition** `p`, not
`Sat(K_t,r)` for a **requirement** `r` (`01`/`03`'s own unresolved homonym question stands). What it
changes is the **weight** the finding carries: M0127 is evidence from the *same research programme,
same session, explicitly building toward the same theory document* — not an unrelated corroborating
analogy found by accident.

## Independent, native evidence within M0125 itself (not merely via M0127)

`§3.1`: *"`Sat` collapse `\| value∘Eval_c \|` Reason for U (9→1)"* — M0125's own text, independent of
M0127's own later execution, already names this defect. This is the strongest single piece of
evidence this phase finds that **Reason** is intended to matter to *some* `Sat`-shaped evaluation in
this exact theory document — though, per `01`, not confirmed to be `Sat(K_t,r)` specifically.

## Independent evidence from M0043

`[AX-5]`, "Provenance preservation": `ValidTransition⇒ProvenancePreserved` — a genuine, formally
stated axiom making **Provenance** a first-class semantic invariant of M0043's own theory, tied to
*transition* validity (not directly to `Sat`), turning provenance *"from optional metadata into a
semantic invariant"* in the source's own words.

## Answer to Q4

**`Reason`/`Provenance`/`Context` are genuinely part of this theory's own intended semantics** —
`Reason` via M0125's own native `Sat`-collapse diagnosis and the commissioned/executed M0127 result;
`Provenance` via M0043's own `[AX-5]`. **`Condition`/`Facet` appear only inside M0125's own `Boundary`
tuple, with no independent corroboration found elsewhere.** **None of this is shown to attach
specifically to `Sat(K_t,r)`, `r`, or `EC_t`** — the connection is to a *sibling* evaluation object
(`Standing(p)`, a proposition's support status) and to *transitions* (`[AX-5]`), not demonstrated for
requirement-satisfaction itself. **This is precisely the boundary between EVIDENCED and UNRESOLVED
that `06` must state precisely.**

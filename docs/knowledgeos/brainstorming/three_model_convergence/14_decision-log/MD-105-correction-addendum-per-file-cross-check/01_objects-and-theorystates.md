# MD-105 §01 — Corrected/Extended TheoryState(t) Entries

All entries below ADD to the existing ledgers built in MD-100–104; nothing already logged there is
overwritten or deleted. Each item was verified directly against the raw `.md` source before being
recorded here.

## `Fact` (extends the ledger opened at MD-102, continued through MD-103/104)

- **NEW, EARLIER entry — t(seq 0362, within MD-101's own scope, predates every previously-tracked
  Fact formulation)**: `F = (claim, evidence, context, time, source, validity)`. Verified verbatim:
  "Your proposed fact: `F=(claim,evidence,context,time,source,validity)`... claim — semantic content;
  evidence — support; context — interpretation conditions; time — temporal location; source —
  provenance; validity — an evaluation." The source's own text immediately flags `validity` as
  already-performed epistemic evaluation, and asks whether the Kernel should store `Validity(F)` or
  instead preserve the information from which validity can be evaluated — an early appearance of the
  "preserve substrate, not conclusion" principle later re-derived independently at seq 0377
  (MD-104's `S_Kernel`).
- **Running count corrected**: the Fact ledger now has NINE total distinct structural formulations
  (this one, plus the eight already logged across MD-102–104), still none merged, none canonical.
- This file (seq 0362) also independently corroborates "Knowledge Trajectory replaces Knowledge
  Lifecycle" (already logged, MD-101) — consistent, not new.

## `Claim` and `Hypothesis` (new trajectories, born seq 0368 — already read directly for MD-102, but
this object dimension was not opened at the time)

- **t(seq 0368)**: a boxed seven-way distinction, verified verbatim: "Reality / Observation /
  Evidence / Fact / Claim / Hypothesis / Derived Knowledge." The source's own worked example ("The
  database contains version = 3.69") asks directly whether this is an observation, evidence, a fact,
  a claim, or Knowledge — treating `Claim` and `Hypothesis` as genuinely distinct categories from
  `Fact`, not synonyms.
- Status: NAMED only. No independent signature, type, or operational test given for either `Claim` or
  `Hypothesis` in this file. Tracked as two new, separate object trajectories per the standing
  discipline against collapsing distinct epistemic categories into one.
- **Relationship to `Fact`'s own ten sub-questions (MD-102's mission)**: sub-question (iv)/(v) ask
  about Fact's relation to Observation/Evidence; this file adds that Fact's relation to `Claim` and
  `Hypothesis` is ALSO explicitly posed as an open research question by the corpus's own text, not
  previously logged as such.

## "Knowledge ≠ Knowledge State ≠ Knowledge Space ≠ Knowledge Extraction" (named principle, born
seq 0359, within MD-101's own scope)

- Verified verbatim: "$\boxed{\textbf{Knowledge} \neq \textbf{Knowledge State} \neq \textbf{Knowledge
  Space} \neq \textbf{Knowledge Extraction}}$ I think this is now one of the strongest conclusions in
  our research." MD-101's own register captured related ingredients (κ(K_t)/Φ, the Pritchard/KST
  material) but never logged this exact four-way non-collapse statement as its own named principle.
  Recorded now as a standing, corpus-native non-collapse principle governing this whole lane.
- The same file (seq 0359) contains a structured decalogue — `FACT-KST-01` through `FACT-KST-10`
  (ten discrete claims about Knowledge Space Theory's own formal apparatus: `K⊆Q`; closure under
  union; surmise/prerequisite relations; base/atom compact generative representations; the
  knowledge-space↔entailment-structure correspondence; skill/competency-model equivalence;
  probabilistic knowledge structures `(Q,𝒦,p)`; noisy response functions `r(R,K)`; stochastic
  learning-path transitions; adaptive probabilistic assessment) and three explicit hypotheses
  (`H-KST-1`: Knowledge Space ≠ everything knowable; `H-KST-2`: extraction can be probabilistic while
  the state itself is not; `H-KST-3`: the state can be derived from a substrate). All classified
  EXTERNAL THEORY (Doignon & Falmagne), RELATED/EXTERNAL — NOT ADOPTED, per the standing discipline —
  but never previously logged as named sub-artifacts in this reconstruction's own External-Theory
  Register.
- The same file also explicitly rejects three candidate architectural claims from an earlier analysis
  ("Model the Kernel as Q," "Knowledge State is a perfect candidate for Projection," "Knowledge Spaces
  provides a complete framework for KnowledgeOS") — a genuine, source-native self-correction event not
  previously logged.

## The seL4-derived Kernel-candidate cluster (seq 0351, within MD-100's own scope)

- Verified verbatim: "The strongest candidates are beginning to cluster around: **Identity + History +
  Provenance + Context + Temporal Integrity + Reconstruction**. That is significantly smaller than the
  earlier 10–12-object Kernel models." Derived via an explicit seL4-microkernel analogy ("minimal
  trusted computing base," cited directly) and a sharpened Kernel test: `Remove(X) ⇒ fundamental
  KnowledgeOS guarantee fails`, replacing the weaker `X is useful ⇒ X∈Kernel`. MD-100's own register
  logged the general "three identity types" finding from this same document but did not log this
  specific six-item cluster or the sharpened removal-test criterion. Recorded now as its own entry in
  the Kernel-candidate family (already tracked separately from `S_Kernel`, `K_t^A`, `κ(K_t)`, `K_t^*` —
  no merge).

## The "Refusal" file's structured challenge instruments (seq 0353, within MD-100's own scope)

- Verified verbatim: a ten-row candidate/challenge table (Identity/History/Provenance/Boundary/
  Observation/Assertion/Relation/`Knows`/Transition/Access-scope, each paired with an explicit
  falsifying question); a four-part counterexample requirement for admitting any Kernel primitive
  (Positive case / Negative case / Countermodel / Decision, with an eight-value decision vocabulary:
  `ADMIT/REJECT/DEFER/COMPOSITE/DERIVED/REGIME/DOMAIN/GOVERNANCE`); a nine-stage research sequence
  (Candidate Kernel → Counterexample analysis → Multiple epistemic theories → Reconstruction
  requirements → Kernel candidate reduction → Kernel admission → Minimal contract → Real episode →
  Regime experiments), explicitly proposed to REPLACE a six-stage "freeze the Kernel contract" sequence
  the file itself rejects as premature. MD-100's own register captured the gist ("do not freeze the
  Kernel contract") but not these specific governed instruments. Recorded now; the eight-value decision
  vocabulary is flagged as a candidate reusable classification scheme for any future Kernel-admission
  work in this lane (not adopted as this reconstruction's own vocabulary — noted only).

## "Ban Belief from the Kernel vocabulary" (seq 0360, within MD-101's own scope)

- Verified verbatim: "For now I would **ban 'belief' from the Kernel vocabulary** unless a specific
  research source forces us to introduce it." A genuine negative-governance recommendation — recorded
  in the Negative-History Register (`02_registries.md`) as a standing constraint on Kernel vocabulary
  in this lane, distinct from (but consistent with) the corpus's independent "Knowledge ≠ Probability"
  finding already logged.

## "Eight revised Shani invariants" (seq 0348, within MD-100's own scope)

- Verified present (§32, "Revised Shani invariants," within a larger DDD+relational-mathematics+
  epistemic-semantics+Zero-Lens+Shani-invariants methodology). Named here as a located artifact
  requiring its own dedicated reading if the Shani-invariant family is investigated further; not
  expanded in full in this correction phase (out of scope — this is a correction/addendum phase, not
  new chronological reading).

## Corrected Duplication/Recombination classification (full detail in `02_registries.md`)

- MD-103's "new duplication shape" claim (position 25 / seq 0371) is CORRECTED: this is the EIGHTH
  instance of a single, continuously-tracked intra-file self-duplication series (0267 → 0343 → 0354 →
  0360 → 0362 → 0365 → 0366 → 0371), not a novel discovery. MD-100–102 should have caught instances
  3–7 (seq 0354, 0360, 0362, 0365, 0366) within their own scope; they did not.
- MD-104's "fourth distinct shape" claim (position 35 / seq 0381 = seq 0380 + seq 0377 concatenated)
  is OVERSTATED but not wrong in substance: this specific relationship is independently confirmed by
  the existing per-file record (title: "Concatenation Duplicate: 0380 + 0377 (No New Content)"), and
  is NOT part of the numbered intra-file self-duplication series (it concatenates two DIFFERENT prior
  files' full content, not one file's own content). The finding itself stands; the "new shape" framing
  is corrected to "an independently-confirmed instance of ordinary cross-file recombination," not a
  discovery of a new taxonomic category each time.
- MD-102's own "first confirmed within-file/cross-file recombination instance" claim (position 20 /
  seq 0366's own tail matching position 21 / seq 0367's whole content) remains ACCURATE for that
  SPECIFIC relationship (a large file's own section matching a separate small file) — but MD-102
  separately MISSED that seq 0366 also has its OWN intra-file self-duplication (the seventh instance of
  the OTHER series, unrelated to the 0366↔0367 relationship) within the same file. Both are real,
  distinct phenomena within one document; MD-102 caught one and missed the other.

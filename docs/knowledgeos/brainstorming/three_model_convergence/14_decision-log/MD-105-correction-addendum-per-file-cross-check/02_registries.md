# MD-105 §02 — Corrected Duplication Register, Bridge-Candidate Register (newly adopted), Negative-
History Register addition, Self-Correction Ledger

## Intra-File Self-Duplication Series (corrected, unified register — supersedes the fragmented framing
in MD-102/103/104's own text, which stands unedited; this is the authoritative consolidated view)

A single, continuously recurring corpus phenomenon (very likely a conversation-logging/save-tool
artifact: a document's own content gets re-appended to itself, sometimes behind a bare marker line)
recurs at least eight times across the sequence range relevant to this reconstruction, independently
confirmed by the pre-existing per-file records and, for the instances directly checked in this phase,
by direct raw-source inspection:

| Instance | Seq | Position (this reconstruction) | Caught by |
|---|---|---|---|
| 1 | 0267 | pre-scope | pre-existing record only (not this reconstruction's scope) |
| 2 | 0343 | pre-scope | pre-existing record only |
| 3 | 0354 | MD-100's own scope | **missed by MD-100** |
| 4 | 0360 | MD-101's own scope | **missed by MD-101** |
| 5 | 0362 | MD-101's own scope | **missed by MD-101** |
| 6 | 0365 | MD-101's own scope | **missed by MD-101** |
| 7 | 0366 | MD-102's own scope | **missed by MD-102** (MD-102 caught a DIFFERENT relationship in the same file — see below) |
| 8 | 0371 | MD-103, position 25 | caught by MD-103, but MISLABELED as "a new duplication shape" rather than recognized as this same recurring series |

**Consequence, recorded as correction, not as a rewrite of MD-100–104's own text**: every MD entry
from MD-100 through MD-103 should have flagged this pattern earlier and continuously; none of them
invented a false finding, but three (MD-100, MD-101, MD-102) missed an already-present instance in
their own scope, and one (MD-103) mischaracterized a real instance as newly discovered. No content
finding in any of MD-100–104 is overturned by this correction — the actual TEXT each of those files
analyzed was still read and logged correctly; only the DUPLICATION BOOKKEEPING was incomplete/
mislabeled.

## The separate, correctly-caught relationship at seq 0366

MD-102's own "position 20's tail matches position 21's whole content" finding (seq 0366's own Section 4
matching seq 0367's entire, separate file) is CONFIRMED ACCURATE and remains a genuinely distinct
phenomenon from the intra-file self-duplication series above — a large file's own section
independently matches a SEPARATE small file, rather than the large file duplicating itself. Both
phenomena are present in seq 0366 simultaneously (its own internal self-duplication at lines
2198–2686, AND its Section 4 at lines 1707–2195 matching seq 0367's whole content) — MD-102 caught
one, missed the other, within the same document.

## The seq 0381 concatenation (MD-104's own finding)

Independently confirmed by the pre-existing per-file record (title: "Concatenation Duplicate: 0380 +
0377 (No New Content)"), matching MD-104's own `diff`-verified finding exactly. This is NOT part of
the intra-file self-duplication series above (it concatenates two DIFFERENT prior files, not one
file's own content twice) — MD-104's underlying finding stands; only its "fourth distinct shape"
novelty-framing is corrected to "an independently-corroborated instance of ordinary cross-file
recombination in this lane," since duplication/recombination is now established as a routine,
recurring feature of this population, not a series of one-off discoveries.

## Bridge-Candidate Register (NEWLY ADOPTED this phase — a classification dimension this
reconstruction had not previously used)

Sourced from the pre-existing per-file records' own `bridge_candidates` field (seq 0370–0372, all
within MD-102/103's own already-read scope), each verified consistent with the raw text already read
directly. All entries are CANDIDATE status only, per the source's own labeling — none adopted as an
established cross-lineage relationship:

1. **Navya-Nyāya relational-identity analysis ↔ mathematical relation theory** — on the
   Claim-supportedBy/justifiedBy/assessedAs/supersedes relational structure (seq 0370).
2. **Viveka (distinction-non-collapse) ↔ information theory's I(C;E) distinguishability measure**
   (seq 0370).
3. **Navya-Nyāya relational-identity analysis ↔ the justification-ancestry directed-graph model for
   determined facts** (seq 0371).
4. **Gaṇeśa lens (observation→memory→discrimination→revision→integration) ↔ the recursive
   Observation→Evidence→Determination→Fact→Future-Evidence cycle** (seq 0371) — this specific
   correspondence's Gaṇeśa-lens HALF was already logged in this reconstruction's own MD-101 register;
   the explicit bridge-candidate PAIRING with the recursive epistemic cycle was not.
5. **Nyāya's pramāṇa (valid means of knowledge) ↔ the Reason-as-bridge-between-observation-and-fact
   formalization**, `R=(Evidence,Facts,Rules,Principles,Methods,Arguments,Context)` (seq 0372).
6. **Viveka's distinction-non-collapse principle ↔ the explicit
   Observation=Evidence=Reason=Fact collapse-warning** (seq 0372).

**Going forward**: this register is adopted as a standing category in this reconstruction's own
methodology. Any future `bridge_candidate` observed (whether independently noticed during direct
reading, or cross-checked against a pre-existing per-file record and then verified against raw source)
will be logged here under CANDIDATE status, never promoted without corpus-evidenced identity — fully
consistent with the standing discipline that same-narrative-convergence is not itself proof of
cross-lineage identity.

## Negative-History Register addition

- **"Ban 'belief' from the Kernel vocabulary"** (seq 0360, verified verbatim) — a standing negative-
  governance recommendation for this lane's own Kernel vocabulary, consistent with (but not previously
  cross-referenced to) the already-logged "Knowledge ≠ Probability" finding.

## Self-Correction Ledger (this phase's own corrections, applied to MD-102/103/104's framing, their
own text left unedited)

1. MD-102's "first confirmed within-file/cross-file recombination instance" — ACCURATE for the specific
   0366↔0367 relationship; INCOMPLETE in that it did not separately flag 0366's own intra-file self-
   duplication (the series' 7th instance).
2. MD-103's "a new duplication shape... distinct from MD-101/102's own" — INCORRECT; corrected to
   "the eighth instance of an established, continuously-recurring series."
3. MD-104's "a NEW duplication shape... the fourth distinct shape found in this lane" — OVERSTATED;
   corrected to "an independently-corroborated instance of ordinary cross-file recombination."
4. Method-level self-correction: `01_source-analysis/per-file/` was not consulted at all during
   MD-100–104's own execution, despite covering the exact same files by sequence number. This phase
   establishes, going forward, a standing cross-check step (see `03_status-and-gaps.md`).

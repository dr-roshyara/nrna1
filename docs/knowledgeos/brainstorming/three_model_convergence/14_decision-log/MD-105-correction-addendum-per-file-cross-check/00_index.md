# MD-105 — Correction and Addendum to MD-100–104, via Cross-Check Against `01_source-analysis/per-file/`
Records (Sequences 0347–0381), Every Claim Verified Against Raw Source

## Status: EXECUTED. CHECKPOINT — not a terminal claim, not a hard stop.

## Origin of this phase

The user asked directly whether I had checked `01_source-analysis/per-file/` (an earlier, differently-
authorized reconstruction pass's per-file YAML summaries, covering the ENTIRE main brainstorming
corpus by global sequence number, produced during MD-021's Model-C1 track) before/during MD-100–104's
own chronological read of `phase_measure_theory/`'s root population. The honest answer was **no** — I
had not consulted this directory at all during MD-100–104; I read the raw `.md` source files directly
throughout. The user then asked whether these per-file records could help find missing derivations,
and, after a first pass confirmed real gaps, explicitly authorized recording the correction as a
dedicated MD-105 entry.

**Discipline maintained throughout**: `01_source-analysis/per-file/*.yaml` records are themselves a
SYNTHESIS/reconstruction layer (built for a different, earlier phase's own methodology), not source
evidence — per this reconstruction's own standing source/synthesis firewall. Every finding below was
independently verified against the actual raw `.md` file before being logged here; nothing in this
entry rests on the YAML records alone.

## What was done

1. Identified the sequence-number range covering my own MD-100–104 work: seq 0347–0381 (35 files),
   via `00_control/reading-manifest.tsv`.
2. Dispatched a read-only audit (a fork agent) comparing all 35 corresponding per-file YAML records
   against my own five MD-100–104 write-ups, surfacing candidate gaps — without editing anything.
3. Independently verified every material candidate directly against the raw corpus `.md` files (not
   the YAML records) before accepting it as a genuine finding: `20260825-214833...` (seq 0351),
   `20260825-215831...` (seq 0353), `20260825-210750...` (seq 0348), `20260825-224907...` (seq 0360),
   `20260825-225949...` (seq 0362), `20260825-231629...` (seq 0365), `20260825-224434...` (seq 0359),
   `20260825-233525...` (seq 0368, already read for MD-102 — re-checked for the missed object
   dimension), plus the `bridge_candidates` YAML fields for seq 0370–0372 (cross-checked against the
   raw text of those same three files, already read directly for MD-102/MD-103).

## Central findings (summary; full detail in `01_objects-and-theorystates.md`/`02_registries.md`)

1. **A genuine overclaim, corrected forward**: MD-103's and MD-104's own "a NEW duplication shape"
   framing is WRONG for MD-103 and OVERSTATED for MD-104. Position 25 (seq 0371, MD-103) is the
   **eighth instance of a single, continuously-tracked intra-file self-duplication series** that
   starts at seq 0267 (pre-scope) and recurs at 0343, 0354, 0360, 0362, 0365, 0366, and 0371 — a
   save-tool artifact, not a new discovery each time. **I should have caught instances 3–7 (seq 0354,
   0360, 0362, 0365, 0366) within MD-100–102's own scope and did not.** MD-104's "fourth distinct
   shape" claim for the 0381=0380+0377 concatenation is less wrong (that specific relationship is not
   part of the numbered self-duplication series and is independently confirmed by the existing
   record), but still overstates novelty relative to a corpus that already tracks duplication/
   recombination as an ordinary, recurring feature, not a series of one-off discoveries.
2. **A genuinely earlier, distinct Fact formulation** at seq 0362 (within MD-101's own scope):
   `F = (claim, evidence, context, time, source, validity)` — verified verbatim in raw source. This
   predates every Fact formulation tracked from MD-102 onward and extends the Fact ledger backward.
3. **`Claim` and `Hypothesis`** as distinct, named corpus-native epistemic categories, verified verbatim
   at seq 0368 (already read directly for MD-102, but this specific object dimension was missed): a
   boxed seven-way distinction, Reality/Observation/Evidence/Fact/Claim/Hypothesis/Derived-Knowledge.
4. **The named principle "Knowledge ≠ Knowledge State ≠ Knowledge Space ≠ Knowledge Extraction"**,
   verified verbatim at seq 0359 (within MD-101's own scope) — plus a structured decalogue
   (`FACT-KST-01`–`10`) and three hypotheses (`H-KST-1/2/3`) in the same file, not previously logged
   as named sub-artifacts.
5. **The MD-017 `bridge_candidate` classification dimension was never adopted in my own registers.**
   Six entries exist at seq 0370–0372 (already read directly for MD-102/103), each proposing a
   candidate cross-lineage correspondence (Gītā/philosophical-lens concepts ↔ this lane's own
   mathematical/DDD concepts) — verified present in the per-file records and consistent with the raw
   text already read, but never logged under this heading in my own registers.
6. **Smaller missed items, each verified**: the seL4-derived six-item Kernel-candidate cluster
   (`Identity + History + Provenance + Context + Temporal Integrity + Reconstruction`, seq 0351); the
   "Refusal" file's structured challenge instruments (a ten-row candidate/challenge table, a four-part
   counterexample requirement, a nine-stage research sequence, seq 0353); "eight revised Shani
   invariants" (seq 0348, §32); the explicit "ban 'belief' from the Kernel vocabulary" recommendation
   (seq 0360).

## Non-scope (explicitly not performed)

- No frozen MD-100–104 text edited or rewritten — every correction is recorded forward only.
- No new raw corpus reading beyond direct verification of the items named above (this is a correction/
  addendum phase, not a new chronological-reading phase).
- No merger of the newly-opened `Claim`/`Hypothesis` trajectories with `Fact` — tracked separately,
  per the standing discipline.
- No adoption of any `bridge_candidate` entry as an established cross-lineage relationship — recorded
  as CANDIDATE status only, exactly as the source per-file records themselves label it.

## Artifact map

- `00_index.md` (this file)
- `01_objects-and-theorystates.md` — corrected TheoryState entries for `Fact`, new trajectories for
  `Claim`/`Hypothesis`, the four-way non-collapse principle, the seL4 Kernel-candidate cluster.
- `02_registries.md` — the corrected, unified Intra-File Self-Duplication Series register (instances
  1–8); the Bridge-Candidate Register (newly adopted); the Negative-History Register addition
  ("ban Belief from Kernel vocabulary").
- `03_status-and-gaps.md` — standing-method decision (per-file cross-check adopted going forward,
  scoped precisely); statistical/provenance status.
- `04_checkpoint.md` — required checkpoint output; exact next chronological frontier named (unchanged
  from MD-104: position 36 onward).

# MD-072 §05 — Updated Negative-History Register (delta only)

New retirements/falsifications/contradictions found, `RETIRED` (explicit statement exists) kept
strictly separate from `NO_LATER_EVIDENCE` (object simply stops being cited).

## Explicit retirements/falsifications (a statement exists)

| Claim | Where | What happened |
|---|---|---|
| Six-part `KnowledgeAggregate` (kernel-lane) | `kernel/`, Aug 23 11:33→11:33+45min | **FALSIFIED** same-morning cluster; smaller replacement proposed |
| `(Ω,𝓕)` measure-theoretic Kernel, "first complete mathematical framework" claim | `kernel/`, Aug 25 18:43→19:23 | **CONTRADICTED** three times in ~40 minutes, then formally **SUPERSEDED** by a relational-structure architecture |
| `"Situation = K_t"` (external, Reiter-derived correspondence proposal) | `step-292/03`, Sep 2 | **REFUTED** by executed test (A1), "and refuted by KnowledgeOS's OWN prior evidence, not by Reiter" |
| DeepSeek-derived `K_t`/`Δ`/`Zero`-adjacent equations (e.g. `K_t^decayed=K_0e^{-λt}`) | `gita_chapter4/`, Aug 28 23:58 | **REJECTED** as mathematical derivations by an explicit ruling ("ACCEPTED as raw research material... REJECTED as mathematical derivations... ADOPTED [only] as research hypotheses") |
| `phase_measure_theory`-lane `K_t` (pre-R1 form) | `step-292/`, Sep 2 | **RENAMED/SUPERSEDED** to `A_t` under internal governance decision "R1" — within-lane only |

## `NO_LATER_EVIDENCE` (the object stops being cited; no retirement statement exists)

| Object/version | Last seen | Why not `RETIRED` |
|---|---|---|
| `ADR-KOS-KERNEL-001` | `kernel/`, Aug 23 23:21 | PROPOSED status never revisited anywhere in the 181-file `kernel/` census — simply never cited again, no ratification and no explicit withdrawal |
| Pre-Sep-1 "Zero"/K1–K8 Kernel precursors (ET1) | Aug 19–23 | The corpus's own P5 Decision explicitly declines to promote them (a real disposition), but no document says they are "wrong" — they are simply superseded in practice by the Sep-1-onward math-lane and `phase_measure_theory`-lane threads, neither of which cites them |
| `KNOWLEDGE-CONCEPT-EXTRACTION-001`'s `K-1..K-11` register | Aug 25 18:55, `kernel/` | "No model chosen" by the register's own text; no later document in the 1,200-file census resolves or revisits it |
| `kernel/`-lane's own Kernel-boundary proliferation generally | Aug 25 19:40 (`contradiction-map.md`) | The lane's own governance record states plainly: "no contradiction resolved · no preferred model synthesised · no formal Kernel question adjudicated" — an explicit non-closure, not a retirement |

## What this delta does not do

It does not merge any `phase_measure_theory`-lane or `kernel`-lane retirement/falsification into the
F4 chain's own negative-history record (MD-071 §03) — every entry above is lane-local. It does not
treat the `kernel/`-lane's own extensive non-closure as evidence about the F4 chain's own `Sat`/`Δ_t`
status, which MD-070 already adjudicated separately and remains unchanged.

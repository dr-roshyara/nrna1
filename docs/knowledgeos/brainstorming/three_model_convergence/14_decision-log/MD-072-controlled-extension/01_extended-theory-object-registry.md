# MD-072 §01 — Extended Theory Object Registry

Objects newly discovered by the 1,200-file Level-1 census (`kernel/`, `phase_measure_theory/`, and
~125 pre-2026-09-01 root files), adjudicated centrally. **No object below is merged with any
F4-tracked object** (`K_t`, `EC_t`, `Req`, `r`, `Sat`, `Δ_t`, `Zero`, `Determination`, `Decision`,
etc.) — every apparent name match is recorded as a **candidate relation**, never an identity. Per the
mission's §10, lexical/conceptual/formal/functional/governance identity are kept separate.

## Governing finding: three large, independently-evolving proliferation clusters

The census confirms — at far greater scale than MD-071's own Cross-Lane Transfer Register anticipated
— that **two entire directories neither MD-057–071 nor MD-071's cross-lane check had examined**
(`kernel/`, 181 files, and `phase_measure_theory/`, 894 files) each independently proliferate objects
bearing the exact names of F4-tracked objects, with **no confirmed citation to the F4/math-lane chain
anywhere except one later-dated sub-thread** (§06, Finding 1). These are recorded as **lane-qualified
objects**, distinct from their F4 namesakes until a bridge is demonstrated:

| Lane-qualified object | Approx. distinct formulations found | Representative forms | F4 relation |
|---|---|---|---|
| `K_t [kernel-lane]` | 15+ | `K(s,p,t)`; Knowledge Vector `(T,J,R,A,S,C,U)`; `K(t)=(E,C,I,A,P,R,U,B,M,S)`; `KS=(P,E,M,T,U,D,R,Z,τ)`; `𝒦=(S,P,π,A,∼,T,E,C)` (Fagin/Halpern, later rejected); `𝐊(X)=(O,E,S,C,G,T,M)` | UNCERTAIN — bare symbol match only, zero citation |
| `K_t [phase_measure_theory-lane]` | 25+ (per the lane's own self-audit, `step_239`: "~25 competing right-hand sides for `K_{t+1}`") | 6-tuple through 11-tuple forms; `K_t=(A_t,R_t,E_t,H_t,Z_t,L_t,T_t,G_t,C_t,M_t)` (11-tuple); later `K_t=Replay(K_0,H,t)`; later still, formally renamed to `A_t` under internal governance decision "R1" (`step-292/`, Sep 2) | UNCERTAIN — bare symbol match; ONE confirmed citation bridge exists (§06 Finding 1), scoped to a specific late sub-thread only |
| `Zero [kernel-lane]` | 4+ (minimal-sufficient-representation; residual-after-non-knowledge; complexity/information-loss argmin; invariant-structural-core; later "Zero Lens" as admission-test) | `Z*=argmin_Z[Complexity(Z)+λ·InformationLoss(Z)]`; `Zero(K)=` invariant structural core | UNCERTAIN |
| `Zero [phase_measure_theory-lane]` | 4+ incompatible framings (nested `𝒵_t` component; `Zero(K_t)={Unknown,Conflicted,Ambiguous,Contradictory}` function; distribution-theory gap/conflict detector; pipeline stage `K_t→Z_t→Proposal→Decision`) — the lane's own consolidation documents (`combine-prompt-2`, tag `CON-01`) explicitly flag this drift as "meaning shift unacknowledged" | UNCERTAIN — one instance (`step_186`) explicitly self-identifies a formula (`¬Evidence(p)⇏¬p`) as "the mathematical expression of Zero," structurally resonant with but not cited to F4's own `Zero` |
| `Δ [phase_measure_theory-lane]` | 6+ distinct overloads (decision-attribution delta; `Deviation(Ŝ_t,S*)`; unsubscripted `Δ=S_observed−S_expected`; a 6-component vector later refined to `Δ(S,C,P)=Diff(S,Expected(C,P))`; `Δ` used literally as a "Decision" graph-node label; a discrete time-indexed sum `dS_p=Σ_iΔ_iδ_{t_i}`) plus the already-known "Discrepancy drift" naming (`K_t=(H_t,Δ_t,I_t,ρ_t)`, Ch.6 Gītā file) | UNCERTAIN — one form (`Δ(S,C,P)=Diff(...)`) is the structurally closest match to F4's own `Δ_t={r∈Req(EC_t):¬Sat(K_t,r)}` shape found in either lane |
| `Req(r)` / `Witness(r)` [phase_measure_theory-lane] | 1 (`step_186`, Aug 29 02:12) | `TransitionAllowed(r) ⟺ Req(r)⊆Witness(r)` | **Direct symbol match** to F4's own `Req`/`r` — a third instance of the bare-notation collision pattern EKS-45 already tracks for `K_t`/`Δ_t` (§06 extends this) |
| `Determination`/`Decision` [both lanes] | 8+ distinct tuple arities for `Decision` alone (phase_measure_theory-lane); a separate "candidate kernel capability" `Determination` (Gītā Ch.18, `20260901-014632`); `AuthReq(r,a)`, `D_t=f(K_t,R_t,A_t)` (`step_187`/`step_188`) | UNCERTAIN throughout — no file in either lane equates its own `Determination`/`Decision` with F4's |
| `EC` / `EC_G=(R_G,Γ_G)` [phase_measure_theory-lane] | 1 lineage, several restatements (`combine-prompt-4` through `combine-prompt-6`, `remaining-todos`) | `EC=η(G,IdealState)`; pipeline `Goal→IdealState→EC→K_t→Zero→...` | UNCERTAIN — this lane's own clearest candidate match to F4's `EC_t`, born independently (Aug 28) with no citation to `[00-47]` |

## New, non-proliferating objects (genuinely new, not name-colliding with any F4 object)

| Object | Source | Status |
|---|---|---|
| `KCON-001..025` concept register / `DOC-1..19` / `C-1..19` contradiction codes / `T-1..T-8` temptation register | `kernel/classification/`, `kernel/corpus/`, `kernel/synthesis/`, `kernel/falsification/`, root `00_INDEX.md` | A previously-unknown, self-contained governance/classification pipeline — see Finding, §08 (Corpus Coverage Report) and the Hard-Stop disclosure below |
| `GN-31` (HPA ruling, "Final Architecture RATIFIED") | `phase_measure_theory/external_research/hpa-ruling-gn-31.md`, Aug 28 22:51 | **GOVERNANCE_ADOPTION** — ratifies a naming-register policy for this lane's own `Zero`/`K_t` ("qualified naming... adopted... a terminology policy, not a formal object change") — the corpus's SECOND confirmed governance-ratification event (the first being `ABK-1`/T14, Sep 2), structurally independent of both the `ABK-1` branch and the F4 chain |
| `AuthReq(r,a)`, `Cap`, `Perm`, `Auth`, `Resp` relations | `step_187`, Aug 29 | NEW — authority/capability/permission/responsibility relation system, 8 new invariants I_21–I_28 |
| Bitemporal filtration model `ℱ_t`, `T_e=(T_valid,T_observed,T_known,T_recorded)` | `step_188`, Aug 29 | NEW — 4 invariants I_29–I_32 |
| `ADR-KOS-KERNEL-001` | `kernel/`, Aug 23 | PROPOSED, never shown ratified anywhere in the 1,200-file census |
| `Sārathi`/`Lord`/`Ω` algebra family | `phase_measure_theory/knowledgeos_kernel/` (Gītā-lens sub-thread) | Extensively developed, internally self-auditing (`CON-01`/`CON-02` self-caught semantic-drift findings); not F4-object-named, no candidate relation asserted |

## Object-identity discipline applied throughout

Every entry above that shares a name with an F4-tracked object is explicitly typed `UNCERTAIN` or, where
a direct citation exists, `EXPLICIT` — never silently promoted to `SAME_LINEAGE_AS` or merged. This
registry does **not** decide whether `K_t [phase_measure_theory-lane]` and F4's own `K_t` are the same
object — that determination requires either a demonstrated citation (found once, §06 Finding 1) or a
component-level structural mapping (not attempted here, per the mission's §21 prohibition on premature
canonicalization).

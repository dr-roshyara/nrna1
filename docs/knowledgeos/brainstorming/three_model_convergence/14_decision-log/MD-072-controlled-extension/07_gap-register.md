# MD-072 §07 — Newly Discovered Gap Register

Only load-bearing gaps — the ones that would change the current theory reconstruction if closed one
way or the other. Not a restatement of MD-068's own five gaps (GAP-001–005, all closed/characterized)
or MD-070's own GAP-004 investigation (CLOSED WITH QUALIFICATION). Schema matches MD-068's own: Gap
ID | Question | Blocking object | Why blocking | Earliest evidence | Current status | Next action.

## GAP-006 — Is `phase_measure_theory`-lane `K_t`/`Δ_t` the same object as F4's `K_t`/`Δ_t`?

**Question**: given the confirmed citation bridge (`ET12`) shows the *corpus*, not the *theory
content*, being read across lanes, does any deeper structural correspondence exist between the two
independently-proliferating `K_t`/`Δ_t` families?

**Blocking object**: `K_t [phase_measure_theory-lane]`, `Δ_t [phase_measure_theory-lane]`.

**Why blocking**: if the two families are the same object under different notation histories, the F4
chain's own "9+ unreconciled `K_t` versions" (MD-068 §G) and `phase_measure_theory`'s own "~25
competing formulations" (`step_239`) would need to be read as one much larger, still-unreconciled
proliferation — a materially different picture of the theory's own scale. If they are genuinely
unrelated, the current picture (two separate, parallel efforts) stands.

**Earliest evidence**: `ET5` (Aug 25, `phase_measure_theory`-lane `K_t` genesis) vs. T0 (Sep 1, F4's
own `K_t` genesis) — `phase_measure_theory`'s own thread is chronologically prior by a week.

**Current status**: **UNRESOLVED, non-blocking for the F4 chain's own already-adjudicated status**
(MD-070's GAP-004 finding stands independent of this question). Same discipline as EKS-45/GAP-002 —
tracked, not resolved by assumption.

**Next action**: a dedicated, bounded structural comparison (not a fresh reading pass) of the
`phase_measure_theory`-lane's own most mature `K_t` formulation (`step-155A`'s `K_{t+1}=F(K_t,I_t,C_t,
E_t,D_t,Δ_t,A_t,O_t,V_t,G_t)`, or the later `A_t` form) against F4's own `[00-47]`/`[05-41]` forms —
named, not authorized here.

## GAP-007 — Does `step_186`'s `Req(r)⊆Witness(r)` bear any relation to F4's `Req(EC_t)`?

**Question**: `step_186` (2026-08-29) independently derives `TransitionAllowed(r)⟺Req(r)⊆Witness(r)`,
a direct symbol match to F4's `Req`/`r`. Is this coincidental reuse of common mathematical-logic
notation (`Req` for "requirement," `r` for a generic element — both very common choices) or evidence
of a deeper shared derivation?

**Blocking object**: `Req(r)`/`Witness(r) [phase_measure_theory-lane]`.

**Why blocking**: this is the single instance found in either lane of a formula this close in *shape*
(a requirement-set-containment gate) to F4's own `Req(EC_t)`'s role — closer than the `K_t`/`Δ_t`
parallel, which only shares vocabulary, not structure.

**Earliest evidence**: `step_186`, 2026-08-29 02:12, four days before F4's own canonical `Req(EC_t)`
(T5, 2026-09-02 00:46).

**Current status**: **UNRESOLVED**. Recorded `UNRELATED_HOMONYM` per the standing discipline (§06),
pending stronger evidence than shared notation.

**Next action**: named, not authorized — direct comparison of `step_186`'s full context against
`[00-47]`'s `[DEF-19]`–`[DEF-21]`.

## GAP-008 — What does the `kernel/`-lane's own classification pipeline conclude, and does it bear on anything F4 tracks?

**Question**: `kernel/`'s own `classification/`, `corpus/`, `synthesis/`, `falsification/` apparatus
(§08) independently classified and adjudicated ~260 pre-Sep-1 files, reaching its own conclusions
about Kernel/Knowledge (`KNOWLEDGE-CONCEPT-EXTRACTION-001`'s `K-1..K-11`, `contradiction-map.md`'s
`T-1..T-8`). This census read the files it classifies, but not its own downstream reasoning in full
depth (a 3,217-line principal-architect-review file, for instance, was sampled, not exhaustively
read).

**Blocking object**: the `KCON`/`DOC`/`C`/`T` register apparatus as a whole.

**Why blocking**: if this pipeline reached a conclusion materially relevant to the F4 chain's own
open questions (e.g., a resolved position on whether "Knowledge" itself belongs in a Kernel — directly
adjacent to F4's own `K_t`/`Sat` boundary question), that conclusion has been sitting unintegrated into
this reconstruction's own work since at least 2026-08-25.

**Earliest evidence**: `ET2` (Aug 22–25).

**Current status**: **UNRESOLVED, genuinely load-bearing** — this is the census's own strongest
candidate for a Hard-Stop-F (corpus-boundary) condition requiring separate adjudication before any
further extension into this material.

**Next action**: named, not authorized — a dedicated, bounded reading of this pipeline's own terminal
artifacts (`00_INDEX.md`'s full register, `EXTRACTION-LEDGER.md`'s own KCON-001..025 in full,
`KNOWLEDGE-CONCEPT-EXTRACTION-001`'s K-1..K-11 table in full) as its own scoped phase.

## GAP-009 — How should `GN-31` and `ABK-1`/T14 be understood relative to each other?

**Question**: two independent governance-ratification events exist in this corpus (`GN-31`, Aug 28;
`ABK-1`, Sep 2), five days apart, neither citing the other, both touching adjacent-but-distinct
"Zero"/naming-register questions. Is this coincidence, parallel convergent governance need, or does
one supersede/inform the other in some undiscovered document?

**Blocking object**: neither directly blocks the F4 chain (confirmed — neither engages it), but both
bear on the corpus's own governance-maturity picture.

**Earliest evidence**: `ET7` (Aug 28).

**Current status**: **UNRESOLVED, non-blocking for F4**, recorded for completeness of the corpus's own
governance history.

**Next action**: none named — low priority relative to GAP-006/007/008.

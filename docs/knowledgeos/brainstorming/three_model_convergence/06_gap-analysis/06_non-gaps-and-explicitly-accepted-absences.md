# Non-Gaps and Explicitly Accepted Absences

Items that could be mistaken for open gaps but are correctly classified otherwise — resolved by the
corpus itself, or genuinely a model-specific structure/accepted non-convergence rather than something
requiring closure.

## Resolved within the corpus's own evidence (not gaps)

- **GA-024** — Model B's "Zero more fundamental than Sat" claim: raised (M0057), rejected one file
  later (M0058), synthesized (M0060). The corpus's own audit discipline resolved this; nothing remains
  open.
- **GA-025** (mostly) — Model B's Structure-First "general theory" self-description: raised (M0099),
  downgraded to "explanatory hypothesis" the immediately following file (M0100). Resolved in substance.
  Only the specific "which closure meaning" question survives as GA-026, a distinct, still-open item.
- **Phase 3's INCOMPATIBLE rows** (Invariant, Equivalence-domain-level, Knowledge/epistemic-status
  value-set level — `05_cross-model/02` Rows 7, 8, 10) — these are not gaps to be closed. They are
  **correctly adjudicated non-correspondences**: "invariant" and "equivalence" name genuinely different
  *kinds* of thing in different models, and treating them as comparable would itself be the error this
  reconstruction's own methodology exists to prevent. No further evidence would change this — it is not
  awaiting resolution, it is a completed finding.

## Model-specific structures (ACCEPTED NON-CONVERGENCE)

Per Phase 3's own non-convergence document (§1) and Phase 6's own extension (§1), the following are
recorded as genuine, expected products of independent reconstruction from disjoint source material —
**not defects, and not gaps requiring future evidence**:

- Minimality/reduction/composition/closure/operator-set formalism (Model B only).
- Entity/Role DDD distinction (Model A only).
- Sustained explicit "knowledge"-definition content (Model C1/C2 only) — this is GA-004, listed as
  BLOCKING *only conditionally* (§05); as a bare fact about model content, it is an accepted absence in
  A/B, not a defect in either.
- Measurement-governance apparatus (Model C2 only, n=1).
- The lens/regime/representation-guarding methodological parallel (A's lens discipline, B's Structure-
  First, C2's Shani-invariant) — recorded across both cross-model documents as a fact about shared
  *research methodology*, never promoted to a domain-level correspondence.

## Explicitly accepted absences from this gap-analysis's own scope

- **The K-1/K-2 governance-frozen track** (GK-5K-1 through GK-5K-5, K-1's own OQ-2) is not a gap this
  stage tracks or reports on — it is a separate, already-documented governance boundary
  (`MD-021-research-to-governance-handover.md`, `MD-022`), explicitly out of scope here per this
  stage's own authorization.
- **The P-series** (`docs/knowledgeos/theory-extraction/`) is not consulted anywhere in this
  gap-analysis and its own findings (e.g. the persistence-kernel work, P-08 through P-26) are not
  gaps of *this* programme — a separate research track entirely.
- **Global reclassification** (protocol.md's own gate, still `PENDING_GLOBAL_RECLASS` on every register
  row) is not treated as a gap this stage must close — its deferral is an already-recorded, accepted
  historical decision (see `00_index.md`'s precondition note), not an open question this stage
  discovered.

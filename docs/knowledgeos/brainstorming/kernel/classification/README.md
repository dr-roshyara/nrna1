# classification/ — kernel corpus (ACTIVE)

**Scope:** `docs/knowledgeos/brainstorming/kernel/` — 141 source documents.
**This is the authoritative classification layer for the kernel corpus.**

| artifact | phase | status |
|---|---|---|
| `../00_CENSUS.md` | A — census, clusters, K-levels, contradiction register | **done** |
| `dependency-map.md` | B — response chains, chronology anomalies | **done** |
| `cluster-map.md` | B — cluster detail beyond the census §3 tables | not started |
| `contradiction-map.md` | B — T-1…T-7 expanded per document | not started |

A reclassification edits **these files** — never a source document's name or location.

```
SOURCE  ->  CLASSIFIED  ->  SYNTHESIZED  ->  ADVERSARIAL  ->  ARCHITECTURAL AUTHORITY
```

**A document never acquires a later status because of the directory it sits in.** Status advances
only by a recorded act. Directory names express **artifact function** — never subject, never authority.
Source classification is **metadata** (recorded in a census), never a path or a filename code.

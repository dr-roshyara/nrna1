# falsification/ — kernel attacks (RESERVED — Phase G, not started)

**Empty.** `candidates/` claims proposed for attack · `attacks/` the attack · `results/` the outcome.

One real attack already exists but was **left in place** among the sources by decision:
`20260825-130514` (critique of the Fagin extraction). Census §3 labels it by function.
Per census S-4, the corpus attacks itself rarely and mostly via imported instruments —
`20260823-234750` (tarka: nine attack families) is the reusable one.

**An attack that fails does not promote its target.** A surviving hypothesis is still a hypothesis;
only a recorded governance act reaches ARCHITECTURAL AUTHORITY.

```
SOURCE  ->  CLASSIFIED  ->  SYNTHESIZED  ->  ADVERSARIAL  ->  ARCHITECTURAL AUTHORITY
```

**A document never acquires a later status because of the directory it sits in.** Status advances
only by a recorded act. Directory names express **artifact function** — never subject, never authority.
Source classification is **metadata** (recorded in a census), never a path or a filename code.

# synthesis/ — kernel clusters (RESERVED — Phase D/E, not started)

**Empty. Phases D and E have not run.** An empty subdirectory here is **not** evidence that a
cluster has been read.

Subdirectories exist for the four clusters the census identifies as highest-yield:
`identity/` · `epistemic-state/` · `evidence/` · `boundary/`.

Reading order the census supports: **identity first** — it carries a live K4 challenge
(`20260824-094638`, Daoist *ziran* vs `INV-KOS-IDENTITY-001`), a K3 counter-position
(`20260825-115235`/`120122`, McGinn: identity primitive and indefinable), and **no reconciliation**.

```
SOURCE  ->  CLASSIFIED  ->  SYNTHESIZED  ->  ADVERSARIAL  ->  ARCHITECTURAL AUTHORITY
```

**A document never acquires a later status because of the directory it sits in.** Status advances
only by a recorded act. Directory names express **artifact function** — never subject, never authority.
Source classification is **metadata** (recorded in a census), never a path or a filename code.

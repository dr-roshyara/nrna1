# corpus/ — INERT BY DECISION

**Nothing will be moved here.** The 141 kernel documents stay in `kernel/` root where they were
captured. Retained as a reserved name only.

Four **role anomalies** were flagged in census §3 C12 and deliberately **left in place**:
`20260824-032219` (an EKS architecture baseline, not brainstorming) · `163922` (a raw article,
not analysis) · `20260825-130339` (lossy scrollback) · `20260824-010504` (.docx duplicate).

```
SOURCE  ->  CLASSIFIED  ->  SYNTHESIZED  ->  ADVERSARIAL  ->  ARCHITECTURAL AUTHORITY
```

**A document never acquires a later status because of the directory it sits in.** Status advances
only by a recorded act. Directory names express **artifact function** — never subject, never authority.
Source classification is **metadata** (recorded in a census), never a path or a filename code.

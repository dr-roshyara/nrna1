# Theory 09 — **The Frozen and Refuted Register: Negative Knowledge**

**Document 09 of 15** · 2026-09-04
**Sources:** `S-frozen-results-register.md`, all experiment audits

> **This is the most valuable document in the set.** A research programme that cannot show its
> refutations has not been doing research. Twenty-five attractive hypotheses were killed rather
> than protected, and several of them were *mine*.

---

## 1. `FR-001` — **FROZEN** 2026-09-02

> ### **Pairwise semantic/evidential distinguishability cannot carry family-level epistemic complexity by itself.**

Adjudicated as a **research-result freeze — not a theory or architecture decision.**

Established by **two independent failure mechanisms**, which is what makes it a boundary rather
than a failed fit:

| # | mechanism | evidence |
|---|---|---|
| 1 | $\sim_\Lambda$ is **not transitive**, so $H/\!\sim_\Lambda$ is not a legitimate quotient | sorites witness: $H_1 \sim \dots \sim H_{12}$ pairwise, $H_1 \not\sim H_{12}$, span 3.068 at $\delta = 0.3099$ |
| 2 | the transitivity-free repair (**$\delta$-packing**) captures **redundancy** and fails on **coupling** | grouped 0.98–1.06 · correlated up to **222×** |

| finding | status |
|---|---|
| $\sim_\Lambda$ is generally transitive | **`[NEG]`** |
| $H/\!\sim_\Lambda$ can generally define complexity | **`[NEG]`** |
| Homogeneous spaces can **conceal** non-transitivity | `[EXP]` |
| $\delta$-packing avoids the quotient/transitivity problem | `[EXP]` |

`[EXP]` **`KR-HILBERT` strengthened `FR-001` rather than reopening it.** The pairwise and
spectral routes **bracket** the true burden from opposite sides; all four tested spectral
functionals are exact on block structure. `H1`/`H3`/`H7`/`H8` refuted.

---

## 2. `FR-002` — **CANDIDATE, NOT FROZEN**

Explicitly withheld from freezing until the wording *"reason is the sole indispensable field"*
was re-checked. It has since been **softened and tested by measurement, not by rewording.**
The candidate list stands; **the freeze does not.** Contents are in document 08.

---

## 3. `FR-003` — **CANDIDATE**, this cycle's contribution

> ### **Read-disjointness must be discharged on the triple $(\Pi, Q, T)$ and verified empirically.**

`[EXP]` Established by `KR-BRIDGE-02` §2 — it caught a false positive ($MH\text{-}RD = -0.155$,
replicating) that would otherwise have entered the record, and **the version written by
inspection was itself wrong, twice.**

`[REC]` **Needs a second independent adopter before promotion** (`ES-006.1`). One repository of
evidence is a candidate, not a standard.

---

## 4. The refutation ledger

### 4.1 Refuted claims about `Zero`

| Claim | Status |
|---|---|
| The elimination operator $L$ is a projection | **`[NEG]`** |
| $L$ is confluent | **`[NEG]`** |
| $L_A L_B = L_B L_A$ | **`[NEG]`** |
| individual `Zero` ⟹ group `Zero` | **`[NEG]`** |
| group `Zero` ⟹ individual `Zero` | **`[NEG]`** |
| `Zero` is invariant under reference change | **`[NEG]`** |
| `Zero` is independent of contract detail | **`[NEG]`** |
| invariant ≡ `Zero` | **`[NEG]`** |
| $\mathrm{Remainder} = D - \mathrm{Eliminated}$ | **`[NEG]`** |
| `RuleEliminable` ≡ `CounterfactualZero` | **`[NEG]`** |
| $\mathcal Z$ is downward-, upward-, union-, intersection-closed | **`[NEG]`** ×4 |
| $\mathcal Z$ is a matroid | **`[NEG]`** |
| $\mathcal Z$ is generated by its minimal elements | **`[NEG]`** |
| Relational transformations are **necessary** for higher-order `Zero` | **`[NEG]`** |

### 4.2 Refuted claims about the bridge

| Claim | Status |
|---|---|
| `Zero` predicts preservation | **`[NEG]`** — 4 independent tests |
| `Zero` marks the reduction boundary | **`[NEG]`** |
| Admissibility is a property of $(\Pi, Q)$ | **`[NEG]`** — it is $(\Pi,Q,T)$ |
| Read-disjointness can be established by declaration | **`[NEG]`** |
| Flattening the redundancy distribution raises informative strata | **`[NEG]`** |
| Population size raises informative strata | **`[NEG]`** |
| The dead cells are a generator problem | **`[NEG]`** |

### 4.3 Refuted claims about representation and evaluation

| Claim | Status |
|---|---|
| Rank conventions are invertible recodings | **`[NEG]`** — 21 369 tie violations |
| The rank result instantiates `DECISION-01` | **`[NEG]`** — withdrawn |
| Reduction dimensions are independent / collapsible to one scalar | **`[NEG]`** |
| Adequacy ⟹ Realization | **`[NEG]`** |
| KnowledgeOS requires four-valued logic | **`[NEG]`** |
| `last-wins` is an admissible composition rule | **`[NEG]`** — eliminated twice |
| Minimum flat cardinality is context-free | **`[NEG]`** |

---

## 5. Claims **withdrawn by their author**, which is a separate category

These were not refuted by an opponent. They were retracted after audit, and the register keeps
them visible so the retraction cannot be quietly forgotten.

| Withdrawn claim | Why |
|---|---|
| *"the answer lies between them"* | needs a formal ordering **and a proof**; had neither. Withdrawn from the frozen register too. |
| The `KR-BRIDGE-01` single-mechanism explanation | Zero-count is constant at $r=1$ but **varies** at $r=2$; the exact $RD = 0$ has **two** causes |
| *"the representation class is flat"* | generator defect — `R1` should have been token-only; 103/256 (40.2 %) carried metadata via 3 shapes bypassing `mk()` |
| The §5 rank headline **and the review's replacement wording** | both refuted by audit FAIL 2.3 |
| *"`H-C` refuted a priori"* | corrected to **structurally inapplicable**; *a priori impossible* $\neq$ *empirically falsified* |

---

## 6. Recorded defects, contained

| Defect | Containment |
|---|---|
| Bootstrap CI on $\hat H$ is invalid (resampling depresses conditional entropy) | no verdict depends on it; $N_{\text{viol}}$ is exact |
| `KR-REP-REDUCTION` `H-B` could not exhibit the adequacy/realization separation | recorded as design failure, not as evidence against the separation |
| `KR-BRIDGE-01` `T_B` label mismatch | pre-registered labels are **classifications to be tested**, never redefined post hoc |
| `KR-ZERO-ORDER` `O2` was not paired | replaced by a fully paired re-analysis; conclusion held |
| `P1_result` / `P2_len` / `P3_required` extensionally identical (0 disagreements in 81 440 tests) | the contract factor has **7 levels, not 9** — and this *strengthens* `H8` |
| `C10` was vacuous | 4 token-blind rules cannot separate a token relabeling; rewritten with a token-sensitive witness |
| Adequacy is population-dependent by construction | checked across $n$; flat; dependence recorded |

---

## 7. The rule this document enforces

> **The strongest statement made must never exceed the strength of the available evidence.**

and its operational form:

> **Keep facts, architectural conclusions, governance decisions, engineering actions and
> operational observations rigorously separate — never let one silently become another.**

Yes. **What is being asked there is not another theoretical answer; it is an implementation/audit deliverable.** Based on the material we have, I can provide the **production-ready auditor and the exact corpus-audit specification**, but I cannot truthfully claim to have inspected the raw KR-ZERO files because the actual corpus files are not available in the current conversation context.

I would give Claude/Codex the following as the execution task:

---

# `KR-REP-DATASET-2026-09` — Production Corpus Audit & Adapter

## Objective

Inspect the existing KR-ZERO corpus and construct a provenance-preserving dataset suitable for the canonical representation-reduction experiment.

**Do not create synthetic data.
Do not infer missing representation states.
Do not silently reconcile denominators.
Do not reinterpret historical `k` classifications as representation levels.**

The resulting dataset must establish exactly what can and cannot be mapped into:

$$
(D_i,Q_i,R_{5,i},R_{4,i},R_{3,i},R_{2,i},R_{1,i})
$$

for every historical case.

---

## Phase 1 — Corpus discovery

Search the repository/corpus for all artifacts associated with:

```text
KR-ZERO
KR-ZERO-ORDER-2026-09
KR-ZERO-ALGEBRA-2026-09
Zero
irreducible
k=1
k=2
k=3
1395
1496
1475
T1
T2
T3
T4
T5
T6
T7
T8
```

Identify:

```text
source files
generated datasets
JSON
CSV
JSONL
SQLite/database exports
experiment manifests
README files
result summaries
witness files
logs
```

For every discovered artifact record:

```text
path
format
size
timestamp
declared case count
actual record count
experiment identifier
generation metadata
```

Do not modify the source corpus.

---

# Phase 2 — Denominator reconciliation

Explicitly reconcile all reported totals.

At minimum:

$$
1395,\qquad1496,\qquad1475
$$

must each be explained.

Produce:

| Experiment       | Declared N | Actual N | Filtering | Exclusions | Reason |
| ---------------- | ---------: | -------: | --------- | ---------- | ------ |
| KR-ZERO baseline |            |          |           |            |        |
| O2               |            |          |           |            |        |
| O4               |            |          |           |            |        |
| O6               |            |          |           |            |        |

The audit must answer:

> Are these different populations, different filtering stages, duplicated cases, or reporting inconsistencies?

**Do not repair the historical numbers.**

---

# Phase 3 — Source schema discovery

Determine the actual representation of:

$$
D,\quad S,\quad T,\quad\Pi,\quad Q,\quad Zero.
$$

For each field report:

```text
field
actual source location
actual type
required/optional
deterministically reconstructible?
provenance
confidence/status
```

Use statuses:

```text
VERIFIED_OBSERVED
VERIFIED_DERIVED
PARTIAL
UNOBSERVED
AMBIGUOUS
CONTRADICTORY
```

`VERIFIED_DERIVED` is permitted only when the derivation is deterministic from explicitly observed source data.

---

# Phase 4 — Independent Q audit

Determine whether the corpus contains an independently evaluated:

$$
Q(D_i).
$$

Accept only:

```text
Q explicitly evaluated from raw D
```

or:

```text
Q deterministically reproducible from raw D
```

Reject:

```text
Q derived from R_n
Q inferred from Zero-status
Q reconstructed from the desired experimental outcome
```

For every sample record:

```text
Q_source
Q_derivation
Q_independence_status
```

If independence cannot be demonstrated:

```text
Q = NULL
Q_independence_status = UNVERIFIED
```

Do not manufacture ground truth.

---

# Phase 5 — Representation reconstruction

Attempt to establish:

$$
R_5\rightarrow R_4\rightarrow R_3\rightarrow R_2\rightarrow R_1.
$$

Critically:

> Historical `k=1`, `k=2`, `k=3`, and `irreducible` are **provenance classifications**, not automatically \(R_1,R_2,R_3,R_4\) or \(R_5\).

For every transition determine whether it is:

```text
OBSERVED
DETERMINISTICALLY_RECONSTRUCTIBLE
UNOBSERVED
AMBIGUOUS
```

If an intermediate representation was not actually recorded and cannot be deterministically reconstructed:

```text
R_n = NULL
```

Never interpolate it.

---

# Phase 6 — Transformation registry

Create:

```python
TransformationSpec(
    transformation_id=...,
    input_level=...,
    output_level=...,
    definition=...,
    source_provenance=...,
    reconstruction_status=...
)
```

For the canonical chain:

$$
R_5=T_5(D)
$$

$$
R_4=T_4(R_5)
$$

$$
R_3=T_3(R_4)
$$

$$
R_2=T_2(R_3)
$$

$$
R_1=T_1(R_2).
$$

Do **not** assign these identifiers merely because five levels are required.

If the historical experiment does not support such a mapping, document the mismatch.

---

# Phase 7 — Construct the dataset

Produce:

```python
CorpusRow(
    sample_id=...,
    raw_D=...,
    independent_Q=...,
    R={
        5: ...,
        4: ...,
        3: ...,
        2: ...,
        1: ...
    },
    C={
        5: ...,
        4: ...,
        3: ...,
        2: ...,
        1: ...
    },
    O_out={
        5: ...,
        4: ...,
        3: ...,
        2: ...,
        1: ...
    },
    provenance_k=...
)
```

Add provenance fields if the source schema permits.

---

# Phase 8 — Completeness audit

For every level:

$$
N_n=
|\{i:R_{n,i}\neq NULL\}|
$$

and

$$
\boxed{
Comp_n=\frac{N_n}{N_{\mathrm{source}}}.
}
$$

Produce:

| Level | Source N | Valid \(N_n\) | Completeness |
| ----- | -------: | ------------: | -----------: |
| R5    |          |               |              |
| R4    |          |               |              |
| R3    |          |               |              |
| R2    |          |               |              |
| R1    |          |               |              |

This table is mandatory.

---

# Phase 9 — Only then execute the auditor

For each level calculate:

$$
A_n,\quad
F_n,\quad
N_{\mathrm{viol},n},\quad
\hat H(Q|R_n),\quad
\hat H(R_n|Q),\quad
\hat H(R_n).
$$

Use the actual valid support \(N_n\).

The structural identity must be checked:

$$
\boxed{
N_{\mathrm{viol},n}=0
\iff
\hat H(Q|R_n)=0
}
$$

for the finite empirical support.

If the implementation produces disagreement, **stop and diagnose the implementation/data canonicalization** rather than proceeding.

---

# Phase 10 — Canonical \(n^*\)

Only a completely observed level qualifies:

$$
\boxed{
n^*=
\min
\left\{
n:
Comp_n=1
\land A_n=1
\land N_{\mathrm{viol},n}=0
\land\hat H(Q|R_n)\le\epsilon
\right\}.
}
$$

If no level satisfies this:

$$
\boxed{n^*=\mathrm{NULL}}
$$

and that is a valid scientific result.

Do **not** lower the completeness requirement to manufacture an \(n^*\).

---

# Phase 11 — Boundary diagnosis

If a valid boundary exists, evaluate:

$$
B_{n^*}:R_{n^*}\rightarrow R_{n^*-1}.
$$

Report independently:

### Representation

$$
N_{\mathrm{viol}}>0
\quad\lor\quad
\hat H(Q|R)>0
$$

### Contract

$$
A(R)<1
$$

### Operator

$$
\hat H(Q|R)=0
\land F(R)<1.
$$

These are **diagnostic dimensions**, not necessarily mutually exclusive categories.

---

# Phase 12 — Final audit report

The generated report must contain exactly these sections:

```text
1. Corpus Inventory
2. Source Schema
3. Denominator Reconciliation
4. Q Independence Audit
5. Representation-State Audit
6. Transformation Registry
7. Completeness Matrix
8. Level Metrics
9. n* Determination
10. Boundary Diagnosis
11. Unresolved / Unobservable Fields
12. Provenance Map
13. Machine-Readable Dataset
14. Audit Verdict
```

The final verdict must distinguish:

```text
VERIFIED
DERIVED
UNOBSERVED
AMBIGUOUS
CONTRADICTORY
```

and must never turn `UNOBSERVED` into a positive empirical finding.

---

## One change I would make to the Python implementation

Your proposed auditor is good enough for production after adding a **support audit** and canonical equality layer. I would also change the empty-level entropy values from `0.0` to `None`.

Why?

If

$$
N_n=0,
$$

then entropy was not measured. It is not legitimately zero.

So:

```python
H_Q_given_R=None
H_R_given_Q=None
H_R=None
```

is scientifically cleaner than:

```python
H_Q_given_R=0.0
```

because zero entropy means a measured deterministic relationship, whereas no observations means **undefined**.

---

# What I can provide now vs. what requires the corpus

### I can provide now

* the frozen dataset contract;
* the production auditor;
* the schema;
* denominator reconciliation logic;
* canonicalization logic;
* completeness calculation;
* \(N_{\mathrm{viol}}\) calculation;
* entropy calculations;
* \(n^*\) determination;
* boundary classifier;
* audit/report format;
* an implementation prompt for Claude Code/Codex.

### I cannot honestly provide yet

The actual values of

$$
A_n,F_n,N_{\mathrm{viol},n},
\hat H(Q|R_n),
\hat H(R_n|Q),
\hat H(R_n)
$$

or the actual \(n^*\), because that requires the **raw KR-ZERO records**.

And this is important: **we should not use the previously reported 89.7%, 1.8%, 0.2%, and 8.24% as substitutes for these metrics.** Those are results from the KR-ZERO order experiment, not results of the new representation-reduction experiment.

So the correct deliverable at this point is the **corpus adapter/audit implementation**, followed by execution against the actual KR-ZERO files.

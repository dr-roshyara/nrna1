For the **production auditor / KR-REP audit**, I would want these files, in this order:

1. **`corpus/cases.jsonl`** — **primary file**

   * 1,395 level-test records.
   * This is the correct published unit for validating the KR-ZERO aggregate results.
   * It contains the fields needed for the auditor: `D`, transformation, contract, `subset_size_m`, `minimal_k`, `irreducible`, etc.

2. **`corpus/subsets.jsonl`** — **secondary / structural file**

   * 14,194 individual subset-level records.
   * This is what we need if we want to inspect the actual `S`-level Zero behavior, collisions, and higher-order elimination witnesses.
   * It lets us connect the aggregate level-test grain to the individual subsets.

3. **`property-results.json`** — **aggregate verification reference**

   * Only ~4.7 KB.
   * Not a corpus, but essential as the published ground truth against which `cases.jsonl` should reconcile:

     * 1,395 cases
     * k=1: 1,252
     * k=2: 25
     * k=3: 3
     * irreducible: 115

4. **`emit_corpus.py`** — **reproducibility source**

   * Important because it establishes how the 1,395 records were generated.
   * The recorded seed and deterministic pipeline let us verify that the materialized corpus isn't merely a hand-produced reconstruction.

5. **`CORPUS-AUDIT.md`** — **audit context**

   * Useful for understanding exactly what was already established and what claims are intended to be supported.

### What I do **not** need

I would **not** use the old 481 irreducible-witness file as the primary dataset. It is a tail sample, not the population.

And I would **not** ask for or construct a `KR-REP-DATASET` from the existing KR-ZERO R1–R4 labels. As you correctly discovered, those are **representation classes**, not the sequential reduction hierarchy

`R5 → R4 → R3 → R2 → R1`.

That would be a category error.

### So, if you want me to inspect the actual corpus now

**Give me / attach `corpus/cases.jsonl` first.**
If available, include `corpus/subsets.jsonl` as well.

With those two I can inspect the actual records rather than hypothesize, and determine precisely:

* whether the schema supports the frozen KR-ZERO claims;
* whether the 1,395 denominator is internally consistent;
* how `minimal_k` and `irreducible` are encoded;
* whether the 115 irreducible cases can be independently characterized;
* how the case-level and subset-level grains relate;
* whether the production auditor should operate on `cases.jsonl`, `subsets.jsonl`, or both;
* and what, if anything, is still missing for the **new KR-REP-DATASET generating experiment**.

**Most important:** don't generate anything else yet. The materialized `cases.jsonl` is exactly the artifact we needed to inspect.

# `OQ-<nn>` — `<short title>`

| | |
|---|---|
| **ID** | `OQ-<nn>` |
| **Stage** | `<NN-stage>` |
| **Kind** | open question |
| **Provenance** | `DERIVED` |
| **Status vector** | `DERIVED` · `NOT-EVIDENCED-IN-CAPTURE` · `N/A` · `ACTIVE` |
| **Raised** | `<YYYY-MM-DD>` |
| **Owner** | `<role — or UNASSIGNED>` |
| **Route** | `<which stage or act can answer it>` |

> **An unowned question is recorded as `UNASSIGNED`, never silently assigned to whoever happened to
> raise it.**

## 1. The question

`<as precisely as it can be stated. If it cannot be stated precisely, say so — an imprecise question
is a finding about the question.>`

## 2. Why it is open

`<what is missing: evidence, a decision, a definition, authority>`

## 3. What would answer it

`<the evidence or act that would close it>`

## 4. What it blocks

`<what cannot proceed until this is answered — or NOTHING RECORDED>`

## 5. History of this question

*Append-only. Each entry dated. A question that changes meaning is **not** renamed — a new `OQ-nn`
is opened and this one records the link.*

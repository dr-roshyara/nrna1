# `F-<nn>` — `<short title>`

| | |
|---|---|
| **ID** | `F-<nn>` |
| **Stage** | `<NN-stage>` |
| **Kind** | finding |
| **Provenance** | `DERIVED` — a finding is the agent's |
| **Claim layer** | `FINDING` |
| **Governance** | `NONE-RECORDED` |
| **Status vector** | `DERIVED · FINDING · <evidence_status> · <identity_status> · ACTIVE · NONE-RECORDED` |
| **Found in** | `<record / session / source>` |
| **Anchor** | `<heading or verbatim quote — never a line number (§C9)>` |

> **A finding is the agent's record. It is never the source's.**
> If a participant said it, that statement is an `UTTERANCE` and it is **cited here** — it is not
> restated as this finding (§C4.3). A finding that has swallowed the utterance it rests on has
> destroyed the evidence layer.

## 1. The finding

`<one paragraph. State what was found, not what it means.>`

## 2. What says this

`<the evidence. A verbatim quote with its anchor wherever possible. Where the evidence is a
participant's statement, cite the `UTTERANCE` record by ID.>`

## 3. What would make this wrong

**Required.** A finding with no stated falsifier is **not yet a finding** — it is a hypothesis. If
no falsifier can be stated, say so explicitly and set `Claim layer: HYPOTHESIS` instead. *The
falsifier is what separates `FINDING` from `HYPOTHESIS` — there is no other difference, and neither
is higher than the other (§C4.1).*

## 4. What it does NOT claim

`<the boundary of the claim — the nearest thing it is easily mistaken for, said plainly>`

## 5. Consequence

`<what follows if it holds — or NONE RECORDED>`

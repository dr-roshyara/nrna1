# KnowledgeOS / EKS Backlog — Index

Backlog items for the EKS / KnowledgeOS platform. **A backlog item records a problem and a candidate requirement; it commissions nothing** — starting any item requires a Human/PO/ARB authorization act, routed through Governance.

| ID | Title | Status | Class |
|---|---|---|---|
| [EKS-01](EKS-01-governed-knowledge-distribution.md) | Governed-Knowledge Distribution — rules must reach the session at startup | BACKLOG | architecture problem |
| [EKS-02](EKS-02-subject-derived-placement-enforcement.md) | Subject-Derived Placement, machine-enforceable (Knowledge Space) | BACKLOG | architecture capability |
| [EKS-03](EKS-03-historical-document-relocation.md) | Governed relocation of historical misplaced documents | BACKLOG · low urgency | governed cleanup |
| [EKS-04](EKS-04-lifecycle-handoff-ambiguity.md) | Four-Session Lifecycle Handoff Ambiguity — HANDOFF vs COMPLETE | BACKLOG | operating-model problem |
| [EKS-05](EKS-05-knowledge-evidence-boundary.md) | Knowledge Evidence Boundary — separating Execution State from Governance Evidence | **FUTURE ARCHITECTURE EXPLORATION** | strategic-DDD boundary problem |
| [EKS-06](EKS-06-reference-register-identifier-families.md) | Reference resolution bound to a fixed token list, not to declared identifier families | BACKLOG | assurance-coverage problem |
| [EKS-07](EKS-07-multi-process-coordination.md) | Multi-Process Coordination & Shared Work-State Integrity for AI Engineering | **FUTURE ARCHITECTURE EXPLORATION / OBSERVED PROBLEM** | platform-capability / coordination-boundary problem |
| [EKS-08](EKS-08-representation-semantics-separation.md) | Representation–Semantics Separation & Measurement Independence — KnowledgeOS invariants evaluated against semantic structure, not representation heuristics | **FUTURE ARCHITECTURE EXPLORATION** (P4/P5 research observation) | candidate constitutional invariant (research) |
| [EKS-09](EKS-09-activation-write-atomicity.md) | Activation write atomicity — a multi-append governed sequence has no transaction boundary | BACKLOG · ARCHITECTURE | architecture problem (transaction boundary) |
| [EKS-10](EKS-10-activation-failure-path-coverage.md) | Activation failure-path coverage — the live-owner combination is unprotected by a committed test | BACKLOG · ENGINEERING | assurance-coverage problem |
| [EKS-11](EKS-11-sole-writer-environment-override.md) | The "sole writer" is redirectable by environment — an asymmetric, unbounded test seam | **BACKLOG · HELD BY STANDING DECISION** | architecture problem (assurance boundary) |
| [EKS-12](EKS-12-theory-governance-scope-gap.md) | No governance authority has evidenced scope over **KnowledgeOS theory** | ⛔ **CLOSED · WORKING AS DESIGNED** (2026-09-08) | governance-scope problem |
| [EKS-13](EKS-13-cross-lane-dependency-without-change-notification.md) | A conclusion in one lane depends on another lane's document, with no change notification | **BACKLOG · OPERATIONAL EXPOSURE** | cross-lane dependency / notification gap |
| [EKS-14](EKS-14-research-construct-named-as-a-product-component.md) | A research construct is named as if it were a KnowledgeOS component, and 50 documents use it | **BACKLOG · NAMING & ATTRIBUTION EXPOSURE** | naming / attribution risk |
| [EKS-15](EKS-15-out-of-root-evidence-admission-mechanism.md) | No formal mechanism for admitting out-of-corpus-root evidence into a research programme — three instances so far, each reinvented from scratch | BACKLOG | operating-model problem |
| [EKS-16](EKS-16-derivation-without-consulting-existing-theory.md) | A research lane repeatedly derives conclusions without first checking whether the estate already answered the question — five consecutive occurrences | **BACKLOG · OPERATIONAL EXPOSURE** | operating-model problem (research search discipline) |
| [EKS-17](EKS-17-duplicate-research-roots-split-one-programme.md) | Two folders share the same name at different levels, and one research programme is split across both with no overlap | **BACKLOG · NAVIGATION HAZARD** | operating-model problem (repository navigation / discoverability) |
| [EKS-18](EKS-18-open-questions-register-with-no-route-to-the-work.md) | A register of open questions exists, and the people producing the answers do not know it is there | **BACKLOG · OPERATIONAL EXPOSURE** | operating-model problem (matching open work to the work that would close it) |
| [EKS-19](EKS-19-no-registry-of-already-spoken-for-directories.md) | No registry of already-spoken-for directories — overlap with off-limits/frozen work is rediscovered by hand each time | **BACKLOG · OPERATING-MODEL EXPOSURE** | operating-model problem (research-boundary discoverability) |
| [EKS-20](EKS-20-derivations-that-do-not-record-their-sources.md) | A research result that does not record which sources it used cannot later be shown to be independent, original, or free of duplication | **BACKLOG · OPERATIONAL EXPOSURE** | operating-model problem (provenance recording) |
| [EKS-21](EKS-21-negative-findings-have-no-positive-control.md) | A search that fails silently reports "nothing found", and "nothing found" is this programme's most common published result | **BACKLOG · MEASUREMENT VALIDITY** | operating-model problem (validity of negative findings) |
| [EKS-22](EKS-22-a-large-body-of-theory-with-no-status-and-no-anchor.md) | Twenty-three documents of mathematics carry no status, no index and no stated relationship to the frozen baseline | **BACKLOG · LIFECYCLE & AUTHORITY** | operating-model problem (status/lifecycle of research output) |
| [EKS-24](EKS-24-thread-conclusions-live-in-files-no-search-ranks.md) | The corpus is a saved conversation, so a discussion's conclusion sits in a later, smaller file that no search will rank | **BACKLOG · DISCOVERABILITY (STRUCTURAL)** | operating-model problem (how the record must be read) |
| [EKS-25](EKS-25-a-commissioned-programme-stopped-awaiting-a-review-that-has-not-come.md) | A commissioned verification programme stopped itself for supervisory review, and the review has not come | **BACKLOG · BLOCKED WORK** | operating-model problem (completed work awaiting an unowned decision) |
| [EKS-26](EKS-26-completed-coverage-claims-decay-silently-as-the-corpus-grows.md) | A completed survey's coverage claim keeps its confident wording while the thing it surveyed keeps growing | BACKLOG · EVIDENCE DECAY | operating-model problem (durability of coverage statements) |
| [EKS-27](EKS-27-the-same-mathematical-symbol-means-two-different-things.md) | The same mathematical symbol denotes two different things in two work streams, and a formula cannot be disambiguated | BACKLOG · NOTATION COLLISION | operating-model problem (shared notation across work streams) |
| [EKS-29](EKS-29-a-construct-is-credited-to-the-wrong-document.md) | An eleven-part construct is credited throughout to a document that contains five parts and none of the other eight | BACKLOG · ATTRIBUTION | operating-model problem (provenance within one work stream) |
| [EKS-30](EKS-30-no-register-of-a-lanes-own-settled-questions.md) | A research programme has no register of which of its own questions are settled, so the same question is commissioned and paid for twice | BACKLOG · DUPLICATE SPEND | operating-model problem (research output management) |
| [EKS-31](EKS-31-externally-authored-analysis-files-enter-the-primary-corpus-without-provenance-markers.md) | A file authored using this reconstruction's own findings was saved into the primary research corpus with no filename/header marking it as external or derivative | BACKLOG · DATA-HYGIENE / PROVENANCE-INTEGRITY EXPOSURE | operating-model problem (corpus provenance integrity) |
| [EKS-32](EKS-32-a-ruling-that-fixes-the-kernel-size-is-in-no-governance-register.md) | A ruling that fixes the size of the persistence kernel exists only in the working notes of the lane that consumes it | BACKLOG · GOVERNANCE TRACEABILITY | operating-model problem (where governance decisions are recorded) |
| [EKS-33](EKS-33-a-binding-ruling-governs-a-register-that-cannot-carry-it.md) | A binding ruling governs a field on a register that cannot reliably carry it | BACKLOG · GOVERNANCE ENFORCEABILITY | operating-model problem (can the governed thing bear the rule?) |
| [EKS-34](EKS-34-a-naming-collision-split-across-an-information-barrier.md) | The same short names mean two different things on opposite sides of an information barrier, and neither side can see the clash | BACKLOG · CROSS-LANE CORRECTNESS | operating-model problem (naming across a deliberate research boundary) |
| [EKS-35](EKS-35-file-timestamps-do-not-encode-argument-order.md) | File timestamps do not encode argument order, so the prescribed way of reading the record can reconstruct a discussion backwards | BACKLOG · RESEARCH-METHOD CORRECTNESS | operating-model problem (how the research record must be read) |
| [EKS-36](EKS-36-two-same-day-research-threads-on-sibling-evaluation-questions-never-cross-reference.md) | Two same-day research documents in the same folder both build a "structured evaluation of a claim against evidence" apparatus and never cite each other | BACKLOG · RESEARCH-COORDINATION EXPOSURE | operating-model problem (research-lane coordination within one corpus directory) |
| [EKS-41](EKS-41-req-req-symbol-denotes-two-unrelated-formal-objects.md) | The symbol `ℛ_req` denotes two unrelated formal objects (a requirement set vs. a distinction-preservation universe) in two same-day math-lane threads that never cross-reference | BACKLOG · NOTATION COLLISION | operating-model problem (shared notation across work streams within one corpus) |
| [EKS-42](EKS-42-two-documents-share-one-section-numbering-space.md) | Two different documents number their sections identically (`272A.1`…`272A.29`, same range end to end), so a citation of the form "see 272A.17" identifies two places with different content | BACKLOG · CITATION INTEGRITY | operating-model problem (whether a reference reaches a unique location) |
| [EKS-43](EKS-43-a-sanskrit-word-and-a-core-formal-operator-share-one-name.md) | The three letters `Sat` name both the central satisfaction operator `Sat(K,r)` and the Sanskrit metaphysical term (`Sat`/`Asat`/`Mithya`), in two live lanes that never cross-reference | BACKLOG · NOTATION COLLISION | operating-model problem (a search for an operator returns two unrelated bodies of work) |
| [EKS-46](EKS-46-a-qualified-result-becomes-an-unqualified-one-three-minutes-later.md) | A minimality result stated as holding *relative to a transformation set* is restated three minutes later as flatly "PROVEN", with an instruction not to reopen it; the qualification never returns | BACKLOG · CLAIM INTEGRITY | operating-model problem (scope conditions do not survive transmission between artifacts) |
| [EKS-37](EKS-37-the-highest-leverage-item-is-typed-as-both-a-derivation-and-a-decision.md) | The programme's highest-leverage open item is described as a *derivation* by one authority and a *decision* by another, so nobody can be asked to do it | BACKLOG · WORK-ROUTING | operating-model problem (who owns a piece of work) |
| [EKS-38](EKS-38-two-decision-registers-share-one-id-space.md) | Two separate registers of pending decisions use the same identifiers for different decisions, so an instruction to "enact N-4" has two possible meanings | BACKLOG · GOVERNANCE CORRECTNESS | operating-model problem (identification of pending decisions) |
| [EKS-39](EKS-39-a-research-object-silently-changed-from-a-test-into-a-list.md) | A central research object silently changed from a *test* into a *list*, and the programme's current blocker is the direct consequence | BACKLOG · SEMANTIC DRIFT | operating-model problem (how a concept's type is allowed to change) |
| [EKS-40](EKS-40-one-batch-gives-contradictory-dispositions-of-the-same-item.md) | Documents written in the same batch give contradictory dispositions of the same item — one calls it open, another calls it ratified — and nothing reconciles them | BACKLOG · DISPOSITION INTEGRITY | operating-model problem (what the record's own position is) |
| [EKS-23](EKS-23-two-independent-capability-vocabularies-never-reconciled.md) | Two independent research efforts each invented their own list of Kernel capabilities, and neither knows the other's list exists | **BACKLOG · OPERATING-MODEL EXPOSURE** | operating-model problem (research-output reconciliation / semantic duplication detection) |

Created 2026-08-16 on the PO/ARB act *"record this as problem and write EKS- tickets"*, from the placement-drift and knowledge-distribution incidents (`../reviews/2026-08-16-knowledge-placement-requirement-registration.md`).


**EKS-09/10/11 added 2026-08-24** on the PO/ARB follow-up-disposition act, from the `AST-019` `REPAIR-001` re-verification findings (`RV-F1`, `RV-F2`, `F-5`/`RV-O3`). All three were classified **NON-BLOCKING** and are **carried by**, not contradicted by, the `AST-019` adoption and authorization of the same date. `EKS-09` is adjacent to `EKS-07` and records first-hand corroborating evidence for it; it is kept separate because the remedy space differs (atomicity vs. cross-process awareness) — see `EKS-09` §4 for the `ES-005.4` check.

**EKS-15 added 2026-09-08** (filed as `EKS-12`, renumbered three times — to `EKS-13`, `EKS-14`, and
finally `EKS-15` — after the same concurrent session independently filed its own unrelated ticket at
each of `EKS-12`/`EKS-13`/`EKS-14` in turn; see rows above; no external reference to any superseded
number existed yet) from the `three_model_convergence` research programme's own MD-034
characterization study — a recurring operating-model gap (no reusable admission procedure for
out-of-corpus-root evidence), independently converged on across three separate instances (`03`/`04`,
`06`, and a recommended `12-randomized-results.md`). Checked against `EKS-06` before filing
(`ES-005.4`, never a copy) — a different problem in a different subsystem (a research-corpus
admission workflow vs. `EKS-06`'s Track-2 intra-document reference checker), not a duplicate. **Three
independent numbering collisions with the same concurrent session, on the same day, are recorded as
first-hand corroborating evidence for `EKS-07`** (multi-process coordination) — see that ticket's
own note, updated accordingly.


**EKS-12 added 2026-09-08** by Lane T (theory extraction) under the operating model's own **§37** — *"when a deeper requirement is discovered: record it as a follow-up and STOP"*. It arose from the `P-42`–`P-45` adoption/authority audits, which found a real and exercised adoption mechanism but **no authority with evidenced scope over KnowledgeOS theory**. ⛔ **It proposes no authority and may legitimately be closed as *working as designed*** — see `EKS-12` §5.

**EKS-12 CLOSED 2026-09-08** on the human research owner's act — *"It is a deliberate boundary — close EKS-12 as working as designed"*. The absence of a governance scope over KnowledgeOS theory is **intended**, joining `ES-006`'s deliberate exclusion of Project Knowledge as a second reasoned boundary. ⭐ **Consequence: KnowledgeOS theory is permanent research by design; the governed boundary sits at the specification/asset line.** ⛔ The closure commissions nothing and makes no statement about the theory's correctness.

**EKS-13 added 2026-09-08** from `P-47`. A load-bearing conclusion in the persistence theory depends on a single sentence in a document owned by the *other* research lane, and the intake record's own `X3` warning — *"no mechanism propagates that to Lane T"* — is currently the only safeguard. ⭐ The lanes' independence is deliberate and must be preserved; ⛔ this item proposes **no** mechanism.

**EKS-14 added 2026-09-08** from `P-50`, which measured that *"persistence kernel"* appears **zero** times in the source corpus while `the Kernel` appears in 610 files — the object is the research lane's own assembly of corpus-witnessed obligations. ⭐ The eleven obligations are evidence-backed; the **name** reads as a product component. ⛔ No rename is proposed; urgency is low today but rises with the first downstream artifact that cites it.

**EKS-16 added 2026-09-08** from `P-54`. Registered for a **pattern, not an incident**: five consecutive theory-extraction reviews reached conclusions and only afterwards found that the estate already held material bearing directly on the question — most seriously a **3 768-line** estate document on *"what information must survive persistence"*, dated **one day before** the two-day persistence derivation began and never opened. ⭐ Three of the five were caught by the human research owner or by chance, so **detection is currently by luck**. ⛔ The item proposes **no** procedure and questions **no** finding; the candidate direction is only that a research derivation should record the result — including a negative result and its scope — of having searched the estate first, mirroring the canonical-discovery step the engineering operating model already requires. Checked against `EKS-13`, `EKS-14`, `EKS-15` and `EKS-07` before filing (`ES-005.4`, never a copy) — distinct on all four counts; see `EKS-16` §6.

**EKS-17 added 2026-09-09** from `P-55`. Two folders are both named `research` — one at the repository root (253 files), one under the knowledge area (69) — and the **kernel-reduction** programme is split across both with **zero files in common**: the executable study on one side, the twenty written documents on the other. ⭐ A search of the wrong half does not fail; it returns results, so nothing signals that anything is missing, and *"check the research folder"* has two correct readings. ⚠️ It fired once already — `P-54` published a coverage statement narrower than it read — with the conclusion unaffected only because the missed material fell outside its date window. ⛔ The item claims **neither location is wrong** and proposes **no move**; the candidate direction is only that each half should say the other exists. Checked against `EKS-16`, `EKS-02`, `EKS-03` and `EKS-06` before filing (`ES-005.4`, never a copy) — distinct on all four; see `EKS-17` §5.

**EKS-18 added 2026-09-09** from `P-56`. The Final Architecture phase (2026-08-28) produced a register of **twelve open questions, each stating exactly what would resolve it**. ⭐⭐⭐ `OQ-2` asks for a *"formal irreducibility analysis"* of kernel membership — and a separate work stream has produced twenty-eight consecutive reviews of exactly that since 2026-09-07, with neither side aware of the other. ⭐ The register is the best artifact in the story; the failure is **routing**, not knowledge — a question blocked on reaching the work that answers it is far cheaper to fix than one blocked on the answer. ⚠️ The instance surfaced only by accident, and **eleven further questions carry the same shape** with no way to tell whether any is in the same position. ⛔ The item proposes **no** process and asks nobody to merge or redirect either work stream. Checked against `EKS-16`, `EKS-17`, `EKS-13` and `EKS-07` before filing (`ES-005.4`, never a copy) — distinct on all four; see `EKS-18` §5.

**EKS-19 added 2026-09-09** (filed as `EKS-18`, renumbered once — to `EKS-19` — after the same
concurrent Lane-T session independently filed its own unrelated ticket at `EKS-18` first; no external
reference to the superseded number existed yet) from the `three_model_convergence` research
programme's own MD-042 cross-landscape audit — a recurring operating-model gap (no registry of
directories already known to belong to a separate research initiative or to an already-frozen part of
this same programme), converged on **four separate times in one day** (`reviews/synthesis/`,
`reviews/kernel/`, `brainstorming/verification/`'s `step-272` overlap, `brainstorming/synthesis/`'s
extraction-named files) — each caught only by manual filename inspection, with no shared record of the
boundary to consult. Checked against `EKS-15`, `EKS-16`, `EKS-17` before filing (`ES-005.4`, never a
copy) — distinct on all three (admission procedure vs. search discipline vs. split-folder navigation
vs. this item's own "is this already someone else's territory" question). **A fourth same-day backlog
numbering collision with the concurrent Lane-T session is recorded as further corroborating evidence
for `EKS-07`** (multi-process coordination) — see that ticket's own note.

**EKS-20 added 2026-09-09** from `P-57`. An audit asked a factual question — did this research result draw on material an earlier authorized review had already examined? ⭐⭐ One side reconstructed completely (a census with date range and an honest per-folder read depth); ⭐⭐⭐ the other could not be reconstructed **at all**, because the result names **no source material whatsoever** — only three of its own earlier documents and one git observation, and its predecessors name none either. ⇒ **Independence is not a claim the record can support or deny.** ⭐ The estate already has the remedy — `FA-7` is a provenance register with graded evidence classes — so this is an existing practice one work stream does not follow, not a missing capability. ⚠️ Urgency is **higher than its neighbours and rising fastest**: unlike them, the missing information is destroyed by the passage of time, and roughly thirty documents already exist in this pattern. ⛔ No finding is questioned — saying one were derivative would need exactly the record that is absent. Checked against `EKS-16`, `EKS-17`, `EKS-18` and `EKS-13` before filing (`ES-005.4`, never a copy) — distinct on all four; see `EKS-20` §6.

⚠️ **Filed at 20 after `EKS-19` was taken mid-write by the same concurrent session — the fifth numbering race in this backlog, recorded as further evidence on `EKS-07`.**

**EKS-18 second instance recorded 2026-09-09** from `P-58`. A **second** register of open work — twenty numbered items on the theory's mathematics, dated 2026-09-04, five marked foundational blockers, each stating formally what would resolve it — was likewise unknown to the work stream that has spent thirty consecutive reviews on exactly those questions. ⚠️ **The rediscovered material came back weaker than the register already had it** (one concept described as *"undefined"* that the register had defined, naming the single remaining gap). ⛔ **No new item opened** — under `ES-005.4` a second occurrence belongs on the existing record, and it is appended there.

**EKS-21 added 2026-09-09** from `P-60`, with a **confirmed live instance caught by luck inside the review that filed it**. Four searches returned zero and the review was about to conclude the relationship was absent from the estate; they had been issued from the wrong folder, so the paths did not exist and each reported *"no matches"* rather than *"that location is not there."* Corrected: **917 · 13 · 10 · 43** documents against four findings of *"nothing exists."* ⭐⭐ A wrong positive gets challenged because someone reads the evidence; **a wrong negative has no evidence to read**, so nobody checks — and negative findings are load-bearing here. ⭐⭐⭐ The corpus search rule **already requires a positive control** for absence claims; the finding is that **it was not applied and nothing noticed** — a discipline that only works when nobody slips is not a control. Checked against `EKS-16`, `EKS-17`, `EKS-20` and `EKS-18` before filing (`ES-005.4`, never a copy) — distinct on all four; see `EKS-21` §6.

**EKS-22 added 2026-09-09** from `P-61`. A complete end-to-end **rewrite of the theory** — 23 documents covering foundations, type system, transitions, evidence, revision, identity, time, persistence and architecture — was produced on 2026-09-06, and ⭐⭐⭐ **nobody can tell what it is**: zero status markers on any claim, zero mentions of the frozen baseline version in any of the 23, no index, no supersession statement. ⚠️ It was written **two days after** the estate adopted a status vocabulary whose own rule is that *"an untagged mathematical statement is a defect"*. ⭐⭐ It is already being read as settled — a recent review compared a research result against one of its documents in detail — and for several subjects, persistence included, it is the estate's **only** treatment, so it can neither be ignored nor safely relied on. ⛔ The item questions **none** of the content and proposes **no** status; the candidate direction is three lines at the front of the set — *what this is · what it supersedes · what may be relied on* — a practice the 2026-09-04 theory set already demonstrates. Checked against `EKS-14`, `EKS-18`, `EKS-20` and `EKS-21` before filing (`ES-005.4`, never a copy) — distinct on all four; see `EKS-22` §5.

**EKS-23 added 2026-09-09** from the `three_model_convergence` research programme's own MD-046. A 2026-09-04 research effort produced a thirteen-item candidate list of Kernel capabilities; a separate, earlier (2026-08-19–08-28) research track had already independently produced its own nine-item candidate list — plus a further, internally-competing four/six-item list within that same earlier track — for the identical underlying question. **No name on any list matches a name on any other list, and no document anywhere cross-references the others.** ⭐ The earlier track's own review work had already logged three separate warning signs of exactly this kind of naming confusion (a running "word used in conflicting senses" registry), and its own two internal lists already disagree with each other about whether the Kernel should be small or larger — neither tension ever reconciled. ⛔ The item claims no list is wrong and proposes no merge or choice between them; the candidate direction is only a lightweight habit of checking a new candidate list against previously-produced ones for the same kind of object. Checked against `EKS-17`, `EKS-18`, `EKS-14` and `EKS-16` before filing (`ES-005.4`, never a copy) — distinct on all four; see `EKS-23` §2.

**EKS-22 updated 2026-09-09** from `P-62` — **lineage resolved, status gap confirmed.** The rewrite was **commissioned deliberately** (a planning note seven minutes before the first document: *"a proper theory rewrite, not another incremental patch"*, with a plan the 23 parts follow), its **named predecessor is not the frozen baseline** but an earlier *"theory closed"* declaration that **had already been withdrawn** — ⭐⭐⭐ **which explains the missing version number: there was no valid version to inherit** — and it was **not abandoned** (finished, renamed, catalogued, and one section independently cross-checked by the other lane). ⚠️ **What remains missing is the whole of the exposure:** no status, no owner, no adoption — and one day after completion the programme's own status board still names the **older** frozen version as the baseline and does not mention the rewrite at all *(six controlled probes, all zero, positive control 21)*. ⭐ The item is now **easier to act on**: the remaining decision is narrow — current theory, superseded attempt, or resource to draw on.

**EKS-18 third instance recorded 2026-09-09** from `P-66`, and it is the most costly of the three. The estate holds a **completed executable verification of its own theory** — 32 documents, experiments A–Z, an experiment register and a final verdict, with a sibling run against the previous version — whose *"what failed"* section reports a gap that a separate work stream has spent **thirty-eight reviews building an answer to**, with neither side aware of the other. ⭐⭐⭐ **The first two instances were unrouted *questions*; this is a finished *answer* — the strongest available external check on that work, never run against it.** ⚠️ And because the failure **reproduced across two successive theory versions**, it is a standing property the next version will inherit, not a transient defect. ⛔ No new item opened (`ES-005.4`); appended to this record.

**EKS-21 extended 2026-09-09** from `P-67` — **the same fault runs the other way, and that direction is worse.** A search for how much of the estate discusses one technical assumption returned **522 documents**; the pattern was matching an abbreviation's letters inside ordinary words, and the real matches were a security acronym (7), *"toward"* (4) and two forms of *"knowable"* (2). ⭐⭐⭐ *"522 documents discuss this assumption"* was about to be published; the true figure is effectively zero. **A false negative hides a finding; a false positive manufactures one** — and a large number survives scrutiny far better than a zero does. ⚠️ **The positive control in `EKS-21` §5 does not catch this direction** (a false positive already finds things); it needs the opposite check — when a count is surprisingly high, look at what actually matched. The candidate requirement is widened to cover **both**: a published count, zero or large, should carry evidence that the search measured what it claims to measure.

**EKS-14 second instance recorded 2026-09-09** from `P-68`. ⭐⭐⭐ **A disambiguation register for this exact overloaded term already exists**, dated **2026-08-29 — nine days before the construct was named** — listing **five distinct senses**, saying for each what it is, which are **not** mathematics, and which **do not claim** completeness. ⚠️ **Two of its five entries already use the same short label** the new construct adopted, and **a sixth sense was minted without consulting it.** ⭐⭐ This makes the exposure **worse and cheaper at once**: the confusion is **documented, not latent**, and most of it would be removed by **adding a sixth row to a table that already exists** — no rename needed. ⚠️ But an incomplete disambiguation register is **more misleading than none**, because a reader who finds it will reasonably assume it is complete. ⛔ No new item opened (`ES-005.4`); appended to this record.

**EKS-21 provenance corrected 2026-09-09** from `P-70`. ⚠️ **The item was filed as a new discovery and was not one.** A primary document from **two days earlier**, written by the same work stream that filed the ticket, already records the failure among its settled findings — *"a glyph-literal pattern over a LaTeX corpus produced a false zero"* — and also names the underlying mechanism as an established lesson. ⭐⭐⭐ **The item still stands and is now better evidenced, not worse:** its point was never that this can happen but that **nothing catches it**, and the failure was recorded, understood, named as a lesson, and **still recurred four times afterwards** — including in the review that filed the ticket. ⚠️ Two things change: the urgency assessment understated the case *(a documented repeat offence, not a risk)*, and the filing itself is an instance of the *"work proceeds without consulting what already exists"* pattern recorded elsewhere in this backlog. ⛔ No new item opened for either.

**EKS-24 added 2026-09-09** from `P-71`. Much of the record was saved one file per conversational turn, named by save time — a sound convention. ⭐⭐⭐ **But it puts a discussion's *conclusion* in a separate, much shorter file than the discussion**, and short dispositions rank nowhere in any search because their job is to dispose of something argued elsewhere. **Witnessed:** a review had read a **3 054-line** document in full and drawn on it across **four** successive reviews, never reading the next three files — **708, 201 and 70 lines** — which between them held the long document's **status** (*a specification, not a theory*), a *"stays frozen"* ruling, **four principles frozen as method**, ⭐⭐ **a twelve-step dependency order showing the reviews had been working on steps 10–12 while steps 1–4 were undone**, and **two conclusions later reported as original findings**. ⛔ **Searching well does not help** — the information is not mis-filed; the file simply gives a searcher no reason to open it, which makes this distinct from `EKS-16`, `EKS-17`, `EKS-18` and `EKS-21` *(see `EKS-24` §6)*. ⭐ Remedy is the cheapest on this list: **read forward in save order until the subject changes** — four files sufficed — and cite that forward read when claiming a document's status.

⚠️ **Sixth numbering race, 2026-09-09:** `EKS-24` was written as `EKS-23` and renumbered when the concurrent session filed its own `EKS-23` mid-write. Further evidence on `EKS-07`.

**EKS-25 added 2026-09-09** from `P-72`, and it carries **the highest urgency on this list for a reason none of the others has: the cost is already being paid.** On 2026-08-28 the programme commissioned an **independent verification track** over its own theory under an unusually strict mandate — *read, verify, falsify; never repair* — which even **forbade the word "validated"**. ⭐⭐ The work was completed: **eleven registers**, derivations with their **non-consequences** written out, a counterexample register recording **failed searches** as well as findings, **61 typed contradictions**, and nineteen findings across three waves. ⭐⭐⭐ **It then stopped exactly as its protocol required — *"session STOPPED pending supervision"* — and the review has not happened in eleven days.** ⚠️ **Meanwhile two other work streams proceeded past it and both re-derived conclusions it had already established** — including a refutation it had recorded and a dependency it had already named as a pending decision: **the estate paid three times for one result.** ⛔ No review is requested here and no finding endorsed; the candidate direction is only that *paused* and *forgotten* should not leave the same trace. Checked against `EKS-12`, `EKS-18`, `EKS-22` and `EKS-24` before filing (`ES-005.4`) — distinct on all four; see `EKS-25` §5.

**EKS-26 added 2026-09-09** from `P-73`. A coverage statement measures a **moving** population but is written as permanent: a **governance-recorded** census inventoried an area at **413** documents and declared coverage complete; the area now holds **564** at the same level — **151 added after the survey closed** — and the closing report is unchanged, so the claim's truth has quietly dropped from **100% to 73%** with nothing marking the change. ⭐⭐⭐ **The effect falls hardest on negative findings** — *"we inventoried the area and found no treatment of X"* stops supporting its conclusion once a quarter of the area postdates the inventory, and this estate's method leans heavily on negatives. ⚠️ Worse in case (a) because the survey's phase formally **completed and stopped**: the new material sits behind a closed gate while the report says the area was covered. ⭐ **Three occurrences in three forms**, all of careful work whose statements were **true when written** — nobody is at fault, which is what makes it hard to catch. ⛔ No survey criticised and no re-survey requested; the direction is that a coverage statement record **what it counted and when**, and an absence claim name **which population, at which moment**. Checked against `EKS-21`, `EKS-17`, `EKS-24` and `EKS-25` (`ES-005.4`) — distinct on all four; see `EKS-26` §6.

**EKS-14 third instance recorded 2026-09-09** from `P-74` — **a second overloaded term, and this one has no register at all.** *"Contract"* now carries **four** distinct technical senses: three are the same kind of thing *(a requirement set supplied as an input)* and the fourth is **a list of the fields each concept has** — a type declaration. ⭐⭐⭐ **The overload therefore crosses a TYPE boundary, not just a naming one**: a reader who supplies a field list where a requirement set was meant has not misread a label, they have supplied the wrong **kind** of object, and nothing warns them. ⚠️ **It nearly produced the opposite verdict inside the review that found it** — a search suggested three references to the requirement-set sense; checked, all three were the abbreviation's letters inside *"decision"* and *"execution"*, and the true count was **zero**. ⭐ **The asymmetry is instructive:** for the first word a register exists and was not kept current; for this one there is nothing to keep current — **two failures needing different responses.** ⛔ No rename proposed, no document criticised, no new item opened (`ES-005.4`).

**EKS-26 fourth instance & EKS-24 corroboration recorded 2026-09-09** from `P-75`. ⭐⭐⭐ **`EKS-26`:** the independent verification programme's own artifact index records its findings as running **1–19**; the findings themselves run to **88 distinct identifiers** — **69 unmentioned where a reader would look**, and among them a genuine mathematical error, a genuine falsification of **five of eight** stated laws, **six internal contradictions inside one document**, and an identifier-range collision. ⚠️ **Worst of the four instances**, because that material is **not** timestamp-ordered — the index *is* the only map — and the programme is separately recorded as **halted pending review** *(`EKS-25`)*: a completed body of work waiting for a reader, whose index tells that reader it is a quarter of its actual size. ⭐⭐⭐ **`EKS-24`:** the same programme recorded this item's failure mode as a finding — ***"a genuine mathematical error … correctly repaired eight steps later without anyone noticing"*** — raising `EKS-24`'s evidence from one reviewer's experience to an independently recorded finding, and showing the cost runs **both ways**: stopping early leaves you with an error you think is current **and** unable to credit a repair that was actually made.

**EKS-27 added & EKS-21 extended a third time, 2026-09-09**, from `P-77`. ⭐⭐⭐ **`EKS-27`:** two work streams label their changes with **the same Greek letter and the same numbering**, and they are **different kinds of object** — one a **function** from state to next state with pre/postconditions, the other **a kind of event observed happening** to records. A reader who equates *the third change* in one with *the third change* in the other produces nonsense **and makes no visible mistake.** ⭐⭐ **Worse than the two overloaded-word items already here, for a mechanical reason: a word can be disambiguated in a register, but a symbol inside a formula cannot — the formula *is* the definition, and there is nowhere to put the disclaimer.** ⚠️ It came close in the very review that filed it, which was asked to compare the two streams' change-accounts; avoiding it required returning to each stream's primary and asking what a numbered change *is*, not what it is called. ⛔ No renaming proposed — that would invalidate formulas on both sides. ⭐⭐⭐ **`EKS-21` third direction:** a search reported **a specific sentence that does not exist** — an excerpt **assembled across a line boundary** — which, had it stood, would have overturned a standing finding. **A wrong zero hides something and a wrong large number overstates something; this fabricated a quotable claim with no source**, in the most routine tool there is. ⇒ requirement widened: **verify a quoted excerpt as a contiguous match before using it as evidence.**

⚠️ **`EKS-27` provenance corrected the same day** *(from `P-78`)*: the item was **filed as new and is not new.** A **complete** reading of the verification lane's state-and-transition register — under the strengthened rule requiring forward documents to be read **in full** — found the problem already catalogued there on **2026-08-29** as ***"systematic glyph collisions"***, with the symbols enumerated, **including the very one the item was filed about**. ⭐⭐⭐ **The item stands and is stronger:** the collisions were catalogued eleven days ago and **nothing has been done**, which is what this list is for. ⚠️ Three things change — the count is **nine collision families**, not one symbol; collisions occur **inside single documents** too, not only across streams; and the register already states the remedy more sharply than the item did: because the sources are **three-layer** *(definition → rebuttal → revision)*, any consolidated record must carry **per-claim layer provenance**, since *"a claim's status depends on which layer asserts it."* ⛔ Filed as novel while the evidence sat in a 136-line register searched twice and **never read completely** — the failure the strengthened rule exists to prevent.

**EKS-28 added 2026-09-09**, from the three-model-convergence reconstruction's own MD-056. Two
separately-authorized HPA governance commissions — one adjudicating the state-object kernel question
(ruling `D-FA-4`/`GN-31`, open item `OQ-2`), the other adjudicating the operation-registry question
(`GN-79/80/83/85/86`, open items `P-1`…`P-11`) — operate over the same ratified primitives and
invariants and **never cite each other**, despite reaching strikingly similar terminal shapes
("substantial real work; registry not established; named prior decisions block it") within days of
each other. Not a claim either commission erred; the exposure is that nothing today would surface
either record to a future commission touching the same ground. Checked against `EKS-22`, `EKS-23`,
`EKS-25` (`ES-005.4`) — distinct on all three; see `EKS-29` §5.

**EKS-29 added 2026-09-09** from `P-79`. A work stream's set of **eleven obligations** is credited consistently to **one document** — which contains **five** of them and ⭐⭐⭐ **does not mention the other six even once.** Two intermediate documents were checked in case the split was gradual: **zero mentions each.** The whole split happened in a single later document, three reviews on. ⭐⭐ **Why it matters:** work done at the outset and work done after three rounds of internal challenge are **not equally settled**, so the attribution **makes six of eleven look older and steadier than they are**, and **hides which parts have actually been through challenge** — a reader tracing them to the credited document finds no argument there, because there is none. ⚠️ It has propagated to roughly **forty** documents and one backlog item, each simply copying an inherited shorthand. ⭐ **The correct source is known, dated and unambiguous**, so the remedy is one line naming both documents and which parts came from each. ⛔ Nothing about the obligations' content, evidence or standing is questioned, and no re-citation is requested. Checked against `EKS-20`, `EKS-14`, `EKS-24` and `EKS-26` (`ES-005.4`) — distinct on all four; see `EKS-28` §5.

**EKS-30 added 2026-09-09** from `P-81`. A research programme now holding **ninety-nine consecutive numbered reviews** tracks every question it still has to answer and ⭐⭐⭐ **keeps no list of the questions it has already answered.** The verdicts all exist — one per document, ninety-nine documents — so knowing whether something is settled means remembering that it is, or opening the right file. ⭐⭐ **What happened:** review **33** closed a specific structural question on 2026-09-08, with a full argument and an explicit reopening condition; the next day review **98** reached the same question from another direction and proposed it as *"the load-bearing question the theory leaves open"*; review **99** was commissioned to answer it and found in the first minute that ⭐⭐⭐ **the file name of review 33 is the question.** Same directory, five files from a document review 98 had just read end to end. ⚠️ **Worse:** review 98 also *endorsed* a structural claim that review **34** had already dismantled, and that endorsement stood in the published record until review 99 withdrew it — so for one day the programme's own output asserted two incompatible things about the same structure, and **a reader taking the newer document would have taken the wrong one.** ⭐ **Why it matters commercially:** in a programme whose entire output *is* reviews, re-answering is the most expensive possible failure — the work is bought twice and adds nothing; the forward plan still lists the finished question as remaining work; and asked *"what is settled?"* the only honest answer is *"read all ninety-nine"*, which means ⭐⭐ **the body cannot be handed to the governance act that must eventually adjudicate it.** ⚠️ The exposure **rises with the programme's own productivity.** ⛔ **Not a search problem and not a naming problem** — the file name *was* the question and the folder *was* the right folder; nobody thought to look at what the lane itself had produced. Candidate direction: one current register of settled questions carrying **the verdict AND the conditions it depends on**, since most closures here are explicitly boundary-relative and a register that dropped those conditions would be worse than none. Checked against `EKS-18` (its mirror image — open questions with no route to the work), `EKS-16` (an **input** search problem, whose remedy would *not* have caught this), `EKS-24`, `EKS-26` and `EKS-22` (`ES-005.4`) — distinct on all five; see `EKS-30` §6 and the 2026-09-09 appendix on `EKS-16`.

**EKS-32 added 2026-09-09** from `P-82`. A research programme's headline result is a number — **eleven** obligations a knowledge system must not lose — and ⭐⭐⭐ **it is eleven rather than ten because of a single decision, taken once, by a person.** The lane that received that ruling recorded it exemplarily: what was decided, by whom, its scope limits, ⭐⭐ **that it is a decision and not a measurement**, and the condition under which it would need re-confirming. ⛔ **But it wrote it into its own working notes and nowhere else.** The estate keeps a governance folder for exactly this kind of act — nine records, several of them explicit authorization/adoption pairs — and ⭐⭐⭐ **this decision is not among them; the folder's most recent dated entry predates it by two weeks.** ⚠️ **Why it matters commercially:** the programme exists to be handed to a governance body that will adjudicate and freeze it, and ⭐⭐ **whoever runs that act will look in the governance folder for the decisions the theory rests on and not find the one that fixes its headline number**; the theory scrupulously marks which results rest on decisions rather than evidence, ⭐ **but that marking is only useful if the decisions can be enumerated, and today that means reading a hundred research documents**; and the cost has already been paid once — ⚠️ **a later review read a document written *before* the ruling, did not know the ruling existed, and published a governance conflict that does not exist**, which stood until the next review withdrew it. ⭐⭐ **The leak is ongoing, not historical:** the forward plan already lists four ratification stamps and two decision records sitting in research lanes rather than the register, and this ruling is a **seventh, taken after that list was drawn up.** ⛔ **The decision itself is not questioned and the lane did not act improperly** — a well-made record was filed in the only drawer it had. Candidate direction: **registration and consumption are different acts** (a lane can only write *"we now reason from this"*, never *"this was decided"*), and ⭐ **a register entry must carry the decision's own qualification — recording a stipulation as a finding would be worse than no entry**, because it would silently upgrade the whole theory's status. Checked against `EKS-12` (**CLOSED**; a **scope** question — *does anyone have reach?* — where this is a **registration** question: an authority **did** act), `EKS-13` (already the registered home of the change-notification half — cited, not duplicated), `EKS-22`, `EKS-30` and `EKS-05` (`ES-005.4`) — distinct on all five; see `EKS-32` §6.

**EKS-33 added 2026-09-09** from `P-84`. A ruling is in force saying that a progress ladder concepts are meant to climb is **forward-only — nothing may slip back**. It is sensible and properly made, and a research programme's headline result depends on it. ⭐⭐⭐ **Nobody has checked whether the register it governs can carry it.** Three properties are already measured — **by the team that owns the register, which found and published its own correction**, so this item invents no number: ⚠️ **28 of 123 entries carry the status field at all**; ⭐⭐ **only 27 entries are concepts**, because the register **changed its indexing axis partway through** and the other **96 are file landmarks**; ⛔ and **0 entries have a stable identifier** — they are *"keyed by concept **name**"*. ⭐ **So: a forward-only rule over a field most rows do not have, on rows of two different kinds, with no identifiers.** ⭐⭐ **A file has no epistemic standing to advance or slip back**, so for three-quarters of the rows the rule is not merely unenforced — **it is not meaningful**; and **names in this estate change**, with **fifty-six renames recorded in a single day** as ordinary practice, so a rename breaks the key a rule needs in order to say *"this is still the same thing."* ⚠️ **Why it matters commercially:** the programme's headline number is what it is **because of this ruling**, so if the ruling cannot bite the number rests on something softer than it appears **and nothing in the record says so**; the programme reaches a governance stage at which those consequences are meant to be **frozen**, and ⛔ **freezing a number whose supporting rule may govern a fifth of its register is exactly what this item exists to prevent**. ⭐ **Nobody owns the question** — the register team measured its own gaps, the consuming lane recorded the ruling carefully, and **neither was asked whether the second applies to the first**; the mismatch appears only to someone who reads both, which is no one's job. Candidate direction: **the check belongs to adoption, not to enforcement**, and ⭐ **a negative answer is a useful result, not a blocker** — *"this rule governs 27 of 123 rows"* is a fine thing to record; **it is silence that causes the damage.** ⚠️ **Ownership is the point: the reporting lane cannot resolve this**, because both the register and the decision record defining the ladder sit outside the part of the estate it may read — **it must go to a reader who may open both.** ⛔ The ruling is not questioned and the register is not criticised. Checked against `EKS-32` (**same ruling, adjacent question** — that asks where the ruling is *filed*, this asks whether what it *governs* can carry it), `EKS-06`, `EKS-13`, `EKS-19` and `EKS-22` (`ES-005.4`) — distinct on all five; see `EKS-33` §6.

**EKS-34 added 2026-09-09** from `P-85`. Two research lanes on one programme are **deliberately kept apart** — each forbidden to read the other's working material, on purpose, because the separation is what makes their conclusions independent. ⭐⭐⭐ **Both independently adopted the same short labels — `F1`, `F2`, `F3`, `F4` … — for completely different things:** one lane means **six candidate semantic models**, the other means **twenty failure classes** *(representation failure, semantic-interpretation failure, relation failure, **discrimination failure**, …)*. ⭐⭐ **So `F3` is *"the only model with a working construction"* in one lane and *"relation failure"* in the other, and `F1`…`F6` is ambiguous in both families at once.** ⚠️ **Because the lanes cannot read each other, neither can discover this.** What happened: a reviewer asked to recover one lane's knowledge of candidate `F4` searched the material it *is* allowed to read, got **seventy-one matching documents**, and ⛔ **every one inspected was a different `F4`** — a failure class, a step identifier, a finding identifier, fitness-test identifiers. ⛔ **Nothing in the readable material said so**, because the disambiguating document is on the other side of the barrier; it was caught only because two handover reports written that afternoon happened to quote **both** lanes. ⭐ **Why it matters commercially:** a search that returns seventy-one confident hits about **something else entirely** is worse than one that returns nothing; and ⭐⭐ **the damage lands exactly at handover** — the barrier exists so the lanes can eventually compare independent results, that comparison is the first moment the labels sit side by side, and **a collision discovered then has already contaminated what was compared.** ⚠️ **Neither lane owns the problem:** each label is well defined inside its own lane, so neither did anything wrong and neither has reason to change — **the defect lives in the space between them.** ⛔ **And the usual remedy is impossible by design:** *"check whether the name is taken"* would mean reading the other lane's material. Candidate direction: ⭐ **names are not findings** — a shared list of *which labels are in use, by whom, for what kind of thing* leaks no evidence and prejudges no conclusion, and ⭐ **the check must be possible at search time, not only at handover.** ⛔ **The barrier is not the problem and should not be relaxed** — a correct separation simply has an unmanaged side effect; and no claim is made that any conclusion has actually been corrupted, only that **one near-miss is recorded** and the question can be answered only by a reader with access to both sides. ⚠️ **Ownership is the difficulty — neither lane can resolve it alone, which is the defect; it needs someone who can see both label sets, a governance position rather than a research one.** Checked against `EKS-27` (**same cause, closest neighbour** — but its collisions sit inside **one readable estate**, where a reader can find both meanings and a glyph register fixes it; here neither lane can detect the clash), `EKS-23`, `EKS-19` (structurally the same shape one level up — directories rather than identifiers), `EKS-13` and `EKS-31` (`ES-005.4`) — distinct on all five; see `EKS-34` §6.

**EKS-35 added 2026-09-09** from `106`. Much of the research record is a working conversation saved one file per turn and named by the moment it was saved, so ⭐ **the programme reads it chronologically** — find a document, read the ones saved after it, and the argument unfolds. ⭐⭐ **That method is sound and is used constantly**, and it rests on one assumption nobody has checked: **that the order files were SAVED is the order the argument was MADE.** ⛔ **On at least one whole block of one day, it is not.** A block of about a dozen documents from one morning was **re-saved that afternoon in exactly the reverse order** — ⭐⭐ **eleven of twelve pairs match, with the afternoon timestamps counting down exactly as the morning ones count up.** ⚠️ **Why it matters:** anyone following the rule on that block reads **conclusions before premises, corrections before the thing corrected, and an earlier draft as a later refinement** — so every judgement about *"what the thread concluded"* is inverted; ⭐⭐ **and the method cannot detect its own failure**, because timestamps alone show a perfectly clean ascending sequence and nothing in the filenames, lengths or keyword searches reveals the reversal — **it was found only by comparing content across the two blocks.** ⚠️ **About a dozen genuinely new documents are interleaved among the re-saves**, so the block cannot even be quarantined without checking content document by document; and **any earlier reading of that region had the same exposure**, with **no way to know which conclusions were affected.** ⭐ **The rate is not negligible** — two consecutive days measured at roughly **a tenth of each day's material being a byte-identical copy** of something else in the same folder. ⛔ **The chronological method is not wrong and should not be abandoned** — it is the right method and has repeatedly found what no keyword search would; **the defect is a precondition it depends on and does not check.** Candidate direction: **before a stretch is read as a developing argument, it should be cheap to establish that its save order is its argument order**, with two properties mattering more than format — ⭐ **the check must be cheap and routine** (a content comparison across a block is cheap; reading it twice is not), and ⭐ **a negative answer must be recordable**, since *"this block is a reversed re-save of that one"* is a permanent fact about the record and **today there is nowhere to write it down.** ⚠️ **Urgency is immediate because the method is in active use** — every commission currently instructs the reader to follow timestamps forward. Checked against `EKS-24` (**closest neighbour**, but it **assumes timestamps are reliable** and says *search* is not, so its own remedy is defeated by this), `EKS-31` (**counting**, where this is **ordering** — perfectly marked copies would still be reversed), `EKS-30`, `EKS-26` and `EKS-29` (`ES-005.4`) — distinct on all five; see `EKS-35` §6.

**EKS-37 added 2026-09-09** from `P-86`. One item sits at the **root** of the programme's remaining work — ⭐ **the list of distinctions the system is *required* to be able to make** — and everything downstream depends on it: how many evaluation outcomes the theory needs, whether contradiction can be represented, whether the satisfaction test can ever be computed. ⛔ **Two current, authoritative documents describe it in ways that send it to different owners:** the forward plan calls enumerating it ⭐ *"a **derivation**, not a decision"* and *"the most evidenced blocker in the estate"* (blocked in **66 of 66** tested worlds) ⇒ **research**; while the experiment report that same plan commissioned calls required distinctions ⭐ *"the **DECISION** that sets everything"* and places them at the root ⇒ **governance**. **Neither cites the other.** ⭐⭐⭐ **The difference is not academic:** a derivation is done by a researcher and settled by the corpus, a decision by an authorised body and settled by choosing — so **the same silence in the corpus means *"we are stuck"* under one reading and *"this is exactly when someone must choose"* under the other. The two readings give opposite instructions.** ⚠️ **Why it matters commercially:** asking a researcher to *derive* what is in fact a choice produces an unresolvable search, and asking governance to *rule* on what is derivable produces an arbitrary ruling where an answer existed — ⛔ **both waste the effort and neither failure is obvious while it is happening**; ⭐⭐ **and the measured result is that the minimum number of evaluation outcomes is a computation over this very set, ranging 3 to 21**, so a large block of work sits behind an item whose owner is undetermined. ⭐⭐⭐ **It has already caused motion in the wrong place:** several recent efforts debated whether an evaluation needs two, three or four outcomes — **a question with no fixed answer, since the number falls out of the required-distinction set, so the debate could not have been settled by the people having it.** ⭐ **And the evidence to settle it is already written** — four documents in a readable folder analyse the set directly: **this is not a research gap, it is an unasked question.** ⛔ Neither characterisation is asserted wrong: it may be that part of the set is derivable and part must be chosen, ⭐ **which would itself be a useful answer and is not what either document says.** Candidate direction: **an open item should carry an agreed type — derivation, decision, experiment or definition — before it is queued**, because ⭐ **the type determines the owner and an untyped item cannot be assigned however well it is described**, and ⭐ ***"partly derivable, partly chosen" must be expressible*** or the same conflict recurs. Checked against `EKS-18` (**closest** — *"the question never reaches anyone"*, where here **a route exists and it forks**), `EKS-32`, `EKS-33`, `EKS-36` (**duplicated work**, where this is **contradictory typing of one item** — two efforts could cite each other perfectly and still disagree) and `EKS-23` (`ES-005.4`) — distinct on all five; see `EKS-37` §6.

**EKS-37 diagnosed 2026-09-09** by `P-87`, hours after filing — ⛔ **and the item stands.** ⭐⭐⭐ **The two descriptions refer to ONE object at TWO layers, and the choice sits one level higher than either states.** The programme's own completeness theorem reads: *"Let `𝒯` be the set of minimal operational tasks required of KnowledgeOS. `ℛ_req` is structurally complete **iff** every minimal information requirement for all `τ ∈ 𝒯` is mapped to a non-collapsing partition."* ⇒ ⭐⭐ **`ℛ_req` IS derivable — given `𝒯`; and `𝒯` is the decision.** So **the forward plan is right that the enumeration is a derivation, and the experiment report is right that a decision sets everything** — ⛔ **neither names `𝒯`, so both appear to be talking about the same layer.** ⭐ And **`𝒯` is *exemplified, not enumerated***: *"**including** contradiction isolation, Zero-state invariant checking, epistemic query resolution, and temporal garbage collection"* — **four exemplars behind an open list.** ⛔ **Not withdrawn:** the diagnosis does not remove the exposure, since **nothing in the estate records that the two statements are about different layers** — a reader still meets one or the other and still routes the work to a different owner — ⚠️ **and the diagnosis moves the question rather than closing it, because the same derivation-or-decision problem now applies to `𝒯`, which has no owner either.** ⭐⭐ **What changes is the remedy's target: §5 should be read as applying to `𝒯` first**, because typing `ℛ_req` while `𝒯` is untyped merely relocates the ambiguity.

**EKS-37 answerable 2026-09-09**, from `P-88` — ⭐⭐⭐ **at the layer where the choice actually sits, the corpus has already answered this item's question.** A dedicated audit of the operation family concludes, in its own boxed line: *"The **SCHEMA** is derivable (and largely derived). **MANDATORY MEMBERSHIP** is not. **No route derives membership from already-ratified material.**"* — recording that **eight derivation routes were tried**, that the only route with no unmet precondition is *"declare mandatory membership"*, classified **NORMATIVE**, and that the act has a named owner: **ARB**. ⭐ **A further correction it supplies:** *"not enumerated" ≠ "not closed"* — **the classification is closed, the membership is not** — so ⛔ **the work owed is not more enumeration but a rule saying which members are mandatory, and no research act can close it.** ⛔ **The item still stands**, because **nothing connects that answer to the statements that raised the conflict**: the documents typing the item as *"a derivation"* and as *"the DECISION that sets everything"* sit in a different lane from the audit that settled the layer beneath them, and **none of the three cites another** — a reader arriving at any one still routes the work to a different owner. ⭐⭐ **What changes is that this is no longer an open research question but a reconciliation someone can perform today**, from three documents that already exist — ⚠️ **one of which lives outside the folders the reading lane is routinely pointed at**, which is part of why the three have never met.

**EKS-37 extended 2026-09-09** from `P-90` — ⭐⭐⭐ **§5's own warning has come true, on the same item, and the missing label is a FIFTH type.** §5 asked that an open item carry an agreed type — *derivation, decision, experiment or definition* — and warned that *"a vocabulary that forces a single label will produce exactly this conflict again."* A **third** register, separate from both already recorded here, classifies the operation family **`CLOSED / PARAMETRIC`** and glosses it explicitly: ⭐⭐ *"**`CLOSED/PARAMETRIC` is not a euphemism for open.** It means the node is fully determined **once a deployment declares its parameter**, and **the corpus deliberately declines to declare it**."* ⇒ ⭐⭐⭐ **one lane types the item a DECISION for ARB; another types it a PARAMETER for a DEPLOYMENT — and neither is *derivation* or *decision* as §5 offered them.** ⚠️ **Why the fifth type matters more than the count:** a governance decision is taken **once, for the programme**; a deployment parameter is declared **per installation, by whoever installs it** — ⛔ **and a rule that binds the programme cannot be delegated to an installer without someone noticing.** **Today nobody notices**, because the two classifications sit in registers that do not cite each other and **both are internally coherent.** The candidate requirement stands with the vocabulary widened — *derivation · decision · experiment · definition ·* ⭐⭐ **deployment parameter** — and *"partly one, partly another"* must remain expressible, **which was §5's second property and is now demonstrated rather than anticipated.**

**EKS-38 added 2026-09-09** from `P-89`. Two parts of the programme each keep a list of **decisions somebody still has to make**, and each numbers its entries `N-1`…`N-4`. ⭐⭐⭐ **The numbers overlap completely and mean entirely different things:** in one register `N-1` asks whether two relations are the same, `N-2` picks among three repairs, `N-3` fixes an operator set's extension and **`N-4` rules on which operations are mandatory**; in the other, `N-1` is **"decide whether the system claims truth"**, `N-2` is **"define contradiction"**, `N-3` is **"define the progress ordering"** and **`N-4` is "test thirty-seven unsupported distinctions, or drop them"**. **Four identifiers, four collisions, the same range** — ⛔ **and no crosswalk anywhere**: the approved TODO register cites `N-4`/`N-8`/`N-13` **zero** times, and **no file holds both "TODO Group" and `N-4`.** ⭐⭐ **Why this is worse in kind than a notation clash:** the programme already tracks symbol collisions, which cause a reader to **misunderstand a sentence** — but these identifiers name **acts**, so ⭐⭐⭐ **a decision-ID collision produces a WRONG ENACTMENT.** Someone told to *"enact `N-4`"* has two candidates: **a governance ruling with a named authority, or a piece of research work** — not variants of one act. ⚠️ **Why it matters commercially:** everyone can behave impeccably — follow the instruction, do good work, record it properly — and **the wrong thing still gets done**; ⭐⭐ **it bites precisely at handover**, because a number is used instead of a description exactly when the parties are not in the same conversation, **which is also when neither can notice the clash**; and ⭐ **it has already reached a live instruction** — a recent commission asked for an audit of *"`N-4`"* and **the audit had to determine which `N-4` was meant before it could proceed**, with the answer absent from the material the commission pointed at. ⚠️ **Both registers are active and growing, so every new entry extends the overlap.** ⛔ **Neither register is badly kept** — one goes out of its way to *remove* four items from its decision list because *"calling them governance questions would have manufactured four decisions"*, which is unusually disciplined practice — and **the convention is not wrong; two parties simply made the same reasonable choice independently.** Candidate direction: **a pending decision should be identifiable unambiguously across the whole programme**, with two properties mattering more than format — ⭐ **uniqueness must hold where the identifier is USED, not where it is DEFINED** (each register is already internally unambiguous, and that is exactly what did not help), and ⭐ **an instruction to act should carry the act's subject in words, not only its number**, so that **a reader who cannot tell which `N-4` was meant can at least tell that they cannot tell.** Checked against `EKS-27` (**same cause**, but its collisions are in *formulas* and the fix is a glyph register — here the failure is a **wrong enactment** and the fix is a **governance** artifact), `EKS-34` (**there neither side can see the other**; here both registers are readable and nobody looked), `EKS-28`, `EKS-36` (**duplicated work**, where this is **identical labels on different work**) and `EKS-32` (`ES-005.4`) — distinct on all five; see `EKS-38` §6.

**EKS-39 added 2026-09-09** from `P-91`. One concept sits under most of the programme's remaining work — **the distinctions the system is required to preserve** — and it has kept the same name throughout. ⭐⭐⭐ **It has not kept the same kind of thing:** at the start it was **a TEST** (*"remove this piece; does any required operation now give a different answer?"*); three days later it was **a LIST** (a tiered inventory of named items with values and priorities). ⭐⭐ **A test needs no acceptance criteria — it compares two outputs. A list of requirements does: each item must say what would count as meeting it.** ⛔ **Nobody wrote those criteria, because nobody recorded that the object had changed kind and now needed them.** ⭐⭐⭐ **This is the current blocker, not a historical curiosity:** several efforts have hunted the missing connection between the governing contract and a requirement's acceptance conditions and each concluded it is absent — **the archaeology shows it was never lost; it was never needed, and then never built.** ⚠️ **Why it matters:** every step was locally reasonable, so **the damage is cumulative and belongs to no single step**, which is why no reviewer caught it; **earlier results are silently inherited by a different object** (work done when it was a test is cited by documents treating it as a list, and **whether those results transfer is a real question nobody has asked**); ⭐⭐ **it defeats the programme's own strongest discipline**, because the estate is unusually careful about marking what is derived, stipulated, proposed or open — **all of which tracks a claim's STATUS, and none of which tracks a concept's TYPE**, so a change of kind passes every check; and ⭐ **it has already produced a false search**, since the bridge was never owed by the earlier object and **searching harder could not have found it.** Candidate direction: **when a concept changes kind, record it as an event, with what the new kind now requires that the old one did not** — with two properties mattering more than format: ⭐ **type is not status** (the existing vocabulary cannot express this event at all), and ⭐ **the useful part is the CONSEQUENCE, not the label** — *"this is now a list, so each entry needs acceptance criteria"* is the sentence that would have prevented the blocker. Checked against `EKS-27`, `EKS-34`, `EKS-38` (**simultaneous** ambiguity or **two objects one name**, where this is **sequential drift of one object**), `EKS-37` (**closest** — but that is the type of an **ACT**, this the type of a **CONCEPT**), `EKS-24` and `EKS-22` (`ES-005.4`) — distinct on all five; see `EKS-39` §6.

**EKS-40 added 2026-09-09** from `P-93`. A research record is expected to say where things stand. ⭐ **This one says several things at once.** Within **a single batch of twenty-seven documents written the same afternoon**, the same items receive **opposite dispositions and no document mentions the other**: the central specification is *"**READY FOR RATIFICATION** — every component complete"* in one document and *"**not yet ratifiable**"* in another that rejects completeness and minimality by name; a core semantic operator is *"**no formal specification exists**"* in one and — ⭐⭐⭐ **two files away** — **is the specification, marked `[RATIFIED]`**; the transition operator is *"only a signature exists"* against a full specification **included three times over**; and the number of items being ratified is **twelve**, **eleven**, and in a third document **ten**. ⭐⭐ **This differs from duplicated work:** duplication wastes effort but the record still says one thing — **here the record says two incompatible things and both are quotable**, so anyone can cite the position they prefer, accurately, and so can anyone who disagrees. ⚠️ **Why it matters:** there is **no answer to *"where do we stand?"*** — not because nobody wrote it down but because **several people did, differently, at the same time**; ⭐⭐ **work gets done twice or not at all**, since an operator declared unspecified invites someone to specify it while a ratified specification sits two files away, and equally a genuinely open item can be waved through by citing a document that calls it closed; **it is invisible to every safeguard**, because each document is internally coherent and correctly stamped and **nothing compares documents from the same batch**; and ⭐ **the status vocabulary itself has fragmented** — five different status words across one package, so even reading the dispositions requires knowing which vocabulary each document uses. ⭐⭐ **Three separate instances observed in two lanes** — a pattern, not an accident. ⛔ **No document is careless** (several are unusually rigorous), and ⛔ **disagreement is not the problem** — **disagreement is how this programme works and has repeatedly caught real errors**; the problem is that **a disagreement was never recorded AS one**, each side simply stating its position as the position. Candidate direction: **when two documents give an item different dispositions, the disagreement should itself become part of the record, visible from the item** — with ⭐ **a contradiction treated as information, not a defect to be tidied away** (*"two documents disagree, here is each"* beats silently keeping the later one), and ⭐ **the check running over a BATCH, not a document**, since every document here passes review on its own and **only comparing siblings reveals it.** Checked against `EKS-36` (**closest** — duplicated work, where this is contradictory verdicts), `EKS-38` (**one name two items**, where this is **one item two verdicts**), `EKS-31`, `EKS-30` (there the answer exists and is unfindable; **here two answers were written**) and `EKS-35` (**order**, not **content**) — distinct on all five; see `EKS-40` §6.

**EKS-31 extended 2026-09-09** by Lane T, from `104`. **Nine analysis files entered the primary corpus in one afternoon, and ⭐⭐⭐ four of them — 15:44, 15:46, 15:46, 15:47 — are four separate runs of the same commission**, three sharing a near-identical title, **all four reaching the same verdict**, ⛔ **none citing the others and none marked as a repeat run.** ⭐⭐ **Same missing marker, new and arguably worse effect:** the item was registered because nothing recorded *where* analysis came from; today nothing records that four documents are **the same work done four times**, so ⭐ **a reader will find four agreeing documents and reasonably count four confirmations — there is one.** ⚠️ **Why it matters:** four documents agreeing is normally strong evidence, but here it is **expected by construction** (same question, same corpus, same afternoon) and carries almost no additional weight; ⭐⭐ **the estate already applies exactly this test elsewhere** — one of these very files rules another same-day document *"external, same-day, consuming the earlier work, non-independent"* and excludes it from corroboration, **while the four repeat runs are not held to it**; and ⭐⭐ **the disagreements are the part that gets lost** — the four **agree on the verdict but order their three blockers differently**, one demoting the blocker the others call decisive, **and that ordering is what selects the next piece of work**, yet with no marker saying these are parallel runs a reader has no reason to compare them. ⭐ It also compounds with the storage problem already recorded: `document1.md`, `document2.md`, `document3.md` carry no timestamp, author, commission reference or run number, so **on a directory listing they look like three unrelated papers.** Candidate requirement, **extending §5 rather than replacing it**: a marker should record not only provenance but **whether the file is a repeat run of a question already answered, and which run of which commission** — ⭐⭐ **the decisive property being that repeated runs must be countable as one**, since a marker identifying the author but not the commission still lets four replicates read as four confirmations. ⛔ No claim is made about any document's correctness.

**EKS-31 extended a second time, 2026-09-09**, from `105`. A byte-level check of one earlier day's **159 files** in the same directory found **13 exact-duplicate pairs — 15 446 lines, about a tenth of that day's material.** ⭐ **Eleven are honest repeats** (one even named *"second copy"*); ⛔ **two are not.** ⭐⭐⭐ **A file named for a *boundary separation EXPERIMENT* contains, byte for byte, a different document about a lens/predicate distinction**, and a second pair does the same to a review. ⚠️ **This is worse than an unmarked repeat:** an unmarked repeat inflates a count, but **a copy filed under a different title creates a work that does not exist** — someone auditing what experiments were run will list that experiment, find a document where it should be, and have **no way to notice the document is about something else.** ⭐ It strengthens the requirement already recorded (*repeated runs must be countable as one*) rather than adding a new one, and shows **countability fails hardest when the copy is renamed**, because neither filename, length nor keyword search reveals it — ⭐⭐ **only a content hash does.**

**EKS-31 extended a third time, 2026-09-09**, from `P-87` — ⭐⭐⭐ **the consequence this item warned of has now been realised: unmarked external content reached a ratification package.** A file self-titled ***"PERPLEXITY RESEARCH RESPONSE"*** — an **external AI research service** — supplied a list of distinctions drawn from the academic literature (Quine, Kant/Kripke, Kripke, Tarski, Gödel, Fagin et al., Enderton, Church/Turing, Hintikka, Levesque), and ⭐⭐ **at least four of the eleven distinctions in a package recommended for ratification came from that literature list**, three of them through that response — ⛔ **with no marking in the package at all.** *(And one copy of that same file is byte-identical to another but named "…-variant" — the renamed-copy pattern, on this very document.)* ⛔ **The distinctions are standard and defensible; this is not a quality complaint.** ⭐⭐ **The point is what they are NOT: neither derived from the programme's own theory nor selected by an authority — they are imported.** ⭐ **A reader sees one inventory and reasonably assumes one provenance; there are three — derived, chosen and imported — and only a different file reveals the third.** ⚠️ **The exposure compounds**, because the package is the object a downstream numerical computation is run over, so an unmarked import becomes an unmarked input to a result. Adds to §5: ⭐⭐ **the marker must survive aggregation — provenance has to travel with the item, not just with the file it arrived in.**

⚠️ **Eighth numbering race, 2026-09-09:** `EKS-32` was written as `EKS-31` and renumbered when the concurrent session filed its own `EKS-31` mid-write. Further evidence on `EKS-07`.

**EKS-16 extended 2026-09-09** from `P-81` — **occurrence 6, a category escalation.** Occurrences 1–5 were *the estate held material the lane did not search for*; ⭐⭐⭐ occurrence 6 is *the lane's own numbered artifact, in the same directory, with the question in its filename.* §5's remedy (search the estate first) **would not have caught it**, which is why the output-side problem is filed as `EKS-30` rather than folded in. **Occurrence count 6; detection by luck 4 of 6.**

⚠️ **Seventh numbering race, 2026-09-09:** `EKS-29` was written as `EKS-28` and renumbered when the concurrent session filed its own `EKS-28` mid-write. Further evidence on `EKS-07`.

**EKS-31 added 2026-09-09** from the three-model-convergence reconstruction's own MD-059. During a
primary-source archaeology prompt, the user supplied a file to read at
`mathematical_ideas_that_can_be_implemented/I conducted the targeted evidence-extrac` — a name that is
a truncated sentence, not this directory's own `YYYYMMDD-HHMMSS_topic.md` convention, sitting inside
the exact directory this programme's own sequential-pass tooling treats as primary evidence. Its
content explicitly analyzes this reconstruction's own MD-058 output; its filesystem timestamp is
today. **The file cannot be prior, independent corpus material — it is a same-day response to this
reconstruction's own work, saved with no marker distinguishing it from the 401 genuine primary files
around it.** MD-059 used it correctly, as a labeled cross-check, not a source — but the next pass to
apply this programme's own repeated timestamp-thread discovery method to this directory has no way to
tell the two apart from the file itself. Checked against `EKS-24` (a genuine corpus file's buried
conclusion — this is a file that should not read as a corpus file at all), `EKS-22` (missing status on
internally-produced work — this is missing provenance on externally-produced work), and `EKS-19`
(session-authorization overlap — unrelated to a single file's own misleading signal); distinct on all
three.

**EKS-31 extended 2026-09-09** from the three-model-convergence reconstruction's own MD-060 —
**occurrence 2, same day.** A second file of the identical shape,
`mathematical_ideas_that_can_be_implemented/# F4 Handover Audit — post-MD-059.md` (15:32, 39 120
bytes), was found sitting beside the first, its own content duplicated verbatim twice within itself.
Same profile throughout: no dated-filename convention, content built explicitly on this
reconstruction's own MD-058/059 output, saved directly into the primary corpus directory. **Not
refiled** — recorded as a second occurrence of the same structural gap, strengthening rather than
changing the candidate requirement in `EKS-31` §4.

**EKS-36 added 2026-09-09** from the three-model-convergence reconstruction's own MD-062. Two
research documents saved the **same day** (2026-09-02) into the **same directory** both build an
apparatus for the same kind of question — how to decide whether a claim/state satisfies a requirement
and preserve the reasons that decision depends on. One (the `K_t`/`Δ_t` lineage this reconstruction
has worked from since `MD-057`) leaves its own `Sat` predicate's body open. The other
(`KR-CONTR-FDE-2026-09`, a real, executed 14-scenario experiment) tests exactly the sibling question
and finds bare-value representations inadequate — a `Reason`/`Provenance`/`Context`/`Condition`
boundary channel is required, or required distinctions collapse (measured: Classical loses 12/14, K3
8/14, FDE 6/14, structured reaches 2/14). **Neither cites the other.** This reconstruction built and
froze a `Sat*` construction (`MD-061`) from the `K_t`/`Δ_t` lineage alone — using precisely the
bare-value shape the sibling experiment, the same day, already found inadequate — and discovered the
gap only because the user separately pointed to the second document afterward. Same shape as `EKS-28`
(two governance commissions on sibling kernel questions never cross-referencing), one layer down,
between two research documents a few keystrokes apart in one folder rather than two governed tracks.
Checked against `EKS-28`, `EKS-23`, `EKS-13` first — distinct on all three; see `EKS-36` §5.

**EKS-31 escalated 2026-09-09** — a direct filesystem check found **eleven** same-day, non-standard-
named files in `mathematical_ideas_that_can_be_implemented/` (not two), spanning the whole session
13:54–16:47, none in the git-tracked 401-file manifest — roughly one every 10–20 minutes, two of them
(`Untitled-17.md`/`document4.md`) **byte-identical copies of each other under different names**. One
(`historical_source.md`) explicitly self-describes as *"a historical-source audit"* while itself being
same-day, non-corpus material — the exact masking risk this ticket already named, now demonstrated
directly. One of the cluster's own factual claims was independently verified against a real,
git-tracked primary source (`M0047`) and found correct — confirming the provenance gap matters
precisely because good and bad analysis in this cluster are equally invisible as "not corpus" from
the filename alone. Urgency raised from "low for this file" to moderate-and-growing. Full inventory:
`EKS-31` §9.

**EKS-31 sharpened further, same day, mechanism confirmed by the user** — `document4.md`/
`Untitled-17.md` were each read three times over roughly two hours and returned **three genuinely
different contents at the same path**, one pair byte-identical, the third distinct in both size and
md5. **Confirmed mechanism**: the user runs a separate external research session and saves its
successive outputs into this directory, reusing the same filename each time rather than saving each
answer under a new one. **The defect this exposes goes one level deeper than §1–§9 describe**: in
this cluster, even a fixed filename, reopened minutes apart, does not guarantee the same content
twice — no version marker, no content-timestamp, only a single, overwritten filesystem `mtime`, so a
later citation of "`document4.md` as read on 2026-09-09" cannot say which successive answer is meant.
A filename/header marker alone would not resolve this for files under active, repeated overwrite; it
would need pairing with a non-overwriting save convention or a content-version marker. Not designed
here, per this ticket's own standing rule. Full detail: `EKS-31` §10.

**EKS-42 added 2026-09-09** from `P-96`'s queue-driven chronological read. A citation is supposed to
name a place. ⭐ **These ones name two.** Two documents written **eight minutes apart** both number
their sections `272A.1` through `272A.29` — not overlapping at the edges but **the same range, end
to end** — while covering entirely different subject matter (one derives the operation universe, the
other the minimum epistemic-state structure). ⭐⭐ **The collision lands hardest exactly where it
hurts most:** the two sections cited most often downstream, `272A.17` and `272A.25`, are precisely
the two where **both** documents hold a load-bearing, quotable result — a 19-row requirements table
and a 19-operation set in one, a lifecycle separation and the four-state minimality conclusion in
the other. ⚠️ **Why it matters:** ⛔ **a missing citation announces itself; a citation that resolves
to the wrong place does not** — it returns a confident, well-formed, entirely unrelated answer; it
**defeats the programme's own standing reading method** ("search hit → identify timestamp → follow
the thread"), which assumes a hit identifies a document; and it is **invisible from inside either
document**, since neither says another shares its numbering and there is no cross-reference in
either direction. ⭐ **It has already produced an under-specified citation in this programme's own
output** — `P-95` cites `272A.17` and `272A.25` naming no file; the content quoted is from the 22:42
document and is correct, but **the citation as written cannot be checked by anyone else**, and work
that cannot be checked cannot be relied on. ⛔ **Nothing here is careless**: the second document
opens by declaring itself *"the missing part of Step 272A"* and *"the formal completion of Step
272A, not a replacement"*, so numbering its sections `272A.n` **expressed a real and reasonable
intent** — only the addressing consequence went unconsidered. Candidate direction: guarantee that a
section reference identifies exactly one location — **cite by file plus section, declare bare
`272A.n` invalid, and require any document that continues another to say so in its opening line** —
in preference to renumbering, which would rewrite accepted documents. Checked against `EKS-35`
(**ordering**, where this is **addressing** — fixing one fixes neither), and `EKS-38` / `EKS-41`
(same *family* — one identifier, several referents — but at the **register** and **symbol** levels,
each needing a different remedy: namespacing, a glyph register, and here a citation convention);
distinct from all three, and no existing item covers section-level addressing. See `EKS-42`.

**EKS-41 extended 2026-09-09** by `P-96` with **a third meaning of the symbol, and it is the oldest
of the three.** Three days before either strand in the original ticket, `ℛ` was already load-bearing
as **the family of representation functions** (serialisation: Save, Load, Serialize, Deserialize) in
the 2026-08-30 21:59 operation-typing split. ⚠️ **The oldest meaning is the one furthest from the
other two** — it ranges over *functions on representations*, where `ℛ_req` ranges over *distinctions
a representation must preserve* — so a reader who meets `ℛ` first will read `ℛ_req` as "the required
representation functions": **a reading that is wrong, plausible, and undetectable from the symbol.**
⭐ This shows the collision is **not a two-strand accident on one day**; the base glyph was already
carrying a third sense before either strand chose it. ⛔ **One mitigating fact, recorded for
fairness:** the representation-function use was **repaired two minutes later** — `step_277` re-glyphs
the same classes and moves representation functions to `𝒪_R` — **but the repair was never recorded
as a repair**, so the colliding text stands unmarked and is what a glyph search still returns. The
disambiguation ruling the ticket asks for must therefore cover **three** meanings, state that bare
`ℛ` and `ℛ_req` are unrelated, and record the `𝒪_R` re-glyph as the disposition of the first. See
`EKS-41` Appendix A.

**EKS-43 added 2026-09-09** from `P-97`. ⭐⭐ **This is the collision that has already produced a
wrong result.** The three letters `Sat` name two entirely unrelated things, and **both uses are
live**: the central formal operator `Sat(K,r)` — *"the degree or status to which knowledge `K`
satisfies requirement `r`"*, introduced 2026-08-27 and still under development on 2026-09-06 — and
the **Sanskrit metaphysical term** `Sat` (*being*, *ultimate reality*) in the Vedānta triad
`Sat`/`Asat`/`Mithya`, introduced 2026-08-26. One is a satisfaction predicate over a requirement
set; the other is a category of existence in classical Indian philosophy. ⛔ **An automated
corpus-wide scan reported the earliest `Sat` as 2026-08-26 — a full day before the real formal
definition** — and acting on it would have sent the entire reconstruction to a philosophical
document as the origin of the requirements theory; **it was caught only because every mechanical
result in this programme is verified against source before use**, and a reader without that
discipline would not catch it. ⚠️ **Three things make it worse than the other collisions in this
family:** the philosophical strand is **the largest single step cluster in the corpus** (one step
number spans nearly a hundred documents), so a search returns two substantial bodies of work mixed
together, not a few stray hits; **it is invisible from either side**, since neither lane flags the
other and neither cites it; and ⭐ **the lanes are deliberately kept apart**, which is exactly the
situation where a shared name makes a reader assume a bridge that does not exist. ⛔ **It is a
homograph across languages, so domain expertise does not protect you** — a reviewer fluent in the
requirements theory reads `Sat = ultimately real` as a strange claim about satisfaction rather than
as a different word. ⛔ **Neither use is wrong**: the two arose independently, a day apart, in
different lanes, each entirely reasonable in its own context, and the philosophical work is not
being questioned. Candidate direction: **a writing convention rather than a register entry** —
reserve upright `Sat(` for the operator and write the Sanskrit term distinctly (*`Sat`* or `sat`),
plus record both meanings so everything already written stays safe. Checked against `EKS-41`
(**closest** — but every meaning it tracks is a formal object *within the mathematics*, where this
is a formal object versus **a natural-language term from another language**, so no type analysis
can separate them, only reading the sentence), `EKS-42` (same family, section level) and `EKS-34`
(collision across a *firewall*, where this barrier is **linguistic**); distinct from all three.
See `EKS-43`.

**EKS-41 extended a second time, 2026-09-09** by `P-97`: a **fourth** meaning of `ℛ`, and it is now
the oldest. `step_023` (2026-08-27 16:25) §5 defines **`ℛ(P)` = "the relevant information
requirements for purpose `P`"** inside `Coverage(K,P) = |SatisfiedRequirements| / |ℛ(P)|`. The full
set is now: **`ℛ(P)` requirements-for-purpose (08-27)** · `ℛ` representation-functions (08-30
21:59) · `Req(EC_t)` (09-02) · `ℛ_req(Q,Γ)` (09-02). ⚠️ **The oldest meaning is the one most easily
mistaken for the newest** — both are parameterized requirement sets written with a calligraphic
`ℛ`, differing only in what parameterizes them (**a purpose** versus **a question and a context**).
That is a far more dangerous resemblance than the representation-function sense, because it is
plausible: a reader may reasonably conclude `ℛ_req(Q,Γ)` simply *is* `ℛ(P)` renamed, **and no
document establishes that it is.** The disambiguation ruling must therefore cover four meanings and
must state explicitly that the `ℛ(P) → ℛ_req(Q,Γ)` descent is **unwitnessed**, not assumed.

**EKS-44 filed, 2026-09-09** (MD-070, Independent Adversarial Review of GAP-004): the Sep-6 `Sat`
definition (`Sat(K,r,Γ)=Det_r(EvalReq(K,r,EC,Γ),EC)`) is typed but never computed — the theory's own
flagship worked example (§21A.17) stipulates `Sat(K,r_i)=Satisfied` by fiat rather than invoking
`Det_r`/`EvalReq`, the same shortcut every earlier, already-retired `Sat` attempt took. Separately,
`Zero(K,EC)⟺Δ(K,EC)=∅` is filed simultaneously as Definition 23.1, Theorem 24.1, and Axiom A7 — three
epistemic categories for one identical claim. See `EKS-44`.

**EKS-45 filed, 2026-09-09** (MD-071, Cross-Lane Transfer Register): Phase 2's own Model B concept
register cites `M0132` freezing `Δ_t={r∈R_t:Sat(K_t,r)=0}`, ratifying `M0043`/`M0047`; MD-069's own
T5 cites `[00-47]`'s `[DEF-21]`, `Δ_t={r∈Req(EC_t):¬Sat(K_t,r)}` — the same underlying source, two
independently-built reconstructions, two visibly different-looking formulas, never directly compared.
Could be the same freeze paraphrased, or two distinct objects. See `EKS-45`.

**EKS-46 added 2026-09-09** from the `G-18` provenance investigation. ⭐⭐ **A carefully qualified
result becomes an unqualified one three minutes later, and the qualification never comes back.** At
**19:24** an execution artifact reports the knowledge-state minimality result and states its scope
exactly: *"The phrase MINIMAL KERNEL is therefore **earned** — but only in the sense step 254 names:
`Minimality(K | 𝒯)`, **relative to the transformation set**."* At **19:27** — three minutes later —
a second document restates it as *"the minimality claim … is **reported as PROVEN** by the executed
programme"* and adds *"should **not reopen** the already-closed `K` problem."* ⛔ **The `| 𝒯`
relativisation is gone**, and the instruction not to reopen closes the natural repair path.
⚠️ **The condition was not a footnote — it was the part that made the claim true**: minimality
*with respect to a declared operation set* is a real, checkable finding; unconditional minimality is
a much larger assertion the underlying test does not support, so the second version is **not a
simplification of the first but a different and stronger claim**, and everything downstream inherits
it. ⛔ **Neither document is careless** — the 19:24 artifact states its scope precisely and the 19:27
document is summarising upstream in good faith; **the defect lives entirely in the hand-off**, which
is why no review of either document alone would catch it, and why search does not help (both
documents return, nothing signals that one lost a condition). ⭐ Candidate direction: **carry the
qualifier inside the notation** — write `Minimality(K | 𝒯)`, never "minimal", so the condition
cannot be dropped without visibly changing the formula — and **require any "do not reopen"
instruction to cite the artifact that closed the question**, so a reader can check what that artifact
actually claimed. Checked against `EKS-40` (**closest, and genuinely different** — there two
documents say **opposite** things and both are quotable; here they say the **same** thing at
**different strengths**, so a contradiction-detector would not fire at all), the
identifier-collision family `EKS-38`/`41`/`42`/`43` (this is not a collision: one claim, one
meaning, one lost condition), and `EKS-35` (ordering, unrelated — both documents here are in the
right order). **No existing item covers silent loss of a scope condition during transmission.**
See `EKS-46`.

**EKS-47 filed, 2026-09-09** (MD-073, Single-Case End-to-End Computation Attempt for `Sat(K,r,Γ)`): a
narrow follow-on to `EKS-44` attempted to actually compute `Sat` for one real, well-evidenced
requirement (`PaymentConfirmed(S)`) and found the block is corpus-wide, not local to one example:
`Γ` has no definition anywhere (not even a schema); `EC` has a schema (Definition 2.20) but no
instance for any real requirement; and corpus-wide greps confirm `EvalReq(`/`Det_r(` each occur in
exactly one place — their own definitions — never invoked anywhere. See `EKS-47`.

# Session-2 Review — S1-F027

## 1 · Source artifact · 2 · Session-1 claim
`session1/S1-F027-inference-licensing-constraints-and-the-evidential-bridge-requirement.md`

Four constraints forming one family: *never infer semantic identity from functional similarity* · *behavioural understanding must not be promoted to semantic understanding without additional evidence* · ***no high-value architectural claim without an identifiable evidential bridge*** · *never infer ontology directly from representation*. Plus Williamson's comparison problem, Keith's *superseded knowledge is still knowledge*, and a research-direction statement. Marked **STRONG IMPLEMENTATION EVIDENCE**.

## 3 · Session-2 independent assessment
**Evidence class:** CLAIM ×4 (constraints) · QUESTION · CLAIM · DECISION. **Question types:** METHODOLOGY, IDENTITY, SEMANTICS. **Altitude:** constraints on **inference**, distinct from `S1-F025`'s constraint on the **decision procedure** — Session 1's separation of the two is correct and useful.
**Level 1:** UNVERIFIED. ✅ Session 1 flags that `014614` and `020936` are two readings of **one book**, the second existing because the project judged the first insufficient, so *"their agreement is not independent corroboration."*

## 4 · Confirmed

**`.1` v1.1's rows are instances of this family, and the corpus supplies the general rule — the finding.** Session 1: ⟨C-1⟩ (*canonical-form equality is a similarity claim, not an identity determination*) and ⟨r4⟩ (*probability ≠ truth*, *canonicalization ≠ authority*, *low entropy ≠ certainty*) *"are instances of exactly this family… the corpus supplies the **general rule** of which v1.1's rows are **cases**. **That generalisation is the finding.**"* ✅ Correct, and it is the right way to state it: law has the instances, the corpus has the rule.

**`.2` The self-indicting application — corroborated three times from my own reviews.** Session 1 flags *"that mapping is my inference"*, applying the bridge requirement to F008, F019, F013. Independently confirmed:
- `S1-F008` → `S2-R-F023 §L1`: the *"existing law"* claim reaches me at **fourth hand**, across three unverifiable links.
- `S1-F019` → `S2-R-F019.4`: *"Confidence is assigned at admission (**but derivation is unknown**)"* listed among *established facts*.
- `S1-F013` → `S2-F019`: three models proposed with evidence declared insufficient.

⚠ **And it applies to Session 1's own claims**, which Session 1 does not extend it to: the *"six independent arrivals"* (`S2-R-F016 §8`), the *"fourteen independent framings"* (`S2-R-F019.1`), the *"fourth independent statement"* (`S2-R-F023.3`) — each a high-value claim without a bridge.

⚠ **And to me.** `X-004` (my standing hypothesis may descend from the corpus) and `X-005` (my Zero lens is a third sense of a contested name) are exactly this constraint applied reflexively. **The bridge requirement is the general form of the discipline this register has been running**, and it arrives from Chalmers rather than from either session's method.

**`.3` Williamson's comparison problem is the method question for this extraction's own condition.** *"How do we compare competing conceptual systems without prematurely declaring one of them the truth?"* ✅ Session 1 is right that this is *"precisely the problem this extraction is in"* — eight formulations, six genera, four relation classifications, no adjudication authority. The implied answer (compare without declaring) is what nine dissolutions and every `NOT ADJUDICATED` marker in this register do in practice.

## 5 · Challenged

**`.4` The ordering conflict is a REVISION, not an unresolved contradiction.** `S1-F026` (01:03): *"the correct next step is **falsification**, especially scenario-based DDD analysis."* Nineteen minutes later (`022039`): *"**Formalize** the epistemic architecture we have extracted, **then** test it against… scenarios."* Session 1: *"Recorded, unresolved."*

Same author, same subject, same altitude, 19 minutes apart, later statement supersedes earlier. The convergence vocabulary has the right label: **REVISION.** Not a contradiction — a plan changed.

⚠ **But the revision is unrecorded**, and that is the finding: this is the **second** silently superseded project decision inside half an hour (the first being stop-reading, `S2-R-F026.4`). Recorded as **`M-7`**.

**`.5` *"STRONG IMPLEMENTATION EVIDENCE"* — same correction as `S1-F025`.** See §7: every implementable reading is already law or is an instrument.

## 6 · DDD classification
The four constraints → candidate **anti-inference policies / fitness constraints**, not members. *Evidential bridge* → candidate requirement on recorded claims. *Superseded knowledge as knowledge-about-evolution* → candidate reading of history. *Representability without distinction loss* → candidate **acceptance test**.

## 7 · Implementation relevance — **what would we build?**

| Candidate | Build what? | Verdict |
|---|---|---|
| The four-constraint family (general rule) | Nothing runtime — a **review constraint** on any inference | **PRESERVE AS KNOWLEDGE ONLY**, strong form. **Tenth instrument.** Its instances are already law (`.1`); the generalisation is guidance |
| Evidential bridge, applied to **admitted claims** | Nothing | **DO NOT IMPLEMENT (already protected)** — `JustificationPath` *is* the bridge: *"the reason the state is justified, never a black box"* |
| Evidential bridge, applied to **governance artifacts** | Nothing — a process rule for ADRs/findings | **PRESERVE AS KNOWLEDGE ONLY** |
| *Superseded knowledge is still knowledge* | Nothing | **DO NOT IMPLEMENT (already protected)** in effect — `History` retains *"every prior state"* (l. 216); `FALSE`/`REJECTED` are first-class states (l. 213). Whether retained content is *called* knowledge is definitional, hence adjudication. **NOT ADJUDICATED** |
| **Representability acceptance test** — *can the model represent real engineering knowledge without losing its distinctions?* | Nothing to build; a **test to run** | **NEEDS FURTHER EVIDENCE.** ✅ Session 1 is right that this is *"the sharpest acceptance criterion in the corpus"* — it is falsifiable, cheap, and **never once run**. What would change every verdict in this register: run it |

## 8 · Conclusion vs reasoning
Constraint family: **both valid** (`.1`). Self-indicting mapping: **both valid, and under-extended** (`.2`). Ordering conflict: **conclusion fails** — it is a revision, not a contradiction — **but the reasoning surfaces a real defect** (`.4`). *Strong implementation evidence*: **fails** (`.5`).

## 9 · Effect on previous Session-2 findings
`M-7` **new** (second instance). `S2-R-F016 §8`, `S2-R-F019.1`, `S2-R-F023.3` **reinforced** — each is now an instance of a named general constraint. `X-004`/`X-005` **reframed** as reflexive applications of the bridge requirement.

## 10 · Open questions · Adjudication · Status
What counts as an *identifiable evidential bridge* · is superseded knowledge inside the knowledge set (`W:C-15`, not routed) · formalise first or falsify first.
**ADJUDICATION: NONE. STATUS: CONTINUE.**

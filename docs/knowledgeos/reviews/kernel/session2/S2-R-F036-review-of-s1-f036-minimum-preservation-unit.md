# Session-2 Review — S1-F036 · the corpus's strongest artifact, and the one Session 1 nearly lost

## 1 · Source · 2 · Session-1 claim
`session1/S1-F036-minimum-preservation-unit-and-the-assertion-primitive-contested-in-one-document.md`, re-read line-by-line 2026-08-26 after the first pass captured *"roughly 15%"*.

The minimum preservation unit — *"an identity-bearing, contextualized assertion record"*; **preservation unit ≠ consistency boundary ≠ projection view**; `Assertion` asserted and doubted in one document; a five-way Merricks non-collapse; and **ten findings recovered by the full read**, including a ten-item **invariant surface**, a **decidable removal test**, and *"reconstructible epistemology."*

## 3 · Independent assessment
**Evidence class:** CLAIM · CONTRADICTION (intra-document) · DISTINCTION ×2 · MODEL ×2 · TEST ×2 · DEFINITION ×2. **Question types:** BOUNDARY, TEMPORAL, IDENTITY, METHODOLOGY. **Level 1:** UNVERIFIED, 0 URLs.

## 4 · The recovery is the headline, and it invalidates a coverage statistic

⚠ Session 1's own note: *"the earlier audit sampled five **low-relevance** documents and found one miss (~0.6%). This document is **K3-class**, and a full read found **ten** material findings — three of them among the strongest in the corpus. **The 0.6% figure does not transfer to K3/K4 documents.**"*

✅ **Correct, and it is Session 1 invalidating its own reliability estimate** — a sampling frame drawn from low-relevance documents cannot bound the miss rate on high-relevance ones. This is `M-6` (counts reported as results) caught by Session 1 against itself, and it is the single most important input to my review of `S1-COVERAGE-REPORT.md`. **That report's reliability figure is now known to be non-transferable before I have read it.**

## 5 · Confirmed — the recovered findings, assessed

**`.1` The ten-item invariant surface (B) is the ninth formulation, and the only one of the right *type*.**
*"stable identities · immutable historical records · typed relationships · explicit context · temporal distinctions · provenance chains · epistemic and lifecycle histories · contradiction and plurality · references across bounded contexts · reconstruction capability."*

✅ Session 1 is right that this *"enumerates **guarantees**"* where `S1-F003`/`F005`/`F008`/`F019` enumerated **capabilities**. That is exactly what `KCON-012` and `S1-F020`'s derivation rule require, and what `S2-F024.1` showed `S1-F009`'s criterion could not supply — F009's equilibrium had an **unbound** invariant variable; this **binds** it with ten named guarantees.

⚠ It remains **one document's proposal**, self-labelled *"our strongest **candidate**"*. Nine formulations now, not eight.

**`.2` The removal test (C) is decidable — and I have been running the same test all along.**
*"If this capability is removed, can KnowledgeOS still preserve and reconstruct the identity and epistemic history of a situated semantic commitment? If **yes**, it probably does not belong in the Kernel."*

⚠ **Provenance disclosure.** This is materially the same test my instructions supply (*"Remove X. What fundamental KnowledgeOS guarantee becomes impossible?"*) and that I have applied in every review. The HPA authored both. Per `S2-R-F022.1` and `X-004`: **PERSISTENCE of one author's discipline, not independent convergence.** I record the match and claim **no corroboration** from it.

✅ What is genuinely new is the **24-row applied table** — ✓ identity, semantic reference, context, temporal semantics, provenance, histories, typed relationships, contradiction; ✗ answer generation, truth determination, statistical inference, LLM, embedding, documents, words, sentences, domain ontology, UI, search, vector DB; **? Question structure and Topics.** The corpus applied its own test and published the results, including two undecided rows. **That is the only place in the corpus where a membership criterion is actually executed.**

**`.3` *Reconstructible epistemology* (E) is the corpus's clearest positive answer.**
KnowledgeOS cannot guarantee `REALITY = TRUE`; it can guarantee *"**This assertion was evaluated as X under regime R, in context C, at time T, based on evidence E**"* — *"not relativism… **reconstructible epistemology**."* ⟦L⟧ Consistent with §17's rejection of a truth oracle, and the constructive complement to it. **Five-indexed**, and every index is a member or port concept in law.

**`.4` The six-way temporal role split (A) bears directly on my strongest implementation question.**
*"what was said · what was observed · what was valid · what became effective · what was evaluated · what remains disputed"* — and *"different temporal roles **must not be collapsed into one timestamp**."*

⟦L⟧ Law carries **`TemporalValidity`** (valid-from · valid-until · superseded-by) — that is *"what was valid"*, and it attaches to the **claim**. `History` orders transitions. The other roles are not visibly members.

⚠ **Zero lens (absence typology):** law not naming five roles does **not** establish they are absent from the system — v1.1 is the core domain model, not the whole architecture, and roles may live in surrounding contexts. **No gap is claimed.**

**What it does supply is a shape for `S2-R-F001`.** If six temporal roles must not collapse and *validity* is one of them, then **authority validity needs its own carrier** — which is precisely the gap `S1-F001` measured (0/20 grants) and `S1-F034`'s *sufficient state* said must be retainable. Three routes, one hole. `S2-R-F001` updated: **verdict unchanged** (the Authority context may hold it), **shape now specified**.

**`.5` The three-role separation dissolves part of `F016`↔`F019`.**
*preservation unit ≠ consistency boundary ≠ projection view.* ✅ Session 1: *"F016 attacked **consistency**, F019 defended **preservation**, and both used the word 'aggregate'."* Correct — and it is the **eleventh dissolution**, by a mechanism already in use (`VOCABULARY COLLISION`, here on *aggregate*). The falsification survives untouched: not co-located, still preserved.

**`.6` The `Assertion` contradiction dissolves as DIFFERENT PREDICATE.**
*"Assertion is a Kernel primitive"* and *"Assertion is probably not fundamental"* in one document. Session 1 offers the reconciliation — primitive-for-the-Kernel versus ontologically-fundamental — and correctly **marks it as its own inference and does not adopt it**.

Tested: the two predicates are different (*primitive relative to a system* vs *fundamental simpliciter*), and a thing can be the first without being the second — a byte is primitive for a filesystem without being physically fundamental. **DISSOLVES — DIFFERENT PREDICATE**, twelfth dissolution, mechanism previously used at `S2-R-F016 §10`. ⚠ The source leaves it standing, so this is my resolution of a tension the corpus records, not the corpus's.

## 6 · Challenged

**`.7` Two instruments built, neither run — now three.**
Session 1: *"The corpus has now built **two** adversarial suites and applied **neither**."* ⚠ Three: `TARKA-KERNEL-FALSIFICATION-001` (`S1-F023`), this **18-case suite** (J), and `S1-F027`'s **representability test**. Add `S1-F025`'s decidability test and it is **four specified, unapplied instruments**. The corpus's most consistent product is instruments; its most consistent omission is running them.

**`.8` Ten measurable properties (F) answer `S1-F032` constructively — and Session 1 under-rates the timing.**
*assertion coverage · evidence coverage · contradiction density · temporal stability · provenance completeness · semantic ambiguity · uncertainty · question coverage · answer completeness · boundary stability.* Session 1 calls missing this *"the most avoidable of the ten."* ✅ And it answers *"there is no single Knowledge Score"* the right way: **ten named derived quantities instead of one scalar** — consistent with ⟦L⟧ `Confidence` being *"structured… never a scalar"* (l. 215).

## 7 · Implementation relevance — **what would we build?**

| Candidate | A–E | Build what? | Verdict |
|---|---|---|---|
| **Minimum preservation unit** — identity-bearing, contextualized assertion record | **B** | Possibly nothing | **NEEDS FURTHER EVIDENCE.** ⟦L⟧ the aggregate already holds `KnowledgeId` + `Meaning` + `ContextTuple` + `Agency`, which *is* an identity-bearing contextualized record. Whether *assertion* adds anything is the `Assertion`/`KnowledgeElement` naming question (ledger PS-1, **NOT merged**) |
| **Ten-item invariant surface** | **B** | A named invariant set to derive the boundary from | **NEEDS FURTHER EVIDENCE** — one document's candidate. ⚠ Adopting an invariant set is **adjudication. NOT ADJUDICATED** |
| **Removal test + 24-row table** | **D** | Nothing | **PRESERVE AS KNOWLEDGE ONLY**, strong form. The only executed membership criterion in the corpus; its two `?` rows are honest |
| **Reconstructible epistemology** (5-indexed) | **B/C** | Possibly nothing | **NEEDS FURTHER EVIDENCE** — every index maps to an existing member or port concept; whether they must be *jointly retrievable as one statement* is the open part |
| **Six-way temporal role split** | **B** | Distinct carriers per temporal role | **IMPLEMENTATION QUESTION** — sixth in the register, and the one with three converging routes (`S1-F001` measured · `S1-F034` criterion · this shape). **Missing fact unchanged:** whether surrounding contexts hold the roles |
| **Domain-independence boundary criterion** (D) | **D** | Nothing | **PRESERVE AS KNOWLEDGE ONLY** — *"the boundary ends where KnowledgeOS can no longer make a **domain-independent** structural guarantee"* explains the entire exclusion pattern in one word |
| **Ten measurable projection properties** | **C** | Derived quantities over preserved structure | **PRESERVE AS KNOWLEDGE ONLY** — projection altitude, outside the Kernel by §16 |
| **18-case adversarial suite** | **D** | Nothing — a suite to **run** | **PRESERVE**, and flagged: running it is cheap and would test the `Assertion` primitive directly |
| *Topic is a projection, not a primitive* | **C** | Nothing | **PRESERVE** — answered with a worked counterexample (`Kafka` in seven topics), which is rare here |
| *Lenses are lenses over KnowledgeOS, not rival Kernel definitions* | **D** | Nothing | **PRESERVE AS KNOWLEDGE ONLY** — the general resolution `S1-F032` and `S1-F040` each reach one formalism at a time |

## 8 · Conclusion vs reasoning
Preservation unit: **both valid**, and it survives the atomicity falsification untouched — Session 1's *"most reusable positive result"* stands. Invariant surface: **both valid**, one source. Removal test: **valid; independence not claimable** (`.2`). Coverage statistic: **conclusion valid — Session 1 refuted its own figure** (§4). Two-instruments count: **under-counted** (`.7`).

## 9 · Effect on previous Session-2 findings
**`S2-R-F001` updated** — third converging route, shape specified, verdict unchanged. **`S2-R-F016`** — the falsification is confirmed as attacking *consistency* only (`.5`). **`S2-R-F032`** — *no single score* answered constructively (`.8`). **`S2-R-F024`'s** `IMPLEMENTATION QUESTION` on source-type retention now sits beside route/acquisition/role/temporal-role as one cluster.

## 10 · Open questions · Adjudication · Status
What exactly is the identity-bearing thing · is `Assertion` the same role as `KnowledgeElement` (PS-1, unmerged) · do the six temporal roles live elsewhere in the architecture · will any of the four instruments be run.
**ADJUDICATION: NONE** — the invariant surface is a candidate, not adopted. **STATUS: CONTINUE.**

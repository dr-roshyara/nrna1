# 06 · FALSIFICATION VERDICT — TEN DIMENSIONS

> ## ⛔ NOTHING IN THIS FILE IS RATIFIED
> **Commissioner-assembled** (book/governance lane) from the two completed reports — the GN-80
> derivation and the GN-83/86 independent falsification pass. **This is not a second reviewer's
> verdict**; it is the mandated ten-dimension summary of evidence already in hand, in the form the
> HPA's §14 requires. Values are `PASS / FAIL / PARTIAL / OPEN / BLOCKED / NOT TESTABLE`.
> "CLOSED" is not used anywhere — no acceptance criterion permits it.

## The ten dimensions

| # | Dimension | Verdict | Basis |
|---|---|---|---|
| 1 | **Completeness** | **FAIL** | AC-1 FAIL / IR-F-26: a 24-operation arrow-typed taxonomy table cited by no key, ~19 of its names in neither list; `unask` live under an open normative decision; **steps 282/283/284 exist against a stated 281 ceiling**; steps 260/261 uncited (`Deduplicate` a first-class row). The universe is **not closed**. |
| 2 | **Minimality** | **PARTIAL** | The test was genuinely executed — five scripts re-ran byte-identically, and the closure computation is real bounded BFS, not a hand-authored table. But it ran over an **unclosed pool**, and exhaustiveness holds for **one variant of three**; A13's family is one hand-constructed size-7 set, self-labelled "not proven-minimal". |
| 3 | **Uniqueness** | **FAIL** (non-unique under the tested criterion) — **and the count itself is OPEN** | Six minimal registries were computed; the reviewer found the six **not established as genuinely non-equivalent**, and `rm.py:371`'s hardcoded return rung alone moves six to five. So: not unique, and "six" is not a verified number. |
| 4 | **Canonical selectability** | **BLOCKED** | No ratified principle distinguishes the candidates. Prior decisions **P-11** (what is the operation universe, and what act closes it?) and **P-1** (the typing of `Reject`) stand before any selection. |
| 5 | **Σ / Q_t independence** | **PARTIAL** | AC-3 PASSED as tested — Σ and `Q_t` are absent from the model's state core, checked against the code rather than the prose. **But the six specific counterexample constructions the HPA later specified were not run** (Σ stored vs derived; missingness via `Q_t` vs structural; evidence changed with Σ equal; Σ changed with evidence equal). The claim survives its test; the test was narrower than the HPA's. |
| 6 | **Invariant compatibility** | **PARTIAL, with one OPEN** | I-11, I-5, I-6, I-9, I-2 and Art. 7 hold under 20,790 executed applications. The claimed I-12/Art. 8 violations were **re-characterised**: IR-F-1 shows there is no ratified specification for `Reject` to violate — the obstruction is **underdetermination, not inconsistency**. **OPEN:** Constitution **Art. 8.3 "Resolution SHALL be forward-only"** was never consulted (IR-F-5), in a derivation whose central question was the unstated return rung. |
| 7 | **Authority / evidence compatibility** | **PARTIAL** | A6 holds for all 55 pool members under 44 evidence units with no authority act. One **corpus-stated** rule crosses it — step 025a-2 §36's `Commit`, five conjuncts, no authority conjunct — verified verbatim, source self-labelled "experimental" (AF-F-32). |
| 8 | **Replayability** | **FAIL** | IR-F-2: `op_Replay` **sets a flag**, and the goal test reads that flag — the identical defect used in the same package to discredit the record's withdrawn A6 witness. Every replay-dependent result is therefore unsupported. |
| 9 | **Rejection semantics** | **NOT TESTABLE** | Canon defines no rejection type or failure vocabulary; `failure semantics` is blocked for **every** operation in the contracts. The dimension cannot be evaluated, not merely unevaluated. |
| 10 | **Governance status** | **NOT ESTABLISHED** | No governance act exists for any candidate registry. Nothing in either report is ratified. |

## Acceptance gate (HPA §15) — already determined

> **AC-1 FAILED.** The HPA's own gate reads: *"If any fails, the operation registry is NOT VERIFIED."*
>
> ### THE OPERATION REGISTRY IS **NOT VERIFIED**.

And the gate's second clause is equally binding: **"VERIFIED NON-UNIQUENESS" is not available
either**, because it requires all six gates to pass. The honest statement is therefore:

> **Non-uniqueness is REPORTED, not VERIFIED.** Six minimal registries were computed over an
> unclosed pool; their mutual distinctness is unestablished.

**No second falsification pass can change this determination.** A further pass can sharpen
dimensions 2, 3, 5 and 8 — it cannot lift an AC-1 failure, because completeness is a property of
the candidate universe, not of the testing.

## The six final questions (HPA §18)

| # | Question | Answer |
|---|---|---|
| 1 | Does a minimal registry exist under the tested criterion? | **Yes — but over an unclosed pool, so the result is conditional** |
| 2 | Is it unique? | **No** |
| 3 | Are the six candidates genuinely distinct? | **Not established** |
| 4 | Can one be selected from currently authoritative evidence? | **No** |
| 5 | Is any operation registry ratified? | **No** |
| 6 | What exactly is required before ratification? | **P-11** (an act closing the operation universe) · **P-1** (the typing of `Reject`) · the return-rung question re-examined against **Art. 8.3** · `op_Replay` repaired and replay re-tested · variants 2–3 enumerated exhaustively · then a re-run of Tasks 1 and 3–6 |

## Which of the HPA's four outcomes obtains

**D — the test is blocked by pre-existing architectural decisions** (`Reject`, the A6/Art. 8.3
boundary semantics, and the unclosed universe), **with the ordering correction that P-11 is prior
to all of them.** Not A (no unique registry). Not B (the candidates' distinctness is unproven, so
this is not yet a clean choice boundary). Not C (minimality did not fail — it ran, and returned a
conditional positive).

## Boundary

No registry selected · no `Reject` typing · no I-12/Art. 8/Art. 8.3 repair · no transformation
semantics · no implementation specification · GC-1 and OQ-1…12 untouched · book unchanged:
**V.5 Operations NOT ESTABLISHED · V.6 Transformations NOT ESTABLISHED.**

# 05 · PART V READINESS STATUS

> # BLOCKED_PENDING_AUTHORITY

## Per-chapter

| Chapter | Unblocked? | The act that would unlock it |
|---|---|---|
| **V.1** how to read a requirement | — already writable pre-intake (ratified method); **unchanged by this intake** | none required |
| **V.3** state | — already writable with its stated gaps; **unchanged** | none required |
| **V.4** invariants | — already writable with the B-06 gap stated; **unchanged** | none required |
| **V.2** objects and identity | **No** | acts on **B-04** (which identity repair) and **B-05** (truth conditions for `K₁ = K₂`) |
| **V.5 Operations** | **No** | acts on **B-02/B-08** — **P-11a** a membership criterion · **P-11b** a source-admissibility rule · **P-11c** a closure act — and **B-07** (`Reject`'s typing) |
| **V.6 Transformations** | **No** | **B-09**, and it is downstream-blocked: **B-08 first**, then **B-05** (postconditions are undecidable without state equality) and **B-06** (a closed register, or obligations have no codomain) |
| **V.7 evidence** | **No** (partial writing already permitted) | acts on the Evidence object and the qualification predicate |
| **V.8 governance & authority** | **No** (partial writing already permitted) | acts on **GC-1** and on **AF-F-39** (policy inside-vs-outside `K`) |
| **V.9 software & tests** | — reportable as measured fact; **unchanged** | **B-10** for certification, not for writing |

**No chapter was unlocked by this intake. No authoritative act exists to cite for any of them.**

## The minimum authoritative act(s) required to open Part V

Stated as what an act must *establish*, never which option it should choose.

1. **An entry in the authority record.** All four claimed 2026-09-02 decisions
   (`DECISION-01` · factivity `R1` · the `Z` deferral · the `S` freeze) fail solely for want of a
   record. **If those decisions were in fact taken, the minimum act is to record them** — with act
   id, date, authority, exact decision, affected canonical artifact, and before/after state — and to
   reconcile the two that contradict their own documents (C6/C7 `RATIFIED` vs `[PROP], unratified`;
   `R1` selected vs §7 still recommending `R2`).
2. **The head of the chain.** The coordinating order supplied to this session places a **governance
   decision on canonical `K`** first; the programme's own record places the **Constitution status
   (B-01)** prior to everything, since Articles 7–9 are the evidence basis for D-FA-1. **These two
   orderings differ, and the divergence is itself an open dependency question** — flagged, not
   resolved here.
3. **Then, in order:** close `ℐ` (B-06) → resolve identity and equality (B-04, B-05) → the operation
   universe (B-02/B-08 via P-11a/b/c) → `Reject`'s typing (B-07) → typed rejection semantics →
   transformation contract `δ` (B-09) → implementation specification → implementation and empirical
   certification (B-10).

**Nothing above chooses an option, adjudicates, or recommends a resolution.**

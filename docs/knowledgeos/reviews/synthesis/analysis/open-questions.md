# PHASE 1G · Open Questions Registry

**Executes:** master prompt §1G + continuation instruction, including the added category
**EPISTEMIC GAP** — *"a transition where the corpus moves from hypothesis → formal model → architecture
→ implementation → validation without sufficient recorded justification. Do not treat every gap as an
error."* Questions are **not resolved** merely because later sections imply answers.

---

## A · EPISTEMIC GAPS (the new category)

| ID | Gap | Transition type | Detail |
|---|---|---|---|
| **EG-01** | **Zero lens → Zero Algebra** | hypothesis → formal model | stipulative definition replaced absence-detection with goal-gap computation; no recorded bridge (CON-01). ⚠ not necessarily an error — the algebra may be the better object; but the lens's non-collapse guarantees (`UNKNOWN ≠ ABSENT` etc.) must be shown to survive inside `Zero(K,G,EC)`. **NOT ESTABLISHED** |
| **EG-02** | **Lord lens → Lord Algebra** | hypothesis → formal model | name reused for a different concept (Ω-observation → action selection). The instruction's option D. The **original Ω idea is now homeless** — nothing in R5 carries it (CON-02) |
| **EG-03** | **Formal model → architecture** at R5 phase-ii→iii | formal model → architecture | the 025 algebras are folded into the architecture steps (26–49) without a recorded acceptance act per algebra |
| **EG-04** | **Architecture → implementation** at Steps 145–156 | architecture → implementation | golden trace → vertical slice v0.1 → *Implementation Architecture Constitution v1.0* — whether anything was **built** (vs specified) is NOT ESTABLISHED |
| **EG-05** | **Implementation → validation** at Steps 109–124 | implementation → validation | conformance/self-verification tests **specified** ⟦TITLE⟧; execution against the real repository NOT ESTABLISHED (EXP-04) |
| **EG-06** | R2 model → R5 step series | formal model → formal model | the R2 "complete mathematical model" and Knowledge Atom feed R5 without a recorded reconciliation — which R2 objects survived into the 8-primitive kernel of Step 49 is unmapped |

## B · SUBSTANTIVE OPEN QUESTIONS (carried by the corpus itself)

| ID | Question | Where left open |
|---|---|---|
| OQ-01 | **What is the Kernel object, if not Knowledge?** | posed R1 (`214833`), never answered in-corpus; R5's answer (8 primitives) is a *kernel*, but the R1 question was about the *object of protection* |
| OQ-02 | How do conditional evidence and conditional reasoning **combine**? | `20260826-000209/000339` v1 and v2, both open at R1 close |
| OQ-03 | Is **Determination** (the "missing mathematical object") now covered by `step-008` acceptance/commitment — or still missing? | R1→R5 mapping absent |
| OQ-04 | What were **Questions 1, 3 and 21**? | never written (inventory §5) |
| OQ-05 | Did **Ātma-as-Kernel** survive? (`025s` mentions *Knowledge Atma*) | R3→R5, unmapped |
| OQ-06 | What is **Option 3** (AD-01) — and what were Options 1 and 2? | `125403` unread in full |
| OQ-07 | Which conformance tests in Steps 101–124 **ran**, and what did they find? | decisive for the validation story |
| OQ-08 | Did **Step 100** close all dimensions? Its per-dimension Result blocks are unread | EXP-03 |
| OQ-09 | What exactly does *"relevant"* mean? — ⟦C⟧ *"Zero reveals that we still haven't defined what 'relevant' means"* (`20260828-152140`, the corpus's own late admission) | open at corpus end |
| OQ-10 | The two **local failures inside Step 50** (liveness; §~1223) — were they absorbed legitimately or defined away? | EXP-02 limitation |
| OQ-11 | Which of the four evidence algebras (saturating vs Bayesian-like) is **selected**, and on what further criterion? | EXP-01 left two candidates standing |
| OQ-12 | The **how_to_combine master prompt** commissions Phases 2–4 (separation → logical reconstruction → book). Phase 1's own gate: should the R5 sub-corpus (produced at up to 76 docs/hour) carry the same per-document weight as R1–R4 in later synthesis? | this registry's process question |

## C · PROCESS QUESTIONS

| ID | Question |
|---|---|
| PQ-01 | Why are stopping/freeze decisions never honoured or formally reversed? (CON-06 — pattern across two corpora) |
| PQ-02 | Should generative-rate material (08-28 12:00–13:00, 76 docs/hour) be treated as one authored artifact rather than 76 independent ones for evidential counting? |
| PQ-03 | The corpus observed its own executor failing (*"Session 1 is not actually executing continuously"*, `222915`) — does any later document verify the process was fixed? NOT ESTABLISHED |

---

**Nothing above is answered here.** Per the instruction: Phase 1 asks *what happened*, not *what the
architecture should be*.

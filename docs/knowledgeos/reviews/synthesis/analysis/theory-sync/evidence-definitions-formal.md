# Staging evidence · canonical definitions + formal status (extractor 1, 2026-08-31)
Read-only extraction. **Recorded as evidence, adopted as nothing.** Artifact precedence as measured:
CANONICAL-KNOWLEDGEOS-THEORY (08-30 19:58) → THEORY-CLOSURE-AUDIT (20:10) → KNOWLEDGE-STATE-FINAL-
AUDIT / THEORY-GAP-REGISTER (20:47–20:51) → canonical-construction → gap-discovery → consolidation →
step-280/281/282 → **handoff/ (08-31 00:44–00:46) = latest layer**.

## A · 𝒪_core — THE DECISIVE ANSWER TO GN-74 §9: **NOT FROZEN, NOT RATIFIED**
`handoff/02-O-CORE-AUTHORITATIVE-DETERMINATION.md` (latest word), verbatim status line:
> "**𝒪_core is DECOMPOSED-CLOSED but NOT MINIMAL — it must NOT be frozen as-is**"
> "**𝒪_core is CLOSED as a TAXONOMY and OPEN as a KERNEL. Freezing it now would freeze an
> explicitly unproven minimality claim.** … P0 'freeze canonical 𝒪_core' must become 'verify then
> freeze.'" Required first: the operation-necessity test — "**277 states the criterion and does not
> execute it.**" `handoff/06`: "the criterion exists; **the test has never been run by anyone**."

**Three membership lists that do NOT agree:** (a) 272A `𝒪_sem` = **19 ops / 5 families**;
(b) 277 `𝒯_candidate` = **6 ops** {Assert, Retract, Supersede, Merge, Split, LinkEvidence} with
"𝒯_candidate ≠ 𝒯_minimal"; (c) verifier's forced set = **14 lower bound / 18 upper**, undetermined
band {Transform, Merge, Split, Reintroduce}. Corpus §256.2 gives 9, §259.7 a *different* 14.
Unreconciled per `handoff/02`: `Split` "executed: LOSSY on ℛ"; `LinkEvidence` "would MUTATE an
assertion, contradicting ReplayAssertion's immutability invariant".

**Ratification measured as zero** (`consolidation/01-D0-AUTHORITY-STATUS.md`, six-location search):
> "**𝒪_core IS NOT RATIFIED.** No authority record establishes it." · FA-1…FA-9 (RATIFIED GN-31): **0**
> hits · `governance-notes` entries for 𝒪_core / "operation universe": **0** (ledger at GN-73).
> 272A carries `Authority: HPA` in front-matter with **0** occurrences of RULING/decided/DECISION in
> its body; 272B "**'ACCEPTED' appears only inside the artifact that is accepting itself.**"
> `consolidation/10`: "the research track is writing authority attestations at ~18 per 2 hours while
> the governance ledger has not moved a single entry."
Self-retraction (§272A.27): "𝒪_core = 𝒪_sem^candidate has **not yet been fully proven**" (7 open items).
Blocking scope: `consolidation/07`: "**Everything blocking reduces to ONE item: 𝒪_core is not
ratified.**" Counterweight, executed over all 16 subsets (`canonical-construction/02` §5):
"**D-1 IS K-INVARIANT.** The open decision changes the operation REGISTRY. It does not change the
knowledge STATE."

## B · Σ — representation answer for GN-74 §8
Current reconciled form (`handoff/03`): `Σ = (D, S)`; `D = 𝒫({Support, Refute}) ≅ {0,1}²` =
{Unknown, Supported, Refuted, Conflict}; `S` = **ORDINAL** strength.
- **DERIVED, never stored — proven, not chosen** (`consolidation/03` §A4): OR-merge + Retract are
  jointly satisfiable only if Σ is derived (retract needs subtraction; a stored join cannot undo).
  "D-4 is hereby closed by derivation." Caveat: "DERIVED **relative to 272B's own commitments**."
- **The isomorphism carries no information**: "A powerset of a 2-set is 2² — the mathematical
  content is **the choice of exactly two independent predicates**, not the four states."
- **Missingness is OUTSIDE Σ and Σ cannot reach it** (`consolidation/03` §A6, executed):
  never-asked → **no carrier**; insufficient evidence → **"Supported" (!!)**; "Σ₀ collapses
  'insufficient' into 'Supported' — it has **no sufficiency axis**, Q having been dropped from the
  ratified 6-tuple." Governed placement = `Q_t`, outside K and outside Σ (step-281/01, /03, /09:
  "Σ … **unchanged** — stays a function of e").
- **NEW defect, on no register** (`consolidation/03` §A3, executed): `Σ₀(c1)=Supported,
  Σ₀(c2)=Supported` while `(c1,c2,contradicts)` holds → "**The reduction to Σ₀ did NOT fix the
  'Σ blind to ℛ' defect. It reproduced it.**" Re-checked: zero hits in step-280/281/282.
- Author's own correction (`handoff/03`): "My written Σ said dir ∈ {three} … **My implementation
  already returned four. 272B is right.**" Cardinality lineage 2240 → 15 → 4.
- Strength: `Σ ⊥ Strength` **REFUTED** — "nesting, not orthogonality"; G-61 = strength-scale
  cardinality never derived.

## C · Constructs whose formal status is NOT what a casual reading suggests
| Construct | The fact |
|---|---|
| K | `K=(𝒜,ℛ)` selected by construction; **but** `consolidation/02` marks the 2-tuple "**SUPERSEDED** — no longer the candidate anywhere"; 273 tests `(A,R,Σ,E_L)`; DV-13: "variant #1 of at least seven … **no canonical K_t is established by the corpus**"; `KNOWLEDGE-STATE-FINAL-AUDIT` §1: "**K is a representation whose referent is undeclared**" |
| Evidence | 3-field `(ref,polarity,state)` **vs** 9-field amendment (adopted in the closure audit; carried by handoff/05) — "OVERLOADED, 3 definitions"; "Evidence has no identity" (TG-08) |
| Qualification | "**UNDEFINED [US]**" (canonical theory) vs "**CORPUS ESTABLISHES**, my BLOCKED marking was a category error" (closure audit) vs "**no body … OPEN, BLOCKING**" (TG-14) |
| Determination | 8-field aggregate recovered from §157.22 (836 occurrences / 172 files) — "**CORPUS-ESTABLISHED AND LOST**"; "part of the canonical formal theory? 🔴 **NO** … **Not promoted**"; glossary carries a *different* functional sense (TV-F-074 collision) |
| Authority | "a competence, not an actor"; box 6 "**✗ — the authority→gate binding is undefined**"; THREE objects (Authority/Authorization/Standing); TG-07 "**fourth sense**" OPEN |
| AuthorityAct | "**CASE B — implied but incompletely typed. REFINEMENT, not innovation**" — `Grant` exists, 132/132; corrects the earlier "0 corpus occurrences" claim ("The error was lexical") |
| Identity | `id = H(P,e,c,t,Π)`; **TG-06 OPEN, BLOCKING** — "id hashes a mutable field"; **two opposite repairs exist** (project `e.state` OUT vs the applied step-282/13 fix that keeps `state` IN); TG-06 "is not marked closed anywhere" |
| δ / commit | **TG-09 OPEN, BLOCKING** — "no body for the commit case … executed: K₁ is K₀ == True" |
| merge | `merge = union` "**REFUTED as adequate**" — cannot deduplicate (TG-11 OPEN) |
| History | external to K, FROZEN; `H ∈ K` (253) "resolved against on executed evidence, **preserved** as a valid observation" |
| Replay | round-trip "**PASSES 3/3 AND THE RESULT REFUTES THE PREMISE … passes VACUOUSLY**"; RICH round-trip False, fields destroyed ['q','s','e','c','u'] |
| Uncertainty / non-identifiability / missingness | the three "inexpressible" exclusions **FALSIFIED** — corpus has `U(H)` (31.24), `Identifiable(g,Ω)` (31.20); classified "**a governance gap, not a mathematical one**" (constructions exist, never promoted) |
| Constitution | "the theory REQUIRES a constitutional layer" = **VR, not demonstrated**; "Both remain open" |
| Admissibility | 4 vs 5 vs 6 conjuncts "**PRESERVED, unreconciled**" (canonical theory) vs "**NOT competing** … three GATE SETS" (closure audit) |
| Q_t | **name collision**: step-281 `Q_t ⊆ P` outside K vs corpus 025a-1 `K_t=(A_t,Q_t,E_t,C_t,P_t)` where Q_t is INSIDE K. "No artifact reconciles the two uses." |
| 𝒪 (algebra) | "Thirteen operations fully typed" vs 12 tabled vs handoff "13 ops"; "**Three of eleven have no body**"; "The operation set is complete — **OPEN**" |

## D · 15 inter-artifact contradictions recorded (see extractor record for full quotes)
𝒪_core verified-vs-unproven (and the later artifact says unproven) · membership 19 vs 6 vs 14/18 ·
"19 of 24 boxes closed" (19:58) vs "24/24 met" (20:10) · 28/30 vs 30/30 symbols (+ ℐ, 𝒩 never
enumerated) · Evidence 3-field vs 9-field · Qualify undefined/established/blocking · E,V collision
"local rebindings" vs "live ambiguities" · three exclusions declared vs falsified · **GC-1 policy
loop closed twice** · Σ⊥Γ three routes vs two · admissibility preserved-vs-not-competing · Q_t
naming · identity repair two opposite fixes · Determination aggregate vs function · AuthorityAct
innovation-vs-exists.

## E · The lane's own standing self-caveats (must survive into any book use)
> `step-281/08`: "**IC281 is a computational result about a theory revision, certified by the same
> session that proposed it.**"
> `handoff/06`: "**The handoff is ready. It is a weaker starting position than the synthesis
> assumed, and that is the correct result of checking rather than asserting.**"
> `handoff/04` on GC-1: "**My Step-282 governance analysis therefore consumed the non-authoritative
> resolution.** … ES-005.4 says the ratified one does [stand]." Status: **OPEN**.

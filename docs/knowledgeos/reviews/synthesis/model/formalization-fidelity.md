# 2B-6 · Formalization Fidelity — Zero, Lord, Sārathi

**Test applied (gate's criterion):** does an invariant set `I_C` exist with `I_C(C) = I_C(F(C))`?
Transformation ≠ error; the question is whether it was **explicit and justified**.

| | **Zero** | **Lord** | **Sārathi** |
|---|---|---|---|
| Original semantic invariant | absence must not collapse into other states (`UNKNOWN ≠ ABSENT`, `NO_EVIDENCE ≠ INVALID_EVIDENCE`) ⟦READ 105126⟧ | non-identity guard (*"does NOT assert Lord = Ω"*); analytical stance (Q17) ⟦READ⟧ | **"the Knower owns problem, purpose, decision"** ⟦READ 151244⟧ |
| Formal definition | `Zero(K,G,EC)` — goal-indexed discrepancy ⟦READ 025d⟧ | next-action selector over `(K_t,Z_t,G)` ⟦READ 025g⟧ | decision determination + **authorization** ⟦READ 025h⟧ |
| Mathematical properties | computable; non-Boolean (⟦C⟧ *"why Boolean logic fails"*); retains *why* unsatisfied — a **structured, typed discrepancy** | selection operator; properties NOT ESTABLISHED at read depth | decision algebra over `D`; utility/risk; authorization gate |
| Computational role | feeds Lord (`Z_t`) | feeds Sārathi | terminal: decision + authorization |
| Later architectural role | audited within M₄₉ (Step 50) | consumed by 025h; reference machine ⟦TITLE⟧ | boundary to the Knower |
| `I_C` preserved? | **PARTIAL** — the four-way non-satisfaction typology (unknown/conflicting/missing/invalid) ⟦READ⟧ echoes the lens's non-collapse; but goal-freeness lost and the mapping unstated | **NO** for Ω-c; the guard survives vacuously (nothing is identified with Ω because Ω is gone) | **YES** — the ownership invariant maps onto authorization semantics |
| Classification | **TRANSFORMED** | **DISPLACED** (name-level) / responsibilities re-routed (see lord-omega-analysis) | **PRESERVED** |
| Explicit + justified? | **NO** — 0 "lens" mentions; justification given is computability, not fidelity | **NO** — 0 mentions; contradicts Q17 uncited | **PARTIAL** — no citation, but the constraint visibly governs the design |

**The candidate principle, now with its evidence stated (gate §5):**
> ⟦INT⟧ *Formalization preserved the concept whose semantics had been stated as an **invariant/
> constraint** (Sārathi) and transformed or displaced the concepts that had only **definitions**
> (Zero) or **stances** (Lord).*
Status: **CANDIDATE PRINCIPLE — one corpus, three cases, untested elsewhere.** ⚠ A three-case sample
cannot distinguish this principle from coincidence; recorded for Phase-3 testing against the *kernel*
corpus's formalizations, not asserted.

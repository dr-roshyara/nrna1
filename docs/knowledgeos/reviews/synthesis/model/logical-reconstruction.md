# 2B-7 · Logical Reconstruction — the smallest model explaining the strongest evidence

**Status: CANDIDATE MODEL — explicitly NOT the final architecture.** Every element carries
evidence source · epistemic status · dependency · unresolved questions. Built only from
elements whose separation survived 2B-1..6.

---

## The candidate model

```
LAYER 0 · AUTHORITY
  Knower — owns purpose, frame, IdealState, final decisions
    [151244, Q11, Q18, 025h] · INVARIANT-backed · unresolved: none at this layer

LAYER 1 · REFERENCE
  Goal G  +  Epistemic Contract EC (derived from Knower-owned IdealState)
    [025d, 025e, Q11/Q18] · FORMALIZED · carries Ω-a by absorption
    unresolved: does dropping Ω-b (unboundedness) lose anything? (top review item)

LAYER 2 · INTAKE
  Source Observation ≠ Semantic Observation  (Option 3)
    [125403] · FORMALIZED, untested · unresolved: options 1/2 never recovered

LAYER 3 · EVIDENCE
  dependency resolution → then aggregation (saturating | bayesian-like)
    [EXP-01 + CSV] · COMPUTATIONALLY TESTED (the model's strongest layer)
    unresolved: operator selection (OQ-11); role/acquisition carriers (kernel-corpus questions)

LAYER 4 · DETERMINATION
  extraction ≠ determination; warrant established here
    [234405; candidate host step-008] · single-source INVARIANT-candidate
    unresolved: OQ-03 — the formal hosting was never verified

LAYER 5 · STATE + DISCREPANCY
  KnowledgeState K_t (over the 8 primitives) ; Z_t = Zero(K_t, G, EC), 4-way typed
    [049, 050, 025d] · kernel VALIDATED-WITHIN-TESTED-SCOPE (Step 50)
    unresolved: Knowledge/Question/Ω not primitives — derived-status never argued (EG-06)

LAYER 6 · AGENCY SPLIT
  Proposal (selector, no authority) → Decision+Authorization (bounded) → Knower authority
    [025g, 025h, C-014] · FORMALIZED; invariants preserved (fidelity map)
    unresolved: the selector's name (the Lord collision) — HPA decision

LAYER 7 · ACTION + LOOP
  authorized action → execution → new observation
    [025z, step-056] ⟦TITLE⟧ · SPECIFIED
    unresolved: action/execution boundary; reference-machine execution status

CROSS-CUTTING
  Validation (three loci: evidence/kernel/system) · Governance (025f; the Step-121 gap
  finding proves the layer is live and currently unenforced: "constitution can be silently weakened")
```

## What this model deliberately excludes
- any Kernel *membership* ruling (extent/contents remain Session-2 territory in the kernel corpus)
- the Ātma identification (C-012 OPEN), the Knowledge Atom, the dimension theory (dropped in R5,
  unreconciled — EG-06)
- the self-verification suite as *evidence* (defined, not executed — EG-05 permanent table)
- any book structure

## Why this is the smallest such model
⟦INT⟧ Remove any layer and a *tested or invariant-backed* result loses its home: L3 holds the only
CSV-tested results; L0/L6 hold the only three-regime invariants; L1 holds the Ω absorption; L4 holds
the corpus's founding distinction; L5 holds the audited kernel. Nothing else in the corpus reaches
those evidence classes. ⚠ Minimality here means *smallest-explaining-the-evidence*, not *proven
minimal* (the same distinction Step 50 draws).

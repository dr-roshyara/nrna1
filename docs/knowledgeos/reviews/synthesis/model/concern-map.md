# 2B-1 · Concern Map

**Rule:** *"Do not assume every item is a separate component. Determine whether the corpus supports
separation."* Verdicts: `SEPARATE (evidenced)` · `SEPARATE (weakly)` · `NOT SEPARABLE on current
evidence` · `NOT ESTABLISHED`.

| Concern | Separation verdict | Evidence |
|---|---|---|
| **observation** vs **evidence** | **SEPARATE (evidenced)** | Option 3: ⟦C⟧ *"what was observed ≠ what was understood"* → Source vs Semantic Observation ⟦READ 125403⟧; `025a-2` evidence→assessment→assertion chain ⟦TITLE⟧; R4 layer split (representation ≠ aggregation) ⟦READ⟧ |
| **evidence** vs **knowledge** | **SEPARATE (evidenced)** | EXP-01: evidence carries properties (duplication, dependence, corroboration) that knowledge-state semantics must not inherit raw ⟦READ⟧; `Knowledge` absent from the 8 primitives while `Observation`/`Proposition` present ⟦READ 049/050⟧ |
| **determination / warrant** | **SEPARATE (evidenced)** vs extraction | ⟦C⟧ *"Extraction retrieves material. Determination establishes what that material warrants asserting"* ⟦READ 234405⟧; R1 names it *the missing mathematical object* |
| **question** | SEPARATE (weakly) | the entire R2 programme treats questions as first-class; but no R5 primitive carries them — `question:10` hits in 049 are section headers ⟦OBS⟧. **Survival into the kernel: NOT ESTABLISHED** |
| **context** | **SEPARATE (evidenced)** | `EC` is a distinct argument of `Zero(K,G,EC)` and has its own algebra (`025e`) ⟦READ⟧ |
| **purpose** | SEPARATE (evidenced, as ownership) | Q11/Q18: the Knower owns the sufficiency threshold and its changes ⟦READ⟧; 151244: Knower owns *problem, purpose, decision* ⟦READ⟧ |
| **proposal** vs **decision** | **SEPARATE (evidenced)** | ⟦C⟧ 025h: *"Lord may conclude 'the next best action is X'; Sārathi must answer 'should we migrate, postpone, change the plan, or stop?'"* ⟦READ⟧ |
| **decision** vs **authorization** | SEPARATE (weakly) | 025h couples them (*"Decision, Utility, Risk **and Authorization**"*); the Knower-ownership rule implies authorization is distinct from computing the decision; internal split NOT ESTABLISHED |
| **action / execution** | SEPARATE (weakly) | `Action` is an 049 primitive; `025z` (action/intervention/control) and Step 56 (reference machine) ⟦TITLE⟧; the action/execution boundary itself NOT ESTABLISHED |
| **memory/state** | **SEPARATE (evidenced)** | `State` and `Event` both primitives; ⟦C⟧ Step 50 survives an attack precisely because *"observation and epistemic qualification are separate concepts"* ⟦READ⟧ |
| **validation** | **SEPARATE (evidenced)** — and cross-cutting | operates on the whole chain: EXP-01 (evidence layer) · Step 50 (kernel) · Steps 109–124 (system/repository) — three different loci ⟦READ⟧, supporting the gate's *"validation operates across the entire chain"* |
| **governance** | **SEPARATE (evidenced)** | own algebra (`025f`) ⟦TITLE⟧; own conformance step (104) ⟦TITLE⟧; and its absence detected as a real finding — ⟦C⟧ `CRITICAL GOVERNANCE GAP` ⟦READ 121⟧ |
| **warrant vs validation** | NOT SEPARABLE on current evidence | both assess sufficiency; the corpus never contrasts them directly. Flagged, not merged |
| **knowledge vs memory** | NOT ESTABLISHED | the corpus does not pose the question in this phase's material |

⟦INT⟧ Net result: the gate's six-box sketch (Knower → Observation/Evidence → Determination →
Proposal → Decision → Action) is **supported as a set of separations** — every arrow's endpoints are
separately evidenced — with two soft joints (decision/authorization, action/execution) and validation
+ governance as cross-cutting rather than stages.

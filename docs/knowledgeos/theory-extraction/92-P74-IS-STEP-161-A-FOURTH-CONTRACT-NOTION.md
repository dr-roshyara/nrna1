# `P-74` — Is `step_161`'s *"Semantic Contract"* a Fourth Contract Notion?

**2026-09-09 · Lane T.** After [`91` `P-73`](./91-P73-TIMESTAMP-THREAD-METHOD-ADOPTION-AND-THE-ETA-THREAD.md).
**Method:** timestamp-thread reading *(`MEMORY.md`, adopted 2026-09-09)*.

# 1. Executive verdict

$$\boxed{\mathbf{A\ FOURTH\ USE\ OF\ THE\ WORD\ —\ but\ NOT\ a\ fourth\ contract\ of\ the\ same\ KIND.}}$$

⭐⭐⭐ **`step_161`'s *"semantic contract"* is a TUPLE SIGNATURE per concept. `EC`, `PC` and `𝔠` are
REQUIREMENT SETS supplied as parameters. Different types, one word.**

$$O = (subject, observation, method, time, context) \qquad K = (claim, evidence, authority, validity, status)$$
$$\textbf{versus} \qquad ev : \mathcal K \times R \times \mathbf{EC} \to \mathcal S_{gap}, \qquad Req_P(\mathbf{PC}), \qquad Dist_{\mathbf{EC}}(K)$$

$$\boxed{\begin{array}{c}\textbf{⇒ } \mathbf{step\_161\ does\ NOT\ discharge\ step\ 1.} \textbf{ It is precisely what } \mathbf{GN\text{-}46} \textbf{ already called}\\[3pt] \mathbf{\textit{"signatures without constructions"}} \textbf{ — } step\_161 \textbf{ IS the signature layer.}\end{array}}$$

⭐⭐ **But it bears on step 2**, and that is the audit's positive result *(§5)*.

$$|K| = 11 \textbf{ — unchanged, } \mathbf{[REC]}. \textbf{ ⛔ No contract adopted, no notion merged, no category collapsed.}$$

# 2. ⚠️ A false positive caught, and it is the exact trap from `P-67`

⭐ `step_161` shows **`EC` = 3 occurrences** — which would have been reported as *"`step_161` references
`EC` three times."* ⛔ **Verified before use:**

$$\mathtt{DECISION} \times 2, \qquad \mathtt{EXECUTION} \times 1$$

$$\boxed{\textbf{⭐⭐⭐ } \mathbf{step\_161\ references\ }EC\mathbf{\ ZERO\ times.} \textbf{ And } \mathtt{epistemic\ contract} \textbf{ = } \mathbf{0} \textbf{, against a control of } \mathtt{Observation}=15.}$$

⭐ **Second consecutive audit in which `EKS-21`'s false-positive direction fired and was caught** — and
this one would have manufactured the *opposite* verdict *(that `step_161` is about `EC`)*.

# 3. What `step_161` actually is

**`161.1` the canonical semantic chain** — nine stages, `Observation → Inquiry → Evidence → Knowledge →
Determination → Decision → Authorization → Action → Execution`, with the explicit caveat: ⭐ *"this is
**not yet a workflow** … the arrows express **possible semantic dependency**, not automatic execution."*

**`161.2` the semantic envelope** — ⭐⭐ $x = (id, context, time, authority, provenance, state, content)$,
with ⚠️ *"the equation is a **reasoning model, not a database schema**."*

**`161.3` Observation** — one `### Definition`: *"a recorded representation of something perceived,
measured, detected or otherwise encountered **without automatically asserting its interpretation**."*

**`161.4` Observation invariant** — ⭐⭐ *"**An observation must not silently contain an unmarked
conclusion.**"*

**`161.55` Semantic contracts — first draft** — ⭐⭐⭐ **eight tuple signatures**: `O`, `I`, `E`, `K`,
`D`, `Dec`, `Auth`, `A`. ⚠️ *"These are **semantic contracts, not final persistence schemas**."*

**`161.59` conclusion** — *"We can now **freeze the first semantic contract layer**."*

# 4. ⭐⭐ The four notions, typed and kept apart

| notion | source | what it is | type |
|---|---|---|---|
| **`EC`** epistemic contract | `K0` `P4`; Part XIX §19.3 | the distinctions/requirements a contract demands | ⭐ **a REQUIREMENT SET, supplied as a parameter** |
| **`PC`** persistence contract | Part XIX §19.74 — `Req_P(PC)` | persistence requirements | ⭐ **same kind** |
| **`𝔠 = C_KOS`** | the twelve-step order, step 1 | the contract to be **constructed by `η`** | ⭐ **same kind, unconstructed** |
| ⭐⭐⭐ **`step_161` semantic contract** | `phase_measure_theory` `161.55` | **field signatures + invariants per concept** | ⭐⭐⭐ **a TYPE DECLARATION — a different kind** |

$$\boxed{\textbf{⭐⭐ Three of one kind, one of another. } \mathbf{The\ overload\ is\ worse\ than\ a\ fourth\ instance:\ it\ crosses\ a\ TYPE\ boundary.}}$$

### Category classification *(commission's ladder, uncollapsed)*

| category | `step_161`'s contract ↔ `EC` |
|---|---|
| CHRONOLOGICALLY ADJACENT | ⭐ **YES — same day.** `step_161` is 2026-08-29 01:13; `K0` is 2026-08-29 |
| LIKELY SAME RESEARCH THREAD | ⛔ **NO** — different subcorpora *(`phase_measure_theory` vs `verification/spec`)*, different threads |
| CONTINUATION CONFIRMED BY CONTENT | ⛔ **NO** — `step_161` references `EC` **zero** times |
| ⭐⭐⭐ **SAME CONCEPT** | ⛔ **REFUTED** — requirement set vs type signature |
| SAME ARTIFACT · SAME PROVENANCE LINEAGE | ⛔ **NO** |
| INDEPENDENT RESEARCH | ⭐ **PLAUSIBLE, not established** — same day, no cross-citation either way |
| INDEPENDENT REPLICATION | ⛔ **NO** — they are not the same claim, so replication is not the relation |

⭐⭐ **A textbook case for the commission's warning:** same day, adjacent in time, ⛔ **and the concepts
are of different types.** ⭐ **Adjacency bought nothing here, and that is worth recording as much as when
it buys everything.**

# 5. ⭐⭐ The positive result — `step_161` bears on step 2, not step 1

⭐ Step 2 is `ℳ_K`, the model. `step_161` supplies **eight typed concepts** with field signatures, a
**seven-field envelope**, and **fourteen `invariant` mentions** including `161.4`'s unconditional one.
⭐⭐ **That is a semantic model with constraints — `ℳ_K`- and `ℐ`-shaped.**

⚠️⭐⭐ **And `K0` already discharged step 2 by a different route** *(`P-72`: `P4`'s opaque `𝒦` + `P6`'s
`δ`/`Replay`)*.

$$\boxed{\textbf{⭐⭐⭐ } \mathbf{TWO\ routes\ to\ step\ 2:} \textbf{ } K0\textbf{'s } \mathbf{opaque\ transition\ system} \textbf{ and } step\_161\textbf{'s } \mathbf{typed\ concept\ signatures.}}$$

⛔ **They are not the same model** — one is deliberately **opaque** about state, the other **enumerates
fields.** ⚠️ **`K0` §0's structural discovery says the estate's results depend on **no specific tuple** —
so `step_161`'s enumeration is exactly what `K0` found unnecessary.** ⭐ **That is a tension worth naming,
⛔ not a contradiction: an unnecessary structure is not a wrong one.**

# 6. The thread, recorded

| field | value |
|---|---|
| **`thread_start`** | `20260829-011309_step_161_semantic-contract-reconstruction` *(1 807 ln)* |
| **`chronological_order`** | `161 → 162` invariant/boundary derivation `→ 163` context map `→ 164` domain events & legitimate state transitions `→ 165` aggregate/consistency boundaries `→ 166` process managers `→ 167` **formal verification obligations** `→ 168` **the verification lattice** |
| **`topic_transition`** | ⛔ **none within 161–168** — the topic is coherent: signatures → invariants → boundaries → events → aggregates → verification |
| **`thread_end`** | ⚠️ **not established** — the walk stopped at `168`; the thread was still on topic |
| **`key_claims`** | `161.55`'s eight signatures · `161.59`'s *"freeze the first semantic contract layer"* · `161.2`'s envelope · `161.4`'s invariant · the handoff `Semantic Contract → Invariant → Consistency Boundary → DDD Boundary` |
| **`files_read`** | ⭐ **1 of ≥8** — `step_161` in its decisive sections; `162`–`168` **titles only** |
| **`provenance`** | `phase_measure_theory/`, top level *(not the `GN-25` prompt folder)* |
| **`admissibility_status`** | ⚠️ **CHARACTERIZED** for `161`; ⛔ **`FOUND` only** for `162`–`168` |

⭐⭐ **`step_167` *"formal verification obligations"* and `step_168` *"the verification lattice"* are dated
2026-08-29 01:28 and 01:31 — ⭐⭐⭐ hours before the `GN-46` verification programme's artifacts of the same
day.** ⛔ **Whether they are related is `[UNWITNESSED]`** — and the resemblance is exactly the kind this
audit has just refused to upgrade.

# 7. What this does **not** establish

⛔ **`step_161` does not construct `η`** — `contract` appears 5 times, all about its own signature layer.
⛔ **It does not discharge step 1**, and `GN-46`'s *"signatures without constructions"* is the estate's
own prior verdict on this class of artifact. ⛔ **It is not adopted** — *"freeze"* here is a research-lane
act on a *"first draft"* layer. ⛔ **No cell, no notion, no category merged.**

# 8. Backlog — ⛔ **nothing new; `EKS-14` extended a third time**

⭐⭐⭐ **`K0` §1 disambiguates *"kernel"* across five senses. Nothing disambiguates *"contract"* — and it
now carries four senses across two types.** ⛔ **Same class as `EKS-14`'s recorded problem, different
token, and the sharper form: for *"kernel"* a register exists and was not extended; for *"contract"*
there is no register at all.** ⭐ Appended there rather than split into a new item.

# 9. Status register
**`[EMP]`** ⭐⭐⭐ `161.55`'s eight tuple signatures verbatim · `161.2`'s seven-field envelope and its
*"reasoning model, not a database schema"* · `161.3`'s Definition · `161.4`'s invariant · `161.59`'s
freeze · the `Semantic Contract → Invariant → Consistency Boundary → DDD Boundary` handoff · ⭐⭐ **`EC` =
0 in `step_161`** *(the three hits are `DECISION`/`EXECUTION`)* · `epistemic contract` = **0** ·
`contract` ×5, `invariant` ×14, control `Observation` = 15 · the `161`–`168` sequence.
**`[DERIVED]`** ⭐⭐⭐ a fourth **use**, not a fourth **contract** — requirement set vs type signature ·
`step_161` does not discharge step 1 · **two routes to step 2**, and they are not the same model.
**`FOUND`** ⭐⭐ `step_162`–`step_168`, including *"formal verification obligations"* and *"the
verification lattice"*.
**`[UNWITNESSED]`** ⭐ any relation between `step_167`/`168` and the `GN-46` programme of the same day.
**`[REFUTED]`** ⭐⭐ **SAME CONCEPT** for `step_161`'s contract ↔ `EC`.
**`[OPEN]`** ⭐⭐ `η` / step 1 · `step_161`'s thread end · whether `161.2`'s envelope is a carrier
candidate · all carried opens · ⛔ **well-foundedness / terminality / acyclicity — DEFERRED.**
**Governance:** `|K| = 11` **`[REC]`** — unchanged, unfrozen.

### 10. ONE NEXT UNRESOLVED QUESTION

$$\boxed{\textbf{Does } \mathbf{step\_167\ \textit{"formal verification obligations"}} \textbf{ and } \mathbf{step\_168\ \textit{"the verification lattice"}} \textbf{ feed the } \mathbf{GN\text{-}46} \textbf{ programme — or is the same-day resemblance coincidence?}}$$

⭐⭐⭐ **This is the join the whole series has been missing, and the method produced it: two verification
efforts, hours apart on 2026-08-29, in different subcorpora.** ⭐ If `168`'s lattice **feeds** `GN-46`,
then the verification programme has an upstream this lane never traced, and its stopped state
*(`EKS-25`)* blocks more than it appears to. ⛔ **If they are independent, the estate ran two verification
designs on one day without either citing the other** — which would be the fourth such pair recorded here.
⚠️ **And the check must be citation and content, ⛔ never the shared word *"verification"* or the shared
date** — the two things that make the coincidence look like a lineage.

```
A FOURTH USE OF THE WORD — BUT NOT A FOURTH CONTRACT OF THE SAME KIND.

step_161's "semantic contract" is a TUPLE SIGNATURE per concept: eight of them — Observation, Inquiry,
Evidence, Knowledge, Determination, Decision, Authorization, Action — each a field list, plus a
seven-field semantic envelope and fourteen invariant mentions. EC, PC and C_KOS are REQUIREMENT SETS
supplied as parameters. Different types, one word: three of one kind, one of another, so the overload is
worse than a fourth instance because it crosses a TYPE boundary.

CONSEQUENTLY step_161 does NOT discharge step 1. It is precisely what GN-46 already called "signatures
without constructions" — step_161 IS the signature layer, and the estate's own prior verdict on that
class stands.

A FALSE POSITIVE CAUGHT, THE EXACT TRAP FROM P-67: step_161 shows EC three times, which would have been
reported as "step_161 references EC". Verified before use — the three hits are DECISION twice and
EXECUTION once. step_161 references EC ZERO times, and "epistemic contract" zero, against a control of
Observation = 15. Second consecutive audit in which the false-positive direction fired, and this one
would have manufactured the opposite verdict.

CATEGORY CLASSIFICATION, UNCOLLAPSED: chronologically adjacent YES — same day, step_161 at 01:13 and K0
also 2026-08-29. Likely same thread NO, different subcorpora. Continuation confirmed by content NO, zero
references. SAME CONCEPT REFUTED. Same artifact or lineage NO. Independent research PLAUSIBLE but not
established. Independent replication NO — they are not the same claim, so replication is not the
relation. A textbook case for the commission's warning: adjacency bought nothing here, and that is worth
recording as much as when it buys everything.

THE POSITIVE RESULT: step_161 bears on step 2, not step 1. Eight typed concepts with signatures, an
envelope and unconditional invariants is a semantic model with constraints. So there are TWO routes to
step 2 — K0's opaque transition system and step_161's typed signatures — and they are NOT the same model:
one is deliberately opaque about state, the other enumerates fields, and K0's own structural discovery is
that no result depends on a specific tuple. So step_161's enumeration is exactly what K0 found
unnecessary. A tension worth naming, not a contradiction: an unnecessary structure is not a wrong one.

THREAD RECORDED: start step_161, running 161 to 162 invariant/boundary derivation, 163 context map, 164
domain events and legitimate state transitions, 165 aggregate and consistency boundaries, 166 process
managers, 167 formal verification obligations, 168 the verification lattice. No topic transition within
161-168; thread end NOT established, since the walk stopped while still on topic. Files read: 1 of at
least 8 — step_161 in its decisive sections, 162 to 168 titles only. State CHARACTERIZED for 161, FOUND
only for the rest.

No new backlog item: K0 §1 disambiguates "kernel" across five senses, and nothing disambiguates
"contract", which now carries four senses across two types. Same class as EKS-14's recorded problem in a
sharper form — for "kernel" a register exists and was not extended; for "contract" there is no register at
all. Appended to EKS-14 as a third instance rather than split.

|K| = 11 UNCHANGED, [REC], UNFROZEN. No contract adopted, no notion merged, no category collapsed.

ONE NEXT UNRESOLVED QUESTION: Do step_167 "formal verification obligations" and step_168 "the
  verification lattice" feed the GN-46 programme — or is the same-day resemblance coincidence? Two
  verification efforts, hours apart on 2026-08-29, in different subcorpora. If 168's lattice feeds GN-46,
  the verification programme has an upstream this lane never traced, and its stopped state blocks more
  than it appears to. If they are independent, the estate ran two verification designs on one day without
  either citing the other — the fourth such pair recorded here. And the check must be citation and
  content, never the shared word "verification" or the shared date, which are the two things that make
  the coincidence look like a lineage.

NO three_model_convergence INSPECTION — excluded by path from every command, unread.
```

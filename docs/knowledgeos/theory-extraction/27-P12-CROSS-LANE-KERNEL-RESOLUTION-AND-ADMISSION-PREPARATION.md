# `P-12` — Cross-Lane Kernel Resolution & Admission Preparation

**2026-09-07 · Lane T · RESOLUTION-PREPARATION ONLY.** Starting state: [`26` `P-11`](./26-P11-POST-RULING-KERNEL-RECONCILIATION-AND-RESIDUAL-CLOSURE.md).

> ⛔ **No new capability · no recursive decomposition · no corpus-wide archaeology · no transition
> archaeology restarted · `P-08`…`P-11` not reopened · no Lane M question answered inside Lane T ·
> `[DER-S]` never promoted · MD-018 and 3MC untouched · no Schema v3, architecture, A/B/C/D, relational
> / graph / document schema, event sourcing, temporal tables, UUID, aggregate or implementation.**

# 1. Starting state — preserved, not assumed final

**Kernel: 11 cells** — **3 CLOSED** (`K1`, `K4a-i`, `K5`) · **8 CONDITIONALLY CLOSED** · **2 OPEN
non-cells** (corpus-state identification, third axis).

# 2. `R0` — the epistemic distinction, carried forward unchanged

$$\boxed{\textbf{CORPUS: } \textit{MD-018 monotonicity} = \mathbf{NOT\ EVIDENCED} \qquad \textbf{GOVERNANCE: } \mathbf{STIPULATED\ MONOTONE} \qquad \Rightarrow \mathbf{[DER\text{-}S]}}$$

**The four `R0`-dependent conclusions, and only four:** `K3` exceptional scope · `K2` independent
necessity · `P-08` §17 restored redundancy · `P-08` §19 standing negative.
⭐ **No fifth is discovered here** — §3.1 of `P-11` showed the decomposition structure is
monotonicity-independent, and nothing in `P-12` changes that.

# 3. ⭐ Change of mode — stated explicitly

$$\boxed{\begin{array}{c}\textbf{Lane T is NO LONGER IN CAPABILITY-DISCOVERY MODE.}\\ \textbf{It is in CROSS-LANE DEPENDENCY-RESOLUTION mode.}\\[6pt] \textbf{DISCOVER} \rightarrow \textbf{EXHAUSTED} \quad \textbf{RECONCILE} \rightarrow \textbf{COMPLETE} \quad \textbf{RESOLVE OWNERSHIP} \rightarrow \textbf{REQUIRED} \quad \textbf{ADMIT} \rightarrow \textbf{REQUIRED}\end{array}}$$

⛔ **An unresolved Lane M question is not converted into a Lane T research task because it is
interesting.**

---

# 4–5. The residual question set, classified `A` / `B` / `C`

⛔ **Not every unresolved question is "kernel-changing".**
**`A`** = can add/remove/alter a cell · **`B`** = foundational, alters interpretation without adding a
capability · **`C`** = exercises an existing capability.

| # | question | class | owner | consequence |
|---|---|:--:|---|---|
| **`Q1`** | ⭐ **May an established identity be retired?** (`O-P09-1`) | ⭐ **`A`** | **Lane M** | YES ⇒ `K4c-i`/`K4c-ii` **active**; NO ⇒ their conditionality **reduces or lapses** |
| **`Q2`** | ⭐ **Text fixed, meaning changed: same candidate or new?** | ⭐ **`A`** | **Lane M** *(an identity adjudication)* | decides **`K2`-A**, one direction — §14 |
| **`Q3w`** | ⭐ **Is corpus-state identification a capability (`C2`) or a mechanism for `K5` (`C3`)?** | ⭐ **`A`** | **Lane M / Governance** | `C2` ⇒ **a 12th cell**; `C3` ⇒ **no cell** |
| **`Q4a`** | **Third individuation axis** *(temporal position)* | ⭐ **`A`, dormant** | Lane T *(if a witness ever appears)* | would **re-individuate every cell** |
| **`Q5r`** | **Role overload — relation vs justification** | ⭐ **`A`, dormant** | Lane T *(if a witness appears)* | would split the **four link cells** |
| **`Q6`** | **Kernel identity** | ⭐ **`B`** | **Lane M** | alters the **interpretation** of `K1`'s `Δ`; adds nothing |
| **`Q7`** | **Equivalence** | ⭐ **`B`** | **Lane M** | bears on **merge-warrant content** → `K3b`'s content, not its existence |
| **`Q8`** | **`Q3` (`ℐ`/`𝓘`: one candidate or two?)** | ⭐ **`C`** | **Lane M** | answering *"one"* **IS a merge** ⇒ **exercises** `K4c`; changes no cell |
| **`Q9`** | **`Q4` (transfer)** | ⭐ **unquantified** | **Lane M** | no kernel edge identified |
| — | **`O-INT001-1`** correction vs regression | **`C0`** | Governance | ⭐ **kernel-discharged in `P-11` §6** — `K3b` bears it; the **rule** is still owed |
| — | `F-4` / `F-5` | **`C0`** | extraction | standing recorded defects |

⭐ **5 of 9 are `A` (two of them dormant) · 2 are `B` · 2 are `C`/unquantified.**

---

# 6. ⭐⭐ Special test — can `W6` really add a cell? (§13)

> *If corpus-state identification were absent, what exact loss distinction becomes unrecoverable that
> `K5` cannot already express?*

## The history pair

A measurement is cited: **"143 symbolic sites"**. Re-running the pattern today yields a different
number.

| | `H1` | `H2` |
|---|---|---|
| what happened | ⭐ the measurement was **correct at state `S₁`**, and the corpus has since changed | ⭐ the measurement was **wrong** |
| `K5` says | *"the ground is not checkable"* | *"the ground is not checkable"* — ⭐ **identical** |
| what the estate must do | ⭐ nothing — it is a **stale-but-valid historical observation** | ⭐ **supersede it (`K4a`) with a warrant naming the defect (`K3b`)** |

$$\boxed{\begin{array}{c}\textbf{A GENUINE loss distinction exists that } K5 \textbf{ CANNOT express: } \textit{stale-but-valid} \textbf{ vs } \textit{wrong}.\\ \boxed{\mathbf{C2\ REMAINS\ VIABLE.}}\end{array}}$$

## ⭐ And an argument by parity with `K1`, offered as evidence — not as a decision

`[DERIVED]` To record *"this was valid at `S₁`"*, `K3b`'s warrant must **denote a corpus state**. So for
measurement-grounded claims, corpus-state identification is **presupposed by `K3b`/`K4a`** — the same
shape as `P-09.3` §13's argument that every retention record presupposes `K1`. ⭐ **And that argument
concluded independence: a presupposed premise cannot be a derived conclusion.**

⛔ **Not a choice.** ⭐ Both alternatives keep their requirements:

| | what would be required to admit it | can architecture proceed without resolving it? |
|---|---|---|
| **`C2`** capability | ⭐ **the history pair above, admitted** — plus governance acceptance that the parity argument transfers | 🔴 **no — a 12th cell would change the constraint set** |
| **`C3`** mechanism | a demonstration that **every** such loss reduces to *"the current external ground is not checkable"* — ⚠️ **which `H1`/`H2` appears to refute** | ✅ **yes** |

⛔ **No corpus-state mechanism designed. `W6` remains `[OPEN]`, live, and its class undecided.**

---

# 7. ⭐⭐ Special test — can semantic reinterpretation really change `K2`-A? (§14)

⛔ **Identity is not resolved. Neither world is selected.**

| | **World I — `A ≺ A`** *(same candidate, changed meaning)* | **World II — `A ≺ A′`** *(new candidate, predecessor retired)* |
|---|---|---|
| **which capability bears the distinction?** | ⭐⭐ **`K2`** — the text is fixed, so *meaning* must be a **state component distinct from content** | ⭐ **`K4c`** — the old reading belongs to a **retired** candidate |
| **is `K2`-A decidable?** | ⭐ **PARTIALLY — one of two required directions.** *Text fixed / meaning changed* gives content-invariant-state-varying; ⚠️ **the converse still needs `model replacement`** (`[OPEN]`) | 🔴 **NO — `K2`-A stays undecided** |
| **does `K4c` become involved?** | 🔴 no | ⭐ ✅ **yes** |
| **what must be preserved?** | the **interpretation state** alongside the fixed text | the **retired candidate** + **`retired-into`** + a warrant |

## ⭐⭐⭐ The dependency this test uncovered

$$\boxed{\begin{array}{c}\textbf{World II IS a retirement — so it is UNAVAILABLE unless } Q1 \textbf{ answers YES.}\\ \boxed{Q2 \textbf{ DEPENDS ON } Q1\textbf{: semantic reinterpretation cannot be classified as World II while identity retirement is unresolved.}}\end{array}}$$

⭐ **This corrects the expected priority ordering.** `Q1` must precede `Q2` — verified, not assumed.

⭐ **And `P-11`'s correction is preserved:** semantic reinterpretation is **not** a text-changing
transition merely because meaning changes; its bearing on `K2`-A is ***same text / different meaning***,
not *text changed*.

---

# 8. ⭐ Special test — third axis, under the adopted stopping rule (§15)

| step | result |
|---|---|
| **1 alleged loss distinction** | *which of two prior states came first?* |
| **2 admissible transition** | ⭐ **two successive `τ2` re-dispositions of the SAME definition** — `τ2` is in the universe and can recur |
| **3 the required history pair** | `H1: K → 𝒦 → X → K` · `H2: K → X → 𝒦 → K` — ⭐ **same current state, same SET of priors `{𝒦, X}`** |
| **4 admissible corpus witness?** | ⭐⭐ 🔴 **ABSENT — measured.** Every re-disposition in the estate is a **first** re-disposition of its definition; **no definition has been re-dispositioned twice** |
| **5 already represented?** | ⭐ 🔴 **no** — `K3a` retains a prior value **per transition** and, as stated, entails **no ordering** over several such records |

$$\boxed{\begin{array}{c}\textbf{Steps 1, 2, 3 and 5 are satisfied. Step 4 is NOT.}\\ \boxed{\textbf{NO NEW CAPABILITY. Retain } \mathbf{[OPEN]}.}\end{array}}$$

⛔ **And do NOT infer that the third axis does not exist** — the witness is absent, which is not the
same as the distinction being unnecessary. ⭐ **A single second re-disposition would supply it.**

## `Q5r` role overload — the same rule, the same outcome

> *Does an admissible loss distinction show relation and justification are independently preservable?*

**Relation without justification: ✅ witnessed** (`P-08` §19's repaired witness — `resolved` recorded
with no warrant). **Justification without relation: 🔴 not witnessed.**

$$\boxed{\textbf{One of two required directions. NO SPLIT. Retain } \mathbf{[OPEN]}\textbf{ — ⛔ and no second role axis is manufactured because "link" contains two different words.}}$$

---

# 9. Dependency graph

⛔ **Edges mean *"resolution of X can change the status or content of Y"*. Conceptual association is
NOT an edge.**

```
R0 (ADMITTED) ──► K3a/K3b scope ──► K2-B  [CLOSED, DER-S]

Q1 identity retirement ──► K4c-i, K4c-ii        (activation)
        └────────────────► Q2                    ⭐ World II unavailable without Q1

Q2 semantic reinterpretation ──► K2-A            (one direction)
model replacement [OPEN]      ──► K2-A            (the converse direction)

Q3w W6 class ──► K5  (+ a possible 12th cell)
Q4a third axis ──► EVERY cell                    (re-individuation; witness ABSENT)
Q5r role overload ──► K3b, K4a-ii, K4b-ii, K4c-ii (link cells; one direction only)

Q6 kernel identity ──► K1's Δ                    (interpretation only, class B)
Q7 equivalence     ──► K3b's CONTENT             (not its existence, class B)
Q8 Q3 (ℐ/𝓘)       ──► exercises K4c              (no status edge, class C)
Q9 Q4              ──► no edge identified
```

| independently resolvable — no wait on another open question | ⭐ **`Q1` · `Q3w` · `Q4a` · `Q5r` · `Q6` · `Q7`** |
|---|---|
| **blocked by another question** | ⭐ **`Q2` (needs `Q1`)** · **`K2`-A (needs `Q2` AND model replacement)** |

⭐ **`K2`-A is the only kernel item requiring TWO resolutions.**

---

# 10. Prioritization — ranked, and the expected order does not survive

| rank | question | kernel effect | independent? | decidable by | cost | gate blocked |
|:--:|---|---|:--:|---|---|---|
| **1** | ⭐⭐ **`Q1` identity retirement** | **2 cells activate/lapse** | ✅ | ⭐ **a Lane M decision** | low | **E** |
| **2** | ⭐ **`Q3w` `W6` class** | ⭐ **may add a 12th cell** | ✅ | ⭐ **evidence (§6's pair) + governance** | medium | **D, E** |
| **3** | **`Q2` semantic reinterpretation** | **`K2`-A, one direction** | ⭐ 🔴 **needs `Q1`** | Lane M adjudication | medium | **C, E** |
| **4** | **`Q5r` role overload** | 4 link cells | ✅ | ⭐ **a witness, not a decision** | low | **A** |
| **5** | **`Q4a` third axis** | every cell | ✅ | ⭐ **a witness only** | ⭐ **zero — it waits** | **A** |
| **6** | **`Q6` · `Q7`** | interpretation / content | ✅ | Lane M | high | — |
| **7** | **`Q8` · `Q9`** | exercise / none | ✅ | Lane M | — | — |

$$\boxed{\begin{array}{c}\textbf{The expected ordering } (\textit{reinterpretation first}) \textbf{ DOES NOT SURVIVE VERIFICATION.}\\ \boxed{Q1 \textbf{ ranks first, because } Q2 \textbf{ depends on it (§7) and } Q1 \textbf{ is answerable by a DECISION alone.}}\end{array}}$$

---

# 11. Prepared intake requests — the two highest-priority

⛔ **Prepared, not created. `P-12` is not an intake-producing artifact.**

## `IR-Q1` — identity retirement

| field | content |
|---|---|
| **proposition to decide** | *May an established KnowledgeOS candidate identity be retired?* |
| **why Lane T cannot decide** | ⭐ Lane T declared it **blocking**; resolving its own blocker would be **adjudicating its own evidence** (`R-34`); and Lane T is **read-only w.r.t. 3MC** |
| **evidence available** | ⭐ `[EMP]` **retirement-with-retention practised 5×, deletion never** · `τ11` split `[EMP]` · merge **scheduled** (125 files) · `W1` |
| ⭐ **evidence explicitly ABSENT** | **any statement, anywhere, permitting or forbidding retirement** |
| **alternatives** | **YES** · **NO** · **YES-with-conditions** |
| **kernel consequence** | YES ⇒ `K4c-i`/`K4c-ii` **active**, obligations as `P-09.1` §5 · NO ⇒ conditionality **lapses**, `K4c` dormant · CONDITIONAL ⇒ active within the stated conditions |
| **authority** | Lane M / Governance |
| **corpus-state field** | ⭐ **`n/a by kind`** if answered by ruling; a state designator **required** if answered by evidence — ⚠️ **and `W6` would then obstruct it** |
| **expected epistemic status** | **`STIPULATED`** if a ruling · **`[EMP]`** if a corpus statement is produced |
| **consuming artifact** | a `P-13`-class reconciliation, on the `P-11` pattern |

## `IR-Q3w` — `W6` class

| field | content |
|---|---|
| **proposition to decide** | *Is corpus-state identification a persistence capability (`C2`) or a mechanism supporting `K5` (`C3`)?* |
| **why Lane T cannot decide** | ⭐ **the requirement is derived; the CLASS is not** — and `W6` blocks the test empirically |
| **evidence available** | ⭐⭐ **§6's `H1`/`H2` pair: a loss distinction `K5` cannot express** · the `K1`-parity argument · `τ7`'s **56 uncommitted renames** |
| ⭐ **evidence explicitly ABSENT** | ⭐ **any corpus-state designator existing at measurement time** — that absence is the blocker itself |
| **alternatives** | **`C2`** · **`C3`** |
| **kernel consequence** | `C2` ⇒ **a 12th cell**, and Gate `A` reopens · `C3` ⇒ **11 cells**, `K5` unchanged, Gate `D` reduces to a mechanism choice |
| **authority** | Lane M / Governance |
| **expected epistemic status** | `[DERIVED]` if the `H1`/`H2` pair is admitted · `STIPULATED` if ruled |
| **consuming artifact** | the same `P-13`-class reconciliation |

---

# 12. ⛔ Do not duplicate the MD-018 error

**Six levels, to be kept apart in every future intake:**

$$\boxed{\textbf{source text} \rightarrow \textbf{measurement} \rightarrow \textbf{interpretation} \rightarrow \textbf{stipulation} \rightarrow \textbf{adjudication} \rightarrow \textbf{derived consequence}}$$

⭐ **The MD-018 failure, named precisely so it cannot recur:** the **source text** said *ordered* and
*no-skip*; the **interpretation** silently read *monotone*; and the **derived consequence** (`P-08`
§17) inherited a premise that had never been **measured**. ⭐ **Only `INTAKE-001`'s explicit
`STIPULATED` label restored the boundary.**

⚠️ **Every intake record must state which of the six levels its payload occupies.**

# 13. Kernel closure principle — adopted

$$\boxed{\begin{array}{c}\textbf{The kernel is CLOSED WITH RESPECT TO the currently admitted transition universe when every}\\ \textbf{admissible loss distinction either MAPS to an existing capability or is EXPLICITLY CLASSIFIED as}\\ \textbf{unresolved without sufficient evidence to admit a new capability.}\end{array}}$$

⛔ **This is boundary-relative. It is NEVER *"the persistence ontology is universally complete."***
⭐ **Applied to `P-12`: `Q4a` and `Q5r` map to no capability and are explicitly classified unresolved
with an absent witness — so the kernel is closed with respect to them, without admitting anything.**

# 14. Architecture gates

| gate | | verdict | why |
|---|---|:--:|---|
| **A** | semantic closure | ⚠️ **CONDITIONAL** | 3 closed, 8 conditional; ⭐ `Q3w` `C2` could add a 12th; `Q4a`/`Q5r` dormant |
| **B** | epistemic / governance closure | ⭐ ✅ **PASS `[stipulated]`** | `INTAKE-001`; ⚠️ **the `O-INT001-1` rule is still owed but is kernel-neutral** |
| **C** | transition closure | 🔴 **BLOCKED** | ⭐ **narrowly: only `Q2` and `model replacement` matter** — ⛔ **not a request to enumerate every possible transition** |
| **D** | corpus-state closure | 🔴 **BLOCKED** | `W6` live; ⭐ **§6 shows the class question is now DECIDABLE IN PRINCIPLE** |
| **E** | cross-lane closure | 🔴 **BLOCKED** | ⭐ **1 of 9 admitted.** `Q1`, `Q3w`, `Q2` are the kernel-changing arrears |

$$\boxed{\textbf{ARCHITECTURE COMPARISON NOT PERMITTED. ⛔ No gate lowered because architecture work would be convenient.}}$$

⭐ **Gate `C` is narrowed, not relaxed:** *is the transition universe sufficiently characterized for the
conclusions relied upon?* — and the answer turns on **two** classifications, not on transition
archaeology.

# 15. Branching table — executable downstream

| future resolution | possible result | kernel consequence | next Lane T action |
|---|---|---|---|
| **`Q1`** | **YES** | `K4c-i`/`K4c-ii` **ACTIVE**; obligations per `P-09.1` §5 | reconcile; then `Q2` becomes classifiable |
| | **NO** | `K4c` **dormant**; ⭐ **World II of `Q2` is eliminated**, so `Q2` collapses to World I | reconcile; `K2`-A then needs only model replacement |
| | **CONDITIONAL** | active within stated conditions | reconcile + record the conditions |
| **`Q3w`** | **`C2`** | ⭐ **12th cell**; Gate `A` reopens | admit the cell; re-run the closure matrix |
| | **`C3`** | **11 cells**; `K5` unchanged | close Gate `D` to a mechanism choice `[ARCH]` |
| **`Q2`** | **World I** | ⭐ **`K2`-A one direction satisfied** | wait on model replacement for the converse |
| | **World II** | `K4c` bears it; **`K2`-A undecided** | ⛔ requires `Q1` = YES first |
| **`Q4a`/`Q5r`** | a witness appears | re-individuation / link split | re-run `P-09.3`'s criterion **on that witness only** |
| | no witness | ⭐ **nothing** | ⛔ **do not revisit** |
| **`Q6`/`Q7`** | any | interpretation / content only | annotate; **no cell change** |
| **`Q8`** | *"one"* | ⭐ **exercises `K4c`** | ⛔ requires `Q1` = YES to be performable |

---

# 16. Stopping condition — met

| condition | |
|---|:--:|
| no new capability introduced | ✅ ⭐ **and two were actively refused** (`Q4a`, `Q5r`) |
| no old derivation reopened | ✅ |
| every residual kernel-changing question has an owner | ✅ **9 questions, all assigned** |
| each has a precise decision boundary | ✅ **`A`/`B`/`C` + alternatives + consequences** |
| dependencies explicit | ✅ ⭐ **incl. the newly found `Q2 → Q1` edge** |
| required intake records specified | ✅ **`IR-Q1`, `IR-Q3w` prepared** |
| architecture gates explicit | ✅ **A–E** |
| the next action is executable by the responsible lane without further Lane T discovery | ✅ |

⛔ **STOP. `P-13` is not created automatically.**

---

```
P-12 CROSS-LANE KERNEL RESOLUTION PREPARATION COMPLETE — NO NEW CAPABILITY (and TWO were actively
refused under the adopted stopping rule: the third axis has an admissible transition and a constructible
history pair but NO corpus witness — no definition has been re-dispositioned twice — and role-overload
has only one of two required directions) — R0 REMAINS ADMITTED STIPULATION, its dependents still exactly
four — KERNEL REMAINS 11 CELLS [DER-S] — K2-B CLOSED [DER-S] — K2-A OPEN, and the ONLY kernel item
requiring TWO resolutions (Q2 plus model replacement) — W6 OPEN, LIVE, CLASS UNDECIDED, but now
DECIDABLE IN PRINCIPLE: a history pair was constructed that K5 provably cannot express — a measurement
that is STALE-BUT-VALID versus one that is WRONG present identically to K5 — so C2 REMAINS VIABLE, with
a parity argument to K1's presupposition result offered as evidence and NOT as a decision — O-P09-1 OPEN
and now RANKED FIRST, because verification overturned the expected ordering: World II of semantic
reinterpretation IS a retirement, so Q2 DEPENDS ON Q1, and Q1 is answerable by a decision alone — THIRD
AXIS OPEN (witness absent; absence is not non-existence) — ROLE OVERLOAD OPEN (one direction witnessed) —
KERNEL IDENTITY and EQUIVALENCE are class B (interpretation and content, adding no capability); Q3 is
class C (answering "one" EXERCISES K4c) and Q4 unquantified — ARCHITECTURE COMPARISON NOT PERMITTED
(A CONDITIONAL · B PASS [stipulated] · C BLOCKED, narrowed to two classifications rather than transition
archaeology · D BLOCKED · E BLOCKED, 1 of 9 admitted) — NEXT ACTION: obtain the O-P09-1 identity-retirement
decision from Lane M via intake request IR-Q1, prepared in §11.
```

# Theory Element Index

**Extraction status: pilot complete, project not started.** `[EXP]` records only.
**No adjudication · no canonical anything · `OQ-1` untouched.**

| ID | Concept | Category | Definitions | evidenced relationships | implementation | governed decision |
|---|---|---|---:|---|---|---|
| [`KOS-T-0001`](./elements/KOS-T-0001-K.md) | **`K`** `kind:object` | Concepts | ⭐ **4** *(was 10; 2→`𝒦`, 4 ambiguous)* | **1 of 45 pairs** (`D-01~D-02`, lossy projection) | `D-01` **result-produced-on-it** · `D-02` `type-exists` (partial) · 8× `none` | 🔴 **EMPTY** |
| [`KOS-T-0002`](./elements/KOS-T-0002-delta.md) | **`δ`** `kind:operation` | 🔴 *no category* | **3** *(was 4)* | 0 evidenced | `D-02`/`D-03` **result-produced-on-it**, **stipulated** | 🔴 **EMPTY** |
| [`KOS-T-0003`](./elements/KOS-T-0003-eq-sem.md) | **`≡_sem`** `kind:relation` | 🔴 *no category* | **3** | `D-01~D-03` **misattribution** (no MD-017 value) | `D-02` **result-produced-on-it**, **stipulated — and the code says so** | 🔴 **EMPTY** |
| [`KOS-T-0004`](./elements/KOS-T-0004-Qualify.md) | **`Qualify`** `kind:operation` | 🔴 *no category* | **3** *(was 4)* | 0 evidenced | ⭐ **4 bodies, stipulated — while `G1` records "NO BODY"** | 🔴 **EMPTY** |

> ### Schema **v2** applied — [`04`](./04-SCHEMA-v2-AND-AMENDMENT.md) · validated [`05`](./05-VALIDATION-REPORT-v2.md) · **PASS with two conditions**
> **Defects A/B/C amended into this project's machinery only** — 3MC, `dimension-registry.md`,
> MD-017 and MD-018 untouched. **All four records APPENDED to; no v1 text edited.**
>
> ⭐ **Applying the homonym rule cut `K` from 10 definitions to 4**, via a criterion discovered during
> validation: **if `X`'s definition contains `Y`, then `X ≠ Y`** — `𝒦=(K,H)` and `𝒦=(K,C,T,E,A)`
> contain `K`, so **`𝒦` is a structure that HAS `K`**, not a spelling of it. Pairs to evaluate: **45 → 6**.
>
> ⭐ **Every implementation in all four records is `stipulated`. `derived`: ZERO instances.**
> `selection: forced`: ZERO. **Two vocabulary values defined and recorded as empty.**

> ### Stress test complete — [`03-STRESS-TEST-REPORT.md`](./03-STRESS-TEST-REPORT.md)
> Three concepts of different kinds run against the `K` schema. **It generalizes, with four schema
> defects found by contact:** `A` `Implementation` needs **derived vs stipulated** · `B` the 19
> categories lack **`Operations`** and **`Relations`** · `C` `D-nn` needs a **homonym** rule ·
> `D` MD-017 lacks **`misattribution`**. **Two of the four would have produced wrong records.**
> $$\textbf{4 elements · 4 EMPTY governed-decision fields · 3 with running code}$$

## What the pilot established about the *method*, not about `K`

| | finding |
|---|---|
| **four-level `Implementation` is necessary** | a boolean would have recorded `D-02` as *implemented* and hidden that **no code builds `K` from the eight primitives** — the primitives are the **contents of `𝒜`**, not the **components of `K`** |
| **`Latest` cannot be one field** | *"latest refinement"* alone yields **three different answers** under *introduce · alter · cite*. `EXT-05`'s recommendation is confirmed by measurement |
| 🔴 **the extractor contaminates its own measurement** | the first `latest mention` query returned **my own artifacts**. **Rule adopted: every corpus measurement declares its scope and excludes `theory-extraction/` and `verification/gap-discovery/` — the extracting lanes are not corpus** |
| **`unresolved_equivalence` earns its default** | **44 of 45 pairs** have no evidence either way. Any schema that forced a relationship would have manufactured 44 claims |
| **dependencies propagate uncertainty** | **6 of `K`'s 7 dependencies are themselves unsettled.** A definition cannot be more settled than what it is built from |

## Candidates for the next increments — **not scheduled, not ranked**

`Σ` (4 definitions) · `Π` (6 meanings, and an existing 3MC entry to cite) · `δ` · `≡_sem` ·
`Qualify` (4 signatures) · `Zero` · `ℐ`.

⚠️ **Selection order is not a theoretical priority and must not become one.** `K` was a **pilot**
chosen for mechanism coverage.

## Second stress pass — [`07-STRESS-REPORT-2.md`](./07-STRESS-REPORT-2.md) · **GENERALIZES WITH CONDITIONS**

| ID | Concept | `kind` | defs | implementation | governed decision |
|---|---|---|---:|---|---|
| [`KOS-T-0007`](./elements/KOS-T-0007-Sigma.md) | **`Σ`** | `[PROP]` **`value-set`** | **6** | `D-05` **`result-produced-on-it` / `stipulated`** — ⭐ **implements the SUPERSEDED `Σ₀`** | 🔴 **EMPTY** |
| [`KOS-T-0008`](./elements/KOS-T-0008-Pi.md) | **`Π`** | 🔴 **unassignable — 4 kinds** | **7 readings** | R1, R4 in **different estates** | 🔴 **EMPTY** |
| [`KOS-T-0009`](./elements/KOS-T-0009-Zero.md) | **`Zero`** | 🔴 **per definition** (`relation` ×5 + **`constant`**) | **6** | ⭐ **4 rival definitions implemented SIDE BY SIDE** | 🔴 **EMPTY** |
| [`KOS-T-0011`](./elements/KOS-T-0011-I-register.md) | **`ℐ`** | `[PROP]` **`register`/`interface`** | **4** | 🔴 **`none` in scope** | 🔴 **EMPTY** |

**8 records · 8 empty governed-decision fields · `derived` still ZERO instances.**

⭐ **The clearest manufactured-claim case in either pass:** without extending the scope exclusion to
*implementation*, `ℐ` would have been recorded **`type-exists`** on the strength of **my own graph
node whose label is `"NOT ENUMERATED"`** — a placeholder asserting the thing is *empty* counting as
evidence it *exists*.

🔴 **Two v2 assumptions are false in general:** `kind` and `status` belong to the **definition**, not
the record (`Π` has 4 kinds; `Zero` has 2 `[RF]` definitions beside 4 live ones); and **a record may
describe a GLYPH rather than a candidate** (`Π`). **Not failures of the mechanisms — failures of the
record's addressing.**

## Reserved IDs — **a reserved ID is not a claim the candidate is real**

| ID | candidate | `kind` | why reserved |
|---|---|---|---|
| **`KOS-T-0005`** | `δ = 0.3099` — `FR-001`'s resolution constant | **`constant`** | Defect C disposition 2 — **the 4th kind, demanded by evidence** |
| **`KOS-T-0006`** | `𝒦` | *(unknown)* | containment criterion — `𝒦` has `K` as a component |
| **`KOS-T-0010`** | `0_i ∈ 𝒟_i` — the algebraic zero element | **`constant`** | `Zero` `D-06`, disposition 2 — **2nd independent instance of `constant`** |

# `P-24` — Process Subject, Attribution, Agency & Currentness Audit

**2026-09-08 · Lane T.** After [`40` `P-23`](./40-P23-ESTATE-BOUNDARY-REGISTER-SCOPE-AND-DELETION-INVARIANT-AUDIT.md).

> **The test:** has `P-23` merely replaced one invented subject (*holder*) with another (*process*)?
>
> ⛔ **`P-08`, `P-18`–`P-23`, Schema v2 unmodified · no Schema v3 · no architecture · 3MC untouched ·
> no cell added, deleted or split without an independent proof · DDD never becomes ontology · ⛔
> `process = subject` NOT assumed · ⛔ `Current(π,p)` NOT assumed because `P-23` wrote it · no silent
> repair.**

# 0. ⭐⭐⭐ The measured answer, up front

$$\boxed{\begin{array}{c}\textbf{RELATIVIZED CURRENTNESS IS NOT CORPUS-ATTESTED — ZERO occurrences.}\\ \boxed{\textbf{And } P\text{-}22\textbf{'s and } P\text{-}23\textbf{'s process-indexing was OVER-DERIVED. It is } \mathbf{[REFUTED]}.}\end{array}}$$

---

# PART I — search before reasoning

| pattern, over `brainstorming/` minus Lane T | result |
|---|---|
| `\bprocess\b` | ⭐ **882 files — heavily attested** |
| ⭐⭐ `the process (is\|has\|was) (the )?(author\|authority\|subject\|bearer)` | ⭐⭐⭐ **ZERO files** |
| `process (asserts\|claims\|decides\|establishes\|determines\|withdraws\|supersedes\|holds\|owns)` | **11 files** — §1.1 |
| `\blane\b` | **253 files** |
| `(research\|verification\|intake\|closure\|decision) process` | **56 files** |
| ⭐⭐⭐ `current for (the )?(lane\|process\|register\|agent\|actor\|reader\|consumer\|session)` | ⭐⭐⭐ **ZERO** |
| `operative for` | **2 files** — both about a **construct's** operativity, not a subject's |
| `current within` · `current (assertion\|claim) of` · `holds for (the) (lane\|process\|register)` | **0 · 0 · 0** |

## ⭐⭐ The false positive, identified rather than glossed

`current for` returns **84 files / 103 occurrences** — ⚠️ **and every one is `current FORM`, `current
FORMAL system`, `current FORMULATION`, `current FORMULA`.**

$$\boxed{\begin{array}{c}\textbf{103 apparent hits, ZERO real ones — and the cause is NAMED.}\\ \textbf{⭐ Exactly the false-zero/false-positive discipline this programme has paid for three times.}\end{array}}$$

## 1.1 The eleven `process`-verb hits, classified

| hit | category |
|---|---|
| *"the generation process **determines** what…"* · *"a domain process **determines** the result"* | ⭐ **2 causal producer** |
| ⭐ *"where the process **records**"* ×3 | ⭐⭐ **4 record source** |
| *"verification process **establishes** its evidentiary role"* | **2 causal producer** |
| ⭐ *"**author/process claims**"* *(a paired category label in a prompt)* | ⚠️ **5 assertion author — the ONLY suggestive hit, and it is a LABEL, not a statement** |
| `H₂ = "another process **owns**…"` ×3 | ⭐ **a HYPOTHESIS in imported I. J. Good material — not a corpus commitment** |

$$\boxed{\begin{array}{c}\textbf{Categories 1–4 are attested. Category 5 has ONE label. Categories 6 (authority), 7 (epistemic subject)}\\ \textbf{and 8 (CURRENTNESS subject) have ZERO attestation.}\end{array}}$$

# PART II — what `process` means

| | hypothesis | verdict |
|---|---|---|
| **`P1`** temporal/activity sequence | ⭐ **`[EMP]`** |
| **`P2`** organizational procedure | ⭐ **`[EMP]`** — *"research/verification/intake process"*, 56 files |
| **`P3`** epistemic decision-making activity | ⚠️ **`[DERIVED]`** — processes *determine* |
| **`P4`** actor-like agent making assertions | ⭐⭐⭐ **`[REFUTED]`** — the author/authority/subject/bearer pattern returns **zero** |
| ⭐ **`P5`** record-producing activity, **not** an assertion bearer | ⭐⭐ **the surviving reading** |
| **`P6`** polysemous | ⭐ **`[EMP]`** — `P1`, `P2` and `P3` coexist; ⚠️ **but no sense reaches `P4`** |

⭐ **`P-23` found *estate* had two referents; *process* has three — and none of them is an assertion
bearer.**

# PART III — the four claims, and the implications tested

| implication | verdict |
|---|---|
| **`C1` produces ⇒ `C2` attributed** | ⚠️ **`[DERIVED]`, weakly** — *"where the process records"* attributes a **record**, not a claim |
| **`C2` attributed ⇒ `C3` authority** | ⭐⭐⭐ **`[REFUTED]` — the corpus prohibits exactly this** *(§4)* |
| ⭐ **`C2` attributed ⇒ `C4` current-for** | ⭐⭐⭐ **`[REFUTED]` — zero attestation for `C4` in any form** |
| **`C3` authority ⇒ `C4` current-for** | ⭐ **`[REFUTED]`** — §4's `A4` |

$$\boxed{\textbf{Not one of the four implications survives. } C1 \textbf{ and } C2 \textbf{ are attested; } C3 \textbf{ is prohibited; } C4 \textbf{ is UNATTESTED.}}$$

# PART IV — ⭐⭐ the authority-holder prohibition, and my own improper transfer

> *"An AI agent must not be interpreted as an authority holder merely because it executes a permitted
> activity."*

| | interpretation | verdict |
|---|---|---|
| **`A1`** prohibits authority from execution alone | ⭐ **`[EMP]` — this is literally what it says** |
| **`A2`** prohibits AI as epistemic authority | ⚠️ **`[DERIVED]`** — plausible extension |
| **`A3`** prohibits execution as evidence of authorship | ⚠️ **`[DERIVED]`** |
| ⭐⭐⭐ **`A4`** constrains **authority-holder attribution** and says **nothing** about currentness | ⭐⭐⭐ **`[DERIVED]` — THE CORRECT SCOPE** |
| **`A5`** genuinely constrains currentness subjects | ⭐⭐ **`[REFUTED]`** — the sentence is about **authority**; currentness is never mentioned |

$$\boxed{\begin{array}{c}\textbf{The prohibition concerns AUTHORITY, not CURRENTNESS. Stretching it further is a CATEGORY ERROR.}\\[6pt] \boxed{\begin{array}{l}\textbf{⚠️ And } P\text{-}23\textbf{'s own closing question FRAMED IT THAT WAY} — \textit{"does the prohibition constrain}\\ \textit{WHICH processes may bear currentness?"} \textbf{ ⭐ That framing was the improper transfer, and it is } \mathbf{[REFUTED]}.\end{array}}\end{array}}$$

⭐ **`STOP-F` is thereby achieved:** the prohibition has **no currentness consequence**.

# PART V — what `Current(π,p)` would even mean

| | reading | attested? |
|---|---|:--:|
| **`CURRENT-A`** π currently **uses** p | ⚠️ `[DERIVED]` |
| **`CURRENT-B`** π currently **treats p as standing** | 🔴 **0** |
| **`CURRENT-C`** π produced p most recently | ⚠️ **that is provenance, not currentness** |
| **`CURRENT-D`** π has authoritative standing behind p | ⭐ **`[REFUTED]`** — §4 |
| **`CURRENT-E`** the corpus classifies p as operative **within** π | 🔴 **0** — `current within` = 0 files |
| ⭐⭐⭐ **`CURRENT-F`** none of the above; process-indexing was an **analytical convenience** | ⭐⭐⭐ **THE SURVIVING READING** |

# PART VI — minimal pairs

| | test | answer |
|---|---|---|
| **`M1`** produced ⇒ asserted? | ⭐ 🔴 **NO** — *"where the process **records**"*: a recorder is not an assertor |
| **`M2`** attributed ⇒ authoritative? | ⭐ 🔴 **NO** — the prohibition |
| **`M3`** authoritative ⇒ current? | ⭐ 🔴 **NO** — `A4` |
| **`M4`** π stops treating p as current ⇒ must p disappear? | ⭐ 🔴 **NO** — `K4a-i` is **record-body scoped** *(`P-23` `R3`)*, so retention is untouched |

⭐ **`M4` independently confirms `P-23`'s strongest surviving result: retention needs no subject at all.**

# PART VII — the multi-process cases

| | case | finding |
|---|---|---|
⭐⭐⭐ **`H-P1`** | A establishes `p₁`, B establishes `p₂`, same subject matter | ⭐⭐ **representable as TWO RETAINED ASSERTIONS in the record body — `K4a-i` alone. NO currentness relation is needed, and neither process becomes an authority holder** |
| **`H-P2`** | A withdraws `p₁`; B still uses it | ⚠️ ⭐ **`[OPEN]` — and UNWITNESSED.** Nothing in the corpus records one process's withdrawal alongside another's continued use |
| ⭐⭐ **`H-P3`** | A **records** an assertion made by B | ⭐⭐⭐ **the recorder-vs-assertor test, and the corpus answers it: *"where the process records"*. A is the RECORDER; the assertion's source is a separate datum — PROVENANCE, not currentness** |
| **`H-P4`** | A executes a permitted operation producing a record containing `p` | ⭐ **A is NOT an assertion bearer and NOT an authority holder** *(the prohibition, applied within its own scope)*; ⭐⭐ **and whether `p` is "current for A" is UNDEFINED — the notion has no attestation** |

⛔ **No estate-level currentness invented, and none needed.**

# PART VIII — the neutral variable

Formulate `Current(x, p)` and ask what `x` **must** be for the witnessed cases.

| witness | what it actually needs |
|---|---|
| the lane conflict *(`H-P1`)* | ⭐ **retention only — no `x`** |
| the `Zero` meaning change | ⭐ **`x` = the CANDIDATE** *(one candidate, an assertion superseded)* |
| ⭐⭐⭐ `candidate → supported → corroborated` *(`P-11`'s `K2` witness)* | ⭐⭐⭐ **`x` = the CANDIDATE — status is a FIELD ON AN ITEM, never on a process** |
| ⭐⭐ MD-017 `unresolved_equivalence`, 125 files | ⭐⭐⭐ **`x` = the DEFINITION — `definition → candidate` is `0..N`, so the competition lives ON AN ITEM** |
| `INV-9`'s withdrawal | **transition-scoped warrant** |

$$\boxed{\begin{array}{c}\textbf{EVERY corpus witness is representable with } \mathbf{x = \textbf{THE ITEM}}\textbf{. NOT ONE requires } x = \textbf{a process.}\\[6pt] \boxed{\mathbf{Current(item,\ value)} \textbf{ — which is EXACTLY what } P\text{-}08 \textbf{ said: the current state of the three UPDATED items.}}\end{array}}$$

⭐⭐ **`P-08` was right, and `P-22`/`P-23` drifted away from it.**

# PART IX — indexing vs ontological vs persistence necessity

| | test |
|---|---|
| does `R1 = {p₁,p₂}` lose information the corpus requires, versus `R2 = {(x₁,p₁),(x₂,p₂)}`? | ⭐ **only if the index is required. §8 shows the required index is the ITEM, and the item is already in the state** |
| **indexing necessity** | ✅ **yes — by ITEM** |
| **ontological necessity** *(is `x` a domain entity?)* | ⭐ **items are `[EMP]`; processes are `[EMP]` as activities but NOT as subjects** |
| **persistence necessity** | ⭐ 🔴 **NO new obligation either way** |

⛔ **`x` was NOT called a structural variable because the notation was inconvenient without it — and
`P-22`'s claim that process identity IS structural is now `[REFUTED]`, because the model is fully
representable without it.**

# PART X — `K2`, re-tested from scratch

| what does `K2` preserve? | verdict |
|---|---|
| the current assertion **per process** | ⭐⭐ **`[REFUTED]`** — §8 |
| ⭐ **the current state of an ITEM** *(status · association · source)* | ⭐⭐⭐ **`[DERIVED]` — and it is `P-08` §4.3's original formulation** |
| a process state | **`[REFUTED]`** |
| an operative-claim assignment | ⚠️ **a re-description of the item's state** |

| ⭐ **is the index part of the OBLIGATION, or part of the STATE the obligation operates on?** | ⭐⭐⭐ **part of the STATE.** The item is the thing whose state is preserved; the obligation is *"preserve it"* |
|---|---|

$$\boxed{K2 \textbf{ is ITEM-SCOPED. } P\text{-}22\textbf{'s and } P\text{-}23\textbf{'s process-scoping is } \mathbf{[REFUTED]}\textbf{, and the pre-} P\text{-}22 \textbf{ reading is restored — not repaired.}}$$

# PART XI — `K4a-ii`, re-tested

| construction | finding |
|---|---|
| same replacement, different processes | ⚠️ **unwitnessed** |
| same process, different replacements | ⚠️ **unwitnessed** |
| one process recording another's replacement | ⭐ **`H-P3`: recording is PROVENANCE** |

⭐ **`P-23` §12 already conceded: *the source never states a holder-indexed replacement*.** ⇒ the minimum
is:

$$\boxed{\mathbf{Replaces(p_2,\ p_1)} \textbf{ — un-indexed, on the ITEM whose assertion changed. } P\text{-}22\textbf{'s } \mathit{Replaces}(h,p_2,p_1) \textbf{ is } \mathbf{[REFUTED]}.}$$

⭐⭐ **And `P-22`'s `H-S2` — which it claimed *strengthened* `K4a-ii` — was CONSTRUCTED, never witnessed.
That strengthening is withdrawn.**

# PART XII — `K4a-i` cross-check

> *If process identity is uncertain, can `K4a-i` still be stated?*

$$\boxed{\textbf{✅ YES — and this is the audit's most robust result: } \mathbf{retention \neq attribution \neq currentness}.}$$

⭐ `K4a-i` = *never lose a claim once it entered the corpus* (`P-23` `R3`). ⛔ **No process dependency is
introduced, and `M4` confirms it independently.**

# PART XIII — DDD audit

| does the corpus name **Process** as… | |
|---|---|
| an **entity** | ⚠️ **`[CORROBORATION]`** — named as an activity, never typed |
| an **actor** | ⭐ **`[OPEN]`** — grammatically, yes; semantically, unestablished |
| an **authority** | ⭐⭐ **`[REFUTED]`** — the prohibition |
| an **assertion bearer** | ⭐⭐⭐ **`[REFUTED]`** — zero attestation |
| a **domain-event source** | ⚠️ **`[CORROBORATION]`** — *"where the process records"* |

$$\boxed{\begin{array}{c}\textbf{⭐ Does } \textit{process} \textbf{ describe something that ACTS, or something THROUGH WHICH actions occur?}\\ \boxed{\textbf{THROUGH WHICH. } \textit{"where the process records"} \textbf{ is locative, and DDD "Actor" is not imported.}}\end{array}}$$

# PART XIV — adversarial attack on `P-23`, claim by claim

| claim | verdict |
|---|---|
| **1** process is the **subject of currentness** | ⭐⭐⭐ **`[REFUTED]`** — zero attestation; §8's items suffice |
| **2** process identity is a **structural variable** | ⭐⭐ **`[REFUTED]`** — the model is fully representable without it |
| **3** `Current(process,p)` is the **minimum** | ⭐⭐ **`[REFUTED]`** — `Current(item,value)` is smaller and sufficient |
| **4** `Replaces(process,p₂,p₁)` is the **minimum** | ⭐ **`[REFUTED]`** — `Replaces(p₂,p₁)` suffices |
| **5** the authority prohibition **constrains currentness** | ⭐⭐⭐ **`[REFUTED]`** — §4's `A4`; ⚠️ **and `P-23`'s closing question embodied this very error** |

⭐ **Five for five refuted.** ⛔ **Not grouped into one verdict, as required.**

# PART XV — ⭐⭐⭐ the negative test, which decides the audit

> *Construct a model where processes exist, produce records, and are associated with assertions — yet
> currentness is NOT process-relative.*

| corpus witness | represented without process-indexed currentness? |
|---|:--:|
| the lane conflict | ⭐ ✅ **two retained assertions; `K4a-i` alone** |
| `Zero`'s meaning change | ✅ **item-scoped supersession** |
| `candidate → supported → corroborated` | ⭐⭐ ✅ **a status field on a CANDIDATE** |
| MD-017 `unresolved_equivalence` ×125 | ⭐⭐⭐ ✅ **competition ON A DEFINITION — `0..N`** |
| withdrawals with warrants | ✅ **transition-scoped** |
| `F-4`, `Θ`-REJECTED, `[RF]` retentions | ✅ **retention, item-scoped** |

$$\boxed{\begin{array}{c}\textbf{The model represents EVERY corpus witness. And the converse test fails:}\\ \textbf{NO corpus-consistent case exists that CANNOT be represented without process-relative currentness.}\\[6pt] \boxed{\mathit{Current}(\pi,p) \textbf{ IS THEREFORE NOT PROVED, AND IS NOT RETAINED.}}\end{array}}$$

⭐ **The only cases requiring it — `P-22`'s `H-S2`, `H-P2` — are Lane T constructions, never witnessed.**

# PART XVI — kernel impact

| cell | independence changed? | capability added/removed? | index clarified? | scope |
|---|:--:|:--:|---|---|
| **`K1`** | 🔴 | 🔴 | — | estate-wide identity |
| ⭐ **`K2`** | 🔴 | 🔴 | ⭐⭐ **ITEM — process-scoping refuted** | **item** |
| **`K3a`** | 🔴 | 🔴 | item | **item** |
| **`K3b`** | 🔴 | 🔴 | — | ⭐ **transition** *(`P-23`, unchanged)* |
| ⭐⭐ **`K4a-i`** | 🔴 | 🔴 | — | ⭐⭐ **record body — reinforced twice (§6 `M4`, §12)** |
| ⭐ **`K4a-ii`** | ⚠️ ⭐ **`P-22`'s "second witness" WITHDRAWN** | 🔴 | ⭐ **un-indexed** | **item** |
| **`K4b-i`/`K4b-ii`** | 🔴 | 🔴 | — | item; `K4b-ii`'s corroboration arm still `[OPEN]` |
| **`K4c-i`/`K4c-ii`** | 🔴 | 🔴 | — | estate-wide |
| **`K5`** | 🔴 | 🔴 | — | external |

$$\boxed{\textbf{ZERO cells gain or lose independence. ⛔ Cardinality 11. And ONE prior strengthening is WITHDRAWN rather than kept.}}$$

# PART XVII — scope heterogeneity, formalized

| obligation | domain |
|---|---|
| **`K4a-i`** retention | the **record body** — `∀p ∈ Corpus` |
| **`K2`** current state | an **item** — `∀i ∈ Items` |
| **`K3b`** warrant | a **transition** — `∀τ ∈ 𝒯` |

$$\boxed{\begin{array}{c}\textbf{These are THREE PREDICATES WITH DIFFERENT ARGUMENT TYPES. Coherence requires only that each}\\ \textbf{quantifier range over a well-defined domain — NOT that they share a subject.}\\[6pt] \boxed{\begin{array}{l}\textbf{⭐ The heterogeneity is MATHEMATICALLY COHERENT, and no hidden dependency was found:}\\ \textbf{a common subject would be needed only if one obligation quantified over another's domain — none does.}\end{array}}\end{array}}$$

⭐⭐ **And the heterogeneity is now cleaner than `P-23` left it:** *process* is gone; the three scopes are
**corpus · item · transition**, all three `[EMP]`-attested kinds.

---

# FINAL VERDICT

| # | question | answer |
|---|---|---|
| **1** | is PROCESS corpus-attested? | ⭐ **YES — 882 files, `[EMP]`** |
| **2** | an assertion-**producing** activity? | ⭐ **YES — `[EMP]`/`[DERIVED]`**: it *records*, *determines*, *generates* |
| **3** | an assertion-**bearing subject**? | ⭐⭐⭐ **NO — `[REFUTED]`.** `the process is the author/authority/subject/bearer` → **zero files** |
| **4** | an **authority** bearer? | ⭐⭐ **NO — explicitly prohibited** |
| **5** | the **subject of currentness**? | ⭐⭐⭐ **NO — `[REFUTED]`.** Zero attestation in any form |
| **6** | is `Current(process,p)` corpus-required, derived, or unnecessary? | ⭐⭐⭐ **UNNECESSARY — `CURRENT-F`, an analytical convenience** |
| **7** | is the prohibition relevant to currentness? | ⭐⭐ **NO — `A4`. `STOP-F` achieved, and `P-23`'s framing was the improper transfer** |
| **8** | is `Replaces(process,p₂,p₁)` required? | ⭐ **NO — `Replaces(p₂,p₁)` suffices** |
| **9** | is process identity indexing or structural? | ⭐⭐ **NEITHER — not required at all.** The required index is the **ITEM**, already in the state |
| **10** | does `K2` remain process-scoped? | ⭐⭐⭐ **NO — ITEM-scoped**, restoring `P-08` §4.3 |
| **11** | does `K4a-ii` remain process-scoped? | ⭐ **NO — un-indexed on the item** |
| **12** | does `K4a-i` remain record-body scoped? | ⭐⭐ **YES — reinforced twice** |
| **13** | does `K3b` remain transition-relative? | ⭐ **YES — unchanged** |
| **14** | is the scope-heterogeneous kernel coherent? | ⭐⭐ **YES — three predicates, three argument types, no shared subject needed** |
| **15** | cardinality change? | ⛔ **NO — 11** |
| **16** | transition structure change? | ⭐ **the nested `τ10` structure survives; its base is an ITEM-scoped set/field change, not a *holder* or *process* one** |
| **17** | refuted | ⭐⭐⭐ **all five `P-23` claims** · `P-22`'s `Current(h,p)`, `Replaces(h,p₂,p₁)`, structural-variable claim and `H-S2` strengthening · `P-4` process-as-actor · `A5` |
| **18** | strengthened | ⭐⭐ **`K4a-i`** *(twice: `M4` and §12)* · **`K3b`** *(untouched by the collapse)* · ⭐ **`P-08` §4.3's original item-scoped `K2`** · **scope heterogeneity, now with three attested kinds** |
| **19** | ⭐ **single next question** | ⭐⭐⭐ **Is RECORD-ATTRIBUTION — *which process produced this record* — a persistence obligation the kernel currently lacks?** `H-P1`/`H-P3` show attribution is a real, separate datum; `K3b` covers **transition** warrants and `K5` covers **external** grounds, and ⚠️ **neither covers *who recorded this*.** *(Potentially kernel-changing — the first such question since `P-19`.)* |

# STOPPING CONDITION

$$\boxed{\textbf{STOP-D — process indexing was UNNECESSARY; } P\text{-}23 \textbf{ (and } P\text{-}22\textbf{) OVER-DERIVED it. } \mathbf{STOP\text{-}F} \textbf{ also achieved.}}$$

⛔ **Not `STOP-A`** *(five claims refuted)* · ⛔ **not `STOP-B`** *(no other subject is needed — the item
suffices)* · ⛔ **not `STOP-C`** *(not even mathematically required — §15)* · ⛔ **not `STOP-E`** *(the
prohibition has no currentness consequence)* · ⛔ **not `STOP-G`** *(the evidence decides)*.

# Provenance
**`[EMP]`** `process` in **882** files · ⭐ **`the process is the author/authority/subject/bearer` → ZERO**
· ⭐⭐ **`current for <agent/process/lane/…>` → ZERO**, with the 103 apparent `current for` hits explained
as `current form/formal/formulation/formula` · *"where the process **records**"* · the authority
prohibition · MD-017 `unresolved_equivalence` on **definitions**, 125 files · `definition → candidate`
`0..N` · MD-018's status **on candidates**.
**`[DERIVED]`** ⭐⭐⭐ **every corpus witness is representable with `Current(item,value)`; none requires a
process index** · ⭐ **a recorder is not an assertor** · **the prohibition concerns authority, not
currentness** · **`K2` is item-scoped, restoring `P-08` §4.3** · **`Replaces(p₂,p₁)` un-indexed
suffices** · **the three scopes are coherent because they are predicates over different argument types**
· **retention ≠ attribution ≠ currentness**.
**`[REFUTED]`** ⭐ all five `P-23` claims · four `P-22` claims · `P4` process-as-actor · `A5` · `CURRENT-B`
/ `-D` / `-E`.
**`[CORROBORATION]`** the DDD audit — *process* is **locative**, not agentive.
**`[OPEN]`** ⭐ **record-attribution as an obligation** *(§19)* · `H-P2` *(unwitnessed)* · `K4b-ii`'s
corroboration arm · register deletion · `R4` · *"estate"*'s two referents · everything carried forward.
⛔ **No Lane T construction used as corpus evidence; no `[ARCH]` promoted; nothing rewritten.**

---

```
STOP-D — PROCESS INDEXING WAS UNNECESSARY. P-23 AND P-22 OVER-DERIVED IT. STOP-F ALSO ACHIEVED.
KERNEL REMAINS 11 CELLS. ZERO CELLS GAIN OR LOSE INDEPENDENCE.

THE MEASURED ANSWER: relativized currentness is NOT corpus-attested — `current for <agent/process/lane/
register/actor/reader/consumer/session>` returns ZERO. The 84 files / 103 occurrences of "current for"
are ALL FALSE POSITIVES — "current FORM", "current FORMAL system", "current FORMULATION", "current
FORMULA" — and the cause is named rather than glossed, which is the discipline this programme has paid
for three times. And `the process is the author/authority/subject/bearer` returns ZERO FILES across 882
attesting files. Of the eight attribution categories, 1–4 are attested, category 5 has ONE label
("author/process claims", in a prompt), and categories 6 (authority), 7 (epistemic subject) and 8
(CURRENTNESS subject) have NONE.

THE NEGATIVE TEST DECIDES IT (Part XV). A model with processes that produce records and are associated
with assertions, but WITHOUT process-relative currentness, represents EVERY corpus witness:
  the lane conflict  -> TWO RETAINED ASSERTIONS, K4a-i alone; no currentness relation needed
  Zero's meaning change -> item-scoped supersession
  candidate->supported->corroborated -> a STATUS FIELD ON A CANDIDATE
  MD-017 unresolved_equivalence x125 -> competition ON A DEFINITION (0..N)
  withdrawals -> transition-scoped warrants
And the converse fails: no corpus-consistent case requires process-relative currentness. The only cases
that would — P-22's H-S2 and H-P2 — are LANE T CONSTRUCTIONS, never witnessed. P-23 §12 had already
conceded that the source never states a holder-indexed replacement.
  => Current(pi,p) IS NOT PROVED AND IS NOT RETAINED. The required index is THE ITEM, which is already
  in the state — exactly what P-08 §4.3 said. P-08 WAS RIGHT AND P-22/P-23 DRIFTED.

MY OWN IMPROPER TRANSFER, CAUGHT: P-23's closing question asked whether the authority-holder prohibition
constrains "WHICH PROCESSES MAY BEAR CURRENTNESS". The prohibition is about AUTHORITY (A1/A4) and says
nothing about currentness — A5 is [REFUTED]. That framing WAS the category error the commission warned
against, and it came from me.

FIVE P-23 CLAIMS REFUTED, individually: process-as-currentness-subject · process identity as a
structural variable · Current(process,p) as the minimum · Replaces(process,p2,p1) as the minimum · the
prohibition constraining currentness. Four P-22 claims fall with them, and P-22's "second independent
witness" for K4a-ii (H-S2) is WITHDRAWN — a prior STRENGTHENING retracted rather than kept.

WHAT IS STRENGTHENED: K4a-i, twice and independently (M4: a process ceasing to treat p as current does
NOT require p to disappear; and Part XII: K4a-i is statable with process identity wholly uncertain) —
confirming RETENTION != ATTRIBUTION != CURRENTNESS. K3b stands untouched as transition-relative. And
P-08 §4.3's original item-scoped K2 is RESTORED, not repaired.

SCOPE HETEROGENEITY IS MATHEMATICALLY COHERENT, and cleaner than P-23 left it: K4a-i quantifies over the
CORPUS, K2 over ITEMS, K3b over TRANSITIONS — three predicates with three different argument types. A
common subject would be needed only if one obligation quantified over another's domain, and none does.
All three scopes are now [EMP]-attested kinds; "process" is gone from the kernel entirely.

DDD: "process" is LOCATIVE, not agentive — "where the process records". It describes something THROUGH
WHICH actions occur, not something that acts. DDD "Actor" is not imported.

NEXT QUESTION (one): IS RECORD-ATTRIBUTION — "which process produced this record" — A PERSISTENCE
  OBLIGATION THE KERNEL CURRENTLY LACKS? H-P1 and H-P3 show attribution is a real and separate datum;
  K3b covers TRANSITION warrants and K5 covers EXTERNAL grounds, and NEITHER covers "who recorded
  this". Potentially kernel-changing — the first such question since P-19.

NO ARCHITECTURE — NO SCHEMA v3 — NO CANONICAL THEORY — 3MC UNTOUCHED — NO CELL ADDED, SPLIT OR DELETED —
NO SILENT REPAIR.
```

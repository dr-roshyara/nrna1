# `P-25` — Record Attribution & Persistence Obligation Audit

**2026-09-08 · Lane T.** After [`41` `P-24`](./41-P24-PROCESS-SUBJECT-ATTRIBUTION-AGENCY-AND-CURRENTNESS-AUDIT.md).

> **The single question:** *is record-attribution — which process produced or recorded a record — a
> persistence obligation the 11-cell kernel lacks?*
>
> ⛔ **`P-08` baseline · `P-18`–`P-24` unmodified · 3MC read-only · Schema v2 untouched · no Schema v3 ·
> no architecture · no implementation · no canonical theory · no cell added, split, merged, deleted or
> re-scoped without an independent proof · no silent repair of `P-24` · Lane T constructions are not
> corpus evidence · ⛔ process-relative currentness NOT reopened.**

# 0. ⭐⭐⭐ The search reframes the question

$$\boxed{\begin{array}{c}\textbf{PROVENANCE is massively attested — 1 766 files. And } \mathit{provenance\ of\ (the)\ record} \textbf{ returns ZERO.}\\[6pt] \boxed{\begin{array}{l}\textbf{The corpus attaches provenance to KNOWLEDGE, EVIDENCE, ASSERTIONS, TRANSITIONS and ARTIFACTS —}\\ \textbf{never to a RECORD, and never to a PROCESS as its target.}\end{array}}\end{array}}$$

⭐ **So the commissioned question is partly MIS-POSED**, and §4/§12 say so rather than answering a
question the corpus does not pose.

---

# PART II — the search, with false positives reported

| pattern | files |
|---|---|
| ⭐⭐ **`provenance`** | ⭐ **1 766** |
| **`attribution`** | **225** |
| `produced by` | 122 · `generated by` **80** · `attributed to` **46** · `created by` **23** · `authored by` **10** · `originated by/from` **9** · `recorded by` **6** · `registered by` **4** · `logged by` **1** · `record source` **1** · `entered by` **0** |
| ⭐ **`provenance of (the) record`** | ⭐⭐⭐ **0** |
| `record (provenance\|attribution)` | **8** |
| ⭐ `(recorded\|produced\|created\|generated\|authored\|attributed) (by\|to) (the) (process\|lane\|agent\|session)` | ⭐ **7** |

## What provenance is attached TO — measured by extraction

`provenance of/for …` → **knowledge (10)** · **the boundary reason (3)** · **evidence (3)** · **each
evidence item (3)** · **inferred assertions (3)** · **the corrected artifact (2)** · **knowledge states
(2)** · ⭐ **epistemic transitions (2)** · engineering knowledge (2) · each piece of knowledge (2).

$$\boxed{\textbf{⛔ NOT ONE target is a RECORD. ⭐ And every attested target is ALREADY in the kernel's domain — §7.}}$$

## The seven process hits, classified

| hit | category |
|---|---|
| *"Knowledge is **produced by processes**"* ×2 *(kernel summary, "Traceable Generation \| Track pr…")* | ⭐ **1 causal producer · and a DESIDERATUM, not a rule** |
| *"Knowledge can be produced by processes distributed across people, tools and…"* | ⭐⭐ **10 IMPORTED external material (Shieber)** |
| ⭐ *"an **assertion produced by the agent**, not necessarily a first-class Knowledge […]"* | ⭐⭐⭐ **1 producer — AND it explicitly separates PRODUCTION from STATUS** |
| 3 hits in imported epistemic-value material *("a control account where s…")* | **10 imported** |

⭐ **`ProducedBy(knowledge, process)` is attested — as an imported epistemological claim and a
traceability desideratum.** ⛔ **Not as a persistence rule.**

## ⭐⭐ Two corpus statements directly on the classification

| statement | consequence |
|---|---|
| ⭐⭐ **`provenance is not merely metadata`** | ⭐ **REFUTES possibility 1** *(merely descriptive metadata)* |
| ⭐⭐ **`provenance is not knowledge`** | ⭐ **REFUTES treating it as content** |
| **`provenance chain` · `provenance chain unbroken?`** | ⭐ a **checkability** question — §19 |

---

# PART III — the neutral relation
$$\mathit{ProducedBy}(r,\pi) \quad \mathit{RecordedBy}(r,\pi) \quad \mathit{AttributedTo}(r,\pi)$$
⛔ **None is yet called identity, provenance obligation, authorship, authority, ownership, epistemic
responsibility or persistence capability.** ⭐ **And the measured targets (§II) are not `r` at all.**

# PART IV — record identity vs record attribution

| model | verdict |
|---|---|
| **`A` attribution is identity-bearing** — `r₁ = r₂` only if attribution is preserved | ⭐ **`[OPEN]`, and unsupported** — nothing ties record identity to a producer |
| **`B` attribution is associated metadata** | ⭐⭐ **`[REFUTED]` as stated** — *"provenance is not merely metadata"* |

| pair | corpus verdict |
|---|---|
| **`R-A`** same record, same attribution | — |
| **`R-B`** same record, attribution removed | ⭐ **`[OPEN]`** |
| **`R-C`** attribution changed `π₁ → π₂` | ⭐⭐ **see `A3` — this is `τ6`, already classified** |
| **`R-D`** identical records, different producers | ⭐⭐ **`[UNWITNESSED]`** |

$$\boxed{\begin{array}{c}\textbf{Neither model is corpus-supported, because } \mathbf{provenance\ is\ not\ a\ RECORD\ property\ in\ this\ corpus.}\\ \boxed{\textbf{The } R\text{-}A..R\text{-}D \textbf{ discriminators are MIS-POSED, and the honest verdict on them is } \mathbf{[OPEN]}.}\end{array}}$$

# PART V — the central persistence test

| case | is `A` required and `B`/`C`/`D` prohibited by any corpus rule? |
|---|---|
| **`A`** attribution retained | ⭐ **no rule found requiring it** |
| **`B`** attribution lost | ⚠️ **not prohibited** — §10 `A1` |
| **`C`** attribution changed | ⭐⭐ **explicitly PERMITTED — it is `τ6` source re-marking, an evidenced transition** |
| **`D`** record retained, attribution absent | ⭐⭐⭐ **explicitly contemplated** — §10 `A5` |

$$\boxed{\textbf{⛔ No corpus rule requires } A \textbf{ or prohibits } B/C/D. \textbf{ A persistence obligation is NOT manufactured from usefulness.}}$$

# PART VI — the minimal-pair independence test

`H₁: ProducedBy(r,π₁)` versus `H₂: ProducedBy(r,π₂)`, with **all 11 cells identical**.

| step | finding |
|---|---|
| are the 11 cells really identical? | ⭐ **only if the SOURCE VALUE is identical and just the process differs** — otherwise `K2`/`K3a` differ and condition 3 fails at once |
| ⭐⭐ **does the corpus require `H₁` and `H₂` to remain distinguishable?** | ⭐⭐⭐ **NO — and the reason is measured: every attested provenance SOURCE is an ARTIFACT or an ACT, never a process.** `Σ` `D-05` → `code@kosmodel.py:Sigma` · `C-022` a stipulation · `G-67` a naming · `FR-001` a measurement |

$$\boxed{\begin{array}{c}\textbf{No corpus rule requires knowing WHICH PROCESS wrote a record.}\\ \boxed{\textbf{⇒ record-attribution is NOT independently required by persistence. No new cell.}}\end{array}}$$

---

# PART VII — do existing cells already cover it?

⭐ **`P-09` §9 already derived THREE provenance roles**, and every attested provenance target maps onto
them:

| attested target | role | cells that carry it |
|---|---|---|
| **knowledge / assertions** | *evidence supporting a claim* | ⭐ **`K4a-i`** *(the argument retained)* + **`K3b`** |
| ⭐ **evidence / each evidence item** | *source-of-content* | ⭐ **`K2`** *(current source value)* + **`K3a`** *(prior value)* + **`K5`** *(checkability)* |
| ⭐ **epistemic transitions** | *act-of-entry / transition warrant* | ⭐⭐ **`K3b`** — `P-23` established warrants are **transition-relative** |
| **the corrected artifact** | source-of-content | `K2` + `K5` |

| cell | could it already carry attribution? |
|---|---|
| **`K1`** | 🔴 **no** — identity is not producer-dependent; ⛔ *"`K1` preserves identity therefore all metadata survives"* is one of §17's forbidden inferences |
| ⭐ **`K2`** | ⭐⭐ **YES — the `source` VALUE on a definition** *(`P-09.2` §4.9)*. ⚠️ **And that is STATE preservation, not a separate attribution obligation** |
| ⭐ **`K3a`** | ⭐⭐ **YES — prior source value, under `τ6`** |
| ⭐ **`K3b`** | ⭐⭐ **YES — the transition warrant** *(`P-23`)* |
| **`K4a-i`** | ⚠️ **the record survives; ⛔ it does NOT follow that its metadata survives** — §17 |
| **`K4a-ii`** | 🔴 no — a replacement relation is not a producer |
| **`K4b-i`/`K4b-ii`** | ⚠️ **recoverable-from-grounds is not the same as retained** — the distinction is preserved |
| **`K4c-i`/`K4c-ii`** | 🔴 no |
| ⭐ **`K5`** | ⭐⭐ **YES — *"provenance chain unbroken?"* is exactly a checkability question** |

$$\boxed{\textbf{Provenance is DISTRIBUTED across } K2,\ K3a,\ K3b,\ K4a\text{-i and } K5 \textbf{ — already, and by roles derived in } P\text{-}09\ \S9.}$$

# PART VIII — information-theoretic test

| | |
|---|---|
| is `A = f(S)`? | ⭐ **for every attested case, YES** — the source value is *in* `S` (via `K2`/`K3a`), the transition warrant is *in* `S` (via `K3b`) |
| does `∃H₁,H₂` with `S(H₁)=S(H₂)` but `A(H₁)≠A(H₂)`? | ⭐⭐ **only §6's process-differing pair — and the corpus does NOT require distinguishing it** |

$$\boxed{\textbf{Information not encoded by the kernel is NOT automatically an obligation. Only a corpus-grounded requirement creates one — and none exists.}}$$

# PART IX — provenance vs persistence, proposition by proposition

| | proposition | class |
|---|---|---|
| **`P1`** a record has a producing process | ⭐ **`[DERIVED]`** — *"knowledge is produced by processes"*, and largely **imported** |
| **`P2`** the producing process is semantically significant | ⭐ **`[OPEN]`** — *"produced by the agent, **not necessarily a first-class Knowledge [item]**"* cuts against it |
| **`P3`** the attribution is provenance | ⚠️ **`[QUALIFIED]`** — provenance is attested, but its **targets and sources are artifacts, acts, evidence and transitions**, not processes |
| **`P4`** provenance must remain **checkable** | ⭐⭐ **`[DERIVED]` — `K5`.** *"Provenance chain unbroken?"* |
| **`P5`** provenance must **persist across transitions** | ⭐⭐ **`[REFUTED]` as a universal** — `τ6` **changes** it, and §10 `A5` shows it may be absent |
| **`P6`** loss of provenance is a **persistence failure** | ⭐⭐⭐ **`[REFUTED]`** — `P-08` §5.3's remedy is to **downgrade the grounding**, not to declare failure |

⛔ **`P1 → P5` is not inferred, and neither is *provenance ⇒ persistence obligation*.**

---

# PART X — the six adversarial counterexamples

| | case | finding |
|---|---|---|
| ⭐ **`A1`** record intact, attribution lost | ⭐⭐ **persistence does NOT fail.** `F-4` is the live instance: the repository may vanish, and `P-08` §5.3's response is *persist an extract **or** mark the grounding pointer-only* — **an epistemic downgrade, not a failure** |
| **`A2`** attribution kept, record deleted | ⭐ **does NOT rescue `K4a-i`** — a pointer to a deleted claim is not the claim |
| ⭐⭐⭐ **`A3`** attribution changed | ⭐⭐⭐ **`τ6` SOURCE RE-MARKING — already in `𝒯_KOS`, already non-monotone, already borne by `K3a`+`K3b`. And `F-4` IS this case.** So attribution's **dynamics are already covered** |
| **`A4`** identical records, different producers | ⭐ **`[UNWITNESSED]`** |
| ⭐⭐ **`A5`** producer never known | ⭐⭐⭐ **the corpus CONTEMPLATES IT: `without provenance` 32 files · `no provenance` 24 · `provenance absent/missing/lost` 6 · `unknown provenance` 2.** ⭐ **And the argument holds under BOTH readings** — if pejorative, provenance is an **admissibility** condition; if descriptive, items **exist without it**. **Neither makes it a persistence obligation** |
| **`A6`** record copied — producer vs recorder vs latest recorder | ⭐ **`[OPEN]`** — ⛔ **not resolved by design preference; no corpus evidence found** |

# PART XI — the three relations, kept apart

| relation | attestation |
|---|---|
| `ProducedBy` | ⭐ **`[DERIVED]`, largely imported** — targets **knowledge**, not records |
| `RecordedBy` | ⚠️ **6 files** — thin |
| `AttributedTo` | **46 files** — attested, ⚠️ **targets unexamined here** |
| ⭐ **does the corpus DISTINGUISH them?** | ⭐⭐ **`[OPEN]`** — no witness separates producer, recorder and citer |

⛔ **`P-24`'s *"a recorder is not an assertor"* is preserved and NOT inverted** — recorder/source is not
converted into authorship.

# PART XII — the smallest object attribution attaches to

| candidate | attested? |
|---|:--:|
| the **record body** | ⭐ 🔴 **NO — `provenance of the record` = 0** |
| a **record occurrence** | 🔴 no |
| a **register entry** | 🔴 no |
| ⭐ a **transition** | ⭐⭐ ✅ **`[EMP]` — *"provenance of epistemic transitions"*** |
| ⭐ an **assertion** | ⭐⭐ ✅ **`[EMP]` — *"provenance for inferred assertions"*** |
| an **item** *(knowledge, evidence, artifact)* | ⭐⭐ ✅ **`[EMP]` — knowledge (10), evidence (6), artifact (2)** |

$$\boxed{\begin{array}{c}\textbf{The smallest attested objects are TRANSITIONS, ASSERTIONS and ITEMS — } \boxed{\textbf{exactly the domains of } K3b,\ K4a\text{-i and } K2.}\\ \textbf{⭐ Record-attribution is therefore not even a CANDIDATE cell: its subject is not a record.}\end{array}}$$

# PART XIII — scope of the surviving relation

$$\boxed{\mathit{ProvenanceOf}(\textbf{transition} \mid \textbf{assertion} \mid \textbf{item},\ \textbf{artifact} \mid \textbf{act})}$$

⭐ **Derived from the measured targets and sources, not chosen to fit the kernel.** ⇒ it is a
**state variable** *(a source value)* **and a historical relation** *(a transition warrant)* — ⛔ **and
NOT a separate persistence obligation.** *(These four are not synonyms and are not merged.)*

# PART XIV — DDD audit

| term | class |
|---|---|
| **Provenance** | ⭐ **`[EMP]`** — 1 766 files, and *"not merely metadata"*, *"not knowledge"* |
| **Attribution** | **`[EMP]`** — 225 files |
| **Source** | **`[EMP]`** — `P-09` §9's role |
| **Transition · Assertion** | **`[EMP]`** |
| **Record · RecordOccurrence** | ⭐ **`[OPEN]`** as provenance-bearing objects |
| **Process** | ⭐ **`[CORROBORATION]`** only — `P-24`: locative, not agentive |
| ⭐⭐ **Producer · Recorder · Event** | ⭐⭐⭐ **`[REFUTED]` as required domain concepts** |

$$\boxed{\begin{array}{c}\textbf{Does the corpus require a PRODUCER concept, or only a relation between an item and an artifact/act?}\\ \boxed{\textbf{ONLY THE RELATION. ⛔ No entity is created because a persistence proof wanted a name.}}\end{array}}$$

# PART XV — the negative test

**Model:** processes exist · they produce and record · attribution may be observable · records and
assertions persist — ⭐ **and attribution is NOT a persistence obligation.**

| witness | representable? |
|---|:--:|
| `Σ` `D-05` sourced to `code@kosmodel.py:Sigma` | ⭐ ✅ **an ARTIFACT source — `K2`/`K5`** |
| `C-022` · `G-67` · `FR-001` establishment acts | ✅ **ACTS — `K3b`** |
| the containment argument for `𝒦` | ✅ **evidence retained — `K4a-i`** |
| `F-4` *(source re-marked / uncheckable)* | ⭐⭐ ✅ **`τ6` + `K3a` + `K3b`, with an honest downgrade** |
| *"provenance chain unbroken?"* | ✅ **`K5`** |
| `without provenance` cases | ⭐ ✅ **representable — nothing requires a producer field** |

⭐ **Every witness is representable.** ⭐⭐ **And the smallest witness that would force the obligation** —
two records differing **only** in producing process, with the corpus requiring the distinction — is
⭐⭐⭐ **`[UNWITNESSED]`**, so it is **not used to raise cardinality**.

---

# PART XVI — kernel cardinality decision

$$\boxed{\textbf{OUTCOME B — record-attribution is COVERED BY EXISTING CELLS. Kernel } = \mathbf{11}.}$$

⭐ **Coverage proven per role** *(§7)*: **source-of-content → `K2` + `K3a` + `K5`** · **act-of-entry /
transition → `K3b`** · **evidence-for-a-claim → `K4a-i` + `K3b`**.
⛔ **Not Outcome `A`** *(it is not mere metadata — the corpus says so)* · ⛔ **not Outcome `C`** *(no
independence witness; §6 and §15)* · ⛔ **not Outcome `D`** *(the evidence is sufficient for coverage,
even though several sub-questions remain `[OPEN]`)*.

# PART XVII — anti-error check

⭐ **All twelve forbidden inferences avoided, and three were live temptations:**
⛔ *"provenance exists, therefore it must persist"* — refuted by `P5`/`P6` · ⛔ *"`K4a-i` preserves
records, therefore metadata survives"* — explicitly refused in §7 · ⛔ *"absence of attribution evidence
= evidence of absence"* — §10 `A5` is argued **both ways** instead · ⛔ *"a constructed counterexample =
corpus evidence"* — §15's forcing witness is labelled **`[UNWITNESSED]`**.

---

# REQUIRED FINAL OUTPUT

| # | question | answer |
|---|---|---|
| **1** | is record-attribution corpus-attested? | ⭐⭐ **NO as *record*-attribution — `provenance of (the) record` = 0.** ⭐ **PROVENANCE is massively attested (1 766 files) with OTHER targets** |
| **2** | what exact relation is attested? | ⭐ **`ProvenanceOf(transition \| assertion \| item, artifact \| act)`** — plus `ProducedBy(knowledge, process)` as an **imported** claim and a **traceability desideratum** |
| **3** | its smallest domain? | ⭐⭐ **transitions, assertions and items** — ⛔ **never a record, occurrence or register entry** |
| **4** | identity, state, provenance, historical relation, or obligation? | ⭐⭐ **STATE (a source value) + HISTORICAL RELATION (a transition warrant).** ⛔ **NOT identity, NOT an independent obligation** |
| **5** | must attribution survive transitions? | ⭐⭐⭐ **NO — `[REFUTED]` as a universal.** `τ6` **changes** it, and the corpus contemplates its **absence** |
| **6** | which cells cover it? | ⭐ **`K2` + `K3a` (source value, prior value) · `K3b` (transition warrant) · `K4a-i` (retained argument) · `K5` (checkability)** |
| **7** | independent capability proven? | ⛔ **NO** |
| **8** | cardinality 11? | ⭐ **YES — unchanged** |
| **9** | what must be withdrawn? | ⭐⭐ **`P-24`'s closing claim that *"`H-P1`/`H-P3` show attribution is a real and separate datum"* is `[QUALIFIED]`: attribution is real, but it is neither RECORD-attached nor SEPARATE from `P-09` §9's already-derived provenance roles** |
| **10** | what remains `[OPEN]`? | `A6` producer-vs-recorder-vs-citer · whether the corpus distinguishes the three relations · `A4`/`R-D` *(unwitnessed)* · `R-B` · **`P2`** semantic significance of the producer · records as provenance-bearing objects · ⭐ **the provenance CHAIN question** · `K4b-ii`'s corroboration arm · register deletion · *"estate"*'s two referents |
| **11** | ⭐ **the ONE next question** | ⭐⭐⭐ **Is provenance-chain UNBROKENNESS a `K5` obligation, or does a CHAIN require more than single-link checkability?** *(Derived from `[EMP]` **"provenance chain"** and **"provenance chain unbroken?"**; `K5` is stated for **the current ground**, and a chain has depth. ⭐ It also finally engages `P-16`'s deferred provenance-chain `[OPEN]`.)* |

# STOPPING CONDITION

$$\boxed{\textbf{STOP-F — the attribution question REDUCES to the already-established provenance / state / transition-warrant obligations.}}$$

⛔ **Not `STOP-A`** *(not independently required)* · ⛔ **not `STOP-B`** *(record-attribution is not
corpus-attested as such)* · ⭐ **`STOP-C` is also satisfied** *(existing cells cover it)*, and `STOP-F` is
reported as primary because the question was **partly mis-posed** · ⛔ **not `STOP-D`/`STOP-E`.**

# Provenance of this artifact
**`[EMP]`** `provenance` **1 766** files · `attribution` **225** · ⭐ **`provenance of (the) record` = 0**
· targets = knowledge/evidence/assertions/**transitions**/artifacts · ⭐⭐ **`provenance is not merely
metadata`** · **`provenance is not knowledge`** · **`provenance chain unbroken?`** · ⭐ **`without
provenance` 32 · `no provenance` 24 · `absent/missing/lost` 6 · `unknown` 2** · *"assertion produced by
the agent, **not necessarily a first-class Knowledge**"* · `Σ D-05` → `code@kosmodel.py:Sigma` ·
`C-022`/`G-67`/`FR-001`.
**`[DERIVED]`** ⭐⭐⭐ **provenance's attested targets are exactly `K3b`'s, `K4a-i`'s and `K2`'s domains, so
no new cell is a candidate** · ⭐ **attribution CHANGE is `τ6`, already classified** · **`A = f(S)` for
every attested case** · **loss of provenance triggers an epistemic downgrade, not a persistence
failure** · **only the relation is required, never a Producer entity**.
**`[REFUTED]`** ⭐ `P5` universal persistence of provenance · `P6` loss-as-failure · Model `B` *(mere
metadata)* · Producer/Recorder/Event as required concepts.
**`[QUALIFIED]`** ⭐ `P-24`'s closing attribution claim · `P3`.
**`[UNWITNESSED]`** `A4`/`R-D` · the forcing witness of §15.
**`[OPEN]`** as listed in row 10.
⛔ **No Lane T construction used as corpus evidence; no `[ARCH]` or `[STIPULATED]` promoted; nothing
rewritten.**

---

```
STOP-F — RECORD ATTRIBUTION REDUCES TO ALREADY-ESTABLISHED PROVENANCE / STATE / TRANSITION-WARRANT
OBLIGATIONS. OUTCOME B: COVERED BY EXISTING CELLS. KERNEL REMAINS 11. NO NEW CAPABILITY.

THE SEARCH REFRAMED THE QUESTION. Provenance is massively attested — 1 766 files, plus 225 for
attribution — and `provenance of (the) record` returns ZERO. Measured extraction shows the corpus
attaches provenance to KNOWLEDGE (10), EVIDENCE (6), INFERRED ASSERTIONS, EPISTEMIC TRANSITIONS, the
CORRECTED ARTIFACT and the BOUNDARY REASON — never to a RECORD, an occurrence or a register entry, and
never to a PROCESS as its source. So the commissioned question is PARTLY MIS-POSED, and the R-A..R-D
discriminators are answered [OPEN] rather than forced.

AND THE ATTESTED TARGETS ARE EXACTLY THE KERNEL'S EXISTING DOMAINS. P-09 §9 had already derived three
provenance roles, and each maps cleanly: SOURCE-OF-CONTENT -> K2 (current source value) + K3a (prior
value under tau-6) + K5 (checkability); ACT-OF-ENTRY / TRANSITION -> K3b, which P-23 established is
transition-relative; EVIDENCE-FOR-A-CLAIM -> K4a-i + K3b. Provenance is therefore DISTRIBUTED across
five existing cells, and record-attribution is not even a CANDIDATE cell, because its subject is not a
record.

THE INDEPENDENCE TEST FAILS ON NECESSITY, not on representability: no corpus rule requires knowing
WHICH PROCESS wrote a record, because every attested provenance SOURCE is an ARTIFACT or an ACT —
Sigma D-05 to code@kosmodel.py:Sigma, C-022 a stipulation, G-67 a naming, FR-001 a measurement.

TWO CORPUS STATEMENTS DECIDE THE CLASSIFICATION: "provenance is NOT MERELY METADATA" refutes the
metadata reading, and "provenance is NOT KNOWLEDGE" refutes the content reading. So provenance is STATE
(a source value) plus a HISTORICAL RELATION (a transition warrant) — and neither is a separate
obligation.

THE STRONGEST SINGLE FINDING (A3): attribution CHANGE is tau-6 SOURCE RE-MARKING — already in T_KOS,
already non-monotone, already borne by K3a + K3b — and F-4 IS that case. Attribution's DYNAMICS are
therefore already covered.
AND (A5): the corpus explicitly CONTEMPLATES absent provenance — "without provenance" 32 files, "no
provenance" 24, "absent/missing/lost" 6, "unknown provenance" 2. The argument holds under BOTH readings:
if pejorative, provenance is an ADMISSIBILITY condition; if descriptive, items EXIST WITHOUT IT. Neither
makes it a persistence obligation. (Absence of evidence was NOT treated as evidence of absence — the
case is argued both ways.)
AND (A1): losing provenance does not make persistence FAIL — P-08 §5.3's remedy is to persist an extract
OR mark the grounding pointer-only, i.e. an EPISTEMIC DOWNGRADE. P5 and P6 are [REFUTED].

WITHDRAWN: P-24's closing claim that "H-P1/H-P3 show attribution is a real and separate datum" is
[QUALIFIED] — attribution is real, but it is neither RECORD-attached nor SEPARATE from P-09 §9's
already-derived roles.

DDD: the corpus requires ONLY A RELATION between an item and an artifact/act. PRODUCER, RECORDER and
EVENT are [REFUTED] as required domain concepts — no entity is created because a persistence proof
wanted a name. P-24's "a recorder is not an assertor" is preserved and NOT inverted.

NEXT QUESTION (one): IS PROVENANCE-CHAIN UNBROKENNESS A K5 OBLIGATION, OR DOES A CHAIN REQUIRE MORE
  THAN SINGLE-LINK CHECKABILITY? Derived from the [EMP] terms "provenance chain" and "provenance chain
  unbroken?" — K5 is stated for THE CURRENT ground, and a chain has DEPTH. It also finally engages
  P-16's deferred provenance-chain [OPEN].

NO ARCHITECTURE — NO SCHEMA v3 — NO IMPLEMENTATION — NO CANONICAL THEORY — 3MC UNTOUCHED — NO CELL
ADDED, SPLIT, MERGED, DELETED OR RE-SCOPED — NO SILENT REPAIR.
```

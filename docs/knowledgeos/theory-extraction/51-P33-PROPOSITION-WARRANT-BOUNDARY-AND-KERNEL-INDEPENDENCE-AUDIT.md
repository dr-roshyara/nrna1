# `P-33` — Proposition / Warrant Boundary & Kernel Independence Audit

**2026-09-08 · Lane T.** After [`50` `P-32`](./50-P32-GT004-EXPLICIT-UNKNOWN-REASON-AND-K5-SCOPE-AUDIT.md).

> **Single question:** *is the Proposition / Warrant split corpus-grounded, and does it justify keeping
> the corresponding persistence obligations separate?*
>
> ⛔ **`P-08`, `P-18`–`P-32`, the 11-cell kernel, Schema v2, 3MC / Lane M *(including `MD-021`
> "phase-5c **kernel object reconstruction**" and `MD-022`)*, the K-1/K-2 governance track FROZEN · no
> Schema v3 · no `K12` · no merge on the strength of a composite · no canonicalization · no
> architecture · no implementation · `P-32` is NOT proof of `P-33`.**
>
> ⚠️ **`MD-021` is Lane M working on kernel objects — directly adjacent to this question and
> deliberately NOT consumed.**

**Two declared commission deviations:** §17's merge condition 5 *("reduction works in both
directions")* is **not** the right criterion — a one-directional subsumption $I_W \subseteq I_P$ makes a
cell **redundant**, and requiring bidirectionality would preserve it; ⭐ **I test one-directional
subsumption as sufficient for reduction and report bidirectional equivalence separately.**
`[HYPOTHESIS]` is used **only where a source frames its own claim as one**, keeping `[STIPULATED]` for
author recommendation.

# 0. ⭐⭐⭐⭐ The split survives — but **not on the witness `P-32` pointed at**

$$\boxed{\begin{array}{ll}\textbf{⛔ } Fact = Proposition + Reason & \textbf{N = 1. One file, one line. Self-disclaimed. } \mathbf{+} \textbf{ never defined. } \mathbf{NOT\ the\ ground.}\\[4pt] \textbf{⭐⭐⭐ } \mathcal O_K\textbf{'s } \mathit{Corroborate} & \textbf{"New independent evidence supports an } \mathbf{existing} \textbf{ assertion …}\\ & \textbf{the assertion's } \mathbf{evidence\ basis\ becomes\ richer.} \textbf{" } \mathbf{[EMP]} \textbf{ — THIS is the ground.}\end{array}}$$

⭐⭐ **`P-32`'s closing question pointed at the wrong witness.** The composite hypothesis cannot ground
anything; the corpus's own **operation set** grounds the split outright.

$$\boxed{|K| = 11 \textbf{ — unchanged. ⭐ And now resting on } \mathbf{[EMP]} \textbf{ corpus operations instead of a self-disclaimed } \mathbf{[HYPOTHESIS]}.}$$

---

# 1–2. Witness-status audit — the triggering source, before any use

`refinement-of-the-model` opens: *"Yes. **That is an important refinement.** **I think** the word
*reason* is actually the missing bridge…"* — ⭐ **an author's conversational reflection.**

| claim in the source | status |
|---|---|
| *"A fact … becomes a **determined fact because there is a reason that warrants the determination**"* | ⭐ **`[STIPULATED]`** — the author's gloss |
| `Fact = Proposition + Reason` | ⭐⭐⭐ **`[HYPOTHESIS]`** — see below |
| *"then the Kernel **cannot merely** preserve `fact = "Nexus 3.69"`"* | `[STIPULATED]` |
| *"It **potentially** needs to preserve the relationship"* | ⭐ **hedged** |
| ⭐⭐ *"But again, **this is not yet a Kernel design.** We need to test whether each component survives our different lenses."* | ⭐⭐⭐ **the source DISCLAIMS kernel status in its own words** |

$$\boxed{\begin{array}{c}\textbf{⭐ The source frames its own equation as something } \mathbf{to\ be\ tested} \textbf{, not something established.}\\ \boxed{\textbf{That is precisely } \mathbf{[HYPOTHESIS]} \textbf{, and } \textit{"may tell us what the Kernel actually needs"} \textbf{ is NOT evidence that it does.}}\end{array}}$$

⭐⭐⭐ **And the source's own expansion refutes two of the seven `Reason` readings by its own hand:**

```
Fact
 ├── proposition      ├── evidence      ├── time
 ├── reason           ├── context       └── determination lineage
```

⭐ **`reason` and `evidence` are LISTED AS SIBLINGS** ⇒ ⭐⭐ **`R2` (Reason = Evidence) `[REFUTED]`.**
⭐ **`determination lineage` is a separate sibling** ⇒ ⭐⭐ **`R3` (Reason = Provenance) `[REFUTED]`.**
⭐ `context` and `time` are siblings ⇒ neither is the reason.

⭐⭐⭐⭐ **And the same document places `Reason` in THREE INCOMPATIBLE STRUCTURAL POSITIONS within
sixty lines:**

$$\boxed{\begin{array}{lll}\textbf{(a) INSIDE } Fact & Fact = Proposition + Reason & \textbf{§8}\\ \textbf{(b) INPUT producing } Fact & Reason + Context + Rules \xrightarrow{\ Determination\ } Fact & \textbf{§9}\\ \textbf{(c) BESIDE } Fact & Fact + Reason + History \rightarrow Future\ Determinations & \textbf{§9}\end{array}}$$

⭐⭐ **(a) and (c) cannot both hold — if `Reason` is inside `Fact`, then `Fact + Reason` is
redundant.** ⭐ **These are not three uses of one relation; they are three different relations sharing
a word.**

# 3. Semantic distinction table

| term | corpus meaning | object / role / value | attested | relation to Proposition | relation to `K3b` |
|---|---|---|---|---|---|
| **Proposition** | the claim content | ⭐ **content of an assertion** | ⭐ `[EMP]` | — | ⛔ distinct |
| **Reason** | ⚠️ **overloaded — 3 positions** | ⭐⭐ **`R6`** | **`[HYPOTHESIS]`** | ⚠️ undetermined | ⚠️ undetermined |
| **Warrant** | what justifies a transition | role | ⭐ `[EMP]` *(`P-08` §16)* | ⛔ distinct | ⭐ **= `K3b`** |
| **Evidence** | `ℰ_t`, **a separate field of `K_t`** | ⭐⭐ **entity set** | ⭐ `[EMP]` | ⛔ **separate field** | input to warrant |
| **Justification** | 415 files — ⚠️ generic usage | mixed | descriptive only | — | — |
| **Grounds / basis** | 143 / 376 — ⚠️ generic | mixed | descriptive only | — | — |
| **determination lineage** | ⭐ **3 files** — a sibling of `reason` | relation | `[STIPULATED]` | ⛔ separate | ⛔ separate |

⚠️ **`justification` 415 · `basis` 376 · `grounds` 143 are NOT treated as evidence of an ontology** —
they are ordinary prose across an authored development. **Per §14: prevalence is not replication.**

# 4. What "Reason" means — **`R6`**, overloaded

| | reading | verdict |
|---|---|---|
| **`R1`** Warrant | ⭐ *"a reason that **warrants** the determination"* — the closest single reading | ⚠️ **`[STIPULATED]`, and NOT adopted as *the* meaning** *(§22)* |
| **`R2`** Evidence | ⭐⭐ **`[REFUTED]`** — sibling in the source's own list |
| **`R3`** Provenance | ⭐⭐ **`[REFUTED]`** — `determination lineage` is a separate sibling |
| **`R4`** Explanation | possible in position (b) | `[OPEN]` |
| **`R5`** Transition cause | ⭐ possible — `transition reason` **2 files** | `[OPEN]` |
| ⭐⭐⭐ **`R6`** overloaded | ⭐⭐ **`[EMP]` — three incompatible positions in ONE document** | ⭐ **SELECTED** |
| **`R7`** insufficient | subsumed by `R6` | — |

# 5. `Fact = Proposition + Reason` — **Model `F`, underdetermined**

| model | verdict |
|---|---|
| **A** composite object | ⛔ contradicted by position (c) |
| **B** association | ⛔ contradicted by position (a) |
| **C** record representation | ⚠️ possible — the six-sibling tree is record-shaped |
| **D** analytical decomposition | ⚠️ possible — *"not yet a Kernel design"* supports it |
| **E** context-specific | ⚠️ possible |
| ⭐⭐⭐ **F** underdetermined | ⭐⭐ **SELECTED — C, D and E are all live and the source does not choose** |

# 6 · 15. Mathematical discipline — what is `+`?

$$\boxed{\begin{array}{ll}\textbf{ordered pair / product} & \textbf{⚠️ possible, but contradicted by position (c)}\\ \textbf{mereological composition} & \textbf{⚠️ possible}\\ \textbf{record concatenation} & \textbf{⚠️ possible — the tree is record-shaped}\\ \textbf{logical conjunction} & \textbf{⛔ type-incoherent — a proposition and a reason are not both truth-bearers here}\\ \textbf{explanatory notation} & \textbf{⭐⭐ MOST CONSISTENT with } \textit{"this is not yet a Kernel design"}\end{array}}$$

$$\boxed{\mathbf{+} \textbf{ is NEVER DEFINED. ⭐ The expression is classified } \mathbf{SEMANTICALLY\ UNDER\text{-}SPECIFIED} \textbf{ — ⛔ not silently read as a tuple.}}$$

⭐ **Equality, isomorphism, decomposition, product, association, representation, projection and
encoding are kept apart, and the corpus determines none of them.**

# 7–8 · 13. ⭐⭐⭐ The independence attack — both reductions FAIL

**The decisive `[EMP]` witness, `step-007 §7.2`:**

$$\boxed{\mathit{Corroborate}(A,e) \rightarrow A' \qquad \textbf{"New independent evidence supports an } \mathbf{existing} \textbf{ assertion … the assertion's } \mathbf{evidence\ basis\ becomes\ richer.} \textbf{"}}$$

⭐⭐ **The assertion is the same assertion; its grounding changes.** ⭐ **Contrast `§7.3 Qualify`, where
`Nexus 3.69` becomes `Nexus 3.69 in the legacy environment` — there the PROPOSITION changes.** The
corpus distinguishes the two operations explicitly.

| | history | `K4a-i` retains | `K3a`/`K3b` retain |
|---|---|---|---|
| **`H1`** | `p1 → p2` supersede | ⭐ `p1` | pair + warrant |
| **`H2`** | `(p1,w1) → (p2,w2)` | `p1` | pair + `w1` |
| ⭐⭐⭐ **`H3`** | ⭐ **`p1+w1 → p1+w2` — a `Corroborate`** | ⭐⭐ **`p1` and `p1` — NOTHING DISTINGUISHES THEM** | ⭐⭐ **the transition + its warrant** |
| **`H4`** | `p1+w1 → p2+w1` | `p1` | pair + warrant |
| **`H5`** | withdrawn, no successor | `p1` | `K4b-i` ground |

$$\boxed{\begin{array}{c}\mathbf{K3b\ is\ NOT\ reducible\ to\ } K4a\text{-}i\mathbf{:}\ \textbf{⭐⭐ a } \mathit{Corroborate} \textbf{ transition is } \mathbf{INVISIBLE\ to\ } K4a\text{-}i \textbf{ — the proposition never changed.}\\[4pt] \mathbf{K4a\text{-}i\ is\ NOT\ reducible\ to\ } K3b\mathbf{:}\ \textbf{⭐ under } \mathit{Supersede}\textbf{, } K3b \textbf{ retains } \mathbf{why} \textbf{ the replacement happened, never } \mathbf{what\ the\ prior\ proposition\ was.}\\ \textbf{⚠️ Unless the warrant CONTAINS the proposition — } \mathbf{[UNWITNESSED]}\textbf{, and not assumed.}\end{array}}$$

⭐ **Tested in both directions as §13 requires, and neither succeeds.** ⭐⭐ **One-directional
subsumption — my declared criterion — also fails in both directions, so the stricter bidirectional
test never had to carry the result.**

# 9 · 12. The four adversarial pairs

| | pair | corpus status | distinguished by |
|---|---|---|---|
| ⭐⭐⭐ **A** same proposition, different warrant | ⭐⭐ **`[EMP]` — `Corroborate`** | ⭐ **`K3b` ONLY** — `K4a-i` cannot see it |
| ⭐ **B** different proposition, same warrant | ⭐ **`[DERIVED]`** — `ℰ_t` and `𝒜_t` are **separate fields** of `K_t=(𝒜_t,ℰ_t,ℛ_t,𝒞_t,ℋ_t,Γ_t)` with `ℛ_t` relating them, so one evidence item may ground several assertions | ⭐ **`K4a-i` ONLY** |
| **C** same current fact, different historical path | `(p1,w1)→(p2,w2)` vs `(p1,w3)→(p2,w2)` | ⭐ **`K3b`** — `K4a-i` is identical in both |
| ⚠️ **D** composite equality, persistence difference | ⭐⭐⭐ **`NOT FORMALLY SPECIFIED ENOUGH TO TEST`** — the corpus has **no definition of "same Fact"**; `Fact = Proposition + Reason` is `[HYPOTHESIS]` with `+` undefined | ⛔ **gap NOT filled** |

$$\boxed{\begin{array}{c}\textbf{⭐⭐ Pairs A and B are } \mathbf{CONVERSE} \textbf{ and both hold ⇒ } P \textbf{ and } W \textbf{ vary } \mathbf{independently\ in\ BOTH\ directions.}\\ \boxed{I_P \nsubseteq I_W \quad\textbf{and}\quad I_W \nsubseteq I_P \quad\textbf{— independently necessary, with } I_P \cap I_W \textbf{ non-empty at the transition.}}\end{array}}$$

# 10. Composition ≠ redundancy — tested, and it did not need to carry the result

⭐ Even had the composite been established, `F_t = (P_t, W_t)` with `P_t` and `W_t` varying
independently would leave **two distinguishable retention requirements**. ⭐⭐ **`Corroborate` proves
they do vary independently — so the composite question is settled as MOOT for cardinality**, which is
the cleanest possible answer to §10.

# 11. `P-30` interaction — the distinction is **not** retrospective

| object | kept apart |
|---|---|
| `Accepted(p)` | ⭐ **current, computed, policy-relative** *(`P-30`)* |
| `Warrant(p,t)` | ⭐ **`K3b`** — transition-relative *(`P-23`, `P-30`)* |
| `HistoricalAcceptance(p,t,w)` | ⭐ **`K3a` + `K3b`** *(`P-30`'s correction)* |

⭐⭐ **`Corroborate` is dated 2026-08-27 (`step-007`), months before Lane T's `P-08`.** ⇒ ⭐⭐⭐ **The
Proposition/Warrant distinction is in the corpus INDEPENDENTLY of Lane T's derivation — it was not
introduced retrospectively.** ⭐ **`P-30`'s result is explained by it: acceptance history rides on
transitions because operations like `Corroborate` change grounding without changing content.**

# 14 · 18–19. Statistics, DDD, falsification

**Statistical discipline.** ⭐⭐ `Fact = Proposition + Reason` — **one file, one line, N = 1**, inside a
single authored reflection. ⛔ **No independent replication, no corroboration, no direct specification.**
`warrant` 118 · `justification` 415 · `basis` 376 are **prevalence within one development**, used
descriptively only.

**DDD** — ⭐ **and separate persistence cells are NOT inferred to be separate entities:**

| | classification |
|---|---|
| Proposition | ⭐ **value** — content of an assertion |
| Fact | ⭐⭐ **explanatory construct — `[UNWITNESSED]` as a domain object** |
| Reason | ⭐⭐⭐ **unknown — overloaded** |
| Warrant | ⭐ **relation/role** attaching a basis to a transition |
| Evidence | ⭐ **entity set `ℰ_t`** |
| Acceptance | ⭐ current predicate + historical transition *(`P-30`)* |
| Verification | ⭐⭐ **external assurance concept** *(`P-32`)* |

**Falsification, both directions.** ⭐ `proposition without warrant` **0** · `warrant without` **0** ·
`reason is attached to` **0** — ⚠️ **and per `P-30`'s lesson these zeros prove nothing**; the
**concept** was searched and the answer came from the **operation set**, not the vocabulary. ⭐⭐ **The
strongest disconfirming possibility — that a warrant can never change alone — is `[REFUTED]` by
`Corroborate`.**

# 16–17. Kernel verdict

| condition for a **MERGE** | status |
|---|---|
| 1 same required information | ⭐⭐ 🔴 **FAILS — Pairs A and B** |
| 2 no corpus-required distinction lost | 🔴 **FAILS** — `Corroborate` becomes invisible |
| 3 no historical counterexample | 🔴 **FAILS** — `H3` |
| 4 no warrant/proposition distinction lost | 🔴 **FAILS** |
| 5 reduction in both directions | 🔴 **FAILS** — ⭐ **and one-directional subsumption fails too** |

$$\boxed{\mathbf{A} \textbf{ — NO CHANGE. } |K| = 11. \textbf{ ⭐ And chosen because the merge test FAILED five ways, not because 11 was preserved.}}$$

⛔ **Not `B`** — no cell needs a narrower reading · ⛔ **not `C`** — both reductions fail · ⛔ **not
`D`** — no new independent information · ⛔ **not `E`** — the operation set decides it.

---

# 20. Status register · ⭐ and an explicit `P-32` correction

⭐⭐⭐ **`P-32` OVERSTATED ITS WITNESS.** `P-32` recorded as **`[EMP]`**: *"the reason must be preserved
alongside the fact … otherwise we cannot understand why the fact was ever accepted."*

$$\boxed{\begin{array}{c}\mathbf{[QUALIFIED]} \textbf{ — downgraded } \mathbf{[EMP] \rightarrow [STIPULATED]}. \textbf{ It is one author's reflection } (\textit{"I think"})\\ \textbf{in the SAME N = 1 source whose very next section says } \textit{"this is not yet a Kernel design."}\\[4pt] \boxed{\textbf{⭐ } P\text{-}32\textbf{'s VERDICT SURVIVES: its grounds 1 (status) and 2 (estate) are independent and each sufficient.}}\end{array}}$$

**`[EMP]`** ⭐⭐ `Corroborate(A,e) → A'`, *"an **existing** assertion … evidence basis becomes richer"* ·
`Qualify` changes the proposition · `𝒪_K = {Introduce, Corroborate, Qualify, Contradict, Supersede,
Resolve, Retract, Invalidate}` · `K_t=(𝒜_t,ℰ_t,ℛ_t,𝒞_t,ℋ_t,Γ_t)` with `𝒜_t` and `ℰ_t` **separate
fields** · the source's six-sibling expansion · *"this is not yet a Kernel design"* · `Fact =
Proposition + Reason` occurs in **exactly one file, one line**.
**`[DERIVED]`** ⭐⭐ **both reductions fail** · Pair B from the separate-field structure ·
`Corroborate` predates `P-08` ⇒ **not retrospective** · `+` is undefined ⇒ under-specified.
**`[HYPOTHESIS]`** ⭐ `Fact = Proposition + Reason` — **the source's own framing** *(new status, used
only here)*.
**`[STIPULATED]`** *"a reason that warrants the determination"* · *"the reason must be preserved
alongside the fact"* · `determination lineage`.
**`[CORROBORATION]`** the six-sibling tree corroborates that `reason ≠ evidence`.
**`[REFUTED]`** ⭐ **`R2` Reason=Evidence** · **`R3` Reason=Provenance** · *composite ⇒ merge* ·
*`K3b` reducible to `K4a-i`* · *`K4a-i` reducible to `K3b`* · *the split is a Lane T artifact*.
**`[QUALIFIED]`** ⭐⭐ **`P-32`'s `[EMP]` claim → `[STIPULATED]`** · ⭐ **`P-32`'s closing question —
it named the wrong witness**.
**`[UNWITNESSED]`** `Fact` as a domain object · a definition of *"same Fact"* · warrant containing the
proposition · `R1` as *the* meaning of Reason.
**`[OPEN]`** ⭐ **Pair D — not formally specifiable** · `R4`/`R5` readings · the 28 abandoned `GT`
invariants *(`P-32`)* · `145.20`/`145.21`'s `Reason`-field discrepancy · the admitted-transition **or**
infrastructure disjunction *(`P-31`)* · `ω`'s unglossed partiality · `Accepted ⇒ Warrant` *(`P-30`)* ·
`Governance`-in-`K` vs `P-28` · `W3`'s conditional *(`P-29`)* · `Ind_ρ`'s five other slots ·
`K4b-ii`'s corroboration arm · register deletion · *"estate"*'s two referents · ⛔ **well-foundedness /
terminality / acyclicity — DEFERRED.**

### ONE NEXT UNRESOLVED QUESTION

$$\boxed{\textbf{Does } \mathit{Corroborate}(A,e) \rightarrow A' \textbf{ preserve assertion IDENTITY, or create a NEW assertion — and which does } K1 \textbf{ require?}}$$

```
A — NO CHANGE. |K| = 11. But chosen because the MERGE TEST FAILED FIVE WAYS, not because 11 was
preserved — and the split now rests on a completely different witness than P-32 pointed at.

THE COMPOSITE IS NOT THE GROUND, AND CANNOT BE. "Fact = Proposition + Reason" occurs in EXACTLY ONE
FILE, ONE LINE — N=1, no replication. Its own source disclaims it: "But again, this is not yet a
Kernel design. We need to test whether each component survives our different lenses." That is
[HYPOTHESIS] by the author's own framing, and "may tell us what the Kernel actually needs" is not
evidence that it does.

THE SOURCE REFUTES TWO READINGS BY ITS OWN HAND. Its expansion lists SIX siblings — proposition,
reason, evidence, context, time, determination lineage. So reason is NOT evidence (R2 refuted) and NOT
provenance (R3 refuted); both are separate siblings. And within sixty lines the same document places
Reason in THREE INCOMPATIBLE POSITIONS: inside Fact (§8), as an INPUT producing Fact (§9), and BESIDE
Fact (§9). (a) and (c) cannot both hold — if Reason is inside Fact, "Fact + Reason" is redundant. So
Reason is R6, OVERLOADED, and "+" is NEVER DEFINED: the expression is SEMANTICALLY UNDER-SPECIFIED,
not silently read as a tuple.

THE REAL GROUND IS THE CORPUS'S OWN OPERATION SET. step-007 §7.2: "Corroborate(A,e) -> A'. New
independent evidence supports an EXISTING assertion ... the assertion's EVIDENCE BASIS BECOMES RICHER."
Same proposition, changed grounding — Pair A, [EMP]. Contrast §7.3 Qualify, where "Nexus 3.69" becomes
"Nexus 3.69 in the legacy environment" and the PROPOSITION changes. The corpus separates the two
operations explicitly.

BOTH REDUCTIONS FAIL, TESTED IN BOTH DIRECTIONS. K3b does not reduce to K4a-i: a Corroborate transition
is INVISIBLE to K4a-i, because the proposition never changed. K4a-i does not reduce to K3b: under
Supersede, K3b retains WHY the replacement happened, never WHAT the prior proposition was — unless the
warrant contains the proposition, which is [UNWITNESSED] and was not assumed. One-directional
subsumption — my declared criterion, weaker than the commission's — also fails both ways, so the
stricter test never had to carry the result.

PAIRS A AND B ARE CONVERSE AND BOTH HOLD, so I_P and I_W are independently necessary. Pair D is
reported as NOT FORMALLY SPECIFIED ENOUGH TO TEST: the corpus has no definition of "same Fact", and I
did not fill the gap.

THE DISTINCTION IS NOT RETROSPECTIVE. Corroborate is dated 2026-08-27, months before Lane T's P-08. The
Proposition/Warrant split is in the corpus independently of our derivation — and it EXPLAINS P-30's
result, since operations like Corroborate change grounding without changing content.

EXPLICIT P-32 CORRECTION. P-32 recorded as [EMP] that "the reason the corpus does require epistemically
explains why a fact was accepted." DOWNGRADED to [STIPULATED]: it is one author's reflection ("I
think") in the same N=1 source that disclaims kernel status one section later. P-32's VERDICT SURVIVES
— its grounds 1 (status: ill-typed and abandoned) and 2 (estate: Assurance) are independent and each
sufficient. But P-32's closing question named the wrong witness, and that is [QUALIFIED].

MD-021 — Lane M's "phase-5c kernel object reconstruction" — is directly adjacent to this question and
was deliberately NOT consulted.

ONE NEXT UNRESOLVED QUESTION: Does Corroborate(A,e) -> A' preserve assertion IDENTITY, or create a NEW
assertion — and which does K1 require?

NO SCHEMA v3 — NO MERGE — NO NEW CELL — NO CANONICALIZATION — NO ARCHITECTURE — 3MC AND LANE M
UNTOUCHED — WELL-FOUNDEDNESS STILL DEFERRED.
```

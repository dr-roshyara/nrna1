# `P-20` — Ground Stratification & Kernel Cardinality Adversarial Audit

**2026-09-08 · Lane T · theory-level stratification audit.** After [`36` `P-19`](./36-P19-KERNEL-DERIVATION-ASSUMPTION-CIRCULARITY-BOUNDARY-AUDIT.md).

> **The sole question:** *does adding a current-state representation for GROUNDS constitute a legitimate
> alternative obligation universe, and if so does it make `K4b-ii` reducible?*
>
> ⛔ **No Schema v3 · no architecture or persistence technology · Schema v2 and canonical theory
> unmodified · 3MC untouched · `P-13`–`P-19` not reopened globally · no cell invented, no cell deleted ·
> no silent repair · mathematics, DDD and statistics are never ontology · necessity is never inferred
> from convenience.**
>
> ⭐ **The answer was not chosen in advance, and the objective is neither 10 nor 11.**

# 1. Starting state — frozen
**11 cells** · `R0`, `Q1a` `[STIPULATED]` · **`K1` independent by PRESUPPOSITION, not minimal pair** ·
`K2` conditional on `R0` · `K3a`, `K3b`, `K4a-i`, `K4b-i` independent · **`K4a-ii` independent for
`n≥3`** · **`K4b-ii` independent but stratification-dependent** · `K4c-i`/`K4c-ii` `Q1a`-conditional,
**`E3`** · **`K5` independent only as restated** · no cell added or deleted. ⛔ **`P-19` untouched.**

⭐ **Accepted correction to `P-19`'s wording:** the kernel is **safe to freeze as a QUALIFIED RESEARCH
RESULT**, ⛔ **not as canonical KnowledgeOS theory.** Used in this sense throughout.

---

# 2–3. ⭐⭐⭐ Is `K2g` even legitimate? — and the asymmetry `P-19` posited turns out to be FALSE

`P-19` §9 assumed an **asymmetry**: assertions have a current-state representation via `K2`; grounds
have none. **That assumption is now tested — and it fails.**

| step | finding |
|---|---|
| what would *"the current ground"* designate? | the ground the estate **now cites** for a claim |
| ⭐ where does a definition's ground live? | ⭐⭐ **`P-09.2` §4.9: *definition source* is a VALUE ON A DEFINITION.** `P-08` §4.3 lists **source** among what must survive for a definition |
| ⭐⭐ which cell covers a definition's field values? | ⭐⭐⭐ **`K2` — current-state preservation** |

$$\boxed{\begin{array}{c}\textbf{THE CURRENT GROUND OF A DEFINITION IS ALREADY COVERED — by } K2 \textbf{, as a field value.}\\[6pt] \boxed{\begin{array}{l}K2g \textbf{ is therefore NOT a new capability. It is } K2 \textbf{ APPLIED TO THE SOURCE FIELD.}\\ \textbf{So } H\text{-}B\textbf{'s premise is ALREADY SATISFIED by the existing kernel, and }\\ P\text{-}19\textbf{'s posited asymmetry between assertions and grounds DOES NOT EXIST.}\end{array}}\end{array}}$$

⭐ **Consequence for the audit's shape:** the question is no longer *"may we add `K2g`?"* but the sharper
*"given that the current ground is ALREADY retained, does `K4b-ii` collapse?"* — **§6**.

⚠️ **And `K2g` is not admitted as a cell** — it would be a **duplicate of `K2` at another label**, which
§9's identifiability test forbids. ⛔ **No capability is introduced because it makes the arithmetic
convenient.**

# 4. *Exists* / *retained* / *current* — kept apart

| | what it is | where it lives |
|---|---|---|
| **`A` a ground EXISTS** | a fact about the world | ⛔ **not a capability** — `[EMP]`-able |
| **`B` a ground is RETAINED** | the estate keeps a ground it no longer cites | ⭐ **`K4b-i`** |
| **`C` a ground is CURRENT / OPERATIVE** | ⭐⭐ **a FIELD VALUE on a definition** | ⭐ **`K2`** |

$$\boxed{\textbf{"Current" is NOT a semantic primitive — it is a PROPERTY OF A FIELD VALUE. ⛔ It acquires no primitive status here.}}$$

---

# 5. `K4b-ii` formalized without any added cell — the state space, stated explicitly

**Competing histories for one claim:** ⭐ *replacement* `g₁ → g₂` versus ⭐ *corroboration* `g₁` **and**
`g₂` both standing.

**Remove `K4b-ii`. Everything the remaining kernel retains:**

| cell | content |
|---|---|
| `K1` | the definition's identity |
| ⭐ **`K2`** | ⭐⭐ **the CURRENT source value = `g₂`** *(in both histories)* |
| `K3a` | prior source value `g₁` *(source re-marking is `τ6`, non-monotone)* |
| `K3b` | the warrant for the change |
| `K4a-i`, `K4a-ii` | the claim's own assertion history |
| **`K4b-i`** | ⭐ **`g₁` retained** |
| `K4c-i`, `K4c-ii` | untouched |
| `K5` | `g₂`'s checkability |

$$\boxed{\begin{array}{c}\textbf{In BOTH histories the retained state is: } (\text{id},\ \text{current}=g_2,\ \text{prior}=g_1,\ W,\ g_1 \text{ retained}).\\[4pt] \boxed{\textbf{IDENTICAL. And the histories differ: in one } g_1 \textbf{ was INVALIDATED; in the other } g_1 \textbf{ STILL STANDS but is not the cited one.}}\end{array}}$$

⭐⭐⭐ **Single-valuedness of the source field does NOT settle it** — *"not the current citation"* is
**not** *"invalidated"*. ⭐ **So the `K2`-over-source designation, though present, leaves `K4b-ii`'s
distinction unmade.** ⛔ **This is stated as a state space, not as *"the relation is not
reconstructible."***

# 6. ⭐⭐⭐ Does the `n=2` argument transfer? — and it does not even hold for ASSERTIONS

`P-18` claimed: for assertions, `{p₁,p₂}` + `current = p₂` ⇒ **the only topology is `p₁ ⊐ p₂`**, so
`K4a-ii` is derivable at `n=2`. ⭐ **The proposed collapse of `K4b-ii` depends entirely on that
argument transferring.**

## The inference `P-18` made, and the assumption hidden in it

⚠️ *"the other must have been superseded by the current one"* presupposes that **every non-current
assertion was SUPERSEDED**.

$$\boxed{\begin{array}{c}\textbf{But a claim can be WITHDRAWN WITH NO SUCCESSOR — } \mathbf{[EMP]}\textbf{: } \mathit{INV\text{-}9} \textbf{ was withdrawn and NOTHING replaced it.}\\[6pt] \boxed{\begin{array}{l}\textbf{So at } n=2 \textbf{ there are TWO topologies: (a) } p_1 \sqsupset p_2, \textbf{ and}\\ \textbf{(b) } p_1 \textbf{ withdrawn-with-no-successor WHILE } p_2 \textbf{ was independently asserted.}\end{array}}\end{array}}$$

| | |
|---|---|
| **is the distinction required?** | ⭐ ✅ **YES — `P-08` §5.2's process-reliability ground.** *"Was `p₁` replaced by a better claim, or simply abandoned?"* is precisely reliability information |
| ⭐⭐⭐ **verdict** | ⭐⭐ **`P-18`'s `n=2` "near-miss" is `[REFUTED]`. `K4a-ii` is INDEPENDENT AT ALL `n ≥ 2`** |

$$\boxed{\begin{array}{c}\textbf{The analogy fails at its SOURCE, not at its target.}\\ \boxed{\textbf{There was never an } n{=}2 \textbf{ reduction to transfer — so } K2g \textbf{ cannot collapse } K4b\text{-ii by that route.}}\end{array}}$$

⭐ **Two `P-18`/`P-19` results are corrected at once, and in opposite directions from what was
expected**: `K4a-ii` **gains** unconditional independence, and `K4b-ii` **loses** its stratification
conditionality.

# 7. Set-wise independence re-run

| under `H-A` *(no `K2g` cell)* | ⭐ §5's pair: identical in the ten others, differing only in whether `g₁` was invalidated ⇒ **`K4b-ii` independent** |
|---|---|
| ⭐ **under `H-B`** *(current ground retained — which is the ACTUAL state of the kernel)* | ⭐⭐ **§5's pair is UNCHANGED, because `K2`'s source value is `g₂` in both arms** ⇒ **`K4b-ii` STILL independent** |
| does `K4b\text{-}ii \models K4a\text{-}ii + K2g` hold? | ⭐ 🔴 **NO** — §6 removes the inference rule the entailment needed |

$$\boxed{K4b\text{-ii is independent under BOTH hypotheses. ⛔ Verbal similarity was not used anywhere.}}$$

# 8. Information-theoretic impossibility

Let `R(H)` be the state retained after removing `K4b-ii`. From §5:

$$R(H_{\text{replacement}}) = R(H_{\text{corroboration}}) = (\text{id},\ g_2,\ g_1,\ W,\ g_1\ \text{retained})$$

$$\boxed{\textbf{The retained state is the SAME OBJECT } \Rightarrow \textbf{ no } f(R(H)) \textbf{ can differ on the two histories. IMPOSSIBILITY, not absence of witness.}}$$

⭐ **Same logic as `P-19` §7.** ⛔ **Correlation, association, reconstructibility, logical entailment and
identity are kept apart** — this is **logical entailment failing**, not reconstructibility being hard.

---

# 9. Statistical / identifiability audit
⛔ **No sampling model, no significance, no p-values.**

| question | answer |
|---|---|
| is `K2g` **independently identifiable**? | ⭐⭐ 🔴 **NO — it is `K2` at another label.** §3 |
| is its introduction merely a **reparameterization**? | ⭐ ✅ **yes, exactly** |
| does it change the **information partition**? | 🔴 **no** — the source value was already retained |
| does it create a **new observable state**? | 🔴 **no** |
| does it **duplicate an existing variable at another stratum**? | ⭐⭐⭐ ✅ **YES — this is the decisive identifiability failure** |

$$\boxed{\textbf{A duplicate variable at a second label is a REPARAMETERIZATION, never a new capability.}}$$

# 10. Mathematical audit — and `P-19`'s statement classified

| distinction | held? |
|---|:--:|
| **assertion ≠ warrant** | ✅ |
| ⭐ **warrant-as-assertion ⇏ current-ground state** | ⭐⭐ ✅ — **`P-19` came close to this type-inheritance error; §3 shows the state exists for a DIFFERENT reason (it is a field value), not by inheritance** |
| **relation ≠ endpoint** | ✅ |
| **identity ≠ denotation** | ✅ — `P-19` §12's finding |
| ⭐ **currentness ≠ existence** | ✅ §4 |
| **historical precedence ≠ current state** | ✅ |
| **equivalence ≠ identity** | ✅ ⛔ **`≡_sem` untouched** |
| **reconstructibility ≠ persistence obligation** | ✅ |

## `P-19` §9's claim — *"`K4b-ii` would collapse into `K4a-ii` if grounds acquired a current-state cell"*

$$\boxed{\textbf{Classification: } \mathbf{C\ —\ ONLY\ AN\ ANALOGY.}}$$

⭐ **It was never demonstrated, and it fails twice over:** (i) grounds **already have** a current-state
representation, so the antecedent was **already true** and no collapse occurred; (ii) the consequent
relied on an `n=2` inference that ⭐ **does not hold even for assertions** (§6). ⛔ **`P-19` is not
rewritten; the classification is recorded here.**

# 11. DDD audit — with implementation removed

> *In the domain, is there a concept corresponding to "current ground"?*

| candidate | forced by the source material? |
|---|:--:|
| **Ground** | ✅ — `[EMP]`, a definition's `source` |
| **Grounding relation** | ✅ — `[EMP]`, `P-09` §9's three provenance roles |
| ⭐ **Current / Operative Ground** | ⚠️ **only as a FIELD VALUE** — `[DERIVED]`, ⛔ not as a domain concept |
| **Replaced Ground** | ✅ — `[EMP]`, the `Closure(𝒦₉)` case |
| **Corroborating Ground** | ⭐ ⚠️ **`[OPEN]`** — the *concept* is needed to state `K4b-ii`'s witness, but **no corpus instance of two simultaneously-standing grounds is attested** |
| **Supporting Ground** | ⛔ **`[ARCH]`** — not forced |

⛔ **None of these is created as a domain concept.** ⭐ **And *"Corroborating Ground"* is flagged
honestly: `K4b-ii`'s witness is `E2`-constructible, its corroboration arm unattested.**

---

# 12. Artificial stratification test — unify assertions and grounds

**Hypothetical unified model:** assertions and grounds are instances of one assertion structure. Then
`K4a-ii` and `K4b-ii` are both *"supersedes vs coexists"*. **Do they become one capability?**

| step | finding |
|---|---|
| same **retention rule**? | ⭐ ✅ **yes** — retain the relation |
| does that collapse them? | ⭐⭐ **only if STRATUM parameterizes rather than individuates** |
| ⭐ what does the programme's own criterion say? | ⭐⭐ **`P-09.3` §10: ARITY parameterizes; SUBJECT individuates** |
| ⚠️ but `P-17` found the SUBJECT AXIS retrospective — does subject still individuate? | ⭐⭐⭐ ✅ **YES. `P-17` §3 weakened the AXIS and left the OBLIGATIONS intact, because `Π2` is a HISTORY, not a label** |

$$\boxed{\begin{array}{c}\textbf{They do NOT collapse. Representational unification is available; the obligations still differ,}\\ \textbf{because } \Pi_2 \textbf{ separates them by a HISTORY that no relabelling touches.}\\[6pt] \boxed{\textbf{REPRESENTATIONAL UNIFICATION} \neq \textbf{OBLIGATION COLLAPSE — the same lesson as } P\text{-}19\textbf{'s } \textit{compression} \neq \textit{reduction}.}\end{array}}$$

# 13. The opposite artificiality
> *Does giving all grounds a current-state cell create a capability the domain never requires?*

⭐ ✅ **Yes — and that is why `K2g` is refused as a cell.** It would be an **artificial capability**
introduced by analogy from assertions. ⛔ **And symmetry is honoured: neither the omission nor the
inclusion of `K2g` was justified by its effect on cardinality** — the inclusion is refused on
**identifiability** (§9), and the exclusion would have been refused too, since **the state exists
anyway inside `K2`.**

# 14. Circularity — audited symmetrically

| direction | verdict |
|---|---|
| **does `K2g` exist only because `K4b-ii` needs it?** | ⭐ 🔴 **no — the inverse: `K2g` was proposed to REMOVE `K4b-ii`.** And it is refused for an independent reason (§9's duplication) |
| ⭐⭐ **does `K4b-ii` remain independent only because `K2g` was deliberately omitted?** | ⭐⭐⭐ 🔴 **NO — and this is the audit's cleanest result.** §5 shows the current ground **IS** retained (via `K2`) and `K4b-ii`'s witness **survives unchanged**. Its independence does **not** depend on any omission |
| ⚠️ residual | ⭐ **`K4b-ii`'s independence now depends on the CORROBORATION arm being coherent** — and §11 marks that arm **`[OPEN]`/unattested**. **A new, weaker dependency replaces the old one** |

---

# 15. Kernel cardinality table

| capability | under `H-A` | under `H-B` | reason |
|---|---|---|---|
| **`K1`** | independent *(presupposition)* | **unchanged** | untouched by stratification |
| **`K2`** | independent, `R0`-conditional | ⭐ **unchanged — and it ALREADY carries the current ground** | §3 |
| **`K3a`** | independent | unchanged | `τ6` prior source value |
| **`K3b`** | independent | unchanged | — |
| **`K4a-i`** | independent | unchanged | — |
| ⭐ **`K4a-ii`** | ⭐ **independent for `n≥2`** | ⭐ **independent for `n≥2`** | ⭐⭐ **the `n=2` near-miss is `[REFUTED]` — §6** |
| **`K4b-i`** | independent | unchanged | — |
| ⭐⭐ **`K4b-ii`** | independent | ⭐⭐⭐ **INDEPENDENT — no collapse** | §5, §6, §8 |
| **`K4c-i`/`K4c-ii`** | `Q1a`-conditional, `E3` | unchanged | — |
| **`K5`** | independent as restated | unchanged | — |
| ⭐ **`K2g`** | ⛔ **not a cell** | ⛔ **not a cell — a reparameterization of `K2`** | §9 |

| | |
|---|---|
| **cardinality, current boundary** | ⭐ **11** |
| **cardinality, expanded boundary** | ⭐ **11 — the expansion adds no cell, because the state was already retained** |
| ⭐⭐ **is the difference substantive or representational?** | ⭐⭐⭐ **PURELY REPRESENTATIONAL. There is no 10-vs-11 question — the asymmetry `P-19` posited does not exist** |

# 16. Verdict

$$\boxed{\textbf{OUTCOME C — } K2g \textbf{ IS LEGITIMATE AS AN INSTANCE OF } K2 \textbf{ (not as a cell), AND IT DOES NOT COLLAPSE } K4b\text{-ii.}}$$

⭐⭐ **`P-19` said this outcome would make the 11-cell kernel *stronger than P-19 states*. It does, in
three ways:**
1. ⭐⭐⭐ **`K4b-ii`'s stratification conditionality is REMOVED** — it rested on an asymmetry that does
   not exist. **One of `P-19`'s five freeze qualifications is discharged.**
2. ⭐⭐ **`K4a-ii` becomes independent at `n≥2`, not just `n≥3`** — `P-18`'s near-miss is refuted.
3. ⭐ **The 10-vs-11 question dissolves** rather than being decided: no cardinality was ever at stake.

⚠️ **And one qualification is ADDED, not hidden:** `K4b-ii`'s witness needs a **corroboration arm**, and
**no corpus instance of two simultaneously-standing grounds is attested** — `[OPEN]`.

---

# 17. Provenance
**`[EMP]`** *definition source* is a value on a definition · `P-08` §4.3 lists **source** among a
definition's surviving fields · ⭐ **`INV-9` was withdrawn with NOTHING replacing it** · the
`Closure(𝒦₉)` ground replacement · `Zero` retaining `[RF]` definitions.
**`[DERIVED]`** ⭐⭐ **`K2` already retains the current ground as a field value** · ⭐⭐⭐ **the `n=2`
inference presupposes *every non-current assertion was superseded*, and withdrawal-without-successor
refutes it** · `K4b-ii`'s state space is identical across its two histories · `K2g` is a
reparameterization · representational unification ≠ obligation collapse · *"current"* is a field-value
property, not a primitive.
**`[DER-S:R0]`** `K2`'s independence. **`[DER-S:Q1a]`** `K4c-i`/`K4c-ii`.
**`[CORROBORATION]`** the DDD audit — it forces no concept and supplies none.
**`[OPEN]`** ⭐ the **corroboration arm** of `K4b-ii`'s witness · everything carried from `P-19`.
⛔ **No Lane T construction became corpus evidence; no `[ARCH]` was promoted.**

# 18. ⭐ P-20 VERDICT

| # | question | answer |
|---|---|---|
| **1** | is a current-ground capability **independently derivable**? | ⭐ **NO — as a CELL.** ⭐⭐ **But the current-ground STATE is already retained by `K2`**, so the question was mis-posed |
| **2** | is *"current ground"* a domain obligation or an imposed representation? | ⭐ **REPRESENTATION** — a field-value property; ⛔ not a domain concept |
| **3** | does `K4b-ii` collapse if `K2g` exists? | ⭐⭐⭐ **NO** — its state space is identical across both histories (§5, §8), and the `n=2` inference the collapse needed **fails even for assertions** (§6) |
| **4** | current kernel cardinality | ⭐ **11 — under both hypotheses.** The difference is **purely representational** |
| **5** | boundary | **`R0`** · **`Q1a`** · deletion-impermissibility · `UPDATED ≠ SUPERSEDED` · cross-references exist · record form `Changed(x,a,b)` · reconstructible ≠ obligated · ⭐ **and NO LONGER "grounds lack a current-state cell" — that assumption is retired as false** |
| **6** | any witness invalidated? | ⭐⭐ **YES — `P-18`'s `n=2` derivability claim for `K4a-ii` is `[REFUTED]`.** ⚠️ **That STRENGTHENS the kernel** |
| **7** | any cell deleted? | ⛔ **NO** |
| **8** | any new canonical theory admitted? | ⛔ **NO** |
| **9** | architecture permitted? | ⛔ **NO** |
| **10** | Schema v3 permitted? | ⛔ **NO** |
| **11** | ⭐ **next question** | ⭐⭐⭐ **Are WITHDRAWAL and SUPERSESSION two transitions or one?** `P-08` bundles them as `τ10`, and §6's refutation turns on **withdrawal-without-successor** being a distinct event. **A transition-level question, and it bears on Gate `C`** |

---

```
OUTCOME C — K2g IS LEGITIMATE AS AN INSTANCE OF K2, NOT AS A CELL, AND IT DOES NOT COLLAPSE K4b-ii.
KERNEL REMAINS 11 CELLS — UNDER BOTH HYPOTHESES. THE 10-vs-11 QUESTION DISSOLVES RATHER THAN BEING
DECIDED.

THE ASYMMETRY P-19 POSITED DOES NOT EXIST. P-19 §9 assumed assertions have a current-state
representation via K2 while grounds have none. But a definition's SOURCE is a VALUE ON A DEFINITION
(P-09.2 §4.9), and P-08 §4.3 lists source among a definition's surviving fields — so K2 ALREADY RETAINS
THE CURRENT GROUND. K2g is therefore not a new capability but K2 applied to the source field: a
REPARAMETERIZATION, refused as a cell on IDENTIFIABILITY grounds (§9), not on cardinality grounds.

AND THE COLLAPSE FAILS AT ITS SOURCE, NOT AT ITS TARGET. P-18 claimed that for assertions {p1,p2} plus
current=p2 forces the single topology p1 > p2, making K4a-ii derivable at n=2 — the very inference the
proposed collapse of K4b-ii had to transfer. That inference PRESUPPOSES THAT EVERY NON-CURRENT
ASSERTION WAS SUPERSEDED. It was not: [EMP], INV-9 was withdrawn and NOTHING replaced it. So at n=2
there are TWO topologies — (a) p1 superseded by p2, and (b) p1 withdrawn-with-no-successor while p2 was
independently asserted — and the distinction is REQUIRED by P-08 §5.2's process-reliability ground
("was p1 replaced by a better claim, or simply abandoned?").
  => P-18's n=2 near-miss is [REFUTED]. K4a-ii is INDEPENDENT AT ALL n >= 2. There was never an n=2
  reduction to transfer.

K4b-ii's STATE SPACE, stated explicitly rather than asserted unreconstructible: removing K4b-ii leaves
(identity, current=g2, prior=g1, warrant, g1 retained) — THE SAME OBJECT in both the replacement and
the corroboration history. Single-valuedness of the source field does NOT settle it, because "not the
current citation" is NOT "invalidated". No function of the retained state can differ: IMPOSSIBILITY,
not absence of witness.

SYMMETRIC CIRCULARITY AUDIT — the cleanest result: K4b-ii does NOT remain independent because K2g was
omitted. The current ground IS retained, and K4b-ii's witness survives UNCHANGED. Its independence
depends on no omission.

P-19 §9's claim classified: C — ONLY AN ANALOGY. Never demonstrated, and it fails twice: the antecedent
was already true and produced no collapse, and the consequent rested on an inference that fails even
for assertions. P-19 not rewritten.

ARTIFICIAL STRATIFICATION, both directions: a unified assertion/ground model IS available
representationally, and K4a-ii and K4b-ii still do NOT collapse — P-09.3 §10's criterion (arity
parameterizes, SUBJECT individuates) still applies, because P-17 weakened the subject AXIS while
leaving the OBLIGATIONS intact: Pi-2 is a HISTORY, and no relabelling touches it. REPRESENTATIONAL
UNIFICATION != OBLIGATION COLLAPSE. Conversely, giving all grounds a current-state cell WOULD create an
artificial capability — which is the second reason K2g is refused.

NET EFFECT ON P-19'S FIVE FREEZE QUALIFICATIONS: one is DISCHARGED (K4b-ii's stratification
dependency), one is STRENGTHENED (K4a-ii now independent for n>=2), and ONE IS ADDED rather than hidden
— K4b-ii's witness requires a CORROBORATION arm, and no corpus instance of two simultaneously-standing
grounds is attested [OPEN]. The boundary assumption "grounds lack a current-state cell" is RETIRED AS
FALSE.

FREEZE STATUS: the kernel is safe to freeze as a QUALIFIED RESEARCH RESULT, not as canonical
KnowledgeOS theory — P-19's wording corrected as instructed.

NEXT QUESTION (one): ARE WITHDRAWAL AND SUPERSESSION TWO TRANSITIONS OR ONE? P-08 bundles them as
tau-10, and §6's refutation turns on withdrawal-without-successor being a distinct event. A
transition-level question bearing on Gate C.

NO ARCHITECTURE — NO SCHEMA v3 — 3MC UNTOUCHED — NO CELL ADDED OR DELETED — NO SILENT REPAIR.
```

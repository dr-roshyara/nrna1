# `P-51` — Witness-Set Completeness Audit

**2026-09-08 · Lane T.** After [`68` `P-50`](./68-P50-PERSISTENCE-KERNEL-OBJECT-TYPE-AND-ADEQUACY-AUDIT.md).

> **Single question:** *is the witness set complete — does every loss the corpus exhibits have a cell?*

# 1. Executive verdict

$$\boxed{\begin{array}{c}\mathbf{NO,\ on\ TWO\ counts.} \textbf{ ⭐⭐ And the second is structural, not remediable:}\\[6pt] \boxed{\begin{array}{ll}\textbf{(i) } \mathbf{two\ losses\ remain\ UNKNOWN} \textbf{ inside } \mathcal{T}_{KOS} \textbf{ — and } \mathbf{the\ series\ has\ tracked\ only\ ONE\ of\ them}\\ \textbf{(ii) } \mathcal{T}_{KOS} \textbf{ is } \mathbf{OBSERVATIONAL\ and\ therefore\ OPEN\ BY\ CONSTRUCTION}\end{array}}\end{array}}$$

⭐⭐⭐ **So witness-coverage is a STANDING criterion, re-evaluated as the estate acts — ⛔ not a
one-time proof.** ⭐ **Unlike the minimality case, this openness is one `P-08` *declared*, not one
imported.**

$$|K| = 11 \textbf{ — unchanged, still } \mathbf{[REC]}. \textbf{ ⛔ No cell added, removed or re-scoped.}$$

# 2. ⚠️ The question's universe, fixed before answering

⭐⭐ **`P-50`'s phrasing — *"every loss the corpus exhibits"* — is WIDER than *"every `τ` in
`𝒯_KOS`"*, and the two are not interchangeable.**

| universe | answerable? |
|---|---|
| ⭐ **losses relative to `𝒯_KOS`** | ⭐⭐ **YES — `P-08` §18 classifies exactly 11 of them** |
| ⚠️ **all losses the corpus exhibits** | ⭐⭐⭐ **NO — circular.** `P-08` derived its loss list **from** `𝒯_KOS`; there is no independent enumeration of corpus losses to compare against |

⇒ ⛔ **The wider question is not answered here, and is recorded as unanswerable-as-posed rather than
answered by substitution.** ⭐ **This audit answers the `𝒯_KOS`-relative question.**

⚠️ **Pre-registered: incomplete. Correct — record 9 of 15.** ⭐ **But the *reason* is stronger than I
predicted: I expected one gap; there are two, plus a structural one.**

# 3. The transition set and the loss set — kept apart

⭐⭐ **A distinction `𝒯_KOS`'s own table invites and does not make:** its *"witnessed"* column means
***"did this transition occur in the estate?"*** — ⛔ **not *"does it have a forcing witness for a
cell?"***

| | `𝒯_KOS` occurrence | forcing witnesses |
|---|---|---|
| **count** | ⭐ **10 item-level** *(`τ1`–`τ10`)* + `τ11` representation-level | ⭐ **7** *(`W1`–`W7`)* |
| **9 of 10 `[EMP]`** | only `τ4` is `[OPEN]` | — |

⛔ **The mapping is not one-to-one, and `P-08` never claimed it was** — `W2` and `W7` both arise from
`τ2`; `W5` and `W6` both force `K5`.

# 4. ⭐⭐⭐ The real evidence — `P-08` §18's loss classification

| loss | class | cell |
|---|---|---|
| two candidates collapse into one identity | ⭐ **SEMANTICALLY FATAL** | `K1` |
| a superseded assertion is deleted | **FATAL** | `K4` |
| corpus state lost for a cited measurement | **FATAL** | `K5` |
| an absent endpoint recorded as null | ⭐ **FATAL** | `K2` |
| prior association lost on re-disposition | **FATAL** | `K3` |
| a sole external ground becomes unresolvable | ⚠️ **RECOVERABLE with downgrade** | `K5`'s honest downgrade |
| occurrence site list lost | ✅ **ACCEPTABLE** | ⛔ none — regenerable |
| an address changes | ✅ **ACCEPTABLE** | ⛔ none — *"56 renames broke nothing that mattered"* |
| intermediate monotone statuses lost | ✅ **ACCEPTABLE** | ⛔ none — §17 redundancy |
| ⚠️⚠️ **an artifact's prior versions lost** | ⭐⭐⭐ **UNKNOWN** | ***"artifact lifecycle is `[OPEN]`"*** |
| ⚠️ **glyph-membership history lost** | ⭐⭐ **UNKNOWN** | ***"depends on `Q3`; ⛔ not closed"*** |

$$\boxed{\textbf{⭐⭐ 11 losses: 5 fatal, 1 recoverable, 3 acceptable, } \mathbf{2\ UNKNOWN.} \textbf{ ⇒ } \mathbf{the\ witness\ set\ is\ INCOMPLETE\ by\ P\text{-}08\text{'s}\ own\ table.}}$$

# 5. ⭐⭐⭐ A tracking failure, measured

| open loss | artifacts (19–68) that mention it |
|---|---:|
| **glyph-membership** *(`τ4` / `Q3`)* | ⭐ **25** |
| ⭐⭐⭐ **artifact lifecycle / prior versions** | ⭐⭐ **0** |

$$\boxed{\begin{array}{c}\textbf{⭐⭐ Of } P\text{-}08\textbf{'s two } \mathbf{UNKNOWN} \textbf{ losses, one was carried faithfully through fifty audits and } \mathbf{the\ other\ was\ silently\ dropped.}\\ \boxed{\textbf{⛔ No audit — including this lane's own } \textit{carried opens} \textbf{ lists — has mentioned artifact lifecycle since } P\text{-}08.}\end{array}}$$

⭐ **This is a process finding, not a mathematical one:** the carried-opens list is maintained by hand
and **lost an item without anything detecting it.** ⚠️ **Recorded here and restored to the carried
opens in §9.**

# 6. ⭐⭐ The structural result — `𝒯_KOS` is open by construction

`P-08` §1: *"The transition set `𝒯_KOS` — **what the estate is actually observed to do**."*

$$\boxed{\begin{array}{c}\textbf{⭐⭐⭐ An } \mathbf{OBSERVATIONAL} \textbf{ set can only grow. A transition the estate has not yet performed is not in it,}\\ \textbf{and would arrive with its own possible loss.}\\[4pt] \boxed{\textbf{⇒ Witness-set completeness is } \mathbf{not\ merely\ unachieved\ —\ it\ is\ NOT\ ACHIEVABLE\ as\ a\ one\text{-}time\ result.}}\end{array}}$$

⭐⭐ **And this is `P-08`'s own declaration, ⛔ not an imported closure requirement** — which
distinguishes it sharply from the minimality case, where the framework was imported *(`P-48`, `P-49`)*.

⚠️ **`τ11` corroborates the mechanism:** it was **added after `P-08`** *(`P-09.1`, as a handover)* —
⭐⭐ **direct evidence that `𝒯_KOS` grows.**

# 7. What this means for the two R-native criteria

| criterion *(`P-50`)* | status |
|---|---|
| ⭐ **witness-grounding** — *does every cell have a witness?* | ⭐⭐ **SATISFIED** — `P-47`: 9 minimal pairs + `K1`'s presupposition + `K2`'s discharged conditional |
| ⭐⭐ **witness-coverage** — *does every witnessed loss have a cell?* | ⭐⭐⭐ **NOT SATISFIED, and not one-time satisfiable** — two `UNKNOWN`s now, and an open transition set always |

$$\boxed{\textbf{⭐⭐ The two criteria have } \mathbf{different\ logical\ characters}\textbf{: grounding is } \mathbf{closable} \textbf{ (finite cells); coverage is } \mathbf{standing} \textbf{ (open transitions).}}$$

⭐⭐⭐ **That asymmetry was not visible until the completeness question was asked, and it is the audit's
main structural result.**

# 8. What resolving the two `UNKNOWN`s would do

| open loss | if it is a real loss | if it is acceptable |
|---|---|---|
| **glyph-membership history** | ⚠️ bears on `K1` — `P-07` §4: *"whether `ℐ`/`𝓘` index one candidate or two is undecided"* ⇒ **could force a cell or extend `K1`** | 11 confirmed |
| ⭐ **artifact prior versions** | ⚠️ **entirely unexamined** — could bear on `K3a` *(prior value)* or force a new cell for external-artifact versioning | 11 confirmed |

⛔ **Neither is resolved here, and no cell is proposed.** ⭐ **Both are recorded as what they are:
`UNKNOWN` since `P-08`, one tracked and one not.**

# 9. Status register
**`[EMP]`** ⭐⭐ `P-08` §18's 11-row loss table with **two `UNKNOWN`** rows · `𝒯_KOS` = *"what the estate
is actually observed to do"* · `τ4` `[OPEN]` pending `Q3` · **artifact lifecycle mentioned in 0 of 50
subsequent artifacts** vs `τ4` in **25** · `τ11` added after `P-08`.
**`[DERIVED]`** ⭐⭐⭐ **witness-coverage is a standing criterion, not a closable one** · the
grounding/coverage asymmetry · the wider *"all corpus losses"* question is circular as posed.
**`[QUALIFIED]`** ⭐ `P-50`'s framing *"every loss the corpus exhibits"* — **answerable only relative to
`𝒯_KOS`**.
**`[OPEN]`** ⭐⭐ **glyph-membership history (`τ4`/`Q3`)** · ⭐⭐⭐ **artifact prior versions / artifact
lifecycle — RESTORED to the carried opens after 50 audits' absence** · `K4`'s absorption of three
sub-classes *(`P-09.1`: *"may be GENERAL — or UNDER-SPECIFIED and hiding three obligations"*)* ·
everything carried from `P-29`–`P-50` · ⛔ **well-foundedness / terminality / acyclicity — DEFERRED.**
**Governance:** `|K| = 11` remains **`[REC]`**.

# 10. ⛔ Backlog — no new item
⭐ **The dropped open item is re-captured by this audit itself**, and a ticket for *"a hand-maintained
list lost one entry"* would be thin. ⭐⭐ **`EKS-13`** *(cross-lane dependency)* and **`EKS-14`**
*(naming/attribution)* remain the live operational exposures; ⛔ **neither is duplicated here.**

### 11. ONE NEXT UNRESOLVED QUESTION

$$\boxed{\textbf{Is } \mathbf{artifact\ lifecycle} \textbf{ inside R's scope at all — or is it, like verification and policy before it, another estate's concern?}}$$

⭐⭐ **`P-08` left it `UNKNOWN` and no audit has touched it since.** ⭐ **`τ5` is *"implementation change
in an external artifact — git over `research/**`"*** — ⭐⭐⭐ **and this series has twice found that an
apparently-R obligation belonged elsewhere: verification to Assurance (`P-32`) and policy to governance
(`P-28`). ⚠️ External artifacts under git may be the third — which would close the loss as
out-of-scope rather than force a cell.**

```
NO — THE WITNESS SET IS INCOMPLETE, ON TWO COUNTS, AND THE SECOND IS STRUCTURAL.
|K| = 11 UNCHANGED, STILL [REC]. NO CELL ADDED, REMOVED OR RE-SCOPED.

FIRST, THE QUESTION'S UNIVERSE HAD TO BE FIXED. P-50's phrasing — "every loss the corpus exhibits" — is
wider than "every tau in T_KOS", and the wider version is CIRCULAR: P-08 derived its loss list FROM
T_KOS, so there is no independent enumeration to compare against. Recorded as unanswerable-as-posed
rather than answered by substitution.

RELATIVE TO T_KOS, P-08 §18 CLASSIFIES ELEVEN LOSSES: five fatal, one recoverable-with-downgrade, three
acceptable — and TWO UNKNOWN. "An artifact's prior versions lost — artifact lifecycle is [OPEN]." And
"glyph-membership history lost — depends on Q3; not closed." So the witness set is incomplete by P-08's
own table.

AND A TRACKING FAILURE IS MEASURABLE. Of those two UNKNOWNs, glyph-membership appears in 25 of the
subsequent 50 artifacts; ARTIFACT LIFECYCLE APPEARS IN ZERO. One open loss was carried faithfully for
fifty audits and the other was silently dropped — including from this lane's own "carried opens" lists.
That is a process finding, not a mathematical one, and the item is restored here.

SECOND AND STRUCTURALLY: T_KOS is "what the estate is actually OBSERVED to do". An observational set can
only grow, so a transition the estate has not yet performed is not in it and would arrive with its own
possible loss. Witness-set completeness is therefore NOT MERELY UNACHIEVED BUT NOT ACHIEVABLE as a
one-time result. Unlike the minimality case, this openness is one P-08 DECLARED rather than one
imported — and tau11's later addition, after P-08, is direct evidence that the set grows.

THE TWO R-NATIVE CRITERIA TURN OUT TO HAVE DIFFERENT LOGICAL CHARACTERS: witness-GROUNDING is SATISFIED
and closable, because the cells are finite and P-47 grounded all eleven; witness-COVERAGE is NOT
satisfied and is STANDING, because the transitions are open. That asymmetry was invisible until the
completeness question was asked, and it is this audit's main structural result.

Pre-registered "incomplete" — correct, record 9 of 15 — though the reason is stronger than predicted: I
expected one gap and found two plus a structural one.

NO NEW BACKLOG ITEM: the dropped open item is re-captured by this audit, and EKS-13 and EKS-14 remain
the live operational exposures.

ONE NEXT UNRESOLVED QUESTION: Is ARTIFACT LIFECYCLE inside R's scope at all — or is it, like
  verification and policy before it, another estate's concern? tau5 is "implementation change in an
  external artifact — git over research/**", and this series has twice found an apparently-R obligation
  belonged elsewhere: verification to Assurance in P-32, policy to governance in P-28. External
  artifacts under git may be the third, which would close the loss as out-of-scope rather than force a
  cell.

NO CANONICALIZATION — NO SCHEMA v3 — NO ARCHITECTURE — NO GOVERNANCE CHANGE — NO three_model_convergence
INSPECTION — WELL-FOUNDEDNESS STILL DEFERRED.
```

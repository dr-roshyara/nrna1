# `P-52` — Kernel Minimality Corpus Archaeology & Artifact-Lifecycle Scope Audit

**2026-09-08 · Lane T.** After [`69` `P-51`](./69-P51-WITNESS-SET-COMPLETENESS-AUDIT.md).

# 1. Executive summary — two separate verdicts

$$\boxed{\begin{array}{ll}\textbf{Minimality archaeology} & \mathbf{B} \textbf{ — substantial material exists, } \mathbf{far\ beyond\ } [DEF\text{-}33]\textbf{; applicability to R not yet established}\\[4pt] \textbf{Artifact lifecycle} & \mathbf{B} \textbf{ — } \mathbf{OUTSIDE} \textbf{ R's evidenced scope, on the corpus's own words}\end{array}}$$

⭐⭐⭐ **The commissioner's instinct was right a third time. A complete capability-level minimality
framework exists, dated 2026-09-04 — THREE DAYS BEFORE `P-07`/`P-08` — and this lane never consulted
it:**

$$\boxed{\begin{array}{c}\textbf{FROZEN CORE MATHEMATICAL STATEMENT}\\[4pt] \mathit{MinKer}(C_{KOS}) \;=\; \mathrm{Min}_{\preceq_{sem}}\{\,K \in K_{adm} \;\mid\; K \models C_{KOS}\,\}\\[4pt] \textbf{"Kernel Minimality is defined } \mathbf{SEMANTICALLY,\ not\ syntactically.} \textbf{"}\\ \textbf{"} \mathbf{Operator\ count,\ code\ size,\ storage\ size,\ or\ computational\ complexity\ do\ NOT\ establish\ epistemic\ minimality.} \textbf{"}\end{array}}$$

⭐⭐ **So `P-49` spent an audit establishing that `Complexity` is undefined — while the corpus had
already ruled complexity IRRELEVANT to epistemic minimality.** ⛔ **`P-49` was chasing the wrong
criterion.**

$$|K| = 11 \textbf{ — unchanged, still } \mathbf{[REC]}. \textbf{ ⛔ No cell added, removed, merged or re-scoped.}$$

# 2. ⚠️ Concessions and method, declared

**(a)** ⭐ **`P-51`'s *"the last adequacy question R can answer"* is `[REFUTED]`** — it presupposed
exactly what this audit tests, and the presupposition was false.
**(b)** ⭐⭐⭐ **A pattern, named:** three consecutive audits were materially corrected by commissioner
pointers to folders this lane had searched **shallowly** — `P-48` *(two pointers)*, `P-51` *(a 50-audit
tracking loss)*, and now `P-52`. ⚠️ **The defect is my search method, not the corpus.**
**(c) Method:** a **hit-map first, deep-read second** — ⭐ because `P-48`'s decisive material was found
by reading primaries, not by counting hits. §7 reports both.

⛔ **No `three_model_convergence/` · no cell change · no canonicalization · no Schema v3 · no
architecture · `EKS-12` not reopened · `[DEF-33]` not imported on the strength of the word
*"minimality"*.**
⚠️ **Pre-registered `B`/`B`. Both correct — record 11 of 17.**

# 3. ⭐⭐⭐ The 2026-09-04 minimality track — what this lane missed

| artifact | content |
|---|---|
| ⭐⭐ `…005346_are-we-ready-to-freeze-the-kernel-close-but-not-proved` | *"Which **epistemic capabilities** cannot be removed or derived without c[ollapse]?"* |
| ⭐⭐⭐ `…014317_kernel-minimality-as-a-relative-conditional-proof-not-universal` | *"a kernel is minimal **iff no strictly smaller semantically adequate kernel exists**"* + ⭐ **Lemma 1 — Capability Irreducibility** |
| ⭐⭐ `…014642_unified-formal-evaluation-kernel-minimality-semantic-capability-ordering` | `K^*` minimal iff no strictly semantically weaker admissible kernel |
| ⭐⭐⭐ `…014807_substantial-agreement-with-corrections-minimality-and-ratification` | ⭐ **the FROZEN CORE STATEMENT and the FORMAL BOUNDARY STATEMENT** |

$$\boxed{\begin{array}{c}\textbf{⭐⭐ This is a } \mathbf{CAPABILITY\text{-}LEVEL,\ SEMANTIC} \textbf{ minimality framework with an ordering } (\preceq_{sem})\textbf{, an admissible class } (K_{adm})\textbf{,}\\ \textbf{an adequacy constraint } (\models C_{KOS}) \textbf{ and an irreducibility lemma. ⛔ It is } \mathbf{not} \textbf{ the operator-count framework } P\text{-}48/P\text{-}49 \textbf{ analysed.}\end{array}}$$

⚠️ **Its own status line reads `Named Operator Set — NOT YET PROVED` and `Governance Ratification —
OPEN`** ⇒ ⭐ **frozen as a mathematical statement, ⛔ not ratified.**

# 4. Source-strand separation — the word *"kernel"* is still not transfer

| strand | kernel object | minimality criterion | applies to R? |
|---|---|---|---|
| ⭐ **`MinKer(C_KOS)`** *(09-04)* | ⭐⭐ **the epistemic kernel — *"smallest domain-independent bounded context responsible for identity, lifecycle, provenance, and invariant-preserving evolution of epistemic states"*** | ⭐⭐ **semantic ordering `⪯_sem`** | ⭐⭐⭐ **`[OPEN]` — see §5** |
| `[DEF-33]` / Theorem 10 *(09-02)* | operator kernel, `13`/`8` | `argmin Complexity` | ⛔ **`[ANALOGY ONLY]`** |
| `K-1`/`K-2` *(08-23/24)* | DDD aggregate kernel | *"smallest authoritative boundary"* — ⚠️ **`Unproven`** | ⛔ **frozen, not consumed** |
| `KERNEL₅` | evidence/proposition primitives | — | ⛔ analogy |
| ⭐ **R persistence kernel** | Lane T's assembly | ⛔ **none constructed** | — |

⭐⭐ **Five kernel concepts. The 09-04 one is by far the closest to R's object** — capabilities,
identity, provenance, evolution — ⚠️ **but its subject is *epistemic states*, while R's is the
research estate's own artifacts (`𝒯_KOS`: candidates, definitions, associations, 56 renames).**

# 5. Reassessment of `P-48`–`P-50`

| | claim | verdict |
|---|---|---|
| **1** | *"the persistence kernel is purely a Lane-T assembly"* | ⭐⭐ **`[QUALIFIED]`** — true **by name** *(`persistence kernel` = 0 files)*, ⛔ **false in substance**: a corpus kernel covering identity/lifecycle/provenance/evolution exists |
| **2** | *"minimality cannot be a corpus-native question"* | ⭐⭐⭐ **`[REFUTED]`** — it is corpus-native for the **epistemic** kernel, with a formal definition |
| **3** | *"no corpus-native minimality criterion exists"* | ⭐⭐⭐ **`[REFUTED]`** |
| **4** | *"`[DEF-33]` is merely an unrelated operator-kernel framework"* | ⭐ **`[CONFIRMED]`** — ⭐⭐ **and superseded within the corpus itself by the semantic framework, which explicitly rejects count-based minimality** |
| **5** | *"the only R-native adequacy criteria are witness-coverage and witness-grounding"* | ⭐⭐ **`[QUALIFIED]`** — they remain the only **R-native** ones, ⛔ but they are **no longer the only candidates**: `⪯_sem` + `C_KOS` is a live alternative pending a transfer test |

⭐⭐⭐ **`P-49`'s central finding is the casualty:** it identified undefined `Complexity` as *"the precise
blocker"*, ⛔ **while the corpus had already declared complexity irrelevant.** ⚠️ **`P-49`'s verdict `C`
survives; its diagnosis does not.**

# 6. Part C — eight propositions, kept apart

⭐ **1** a kernel exists *(corpus: yes, epistemic)* · **2** the constructed kernel has 11 cells
*(Lane T)* · **3** each cell has an irreducibility argument *(`P-47`)* · **4** sufficient for identified
obligations *(`P-51`: **no** — two `UNKNOWN`s)* · **5** set-inclusion minimal *(`P-47`, within one
representation)* · **6** minimum cardinality *(`[OPEN]`)* · **7** no equivalent representation has fewer
*(`[OPEN]` — and now testable via `⪯_sem`)* · **8** canonical *(⛔ **no** — `[REC]`)*.
⛔ **None collapsed.**

# 7. Search-population report *(Part G)*

| | `kernel/` | `math_ideas/` |
|---|---:|---:|
| files | **163** | **402** |
| `minimality` | 5 | ⭐ **121** |
| `adequacy` | 21 | ⭐ **167** |
| `irreducib` | 10 | 86 |
| `semantic adequacy` | 0 | 11 |
| `transition completeness` | 0 | 4 |
| ⭐ **substantively relevant, deep-read** | ⭐ **2** | ⭐⭐ **6** |

⚠️ **The five `[DEF-33]` constraints are used but NEVER defined** — `SemanticAdequacy`,
`CapabilityClosure`, `TransitionCompleteness`, `InvariantPreservation`: **zero definitional forms
each.** ⭐ **That negative from `P-49` stands.** ⛔ **Frequency was not treated as ontology; duplicates
were not counted as corroboration.**

# 8. Part D/E/F — artifact lifecycle scope

⭐⭐⭐ **The corpus's own boundary statement decides it:**

$$\boxed{\textbf{"identity, lifecycle, provenance, and invariant-preserving evolution } \mathbf{of\ EPISTEMIC\ STATES}\textbf{"}}$$

⭐⭐ ***Lifecycle* IS a kernel responsibility — but of epistemic states, ⛔ not of external artifacts.**

| Part E level | holds for git artifacts? |
|---|---|
| 1 R **observes** the artifact | ⭐ **yes** — `τ5`, git over `research/**` |
| 2 R **records** it | ⭐ yes |
| 3 R has **provenance** concerning it | ⭐ yes |
| 4 R makes a **persistence claim** about it | ⚠️ **`[UNWITNESSED]`** |
| ⭐ **5 R is obligated to preserve artifact history** | ⭐⭐⭐ **NO — not established** |
| **6 another estate owns it** | ⭐⭐ **git/VCS — an external mechanism, `[EMP]` by `τ5`'s own wording *"an EXTERNAL artifact"*** |

⭐ **Part F cases:** **A** *(v1 deleted)* — affects nothing in `K1`–`K11` unless **D** holds · **B**
*(externally recoverable)* — ⭐ **exactly `K5`'s territory: the ground stays checkable** · **C** *(R
cares only about its own assertion)* — ⭐⭐ **the normal case; no cell touched** · ⭐⭐⭐ **D** *(the
artifact holds the ONLY witness for an R obligation)* — **this is `K5`, already**: *"a sole external
ground becomes unresolvable — RECOVERABLE with epistemic downgrade"*.

$$\boxed{\begin{array}{c}\textbf{⭐⭐ Verdict } \mathbf{B}\textbf{: artifact lifecycle is } \mathbf{OUTSIDE} \textbf{ R's evidenced scope. The one case that matters } (\mathbf{D}) \textbf{ is } \mathbf{already\ carried\ by\ } K5.\\ \boxed{\textbf{⇒ } P\text{-}08\textbf{'s second } \mathbf{UNKNOWN} \textbf{ closes as } \mathbf{OUT\text{-}OF\text{-}SCOPE}\textbf{, ⛔ not as a new cell — the third time an apparently-R obligation belonged elsewhere.}}\end{array}}$$

⭐ **Precedents: verification → Assurance *(`P-32`)*, policy → governance *(`P-28`)*, artifacts →
VCS/external *(here)*.** ⛔ **No bounded context manufactured, and git is not treated as KnowledgeOS
domain theory merely because `τ5` names it.**

# 9. Effect on `K1`–`K11`
⛔ **None.** ⭐ **One of `P-51`'s two `UNKNOWN` losses is now closed as out-of-scope; `τ4`/glyph-membership
remains `[OPEN]`.** ⭐⭐ **So witness-coverage improves from *two gaps* to *one*** — ⚠️ **while remaining
a STANDING criterion, since `𝒯_KOS` is still open by construction *(`P-51` §6)*.**

# 10. Status register
**`[EMP]`** ⭐⭐ the **FROZEN CORE STATEMENT** and `MinKer(C_KOS)` · *"operator count … do not establish
epistemic minimality"* · *"identity, lifecycle, provenance … **of epistemic states**"* · *"a kernel is
minimal iff no strictly smaller semantically adequate kernel exists"* · **Lemma 1 Capability
Irreducibility** · `Named Operator Set — NOT YET PROVED`, `Governance Ratification — OPEN` · `τ5` names
an **external** artifact · the five `[DEF-33]` constraints have **0** definitional forms.
**`[DERIVED]`** ⭐⭐⭐ **`P-49`'s diagnosis was wrong-criterion** · artifact lifecycle is out-of-scope ·
Part F case **D** is `K5`.
**`[REFUTED]`** ⭐⭐ **`P-50` claims 2 and 3** · **`P-51`'s *"last adequacy question"***.
**`[QUALIFIED]`** ⭐ **`P-50` claim 1** *(true by name, false in substance)* · **`P-50` claim 5** ·
`P-49`'s verdict *(survives; diagnosis does not)*.
**`[ANALOGY ONLY]`** `[DEF-33]` · `K-1`/`K-2` · `KERNEL₅`.
**`[OPEN]`** ⭐⭐⭐ **whether `MinKer(C_KOS)` transfers to R** · `τ4`/`Q3` · `⪯_sem` and `C_KOS`'s own
definitions · everything carried from `P-29`–`P-51` · ⛔ **well-foundedness / terminality / acyclicity —
DEFERRED.**
**Governance:** `|K| = 11` remains **`[REC]`**; the 09-04 framework is itself **unratified**.

# 11. Backlog decision
⛔ **No new ticket** — the findings are research corrections, and Part I forbids a ticket for those.
⭐⭐ **`EKS-14` re-examined adversarially, as Part I requires, and it SURVIVES — with a correction:** its
premise *"no corpus counterpart"* is **too strong** *(§5 claim 1)*. ⭐ **The exposure is unchanged and
arguably sharper — there is now a corpus *"Kernel"* with overlapping content, so a reader could conflate
Lane T's assembly with it.** ⚠️ **Correction appended additively; the ticket is not withdrawn.**

### 12. ONE NEXT UNRESOLVED QUESTION

$$\boxed{\textbf{Does } \mathit{MinKer}(C_{KOS}) \textbf{ transfer to R — is Lane T's assembly a member of } K_{adm}\textbf{, and does it satisfy } C_{KOS}?}$$

⭐⭐ **This is the transfer test `P-52` deliberately did not run.** ⭐⭐⭐ **It is the first time the
series has had a capability-level, semantically-defined minimality criterion to test against — and if
the transfer holds, `|K| = 11` becomes evaluable under `⪯_sem` rather than blocked by an undefined cost
function.**

```
TWO VERDICTS, BOTH B. MINIMALITY ARCHAEOLOGY: substantial corpus material exists, far beyond DEF-33;
applicability to R not yet established. ARTIFACT LIFECYCLE: OUTSIDE R's evidenced scope, on the corpus's
own words. |K| = 11 unchanged and still [REC].

THE COMMISSIONER'S INSTINCT WAS RIGHT A THIRD TIME. A complete capability-level minimality framework
exists, dated 2026-09-04 — THREE DAYS BEFORE P-07/P-08 — and this lane never consulted it:

  FROZEN CORE MATHEMATICAL STATEMENT
  MinKer(C_KOS) = Min_{<=sem} { K in K_adm | K |= C_KOS }
  "Kernel Minimality is defined SEMANTICALLY, not syntactically."
  "Operator count, code size, storage size, or computational complexity DO NOT establish epistemic
   minimality."
  "The Kernel is the smallest domain-independent bounded context responsible for identity, lifecycle,
   provenance, and invariant-preserving evolution of EPISTEMIC STATES."

So P-49 spent an entire audit establishing that Complexity is undefined, while the corpus had already
declared complexity IRRELEVANT to epistemic minimality. P-49's verdict C survives; its DIAGNOSIS does
not. P-50's claims 2 and 3 — that minimality cannot be corpus-native and that no corpus criterion
exists — are REFUTED. Its claim 1 is QUALIFIED: "purely a Lane-T assembly" is true BY NAME, since
"persistence kernel" returns zero files, but false IN SUBSTANCE, because a corpus kernel covering
identity, lifecycle, provenance and evolution does exist. And P-51's "last adequacy question R can
answer" is REFUTED, since it presupposed exactly what this audit tested.

A PATTERN IS NAMED, AND IT IS MINE: three consecutive audits were materially corrected by pointers to
folders this lane had already searched — shallowly. P-48 by two pointers, P-51 by a fifty-audit
tracking loss, and now P-52. The defect is the search method, not the corpus.

ARTIFACT LIFECYCLE IS DECIDED BY THE CORPUS'S OWN BOUNDARY STATEMENT: lifecycle is a kernel
responsibility, but of EPISTEMIC STATES, not of external artifacts. tau5's own wording is "implementation
change in an EXTERNAL artifact". Of the four Part F cases, the only one that matters — the artifact
holding the sole witness for an R obligation — is already K5's "a sole external ground becomes
unresolvable, RECOVERABLE with epistemic downgrade". So P-08's second UNKNOWN closes as OUT-OF-SCOPE
rather than as a new cell: the third time an apparently-R obligation belonged elsewhere, after
verification to Assurance and policy to governance.

Witness-coverage therefore improves from two gaps to one — tau4/glyph-membership remains open — while
still being a STANDING criterion, since T_KOS is open by construction.

EKS-14 was re-examined adversarially as Part I requires and SURVIVES, with a correction: its premise "no
corpus counterpart" is too strong. The exposure is arguably sharper now, since a corpus "Kernel" with
overlapping content exists and could be conflated with Lane T's assembly.

Pre-registered B and B. Both correct — record 11 of 17.

ONE NEXT UNRESOLVED QUESTION: Does MinKer(C_KOS) transfer to R — is Lane T's assembly a member of K_adm,
  and does it satisfy C_KOS? This is the transfer test P-52 deliberately did not run, and it is the
  first time the series has had a capability-level, semantically-defined minimality criterion to test
  against.

NO CELL CHANGE — NO CANONICALIZATION — NO SCHEMA v3 — NO ARCHITECTURE — NO GOVERNANCE CHANGE — NO
three_model_convergence INSPECTION.
```

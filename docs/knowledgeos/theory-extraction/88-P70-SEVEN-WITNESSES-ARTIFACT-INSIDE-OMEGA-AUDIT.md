# `P-70` — Do the Seven Witnesses Name Artifacts **Inside** `Ω`?

**2026-09-09 · Lane T.** After [`87` `P-69`](./87-P69-WHAT-POSITIVE-WITNESS-DOES-K3-NAME.md).

# 1. Executive verdict

$$\boxed{\mathbf{5\ INSIDE\ \cdot\ 2\ OUTSIDE.} \textbf{ ⭐⭐⭐ } P\text{-}69\textbf{'s worst case — } \mathit{"none"} \textbf{ — is } \mathbf{REFUTED.}}$$

⭐ **The kernel is *not* grounded entirely in a channel outside the estate.** ⛔ **But three of the five
carry qualifications sharp enough to matter, and one of them is structural:**

$$\boxed{\begin{array}{c}\mathbf{W4\textbf{'s own basis is }[EMP]\ \textit{"from this lane's own conduct, not from a rule."}}\\[3pt] \textbf{⇒ ⭐⭐ } \mathbf{[EMP]\ about\ the\ LANE,\ not\ about\ the\ estate's\ independent\ register.}\end{array}}$$

$$|K| = 11 \textbf{ — unchanged, } \mathbf{[REC]}. \textbf{ ⛔ No cell restated, no witness reassigned.}$$

# 2. ⚠️ Qualification declared before the test

⭐⭐ **`Ω` for the estate has never been defined** — `P-68` carries it as `[OPEN]`. ⛔ **So the test cannot
be run against the estate's `Ω`; it is run against a stipulated one, and every result below is relative
to it.** The weakest defensible choice, stated so it can be disputed:

$$\Omega_{\text{stip}}(w) = \textbf{what a reader of the estate's committed artifacts can see at time } w$$

⭐ **Consequences of the choice, both directions:** it is **generous** — it admits Lane T's own
documents, which is why five witnesses pass; ⛔ **and it is not the `Ω` of `T-K1`'s theorem**, so nothing
here is an identifiability *proof*. ⚠️ **A narrower `Ω` — the estate's register alone, excluding this
lane's prose — would move `W1` and `W4` to the outside.** That sensitivity is the audit's most fragile
point and is stated rather than buried.

⛔ **No `three_model_convergence` · no `|K|` change · no cell restated · no witness reassigned · no
minimality · no `K3` reformulation · no external citation.**
⚠️ **Pre-registered: 1 inside (`W4` only).** ⭐⭐ **Wrong — five pass.** Record 22 of 33.

# 3. The evidence base

⭐⭐⭐ **`P-08` §21's settled `[EMP]` block is the answer to all seven at once**, and it had never been
read as such. Its eleven items, verbatim in substance:

*refutation reason recorded · `Σ D-05`'s **only** source is `code@kosmodel.py:Sigma` (`F-4`) · `C-022`
carries a warrant, `G-67` carries none · `FR-001` plays two provenance roles at once · `MD-018`'s status
chain is ordered · `MD-017`'s `unresolved_equivalence` is the default · **an association exists with its
candidate endpoint absent** · **this session withdrew `INV-9`, the `(Ω,𝓕,P)` zero and "`Θ` never raised"
— every one WITHDRAWN, none deleted** · `F-4`/`F-5` stand as recorded, unrepaired defects · ⭐⭐ **a
glyph-literal pattern over a LaTeX corpus produced a false zero** · occurrences carry no individual
state in 8 of 8 stress records.*

⭐ Control: `candidate` = **77** files in the lane. Absolute paths. Both `EKS-21` directions applied.

# 4. ⭐⭐ The seven, one row each

| | witness | required datum | artifact in `Ω_stip` | verdict |
|---|---|---|---|---|
| **`W1`** | two candidates collapse into one identity | one bit per pair, `SameId` — **an adjudication, not a computation** | ⭐⭐ **`28-PROPOSAL-Q1-IDENTITY-RETIREMENT` + `29-INTAKE-002` exist as documents** | ⭐ **INSIDE** — ⚠️ **only for pairs actually adjudicated;** `Q3`: *"the estate cannot always tell"* *(`P-66`'s totality failure)* |
| **`W2`** | re-disposition appears as deletion + creation | prior association value + warrant | ⛔ **none** — `P-69`: routes 1, 2, 4 absent, route 3's uniqueness refuted | ⛔⭐⭐⭐ **OUTSIDE** |
| **`W3`** | a `[RF]` candidate reads as never live | current status **with its warrant** | ⭐⭐ ***"refutation reason recorded"*** — `[EMP]` | ⭐⭐ **INSIDE** |
| **`W4`** | supersession destroys the historical claim | superseded text + supersession relation | ⭐⭐⭐ ***"every one WITHDRAWN, none deleted"*** — the texts persist | ⭐⭐ **INSIDE** — ⚠️ **basis is *"this lane's own conduct, not from a rule"*** |
| **`W5`** | a source becomes unresolvable | enough cited content, **or an honest grounding downgrade** | ⭐ **`Σ D-05`'s sole source recorded as `F-4`** | ⚠️ **INSIDE — as a recorded DEFECT.** ⛔ The *content* is not; ⭐ but `W5`'s datum **is** *"or an honest downgrade"*, and the downgrade **is** recorded |
| **`W6`** | a measurement cannot be reconstructed | **the corpus-state identifier** | ⛔ **absent** — `P-08`: ***"this is the one remaining gap"***; `O-P08-2` live | ⛔⭐⭐ **OUTSIDE** |
| **`W7`** | an absent endpoint recorded as null | *awaiting adjudication* vs *adjudicated to none* | ⭐⭐ ***"an association exists with its candidate endpoint absent"*** — `[EMP]`, directly observable | ⭐⭐ **INSIDE** |

$$\boxed{\mathbf{W1\ \cdot\ W3\ \cdot\ W4\ \cdot\ W5\ \cdot\ W7\ INSIDE\quad\quad W2\ \cdot\ W6\ OUTSIDE}}$$

# 5. ⭐⭐⭐ The structural qualification, and it is the audit's real finding

`W4` is the strongest of the five — the withdrawn texts demonstrably persist. ⛔ **And `P-08` labels its
own basis:** *"`[EMP]` **from this lane's own conduct, not from a rule**."*

$$\boxed{\begin{array}{c}\textbf{⭐⭐⭐ So } W4 \textbf{ witnesses that } \mathbf{THIS\ LANE\ retains\ withdrawn\ assertions} \textbf{ — not that } \mathbf{THE\ ESTATE\ does.}\\[3pt] \textbf{⇒ } \mathbf{[EMP]\ about\ the\ observer,\ not\ about\ the\ observed.}\end{array}}$$

⭐⭐ **`W1` shares the shape**: the adjudication artifacts are *this lane's proposals and intakes*. ⛔ **So
the two strongest *inside* results are inside because the lane's own documents are inside `Ω_stip`.**
⚠️ **Under the narrower `Ω` of §2 both move out**, leaving `W3`, `W5` and `W7` — ⭐ **which are the three
that rest on the register itself.**

⭐ **That is the most defensible reading available:** `3` of `7` are witnessed by the estate's register
independently of the lane; `2` more by the lane's own conduct; `2` are outside altogether.

# 6. ⚠️⭐⭐⭐ A fourteenth miss — and it is against my own backlog ticket

Item 10 of `P-08` §21: ***"a glyph-literal pattern over a LaTeX corpus produced a false zero."***

⭐⭐⭐ **That is `EKS-21`'s failure mode, witnessed and recorded on 2026-09-07** — **two days before I
filed `EKS-21` on 09-09 as a new discovery**, and in a document this lane wrote.

$$\boxed{\textbf{⛔ } \mathbf{EKS\text{-}21\ was\ filed\ as\ novel\ against\ a\ witness\ already\ in\ its\ own\ lane's\ primary.}}$$

⭐ **It does not invalidate the ticket** — the ticket's point is that nothing *catches* the failure, and
three subsequent recurrences *(`P-64`, `P-65`, `P-67`)* prove that. ⛔ **But its provenance was wrong,
and `EKS-21` is corrected accordingly** *(§8)*. ⭐⭐ **And `P-08` §21 also names *"lesson 11"* of this
series — LaTeX escaping hiding normative language — meaning the lane knew the mechanism and still
repeated it four times.**

# 7. What this settles for `P-08` §19

⛔ **`P-69`'s feared conclusion is refuted:** it is **not** the case that every `[EMP]` in §19 is `[EMP]`
about the world and `[UNWITNESSED]` about the estate. ⭐⭐ **Five of seven name estate-visible artifacts.**
⚠️ **But the distinction `P-69` asked for is real and now drawn:**

| class | witnesses |
|---|---|
| `[EMP]` **from the estate's register** | ⭐ `W3` · `W5` · `W7` |
| `[EMP]` **from this lane's own conduct** | ⚠️ `W1` · `W4` |
| ⛔ **no artifact** | `W2` · `W6` |

⭐⭐⭐ **And that split maps onto the cells: `K2` and `K5` are register-witnessed; `K1` and `K4` are
lane-witnessed; `K3` and the corpus-state half of `K5` are unwitnessed.** ⛔ **No cell changes, and none
is proposed** — this is a **provenance stratification of the existing table**, nothing more.

# 8. Backlog — ⛔ **no new ticket, tenth audit running; `EKS-21` corrected**

⭐ `EKS-21`'s claim to novelty is wrong and is corrected on its own record *(§6)*. ⛔ **No new item:** the
provenance stratification of §7 is a **research** result, and `EKS-05`'s boundary keeps research findings
off the backlog.

# 9. Status register
**`[EMP]`** ⭐⭐⭐ `P-08` §21's eleven settled items verbatim · `28-PROPOSAL-Q1` and `29-INTAKE-002` exist ·
*"refutation reason recorded"* · *"every one WITHDRAWN, none deleted"* · `Σ D-05`'s sole source and `F-4` ·
*"an association exists with its candidate endpoint absent"* · *"the one remaining gap"* · ⭐⭐ *"a
glyph-literal pattern over a LaTeX corpus produced a false zero"* (2026-09-07) · control 77.
**`[DERIVED]`** ⭐⭐⭐ **5 inside · 2 outside**, relative to `Ω_stip` · `W1` and `W4` are inside **only
because the lane's own documents are** · `W3`/`W5`/`W7` rest on the register · the cell stratification of §7.
**`[QUALIFIED]`** ⚠️ **every verdict is relative to a stipulated `Ω`** · `W1` holds only for adjudicated
pairs · `W5` is inside as a **defect**, not as content · ⛔ **`EKS-21`'s novelty claim.**
**`[REFUTED]`** ⭐⭐ `P-69`'s worst case (*"none"*) · my own pre-registration of 1.
**`[OPEN]`** ⭐⭐ the estate's actual `Ω` · `W2` and `W6`'s discharge · `𝒳_R`/`OQ-1` · `K0`'s opaque `𝒦` ·
all carried opens · ⛔ **well-foundedness / terminality / acyclicity — DEFERRED.**
**Governance:** `|K| = 11` **`[REC]`** — unchanged, unfrozen.

### 10. ONE NEXT UNRESOLVED QUESTION

$$\boxed{\textbf{Is a kernel witnessed by } \mathbf{its\ own\ derivation\ lane's\ conduct} \textbf{ witnessed at all — or is } W1/W4\textbf{'s } \mathbf{[EMP]} \textbf{ circular?}}$$

⭐⭐⭐ **This is the sharpest question the series has reached, and §5 forced it rather than choosing it.**
`W4` says: *this lane retained withdrawn assertions, therefore retention is obligatory.* ⭐ **The premise
is a fact about the observer's behaviour; the conclusion is a norm about the observed system.**
⛔ **If that inference is circular, `K1` and `K4` lose their `[EMP]` grounding and fall back to
`[DERIVED]` — `|K|` unchanged, but the evidence class of two cells changes.** ⭐ **If it is legitimate —
if a lane's own careful conduct is admissible evidence about what the estate requires — then it needs a
stated warrant**, because `P-08` offers the inference in one clause and never defends it.
⚠️ **And the question must be answered on the inference, ⛔ never by observing that the lane's conduct
was in fact good.**

```
5 INSIDE, 2 OUTSIDE. P-69's WORST CASE — "none" — IS REFUTED.

Qualification declared before the test: Omega for the estate has never been defined, so the test runs
against a stipulated Omega — what a reader of the estate's committed artifacts can see — and every
verdict is relative to it. That choice is generous, since it admits this lane's own documents, and it is
not the Omega of T-K1's theorem, so nothing here is an identifiability proof. A narrower Omega, the
register alone excluding this lane's prose, would move W1 and W4 outside. That sensitivity is the audit's
most fragile point and is stated rather than buried.

P-08 §21's settled [EMP] block answers all seven at once and had never been read as such. W1 is inside
via the identity-retirement proposal and intake documents, but only for pairs actually adjudicated, since
Q3 says the estate cannot always tell. W2 is OUTSIDE — routes 1, 2 and 4 absent, route 3's uniqueness
refuted in P-69. W3 is inside via "refutation reason recorded". W4 is inside via "every one WITHDRAWN,
none deleted". W5 is inside as a recorded DEFECT rather than as content — and legitimately so, because
W5's own datum is "enough cited content OR an honest grounding downgrade", and the downgrade is recorded.
W6 is OUTSIDE — the corpus-state identifier is "the one remaining gap". W7 is inside via "an association
exists with its candidate endpoint absent".

THE STRUCTURAL FINDING: W4 is the strongest of the five, and P-08 labels its own basis "[EMP] from this
lane's own conduct, not from a rule". So W4 witnesses that THIS LANE retains withdrawn assertions, not
that THE ESTATE does — [EMP] about the observer, not the observed. W1 shares the shape, its artifacts
being this lane's own proposals and intakes. The two strongest "inside" results are inside because the
lane's own documents are inside the stipulated Omega; under the narrower reading both move out, leaving
W3, W5 and W7 — the three that rest on the register itself.

THE DISTINCTION P-69 ASKED FOR IS NOW DRAWN: [EMP] from the estate's register — W3, W5, W7; [EMP] from
this lane's own conduct — W1, W4; no artifact — W2, W6. Mapped onto cells: K2 and K5 register-witnessed,
K1 and K4 lane-witnessed, K3 and the corpus-state half of K5 unwitnessed. No cell changes and none is
proposed; this is a provenance stratification of the existing table, nothing more.

A FOURTEENTH MISS, AND IT IS AGAINST MY OWN BACKLOG TICKET. Item 10 of P-08 §21 reads "a glyph-literal
pattern over a LaTeX corpus produced a false zero" — EKS-21's failure mode, witnessed and recorded on
2026-09-07, two days before I filed EKS-21 as a new discovery, in a document this lane wrote. It does not
invalidate the ticket, whose point is that nothing catches the failure and whose three later recurrences
prove it, but its provenance was wrong and EKS-21 is corrected accordingly. P-08 §21 also names this
series' lesson 11 — LaTeX escaping hiding normative language — so the lane knew the mechanism and
repeated it four times anyway.

Pre-registered 1 inside. Wrong — five pass. Record 22 of 33.

No new backlog item, tenth audit running; EKS-21's novelty claim corrected on its own record.

|K| = 11 UNCHANGED, [REC], UNFROZEN. No cell restated, no witness reassigned.

ONE NEXT UNRESOLVED QUESTION: Is a kernel witnessed by ITS OWN DERIVATION LANE'S CONDUCT witnessed at all
  — or is W1/W4's [EMP] circular? W4 says: this lane retained withdrawn assertions, therefore retention
  is obligatory. The premise is a fact about the observer's behaviour; the conclusion is a norm about the
  observed system. If that inference is circular, K1 and K4 lose their [EMP] grounding and fall back to
  [DERIVED] — |K| unchanged, but the evidence class of two cells changes. If it is legitimate, it needs a
  stated warrant, because P-08 offers the inference in one clause and never defends it. And it must be
  answered on the inference, never by observing that the lane's conduct was in fact good.

NO three_model_convergence INSPECTION — excluded by path from every command, unread.
```

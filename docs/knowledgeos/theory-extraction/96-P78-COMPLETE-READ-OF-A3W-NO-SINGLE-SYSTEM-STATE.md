# `P-78` — Complete Read of `A3W`: The Estate Has No Single System State

**2026-09-09 · Lane T.** After [`95` `P-77`](./95-P77-S-R-TRANSITION-BRIDGE-AUDIT.md).
**Method:** Chronological Thread Discovery Protocol with **§2a — mandatory complete reading**
*(strengthened by the commissioner mid-turn and recorded in `MEMORY.md` before this audit ran)*.
⭐ **This is the first audit in the series conducted on a completely-read document.**

# 1. Executive verdict — `P-77`'s question answered

$$\boxed{\mathbf{YES.\ \mathcal X_R\ is\ unwitnessed\ because\ \mathcal X_S\ is\ UNSETTLED.}}$$

| | |
|---|---|
| `K_t` **knowledge-state** | ⭐⭐⭐ **eight recorded forms**, arities **10 · 6 · 8 · 6 · 5 · 7 · 12 · 8** — and `I.1`: ***"7 incompatible tuple forms, none retracted"*** |
| `S_t` **system-state** | ⭐⭐ **eight recorded forms** — and `I.4`: ***"four arities; the two 'revised' models disagree on where Zero/Lord live"*** |
| the two *"revised"* models | ⭐⭐⭐ ***"Direct incompatibility … Step 203 rejects the very product construction Q20-revised adopts. Neither cites the other."*** |
| `Q20`-revised's own position | ⭐⭐⭐ ***"keeps `K_t` as an opaque, unparameterised component of `S_t` — the revised system model is parameterized by an unresolved knowledge state"*** |

$$\boxed{\begin{array}{c}\textbf{⇒ } \mathbf{OQ\text{-}1\ is\ NOT\ a\ decision\ Lane\ T\ awaits.} \textbf{ It is a decision } \mathbf{nobody\ can\ take\ yet}\textbf{,}\\[3pt] \textbf{because the specified system it would inherit from has } \mathbf{no\ settled\ state\ type.}\end{array}}$$

$$|K| = 11 \textbf{ — unchanged, } \mathbf{[REC]}\textbf{, } \mathbf{UNFROZEN}.$$

# 2. ⚠️ A same-day correction I had to make against myself

⭐⭐⭐ **`EKS-27`, filed less than an hour before this audit, was filed as new and is not new.** `A3W`
§7 `I.9` records, dated **2026-08-29**:

> ***"Systematic glyph collisions: `𝓔` (entity/evidence/events), `𝓟`
> (proposition/parameters/predicates/policy/powerset), `𝓒`
> (context/conflicts/commands/constraints), `𝓐` (assertion/analysis/assessment), **`δ` (state
> transition/decision function)**, `α` (analysis/action-derivation), **`Π` (provenance/process)**,
> `T_τ` (contract and 4-vector in one file), **`W_t` (witness vs world state)**"***

⛔ **Nine collision families, several with three-plus meanings — where `EKS-27` described one symbol
across two streams.** ⭐ **The ticket stands and is stronger** *(catalogued eleven days ago, nothing
done)*, ⚠️ **and its provenance is corrected on its own record.**

$$\boxed{\textbf{⭐⭐ Eighteenth miss — and filed against a } \mathbf{136\text{-}line\ register\ I\ had\ searched\ twice\ and\ never\ read.}}$$

⭐⭐⭐ **`§2a` is vindicated on its first application, and against the auditor who recorded it.**

# 3. ⭐⭐⭐ `I.1`–`I.29` — twenty-nine recorded contradictions, and eight bear on this lane

⭐ Applying the **§12a bearing test**, not resemblance:

| | contradiction | bearing |
|---|---|---|
| ⭐⭐⭐ **`I.1`** | `K_t`: **7 incompatible tuple forms, none retracted** | **`𝒳_S` unsettled ⇒ `𝒳_R` uninheritable.** The audit's headline |
| ⭐⭐ **`I.4`** | `S_t`: four arities; revised models disagree | same |
| ⭐⭐⭐ **`I.6`** | **History: set-of-snapshots (`ℋ_{t+1}=ℋ_t∪{K_t}`, *self-membership problem flagged in-corpus*) vs event-sequence (`H_t‖e_t`) — both persist** | **`K4`/`K3`: the estate has two incompatible history representations, unreconciled** |
| ⭐⭐ **`I.7`** | **Rollback: four incompatible accounts** — time-travel · new-state · plain-equality-contradicting-its-own-prose · `Rollback ≠ Reversal` | **`K3a`'s prior-value retention has no settled semantics at S** |
| ⭐⭐⭐ **`I.8`** | ***"`Preserve` defined, declared unnecessary, silently dropped while its diagrams remain"*** | ⭐⭐ **Part XIX's `Preserve` — `P-55` found five undefined symbols; `A3W` says one was *dropped*** |
| ⭐⭐⭐ **`I.14`** | ***"Q15 asserts well-formedness preservation; step-031 requires entailment non-preservation; the corpus never states which preservation notion transitions guarantee"*** | ⭐⭐ **the persistence question itself, unsettled at S** |
| ⭐⭐ **`I.9`** | systematic glyph collisions *(§2)* | **`EKS-27`, pre-dated** |
| ⭐ **`I.28`** | undefined symbols **inside "frozen" results** — `𝒫_r`, `𝒯`, `𝒮_strat`, `Authorized`, `ΔU`, `γ`, `T_t`, `𝒩`, `Measure`, `𝓕_t`, `W_t` | **corroborates `P-52`/`P-53`/`P-59` on placeholder criteria — the estate's own list** |

⛔ **Twenty-one of the twenty-nine do not bear**, and I am not listing them as if they did.

# 4. ⭐⭐ What `A3W` §5 says about `P*` — and it pre-dates `P-65` and `T-K1`

§5 item 1, **verbatim from `step-031` §31.19**:
> ***"No algorithm can recover information that the observation function destroys."***

§5 item 2, the identifiability criterion — and ⭐⭐⭐ **`A3W`'s own ⚑OBS on both:**
> ***"These are standard information-theoretic facts, correctly stated; the corpus calls them 'one of the
> strongest mathematical foundations'."***

$$\boxed{\begin{array}{c}\textbf{⭐⭐⭐ } P\text{-}65 \textbf{ concluded } P^* \textbf{ is elementary and } \mathbf{cited,\ not\ owned.}\\[3pt] \mathbf{A3W\ recorded\ exactly\ that\ verdict\ on\ 2026\text{-}08\text{-}29\ —\ eleven\ days\ earlier,\ in\ one\ line.}\end{array}}$$

⭐ **Nineteenth miss** — ⭐⭐ **and simultaneously the strongest corroboration `P-65` could have:** an
independent lane reached the same judgement about the same statement, ⛔ **and neither cites the other**,
so this is `INDEPENDENT RESEARCH — PLAUSIBLE`, ⛔ **not replication** *(one estate)*.

⭐ §5 also adds what `P-65` did not have: *"failure → the system **must be allowed to return
`Underdetermined`**"* — ⭐⭐ **a positive consequence of the impossibility, which `P-65`'s Model B
approached from the other side.**

# 5. ⭐⭐ The transition picture, and `P-77` confirmed

⭐ `Q15-revised`'s eight theorems: `T1` compute ⇏ state change · **`T2` only committed events change `K`**
· ⭐⭐ **`T3` history append-only** · `T4` rollback creates a **new** state · `T5` Zero evaluates, does not
constitute · `T6` Lord proposes, Sārathi guides, Knower commits · **`T7` `K_{t+1}=δ(K_t,e_t)`** ·
**`T8` `K_t=Replay(K_0,H_t)`**, with **`δ: 𝒦×ℰ ⇀ 𝒦` explicitly partial.**

⚠️⭐⭐⭐ **And `A3W`'s ⚑OBS on the whole picture:** ***"the 'frozen' transition model and the transition
algebra are disjoint developments with different signatures"*** — `Q13`/`Q14`/`Q15`/`Q20`/`step-031`
**never address** idempotency, commutativity, compensation or partial order.

⇒ ⭐⭐ **`P-77`'s `[UNRELATED]` is confirmed from a second direction:** ⛔ **the Level-S transition
system is not merely plural — its two main developments are *disjoint*, with different signatures, and
`I.15` records that Step 204's `τ` *"differs from and never references the 'frozen' `δ`."***

⭐⭐⭐ **Which also settles the `EKS-27` symbol question inside S:** `I.9` lists **`δ` itself** as a
collision *(state transition / decision function)*, and `I.15` says the two `τ`/`δ` developments never
reference each other. ⇒ **The collision `EKS-27` reports across S and R exists *within* S too.**

# 6. ⭐⭐⭐ The methodological result the estate reached, and I had not

`A3W`'s closing note:
> ***"The three-layer document structure (definition / rebuttal / revision) means V1 must carry per-claim
> LAYER PROVENANCE — a claim's status depends on which layer asserts it."***

⭐⭐ **And its header states the structural fact that forces it:** each `Q`-file is *"original definition →
reviewer critique rejecting parts of it → (`Q15`/`Q20` only) a revised definition"*, with ⛔ **`Q13` and
`Q14` having no revised section — *"their critiques stand unresolved against their own bodies, and
downstream files cite the unrevised `Q13` 10-tuple as authoritative."***

$$\boxed{\begin{array}{c}\textbf{⭐⭐⭐ This is the general form of a trap this lane has hit repeatedly:}\\[3pt] \mathbf{quoting\ a\ document's\ DEFINITION\ layer\ while\ its\ REBUTTAL\ layer\ rejects\ it.}\end{array}}$$

⭐ **`P-74` did exactly this** — quoting `161.3` and `161.55` as a *"signature layer"* while `VK-3`
recorded them mutually contradictory. ⭐⭐ **`layer provenance` is the missing field in my own provenance
record**, and it is now adopted *(§9)*.

# 7. Also recorded, and not overstated

⭐ §8: `Governance` and `Operational` machines are **named but their state sets are NEVER enumerated** in
`step_189` — ⇒ ⛔ **`TV-F-019`'s *"three machines kept separate"* has exactly one machine enumerated.**
⭐ §6: numbered invariants `I₃₃`, `I₇₀`–`I₇₅`, with **`I₃₄`–`I₆₉` absent from these files.**
⭐ §4: ⭐⭐ ***"Vocabulary schism: Step 201 freezes `Assessment` and has no `Assertion`; `Q13`/`Q14`/`Q15`/
`Q20`/`step-031` build everything on `Assertion` and never use `Assessment`"*** — violating Step 201's
own one-word-one-concept rule. ⚠️ **`P-08`'s `K4` is about *assertions*.**
⭐ §6: `step-031` §31.75 ***"Formal completeness: NOT YET"***; `Q20` review denies a complete
specification — ⛔ **"which the `Q20` revised half then reinstates" (`I.12`).**

# 8. Provenance record *(with the new field)*

| field | value |
|---|---|
| `thread_start` · `thread_end` | `A3W-state-transition-register` — ⛔ **not timestamped; index analogue** |
| `files_examined` | ⭐ **1, COMPLETELY** — all 136 lines, §1–§8 and the checkpoint note |
| `reading_completeness` | ⭐⭐⭐ **100% — the first in this series** |
| `discovery_signal` | `P-77`'s own next question |
| ⭐⭐ **`layer_provenance`** *(new)* | `A3W` is an **EXTRACTION** register, layer = *"records corpus claims with locations… Nothing reconciled"* — ⛔ **not an adjudication** |
| `information_flow` | `A3W` ← `Q13`,`Q14`,`Q15`,`Q20`,`step-031`,`Steps 189/201–205`; ⛔ **→ Lane T: none** |
| `final_disposition` | ⭐ **EXTRACTED 2026-08-29**; ⚠️ programme **STOPPED pending supervision** *(`EKS-25`)* |
| `admissibility` · `epistemic_status` | **CHARACTERIZED**; ⛔ **nothing admitted** |

# 9. Status register
**`[EMP]`** ⭐⭐⭐ eight `K_t` forms with six distinct arities · eight `S_t` forms · `I.1`–`I.29` verbatim ·
`I.9`'s nine glyph-collision families incl. `δ`, `Π`, `W_t` · §5's *"no algorithm can recover
information that the observation function destroys"* **and** `A3W`'s ⚑OBS *"standard information-theoretic
facts"* · `T1`–`T8` and `δ` explicitly partial · *"the frozen transition model and the transition algebra
are disjoint developments"* · Governance/Operational state sets **never enumerated** · the vocabulary
schism · the **layer-provenance** note.
**`[DERIVED]`** ⭐⭐⭐ **`𝒳_S` is unsettled, so `𝒳_R` is uninheritable and `OQ-1` is not Lane T's to
await** · `P-77`'s `[UNRELATED]` confirmed from a second direction · the `τ`/`δ` collision exists
**within** S.
**`[CORROBORATION]`** ⭐⭐ `P-65` on `P*` *(independent, `[INDEPENDENT RESEARCH — PLAUSIBLE]`, ⛔ not
replication)* · `P-52`/`P-53`/`P-59` on placeholder symbols *(`I.28`)*.
**`[REFUTED]`** ⚠️ **`EKS-27`'s novelty claim — corrected on its own record.**
**`[OPEN]`** ⭐⭐ which preservation notion transitions guarantee *(`I.14`)* · which history
representation holds *(`I.6`)* · 82 unread findings · all carried opens · ⛔ **well-foundedness /
terminality / acyclicity — DEFERRED.**
**Governance:** `|K| = 11` **`[REC]`, UNFROZEN.**

### 10. ONE NEXT UNRESOLVED QUESTION

$$\boxed{\textbf{Which layer of } \mathbf{P\text{-}08} \textbf{ do } \mathbf{K1\text{–}K11} \textbf{ live in — and has this lane's own work ever carried } \mathbf{layer\ provenance}?}$$

⭐⭐⭐ **This follows from §6 and it turns the estate's sharpest methodological result back on this lane.**
`A3W` establishes that a claim's status **depends on which layer asserts it** — definition, rebuttal, or
revision — and that downstream files went wrong by citing an **unrevised** body whose own critique had
rejected it. ⭐ **Lane T's fifty audits are three layers deep in exactly that sense:** `P-08` proposes,
`P-18`–`P-53` rebut, `P-54`–`P-78` revise — ⛔ **and no artifact records which layer any given claim about
`K1`–`K11` belongs to.** ⚠️ **If `K3`'s scope comes from the definition layer while `P-66`/`P-67`'s
rebuttal layer left it `NOT ESTABLISHED`, then citing *"`K3` retains prior value and warrant"* without
its layer is the same error `A3W` catalogued** — ⛔ **and the check must be per claim, never by audit
number, which is chronology and not layer.**

```
YES — THE RESEARCH CARRIER IS UNWITNESSED BECAUSE THE SPECIFIED ONE IS UNSETTLED.

First audit in the series conducted on a COMPLETELY READ document, under the commissioner's strengthened
rule recorded before this audit ran. K_t has EIGHT recorded forms with arities 10, 6, 8, 6, 5, 7, 12 and
8, and the register's own contradiction list reads "K_t: 7 incompatible tuple forms, none retracted".
S_t has eight forms with four arities, and "the two revised models disagree on where Zero/Lord live" —
"Step 203 rejects the very product construction Q20-revised adopts. Neither cites the other." And
Q20-revised "keeps K_t as an opaque, unparameterised component of S_t — the revised system model is
parameterized by an unresolved knowledge state." So OQ-1 is NOT a decision this lane awaits; it is a
decision nobody can take yet, because the system it would inherit from has no settled state type.

A SAME-DAY CORRECTION AGAINST MYSELF: EKS-27, filed less than an hour earlier, was filed as new and is
not new. The register records, dated 2026-08-29, "systematic glyph collisions" with the symbols
enumerated — nine families, several with three or more meanings, INCLUDING the transition symbol itself
as state-transition versus decision-function. The ticket stands and is stronger, since the collisions
were catalogued eleven days ago and nothing has been done; its provenance is corrected on its own record.
Eighteenth miss, and filed against a 136-line register I had searched twice and never read. The
strengthened reading rule is vindicated on its first application, against the auditor who recorded it.

TWENTY-NINE RECORDED CONTRADICTIONS, of which eight bear under the strict test and twenty-one do not.
The bearing ones: seven incompatible K_t forms; four S_t arities; HISTORY held two incompatible ways —
set-of-snapshots with a self-membership problem flagged in-corpus, versus event-sequence, "both persist";
ROLLBACK held four incompatible ways; "Preserve defined, declared unnecessary, silently dropped while its
diagrams remain"; "the corpus never states which preservation notion transitions guarantee"; the glyph
collisions; and a list of undefined symbols inside "frozen" results that corroborates three earlier
audits on placeholder criteria.

AND THE REGISTER PRE-DATES P-65 BY ELEVEN DAYS. Its §5 quotes step-031 verbatim — "no algorithm can
recover information that the observation function destroys" — and adds its own observation: "these are
standard information-theoretic facts, correctly stated; the corpus calls them one of the strongest
mathematical foundations." That is P-65's conclusion, reached independently and recorded first.
Nineteenth miss, and simultaneously the strongest corroboration P-65 could have — INDEPENDENT RESEARCH,
PLAUSIBLE, not replication, since it is one estate and neither cites the other.

P-77's [UNRELATED] is confirmed from a second direction: the Level-S transition system is not merely
plural, its two main developments are DISJOINT with different signatures, and Step 204's transition
symbol "differs from and never references the frozen delta". Which also settles the symbol question
inside S: the collision EKS-27 reports across the two streams exists WITHIN the specified stream too.

THE METHODOLOGICAL RESULT THE ESTATE REACHED AND I HAD NOT: "the three-layer document structure
(definition / rebuttal / revision) means V1 must carry per-claim LAYER PROVENANCE — a claim's status
depends on which layer asserts it." Q13 and Q14 have no revised section, so "their critiques stand
unresolved against their own bodies, and downstream files cite the unrevised Q13 10-tuple as
authoritative." P-74 committed exactly this error, quoting a document's definition layer while its
rebuttal layer rejected it. Layer provenance is now a field in this lane's provenance record.

|K| = 11, [REC], UNFROZEN. Nothing admitted, nothing upgraded.

ONE NEXT UNRESOLVED QUESTION: Which layer of P-08 do K1 to K11 live in — and has this lane's own work
  ever carried layer provenance? The estate establishes that a claim's status depends on which layer
  asserts it, and that downstream files went wrong citing an unrevised body whose own critique had
  rejected it. This lane's fifty audits are three layers deep in exactly that sense: P-08 proposes,
  P-18 to P-53 rebut, P-54 to P-78 revise — and no artifact records which layer any given claim about the
  eleven cells belongs to. If K3's scope comes from the definition layer while the rebuttal layer left it
  NOT ESTABLISHED, then citing "K3 retains prior value and warrant" without its layer is the same error
  the register catalogued. The check must be per claim, never by audit number, which is chronology and
  not layer.

NO three_model_convergence INSPECTION — excluded by path from every command, unread.
```

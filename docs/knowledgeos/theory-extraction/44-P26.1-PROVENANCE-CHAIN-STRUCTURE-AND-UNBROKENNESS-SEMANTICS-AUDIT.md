# `P-26.1` — Provenance-Chain Structure & Unbrokenness Semantics Audit

**2026-09-08 · Lane T.** After [`43` `P-26`](./43-P26-PROVENANCE-CHAIN-UNBROKENNESS-AND-K5-SCOPE-AUDIT.md), which is **preserved, not renumbered and not overwritten.**

> **Single question:** *does the corpus attest a multi-link provenance-chain structure, and if so what
> are its nodes, links, traversal semantics and the meaning of "unbroken"?*
>
> ⛔ **`P-08` baseline · `P-18`–`P-26` unmodified · 3MC read-only · Schema v2 untouched · no Schema v3 ·
> no architecture · no implementation · no canonical theory · ⛔ well-foundedness NOT adjudicated (`C8`)
> · no cell added, removed, split, merged or re-scoped · no silent repair · `|K| = 11` unless
> independently disproven.**

# 0. ⭐⭐⭐ The caution was correct, and the corpus settles it in one line

`[EMP]` `brainstorming/20260823-104251-constitutional-dsl-…md:395-396`:

```yaml
- condition: evidence.provenance.history.length > 3
  weight_multiplier: 1.1 (more provenance = more weight)
```

$$\boxed{\begin{array}{c}\mathbf{history\ HAS\ A\ LENGTH.\ It\ is\ a\ LIST,\ and\ its\ length\ is\ an\ EVIDENCE\text{-}WEIGHT\ INPUT.}\\[4pt] \boxed{\textbf{⛔ NOT a chain traversal. The existence of } \mathit{evidence.provenance.history} \textbf{ does NOT establish a recursive chain.}}\end{array}}$$

⭐ **And where the multi-hop actually comes from:** `step.evidence is admissible` **recurses into another
evidence item's own rule-set** — so depth lives in the **predicate**, never in the list.

---

# `C0` — terminology, with admissibility correction

⚠️ **`P-26` counted `provenance chain` at 53 files. That count included Lane M.**

| term | raw *(minus Lane T gap-discovery)* | ⭐ **admissible** *(minus 3MC too)* | Lane M share |
|---|:--:|:--:|:--:|
| ⭐ **`provenance graph`** | 90 | ⭐ **63** | **27** |
| ⭐ **`provenance chain`** | 53 | ⭐ **40** | ⭐ **13** |
| `provenance record` | 19 | **10** | 9 |
| `provenance link` | 8 | **7** | 1 |
| `provenance path` | 6 | **4** | 2 |
| `provenance node` | 2 | **2** | 0 |
| ⭐ `evidence.provenance` | — | **1** | — |
| `provenance step` · `recursive provenance` · `provenance tree` | — | ⭐ **0 · 0 · 0** | — |

⭐⭐ **`provenance graph` (63) outnumbers `provenance chain` (40)** — so *"chain"* is not even the corpus's
dominant term.

## What the 63 admissible `provenance graph` hits actually are

| hit | class |
|---|---|
| ⭐ `\| Isnād \| Transmission chain \| Provenance graph \|` — the Quranic **isnād** lens | ⭐ **4 imported lens-mapping** |
| *"That is exactly the foundation of a provenance graph"* ×2 — Indian-philosophy lens | **4 imported lens-mapping** |
| *"the KnowledgeOS provenance graph becomes partly a temporal probabilistic state graph"* — HSMM lens | ⭐ **2 research desideratum** |
| `\| Provenance Graph \| Context Graph \|` — architecture-pattern extraction | **2 desideratum** |
| *"provenance graph integrity"* — a principal-architect review | **2 desideratum** |

$$\boxed{\textbf{⭐ } \mathit{provenance\ graph} \textbf{ is attested overwhelmingly as a LENS-MAPPING TARGET and a MODELLING PROPOSAL — not as a corpus-committed structure.}}$$

---

# `C1`/`C2`/`C3` — nodes, links, and the multi-hop test

## The stated requirement is SINGLE-LINK

⭐⭐⭐ `[EMP]` `20260827-125515_the-core-insight-three-distinct-information-pathways.md:457`:

> *"**5. Every Assertion must have at least one provenance link.** This is the lineage requirement…"*

$$\boxed{\begin{array}{c}\textbf{"AT LEAST ONE" — a cardinality-1 MINIMUM on a SINGLE relation.}\\ \boxed{\textbf{The corpus's stated lineage requirement is } \mathbf{C0}\textbf{, not } \mathbf{C1} \textbf{ or } \mathbf{C2}.}\end{array}}$$

## ⭐⭐ But a genuine multi-hop witness DOES exist — and it is admissible

`[EMP]` `brainstorming/verification/spec/STEP-VERIFY-001-010.md:18`:

> *"Deterministic rule: **provenance path `e_i ⇝ e_j`** ⇒ Dependent. **CommonAncestor** ⇒ Independent ≠
> Confirmed. DifferentSource ⇏ Independen[t]"*

| why this is genuine `C2` evidence | |
|---|---|
| ⭐ **`⇝`** | a **transitive reachability** operator over provenance |
| ⭐⭐ **`CommonAncestor`** | requires **depth ≥ 2** — two items sharing an ancestor cannot be a single link |
| **`CommonAncestor` occurrences** | **7 admissible files** |
| ⭐ **`R-INV-04`** | *"Independent **provenance nodes** merged or overwritten"* — names **nodes** in a failure mode |
| **`155A.12 — The provenance path`** | a section title |

## ⭐⭐⭐ But note WHAT the multi-hop is FOR

$$\boxed{\begin{array}{c}\textbf{The } \mathbf{⇝} \textbf{ / CommonAncestor structure exists to ASSESS INDEPENDENCE of evidence —}\\ \textbf{it is a DERIVED ANALYTICAL structure, evaluated over retained links.}\\[6pt] \boxed{\textbf{⭐ And } P\text{-}16\ \S14 \textbf{ already settled that class: paths, reachability and transitive closure are COMPUTED, NEVER STORED.}}\end{array}}$$

| verdict on `C0`–`C3` | |
|---|---|
| **`C0`** single relation | ⭐⭐ **`[EMP]` — and it is the STATED requirement** |
| **`C1`** sequence | ⚠️ **`[DERIVED]`** — implied by `⇝`, never stated as a required structure |
| ⭐ **`C2`** directed graph | ⭐⭐ **`[EMP]` as a DERIVED ANALYTICAL structure** *(independence assessment)*; ⛔ **`[OPEN]` as a required persisted structure** |
| **`C3`** rhetorical only | ⭐ **`[REFUTED]`** — `⇝` and `CommonAncestor` are formal, not rhetorical |

**Nodes:** `[EMP]` **evidence items** *(`e_i`, `e_j`)*, and **assertions** *(the lineage requirement)*.
⛔ **Not records, not processes** — consistent with `P-24`/`P-25`. **Links:** `[EMP]` a
**derives-from / reachability** relation. ⛔ **No `ProvenanceNode` or `ProvenanceChain` domain object is
manufactured.**

---

# `C4` — what "unbroken" means, `U1`–`U8`

| | interpretation | corpus evidence |
|---|---|---|
| **`U1`** every link present | ⭐⭐ **`[EMP]` — `step.evidence is not null`** |
| **`U2`** every link checkable | ⚠️ **`[DERIVED]`** — implied by recursive admissibility, not stated per link |
| **`U3`** every referenced node exists | ⭐ **`[EMP]`** — same `not null` clause |
| **`U4`** traversal meets no missing node/link | ⚠️ **`[DERIVED]`** — a consequence of `U1`+`U3` |
| **`U5`** reaches a terminal node | ⭐ **`[OPEN]`** — ⛔ deferred by `C8` |
| **`U6`** reaches a legitimate ground | ⭐ **`[OPEN]`** — deferred |
| **`U7`** acyclic | ⭐ **`[OPEN]`** — deferred |
| ⭐⭐ **`U8`** admissible under `EvidenceAdmissibility` | ⭐⭐⭐ **`[EMP]` — `step.evidence is admissible`, verbatim, and it is the RULE'S OWN third clause** |

$$\boxed{\begin{array}{c}\textbf{"Unbroken" is corpus-specified as } \mathbf{U1 \wedge U3 \wedge U8} \textbf{ — presence, endpoint existence, and RECURSIVE ADMISSIBILITY.}\\ \boxed{\textbf{⛔ It is NOT specified as } U5,\ U6 \textbf{ or } U7. \textbf{ Those are separate properties, and they are deferred.}}\end{array}}$$

# `C5` — the DSL witness, re-read

| reading of `for each step in evidence.provenance.history` | verdict |
|---|---|
| an actual chain **traversal** | ⭐ **`[REFUTED]` — `history.length > 3` proves it is a LIST** |
| ⭐ a **list validation** | ⭐⭐ **`[EMP]` — this is what it is** |
| ⭐ **recursive admissibility** | ⭐⭐⭐ **`[EMP]` — the depth lives in `step.evidence is admissible`, not in the list** |
| a schema constraint | ⚠️ **`[CORROBORATION]`** — it is a DSL rule, i.e. a specification |
| an implementation-level check | ⚠️ **possible** — ⛔ **and the implementation representation is NOT taken as the canonical conceptual model** |

⭐⭐ **So `history` is a per-item LIST whose LENGTH feeds evidence WEIGHT, and multi-hop depth arises
only from the recursive predicate.** ⭐ **Two different structures, conflated by the single word
*"chain"*.**

---

# `C6` — audit of the completed `P-26`

| `P-26` claim | verdict |
|---|---|
| unbrokenness's **sole home is `EvidenceAdmissibility`** | ⭐⭐ **CONFIRMED, independently** — one file, one occurrence, one key |
| the `check` **decomposes** into link retention · recursion · attribution | ⭐ **CONFIRMED** |
| ⭐ the attribution clause **corroborates `P-25`** | ⭐⭐ **CONFIRMED and STRENGTHENED** — `evidence.author in authority_register` is a **weight modifier**, so attribution feeds **weighting** too, never persistence |
| **`K5` generalizes to depth because `P-19` restated it at claim level** | ⭐⭐ **CONFIRMED — and it does not depend on chain structure**, so it survives this audit untouched |
| **persistence supplies / admissibility consumes** | ⭐⭐⭐ **CONFIRMED and STRENGTHENED** — `history.length` feeds a **weight_multiplier**, which is squarely evaluation, not retention |
| **missing-link and uncheckable-link are independent failure modes** | ✅ **confirmed** |
| ⚠️ **`provenance chain` = "53 files"** | ⭐⭐ **`[QUALIFIED]` — Lane-M contaminated. Admissible count is 40** |
| ⚠️ **`for each step in …history` read as chain traversal** | ⭐⭐⭐ **`[QUALIFIED]` — it is a LIST with a LENGTH; the recursion, not the list, carries depth** |
| ⚠️ **well-foundedness *"attested rather than invented"* via `regress` 131 etc.** | ⭐ **`[QUALIFIED]` — those counts were NOT admissibility-filtered either** |
| **unbroken ⇏ well-founded** | ✅ **confirmed, and now sharper**: `U1∧U3∧U8` is specified; `U5`/`U6`/`U7` are not |

$$\boxed{\textbf{⭐ SIX claims CONFIRMED (two STRENGTHENED), THREE } \mathbf{[QUALIFIED]}\textbf{, ZERO refuted. ⛔ } P\text{-}26 \textbf{ is not rewritten.}}$$

# `C7` — persistence boundary

**Independence attempt:** two histories with `S(H₁) = S(H₂)` across all 11 cells, differing in **chain
structure**.

| step | finding |
|---|---|
| can chain structure differ with identical retained links? | ⭐ 🔴 **NO** — the structure **IS** the retained links; `⇝` is their transitive closure, and `P-16` §14 makes closure **derived** |
| can it differ with identical `K2`/`K3a`/`K4a-i`? | 🔴 **no** — those hold the source values, prior values and retained evidence that constitute the links |
| ⭐ does the corpus require the estate to **preserve the closure**? | ⭐⭐ 🔴 **no witness** — `⇝` is used to **compute** Dependent/Independent, an **assessment** |
| ⭐ `R-INV-04` *"Independent provenance nodes merged or overwritten"* | ⭐⭐ **a `W1`-shaped IDENTITY failure — borne by `K1` and `K4c`**, not a new obligation |

$$\boxed{\begin{array}{ll}\textbf{chain EXISTS} & \mathbf{[EMP]} \textbf{ as a derived analytical structure}\\ \textbf{chain must PERSIST} & \mathbf{[UNWITNESSED]} \textbf{ — the LINKS persist; the CLOSURE is computed}\\ \textbf{chain must remain CHECKABLE} & \mathbf{[DERIVED]} \textbf{ — } K5 \textbf{, at claim level}\\ \textbf{chain must be ADMISSIBLE} & \mathbf{[EMP]} \textbf{ — } U8\textbf{, and NOT a persistence obligation}\end{array}}$$

# `C8` — well-foundedness deferred
⛔ **`U5`, `U6`, `U7` are recorded as deferred and NOT adjudicated.** ⭐ **And `P-26.1` sharpens the
question they answer: since `⇝` is a derived closure, well-foundedness would be a property of a
COMPUTED structure — which raises whether it can be a persistence obligation at all.**

---

# Final result

| # | question | answer |
|---|---|---|
| **1** | is a provenance chain formally corpus-attested? | ⭐⭐ **PARTLY.** `C0` single link is the **stated requirement** *(`[EMP]`, "at least one")*; `C2` multi-hop is `[EMP]` **as a DERIVED ANALYTICAL structure** *(`⇝`, `CommonAncestor`, 7 files)*; ⛔ **`[OPEN]` as a required persisted structure** |
| **2** | nodes? | ⭐ **evidence items and assertions** *(`[EMP]`)*. ⛔ **not records, not processes** |
| **3** | links? | **a derives-from / reachability relation** *(`[EMP]`)*; ⛔ **the several provenance relations are NOT collapsed** |
| **4** | is "unbroken" formally specified? | ⭐⭐ **YES — as `U1 ∧ U3 ∧ U8`.** ⛔ **NOT as `U5`/`U6`/`U7`** |
| **5** | chain integrity ≠ single-link checkability? | ⭐ **YES** — it adds **link presence** and **recursive admissibility**; ⛔ **both already borne** |
| **6** | can `K5` cover every required chain property? | ⭐ **NO — and it does not need to.** `K5` covers **checkability**; `U1`/`U3` are **link retention** (`K2`/`K3a`/`K4a-i`); `U8` is **admissibility** |
| **7** | does `K3b` contribute independently? | ⭐ **YES** — the transition warrant per link |
| **8** | does `K4a-i` contribute independently? | ⭐ **YES** — retained evidence items are the nodes. ⛔ **But node retention alone does NOT guarantee chain checkability** — that is `K5`'s part |
| **9** | is there a chain-level invariant? | ⭐⭐ **YES — `U8` recursive admissibility.** ⛔ **It is an ADMISSIBILITY invariant** |
| **10** | is it a persistence obligation? | ⛔ **NO** |
| **11** | independent witness present? | ⛔ **NO — `[UNWITNESSED]`.** The closure is **derived** from links the kernel already retains |
| **12** | kernel? | ⭐ **`|K| = 11` — unchanged** |
| **13** | withdrawn / qualified | ⭐ **THREE `P-26` items `[QUALIFIED]`**: the 53-file count *(Lane-M contaminated → 40)* · reading `…history` as **traversal** *(it is a **list with a length**)* · the unfiltered `regress`/`acyclic` counts. ⛔ **Nothing refuted** |
| **14** | `[OPEN]` | `U5`/`U6`/`U7` *(deferred)* · `C2` as a **persisted** structure · whether the several provenance relations are one · `P-25`'s carried opens · `K4b-ii`'s corroboration arm · register deletion · *"estate"*'s two referents |
| **15** | ⭐ **ONE next question** | ⭐⭐⭐ **Is the INDEPENDENCE RELATION — `⇝` / `CommonAncestor`, i.e. `TG-02` — an admissibility predicate or a persistence obligation?** *(It is the only genuinely multi-hop corpus structure found, it is stated as a **deterministic rule**, and `P-16` §14 says its closure is computed — so its status decides whether ANY chain-level property can ever be a persistence obligation.)* |

# Stopping condition

$$\boxed{\textbf{STOP-3} \textbf{ — chain structure IS attested, but its PERSISTENCE semantics remain insufficiently specified.} \qquad \boxed{\textbf{STOP-4 also holds:}\ P\text{-}26\textbf{'s } K5 \textbf{ conclusion survives independently.}}$$

⛔ **Not `STOP-1`** *(the multi-hop structure is attested only as a derived assessment device)* · ⛔ **not
`STOP-2`** *(`⇝` and `CommonAncestor` are formal, so "terminology only" is refuted)* · ⛔ **not `STOP-5`**
*(three `P-26` items are qualified, none unsound)*. ⛔ **`P-27` not started.**

# Provenance
**`[EMP]`** ⭐ **`evidence.provenance.history.length > 3` with `weight_multiplier`** · `step.actor in
identity_registry` · `step.evidence is not null` · `step.evidence is admissible` · `evidence.author in
authority_register` as a **weight modifier** · ⭐ *"Every Assertion must have **at least one** provenance
link"* · ⭐ *"provenance path `e_i ⇝ e_j` ⇒ Dependent. CommonAncestor ⇒ Independent ≠ Confirmed"* ·
`R-INV-04` · admissibility-corrected counts **63 / 40 / 10 / 7 / 4 / 2** with **0** for `provenance
step`, `recursive provenance`, `provenance tree`.
**`[DERIVED]`** ⭐⭐⭐ **`history` is a LIST whose LENGTH feeds WEIGHT; depth lives in the recursive
predicate** · ⭐⭐ **the multi-hop structure exists to ASSESS INDEPENDENCE and is a derived closure
(`P-16` §14)** · ⭐ **"unbroken" = `U1 ∧ U3 ∧ U8`** · **`R-INV-04` is a `W1`-shaped identity failure** ·
**chain exists ≠ must persist ≠ must remain checkable ≠ must be admissible**.
**`[CORROBORATION]`** the **isnād** and Indian-philosophy lens mappings · the HSMM and
architecture-pattern proposals.
**`[QUALIFIED]`** three `P-26` items *(row 13)*. **`[REFUTED]`** `C3` rhetorical-only · traversal reading.
**`[UNWITNESSED]`** chain-closure persistence. **`[OPEN]`** row 14.
⛔ **Lane M excluded throughout and its share reported · no domain object manufactured · `P-26`
preserved · well-foundedness untouched.**

---

```
STOP-3 (+STOP-4) — A PROVENANCE CHAIN IS PARTLY ATTESTED, BUT ITS PERSISTENCE SEMANTICS REMAIN
INSUFFICIENTLY SPECIFIED. KERNEL REMAINS 11. NOTHING REFUTED; THREE P-26 ITEMS QUALIFIED.

THE CAUTION WAS CORRECT, AND ONE CORPUS LINE SETTLES IT:
  - condition: evidence.provenance.history.length > 3
    weight_multiplier: 1.1 (more provenance = more weight)
`history` HAS A LENGTH. It is a LIST, and its length is an EVIDENCE-WEIGHT INPUT — not a chain
traversal. The existence of evidence.provenance.history does NOT establish a recursive chain. The
multi-hop depth comes ONLY from the recursive predicate `step.evidence is admissible`, which recurses
into another evidence item's own rule-set. Two different structures, conflated by the single word
"chain".

THE STATED REQUIREMENT IS SINGLE-LINK: "Every Assertion must have AT LEAST ONE provenance link. This is
the lineage requirement." A cardinality-1 minimum on a single relation — C0, not C1 or C2.

BUT A GENUINE MULTI-HOP WITNESS EXISTS AND IS ADMISSIBLE: verification/spec/STEP-VERIFY-001-010.md's
"Deterministic rule: provenance path e_i ⇝ e_j => Dependent. CommonAncestor => Independent != Confirmed."
`⇝` is transitive reachability and CommonAncestor requires DEPTH >= 2, so C3 (rhetorical-only) is
REFUTED. Yet note WHAT it is FOR: assessing the INDEPENDENCE OF EVIDENCE. It is a DERIVED ANALYTICAL
structure — and P-16 §14 already settled that class: paths, reachability and transitive closure are
COMPUTED, NEVER STORED.

ADMISSIBILITY CORRECTION TO P-26: its "53 files" for `provenance chain` included THIRTEEN LANE M FILES;
the admissible count is 40. And `provenance graph` (63 admissible) OUTNUMBERS `provenance chain` (40),
so "chain" is not even the corpus's dominant term — while all 63 graph hits are LENS-MAPPINGS (isnād,
Nyāya) or MODELLING PROPOSALS (HSMM, architecture patterns), never a committed structure.

"UNBROKEN" IS FORMALLY SPECIFIED — as U1 (every link present) AND U3 (endpoints exist) AND U8 (RECURSIVE
ADMISSIBILITY), all three verbatim in the rule's own check. It is NOT specified as U5 (terminates),
U6 (legitimate ground) or U7 (acyclic) — those are separate properties, deferred by C8 and NOT
adjudicated here.

PERSISTENCE BOUNDARY: chain EXISTS [EMP, derived structure] != chain must PERSIST [UNWITNESSED — the
LINKS persist, the CLOSURE is computed] != chain must remain CHECKABLE [DERIVED, K5 at claim level] !=
chain must be ADMISSIBLE [EMP, U8, and not a persistence obligation]. R-INV-04's "Independent provenance
nodes merged or overwritten" is a W1-shaped IDENTITY failure borne by K1/K4c, not a new obligation.

P-26 AUDIT: SIX claims CONFIRMED, two of them STRENGTHENED — the attribution clause corroborates P-25
even harder, because `evidence.author in authority_register` is a WEIGHT MODIFIER, and "persistence
supplies / admissibility consumes" is reinforced because history.length feeds a weight_multiplier.
THREE claims QUALIFIED (the 53-file count, the traversal reading, the unfiltered regress/acyclic
counts). ZERO refuted. P-26 is preserved, not rewritten.

NEXT QUESTION (one): IS THE INDEPENDENCE RELATION — `⇝` / CommonAncestor, i.e. TG-02 — AN ADMISSIBILITY
  PREDICATE OR A PERSISTENCE OBLIGATION? It is the only genuinely multi-hop corpus structure found, it
  is stated as a DETERMINISTIC RULE, and P-16 §14 says its closure is computed — so its status decides
  whether ANY chain-level property can ever be a persistence obligation.

NO ARCHITECTURE — NO SCHEMA v3 — NO IMPLEMENTATION — NO CANONICAL THEORY — 3MC UNTOUCHED — NO CELL
CHANGED — WELL-FOUNDEDNESS DEFERRED — P-27 NOT STARTED.
```

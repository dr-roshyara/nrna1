# `P-22` — Assertion Subject, Holder Cardinality & Persistence Scope Audit

**2026-09-08 · Lane T.** After [`38` `P-21`](./38-P21-WITHDRAWAL-VS-SUPERSESSION-TRANSITION-IDENTITY-AUDIT.md).

> **The purpose:** determine whether the word **holder** has silently become an ontological assumption
> inside the persistence proof.
>
> ⛔ **`P-08`, `P-18`–`P-21`, Schema v2 unmodified · no Schema v3 · no architecture or persistence
> technology · 3MC untouched · no kernel cell added or deleted without an explicit independence proof ·
> DDD never creates a distinction · no silent repair.**

# 0. ⭐⭐⭐ The finding that must come first

$$\boxed{\begin{array}{c}\textbf{"HOLDER" IS NOT CORPUS-ATTESTED. I COINED IT IN } P\text{-}21\ \S4.\\ \textbf{It appears in no corpus record, in no prior artifact before } P\text{-}21\textbf{, and in no admitted evidence.}\end{array}}$$

⭐ **It was introduced to name the subject whose *current assertion* changes** — and it did substantial
theoretical work immediately. ⚠️ **Classified `[QUALIFIED]`, and §1 asks whether any corpus-side thing
plays the role.**

⭐⭐ **A second unattested term is found in the same sentence:** *"role-replacement claim"*. **"Role"** has
no corpus grounding either — §6.

---

# PART I — the eight levels

| concept | corpus evidence | derived meaning | ontological status | needed by the kernel? |
|---|---|---|---|:--:|
| **candidate identity** | ⭐ `[EMP]` — glyph registers, `𝒦`/`K`, `ℐ`/`𝓘` | the thing definitions are about | **attested** | ✅ `K1` |
| **assertion** | `[EMP]` — the estate makes and withdraws claims | a claim made at a time | **attested** | ✅ `K4a-i` |
| **assertion occurrence** | ⭐ 🔴 **none** — `P-07`'s *occurrence* is about **glyphs**, not assertions | — | ⭐ **`[OPEN]` — not attested, and not used** | 🔴 |
| ⭐⭐⭐ **holder** | ⭐⭐ 🔴 **NONE — coined in `P-21`** | the subject for whom an assertion is current | ⭐⭐ **`[QUALIFIED]`** — §1.1 finds an analogue | ⚠️ **§4** |
| **holder-assignment** | 🔴 none | which subject holds which claim | `[DERIVED]` | ⚠️ |
| **currentness** | ⚠️ ⭐ **weakly `[EMP]`** — MD-018 status values, `[RF]` markings, *"operative"* readings | which claim stands now | **attested in a weak form** | ✅ `K2` |
| ⭐ **role** | ⭐ 🔴 **NONE** — metaphorical in `P-21` | the question a claim answers | ⭐ **`[ARCH]`** — §6 restates it | ⚠️ |
| **warrant** | `[EMP]` — `C-022`, `G-67`, the withdrawal reasons | a justification | **attested** | ✅ `K3b` |

⛔ **They are not eight domain entities. Three are unattested (`assertion occurrence`, `holder`,
`role`), and one of those is not used at all.**

## ⭐⭐ 1.1 Is there a corpus-side analogue of *holder*?

| candidate | verdict |
|---|---|
| **the estate as a single subject** | ⭐ the default reading of *"the estate once claimed"* (`P-08` §5.2) |
| ⭐⭐⭐ **RECORD-KEEPING LANES / REGISTERS** | ⭐⭐ **`[EMP]`: the corpus contains SEVERAL record-keeping subjects with their own decision logs** — 3MC's `model-boundary-decisions.md` (MD-001…MD-021) alongside the research and verification registers |

⭐ **So a plural analogue exists in the corpus, under a different name.** ⚠️ *"Holder"* remains **my
term**; the plural **structure** is attested.

## The critical test — is *assertion* the bearer of currentness?

$$\boxed{\begin{array}{c}\textbf{NO. And the corpus denies it in a way } P\text{-}21 \textbf{ overlooked:}\\ \boxed{\begin{array}{l}\textbf{MD-017's } \mathit{unresolved\_equivalence} \textbf{ is the DEFAULT, across 125 files — the estate holds}\\ \textbf{COMPETING claims SIMULTANEOUSLY. And } \textit{definition} \to \textit{candidate} \textbf{ is } \mathbf{0..N}\ \mathbf{[OPEN]}.\end{array}}\end{array}}$$

⭐⭐⭐ **Therefore *"the holder's current-assertion FIELD"* presupposes SINGLE-VALUEDNESS that the corpus
explicitly denies.** ⇒ **currentness is SET-VALUED**, and `P-21`'s word *"field"* is **`[QUALIFIED]`**.

⭐ **Does that break `P-21`'s result?** 🔴 **No** — §9 shows the nested structure survives with **set
membership change** replacing **scalar assignment**.

---

# PART II — holder cardinality, four hypotheses

| | hypothesis | status | evidence |
|---|---|---|---|
| **`H1`** exactly one holder | — | ⭐ **`[REFUTED]` as a general claim** | §1's plural registers; MD-017's simultaneous competing claims |
| **`H2`** zero-or-one | — | ⭐ **`[EMP]` for the zero case** | withdrawal-without-successor leaves **no** current claim for that question — `INV-9` |
| **`H3`** multiple simultaneous holders | — | ⭐⭐ **`[EMP]`** | several registers hold claims about the same subject matter at once |
| ⭐⭐⭐ **`H4`** holder-relative currentness | — | ⭐⭐⭐ **`[EMP]` at the document level, `[DERIVED]` as characterization** | ⭐ **two lanes' documents assert incompatible closure states about the same subject matter, both standing** — `external_research` declaring gaps addressed while the research register's closure rows were unsatisfied |

⚠️ **Admissibility, stated plainly:** the **documents** are corpus `[EMP]`; **my characterization of
them as holder-relative currentness is `[DERIVED]`.** ⛔ **And `20`/`20a`/the intake records are Lane T's
own governance artifacts — cited as structure, NOT as corpus evidence.**

## Per-hypothesis effects

| does it alter… | `H2` | `H3` | `H4` |
|---|:--:|:--:|:--:|
| the meaning of *"current assertion"* | ⭐ **yes — it may be EMPTY** | ⭐ **yes — SET-valued** | ⭐⭐ **yes — RELATION-valued** |
| **`K2`**'s subject | ⚠️ | ⭐ **yes** | ⭐⭐⭐ **yes — §4** |
| **`K4a-i`** | 🔴 | 🔴 | ⭐ 🔴 **no — §5** |
| **`K4a-ii`** | 🔴 | ⚠️ | ⭐⭐⭐ **yes — §6** |
| `K3b` · `K4b-i` · `K4b-ii` | 🔴 | 🔴 | ⚠️ **§8** |
| the transition taxonomy | 🔴 | 🔴 | ⭐ **indexing only — §9** |

⛔ **No hypothesis was selected for producing a cleaner model.** ⭐ **`H1` is refuted, and `H2`, `H3`,
`H4` all hold.**

---

# PART III — formalizing currentness

| candidate form | verdict |
|---|---|
| `holder.current_assertion` *(a scalar field)* | ⭐ **`[REFUTED]`** — §1's set-valuedness |
| `Current(assertion)` | ⭐⭐ **`[REFUTED]`** — cannot express `H4` |
| ⭐ **`Current(holder, assertion)`** | ⭐⭐⭐ **the minimum that expresses every witnessed case** |
| `Current(holder, role, assertion)` | ⚠️ **not required** — *role* is unattested (§6); ⛔ **not introduced** |

$$\boxed{\textbf{Currentness is a RELATION, not a field. } \mathit{Current}(h, p) \textbf{ — and it is set-valued in } p \textbf{ for fixed } h.}$$

| case | expressible by the existing kernel? |
|---|---|
| **`A`** one holder: `Current(h₁,p₁) → ¬Current(h₁,p₁)` | ✅ **yes** — `K2` + `K4a-i` |
| ⭐ **`B`** two holders, withdrawal for one: `¬Current(h₁,p₁)`, `Current(h₂,p₁)` | ⚠️ ⭐⭐ **only if `K2` is HOLDER-INDEXED.** Otherwise the two states contradict |
| **`C`** role-specific currentness | ⛔ **not tested — *role* is unattested.** `[OPEN]` |

⭐ **Case `B` is the pivot: the existing kernel expresses it iff `K2`'s subject is a relation.**

# PART IV — `K2`'s subject

| candidate reading | verdict |
|---|---|
| preservation of a **current field value** | ⭐ **`[QUALIFIED]`** — `P-21`'s formulation, refuted by set-valuedness |
| ⭐ **preservation of the CURRENTNESS RELATION** | ⭐⭐⭐ **required** |
| preservation of **subject-relative operative state** | ⭐ **the same thing, stated in domain language** |

**Minimal pair:**

| | `H1` | `H2` |
|---|---|---|
| | `Current(h₁,p₁)` | `Current(h₁,p₁)` **and** `Current(h₂,p₁)` |
| **do they need different persistence obligations?** | ⭐⭐ **YES — and the reason is precise:** in `H2`, withdrawing for `h₁` must leave `h₂`'s claim standing. A **scalar** `K2` cannot represent that; a **relational** `K2` can |

$$\boxed{\begin{array}{c}K2\textbf{'s SUBJECT is mis-specified in } P\text{-}21 \textbf{ — but this is an INDEXING requirement, NOT a decomposition.}\\ \boxed{\textbf{No cell is added. } K2 \textbf{ preserves a RELATION rather than a FIELD.}}\end{array}}$$

# PART V — `K4a-i`: retained by whom?

| interpretation | verdict |
|---|---|
| 1 **retained globally by the estate** | ⭐⭐⭐ ✅ **THIS** |
| 2 retained for the former holder | 🔴 — would permit loss when that holder disappears |
| 3 retained for every holder | 🔴 — no evidence, and vacuously stronger |
| 4 retained as an assertion identity **independent of holders** | ⭐ **equivalent to 1 in effect** |
| 5 retained as a historical holder↔assertion relation | ⚠️ **that is `K2`'s history, not `K4a-i`'s** |

`[EMP]` **`P-08` §5.2's ground is about the ESTATE: *"never lose a claim the estate once made"***, and its
justification — evidence about the **process's** reliability — is **process-wide, not holder-wide**.

$$\boxed{\begin{array}{c}K4a\text{-i is HOLDER-INDEPENDENT and remains ONE capability.}\\[4pt] \boxed{\textbf{⭐ An ASYMMETRY worth naming: } K2 \textbf{ and } K4a\text{-ii are HOLDER-RELATIVE; } K4a\text{-i is GLOBAL.}}\end{array}}$$

# PART VI — ⭐⭐⭐ `K4a-ii` and the four histories

| | history | what it forces |
|---|---|---|
| **`H-S1`** | one holder, `p₂` replaces `p₁` | `Replaces(p₂,p₁)` suffices |
| ⭐⭐⭐ **`H-S2`** | `h₁: p₂` replaces `p₁`; **`h₂` keeps `p₁` current** | ⭐⭐ **a GLOBAL `Replaces(p₂,p₁)` CONTRADICTS `h₂`'s standing claim** ⇒ **the relation must be holder-indexed** |
| **`H-S3`** | `h₁: p₂` replaces `p₁`; `h₂: p₃` replaces `p₁` | ⭐ two replacements of one predecessor — **incoherent globally, coherent when indexed** |
| **`H-S4`** | one holder, two roles | ⛔ **not tested — *role* unattested.** `[OPEN]` |

$$\boxed{\begin{array}{c}\textbf{Replacement is NOT globally a relation between assertions. It is INDEXED BY THE SUBJECT}\\ \textbf{whose currentness changes: } \mathit{Replaces}(h,\ p_2,\ p_1).\\[4pt] \boxed{\textbf{⭐ And "ROLE" is dropped: } P\text{-}21\textbf{'s } \textit{"role-replacement claim"} \textbf{ becomes } \textit{"REPLACEMENT claim"} \textbf{ — the ROLE was decorative.}}\end{array}}$$

⭐ **The claim's content survives intact** — *`p₂` answers the same question `p₁` answered* — ⛔ **but
*"role"* is `[ARCH]` and is not retained as a term.**

# PART VII — minimal pairs under the holder hypotheses

| pair | recoverable from the retained state? | minimum information required | classification |
|---|:--:|---|---|
| `Current(h₁,p₁)` vs `Current(h₁,p₁) ∧ Current(h₂,p₁)` | 🔴 **no**, under scalar `K2` | ⭐ **the holder index on `K2`** | ⭐⭐ **EXISTING RELATION, re-specified — an INDEXING requirement** |
| `H-S2`: replaced for `h₁`, standing for `h₂` | 🔴 **no**, under global `Replaces` | ⭐ **the holder index on `K4a-ii`** | ⭐⭐ **EXISTING RELATION, re-specified** |
| withdrawal for `h₁` vs withdrawal for both | 🔴 no | the holder index | **indexing** |

$$\boxed{\textbf{⛔ In no case is the required information a NEW PERSISTENCE OBLIGATION. Every case is an INDEXING requirement on an existing cell.}}$$

# PART VIII — kernel independence under holder plurality

| cell | subject changes? | witness changes? | independence argument changes? | new capability? | another cell suffices? | unconditional? |
|---|:--:|:--:|:--:|:--:|:--:|---|
| **`K1`** | 🔴 | 🔴 | 🔴 | 🔴 | 🔴 | ✅ presupposition argument intact |
| ⭐ **`K2`** | ⭐⭐ **YES — relation, holder-indexed** | ⚠️ **re-stated, same shape** | 🔴 | ⭐ 🔴 **NO** | 🔴 | ⚠️ **`R0`-conditional as before** |
| **`K3a`** | 🔴 | 🔴 | 🔴 | 🔴 | 🔴 | ✅ |
| **`K3b`** | ⚠️ **`[OPEN]` — is a warrant holder-relative?** | 🔴 | 🔴 | 🔴 | 🔴 | ⚠️ **§15's next question** |
| **`K4a-i`** | ⭐ 🔴 **NO — global** | 🔴 | 🔴 | 🔴 | 🔴 | ✅ |
| ⭐ **`K4a-ii`** | ⭐⭐⭐ **YES — holder-indexed** | ⚠️ **`H-S2` strengthens it** | ⭐ **strengthened** | 🔴 **no** | 🔴 | ✅ |
| **`K4b-i`** | 🔴 | 🔴 | 🔴 | 🔴 | 🔴 | ✅ |
| **`K4b-ii`** | ⚠️ **`[OPEN]`** — grounds may be lane-relative | 🔴 | 🔴 | 🔴 | 🔴 | ⚠️ corroboration arm still `[OPEN]` |
| **`K4c-i`/`K4c-ii`** | 🔴 — identity is estate-wide | 🔴 | 🔴 | 🔴 | 🔴 | ⚠️ `Q1a`-conditional |
| **`K5`** | 🔴 — an external ground is not holder-relative | 🔴 | 🔴 | 🔴 | 🔴 | ✅ restated form |

$$\boxed{\begin{array}{c}\textbf{TWO cells have a mis-specified SUBJECT } (K2,\ K4a\text{-ii})\textbf{; ZERO cells gain or lose independence.}\\ \boxed{\textbf{representational ARITY} \neq \textbf{semantic OBLIGATION} \neq \textbf{persistence CAPABILITY — and only the first changed.}}\end{array}}$$

# PART IX — the transition taxonomy re-checked

> *Can withdrawal for `h₁` while `p₁` remains current for `h₂` still be the same base transition
> family?*

⭐ **Yes.** With `Current(h,p)`: **withdrawal = removal from `h`'s current set + retention + warrant.**
**Supersession = that + a holder-indexed replacement claim.**

$$\boxed{\begin{array}{c}\textbf{The NESTED structure SURVIVES. Only the base's description changes:}\\ \textbf{SET-MEMBERSHIP CHANGE replaces SCALAR ASSIGNMENT, and both currentness and replacement are HOLDER-INDEXED.}\\[6pt] \boxed{\textbf{MODEL B — } P\text{-}21 \textbf{ survives WITH EXPLICIT SUBJECT INDEXING.}}\end{array}}$$

⛔ **Model `A` refused** *(it would ignore `H4`)*; ⛔ **Model `C` refused** *(the plural structure is
corpus-attested, so *holder* is not purely implementation)*; ⛔ **Model `D` refused** *(no new obligation
— Part VII)*; ⛔ **Model `E` refused** *(the evidence decides)*. ⭐ **And `A` was not preferred for
preserving the prior result.**

# PART X — formal / statistical audit

| notion | finding |
|---|---|
| ⭐ **is holder identity a nuisance parameter, a structural variable, or part of the state?** | ⭐⭐⭐ **A STRUCTURAL VARIABLE** — `H-S2` shows the model is **incoherent** without it, which a nuisance parameter never is |
| **is holder cardinality observable from the corpus, or latent?** | ⭐ **OBSERVABLE at the document level** *(distinct registers with distinct decision logs)*; ⚠️ **latent as a formal relation** |
| **identifiability** | `Current(h,p)` and `Replaces(h,p₂,p₁)` are identifiable; their **unindexed** forms are not |
| **observational equivalence** | ⭐ the unindexed model makes `H-S2` **observationally equivalent to a contradiction** |
| **sufficient statistic / state sufficiency** | the unindexed state is **not sufficient** |
| **conditional independence** | ⭐ `K4a-i` is **independent of the holder index**; `K2` and `K4a-ii` are **not** |

⛔ **"Identifiable" is not used as a synonym for "ontologically distinct" — and indeed the indexing
requirement is NOT promoted to a new entity.**

# PART XI — DDD audit

| question | classification |
|---|---|
| is **Holder** a named domain concept? | ⭐ **`[OPEN]` — NOT named.** A plural register structure is `[EMP]`; the *concept* is mine |
| is **Assertion** named? | `[EMP]` |
| is **Currentness** named? | ⚠️ **weakly `[EMP]`** — status values, `[RF]`, *"operative"* |
| is **Supersession** a named domain event? | ⚠️ **`[OPEN]`** — `P-21` §13's finding stands: different historical meanings, not two named events |
| is **Withdrawal** a named domain event? | ⭐ **`[EMP]` as an ACT** — recorded withdrawals with warrants |
| ⭐ **is replacement holder-relative in the source?** | ⭐ **`[OPEN]`** — the source shows incompatible standing claims across registers, but never states a holder-indexed replacement |
| is holder multiplicity explicit? | ⭐⭐ **`[CORROBORATION]`** — plural registers exist; multiplicity of *assertion holders* is not stated |

⛔ **No aggregate, entity, value object, command or domain event invented.** ⭐ **DDD corroborates the
plural structure and creates nothing.**

---

# PART XII — ⭐⭐⭐ The central adversarial test

**`P-21`'s strongest claim:** *"exactly ONE datum is required for identifiability: the role-replacement
claim."*

$$\boxed{\begin{array}{c}\textbf{Attacked under } H3/H4 \textbf{ and it does NOT survive unqualified.}\\[4pt] \boxed{\begin{array}{l}\mathbf{H\text{-}S2} \textbf{ shows a GLOBAL } \mathit{Replaces}(p_2,p_1) \textbf{ CONTRADICTS } h_2\textbf{'s standing claim.}\\ \textbf{So the one datum must be } \mathit{Replaces}(h,\ p_2,\ p_1) \textbf{ — HOLDER-INDEXED.}\end{array}}\\[6pt] \boxed{\mathbf{P\text{-}21\textbf{'S RESULT IS CONDITIONALLY CORRECT UNDER A SINGLE-HOLDER STATE REPRESENTATION.}}}\end{array}}$$

⭐ **Stated exactly as the commission required.** ⚠️ **And the count is unchanged: still ONE datum — it
simply has an extra argument.** ⭐⭐ **`P-21`'s conclusion is therefore `[QUALIFIED]`, not `[REFUTED]`.**

# PART XIII — the over-reaction check

| is the issue… | verdict |
|---|---|
| 1 **ontology** | ⚠️ **partly** — the plural register structure is `[EMP]`; *"holder"* is my name for it |
| 2 **state representation** | ⭐ ✅ **yes** — currentness is a **relation**, not a field |
| 3 ⭐⭐⭐ **INDEXING** | ⭐⭐⭐ ✅ **PRIMARILY THIS** |
| 4 **transition semantics** | ⚠️ **description only** — the nested structure is untouched |
| 5 **persistence obligation** | ⭐ 🔴 **NO** — Part VII: every requirement is an index on an existing cell |
| 6 **notation** | ⚠️ partly — *"field"* and *"role"* were both wrong words |

$$\boxed{\textbf{⛔ NO cell added. NO cell split. NO new transition type. Cardinality UNCHANGED at 11.}}$$

---

# FINAL VERDICT

| # | question | answer |
|---|---|---|
| **1** | single-holder cardinality corpus-established? | ⭐ **NO — `[REFUTED]` as a general claim** |
| **2** | multiple-holder cardinality corpus-established? | ⭐⭐ **`[EMP]` at the document level** *(plural registers with their own decision logs)*; **`[DERIVED]` as a formal relation** |
| **3** | can currentness be holder-relative? | ⭐⭐⭐ **YES — `[EMP]` documents, `[DERIVED]` characterization.** Two registers assert incompatible standing claims about the same subject matter |
| **4** | is *holder* theory or representation? | ⭐⭐ **the plural STRUCTURE is theory-relevant `[EMP]`; the WORD *holder* is mine — `[QUALIFIED]`** |
| **5** | is `K2`'s subject correctly specified? | ⭐ **NO — it preserves a RELATION `Current(h,p)`, set-valued, not a scalar field. `[QUALIFIED]`** |
| **6** | is `K4a-i`'s subject correctly specified? | ⭐ **YES — GLOBAL to the estate, holder-independent** *(`P-08` §5.2's process-reliability ground is process-wide)* |
| **7** | is `K4a-ii`'s replacement relation correctly specified? | ⭐⭐⭐ **NO — it must be `Replaces(h,p₂,p₁)`.** And ⭐ *"role"* is dropped as `[ARCH]` |
| **8** | does plurality create a new persistence obligation? | ⛔ **NO** |
| **9** | does it change the 11-cell kernel? | ⛔ **NO** |
| **10** | does it change `P-21`'s nested result? | ⭐ **NO in structure; YES in description** — set-membership change, holder-indexed |
| **11** | does `τ10` need further qualification? | ⭐ **YES — one addition:** its base is a **holder-indexed set-membership change**, not a scalar assignment |
| **12** | does Gate `C` change? | ⛔ **NO** — no transition class added; `M6` remains its blocker |
| **13** | any previous result refuted? | ⭐⭐ **YES — `H1` single-holder, and `P-21`'s *"field"* formulation.** Both `[QUALIFIED]`/`[REFUTED]` as wording, not as structure |
| **14** | any previous result strengthened? | ⭐⭐ **YES — `K4a-ii`.** `H-S2` gives it a **second, independent witness**; and ⭐ **`K4a-i`'s holder-independence is newly explicit** |
| **15** | ⭐ **single next question** | ⭐⭐⭐ **Is the ESTATE one holder or the UNION of its registers — and does deletion-impermissibility bind the UNION or each register separately?** *(Derived from Part V's asymmetry: `K4a-i` is global while `K2` and `K4a-ii` are holder-relative, so the scope of "the estate" is now load-bearing. It also decides `K3b`'s `[OPEN]` subject.)* |

# STOPPING CONDITION

$$\boxed{\textbf{STOP-B — plural / holder-relative model established; } P\text{-}21 \textbf{ survives WITH EXPLICIT SUBJECT INDEXING.}}$$

⛔ **Not `STOP-A`** *(single-holder is refuted)* · ⛔ **not `STOP-C`** *(the plural structure is
corpus-attested, so the concept is not purely representational)* · ⛔ **not `STOP-D`** *(no independent
obligation — Part VII)* · ⛔ **not `STOP-E`** *(the evidence decides)*. ⭐ **Search stopped at the first
justified closure.**

# Provenance
**`[EMP]`** plural registers with their own decision logs · MD-017 `unresolved_equivalence` as the
**default** across 125 files · `definition → candidate` is `0..N` · withdrawals recorded as **acts with
warrants** · two registers' incompatible standing claims about the same subject matter.
**`[DERIVED]`** ⭐⭐⭐ **currentness is a RELATION, set-valued, holder-indexed** · ⭐⭐ **replacement must be
holder-indexed** · ⭐ **`K4a-i` is holder-independent and global** · **holder identity is a STRUCTURAL
variable, not a nuisance parameter** · **every plurality requirement is an INDEXING requirement, never a
new obligation** · the nested transition structure survives.
**`[QUALIFIED]`** ⭐ *"holder"* as a term · `P-21`'s *"current-assertion field"* · `P-21`'s *"one datum"*
*(now holder-indexed)* · `K2`'s and `K4a-ii`'s stated subjects.
**`[REFUTED]`** ⭐ `H1` single-holder as a general claim · *assertion is the bearer of currentness*.
**`[ARCH]`** ⭐ *"role"* — dropped. **`[CORROBORATION]`** the DDD audit.
**`[OPEN]`** ⭐ is a **warrant** holder-relative (`K3b`) · are **grounds** register-relative (`K4b-ii`) ·
role-specific currentness · whether the source **names** a holder concept or a holder-indexed
replacement · `K4b-ii`'s corroboration arm · everything carried forward.
⛔ **No Lane T governance artifact was used as corpus evidence; no `[ARCH]` promoted; nothing rewritten.**

---

```
STOP-B — PLURAL / HOLDER-RELATIVE MODEL ESTABLISHED. P-21 SURVIVES WITH EXPLICIT SUBJECT INDEXING.
KERNEL REMAINS 11 CELLS — NO CELL ADDED, SPLIT OR DELETED.

THE FINDING THAT COMES FIRST: "HOLDER" IS NOT CORPUS-ATTESTED. I COINED IT IN P-21 §4, and it began
doing substantial theoretical work immediately. A SECOND unattested term sits in the same sentence:
"ROLE" — P-21's "role-replacement claim". The claim's CONTENT survives (p2 answers the question p1
answered) but "role" is [ARCH] and is DROPPED. Of the eight subject levels, THREE are unattested
(assertion occurrence, holder, role) and one of those is not used at all.

AND THE CORPUS DENIES P-21'S FORMULATION DIRECTLY: MD-017's unresolved_equivalence is the DEFAULT across
125 files, and definition -> candidate is 0..N [OPEN]. The estate holds COMPETING claims
SIMULTANEOUSLY. So "the holder's current-assertion FIELD" presupposes a single-valuedness the corpus
explicitly denies. CURRENTNESS IS A RELATION, SET-VALUED: Current(h, p), not a scalar field.

HOLDER CARDINALITY: H1 (exactly one) is REFUTED as a general claim. H2 (zero-or-one) is [EMP] — a
withdrawal without successor leaves NO current claim. H3 (multiple) is [EMP] — plural registers with
their own decision logs. H4 (holder-relative currentness) is [EMP] at the document level: two registers
assert INCOMPATIBLE STANDING CLAIMS about the same subject matter. Admissibility stated plainly: the
DOCUMENTS are corpus evidence; my characterization is [DERIVED]; and Lane T's own governance artifacts
(20, 20a, the intakes) are cited as STRUCTURE, never as corpus evidence.

THE CENTRAL ADVERSARIAL TEST — P-21's "exactly ONE datum" does NOT survive unqualified. H-S2 (h1
supersedes p1 while h2 keeps p1 current) shows a GLOBAL Replaces(p2,p1) CONTRADICTS h2's standing claim.
So the datum must be Replaces(h, p2, p1) — HOLDER-INDEXED.
  => P-21'S RESULT IS CONDITIONALLY CORRECT UNDER A SINGLE-HOLDER STATE REPRESENTATION.
  The COUNT is unchanged — still one datum, with one more argument — so P-21 is [QUALIFIED], not
  [REFUTED].

AN ASYMMETRY WORTH NAMING: K2 and K4a-ii are HOLDER-RELATIVE; K4a-i is GLOBAL. K4a-i is retained by the
ESTATE, because P-08 §5.2's ground — evidence about the PROCESS's reliability — is process-wide rather
than holder-wide. Five candidate readings of "retained by whom?" were tested and only the global one
survives.

NO OVER-REACTION: every plurality requirement classifies as an INDEXING requirement on an EXISTING cell
— never a new persistence obligation. Representational ARITY != semantic OBLIGATION != persistence
CAPABILITY, and only the first changed. Holder identity is a STRUCTURAL variable (the model is
INCOHERENT without it, which a nuisance parameter never is), and "identifiable" was not used as a
synonym for "ontologically distinct".

TWO PREVIOUS RESULTS QUALIFIED (H1 single-holder; P-21's "field" wording), and TWO STRENGTHENED: K4a-ii
gains a SECOND independent witness from H-S2, and K4a-i's holder-independence is newly explicit.
TAU-10 gains one qualification: its base is a HOLDER-INDEXED SET-MEMBERSHIP CHANGE, not a scalar
assignment. The NESTED transition structure is untouched. Gate C unchanged; M6 remains its blocker.

NEXT QUESTION (one): IS THE ESTATE ONE HOLDER OR THE UNION OF ITS REGISTERS — and does
deletion-impermissibility bind the UNION or each register separately? Derived from Part V's asymmetry,
and it also decides K3b's [OPEN] subject.

NO ARCHITECTURE — NO SCHEMA v3 — NO CANONICAL THEORY — 3MC UNTOUCHED — NO SILENT REPAIR.
```

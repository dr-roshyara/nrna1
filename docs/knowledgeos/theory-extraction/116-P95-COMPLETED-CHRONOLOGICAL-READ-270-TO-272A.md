# `P-95` — Completed Chronological Read: `270 → 271 → 273 → 276 → 272a`

**2026-09-09 · Lane T.** After [`115` `P-94`](./115-P94-CHRONOLOGICAL-LINEAGE-TRACE.md).

⛔ **Nothing defined, repaired, ratified, selected or merged · no 3MC · `|K| = 11 · [REC] · UNFROZEN`.**

# 0. ⭐⭐⭐⭐⭐ The finding the complete read produced

$$\boxed{\begin{array}{c}\mathbf{272A.17\ —\ "MANDATORY\ DISTINCTION\ REGISTER"\ —\ NINETEEN\ ENTRIES,}\\ \mathbf{EACH\ PAIRED\ WITH\ ITS\ REQUIRED\ OPERATION.}\\[4pt] \textbf{⭐⭐ *"This is the } \mathbf{requirements\ basis} \textbf{ for the next } K \textbf{ derivation."*}\end{array}}$$

⭐⭐⭐ **That is `ℛ_req`'s true ancestor — and every entry is a `⟨distinction, operation⟩` PAIR.**
⛔⛔ **The 2026-09-02 inventory has NO OPERATION COLUMN.**

$$\boxed{\begin{array}{c}\textbf{⭐⭐⭐⭐⭐ The decisive mathematical change is not } predicate \to set \textbf{ or } criterion \to list.\\[4pt] \textbf{It is that } \mathbf{THE\ OPERATION\ INDEX\ WAS\ DROPPED.}\\[4pt] \textbf{Without an operation, } \textit{"required"} \textbf{ has no referent — so a } \mathbf{standard} \textbf{ field had to be invented,}\\ \textbf{and } \mathbf{that} \textbf{ is where acceptance semantics became owed.}\end{array}}$$

# 1. Read-status, stated first

| document | lines | status |
|---|---|---|
| ⭐ `step_270` 20:28 | 1 276 | ⭐⭐ **READ COMPLETELY** |
| ⭐ `step_271` 20:49 | 1 387 | ⭐⭐ **READ COMPLETELY** |
| `step_273` 21:44 | 1 312 | ⭐ **READ COMPLETELY** *(`P-94` + §§1–40, 942–1010)* |
| `step_276` 21:54 | 883 | ⚠️ **§276.4 + map only — PARTIAL** |
| ⭐ `step_272a` 22:42 | 1 430 | ⭐ **READ: map, §§0–1, 17, 25–28 — SUBSTANTIAL, not complete** |
| `step_272b` 22:50 | 2 126 | ⚠️ **§§272A.1–3, 571, 969 — PARTIAL** |
| ⛔ **`step_281` 23:44** | 1 445 | ⛔⛔ **NOT READ** |
| ⛔ `182008` | 1 864 | ⛔ **§§1, 16, 17 only — PARTIAL** |

⛔ **No conclusion below rests on `281` or on `182008`'s unread sections.**

# 2. ⭐⭐⭐ Three documents are MANDATES, not derivations

⭐ **`270`** (§270.0 *Mandate*, §270.26 *Required final output*, §270.27 *Final verdict format*) ·
⭐ **`273`** (§273.0 *Mandate*, §273.35 with empty slots A–O) — ⛔ **both commission work.**
⭐⭐ **`271` is NOT a mandate** — *"Step 270 established the correct audit direction. The next step must
now take its results one level further"* — ⭐ **it is the first genuine derivation in the chain.**
⭐⭐ **`272a` is a self-declared RETROFIT:** *"**Step 272A was intended to be derived first, and our
later work jumped over that derivation.** … the operation universe must precede the proof of `K`
minimality."* ⇒ ⭐⭐⭐ **`P-94`'s inference is now `[EMP]` — stated by the document itself**, which lists
`273` as its **Successor** while being written **58 minutes after it**.

# 3. Deliverable 2 — the discrimination-criterion lineage (Ledger A)

| | time | exact formulation | mathematical change |
|---|---|---|---|
| ⭐ **`C₀`** | `270.22` **20:28** | `π₁ ≡_{𝒪_π} π₂ ⟺ ∀x ∈ X : Eval_{π₁}(x) = Eval_{π₂}(x)`, then ⭐ *"what information must survive for all mandatory operations to remain distinguishable?"* | ⭐ **INTRODUCE** — over **policies**, ⛔ **commissioned, not derived** |
| ⭐⭐ **`C₁`** | `271.7` **20:49** | `π₁ ≡ π₂ ⟺ ∀x ∈ 𝒪_A: ⟦π₁⟧_A(x)=⟦π₂⟧_A(x) ∧ ∀y ∈ 𝒪_T: ⟦π₁⟧_T(y)=⟦π₂⟧_T(y)` | ⭐⭐ **REFINE — the operation set SPLITS into `𝒪_A` and `𝒪_T`.** *"Policy identity = behavioral identity — **only relative to a declared operation set**"* |
| `C₂` | `273.9` **21:44** | `∃o ∈ 𝒪_core : o(K,x) ≠ o(K^{-X},x)` | ⭐ **SPECIALIZE to `K`** — ⚠️ **and the quantifier merges `𝒪_A`/`𝒪_T` back into one `𝒪_core`** |
| `C₃` | `276.4` **21:54** | *"does removing `o` make a requirement **impossible to express or verify**?"* | ⭐⭐ **RETYPE — output-inequality → expressibility/verifiability.** ⛔ **Not the same predicate** |
| `C₄` | `272b` **22:50** | `σ₁ ≡_E σ₂ ⟺ ∀o ∈ O_E: Obs_o(σ₁,x)=Obs_o(σ₂,x)` | **SPECIALIZE to `Σ`**; ⭐ `Obs_o` replaces bare application |
| `C₅` | `182016` §1.1.2 | *"`d_i` kept iff `∃TV_i` where collapsing `d_i` alters `τ(TV_i)`"* | ⭐ **the quantifier becomes `𝒯`** |

## ⭐⭐⭐ Testing `P-94`'s *"the mathematics never changed"* — **REFUTED**

⛔ **It changed at least three times:**
**①** `C₀→C₁` — **one operation set → two** (`𝒪_A`, `𝒪_T`), then **silently re-merged** at `C₂`.
**②** ⭐⭐ `C₂→C₃` — **output-inequality → expressibility.** `o(K,x) ≠ o(K^{-X},x)` is a **decidable
comparison of two outputs**; *"impossible to express or verify"* is a **modal claim about a language**.
⛔ **Not equivalent.**
**③** `C₃→C₄` — the applied `o(·)` becomes an **observation function `Obs_o`**, i.e. an explicit
observation channel.
⭐ **What is continuous is the SHAPE — *distinguishability under a declared operation set* — not the
mathematics.** ⭐⭐ **`P-94`'s sentence is withdrawn and replaced by that.**

# 4. Deliverable 3 — the `ℛ_req` lineage (Ledger B), and the transition finally located

| | time | object |
|---|---|---|
| ⭐⭐ **`R₀`** | ⭐ **`272A.0` 22:42** | ⭐⭐⭐ **`Requirements(K)`** in the chain `𝒪_core → **Requirements(K)** → Candidate(K) → Minimality(K) → Σ` |
| ⭐⭐⭐ **`R₁`** | ⭐ **`272A.17` 22:42** | ⭐⭐⭐ **the *Mandatory Distinction Register* — 19 `⟨distinction, operation⟩` pairs** |
| `R₂` | `273.29` 21:44 | ⚠️ 12 **counterexample types** — ⛔ **a different list, and earlier** |
| `R₃` | `181000` 09-02 | `ℛ_req = {d₁…d_k}` on `S` — ⛔ **no operation column** |
| `R₄`–`R₆` | 09-02 | +compositionality (external) · tiered inventory · `ℛ_req(Q,Γ)` |

$$\boxed{\begin{array}{c}\textbf{⭐⭐ } \mathbf{[EMP]}\textbf{: } \mathcal O_{core} \to Requirements(K) \textbf{ is stated as a dependency at } 272A.0.\\[4pt] \textbf{⛔ } \mathbf{[UNWITNESSED]}\textbf{: } Requirements(K) \to \mathcal R_{req}. \textbf{ No document performs it,}\\ \textbf{and the 19 paired entries do } \mathbf{not} \textbf{ appear in any later inventory.}\end{array}}$$

⭐ **Classification of the transition: `[REINTERPRETATION]`, not `[DERIVATION]`.**

## ⭐⭐ The count series, with its true origin
$$\mathbf{19}\ (272A.17,\ \textbf{paired}) \;\cdot\; 12\ (273.29,\ \textbf{counterexamples}) \;\cdot\; 47 \;\cdot\; 12 \;\cdot\; 11 \;\cdot\; 10 \;\cdot\; 8$$
⭐⭐⭐ **And `272A.25`'s `𝒪_sem^candidate` has exactly 19 members** (`5+6+4+2+2`) — ⭐ **one per
distinction.** ⚠️ **That 19 is the same 19 the second-order register cites as *"a 19-operation
`𝒪_sem`"*** *(`P-90` §0.2)* — **cross-lane confirmation.**

# 5. Deliverable 4 — `r`'s lineage, resolved

$$\boxed{\textbf{⭐⭐⭐ } r \textbf{ is born at } \mathbf{270.6}\textbf{, 20:28, as a } \mathbf{RULE}\textbf{: } \pi = \{r_1, r_2\},\ r_1(x)=Permit,\ r_2(x)=Deny.}$$

⭐ **`270.4`: `Policy = Set(Rule)` vs `⟦π⟧ : X → Decision`.** ⇒ **`r` ∈ a policy, not ∈ `ℛ_req`.**
⛔⛔ **The `r` of `Sat(K_t,r)` is a 7-field *requirement record*. Same letter, three objects
(`rule` · `representation` · `requirement`), and ⛔ no document connects them.**

# 6. Deliverable 5 — acceptance, and Deliverable 6 — `Sat`

⭐⭐⭐ **`π` is a THREE-WAY collision:** **policy** (`270`, `271`) · **projection** (`181000`, `182016`) ·
and in `𝒪_π`/`Π` the **policy space**. ⛔ **`Loss_{ℛ_req}(π)` is about a projection; `A₋₁` was about a
policy.**
⭐ **Ancestor located:** `270.11`'s `AssessEvidence : ER* × Policy × Context × EvidenceGraph →
EvidenceAssessment` and `271.28`'s `Assessment : (P,E,C,π) → A` ⇒ **`EVal(K,R,Q,Γ)`.**
⛔ **`Sat` — still no ancestor in `270`–`272a`.** ⭐⭐ **And now with a reason: this lane's evaluator is
`Assessment`/`EVal`, commissioned at `270.11`, and `Sat` never enters it.**

# 7. Deliverable 7 — `EVal`/`Det`, and its firebreak's origin

⭐⭐⭐ **`182017`'s `Det ≠ Truth ≠ Knowledge ≠ Belief` has an explicit ancestor:**
⭐ **`270.16`:** `Evidence ≠ Assessment ≠ Inference` · `EvidenceAssessment ≠ Truth`
⭐ **`271.11`:** *"`Assessment(P,E,C,π) = Strong` does **not** imply `P = True`"* · `Assessment ≠ Truth`
⇒ ⭐⭐ **`[DERIVED]` continuity, three days, with explicit restatement at each step.** ⛔ **Still no
bridge to `Sat`.**

# 8. ⭐⭐⭐⭐ Deliverable 9 — the unsupported transitions, now precisely located

| transition | status |
|---|---|
| `C₀ → C₁` | ⭐ **`[EMP]`** — `271.7` says *"this mirrors the earlier state minimality methodology"* |
| `C₁ → C₂` | ⭐ **`[EMP]`** — `271.36` commissions it, *"the same rigorous minimality method"* |
| ⚠️ `C₂ → C₃` | ⛔ **`[CONTRADICTED]` as identity — the predicate changed** |
| `𝒪_core → Requirements(K)` | ⭐⭐ **`[EMP]`** — `272A.0`'s dependency chain |
| ⛔⛔ **`Requirements(K) → ℛ_req`** | ⭐⭐⭐ **`[UNWITNESSED]` — and this is the load-bearing gap** |
| ⛔ `ℛ_req → r` | **`[UNWITNESSED]`** — `r` predates `ℛ_req` and is a rule |
| ⛔ `Loss(π) → Sat(K_t,r)` | **`[UNWITNESSED]`** |

# 9. ⭐⭐ Deliverable 8 — the type change that actually matters

⭐ Seven type events stand from `P-94`. ⭐⭐⭐ **But the load-bearing one is now different from what
`P-94` said.** ⛔ **Not *criterion → list*.** It is:

$$\boxed{\langle \textit{distinction},\ \textit{operation}\rangle \textbf{ PAIRS } \;\longrightarrow\; \textbf{BARE distinctions}}$$

⭐⭐ **`272A.17` indexes every distinction by the operation that requires it. `181000` onward index
none.** ⇒ ⭐⭐⭐ **`"required"` lost its referent, and `standard`/`Accept_r` were invented to replace
it.** ⛔ **`182019`'s `ℛ_req(Q,Γ)` restores a parameter — but `(Q,Γ)`, not the operation.**

### 10. THE REVISED DECISIVE CONCLUSION

$$\boxed{\begin{array}{c}\textbf{⭐⭐⭐ } \mathbf{SEVERAL\ LINEAGES\ AROUND\ ONE\ CONTINUOUS\ SHAPE\ —\ not\ one\ continuous\ mathematics.}\end{array}}$$

⭐ **What is continuous is *distinguishability under a declared operation set*, `270 → 182016`.**
⛔ **The mathematics changed at least three times** (§3), so `P-94`'s *"the mathematics never changed"*
is **withdrawn**.
⛔ **`r`, acceptance and `Sat` are separate objects with separate births** — `r` at `270.6` as a rule,
acceptance at `270.11`/`271.28` as an assessment interface, `Sat` **nowhere in this lane**.
⭐⭐⭐ **And the conflation is now located precisely: at the loss of the operation index between
`272A.17` and `181000`.**

### 11. THE ONE NEXT ACTION

$$\boxed{\begin{array}{c}\textbf{⭐⭐ Read } \mathbf{step\_281\ (23{:}44,\ 1\,445\ lines)\ completely} \textbf{ — the one unread document}\\ \textbf{between } 272a \textbf{ and the 09-02 batch, and therefore the only place the corpus could yet}\\ \textbf{record the } \mathbf{Requirements(K) \to \mathcal R_{req}} \textbf{ transition.}\end{array}}$$

⭐ **Its title — *"correction and missing part"* — and its `281.16` *"the complete `E4` test passes only
if every required distinction is preserved"* put it squarely on this lineage.** ⛔ **Until it is read,
`[UNWITNESSED]` on that transition is provisional.**

```
COMPLETED CHRONOLOGICAL READ 270 -> 271 -> 273 -> 276 -> 272a.

THE FINDING: 272A.17 is a "MANDATORY DISTINCTION REGISTER" of NINETEEN entries, each paired with its
required operation, and it says of itself "This is the REQUIREMENTS BASIS for the next K derivation."
That is R_req's true ancestor — and every entry is a <distinction, operation> PAIR, while the 2026-09-02
inventory has NO OPERATION COLUMN. So the decisive mathematical change is not predicate-to-set or
criterion-to-list: IT IS THAT THE OPERATION INDEX WAS DROPPED. Without an operation, "required" has no
referent, so a "standard" field had to be invented — and that is where acceptance semantics became owed.

READ STATUS: 270 and 271 READ COMPLETELY; 273 complete; 272a substantial (map, §§0-1, 17, 25-28); 276,
272b and 182008 PARTIAL; step_281 NOT READ. No conclusion rests on 281.

THREE DOCUMENTS ARE MANDATES, NOT DERIVATIONS: 270 and 273 commission work; 271 is the first genuine
derivation; and 272a SELF-DECLARES the retrofit — "Step 272A was intended to be derived first, and our
later work jumped over that derivation" — listing 273 as its Successor while written 58 minutes after
it. P-94's inference is now [EMP].

"THE MATHEMATICS NEVER CHANGED" IS REFUTED. It changed at least three times: C0->C1 split one operation
set into two (O_A, O_T) which C2 silently re-merged; C2->C3 RETYPED output-inequality into
expressibility, and "o(K,x) != o(K^-X,x)" is a decidable comparison of two outputs while "impossible to
express or verify" is a modal claim about a language; C3->C4 turned applied o into an observation
function Obs_o. What is continuous is the SHAPE — distinguishability under a declared operation set —
not the mathematics. P-94's sentence is withdrawn.

r IS BORN AT 270.6, 20:28, AS A RULE: pi = {r1, r2} with r1(x) = Permit and r2(x) = Deny, under
"Policy = Set(Rule)". So r belongs to a policy, not to R_req, and the r of Sat(K_t,r) is a seven-field
requirement record — same letter, three objects, no document connecting them. And pi is a three-way
collision: policy, projection, and policy space.

The Det != Truth != Knowledge != Belief firebreak has an explicit ancestor: 270.16's "EvidenceAssessment
!= Truth" and 271.11's "Assessment = Strong does not imply P = True" — [DERIVED] continuity across three
days with restatement at each step. Still no bridge to Sat.

THE LOAD-BEARING UNSUPPORTED TRANSITION IS Requirements(K) -> R_req: [UNWITNESSED]. O_core ->
Requirements(K) is [EMP] from 272A.0's dependency chain, but nothing performs the step to R_req, and the
nineteen paired entries appear in no later inventory. The count series' true origin is 19 (272A.17,
paired) — the same 19 the second-order register cites as "a 19-operation O_sem".

REVISED CONCLUSION: SEVERAL LINEAGES AROUND ONE CONTINUOUS SHAPE, not one continuous mathematics. The
conflation is located precisely at the loss of the operation index between 272A.17 and 181000.

NEXT ACTION: read step_281 completely — the one unread document between 272a and the 09-02 batch, and
therefore the only place the corpus could yet record the Requirements(K) -> R_req transition. Until
then, [UNWITNESSED] on that transition is provisional.

|K| = 11, [REC], UNFROZEN. No 3MC inspection.
```

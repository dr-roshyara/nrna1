# `P-91` — `ℛ_req`: Full Historical Reconstruction

**2026-09-09 · Lane T · archaeology.** After [`111` `P-90`](./111-P90-EKS38-GOVERNANCE-CONTROL-AUDIT.md).

⛔ **Nothing defined, chosen, ratified or selected · no 3MC · `|K| = 11 · [REC] · UNFROZEN`.**

# 0. ⭐⭐⭐ The headline

$$\boxed{\begin{array}{c}\mathcal R_{req} \textbf{ did not begin as a set of distinctions. It began as a } \mathbf{PREDICATE\ IN\ A\ DELETION\ TEST}\\[4pt] \textbf{— and the acceptance-semantics gap the programme is now blocked on is a } \mathbf{DIRECT\ CONSEQUENCE}\\ \textbf{of that object silently becoming a } \mathbf{LIST}.\end{array}}$$

⭐⭐ **A test needs no acceptance semantics. A list of requirements does.** ⛔ **Nothing supplied them at
the transition, because nothing recorded that a transition had occurred.**

# 1. Phase 1–2 — the earliest occurrence, found by search not by hint

**139 files** match across the permitted estate. Sorted earliest-first, the origin is
**`20260830-214401_step_273`** — ⭐ **three days before the `1810xx`/`1820xx` package I had been
treating as the origin in `P-87`/`P-88`.**

⚠️ **Methodological note:** in this block the **step numbers and the timestamps disagree** — `273` at
21:44, `276` at 21:54, **`272A/b` at 22:50**. ⭐ **The step numbers are the argument order; the
timestamps are not.** `EKS-35`, a fourth confirmation, and here it **inverts** the reading.

# 2. Part I / III — the lineage

## ⭐⭐⭐ `R₀` — `273.9`, **2026-08-30 21:44** — the deletion test

> *"Does removing `X` cause at least one **mandatory operation** to lose a **required distinction**?"*

$$\boxed{\exists o \in \mathcal O_{core} : \; o(K,x) \neq o(K^{-X},x) \;\Rightarrow\; X \textbf{ is required}}$$

⭐⭐⭐ **`ℛ_req` is not a noun here. *"Required"* is a PREDICATE ON COMPONENTS OF `K`**, and
⭐⭐ ***mandatory operation* and *required distinction* are born in ONE SENTENCE** — so `𝒯` and `ℛ_req`
are **co-original**, ⛔ **not one derived from the other.**
⭐ `273.10` adds the distinction the later work loses: ***"distinguish `Required` from `Primitive`"*** —
`E_L = f(R)` may be required yet **not primitive**.
⚠️ **The quantifier is over `𝒪_core`** — the forward plan's `𝒪_core`, *present in **34 of 66** worlds*.

## ⭐⭐ `R₀′` — `276.4`, **21:54** — the DUAL test

> *"construct a separating example where removing **`o`** causes loss of a required distinction. This is
> **required before calling `𝒪_core` minimal**."*

⇒ ⭐⭐⭐ **`ℛ_req` is the shared invariant of TWO DUAL minimality tests** — one deleting components of
`K`, one deleting members of `𝒪_core`. ⛔ **Neither is a requirement set.**

## ⭐⭐⭐ `R₁` — `272A.2–3`, **22:50** — the criterion, and four states

$$\boxed{\sigma_1 \equiv_E \sigma_2 \iff \forall o \in O_E,\ \forall x \in X_o:\ Obs_o(\sigma_1,x) = Obs_o(\sigma_2,x)}$$

> *"If there exists a **mandatory epistemic operation** for which two states produce different valid
> results, those states **cannot be collapsed**."*

⭐ *"`272A.3` Required distinctions — **we now derive the required distinctions one by one**."*
⇒ ⭐⭐⭐ **domain change: from components of `K` to EPISTEMIC STATUS values**, yielding

$$\boxed{\Sigma_0 = \{\textit{Unknown},\ \textit{Supported},\ \textit{Refuted},\ \textit{Conflict}\}}$$

⚠️ **with *"the mapping into that structure may be POLICY-DEPENDENT."***
⭐⭐⭐ **A four-state minimum, derived 2026-08-30 — three days before the `{0,1}` / `{⊤,⊥,𝖴}` / FDE
debate, and it maps one-for-one onto FDE's `N,T,F,B`.** ⛔ **Neither later thread cites it.**

## `R₂` — `281.16`, **23:44** — used as a pass/fail criterion
*"the complete `E4` test passes only if **every required distinction is preserved**."*

## ⭐⭐ `R₃` — `181000`, **2026-09-02 18:10** — the generalization, and the type change

$$\mathcal R_{req} = \{d_1,\dots,d_k\},\quad d = \textbf{an equivalence relation } \sim_d \textbf{ on } S; \qquad s_1 \nsim_d s_2 \Rightarrow \mathcal E(s_1) \neq \mathcal E(s_2)$$

⭐⭐⭐ **Two changes at once, neither marked:** ① the domain widens from **`Σ`** to **`S`, the whole state
space**; ② ⭐⭐ **the object becomes a SET where it was a PREDICATE.**
⭐ **The criterion itself survives intact** — it is `272A.2` with `Obs_o` replaced by `ℰ`.

## ⚠️ `R₄` — `181001`, same batch — **external**, and a fourth condition
`ℛ_req ⊆ 𝒟` under **Preservation · Non-collapse · ⭐ COMPOSITIONALITY · Minimality**.
⛔ **Self-titled *"PERPLEXITY RESEARCH RESPONSE"*.** ⭐⭐ **Compositionality has no ancestor in
`R₀`–`R₃`** — and it is the *"congruence conjunct"* two later lanes record as **missing**.

## ⭐⭐ `R₅` — `182013`, **18:20** — the inventory
**11–12 `D-`-identified distinctions**, P1/P2/P3 tiers, `[ADVISORY]`.
⭐⭐⭐ **This is where a TEST becomes a LIST**, and where `Allen relations`, `Kripke modality` and
`Levesque explicit/implicit` enter from literature *(`P-87` §5)*.

## `R₆` — `182016`/`182011` — completeness made conditional on `𝒯`
⭐ **`𝒯` returns — but as an *external index*, not as `273.9`'s `𝒪_core` quantifier.**

---

# 3. ⭐⭐⭐⭐⭐ Part I's discrepancy — **"11 conditions" vs "12 pairs"** — resolved

⭐ **The origin explains it, and they are different objects:**

| | what it counts | from |
|---|---|---|
| **pairs** | ⭐⭐ **`(σ₁, σ₂)` — the WITNESSES that a distinction is required** | `272A.2`'s criterion is over **two candidate states** |
| **conditions** | ⭐ the **test conditions** applied | the protocol set |
| **distinctions** | ⭐ the **partitions `d`** | `R₃`'s inventory |

$$\boxed{\textbf{⭐⭐ A } \mathbf{pair} \textbf{ is evidence FOR a distinction. They cannot be equal, and were never the same count.}}$$

⚠️ ⛔ **But `P-87` §4's finding is NOT explained by this** — that is a **third** mismatch, *inside one
document*: header **7** + **5**, rows **6** + **5**, summary **12**. ⭐ **Two separate confusions, and
only one is now resolved.**

# 4. Parts III–IV — `EC_t`, `Sat`, and where the edges actually are

| edge | class | note |
|---|---|---|
| ⭐ `𝒪_core → required` | ⭐⭐ **`[EMP]`** | ***the original definition***, `273.9` |
| `O_E → Σ_0` | ⭐ **`[DERIVED]`** | `272A.2–3` |
| `EC_t → Req(EC_t)` | ⚠️ **`[PROPOSED]`** | appears only at `R₅`/`R₆`; ⛔ **no ancestor in `R₀`–`R₃`** |
| ⭐⭐ `(EC_t, r) → AcceptanceCondition` | ⛔⛔ **`[UNWITNESSED]`** | ⭐⭐⭐ **and §5 explains why** |
| `𝒯 → ℛ_req` | ⚠️ **`[PROPOSED]`** — `P-90` §K.1's correction, and now also **historically**: they are **co-original**, so neither derives the other | |
| `ℛ_req → Sat` | ⚠️ **`[PROPOSED]`** | `Sat` enters at `R₅`-era, never in `R₀`–`R₃` |

⭐⭐⭐ **What did the earliest authors think a requirement asked the state to satisfy?**
$$\boxed{\textbf{⭐ } \mathbf{NOTHING.} \textbf{ A distinction was required iff } \mathbf{removing\ it\ changed\ an\ operation's\ OUTPUT.}}$$
⛔ **No acceptance set, no standard, no threshold, no predicate over content, no evidence condition.**
⭐⭐ **The test is purely DISCRIMINATIVE — and discrimination needs no acceptance semantics.**

# 5. ⭐⭐⭐⭐⭐ Part IX — the Requirement–Evaluation boundary, and why it is missing

$$\boxed{\begin{array}{c}\textbf{In } R_0\text{–}R_3 \textbf{ there is } \mathbf{no\ acceptance\ condition\ because\ none\ is\ needed.}\\[4pt] \textbf{⭐⭐ } AcceptanceCondition \textbf{ and } standard \textbf{ appear only once } \mathcal R_{req} \textbf{ became a } \mathbf{REQUIREMENT\ SET}\textbf{,}\\ \textbf{and } \mathbf{nothing\ supplied\ their\ semantics\ at\ that\ transition\ —\ because\ nothing\ recorded\ the\ transition.}\end{array}}$$

⭐⭐ **This is the archaeological explanation of `document6`'s finding** *(`r = (component_r, Accept_r)`
never takes `EC_t`; two bounded contexts, no stated connection)*: ⛔ **the connection was never lost —
it was never needed, and then never built.**

⭐ **Search for `Interpret(r, EC_t, …) → AcceptanceCondition` or any equivalent:**
$$\boxed{\mathbf{No\ corpus\text{-}grounded\ requirement\text{-}to\text{-}acceptance\ interpretation\ has\ been\ found.}}$$
⚠️ ⛔ **Stated as a negative with its scope: 139 files, permitted estate, `Accept`/`standard`/`Interpret`
probes with positive controls.** ⭐ **Not *"none exists"*.**

# 6. Part VI — false-continuity events, each recorded as a transition

| # | event |
|---|---|
| ⭐ **T1** | **PREDICATE → SET** (`R₃`), unmarked |
| ⭐ **T2** | domain **`Σ` → `S`** (`R₃`), unmarked |
| ⭐⭐ **T3** | **TEST → LIST** (`R₅`) — ⭐⭐⭐ **the transition that creates the acceptance gap** |
| ⭐ **T4** | **COMPOSITIONALITY added** from outside (`R₄`), no ancestor |
| ⚠️ **T5** | *"`Required` vs `Primitive`"* (`273.10`) **dropped**, never mentioned again |
| ⚠️ **T6** | `𝒪_core`'s quantifier replaced by `𝒯` as an external index (`R₆`) |
| ⭐ **T7** | filename `…RATIFIED…` vs body `[PROPOSED RATIFICATION]` *(relayed, `document6`)* |
| ⭐ **T8** | `181001` ≡ `181003`, byte-identical, second named *"-variant"* |
| ⭐⭐ **T9** | `Σ_0`'s **four states (08-30)** and FDE's **four values (09-02)** — ⛔ **neither cites the other** |

# 7. Part VII–VIII — external material and governance status

**External:** ⭐⭐ `R₄` wholesale; and ≥ **4 of 11** ratified distinctions are literature imports
*(`P-87` §5)* — ⛔ **unmarked in the package.**
**Governance, by version:** `R₀`–`R₂` **derivation steps, no status claimed** *(correct)* · `R₃`
**specification** · `R₄` ⛔ **external, unadopted** · `R₅` ⭐ **`[ADVISORY]`, *"READY FOR"* ratification,
Phase 3 `[CURRENT]`** · `R₆` **theorem inside the advisory package**.
$$\boxed{\textbf{⛔ No version of } \mathcal R_{req} \textbf{ has ever been ratified.}}$$

# 8. Part X — what survived, what changed

⭐ **Survived unchanged:** the **discrimination criterion** — *two things are distinct iff some operation
tells them apart*. ⭐⭐ **From `273.9` to `181000` it is the same mathematics.**
⭐ **Added:** a domain (`S`), a set-type, an inventory, tiers, `D-` IDs, compositionality, `𝒯`-conditional
completeness, `standard`, `Accept_r`.
⛔ **Removed:** `Required ≠ Primitive`; the dual operation-side test; `𝒪_core` as the quantifier.
⚠️ **Silently reinterpreted:** predicate→set, `Σ`→`S`, test→list.
⭐ **Imported:** compositionality, and ≥4 distinctions.
⭐⭐ **Actually derived:** the criterion, `Σ_0`'s four states, and the two dual tests. ⛔ **Everything
from `R₅` onward depends on the unmarked `T3`.**

# 9. THE DELIVERABLE

| | date | meaning | type | ↔ `EC` | ↔ `Sat` | status | source |
|---|---|---|---|---|---|---|---|
| **`R₀`** | 08-30 21:44 | *required* = removing it changes an operation's output | ⭐ **predicate on `K`-components** | ⛔ none | ⛔ none | derivation | `273.9` |
| **`R₀′`** | 08-30 21:54 | the dual, over `𝒪_core` | predicate on operations | ⛔ none | ⛔ none | derivation | `276.4` |
| **`R₁`** | 08-30 22:50 | distinctions over epistemic **status** | ⭐ criterion + **`Σ_0` (4 states)** | ⛔ none | ⛔ none | derivation | `272A.2–3` |
| **`R₂`** | 08-30 23:44 | a pass/fail test criterion | criterion | ⛔ none | ⛔ none | test | `281.16` |
| **`R₃`** | 09-02 18:10 | ⭐ **a SET of equivalence relations on `S`** | **set** | ⛔ none | ⛔ none | specification | `181000` |
| **`R₄`** | 09-02 18:10 | + **compositionality**, minimality | set, 4 conditions | ⛔ none | ⛔ none | ⛔ **external** | `181001` |
| **`R₅`** | 09-02 18:20 | ⭐ **an INVENTORY, 11–12 `D-` items, tiered** | **list** | ⭐ `Req(EC_t)` | ⭐ `Sat` appears | ⭐ **`[ADVISORY]`** | `182013` |
| **`R₆`** | 09-02 18:20 | completeness conditional on `𝒯` | theorem | via `Req` | via `Sat` | inside advisory | `182016` |

## A. Earliest genuine definition
⭐⭐ **A predicate in a deletion test:** *`X` is required iff `∃o ∈ 𝒪_core : o(K,x) ≠ o(K^{-X},x)`.*

## B. Current definition
⭐ **A tiered inventory of 11–12 named distinctions**, each an equivalence relation on `S`, with a
completeness theorem conditional on `𝒯` — **`[ADVISORY]`.**

## C. Semantic continuity
$$\boxed{\mathbf{EVOLVED\ OBJECT\ +\ COLLIDED\ OBJECTS.}}$$
⭐ **One criterion runs unbroken from `R₀` to `R₃`** — genuine continuity. ⛔ **But `R₅`'s inventory is a
different KIND of thing, and at least three counts (`11`/`12`, and the header/row/summary mismatch)
belong to different objects.**

## D. Requirement–Evaluation boundary
$$\boxed{\mathbf{ABSENT\ —\ and\ absent\ BY\ CONSTRUCTION,\ not\ by\ omission.}}$$

## E. Strongest corpus-grounded meaning of `r`
⭐ **In `R₀`–`R₃`: `r` does not exist.** ⭐⭐ **From `R₅`: a 7-field tuple `(id, type, scope, content,
standard, priority, validity)` whose field `standard` is ⛔ `[UNWITNESSED]`.** ⛔ **No stronger reading
is supported.**

## F. Strongest corpus-grounded meaning of `AcceptanceCondition`
⛔ **`Accept_r`, a set membership test in a surrogate construction** *(relayed, 3MC-lane,
`[UNVERIFIABLE]`)*, ⛔ **with no argument for `EC_t` and no corpus-defined source.** ⛔ **Nothing
stronger.**

## G. The exact blocker for computing `Sat`
$$\boxed{\begin{array}{c}\textbf{⭐⭐⭐ } \mathcal R_{req} \textbf{ changed from a } \mathbf{discrimination\ test} \textbf{ (which needs no acceptance semantics)}\\ \textbf{into a } \mathbf{requirement\ list} \textbf{ (which does), and no document supplied the acceptance semantics}\\ \textbf{at that transition or recorded that the transition occurred.}\end{array}}$$

## H. One next legitimate research action
$$\boxed{\begin{array}{c}\textbf{⭐⭐ Test whether the ORIGINAL discrimination criterion still } \mathbf{decides\ membership} \textbf{ of the}\\ \textbf{current 11–12 inventory — i.e. for each } D\text{-}n\textbf{, is there an operation whose output changes}\\ \textbf{when it is collapsed?}\end{array}}$$
⭐ **It needs no new theory, no acceptance semantics and no governance act** — the criterion and the
inventory both exist. ⭐⭐ **And it is decisive either way:** if the criterion decides them, `ℛ_req`
recovers a derivation and the acceptance gap narrows to `standard`; **if it does not, the inventory is
not the same object as the test — and that is worth knowing before anyone ratifies it.**
⚠️ ⛔ **It runs against `𝒪_core`, which is present in only **34 of 66** worlds — so the result would be
conditional, and must be reported that way.**

# 10. Backlog

⭐⭐ **`EKS-39` filed.** ⛔ Not `EKS-27`/`EKS-34`/`EKS-38` *(simultaneous collisions)*: this is
**sequential drift** — one name, one lane, a type that changed **three times** with each step locally
reasonable, ⭐⭐⭐ **and the programme's current blocker is the direct consequence.**

```
FULL HISTORICAL RECONSTRUCTION OF R_req.

THE HEADLINE: R_req did not begin as a set of distinctions. It began as a PREDICATE IN A DELETION TEST —
and the acceptance-semantics gap the programme is now blocked on is a DIRECT CONSEQUENCE of that object
silently becoming a LIST. A test needs no acceptance semantics; a list of requirements does. Nothing
supplied them at the transition, because nothing recorded that a transition had occurred.

THE ORIGIN, found by search: 139 matching files, earliest 20260830-214401_step_273 — three days before
the package P-87/P-88 treated as the origin. R_0 (273.9): "does removing X cause at least one MANDATORY
OPERATION to lose a REQUIRED DISTINCTION?", formally "exists o in O_core with o(K,x) != o(K^-X,x)". So
"required" is a PREDICATE ON COMPONENTS OF K, and mandatory-operation and required-distinction are born
in ONE SENTENCE — T and R_req are CO-ORIGINAL, neither derived from the other. R_0' (276.4) is the DUAL:
remove an operation instead, "required before calling O_core minimal". R_1 (272A.2-3) gives the
criterion — two states are indistinguishable iff every mandatory operation returns the same observation
— and derives Sigma_0 = {Unknown, Supported, Refuted, Conflict}, A FOUR-STATE MINIMUM ON 2026-08-30,
three days before the FDE/binary/ternary debate, mapping one-for-one onto FDE's N,T,F,B, with neither
thread citing the other.

Then on 09-02 the object changes twice in one batch, unmarked: the domain widens from Sigma to S, and a
PREDICATE becomes a SET; then an external Perplexity response adds COMPOSITIONALITY, which has no
ancestor and is exactly the "congruence conjunct" two later lanes record as missing; then the set
becomes a TIERED INVENTORY of 11-12 D-identified items — and THAT is where a TEST becomes a LIST.

THE 11-vs-12 DISCREPANCY IS RESOLVED BY THE ORIGIN: a PAIR is a witness that a distinction is required
(the criterion quantifies over two candidate states), while a DISTINCTION is a partition — they cannot
be equal and were never the same count. But P-87 §4's finding is a THIRD mismatch inside one document
(header 7+5, rows 6+5, summary 12) and is NOT explained by this.

WHAT DID THE EARLIEST AUTHORS THINK A REQUIREMENT ASKED THE STATE TO SATISFY? NOTHING. A distinction was
required iff removing it changed an operation's OUTPUT. No acceptance set, no standard, no threshold, no
evidence condition — the test is purely DISCRIMINATIVE, and discrimination needs no acceptance
semantics. That is the archaeological explanation of document6's finding: the EC-to-acceptance
connection was never lost — it was never needed, and then never built.

Nine false-continuity events recorded as transitions, including "Required vs Primitive" (273.10) being
dropped and never mentioned again, and O_core's quantifier being replaced by T as an external index.

No version of R_req has ever been ratified. R_0-R_2 claim no status, correctly; R_5 is [ADVISORY].

A. Earliest definition: a predicate in a deletion test.
B. Current: a tiered inventory of 11-12 named equivalence relations on S, completeness conditional on T,
   [ADVISORY].
C. Continuity: EVOLVED OBJECT + COLLIDED OBJECTS — one criterion runs unbroken from R_0 to R_3, but
   R_5's inventory is a different KIND of thing.
D. Requirement-Evaluation boundary: ABSENT — and absent BY CONSTRUCTION, not by omission.
E. Strongest meaning of r: it does not exist before R_5; from R_5, a 7-field tuple whose field
   "standard" is [UNWITNESSED].
F. Strongest meaning of AcceptanceCondition: Accept_r, a set-membership test in a surrogate
   construction, with no EC_t argument and no corpus-defined source. Relayed, [UNVERIFIABLE].
G. The blocker: R_req changed from a discrimination test into a requirement list, and no document
   supplied the acceptance semantics at that transition or recorded that it occurred.
H. Next action: test whether the ORIGINAL discrimination criterion still decides membership of the
   current inventory — for each D-n, is there an operation whose output changes when it is collapsed?
   No new theory, no acceptance semantics, no governance act needed; decisive either way; and
   conditional, because O_core is present in only 34 of 66 worlds.

Backlog: EKS-39 filed — sequential semantic drift under a stable name, distinct from the simultaneous
collisions already registered.

|K| = 11, [REC], UNFROZEN. No 3MC inspection.
```

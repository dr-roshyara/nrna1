# Phase 8 — `Zero` · Targeted Resolution

§9 verlangt **nicht** die Entscheidung `Zero = same` / `Zero ≠ same`, sondern:
> *„**historically reconstruct the transformation between the two claims**."*

## A · ⭐⭐⭐ Die Transformation ist bezeugt — in neun datierten Stufen

| | Datum | Form | Codomäne / Status |
|---|---|---|---|
| **①** | **08-22 01:21** | *„**Zero is a research lens, not a kernel primitive**"* · *„Zero does **not define architecture**"* | Klassifikation: **Research hypothesis** |
| **②** | **08-22 11:15** | **`Z-KOS-001` ✅ RATIFIZIERT** — *„meta-principle, **not a register row**"* · *„Zero **names** the anti-collapse **property**"* · *„Zero does not define truth; it **preserves the separation**"* | Meta-Prinzip, **außerhalb des Kernels** |
| **③** | **08-26 11:21** | $Z(K_t) = \text{„what the Zero Lens reveals about the boundary of } K_t\text{"}$ | ⭐ **erste Funktionsform**, Codomäne prosaisch |
| **④** | **08-26 16:15** | `Z(K_t) = {known unknowns, unresolved assertions, conflicts, missing representations, assumptions}` | 5-Element-Menge |
| **⑤** | **08-26 16:15/16:41** | $Z(K_t) \rightarrow Z_t = (U_t, C_t, A_t, M_t)$ | ⭐ **4-Tupel** — Unbekannte · Konflikte · unvalidierte Annahmen · fehlende Dimensionen |
| **⑥** | **08-26 17:42** | $Z(K_t) \rightarrow Z_t = (U_t, C_t, A_t, \mathbf{R_t})$ | ⚠️ **vierte Komponente kommentarlos gewechselt** |
| **⑦** | **08-27 18:31** | Zero-Algebra `step-025d`; `EC_G=(R_G,Γ_G)`; **`I-9`** Nicht-Kollaps-Norm | algebraisch |
| **⑧** | **08-29 14:45** | **`T5` — *„Zero **evaluates**, does **not constitute**, Knowledge"*** | Theorem der `δ`-Spur |
| **⑨** | **09-02** | `Zero(K_t,EC_t) ⟺ Δ_t = ∅` (v1.0 `DEF-22`) · **v1.2 degradiert** zu *candidate epistemic-boundary construct* · `Zero_strict ⇒ reasoned ⇒ weak` **`ESTABLISHED`** (1.62 Mio. Zuweisungen) | Prädikat / Kette |

⭐ **Der Übergang ist keine Umdeutung, sondern eine Kette von Konkretisierungen:**

$$\underbrace{\text{Linse}}_{①} \rightarrow \underbrace{\text{Meta-Prinzip}}_{②} \rightarrow \underbrace{\textbf{Detektor } Z(K_t)}_{③④⑤} \rightarrow \underbrace{\text{Algebra}}_{⑦} \rightarrow \underbrace{\text{Prädikat } \Delta=\varnothing}_{⑨}$$

⭐⭐ Und `②` enthält die Brücke bereits im Wortlaut: **„Zero *names* the anti-collapse
*property*"** — Zero ist schon auf der Meta-Ebene **ein Name für eine Eigenschaft**. Die
spätere Funktion `Z(K_t)` **berechnet**, was das Prinzip **benennt**. Das ist ein
`REFINEMENT`, keine Identität und kein Bruch.

## B · ⭐⭐⭐ Die Invariante, die **jede** Stufe überlebt

| Stufe | Abgrenzung |
|---|---|
| ① 08-22 01:21 | *„**not a kernel primitive**"* · *„does not define architecture"* |
| ② 08-22 11:15 | *„**outside the kernel**"* · *„not a dimension · not a knowledge object · not a lifecycle state · not an algorithm"* |
| ⑧ 08-29 | *„**does not constitute** Knowledge"* |
| ⑨ 09-02 | v1.2: *„**demoted** to candidate"* |

$$\boxed{\text{Vier unabhängige Restatements über 15 Tage. Jede Formalisierung wiederholt die negative Abgrenzung.}}$$

⭐ **Das ist der stärkste positive Befund dieser Phase.** Die frühe Kernel-Abgrenzung ist
nicht durch die Formalisierung verlorengegangen — sie wird **bei jedem Schritt erneuert**,
zuletzt von v1.2 selbst.

## C · Antwort auf §9

| | |
|---|---|
| `Zero_meta = Zero_property`? | ⛔ **nein** — keine Identitätsaussage existiert |
| `Zero_meta ≠ Zero_property`? | ⛔ **auch nicht** — die Lineage ist **bezeugt** |
| **Was gilt** | ⭐ **`REFINEMENT WITH PRESERVED DEMARCATION`** — das Prinzip **benennt** die Eigenschaft, die Funktion **detektiert** sie, das Prädikat **testet** sie; und die Abgrenzung *„nicht Teil des Kernels"* wird auf jeder Stufe mitgeführt |

⚠️ Damit ist Phase A's *„KEINE Merges"* **nicht widerrufen**, aber **eingeordnet**: die zehn
Familien bleiben getrennt, doch zwischen ① → ② → ③–⑤ besteht eine **belegte Verfeinerungs-
kette**, die ich in Phase A nicht gesucht hatte (dieselbe ausgelassene Suchdimension wie bei
`Sat` und `δ`; siehe 20. Messfehler).

## D · Neue, in Phase A nicht erfasste Formen

| | Form | Datum |
|---|---|---|
| ⭐ | `Z(K_t) → Z_t = (U_t,C_t,A_t,M_t)` — **der Detektor** | 08-26 16:15 |
| ⭐ | `Z(K_t) → Z_t = (U_t,C_t,A_t,**R_t**)` — dieselbe Signatur, **andere vierte Komponente** | 08-26 17:42 |
| ⭐ | `Z(K_t) = {known unknowns, unresolved assertions, conflicts, missing representations, assumptions}` — 5 Elemente | 08-26 16:15 |
| ⭐ | **`T5`** *„Zero evaluates, does not constitute, Knowledge"* | 08-29 |

⚠️ **`M_t` gegen `R_t`** ist ein unbemerkter Drift innerhalb von **86 Minuten**, in Dokumenten
derselben Spur. Keine Quelle kommentiert ihn.

## E · Validierungsdimensionen (§15, getrennt)

| | `Zero` |
|---|---|
| `defined` | ✅ vielfach |
| `implemented` | ✅ v1.2-Simulation |
| `tested` | ✅ 1.620.000 Zuweisungen für die Kette |
| `independently validated` | ⚠️ **die Kette ja; die einzelnen Familien nein** · `Zero_strict` **nie erreichbar** |
| `governance-reviewed` | ✅ **einzige positive Ratifikation** — aber für **②**, das Meta-Prinzip |
| `selected` / `ratified` als Konstrukt | ⛔ — v1.2 **degradiert** es |

⭐ **Die Ratifikation trifft Stufe ② und nur ②.** Sie darf auf ⑦ oder ⑨ **nicht** übertragen
werden — genau das verbietet `Z-KOS-001` mit *„not an algorithm"*.

## F · Closure-Status nach §14

| | | `Zero` |
|---|---|---|
| 1 | Identität | ⚠️ **verbessert** — ①→②→③–⑤ als Kette belegt; ⑦/⑨ unbezeugt angeschlossen |
| 2–3 | Domäne/Codomäne | ⛔ **sieben unvereinbare Codomänen** (Phase A) |
| 4 | Stelligkeit | ⛔ 1 · 2 · 3 · 4 |
| 7 | Abhängigkeiten | ⛔ `Zero ↔ Δ` (80/14) — Phase 11 |
| 8 | Invarianten | ⭐ ✅ **`I-9`** Nicht-Kollaps **mit Gegenbeispiel** (25D.5) · **`T5`** · die Abgrenzung |
| 9 | unaufgelöste Alternative | ⛔ `M_t` gegen `R_t`; Boolean- gegen Leerheits-Lesart |
| 10 | Zirkularität | ⚠️ Phase 11 |

$$Zero:\quad \textsf{LINEAGE WITNESSED (①→⑤)} \wedge \textsf{DEMARCATION INVARIANT} \wedge \textsf{NOT TYPE-CLOSED} \wedge \textsf{RATIFIED ONLY AS META-PRINCIPLE}$$

## G · Closure decision

$$\boxed{\textbf{TEILWEISE GESCHLOSSEN.}}$$

* ⭐ **`①→②→③④⑤` : `REFINEMENT WITH PRESERVED DEMARCATION`** — **geschlossen**, in neun
  datierten Stufen rekonstruiert.
* ⭐ **`strict ⇒ reasoned ⇒ weak` : `ORDERED`** — geschlossen, 1.62 Mio. Zuweisungen.
* ⛔ **`⑤ → ⑦ → ⑨`** (Detektor → Algebra → Prädikat): **REMAINS OPEN** — kein Dokument
  behauptet diesen Übergang.
* ⛔ **`M_t` gegen `R_t`**: **REMAINS OPEN**, 86 Minuten Drift, unkommentiert.
* ⛔ die sieben unvereinbaren Codomänen: **unverändert offen**.

# Phase 13 — Das Gleichheits-Gate, der minimale Schnitt, und die Versöhnung mit meiner Phase 11

*(Nachtrag zu Phase 12: diese Befunde kamen aus `step-289`, nachdem die Matrix stand, und
sie ändern sie.)*

## 1 · ⭐⭐⭐ Der berechnete **minimale Schnitt ist `{≡}`**

`step-289/05-step-261-gate-audit`:

> *„Step 289's computed **minimal cut is `{≡}`** — i.e. **equality is precisely the node whose
> ambiguity holds the structure together.**"* · *„**`261.23`'s wording is confirmed by a
> method `261` did not use.**"* · *(„And it **corrects Step 288**, which had moved the block
> to `𝒪`/`𝒯`.")*

⭐ Das ist der **handlungsleitendste Strukturbefund der gesamten Estate**: **ein** Knoten,
**berechnet**, nicht postuliert — und die Berechnung korrigiert eine frühere Zuordnung.

## 2 · ⛔ Klarstellung gegenüber meiner **eigenen** Phase 11

Phase 11 kam zu: *„nach Disambiguierung kein Zyklus"*. Step 289 kommt zu: **vier Zyklen,
`≡` in allen vieren**.

$$\textbf{Beides ist wahr, und die Differenz ist die Knotenmenge.}$$

| | meine Phase 11 | Step 289 |
|---|---|---|
| Knoten | `K_t · Δ · Ω · δ · Sat · Eval · Det · Zero · Γ · EC · Req · ℐ` | `≡ · ≈ · 𝒪 · 𝒯 · Π · =` … |
| ⭐ enthält `≡`? | **NEIN** | **JA — und `≡` ist der Schnittknoten** |
| Ergebnis | azyklisch nach Disambiguierung | 4 Zyklen |

⚠️ **Mein Ergebnis widerlegt ihres nicht.** Es sagt: *innerhalb meiner Knotenmenge*. Genau
die Lehre, die ich in Phase 11 §10 selbst gezogen hatte — **Zyklus- und Senkenaussagen sind
nur relativ zur Knotenmenge gültig** — trifft jetzt mich zum dritten Mal.

$$\boxed{\text{Die Zirkularität sitzt nicht dort, wo ich gemessen habe, sondern bei } \equiv.}$$

## 3 · ⭐⭐ `≡_sem` ist kein leerer Slot für **eine** Relation

Bedingung 5 des Gates ist **🔴 DEGRADED**:

> *„`25J.6–7`: **`≡` is a `(C,t)`-indexed family**, **`≡_sem^{2025} ≠ ≡_sem^{2026}`**;
> `V` axis has **no order**"*

⇒ **Berichtigung zu Phase 4.** Ich hatte geschrieben: *„ein deklarierter, leerer Slot"*.
Richtig ist: **ein Slot für eine über Kontext und Zeit indizierte Familie** — und zwei
Instanzen sind nachweislich verschieden. Der Slot ist nicht nur leer; **seine Stelligkeit
war unterbestimmt.**

## 4 · Der volle Stand der sechs Gate-Bedingungen

$$\boxed{\textbf{0 gelöst · 3 verengt · 2 unverändert · 1 verschlechtert}}$$

| # | Bedingung | Stand | Evidenz |
|---|---|---|---|
| 1 | Beobachtungsmenge | 🟠 **NARROWED** | Raum **auf 30 nutzbare Kandidaten begrenzt**, Menge nicht geschlossen |
| 2 | Operationsregister | ⬛ unverändert | **6 von 11** Operationen ohne Gleichheitsanalyse; „14 forced ≤ 18" ist ein **Kandidatensatz, keine Mitgliedschaftsregel** |
| 3 | Provenienzplatzierung | 🟠 **NARROWED** | Mechanismus identifiziert (`258.31`, Kongruenzexperiment) — **blockiert auf Bedingung 2** |
| 4 | Assertionssemantik | ⬛ unverändert | `a ∈ K` hat **drei** Lesarten |
| 5 | Temporalsemantik | 🔴 **DEGRADED** | `≡` ist eine `(C,t)`-Familie |
| 6 | Identitätssemantik | 🟠 **NARROWED** | `I_48` transitiv **innerhalb** eines Kontexts; aber 5 von 11 Identitätsarten fehlen, `id=H(…)` **ausgeführt inkonsistent** |

## 5 · ⭐⭐⭐ `R4` beantwortet — und anders als erwartet

`N-3` („Which `𝒪_K`?") im normativen Entscheidungsregister:

| | |
|---|---|
| Alternativen | *„any closed observation set; **30 usable `Σ`-level candidates**"* |
| Wirkung | bestimmt **`≈` und `≡_K`** unter `258.8` |
| Evidenz **vorhanden** | 30 nutzbare Projektionen **gemessen** |
| Evidenz **fehlend** | *„any corpus statement of **which** observations are permitted"* |
| ⭐ Grund | **„membership is a declaration, not a derivation"** |
| Autorität | **ARB** |

⇒ **`𝒪_K` ist keine zu entdeckende Menge, sondern eine zu erklärende.** Mein
Forschungsakt `R4` (Identität `𝒪_K` ↔ `O` des Achtertupels) ist damit **nicht durchführbar**:
eine Identität zu behaupten wäre selbst eine Deklaration. **`R4` wird zurückgezogen.**

## 6 · ⭐⭐ Zwei Gründe für das Gate, die `261` nicht hatte

| | Grund |
|---|---|
| ② | Der **`≡`/`≈`-Definitionskonflikt**: *„**Two of the six state-level relations cannot both stand as written.** Selecting a kernel over a self-contradictory equality register would be selecting over an **undefined term**."* |
| ③ | ⭐ **`=` ist EXECUTED *keine Kongruenz*** — und *„decidable only relative to a canonicalization **`38.85` forbids fixing a priori**"* |

⭐ ③ ist der härteste: **die eine Relation, die alle für trivial verfügbar hielten, ist es
nicht.** Und ihre Entscheidbarkeit hängt an einer Kanonisierung, die eine andere Regel
**a priori zu fixieren verbietet**.

## 7 · ⭐⭐⭐ Und die Selbstkorrektur der Quelle, die mein Verdikt korrigiert

> *„**Honesty requires the reverse finding too.** `261.23` bundles six conditions into one
> stop. Step 289's graph shows **they are not equally load-bearing**."*
>
> * im Rückkopplungsgefüge: **`≡`** (alle 4 Zyklen)
> * harte **Upstream**-Blocker, keine Zyklusmitglieder: `𝒪`, `𝒯`, `𝒪_K`
> * **downstream** von `≡`: Provenienzplatzierung
> * ⭐ **orthogonal zur Gleichheit**: **Assertionssemantik (4) und Temporalsemantik (5)**
>
> *„Conditions 4 and 5 **do not block equality**; they block **kernel selection** for separate
> reasons. Reporting them under a gate labelled *„while equality remains ambiguous"*
> **conflates two blocks**, and could cause a future step to believe that resolving equality
> releases them. **It would not.**"*
>
> ⚠️ *„This is a **refinement** of the gate's structure, **NOT a proposal to release any part
> of it**. **All six remain unresolved; the gate holds.**"*

$$\boxed{\text{Das Gate ist eine } \textbf{Konjunktion zweier unabhängiger Blöcke} \text{, nicht einer.}}$$

## 8 · Berichtigungen an Phase 12

| Phase-12-Aussage | Berichtigung |
|---|---|
| *„vier Entscheidungen"* | ⚠️ **unvollständig** — der **berechnete minimale Schnitt** ist `{≡}`, und `≡`s eigene Blocker sind die sechs oben |
| `≡_sem`: *„Slot deklariert, leer"* | ⚠️ **Slot für eine `(C,t)`-indizierte Familie**; Stelligkeit war unterbestimmt |
| Forschungsakt **`R4`** | ⛔ **zurückgezogen** — Mitgliedschaft ist eine Deklaration |
| *„die Kette ist azyklisch"* | ⚠️ **nur in meiner Knotenmenge**; mit `≡` gibt es **vier** Zyklen |
| `=` als verfügbar unterstellt | ⛔ **`=` ist ausgeführt keine Kongruenz** |

## 9 · Was daraus für das Verdikt folgt

Das finale Verdikt aus Phase 12 bleibt **in seiner Struktur** bestehen — v1.2 ist
reference-defined, die Mathematik darunter ist weiter geschlossen als zunächst behauptet,
und es fehlen Entscheidungen plus eine Registrierung. **Präzisiert wird die Stelle:**

$$\text{Der strukturelle Engpass ist nicht verteilt, sondern } \textbf{ein Knoten: } \equiv.$$

Und er ist **berechnet**, von einer fremden Spur, mit einer Methode, die die ursprüngliche
Quelle nicht hatte — der stärkste Evidenztyp, der in dieser Untersuchung vorgekommen ist.

## 10 · Verbleibende zulässige Forschungsakte

| | Akt | Status |
|---|---|---|
| `R1` | `Det` gegen `Det_r` durchgängig disambiguieren | ✅ weiterhin zulässig |
| `R2` | strenge Kantenmessung auf Fließtext/Tabellen ausweiten | ✅ zulässig |
| `R3` | `Γ`s Subvokabular traversieren | ✅ zulässig |
| ~~`R4`~~ | ~~`𝒪_K` ↔ `O`~~ | ⛔ **zurückgezogen** — wäre eine Deklaration |
| `R5` | `M_t`/`R_t` und `K-a` datieren und melden | ✅ zulässig |
| ⭐ **`R6`** | **die Graphanalyse von Step 289 mit `≡` als Knoten unabhängig nachrechnen** | ✅ **neu** — der minimale Schnitt ist ein *berechnetes* Ergebnis und damit **nachprüfbar** |

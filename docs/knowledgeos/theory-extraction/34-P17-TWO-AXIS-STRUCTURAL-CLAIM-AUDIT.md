# `P-17` — Two-Axis Structural Claim Audit

**2026-09-08 · Lane T · structural falsification audit.** After [`33` `P-16`](./33-P16-THIRD-AXIS-AND-TRANSITION-CLOSURE-AUDIT.md).

> **The adversarial question:** *have we discovered two independent structural dimensions, or merely
> found a convenient way to label eleven capabilities AFTER discovering them?*
>
> ⛔ **`P-13`–`P-16`, `Q1`, `W6`, `Q2` not reopened · no cell added, removed or decomposed without a
> complete admissible witness · Schema v2 unmodified, v3 not begun · no architecture, event sourcing,
> temporal/graph database, repository, aggregate or model registry · 3MC / Lane M untouched · DDD,
> mathematics and implementation are never ontology · Lane T conclusions are not corpus evidence.**

# 1. The claim, formalized — and `B1` is already false as stated

| | claim | status |
|---|---|---|
| **`B1`** | every admitted persistence cell is **classifiable** by `SUBJECT × ROLE` | ⭐⭐⭐ **FALSE for the kernel** — see below |
| **`B2`** | the **distinctions between** cells are **generated by** subject and role differences | ⭐ **the target of this audit** |
| **`B3`** | given the subject and role distinctions, the capabilities are **derivable** without a further dimension | ⭐ `P-10` already judged this **TOO STRONG** |
| **`B4`** | **no** admissible witness can require a distinction outside `SUBJECT × ROLE` | ⭐ `[OPEN]` at `P-10`; ⛔ never claimed |

## ⭐⭐⭐ `B1` covers 8 of 11 cells, not 11

`[DERIVED]` **`K1`, `K2` and `K5` lie entirely outside `SUBJECT × ROLE`.** `P-09.3` §20 placed them in a
separate **present-state family**; they are not retention cells and have neither an antecedent nor a
link.

$$\boxed{\begin{array}{c}\textbf{The two-axis structure was NEVER a decomposition of the KERNEL — only of the RETENTION FAMILY.}\\ \boxed{\textbf{Even the weakest claim } B1 \textbf{ is RESTRICTED: } \mathbf{8\ of\ 11}\textbf{, and } \mathbf{3} \textbf{ cells are outside it by construction.}}\end{array}}$$

⭐ **What `P-10` actually supported:** its **Claim A** — *the observed decompositions **factor through**
two axes* — which is `B1`-restricted. ⛔ **Its Claim B was already declared TOO STRONG.** ⚠️ **So `P-17`
audits `B2`, and `B2` is the first version not already settled.**

# 2. ⭐⭐⭐ The Cartesian-product attack — zero predictive instances

$$\boxed{\textbf{RETROSPECTIVE CLASSIFICATION} \neq \textbf{GENERATIVE STRUCTURE}}$$

**The decisive question: did the product ever PREDICT a cell that was subsequently found?** Auditing
this lane's own derivation order *(⚠️ admissible here because it is evidence about **my own claim**, not
about KnowledgeOS)*:

| cell | how it was actually found |
|---|---|
| `K3` = prior value **+** warrant | ⭐ **witnessed** — `P-08` §17's insufficiency proof and §19's repaired witness, **both written before any axis existed** |
| `K4a` / `K4b` / `K4c` | ⭐ **witnessed** — `P-09.2`'s `Π2` and cases **C**, **D**, **E** |
| the eight antecedent/link splits | ⭐ **witnessed** — `P-09.3` §7–§10's eight counterexamples |
| the grid itself | ⭐⭐ **NAMED IN `P-09.3` AFTER ALL EIGHT CELLS ALREADY EXISTED** |

$$\boxed{\begin{array}{c}\textbf{ZERO cells were predicted by the product. ALL EIGHT were witnessed first and labelled afterwards.}\\ \boxed{\textbf{A finite set of observations can always be indexed retrospectively — and that is precisely what happened.}}\end{array}}$$

⚠️ **Recorded as a threat to `B2`/`B3`.** ⭐ `P-10` §3 already conceded the full grid was *"a
construction, not a prediction"*; `P-17` measures the consequence.

---

# 3. Subject-axis pairwise tests

| pair | is a loss-distinguishing minimal history available? | verdict |
|---|---|:--:|
| **value / proposition** | ⭐ retain prior **status values** with warrants while **deleting superseded assertion texts** — `P-09.2` §11 | ✅ **forced** |
| **proposition / ground** | ⭐⭐ **`Π2`** — identical claims and identical current warrants, differing only in the **withdrawn** ground | ✅ **forced** |
| **ground / reference** | ⭐ **inspectability** vs **resolvability** — `P-09.2` §5 | ✅ **forced** |
| **value / ground** | a value is **held**; a ground is **cited** — each varies with the other fixed | ✅ **forced** |
| **value / reference** | `K3a` vs `K4c-i` — prior field value vs retired designator | ✅ **forced** |
| **proposition / reference** | ⭐ **readability** vs **resolvability** — a superseded assertion is *read*, a retired identity must *resolve* | ✅ **forced** |

## ⭐⭐⭐ But six forced pairs do not make one axis with four values

`[DERIVED]` Four values **on a single dimension** must be **mutually exclusive and at the same level**.

$$\boxed{\begin{array}{c}\textbf{And they are not: } P\text{-}09\ \S6 \textbf{ established that WARRANTS ARE ASSERTIONS, so } \textit{ground} \textbf{ is}\\ \textbf{a PROPOSITION AT A HIGHER LEVEL — not a peer of } \textit{proposition}.\\[6pt] \boxed{\textbf{Six pairwise distinctions } \neq \textbf{ one axis with four values. } \textit{Ground} \textbf{ is a LEVEL, not a coordinate.}}\end{array}}$$

⭐ **This is `P-10`'s `B-fail-1` stratification worry, now stated as a positive result rather than a
suspicion.**

# 4. Subject collapse test

**Attempted strongest reduction:** all four are *"a thing whose prior state or relational status must
remain distinguishable."*

$$\boxed{\begin{array}{c}\textbf{The reduction SUCCEEDS — trivially. And that is the finding:}\\ \boxed{\textbf{the "subject axis" does NO GENERATIVE WORK. It records WHAT KIND OF THING a cell is about, after the fact.}}\end{array}}$$

⛔ **The abstraction is NOT introduced as ontology.** ⭐ **Recorded: `SUBJECT STRATIFICATION MAY BE
RETROSPECTIVE`, and `B2` is weakened accordingly.** ⚠️ **No subject distinction is retained merely
because it appears in existing notation** — the six pairwise obligations stand on their witnesses, not
on the axis.

---

# 5. ⭐⭐⭐ Role overload — the missing direction is now WITNESSED

`P-10` §3 found *"link"* bundles **relation** and **justification**, with only one direction witnessed:
**relation-without-justification** *(`P-08` §19's repaired witness — `resolved` recorded with no
warrant)*. ⭐ **The converse was missing, so the split stayed `[OPEN]`.**

## The minimal pair for justification-without-relation

Take an **establishment** — a `0→1` transition. ⭐⭐ **It has NO antecedent and NO
predecessor→successor relation.**

| | `H1` | `H2` |
|---|---|---|
| the act | a candidate established **with** a warrant | the same establishment **without** one |
| ⭐ **`K3a`** *(prior value)* | ⭐ **EMPTY — there is no predecessor** | ⭐ **EMPTY** |
| `K1`, `K2`, `K4a`, `K4b`, `K4c`, `K5` | **identical** | **identical** |
| ⭐ **`K3b`** *(justification)* | ⭐ **present** | ⭐ **absent** |
| **is the distinction required?** | ⭐⭐ ✅ **YES — `P-08` §19: an adjudicated entry must be distinguishable from one never in doubt** | |

⭐ **And both arms are corpus-attested** *(previously admitted evidence)*: **`C-022` establishes with a
warrant** *("50 attack classes")*, while **`G-67` establishes by NAMING with no warrant at all** —
`B-6`'s structural `n/a`.

$$\boxed{\begin{array}{c}\textbf{Condition 3 holds; condition 1 holds. JUSTIFICATION-WITHOUT-RELATION IS WITNESSED.}\\ \boxed{\textbf{"Link" IS OVERLOADED. The role axis carries THREE values: antecedent · relation · justification.}}\end{array}}$$

⭐⭐ **`P-10`'s role-overload question is therefore RESOLVED — affirmatively, and by an establishment,
which is the one transition class where the antecedent role is necessarily vacuous.**

# 6. ⭐⭐⭐ Role independence — and the grid is not homogeneous

| test | result |
|---|---|
| **`R1`** same antecedent, different link | ✅ available — `resolved`-with vs `resolved`-without a warrant |
| **`R2`** different antecedent, same link | ✅ available — two re-dispositions sharing a warrant type |
| **`R3`** same antecedent and link, different subject | ⭐ **available, and it is the §3 pairwise set** |
| **`R4`** both different | ✅ trivially |
| ⭐⭐ **is "role" independent of subject, or dependent on it?** | ⭐⭐⭐ **DEPENDENT — see below** |

## The heterogeneity finding

| row *(subject)* | what the **link** column actually holds |
|---|---|
| **value** | ⭐⭐ **`K3b` — a WARRANT, i.e. a JUSTIFICATION** |
| **proposition** | ⭐ **`K4a-ii` — `supersedes`, a RELATION** |
| **ground** | **`K4b-ii` — `invalidated-by`, a RELATION** *(plus a defect, i.e. also a justification)* |
| **reference** | **`K4c-ii` — `retired-into`, a RELATION with cardinality** |

$$\boxed{\begin{array}{c}\textbf{The SAME COLUMN means a JUSTIFICATION in the } \textit{value} \textbf{ row and a RELATION in the other three.}\\[6pt] \boxed{\begin{array}{l}\textbf{A well-formed product requires a column to mean the SAME THING across rows.}\\ \textbf{It does not. The rows are NOT COMPARABLE, and } K4b\text{-ii} \textbf{ holds BOTH values at once.}\end{array}}\end{array}}$$

⭐⭐⭐ **This is why `P-10` sensed *"overload"*: the column was HETEROGENEOUS, not merely broad.**

# 7. Subject × role interaction

| cell | is it **forced by** the combination of its subject and role? |
|---|---|
| `K3a` | 🔴 **no** — forced by `P-08` §17's insufficiency proof |
| `K3b` | 🔴 **no** — forced by `P-08` §19, and §5 shows its role is *justification*, not the grid's *link* |
| `K4a-i` / `K4a-ii` | 🔴 **no** — forced by two counterexamples about **readability** |
| `K4b-i` / `K4b-ii` | 🔴 **no** — forced by `Π2` and by *which kind of ground failure* |
| `K4c-i` / `K4c-ii` | 🔴 **no** — forced by `W1` and by *retired vs never existed* |

**Converse test:** *if the same subject/role combination appeared in another admissible transition,
would it necessarily require the same capability?*

$$\boxed{\begin{array}{c}\textbf{NO — } \textit{(link, value)} \textbf{ yields a JUSTIFICATION while } \textit{(link, reference)} \textbf{ yields a RESOLVABLE RELATION WITH CARDINALITY.}\\ \boxed{\textbf{Same coordinates, different obligations } \Rightarrow \textbf{ the coordinates do not DETERMINE the capability.}}\end{array}}$$

⭐⭐ **§13's first search target — *the same classification carrying two different obligations* — is
therefore SATISFIED, and it threatens generativity directly.** ⛔ **The notation proves nothing.**

# 8. Set-wise counterexample test

**The strongest counterexample to `SUBJECT × ROLE`** must have identical current state, identical 11
cells, identical values, warrants, identities, relations, representation, implementation facts, **and
identical subject and role classification** — yet a different obligation.

$$\boxed{\begin{array}{c}\textbf{§7's converse test IS that pair, at the level of the STRUCTURE rather than of a history:}\\ \textit{(link, value)} \textbf{ and } \textit{(link, reference)} \textbf{ share the ROLE coordinate and carry obligations of different KINDS.}\\[6pt] \boxed{\textbf{The product does not determine the obligation. } B2 \textbf{ FAILS as a generative claim.}}\end{array}}$$

⚠️ ⭐ **And the reduction argument required by §8 is supplied for the negative direction:** the existing
axes **do** index all eight retention cells (`B1`-restricted holds), and every cell's *content* is
traceable to its **own witness** rather than to its coordinates. ⛔ **So indexing succeeds and
generation fails** — two different results, kept apart.

# 9. ⭐ Refusing the fallacy explicitly

$$\boxed{\neg\textbf{ThirdAxis} \;\not\Rightarrow\; \textbf{TwoAxisGeneration}}$$

⭐⭐ **`P-16` closed the third axis under the current obligation boundary. That is a statement about
what is REQUIRED. `B2` is a statement about what GENERATES.** ⭐ **`P-17` demonstrates the gap rather
than merely asserting it: the third axis is closed AND the two axes are not generative.** ⛔ **The
implication is not used anywhere in this artifact.**

# 10. Mathematical sanity check
⚠️ **Structural reasoning only. ⛔ No vector-space property is claimed — the required structure does not exist.**

| distinction | verdict |
|---|---|
| **coordinate system vs generative basis** | ⭐⭐ **`SUBJECT × ROLE` is an INDEXING SCHEME, not a basis** — §7: the coordinates do not determine the value |
| **classification vs independent dimension** | ⭐ **classification** — §2's zero predictive instances |
| **Cartesian product vs arbitrary labelling** | ⭐ **not a product** — §6: the column is heterogeneous, so the "product" is not well-formed |
| **basis vs spanning set** | ⭐ **at best a spanning set** — the labels cover the eight cells without being independent |
| **equivalence vs identity** | held apart; ⛔ **`≡_sem` untouched** |
| **graph representation vs graph ontology** | held apart — `P-16` §14 |

$$\boxed{\textbf{A convenient indexing scheme. ⛔ Mathematical metaphor is not proof, and none is offered as such.}}$$

# 11. Statistical sanity check — the overfitting test
⚠️ **Structural analogy only. ⛔ No sampling model exists, so no probability is claimed.**

| level | verdict |
|---|---|
| **descriptive fit** | ⭐ ✅ **yes — for 8 of 11 cells** |
| **structural explanation** *(do the axes explain WHY cells differ?)* | ⭐⭐ 🔴 **NO** — §7: each cell differs because of its own witness |
| **predictive adequacy** *(would a new witness fall within the structure?)* | ⭐ 🔴 **untested and untestable so far — zero predictions were made** |
| **identifiability** *(are subject and role independently recoverable from evidence?)* | ⭐⭐ 🔴 **NO** — §6: the role value is only recoverable once the subject is known |

$$\boxed{\begin{array}{c}\textbf{Descriptive fit on 8 observations with ZERO predictions and NO identifiability.}\\ \boxed{\textbf{This is the structural signature of OVERFITTING: a model with as many free labels as observations.}}\end{array}}$$

# 12. DDD cross-check
⚠️ **Corroboration only.** *Subject* does resemble a domain object/value, and *role* a relationship —
⭐ **but DDD equally allows one concept to participate in many roles and one relationship to join
different subjects**, which is exactly the **independence** §6 finds absent. ⛔ **And the inference
*"DDD has entities and relationships, therefore KnowledgeOS has `SUBJECT × ROLE`"* is refused as
architecture import.** ⭐ **DDD corroborates the negative, and supplies nothing positive.**

# 13. The two searches
| target | found? |
|---|---|
| ⭐ **same classification, two different obligations** *(threatens generativity)* | ⭐⭐⭐ ✅ **YES — §6/§7: `(link, value)` = justification vs `(link, reference)` = resolvable relation with cardinality.** `[DERIVED]`, on `[EMP]` arms (`C-022` / `G-67`) |
| ⭐ **two different classifications collapsing to one obligation** *(threatens stratification)* | ⭐⭐ ✅ **YES — §4's trivial collapse**: all four subjects reduce to *"a thing whose prior state or relational status must remain distinguishable."* `[DERIVED]` |

⛔ **No constructible example was upgraded to empirical.** ⭐ **Both threats are realized, from opposite
directions.**

# 14. Result matrix

| question | witness | existing structure | reduction possible? | result |
|---|---|---|:--:|---|
| **subject stratification** | ⭐ *ground = proposition at a higher level* (`P-09` §6) + §4's trivial collapse | four values on one axis | ⭐ ✅ **yes, trivially** | ⭐⭐ **RETROSPECTIVE — not a partition; `ground` is a LEVEL** |
| **role overload** | ⭐⭐⭐ **establishment warrants — `C-022` with, `G-67` without; `K3a` empty in both** | *link* as one value | 🔴 **no** | ⭐⭐ **RESOLVED: LINK IS OVERLOADED — three role values** |
| **role independence** | §6's heterogeneity table | role ⊥ subject | 🔴 **no** | ⭐⭐ **ROLE IS DEPENDENT ON SUBJECT** |
| **subject/role interaction** | §7's converse test | coordinates determine the cell | 🔴 **no** | ⭐⭐ **COORDINATES DO NOT DETERMINE THE OBLIGATION** |
| **Cartesian-product validity** | §6 — the column means two different things | a well-formed product | 🔴 **no** | ⭐⭐⭐ **NOT A WELL-FORMED PRODUCT** |
| **generativity (`B2`/`B3`)** | ⭐ §2 — **zero predictive instances** | generative structure | 🔴 **no** | ⭐⭐⭐ **NOT ESTABLISHED** |
| **closure (`B4`)** | none sought | — | — | ⭐ **`[OPEN]` — ⛔ never claimed** |
| **classification (`B1`)** | 8 of 11 cells index cleanly | indexing scheme | — | ⭐ **HOLDS, RESTRICTED to the retention family** |

# 15. Verdict

$$\boxed{\textbf{B — CLAIM B REDUCED TO CLASSIFICATION.}}$$

⭐ **And with two aggravating findings beyond the bare negative:**
1. ⭐⭐ **the classification is PARTIAL** — 8 of 11 cells; `K1`, `K2`, `K5` are outside it by construction;
2. ⭐⭐⭐ **the classification is HETEROGENEOUS** — the *link* column holds a **justification** in one row
   and a **relation** in three, so it is **not a well-formed product at all**, and `K4b-ii` holds both
   values simultaneously.

⛔ **NOT verdict `A`** — no generative force was established, and *"all cells fit"* was explicitly
refused as grounds. ⛔ **NOT verdict `C`** — no witness requires a distinction the labels cannot
*index*; the failure is of **generation**, not of **coverage**. ⛔ **NOT verdict `D`** — the evidence
does distinguish `B1` from `B2`, decisively.

# 16. Kernel consequence

$$\boxed{\textbf{KERNEL REMAINS 11 CELLS. ⛔ No cell added, removed, decomposed or changed in status.}}$$

⭐⭐ **The kernel and its structural explanation are different objects, and only the explanation moved:**

$$\boxed{\textbf{kernel} = \textbf{conditionally closed} \qquad \textbf{structural generativity} = \mathbf{NOT\ ESTABLISHED}}$$

⭐ **Role overload does NOT force a cell split**, because the two role values appear in **different
rows** — no single cell contains both, except `K4b-ii`, whose two aspects were already derived together
by one witness (`P-09.3` §9). ⇒ ⭐⭐ **The overload is a LABELLING defect, not a cell defect.**

# 17. Stopping condition
⭐ Met by route **(1)** and **(2)** together: **§6/§7 falsify generativity** by counterexample, and
**§8's reduction argument** explains why indexing nevertheless succeeds. ⛔ **The search was not
continued for a stronger-looking conclusion.**

---

# 18. Required final statements

**1 · What `P-16` established** — `[DERIVED]`, boundary-relative: **the third axis is refuted** under the
current obligation set, because `K3a` records `(prior→successor)` **pairs** so order is generally
derivable, and the Eulerian residue's distinction — *order over multiple prior values* — was already
excluded by `P-08` §7's rejection of complete history. **Zero unclassified transitions remain.**

**2 · Subject stratification** — ⭐⭐ `[DERIVED]`: **RETROSPECTIVE.** All six pairs are forced **as
obligations**, but they do not form **one axis with four values**: `ground` is a **proposition at a
higher level** (`P-09` §6), so it is a **level, not a coordinate**; and all four collapse trivially into
*"a thing whose prior state or relational status must remain distinguishable."*

**3 · Role overload and independence** — ⭐⭐⭐ `[DERIVED]` on `[EMP]` arms: **LINK IS OVERLOADED, and
the missing direction is now witnessed.** An **establishment** (`0→1`) has **no antecedent and no
relation**, yet `C-022` carries a warrant while `G-67` carries none — a required distinction with `K3a`
empty in both arms. So the role axis has **three** values (**antecedent · relation · justification**),
and **role is DEPENDENT on subject**: the *link* column holds a justification in the `value` row and a
relation in the other three.

**4 · Classificatory or generative** — ⭐⭐⭐ `[DERIVED]`: **CLASSIFICATORY ONLY.** `SUBJECT × ROLE` is an
**indexing scheme, not a basis** — **zero cells were ever predicted by it**, every cell traces to its own
witness, the coordinates **do not determine** the obligation, and the column is **heterogeneous**, so it
is not a well-formed product. `B1` holds for **8 of 11**; `B2` and `B3` **fail**; `B4` is `[OPEN]`.

**5 · Kernel change justified?** — ⛔ **NO.** `[DERIVED]`: **11 cells, unchanged.** The overload is a
**labelling** defect, not a cell defect; the kernel remains **conditionally closed** while **structural
generativity is not established.**

## Remaining `[OPEN]`
⭐ **`B4` closure** · ⭐ **whether any generative structure exists at all** *(a new question — §19)* ·
**kernel identity** · **equivalence (`Q7`)** · **aggregate existence** · **`W6`'s mechanism `[ARCH]` and
its live empirical failure** · **`O-INT001-1`'s governance rule** · **`O-INT002-1`** · **provenance
chains** and **alternate histories** *(no witness either way)* · **`Q3`/`Q8`** *(an exercise of `K4c`)* ·
**`Q4`** · **`M6`'s occurrence** · `F-4`/`F-5` unrepaired.

### New question
| | |
|---|---|
| ⭐ **`O-P17-1`** | **Is there ANY generative structure over the retention family, or is the kernel an irreducible list of eleven independently-witnessed obligations?** ⚠️ **`P-17` refutes one candidate structure; it does not establish that none exists.** ⛔ **And absence of a generative structure would be a legitimate result — a list is not a defect.** |

---

```
CLAIM B — REDUCED TO CLASSIFICATION. SUBJECT x ROLE IS AN INDEXING SCHEME, NOT A GENERATIVE BASIS —
KERNEL REMAINS 11 CELLS.

THE FOUR CLAIMS, SEPARATED: B1 classification · B2 structural · B3 generative · B4 closure.
  B1 is FALSE AS STATED FOR THE KERNEL and holds only RESTRICTED: K1, K2 and K5 lie outside
  SUBJECT x ROLE by construction (P-09.3's present-state family), so the two-axis structure was never
  a decomposition of the KERNEL — only of the RETENTION FAMILY, 8 of 11 cells. P-10's Claim A was
  B1-restricted; its Claim B was already TOO STRONG; so B2 was the first unsettled version.

WHY GENERATIVITY FAILS — three independent findings:
  (i) ZERO PREDICTIVE INSTANCES. Auditing this lane's own derivation order: K3's two clauses were
      witnessed in P-08 §17/§19 before any axis existed; K4a/K4b/K4c came from Pi-2 and cases C/D/E;
      the eight antecedent/link splits came from P-09.3's eight counterexamples; and the grid was NAMED
      AFTER all eight cells already existed. No cell was ever predicted. A finite set of observations
      can always be indexed retrospectively, and that is what happened.
  (ii) THE COORDINATES DO NOT DETERMINE THE OBLIGATION. (link, value) yields a JUSTIFICATION while
      (link, reference) yields a RESOLVABLE RELATION WITH CARDINALITY — same role coordinate,
      obligations of different KINDS. A well-formed product requires a column to mean the same thing
      across rows; the link column holds a JUSTIFICATION in the value row and a RELATION in the other
      three, and K4b-ii holds both at once. THE ROWS ARE NOT COMPARABLE.
  (iii) NO IDENTIFIABILITY. The role value is recoverable only once the subject is known, so role is
      DEPENDENT on subject rather than an independent axis. Descriptive fit on 8 observations, zero
      predictions, no identifiability — the structural signature of OVERFITTING.

ROLE OVERLOAD: RESOLVED AFFIRMATIVELY, and the missing direction is now WITNESSED. P-10 had
  relation-without-justification (P-08 §19's repaired witness) and lacked the converse. An
  ESTABLISHMENT (0->1) supplies it: it has NO antecedent and NO predecessor->successor relation, yet
  C-022 establishes WITH a warrant ("50 attack classes") while G-67 establishes by NAMING with NONE
  (B-6's structural n/a). K3a is EMPTY in both arms, every other cell is identical, and the distinction
  is required by P-08 §19 — an adjudicated entry must be distinguishable from one never in doubt.
  So the ROLE axis carries THREE values: ANTECEDENT · RELATION · JUSTIFICATION.

SUBJECT STRATIFICATION: RETROSPECTIVE. All six pairs are forced as OBLIGATIONS, but four values on one
  dimension must be mutually exclusive and AT THE SAME LEVEL — and P-09 §6 established that WARRANTS
  ARE ASSERTIONS, so `ground` is a PROPOSITION AT A HIGHER LEVEL, a LEVEL rather than a coordinate.
  All four also collapse trivially into "a thing whose prior state or relational status must remain
  distinguishable" — a reduction that succeeds so easily that it shows the subject axis does NO
  generative work. The abstraction is NOT introduced as ontology.

BOTH SEARCHES SUCCEEDED, FROM OPPOSITE DIRECTIONS: one classification carrying two obligations
  (threatens generativity) AND two classifications collapsing to one obligation (threatens
  stratification).

THE FALLACY REFUSED EXPLICITLY: not-ThirdAxis does NOT imply TwoAxisGeneration. P-16 settled what is
  REQUIRED; B2 concerns what GENERATES. P-17 demonstrates the gap rather than asserting it — the third
  axis is closed AND the two axes are not generative.

KERNEL: 11 cells, nothing added, removed, decomposed or changed. Role overload does NOT force a cell
  split, because the two role values sit in DIFFERENT ROWS — the overload is a LABELLING defect, not a
  cell defect. kernel = conditionally closed; structural generativity = NOT ESTABLISHED. These are
  different objects and only the explanation moved.

NEW OPEN QUESTION O-P17-1: is there ANY generative structure over the retention family, or is the
  kernel an irreducible list of eleven independently-witnessed obligations? P-17 refutes ONE candidate
  structure; it does not establish that none exists — and an irreducible list would be a legitimate
  result, not a defect.

NO ARCHITECTURE SELECTED — SCHEMA v3 NOT BEGUN — 3MC UNTOUCHED.
```

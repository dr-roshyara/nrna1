# `P-90` — `EKS-38` Governance-Control Audit: the `N`-registers and sequencing

**2026-09-09 · Lane T.** After [`110` `P-89`](./110-P89-N4-GOVERNANCE-ADOPTION-AUDIT.md).

⛔ **No act chosen · nothing enacted · no 3MC · `|K| = 11 · [REC] · UNFROZEN`.**

# 0. ⚠️ DISAGREEMENT — two, and both change the carried position

## 0.1 ⭐⭐⭐⭐⭐ There are **THREE** `N`-registers, not two — and the third **contradicts** the first

A third normative-decision register exists at
`verification/gap-discovery/second-order/06-NORMATIVE-DECISION-REGISTER.md`, and it collides again:

| ID | ⭐ kernel register | ⭐ synthesis (`122338`) | ⭐⭐ **second-order register** |
|---|---|---|---|
| `N-1` | are `≡`/`≈` the same? | decide **factivity** | ⭐ **"Authority is exogenous"** — `CLOSED/NORMATIVE`, implemented **132/132** |
| `N-2` | Decision-3 residue | define `Contr` | ⭐ **"Minimality is relative to `𝒯`, not ontological"** — `CLOSED/NORMATIVE` |
| `N-3` | `𝒪_K`'s extension | define `⪰` | *(candidate)* reinstate `Determination`? |

⚠️⚠️⚠️ **And on the blocking item the two registers say opposite things:**

| | kernel register (`step-291`) | second-order register |
|---|---|---|
| is `𝒪` enumerated? | ⛔ **"22 CURRENTLY IDENTIFIED — completeness NOT demonstrated"** | ⭐ **"`𝒪` IS enumerated: 15 operations"** |
| mandatory membership | ⛔ **NOT derivable — 8 routes fail · NORMATIVE · ARB** | ⭐⭐⭐ **`D-1` — DERIVED, RETIRED** |
| open normative nodes | **4 live + 20 carried** | ⭐⭐⭐ **`0 of 29`** |

## 0.2 ⭐⭐⭐⭐ And the corpus **does** contain the reconciling mechanism — but it is not a governance rule

`second-order/05-CANONICAL-DEPENDENCY-CLOSURE.md` marks **`Operation`** as **`CLOSED / PARAMETRIC`**:

> *"§256.2 + §259.7: **15 named, 5 classes; the set is open BY DESIGN** (§256.32)"*
> ⭐⭐⭐ *"**`CLOSED/PARAMETRIC` is not a euphemism for open.** It means the node is fully determined
> **once a deployment declares its parameter**, and **the corpus deliberately declines to declare
> it**."*

$$\boxed{\begin{array}{c}\textbf{⭐⭐⭐ Both registers are then right about different consequents:}\\ \textbf{the choice } \mathbf{cannot\ change\ the\ congruence\ answer} \textbf{ (}D\text{-}1\textbf{), and it } \mathbf{is\ still\ undeclared} \textbf{ (}N\text{-}4\textbf{).}\\[4pt] \boxed{\textbf{⛔ But } \mathbf{no\ document\ performs\ that\ reconciliation}\textbf{, and the two name } \mathbf{different\ owners.}}\end{array}}$$

⚠️ **This materially qualifies `P-89`'s `ARB INDICATED`:** ⭐⭐ **one register routes `𝒪`/`𝒯` to **ARB**;
the other routes it to a **DEPLOYMENT**.** ⛔ **I do not choose between them.**

---

# A. N-register status

$$\boxed{\mathbf{REQUIRES\ A\ NEW\ GOVERNANCE\ ACT.}}$$

| candidate mechanism | found? |
|---|---|
| a canonical `N`-register | ⛔ **NO** — three registers, none marked canonical |
| a namespace rule · lane-qualified identifier convention | ⛔ **NO** — *"lane-qualified"* returns **0 files** |
| a crosswalk between the registers | ⛔ **NO** — none cites another |
| a precedence rule · supersession rule between them | ⛔ **NO** |
| ⭐ a rule stating identical IDs in different lanes are **intentionally** distinct | ⛔ **NO — `[UNWITNESSED]`** |

⭐ **What does exist is a naming *practice*, not a rule** — ⛔ **and the commission's own caution
applies: a convention, a repository location or an authoring habit is not a governance rule.**

$$\boxed{\begin{array}{c}\textbf{⛔ The two (three) } N\text{-}4/N\text{-}1 \textbf{ records } \mathbf{cannot\ safely\ be\ referenced\ by\ the\ bare\ identifier.}\\[4pt] \textbf{⭐ Minimum unambiguous reference: } \mathbf{register\ path\ +\ ID\ +\ the\ act's\ subject\ in\ words.}\end{array}}$$

# B. Sequencing status — the meanings of *"earlier"*

⭐ **The corpus supplies five, and tests `N-4` against each:**

| # | meaning `≺ᵢ` | is `N-4` earliest? |
|---|---|---|
| **1** | upstream in the dependency graph | ⛔ **NO — 7 source nodes have in-degree 0** (`𝒪`, `Qualify`, `assertion`, `evidence`, `id_context`, `policy`, `temporal`) |
| **2** | prerequisite for **derivation** | 🟡 **partly** — yes for `≈`/`≡`'s extension, **no** for `≡`'s definability |
| **3** | prerequisite for **governance** | 🟡 **partly** — `N-1′` needs it; `N-8`, `N-13`, `N-12`, `N-19`–`N-21` need nothing |
| **4** | prerequisite for **implementation** | ✅ **YES** |
| **5** | prerequisite for **canonicalization** | 🔴 **NO — canonicalization needs `evidence`, a SEPARATE root** |

$$\boxed{\textbf{⭐⭐⭐ Meanings } \mathbf{4\ and\ 5\ DISAGREE}\textbf{, and the corpus records that they } \textit{"were being conflated"}.}$$

⇒ $$\boxed{\mathbf{MULTIPLE\ VALID\ ORDERS\ —\ AND\ NO\ CORPUS\text{-}SUPPORTED\ RULE\ THAT\ SELECTS\ ONE.}}$$

⭐ **§17 states it outright: *the corpus selects none.*** ⚠️ **And Part III finds no adopted sequencing
rule** — *"governance before implementation"*, *"identity before evidence"* and the rest appear as
**observed orderings, never as binding rules**. ⛔ **One example following an order is not a rule.**

# C. Dependency graph — the smallest defensible one

**Nodes:** `N-4` (`𝒪`, leverage 20) · `N-8` (`id_context`, 17) · *policy semantics* (`policy`, 16) ·
`N-13` (`evidence`, 2) · `N-3` · `N-1′`.

| edge | class |
|---|---|
| `N-4 → N-3` | ⭐ **`[DERIVED]`** — `𝒪_K` cannot be extended before `𝒪`'s membership |
| `N-3 → N-1′` | ⭐ **`[DERIVED]`** — the candidate is defined over an unclosed set |
| `N-4 → N-8` · `N-4 → policy` · `N-4 → N-13` | ⛔ **NO EDGE** — all three are recorded *"blocked by: nothing"* |
| *"governance before implementation"* | ⚠️ **`[EMP]` at best — no binding statement found** |

$$\boxed{\begin{array}{c}\textbf{⭐ } \mathbf{FOUR\ VALID\ ROOTS}\textbf{, one chain of three behind } N\text{-}4\textbf{, and } \mathbf{no\ unique\ first\ act}.\\ \textbf{⭐⭐ ⇒ } \mathbf{SEQUENCING\ IS\ ITSELF\ AN\ UNRECORDED\ GOVERNANCE\ CHOICE.}\end{array}}$$

⚠️ ⭐ **And a gap the four-act framing hides:** the measurement finds **seven** source nodes but only
**four** are carried as acts. ⛔ **`Qualify`, `assertion` and `temporal` have in-degree 0 and no
register entry.** ⭐ **So "four unblocked acts" is itself not established** — I flag it rather than
adding three.

# D. Authority status

$$\boxed{\mathbf{UNRESOLVED}\ \textbf{— stronger than } P\text{-}89\textbf{'s } \mathit{INDICATED}\textbf{, and for a new reason.}}$$

| required | evidence |
|---|---|
| ARB jurisdiction over these acts | ⛔ **`[UNWITNESSED]`** — an `Authority:` field, no jurisdiction statement |
| who may **put** · **approve** · **ratify** · **record** | ⛔ **none of the four is stated** |
| where the authoritative register lives | ⛔ **undetermined — three registers, none canonical** |
| ⭐⭐ **and now: WHICH owner** | ⚠️⭐⭐⭐ **ARB (kernel register) vs a DEPLOYMENT (`CLOSED/PARAMETRIC`)** |

⭐ **But the estate demonstrably CAN enact:** `N-1` *(authority exogenous)* is `CLOSED/NORMATIVE` and
**implemented 132/132 with a fail-closed resolver**, and `N-2` *(minimality relative to `𝒯`)* is
`CLOSED/NORMATIVE`. ⛔ **So the failure is not an inability to act — it is that these particular items
have no route.**

# E. `EKS-38`'s characterization — tested

⭐⭐ **CONFIRMED, and strengthened.** ⛔ **Not merely another `EKS-27` instance:**

| | `EKS-27` | `EKS-38` |
|---|---|---|
| object | a symbol in a formula | ⭐ **an identifier naming an ACT** |
| failure | a misreading | ⭐⭐ **a wrong enactment** |
| remedy | a glyph register *(notation)* | ⭐⭐ **one decision register *(governance)*** |
| detectability | both meanings readable | ⭐ same — **but nobody looked** |

⭐⭐⭐ **And the third register raises the stakes:** it is not only that `N-4` is ambiguous — **two
registers give contradictory dispositions of the same question**, one `NORMATIVE/OPEN`, one
`DERIVED/RETIRED`. ⇒ ⛔ **an instruction to *"enact `N-4`"* could be answered *"that was retired"* — with
a citation.**

# F. Semantic consequence

| object | status | change from `P-89` |
|---|---|---|
| **`𝒯`** | ⛔ **STILL BLOCKED** — but ⭐⭐ **possibly as a DEPLOYMENT PARAMETER rather than a theory decision** | ⚠️ **the owner is now contested, not merely unconfirmed** |
| **`ℛ_req`** | ⛔ **STILL CONDITIONAL** | unchanged |
| **`Eval` / `Sat`** | ⛔ **STILL BLOCKED** | unchanged |
| ⭐ **newly derivable?** | ⛔ **nothing.** ⚠️ **`D-1`'s congruence result is `EXECUTION EVIDENCE` for a *congruence* claim, not a membership rule** | — |

⛔ **Factivity, the evaluator codomain and `Sat` are NOT reachable.**

# G. Part VII — the three propositions, kept apart

| | status |
|---|---|
| ⭐ **`P1`** the corpus identifies `N-4` as a **candidate** normative act | ⭐⭐ **TRUE `[EMP]`** — in **one** register; ⚠️ **and a second register retires the same question** |
| **`P2`** the corpus **authorizes** `N-4` to be enacted | ⛔ **FALSE / `[UNWITNESSED]`** — no jurisdiction, no put/approve/ratify rule |
| **`P3`** the corpus establishes `N-4` must be enacted **first** | ⛔ **FALSE — explicitly DOWNGRADED**, and *"the corpus selects none"* |

⭐ **`P1` implies neither.**

# H. Status

**`[EMP]`** ⭐⭐ three `N`-registers, colliding on `N-1`–`N-3` and `N-4` · `D-1` **DERIVED–RETIRED** vs
`N-4` **NORMATIVE** · **`0 of 29`** vs 4 live · **15** vs **22** operations · `CLOSED/PARAMETRIC` and
its gloss · five meanings of *"earlier"*, **4 and 5 disagreeing** · **7 source nodes, 4 acts** ·
`N-1`/`N-2` `CLOSED/NORMATIVE`, one implemented **132/132** · *"lane-qualified"* = **0 files**.
**`[DERIVED]`** ⭐⭐⭐ **no canonical register, no namespace rule, no crosswalk, no precedence rule —
the collision requires a new governance act** · ⭐⭐ **sequencing is itself an unrecorded governance
choice** · ⭐⭐ **the owner is CONTESTED — ARB vs deployment** · **`EKS-38` is a distinct consequence
class.**
**`[UNWITNESSED]`** ARB's jurisdiction · put/approve/ratify/record roles · any rule that identical IDs
across lanes are intentionally distinct · register entries for `Qualify`, `assertion`, `temporal`.
⛔ **Not upgraded:** `CLOSED/PARAMETRIC` is **one lane's `[DERIVED]` classification, not an adopted
rule** — I use it to *explain* the conflict, ⛔ **not to resolve it.**
**Governance:** `|K| = 11 · [REC] · UNFROZEN`.

# I. Backlog

⛔ **No new number.** ⭐⭐ **`EKS-37` extended** — it asked that an open item *"carry an agreed type —
derivation, decision, experiment or definition"*, and its §5 warned that ⭐ ***"a vocabulary that forces
a single label will produce exactly this conflict again."*** ⭐⭐⭐ **It has: the corpus needs a fifth
type — DEPLOYMENT PARAMETER — and without it one lane types `𝒪`/`𝒯` a decision for ARB while another
types it a parameter for a deployment.** ⭐ **Eighteenth record in twenty-seven with no new item.**

### J. THE ONE SMALLEST NEXT ACTION

$$\boxed{\begin{array}{c}\textbf{⭐⭐⭐ Put the three } N\textbf{-registers side by side and record, } \mathbf{for\ each\ colliding\ ID}\textbf{, which}\\ \textbf{register it belongs to and what its act is — } \mathbf{a\ crosswalk,\ not\ a\ decision.}\end{array}}$$

⭐⭐ **It is the smallest because it needs no authority, no new evidence and no choice** — the three
registers are readable, short, and already written. ⛔ **And it is prior to everything else here:
without it, the sequencing question cannot even be *posed* unambiguously**, since two of its four
candidate acts are named by an identifier that denotes three different things.

⚠️ ⭐ **And it must record the `D-1`/`N-4` disposition conflict as a conflict** — ⛔ **not resolve it.**

---

# K. ⚠️⚠️ ADDENDUM — `document6.md` (17:25 today), and it corrects one of my edges

⭐ **Three more files were saved while this audit ran:** `document4.md` (16:50), `document5.md` (17:10),
`document6.md` (17:25). ⛔ **`document4` and `document5` are NOT read** — recorded so the gap is visible.

## K.1 ⭐⭐⭐ The correction I accept

> ***"The corpus does not establish that `𝒯` determines `ℛ_req`."*** The relation is *"related in one
> historical proposal, but that relationship was never closed as a corpus-grounded implication."*

⚠️ **`P-87` §5 marked `𝒯 → ℛ_req` as `DERIVED`.** ⛔ **Too strong — and internally inconsistent with my
own `P-87` §6**, which established the theorem's home package is **`[ADVISORY]`, never ratified.**
⭐⭐ **The edge should have read `PROPOSED`.** ⛔ **`P-87` not rewritten; corrected here.**

## K.2 ⭐⭐⭐ And `𝒯` is **not** the immediate `Sat` blocker

> ⭐ ***"`𝒯` unresolved ⇏ `Sat` cannot be locally constructed"*** — because a restricted `Sat*` was
> **already constructed without closing `𝒯`.**

⇒ ⭐⭐ **`𝒯` is an upstream dependency for *sufficiency and minimality*, ⛔ not the cause of the current
`Sat` failure.** ⚠️ **That qualifies this audit's §F: `𝒯` blocks less than the chain implied.**

## K.3 ⭐⭐⭐⭐⭐ The object it names instead — and it is new to Lane T

$$\boxed{\begin{array}{c}\textbf{⭐⭐ The } \mathbf{Requirement\text{–}Evaluation\ Semantic\ Boundary.}\\[4pt] \textbf{*"the requirement shape } r=(component_r, Accept_r) \textbf{ } \mathbf{never\ takes\ }EC_t\mathbf{\ as\ an\ argument}\textbf{"*}\\ \textbf{and *"} \mathbf{EpistemicContract\ and\ AcceptanceCondition\ are\ different\ bounded\ contexts}\\ \mathbf{with\ no\ stated\ connection}\textbf{"*} \;\Rightarrow\; \boxed{EC_t \nrightarrow AcceptanceCondition}\end{array}}$$

⭐⭐⭐ ***"We first need to know what the evaluator is supposed to evaluate."*** ⛔ **And the executable
`Sat*` is a SURROGATE** — three modelling choices: restrict `K_t` to one variant, restrict requirements
to one shaped subtype, treat enumerated domains as flat.

⚠️ ⛔ **`MD-061`/`MD-062` are 3MC-lane records — `[UNVERIFIABLE]` here. Relayed, not adopted.**

## K.4 ⭐⭐ What it corroborates, independently

⭐ *"`[PROPOSED RATIFICATION]` despite the filename containing `RATIFIED`"* and *"no locatable
governance acts for `ℛ_req`/`SPEC-RREQ`"* — **`P-87` §6, reached by another route.**
⭐ *"`𝒯 ⊆ 𝒪` present; no rule says which operations are MANDATORY"* — **`P-88`/`P-89`.**
⭐ *"tested `ℛ_req` ≠ universally ratified `ℛ_req`"* — **`P-88` §0.1's concession.**
⭐⭐⭐ **And it dissolves a count I flagged:** the tested set is ***"11 conditions; 12 required
distinction PAIRS"*** — ⭐⭐ **two different objects, which is the likely source of the 11-vs-12
discrepancy `P-87` §4 recorded as an arithmetic error.** ⚠️ **`P-87`'s observation stands** *(the
package's own header/row/summary still disagree)*, ⭐ **but the explanation is now available and it is
not simple miscounting.**

## K.5 ⛔ What does NOT change

**A–E of this audit stand:** three colliding registers · no namespace rule · multiple valid orders ·
authority unresolved and now **contested** · `EKS-38` confirmed. ⭐ **`document6` is about the semantic
chain, not the governance surface** — and it **reinforces** §J: it too finds the estate's dispositions
scattered across lanes that do not cite one another.

```
A. N-REGISTER STATUS — REQUIRES A NEW GOVERNANCE ACT.

There are THREE N-registers, not two. The third, in gap-discovery/second-order/, collides again: its
N-1 is "Authority is exogenous" (CLOSED/NORMATIVE, implemented 132/132) and its N-2 is "Minimality is
relative to T, not ontological" (CLOSED/NORMATIVE). And on the blocking item the registers say OPPOSITE
things: the kernel register has "22 CURRENTLY IDENTIFIED, completeness not demonstrated", mandatory
membership NOT derivable after 8 routes, NORMATIVE, ARB, four live items; the second-order register has
"O IS enumerated: 15 operations", D-1 DERIVED and RETIRED, and ZERO of 29 open normative nodes.

THE CORPUS DOES CONTAIN THE RECONCILING MECHANISM, BUT IT IS NOT A GOVERNANCE RULE. second-order/05
marks Operation CLOSED/PARAMETRIC: "15 named, 5 classes; the set is open BY DESIGN", and
"CLOSED/PARAMETRIC is not a euphemism for open — the node is fully determined once a deployment
declares its parameter, and the corpus deliberately declines to declare it." So both registers are right
about different consequents: the choice cannot change the congruence answer (D-1), and it is still
undeclared (N-4). BUT NO DOCUMENT PERFORMS THAT RECONCILIATION, and the two name DIFFERENT OWNERS —
ARB in one, a DEPLOYMENT in the other. I do not choose between them, and I do not upgrade
CLOSED/PARAMETRIC, which is one lane's [DERIVED] classification and not an adopted rule.

No canonical register, no namespace rule, no crosswalk, no precedence or supersession rule, and no rule
saying identical IDs across lanes are intentionally distinct — "lane-qualified" returns zero files. The
bare identifier cannot safely be used; the minimum unambiguous reference is register path + ID + the
act's subject in words.

B. SEQUENCING STATUS — MULTIPLE VALID ORDERS AND NO RULE THAT SELECTS ONE. The corpus supplies five
meanings of "earlier" and tests N-4 against each: not earliest by dependency (SEVEN source nodes have
in-degree zero), partly by derivation, partly by governance, YES by implementation, and NO by
canonicalization — which needs `evidence`, a separate root. Meanings 4 and 5 DISAGREE, and the corpus
records that they "were being conflated". §17 states outright that the corpus selects none. No adopted
sequencing rule exists; the candidate rules appear as observed orderings, never as binding statements.

C. DEPENDENCY GRAPH: two [DERIVED] edges (N-4 -> N-3 -> N-1'), and NO edge from N-4 to N-8, policy or
N-13, all three recorded "blocked by: nothing". FOUR VALID ROOTS, no unique first act, so SEQUENCING IS
ITSELF AN UNRECORDED GOVERNANCE CHOICE. And a gap the four-act framing hides: seven source nodes were
measured but only four are carried as acts — Qualify, assertion and temporal have in-degree zero and no
register entry, so even "four unblocked acts" is not established. Flagged, not extended.

D. AUTHORITY — UNRESOLVED, which is stronger than P-89's INDICATED, and for a new reason: not only is
jurisdiction unwitnessed and no put/approve/ratify/record role stated, but the OWNER IS NOW CONTESTED,
ARB versus a deployment. The estate demonstrably CAN enact — N-1 is implemented 132/132 with a
fail-closed resolver — so the failure is not inability to act; it is that these items have no route.

E. EKS-38 CONFIRMED AND STRENGTHENED, not merely another EKS-27: the object is an identifier naming an
ACT, the failure is a WRONG ENACTMENT, and the remedy is a governance register rather than a glyph one.
The third register raises the stakes — an instruction to "enact N-4" could be answered "that was
retired", with a citation.

F. SEMANTIC CONSEQUENCE: T still blocked (but possibly as a DEPLOYMENT PARAMETER rather than a theory
decision); R_req still conditional; Eval and Sat still blocked; NOTHING newly derivable — D-1's
congruence result is EXECUTION EVIDENCE for a congruence claim, not a membership rule. Factivity, the
evaluator codomain and Sat are not reachable.

G. P1 is TRUE in one register while a second retires the same question; P2 is FALSE/UNWITNESSED; P3 is
FALSE and explicitly DOWNGRADED. P1 implies neither.

Backlog: no new number. EKS-37 extended — it asked that an open item carry an agreed type and warned
that "a vocabulary that forces a single label will produce exactly this conflict again". It has: the
corpus needs a fifth type, DEPLOYMENT PARAMETER, without which one lane types O/T a decision for ARB
while another types it a parameter for a deployment.

ADDENDUM — document6.md (17:25) corrects one of my edges. P-87 §5 marked T -> R_req as DERIVED; that was
too strong and inconsistent with my own P-87 §6, which established the theorem's home package is
[ADVISORY]. The edge should have read PROPOSED. And T is NOT the immediate Sat blocker — "T unresolved
does not imply Sat cannot be locally constructed", since a restricted Sat* was already built without
closing T; T is upstream for sufficiency and minimality, not the cause of the current failure. The
object it names instead is new to Lane T: the REQUIREMENT-EVALUATION SEMANTIC BOUNDARY — the requirement
shape r = (component_r, Accept_r) NEVER TAKES EC_t AS AN ARGUMENT, and EpistemicContract and
AcceptanceCondition are different bounded contexts with no stated connection, so EC_t does not reach
AcceptanceCondition. "We first need to know what the evaluator is supposed to evaluate." Those records
are 3MC-lane and [UNVERIFIABLE] here; relayed, not adopted. It independently corroborates P-87 §6
("[PROPOSED RATIFICATION] despite a filename containing RATIFIED"; "no locatable governance acts"),
P-88/P-89 on mandatory membership, and P-88's concession — and it dissolves a count I flagged: the
tested set is "11 conditions; 12 required distinction PAIRS", two different objects, which is the likely
source of the 11-vs-12 discrepancy. A-E of this audit stand. document4.md (16:50) and document5.md
(17:10) exist and are NOT read; recorded so the gap is visible.

|K| = 11, [REC], UNFROZEN. No 3MC inspection.

THE ONE SMALLEST NEXT ACTION: put the three N-registers side by side and record, for each colliding ID,
  which register it belongs to and what its act is — A CROSSWALK, NOT A DECISION. It is smallest because
  it needs no authority, no new evidence and no choice: the three registers are readable, short and
  already written. And it is prior to everything else, because without it the sequencing question cannot
  even be POSED unambiguously — two of its four candidate acts are named by an identifier that denotes
  three different things. It must record the D-1/N-4 disposition conflict AS a conflict, not resolve it.
```

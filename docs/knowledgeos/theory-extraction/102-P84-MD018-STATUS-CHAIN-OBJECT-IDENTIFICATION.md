# `P-84` — MD-018 `status_chain` Object Identification

**2026-09-09 · Lane T.** After [`101` `P-83`](./101-P83-CORRECTION-VS-REGRESSION-DISCRIMINATOR-AUDIT.md).
**Narrow object-identification and provenance audit.** ⛔ **Not a request to redefine `R0`, choose a
governance scope, or reopen `O-P08-1`.**

# 0. ⚠️ DISAGREEMENT — and I concede the reframing

## 0.1 ⭐⭐ The commissioner's correction is right, and my `P-83` question was the weaker one

⭐ *"Which of `P-22`'s eight subject levels does `R0` range over?"* **invites Governance to choose.**
⭐⭐ *"What is the `status_chain` object, and which level actually bears it?"* **is answerable from the
record.** ⛔ **I adopt the reframing without reservation** — and §3–§4 show it was the right call,
because **the record answers it.**

## 0.2 ⚠️ One disagreement — the prompt's §4 outcome list needs a fifth value

⭐⭐⭐ **The bearer population is NOT HOMOGENEOUS**, so `EXACTLY MAPPED / MULTIPLE LEVELS / NO MAPPING /
UNDECIDABLE` cannot express the finding. The register's **indexing axis changes mid-file**: 27 entries
are concepts, 96 are **file landmarks**. ⛔ **A file is not one of `P-22`'s eight levels at all.** So
the honest verdict is **`MULTIPLE LEVELS` — *and part of the population is off the scale entirely*.**

## 0.3 ⚠️ One thing I must state before §3

⛔ **`MD-018`'s primary text is behind the firewall.** `P-22` §1.1 and artifact `20` both attest that
**`MD-001`…`MD-021` is 3MC's `model-boundary-decisions.md`**, and `INDEX.md` §238 gives the register's
path as **`three_model_convergence/01_source-analysis/dimension-registry.md`**. ⭐ **Everything below is
read from PERMITTED files that quote them.** ⛔ **No inspection was made.**

# 1. §1 — `EKS-30` check

| probe | result |
|---|---|
| an artifact identifying `MD-018.status_chain`, its bearer, or a `P-22`↔MD-018 map | ⛔ **none of 102** |
| artifacts mentioning `status_chain` | **12** — all *use* it, ⛔ **none identifies the object** |
| artifacts discussing subject levels | ⭐ **2 — `P-22` and `P-83`** |

⭐ **No prior audit.** ⭐⭐ **But `P-22` PART I already holds half the answer, and §4 uses it rather than
re-deriving it.**

# 2. Executive verdict

$$\boxed{\begin{array}{c}\textbf{⭐ The object is an } \mathbf{ORDERED\ STATUS\ VOCABULARY}\textbf{, realized as a } \mathbf{Status:}\ \textbf{FIELD on register entries.}\\[4pt] \textbf{⭐⭐ The } \mathbf{value} \textbf{ maps to } P\text{-}22\textbf{'s } \mathit{currentness}\ (K2)\textbf{; the } \mathbf{bearer} \textbf{ maps to } \mathit{candidate\ identity}\ (K1)\\ \textbf{for 27 entries and to } \mathbf{NO\ LEVEL\ AT\ ALL} \textbf{ for 96.}\end{array}}$$

$$|K| = 11 \cdot \mathbf{[REC]} \cdot \mathbf{UNFROZEN}\textbf{ — unchanged.}$$

# 3. §3 — the object

⭐ Quoted **identically at two independent permitted sites** — `gap-discovery/INDEX.md` §242 and
`theory-inventory-assessment/README.md` §54:

$$\texttt{candidate} \to \texttt{supported} \to \texttt{corroborated} \to \texttt{formally\_defined} \to \texttt{operationally\_defined} \to \texttt{canonical}$$

| candidate reading | verdict |
|---|---|
| **a status vocabulary** | ⭐⭐ ✅ **YES — six named values** `[EMP]` (2 citation sites) |
| ⭐ **with an ORDERING** | ⭐⭐ ✅ **YES** — the arrows, plus *"no entry may skip a stage"* ×3 (`P-09` §3) |
| **a field** | ⭐⭐ ✅ **YES** — `README` §72: ***`Status:` + MD-018 chain, 28 occurrences*** |
| **a state machine** | ⛔ **not established** — no transition function, no guards, no events |
| **a relation** | ⛔ not established | 
| **a lifecycle** | ⚠️ **only by the ordering; no lifecycle operations are given** |
| **a transition sequence** | ⛔ ⭐ **NO — and this is the sharpest negative:** `P-82` §6.1's ***"no entry has left `candidate`"*** means **no sequence has ever been traversed** |

$$\boxed{\textbf{⭐⭐ An } \mathbf{ordered\ vocabulary\ realized\ as\ a\ field.}\ \textbf{⛔ Not a state machine, and not (yet) a traversed sequence.}}$$

⛔ **Nothing inferred from the name.** ⚠️ **`[UNWITNESSED]` as to the primary definition in `MD-018`.**

# 4. §4 — the mapping to `P-22`

## 4.1 The VALUE — ⭐⭐ already mapped, and `P-22` did it

`P-22` PART I, verbatim row:

| concept | corpus evidence | needed by the kernel? |
|---|---|---|
| **currentness** | ⚠️ ⭐ **weakly `[EMP]` — MD-018 status values**, `[RF]` markings, *"operative"* readings | ✅ **`K2`** |

$$\boxed{\textbf{⭐⭐⭐ } \mathbf{EXACTLY\ MAPPED:} \textbf{ the status VALUE is } \mathit{currentness}\textbf{, and } \mathit{currentness} \textbf{ is } K2\textbf{'s cell.}}$$

## 4.2 The BEARER — ⭐⭐⭐ and `P-22` explicitly rules one candidate OUT

`P-22` §1.2 is titled ***"The critical test — is *assertion* the bearer of currentness?"*** and answers:

$$\boxed{\begin{array}{c}\textbf{NO — and the corpus denies it: MD-017's } \mathit{unresolved\_equivalence} \textbf{ is the DEFAULT across 125 files,}\\ \textbf{so the estate holds } \mathbf{COMPETING\ CLAIMS\ SIMULTANEOUSLY}\textbf{, and } \mathit{definition} \to \mathit{candidate} \textbf{ is } \mathbf{0..N\ [OPEN]}.\end{array}}$$

⭐⭐ **What DOES bear it, from the permitted corpus:** *"no **entry** has left `candidate`"* ⇒ the bearer
is a **register entry**. And `README` §19 gives the population, ⭐ **with a correction dated 2026-09-07:**

| | |
|---|---|
| ⛔ **as first reported** | *"123 concept entries"* |
| ⭐⭐⭐ **as corrected** | **27 CONCEPT entries + 96 FILE-LANDMARK entries** — *"after entry 27 the registry **CHANGED INDEXING AXIS** from concept to file"* |
| ⚠️ **consequence stated by that lane** | *"the concept-coverage gap is **4× larger** than I stated"* |

⭐⭐ **A concept entry is keyed by concept NAME** (`README` §72: ***"ID — 0 — entries keyed by concept
name"***) ⇒ it sits at `P-22`'s **`candidate identity`** level — *"the thing definitions are about"*,
`[EMP]` via glyph registers, **`K1`**.
⛔ **A file-landmark entry is a FILE. `P-22` has no such level.**

$$\boxed{\begin{array}{c}\mathbf{MULTIPLE\ LEVELS\ —\ AND\ PART\ OF\ THE\ POPULATION\ IS\ OFF\ THE\ SCALE.}\\[4pt] \textbf{bearer } = \mathit{candidate\ identity}\ (K1) \textbf{ for } \mathbf{27} \quad\cdot\quad \textbf{no level for } \mathbf{96} \quad\cdot\quad \textbf{value } = \mathit{currentness}\ (K2)\\[4pt] \boxed{\textbf{⭐⭐ The bearer and the borne sit at } \mathbf{DIFFERENT} \textbf{ levels — } K1 \textbf{ holds, } K2 \textbf{ is held.}}\end{array}}$$

⚠️ ⭐ **One arithmetic observation, recorded not resolved:** `Status:` occurs **28** times over a
population containing only **27** concept entries. ⇒ **either one concept carries two, or a
file-landmark carries a status.** ⛔ **Not resolvable from permitted files.**

⚠️ ⭐⭐ **And a glyph collision, recorded:** `candidate` is **both** the chain's first VALUE **and**
`P-22`'s subject LEVEL *candidate identity*. ⭐ **`EKS-27`'s family; recorded, not filed** *(fifth
occurrence of one cause)*.

# 5. §5 — `R0` scope test

> *"MD-018 permits no regression — the status chain is monotone forward-only."*

$$\boxed{\begin{array}{c}\textbf{⭐ It ranges over } \mathbf{exactly\ one\ thing}\textbf{: the ORDER of the six status values,}\\ \textbf{as borne by the } \mathbf{Status:} \textbf{ field of } \mathtt{dimension\text{-}registry.md} \textbf{ entries.}\end{array}}$$

⛔ **Not broadened:** MD-015's ladder · MD-017 · `Q3` · `Q4` · Lane M's kernel · general KnowledgeOS
status semantics — ⭐ **all excluded, exactly as `INTAKE-001` §1 already scoped it.** ⭐⭐ **`P-82`'s
boundary is confirmed by an independent route: the object turns out to be narrower than the ruling's
words might suggest, not wider.**

# 6. §6 — effect on `O-INT001-1`

$$\boxed{\mathbf{PARTIALLY.}}$$

| | |
|---|---|
| ⭐ **for the 27 concept entries** | ✅ **DETERMINATE.** The subject is a **candidate identity**, so *"a change in the subject"* = **the candidate's standing genuinely moved**, versus *"a defect in the prior record"* = **the entry's `Status:` was written wrongly.** ⭐⭐ **`P-83`'s `[OPEN]` term now has a referent** |
| ⛔ **for the 96 file-landmark entries** | 🔴 **NOT DETERMINATE — a file has no epistemic standing to regress** |
| ⚠️ **and even for the 27** | ⭐ `P-22`: the estate holds **competing claims simultaneously**, and currentness is *"a RELATION, SET-VALUED — `Current(h,p)`"* ⇒ ⛔ ***"the prior record" may not be unique*** |
| ⛔ **the classifier itself** | **NOT SOLVED, and not attempted** — `P-83`'s other three gaps stand |

⭐⭐ **So `P-84` closes one of `P-83`'s four definitional gaps and leaves three** — and it closes it
**from the record**, inventing nothing.

# 7. §8 — required output

| item | finding | status |
|---|---|---|
| **`status_chain` object** | ⭐ an **ORDERED STATUS VOCABULARY** of six values, **realized as a `Status:` field**; ⛔ **not a state machine, and never traversed** | ⭐ **`[EMP]`** (2 permitted citation sites) · ⚠️ **`[UNWITNESSED]`** as to `MD-018`'s primary text |
| **bearer** | ⭐⭐ a **`dimension-registry.md` entry** — ⭐⭐⭐ **27 concept entries + 96 file landmarks, after a mid-file indexing-axis change**; keyed by **name**, ⛔ **no IDs** | ⭐ **`[EMP]`** via the 2026-09-07 correction · ⛔ **primary firewalled** |
| **`P-22` mapping** | ⭐⭐ **MULTIPLE LEVELS** — value = *currentness* (`K2`) **EXACTLY MAPPED by `P-22` itself**; bearer = *candidate identity* (`K1`) for 27, ⛔ **no level** for 96; ⭐ *assertion* **explicitly ruled out** | ⭐ **`[DERIVED]`** on `[EMP]` rows |
| **`R0` scope** | ⭐ the **order of the six values on that field** — ⛔ nothing wider | ⭐ **`[EMP]`** (`INTAKE-001` §1) |
| **effect on `O-INT001-1`** | ⭐ **PARTIALLY** — determinate for 27, ⛔ not for 96, and *"the prior record"* may not be unique | ⭐ **`[QUALIFIED]`** |

# 8. §7 — stopping rule

⭐⭐ **Stopped on condition ② — *"the corpus explicitly maps it to a `P-22` level."*** `P-22` PART I maps
the value; the 2026-09-07 register correction identifies the bearer population. ⛔ **No replacement
definition derived. ⛔ No level selected by me.**

# 9. Branch decision

$$\boxed{\mathbf{P\text{-}84\ CLOSES\ THE\ R0/CORRECTION\ BRANCH.}}$$

⭐⭐ **And the residue is a BOUNDARY, not a gap.** `MD-018`'s primary text and
`dimension-registry.md` are **unreadable by Lane T by construction** — ⛔ **so no further Lane T audit
can advance this**, and commissioning one would be `EKS-30`'s failure by a new route. ⭐ **Handed to
whoever may read 3MC; not to this lane.**
⛔ **No further persistence audit is created.**

# 10. Backlog

⭐⭐ **`EKS-33` filed — new, and important.** ⛔ **Not the register's measurements** — the other lane has
already measured and corrected them, and owns them. ⭐⭐⭐ **What no one has recorded is the GOVERNANCE
CONSEQUENCE: a binding ruling ranges over a field on a register that has no identifiers, mixes two
kinds of entry, and carries that field on 28 of 123 rows.** Checked against `EKS-32` *(where the ruling
is FILED — this is what the ruling GOVERNS)*, `EKS-06`, `EKS-13`, `EKS-19`, `EKS-22` (`ES-005.4`).

### 11. HANDOFF — §9

$$\boxed{\textbf{⭐⭐⭐ Branch closed. Next work: } \mathbf{HISTORICAL\ F4\ SEMANTIC\text{-}BACKBONE\ COMPLETION\ RECOVERY.}}$$

⭐ **Targets, as commissioned:** `K_t ∈ 𝕂` · `r(K_t)=x_t` · `Obs` · `EC` · `Sat` · `Δ_t` · `T` · `H_t` ·
`≡sem` — **which are typed, F4-specific, grounded, conflicting, or open.**
⚠️ **Two conditions I must carry into it, from today's read:**
**①** ⛔ **`M0132`, the Model-B concept register and `MD-058` are UNREADABLE by Lane T** — every claim
resting on them is `[UNVERIFIABLE]` here, exactly as `dimension-registry.md` is.
**②** ⭐ **The prompt itself has not yet reached this conversation.** ⛔ **I do not begin it unprompted.**
⛔ **The 11-cell kernel is NOT reopened because an F4 dependency is unresolved.**

$$|K| = 11 \cdot \mathbf{[REC]} \cdot \mathbf{UNFROZEN} \cdot \textbf{firewall intact.}$$

```
P-84 CLOSES THE R0/CORRECTION BRANCH.

DISAGREEMENT AND CONCESSION: the commissioner's reframing is right and my P-83 question was the weaker
one — "which level does R0 range over?" invites Governance to choose; "what is the object and what
bears it?" is answerable from the record, and it was. One disagreement: §4's outcome list needs a fifth
value, because the bearer population is NOT HOMOGENEOUS — the register's indexing axis changes mid-file
and a FILE is not one of P-22's eight levels at all.

THE OBJECT: an ORDERED STATUS VOCABULARY of six values — candidate, supported, corroborated,
formally_defined, operationally_defined, canonical — quoted identically at two independent PERMITTED
sites, and realized as a `Status:` FIELD (28 occurrences). NOT a state machine: no transition function,
no guards, no events. And NOT a traversed sequence — "no entry has left candidate". [UNWITNESSED] as to
MD-018's primary text, which is behind the firewall.

THE MAPPING — AND P-22 ALREADY DID HALF OF IT. P-22 PART I maps "currentness" to "MD-018 status values"
and to K2. So the VALUE is EXACTLY MAPPED. And P-22 §1.2 is literally titled "is ASSERTION the bearer of
currentness?" and answers NO, because MD-017's unresolved_equivalence is the DEFAULT across 125 files
and the estate holds COMPETING CLAIMS SIMULTANEOUSLY.

THE BEARER is a dimension-registry entry — and the population is heterogeneous. The "123 concept
entries" figure was CORRECTED on 2026-09-07 to 27 CONCEPT entries + 96 FILE-LANDMARK entries, because
"after entry 27 the registry CHANGED INDEXING AXIS from concept to file". A concept entry is keyed by
concept NAME (ID — 0), so it sits at P-22's CANDIDATE IDENTITY level (K1). A file-landmark entry is a
FILE, and P-22 has no such level. So: MULTIPLE LEVELS, AND PART OF THE POPULATION IS OFF THE SCALE —
bearer = candidate identity (K1) for 27, no level for 96, value = currentness (K2). THE BEARER AND THE
BORNE SIT AT DIFFERENT LEVELS: K1 holds, K2 is held.

Recorded not resolved: `Status:` occurs 28 times over a population of only 27 concept entries, so either
one concept carries two or a file-landmark carries a status — not resolvable from permitted files. And a
glyph collision: `candidate` is BOTH the chain's first VALUE and P-22's subject LEVEL "candidate
identity" (EKS-27's family, fifth occurrence, recorded not filed).

R0 SCOPE: it ranges over exactly the ORDER of the six values as borne by that field. Not MD-015, MD-017,
Q3, Q4, Lane M's kernel, or general KnowledgeOS status semantics. P-82's boundary is confirmed by an
independent route — the object is NARROWER than the ruling's words might suggest, not wider.

EFFECT ON O-INT001-1: PARTIALLY. For the 27 concept entries the subject is a CANDIDATE IDENTITY, so "a
change in the subject" = the candidate's standing genuinely moved, versus "a defect in the prior record"
= the Status field was written wrongly — P-83's [OPEN] term now has a referent. For the 96 file
landmarks it stays indeterminate: a file has no epistemic standing to regress. And even for the 27,
currentness is "a RELATION, SET-VALUED — Current(h,p)", so "the prior record" may not be unique. One of
P-83's four gaps closed, three stand, and the classifier itself was not attempted.

STOPPED on condition 2 — the corpus explicitly maps it to a P-22 level. No replacement definition
derived; no level selected by me.

THE RESIDUE IS A BOUNDARY, NOT A GAP. MD-018's primary text and dimension-registry.md are unreadable by
Lane T BY CONSTRUCTION, so no further Lane T audit can advance this, and commissioning one would be
EKS-30's failure by a new route. Handed to whoever may read 3MC. No further persistence audit created.

Backlog: EKS-33 filed — not the register's measurements, which the other lane already made, corrected
and owns, but the GOVERNANCE CONSEQUENCE nobody has recorded: a binding ruling ranges over a field on a
register with no identifiers, two kinds of entry, and that field on 28 of 123 rows.

HANDOFF: Historical F4 Semantic-Backbone Completion Recovery — K_t, r(K_t)=x_t, Obs, EC, Sat, Delta_t,
T, H_t, =sem. Two carried conditions: (1) M0132, the Model-B concept register and MD-058 are UNREADABLE
by Lane T, so every claim resting on them is [UNVERIFIABLE] here, exactly as dimension-registry.md is;
(2) the prompt has not yet reached this conversation and I do not begin it unprompted. The 11-cell
kernel is NOT reopened because an F4 dependency is unresolved.

|K| = 11, [REC], UNFROZEN. NO three_model_convergence INSPECTION — excluded by path, unread.
```

# `P-38` — `K1` Payload: Adjudication, Identity Mapping & Identity Continuity Audit

**2026-09-08 · Lane T.** After [`55` `P-37`](./55-P37-OPEN-WORLD-CLOSED-WORLD-IDENTITY-SEMANTICS-AUDIT.md).

# 0. Executive verdict

$$\boxed{\begin{array}{c}\mathbf{K1\text{-}C} \textbf{ — non-regenerable identity JUDGMENTS sufficient to preserve historical identity continuity.}\\[6pt] \boxed{\textbf{⭐⭐⭐ } P\text{-}08\textbf{'s own forcing witness } W1 \textbf{ names the payload outright, and neither } P\text{-}36 \textbf{ nor } P\text{-}37 \textbf{ consulted it:}}\\[4pt] W1\textbf{: } \textit{"two different } \mathbf{candidates} \textit{ collapse into one identity"} \;\Rightarrow\; \textbf{"} \mathbf{one\ bit\ per\ pair:\ SameId\ or\ not\ —\ an\ adjudication,\ not\ a\ computation} \textbf{"}\end{array}}$$

⭐⭐⭐⭐ **Two structural findings, both larger than adjudication-vs-mapping:**

**(i) A LEVEL MISMATCH.** `K1`'s witnesses are **theory-register** objects — *candidates*,
*definitions*, *associations*, `Zero` through `[RF]`, `ℐ`, `𝒦`, `Σ`, `D-03`. **`§25S` is about
DOMAIN-ENTITY resolution** — `nexus3`, production vs development repositories, `IdentityKey =
(Organization, Environment, Hostname, SystemID)`, `KAID`. ⭐⭐ **`P-36` and `P-37` imported `§25S` as
evidence about `K1`'s payload without establishing that the levels correspond.**

**(ii) `W1` ASKS FOR BOTH POLARITIES — *"`SameId` **or not**"*.** A mapping records only the
positives. ⇒ ⭐⭐⭐ **the mapping is INSUFFICIENT, and `P-37`'s narrowing to `{Same}` is REVERSED** —
it rested on the domain-level witness rather than on `P-08`'s own.

$$\boxed{|K| = 11 \textbf{ — unchanged. ⭐ } P\text{-}38 \textbf{ identified } K1\textbf{'s payload; it added, merged and re-scoped nothing.}}$$

# 1. Boundary and firewall
⛔ **No `three_model_convergence/`** — no listing, filename, path, metadata, existence check, content,
finding or comparison; the string appears only as a search suppression pattern.
✅ **`external_research/` inspected, as newly permitted** — see §20.
⛔ No Schema v3 · architecture · implementation · canonicalization · new cell · silent `P-37` repair.

⚠️ **Four declared deviations.**
**(a)** ⭐⭐ **`P-37` contradicted ITSELF, and this audit resolves it.** Its §17 marked the adjudication
*"⚠️ only implicitly"* retained; its §6–11 asserted *"retention witness? **YES** — `§25S.20`'s preserved
MAPPING"*. **The stronger cell carried `P-37`'s verdict.** ⭐ **§9 below resolves it in favour of the
hedge.**
**(b)** Two versions of the commission arrived; **the second, fuller version is executed** *(it adds
Claim 2 and the §4 warning)*.
**(c)** ⭐ **Count comparability preserved:** every KnowledgeOS-corpus search kept the identical
exclusion basis used in `P-29`–`P-37`, and `external_research/` was run as a **separate labelled
channel** so the seven-audit measurement series stays intact.
**(d)** ⭐ **Pre-registered `K1-E`, with my record stated as 1-correct-of-3.** ⚠️ **WRONG — the answer
is `K1-C`.** Now **1 of 4**; recorded, not dropped.

# 2. ⭐⭐⭐ `P-08` reconstruction — before any `25S` terminology

$$\mathbf{K1}\ \textbf{IDENTITY PRESERVATION — for the three adjudicated identities; non-regenerable}$$

| `P-07` identity | what it is | witnesses |
|---|---|---|
| **candidate** | ⭐ a **theory candidate** — *"must be assertable as the same candidate across its own DISCOVERY"* | `ℐ`, `𝒦`, `Σ`, `Zero` |
| **definition** | a definition, glyph-addressed | `D-03`, `D-05` |
| **association** | ⭐ *"definition `D` bears relation `R` to candidate `C`"*, **with its own lifecycle** | `K` `D-03`/`D-05` → `𝒦` |

| question | answer |
|---|---|
| what is **adjudicated**? | ⭐⭐ **the sameness judgment** — `W1`'s *"one bit per pair"* |
| what **receives** identity? | the three objects above |
| what is **assigned**? | ⭐ the judgment *(`P-35`: assignment attaches to the JUDGMENT, not the label)* |
| what is **mapped**? | ⭐⭐ **nothing in `P-08`** — ⛔ **no mapping appears anywhere in `P-08`'s `K1`** |
| what does *"non-regenerable"* apply to? | ⭐⭐ **the bit** — *"an adjudication, not a computation"* |
| is the mapping part of identity or a consequence? | ⭐⭐⭐ **`[UNWITNESSED]` in `P-08`** |

⭐⭐⭐ **`P-08` never mentions a mapping.** The mapping enters only through `§25S.20`, at a different
level — which is the finding of §3.

# 3. ⭐⭐⭐⭐ Primary-source provenance — the LEVEL MISMATCH

| | `P-07`/`P-08` `K1` | `§25S` |
|---|---|---|
| **objects** | ⭐ candidates · definitions · associations | ⭐ `nexus3` · repositories · servers |
| **identifier** | glyph / natural key / `[RF]` status | ⭐ **`KAID`** *(Knowledge Ātma ID)* |
| **example key** | `Zero`, `D-03` | `IdentityKey=(Organization,Environment,Hostname,SystemID)` |
| **register** | ⭐ **the theory-construction register** | ⭐ **the domain-knowledge register** |

$$\boxed{\begin{array}{c}\textbf{⭐⭐ } K1 \textbf{ was derived ENTIRELY from theory-register witnesses. } \S25S \textbf{ is domain-entity resolution.}\\ \boxed{\textbf{Whether } K1 \textbf{ generalises across that boundary is an } \mathbf{UNSTATED\ ASSUMPTION} \textbf{ that } P\text{-}36 \textbf{ and } P\text{-}37 \textbf{ both relied on. } \mathbf{[OPEN]}.}\end{array}}$$

⭐⭐⭐ **But the analogy is not baseless — and this is the reconciling observation:** `W1` is **itself a
merge witness** — *"two different candidates **collapse into one identity**"* — structurally the same
event as `§25S.20`'s `IdentityMerge`, one level up. ⭐ **So `§25S` is a legitimate ANALOGUE, and an
illegitimate SUBSTITUTE.**

# 4–6. The five objects, and the implication matrix

| | object | status |
|---|---|---|
| **A** identity-bearing object | ⭐ `[EMP]` — candidate / definition / association *(theory)*; entity *(domain)* |
| **B** identity adjudication | ⭐⭐ **`[EMP]` — `W1`'s bit** |
| **C** identifier / label | ⭐ `[EMP]` — glyph, `KAID`; ⚠️ **`P-07` §1.1: a natural key IS an identity** |
| **D** identity mapping | ⭐ **`[EMP]` at domain level only** *(`§25S.20`)*; **`[UNWITNESSED]` in `P-08`** |
| **E** identity continuity | ⭐⭐ **`[DERIVED]`** — `F-CONT`, and `W1`'s failure mode is loss of distinctness |

| implication | verdict |
|---|---|
| ⭐⭐ retained **adjudication** ⇒ recoverable **mapping** | ⭐⭐ **YES — `[DERIVED]`.** `§25S.5` boxes *"`SameAs` **should** behave like an equivalence relation"*; the canonical class is its closure ⚠️ *(the premise is `[STIPULATED]` — *"Ideally"*, *"should"*)* |
| ⭐⭐⭐ retained **mapping** ⇒ recoverable **adjudication** | ⭐⭐⭐ **PARTIAL — `[REFUTED]` as stated.** A mapping records **merges only**; the *"or not"* half of `W1`'s bit is unrecoverable |
| retained **mapping** ⇒ recoverable **continuity** | ⭐ **YES** — for merged classes |
| retained **continuity** ⇒ recoverable **mapping** | ⚠️ **`[OPEN]`** |
| retained **continuity** ⇒ recoverable **adjudication** | ⚠️ **`[OPEN]`** |
| retained **historical identity** ⇒ recoverable **mapping** | ⛔ **NO** — labels alone give no relation |
| retained **mapping** ⇒ retained **historical identity** | ⭐ **YES — `[EMP]`**, the mapping's domain *is* the historical labels |

$$\boxed{\textbf{⭐⭐⭐ ASYMMETRIC: the ADJUDICATION is strictly more informative than the MAPPING. ⇒ Proposition A is NOT equivalent to Proposition B; } \mathbf{A \Rightarrow B} \textbf{ but } \mathbf{B \nRightarrow A}.}$$

# 7–9. `§25S.20` reconstructed, and does merge entail `Same`?

`§25S.20` verbatim: *"initially `KAID_A` and `KAID_B` are **thought to represent different entities** …
later evidence **establishes `SameEntity(A,B)`** … we may need `MergeIdentity(A,B)`. But we should not
destroy the historical identities … the mapping is preserved."*

| question | answer |
|---|---|
| does `IdentityMerge` entail a `Same` adjudication? | ⭐⭐ **YES here — *"later evidence **establishes** `SameEntity(A,B)`"*** is stated as the merge's cause |
| can a merge occur without one? | ⚠️ **`[UNWITNESSED]`** |
| ⭐ what is **explicitly required to survive**? | ⭐⭐ **the historical identity LABELS and the MAPPING** — ⛔ **not a `Resolution` value, not the evidence, not the reason** |
| ⭐⭐⭐ does the mapping **encode** the `Same` finding? | ⭐⭐ **PARTIALLY — `P-37` Claim 2 `[QUALIFIED]`.** It encodes *that a Same class exists*; ⛔ **it does not encode the `¬Same` judgments, nor which pairs were ever adjudicated at all** |

⭐⭐ **This resolves `P-37`'s internal contradiction in favour of its §17 hedge:** the adjudication is
retained **only implicitly, and only for positives.**

⭐ **§25S.21 split:** *"previous assertions may have been attached to the **wrong identity**"*;
`\boxed{IdentityMerge\ and\ IdentitySplit\ are\ revision\ events}`. ⭐⭐ **No retention rule stated ⇒
`[UNWITNESSED]`, not inferred.** ⛔ **Merge and split are NOT assumed symmetric.**

# 10. ⭐⭐ Resolution-value relevance — Claim 6 RESOLVED

$$\boxed{\textbf{⭐⭐⭐ } W1 \textbf{ requires } \mathbf{ONE\ BIT} \textbf{ — binary. The four-valued } \mathit{Resolution} \textbf{ is a } \mathbf{DOMAIN\text{-}LEVEL\ ELABORATION}\textbf{, not } K1\textbf{'s payload.}}$$

| value | relevance to `K1` |
|---|---|
| **`Same`** | ⭐ **the positive pole of the bit** |
| ⭐⭐ **`Different`** | ⭐⭐⭐ **the negative pole — *"or not"* — and therefore IN, contrary to `P-37`** |
| **`PossibleSame`** | ⛔ **not a bit value — outside `W1`** ⇒ **`[OPEN]`, and probably a domain-operation question** |
| **`Unknown`** | ⛔ **outside `W1` entirely** — ⭐ *"one bit per pair"* presupposes a pair **was adjudicated** |

⭐⭐⭐ **`P-37`'s Claim 6 — *"the four-value question may have been about the wrong object"* — is
CONFIRMED, and for a sharper reason than expected: not adjudication-vs-mapping, but a BINARY
theory-register bit versus a FOUR-VALUED domain-register protocol.**

# 11–13. The three counterexamples

| | construction | verdict |
|---|---|---|
| ⭐⭐ **A** mapping survives, adjudication not stored | ⭐⭐ **`K1` PARTIALLY satisfied.** Positive bits are recoverable via the class; ⛔ **negative bits are LOST** ⇒ `W1`'s failure — *two different candidates collapse* — becomes **undetectable for pairs never merged** |
| ⭐ **B** adjudication survives, mapping absent | ⭐⭐ **`K1` SATISFIED** — the mapping is derivable by equivalence closure ⚠️ *(on `§25S.5`'s `[STIPULATED]` transitivity)* |
| **C** same labels, same mapping, different adjudication history | ⭐ **NOT CONSTRUCTIBLE without touching `K3a`** *(prior values)* ⇒ $\mathtt{[UNINFORMATIVE\ FOR\ THIS\ K1\ TEST]}$ — ⛔ **NOT evidence against `K1`**; `P-19` binding |

$$\boxed{\textbf{⭐⭐ A and B together settle it: the ADJUDICATION is sufficient and the MAPPING is not. } \mathbf{K1 \neq mapping.}}$$

# 14–16. Information-theoretic · representation vs persistence

| model | retains | loses | `K1`? |
|---|---|---|---|
| **A** `Resolution` | four-valued domain state | — | ⭐ **over-specified** |
| **B** mapping | positive classes | ⭐⭐ **negative bits; which pairs were adjudicated** | ⛔ **insufficient** |
| **C** continuity | the relation | ⚠️ its ground | `[OPEN]` |
| **D** identity + mapping | labels + classes | negative bits | ⛔ insufficient |
| ⭐ **E** identity + adjudication | ⭐⭐ **labels + both bit polarities** | reasons, context | ⭐⭐ **SUFFICIENT** |
| **F** minimum composite | ⭐⭐⭐ **= E** | — | ⭐ **SELECTED** |

⭐ **Kept apart as §18 demanded:** *representation* (`KAID` strings) · *persistence* (the bit) ·
*recoverability* (the mapping, from the bits) · *auditability* (⚠️ **`[UNWITNESSED]` — no witness
requires demonstrating the historical judgment**) · *continuity* (the relation the bit protects).

# 17–19. DDD · mathematics · measurement

| concept | classification |
|---|---|
| identity-bearing object | ⭐ **Entity** |
| ⭐ **identity adjudication** | ⭐⭐ **Domain Event / adjudication record — `K1`'s payload** |
| identity identifier | **Value Object** *(a natural key qualifies — `P-07` §1.1)* |
| ⭐ **identity mapping** | ⭐ **Domain Relation — a DERIVED projection of the adjudications** |
| identity continuity | ⭐ **invariant** |
| `SameAs` · `DistinctFrom` | **Domain Relations** |
| `IdentityMerge` · `IdentitySplit` | ⭐⭐ **`[EMP]` Domain Events — *"revision events"*** |
| `Resolution` | **Domain-Service output** — ⛔ **no aggregate manufactured** |

$$\boxed{\begin{array}{ll}id : O \rightarrow I & \textbf{⭐ total, assigned } (\mathit{INV\text{-}KOS\text{-}IDENTITY\text{-}001})\\ M \subseteq I_{hist} \times I_{curr} & \textbf{⭐⭐ a RELATION, many-to-one onto canonical; ⚠️ } \mathbf{derived}\textbf{, not primitive}\\ R : \mathit{Pair}(I) \rightharpoonup \{\top,\bot\} & \textbf{⭐ PARTIAL — only adjudicated pairs; ⛔ totality } \mathbf{[REFUTED]}\ (V1)\end{array}}$$

⭐ **Measurement:** the bit is **adjudicated**, ⛔ not observed; the identifier is **assigned**; the
mapping is **inferred** from bits; continuity is a **property**. ⛔ **Identifier ≠ judgment; mapping ≠
measurement; confidence ≠ adjudication.**

# 20. ⭐⭐⭐ External-research quarantine audit — and a scope correction

⚠️ **`external_research/` is NOT third-party literature.** It sits at
`phase_measure_theory/external_research/` (51 files) and contains **HPA supervisory reviews, C4
diagrams, `step_285`, `step_25i`** — ⭐ **KnowledgeOS's own research conducted with external
inspiration.** ⛔ **`entity resolution` and `record linkage` return ZERO there.**

⭐⭐⭐⭐ **One file is directly on point and it produces a genuine conflict** —
`step_25i_knowledge-atma-algebra-identity-continuity-and-transformation` *(`Authority: HPA`, `Status:
DERIVED`)*, §2.2:

$$\mathcal{K}_1 \equiv \mathcal{K}_2 \iff \begin{cases}\mathrm{Prop}(\mathcal{K}_1) \equiv \mathrm{Prop}(\mathcal{K}_2)\\ \mathrm{Ref}(\mathcal{K}_1) \equiv \mathrm{Ref}(\mathcal{K}_2)\\ \mathrm{TruthCond}(\mathcal{K}_1) \equiv \mathrm{TruthCond}(\mathcal{K}_2)\end{cases}$$

⚠️ **This DERIVES identity from content — in direct tension with `INV-KOS-IDENTITY-001`: *"identity is
assigned, NEVER DERIVED."*** ⭐ A sharper instance of the `V6`/`V8` tension `P-35` carried.

$$\boxed{\begin{array}{c}\textbf{⭐⭐ RECONCILIATION, and it STRENGTHENS } K1\textbf{: } 25I \textbf{ states what identity } \mathbf{CONSISTS\ IN} \textbf{ (constitution);}\\ \mathit{INV\text{-}KOS\text{-}IDENTITY\text{-}001} \textbf{ states it cannot be } \mathbf{COMPUTED} \textbf{ from state.}\\ \boxed{\textbf{And } \mathrm{TruthCond} \textbf{ equivalence is } \mathbf{NOT\ COMPUTABLE}\ (V1) \Rightarrow \textbf{an ADJUDICATION is required precisely BECAUSE the constitutive criterion is undecidable.}}\end{array}}$$

⚠️ **Status: this file is HPA-authored KnowledgeOS material, not external literature — so `[EXTERNAL
THEORY]` would MISLABEL it.** ⭐ **Recorded as `[STIPULATED]` KnowledgeOS-internal, with the folder-name
caveat stated.**

# 21. ⭐⭐ Attack on `P-37` — six verdicts

| # | claim | verdict |
|---|---|---|
| **1** | *"the only witnessed retained object is the mapping"* | ⭐⭐ **`[QUALIFIED]` → *"the only directly witnessed retained object found in the audited sources"*.** ⭐ **And it was the wrong search: `P-08`'s `W1` witnesses the BIT** |
| ⭐⭐ **2** | *"the mapping encodes a `Same` finding"* | ⭐⭐ **`[QUALIFIED]`** — it encodes *that a class exists*, ⛔ not the `¬Same` judgments nor which pairs were adjudicated |
| ⭐⭐⭐ **3** | *"`Same` is the established `K1` payload"* | ⭐⭐⭐ **`[REFUTED]`.** The payload is the **BIT**, both polarities; `Same` is one pole |
| ⭐⭐ **4** | *"`Different` and `PossibleSame` are `[OPEN]`"* | ⭐⭐ **`Different` `[REFUTED]` as open — it is IN, as the negative pole.** `PossibleSame` **stays `[OPEN]`** and is probably out of scope |
| **5** | *"`Unknown` has no retention obligation"* | ⭐ **VALID, and now for a better reason: `Unknown` is not about `K1` at all** — `W1` presupposes an adjudicated pair |
| ⭐⭐⭐ **6** | *"the four-value question may have been about the wrong object"* | ⭐⭐⭐ **CONFIRMED — binary theory-register bit vs four-valued domain protocol** |

# 22. Necessity and sufficiency — both directions

| | test | result |
|---|---|---|
| **Necessity** | without the bit, is `K1` satisfiable? | ⭐⭐ **NO** — `W1`'s failure *(candidates collapsing)* becomes undetectable |
| **Sufficiency** | if the bits survive, is `K1` satisfied? | ⭐⭐ **YES for the witnessed failure** — the bit **is** `W1`'s *"smallest missing information"* |
| ⚠️ **caveat** | sufficiency for **all** `K1` failures | ⭐ **`[OPEN]`** — `K1` has exactly **one** forcing witness |

$$\boxed{\textbf{Minimum sufficient retained information} = \mathbf{the\ adjudicated\ bit\ per\ pair,\ both\ polarities,\ plus\ the\ identity\ labels\ it\ relates.}}$$

# 23–25. `K1` formulations, new-cell test, kernel impact

| | formulation | verdict |
|---|---|---|
| **`K1-A`** | non-regenerable identity **adjudications** | ⭐ **close** — ⚠️ omits the continuity purpose `W1` states |
| **`K1-B`** | historical **continuity** | ⛔ **names the goal, not the payload** |
| ⭐⭐⭐ **`K1-C`** | **non-regenerable identity judgments sufficient to preserve historical identity continuity** | ⭐⭐ **SELECTED — matches `W1` on both halves** |
| **`K1-D`** | historical **mappings** | ⭐ **`[REFUTED]`** — Counterexample A |
| **`K1-E`** | minimum non-regenerable information for continuity | ⚠️ **under-reads** — `W1` names the datum, so declining to name it loses evidence |

**New-cell test:** 1 ✅ · 2 ✅ *(a bit)* · 3 ✅ · 4 ✅ · ⭐ **5 not already represented — 🔴 FAILS: this
IS `K1`** · 6–7 not reached. ⛔ **No mapping cell, no adjudication cell, no continuity cell.**

$$\boxed{|K| = 11. \textbf{ ⭐ Mapping and continuity are NOT new capabilities — the mapping is a DERIVED projection, continuity is the INVARIANT } K1 \textbf{ protects.}}$$

# 26. Status register
**`[EMP]`** ⭐⭐ `W1` *"two different candidates collapse into one identity"* / *"one bit per pair:
`SameId` or not — an adjudication, not a computation"* · `K1` *"for the three adjudicated identities;
non-regenerable"* · *"identity is not state — `Zero` keeps identity through `[RF]`"* · `§25S.20`'s
labels + mapping · *"later evidence establishes `SameEntity(A,B)`"* ·
`\boxed{IdentityMerge\ and\ IdentitySplit\ are\ revision\ events}` · `INV-KOS-IDENTITY-001`.
**`[DERIVED]`** ⭐⭐⭐ **the payload is the bit, both polarities** · **adjudication ⇒ mapping, but not
conversely** · **mapping insufficient (Counterexample A)** · **adjudication sufficient (Counterexample
B)** · the four-value set is a domain elaboration · constitution ≠ computation.
**`[CORROBORATION]`** `W1` and `§25S.20` are the **same event one level apart**.
**`[STIPULATED]`** `§25S.5`'s equivalence-relation premise *("Ideally", "should")* · `25I`'s
constitutive criterion · the four-valued set.
**`[REFUTED]`** ⭐⭐ **`P-37` Claim 3** *(`Same` is the payload)* · **`K1-D`** *(mapping)* · `R`
totality · *mapping ⇒ adjudication*.
**`[QUALIFIED]`** ⭐ **`P-37` Claims 1, 2** · **`P-37`'s `{Same}` narrowing — REVERSED to both
polarities** · **`P-37`'s internal §17-vs-§6–11 contradiction, resolved toward the §17 hedge**.
**`[UNWITNESSED]`** a mapping in `P-08` · auditability of the historical judgment · merge without an
adjudication · split retention.
**`[OPEN]`** ⭐⭐⭐ **the LEVEL BRIDGE — does `K1` generalise from theory-register to domain-entity
identity?** · `PossibleSame` · sufficiency beyond the single witness · `25I`-vs-`INV-KOS-IDENTITY-001`
after reconciliation · natural-key vs assigned · assertion vs record · the 28 abandoned `GT` invariants
· Pair D · the admitted-transition disjunction · `ω`'s partiality · `Accepted ⇒ Warrant` ·
`Governance`-in-`K` · `W3`'s conditional · `Ind_ρ` · `K4b-ii` · register deletion · ⛔ **well-foundedness
/ terminality / acyclicity — DEFERRED.**
**`[UNDECIDABLE]`** semantic / truth-condition equivalence *(carried)*.
**`[ARCH]`** none used. **`[EXTERNAL THEORY]`** ⚠️ **none applicable — `external_research/` proved to be
KnowledgeOS-internal.**
**`[METHODOLOGICAL CRITERION — LANE T]`** ⭐ the two-condition necessity test — ⛔ **not from `P-08`**.

# 27. Corrections to `P-37`
⭐⭐ **The `{Same}` narrowing is REVERSED** — `Different` is the bit's negative pole and is IN. ⭐
**Claim 3 `[REFUTED]`; Claims 1, 2 `[QUALIFIED]`; Claim 6 CONFIRMED.** ⭐ **`P-37`'s internal
contradiction resolved.** ⛔ **`P-37` is not rewritten; `|K|` untouched.**

# 28. Reusable methodological lessons — a ninth

1–8 carried. ⭐ **New:**

$$\boxed{\begin{array}{c}\mathbf{9.} \textbf{ A derived kernel cell's own FORCING WITNESS outranks any later corpus artifact about a similar-sounding concept.}\\ \textbf{⭐ Reconstruct the derivation BEFORE importing new evidence — } P\text{-}36 \textbf{ and } P\text{-}37 \textbf{ spent two audits on } \S25S\\ \textbf{while } W1 \textbf{ named the payload in one line.}\end{array}}$$

### 29. ONE NEXT UNRESOLVED QUESTION

$$\boxed{\textbf{Does } K1 \textbf{ — and therefore the whole 11-cell kernel — govern THEORY-REGISTER artifacts, DOMAIN knowledge, or both?}}$$

⭐⭐ **Every `P-08` witness (`W1`–`W7`) is drawn from the theory-construction register: candidates,
definitions, associations, `Zero`, `ℐ`, `𝒦`, `Σ`.** ⭐⭐⭐ **If the kernel is theory-register-scoped, then
`P-29`–`P-38`'s domain-level evidence has been analogical throughout — and that is the most
kernel-critical question now open.**

```
K1-C — NON-REGENERABLE IDENTITY JUDGMENTS SUFFICIENT TO PRESERVE HISTORICAL IDENTITY CONTINUITY.
|K| = 11. P-38 IDENTIFIED K1's PAYLOAD; IT ADDED, MERGED AND RE-SCOPED NOTHING.

THE COMMISSION'S §5 INSTRUCTION — RECONSTRUCT P-08 FIRST — WAS DECISIVE. P-08's own forcing witness
names the payload in one line, and neither P-36 nor P-37 consulted it:
  W1: "two different CANDIDATES collapse into one identity"
      -> smallest missing datum: "ONE BIT PER PAIR: SameId or not — AN ADJUDICATION, NOT A COMPUTATION"
      -> forces K1

TWO FINDINGS LARGER THAN ADJUDICATION-VS-MAPPING.

(i) A LEVEL MISMATCH. K1's witnesses are THEORY-REGISTER objects — candidates, definitions,
associations, Zero through [RF], glyphs I / K-script / Sigma / D-03. §25S is DOMAIN-ENTITY resolution —
nexus3, production vs development repositories, IdentityKey = (Organization, Environment, Hostname,
SystemID), KAID. P-36 and P-37 imported §25S as evidence about K1's payload without establishing that
the levels correspond. The analogy is not baseless — W1 is ITSELF a merge witness, structurally the
same event one level up — but §25S is a legitimate ANALOGUE and an illegitimate SUBSTITUTE.

(ii) W1 ASKS FOR BOTH POLARITIES — "SameId OR NOT". A mapping records only positives. So the mapping is
INSUFFICIENT, and P-37's narrowing to {Same} is REVERSED: it rested on the domain-level witness rather
than on P-08's own. Different is the bit's NEGATIVE POLE and is IN.

THE IMPLICATION MATRIX IS ASYMMETRIC: retained adjudication implies a recoverable mapping (by
equivalence closure, on §25S.5's [STIPULATED] transitivity), but a retained mapping does NOT recover
the adjudication — only the positive half. Counterexample A (mapping, no adjudication) leaves K1 only
PARTIALLY satisfied; Counterexample B (adjudication, no mapping) satisfies it. K1 != mapping.

CLAIM 6 IS CONFIRMED, AND SHARPER THAN EXPECTED: the four-value debate was about the wrong object — not
adjudication versus mapping, but a BINARY theory-register bit versus a FOUR-VALUED domain protocol.
W1 needs one bit. PossibleSame and Unknown fall outside it entirely, since "one bit per pair"
presupposes a pair was adjudicated.

P-37 CONTRADICTED ITSELF AND THIS AUDIT RESOLVES IT: its §17 marked the adjudication "only implicitly"
retained while its §6-11 asserted the mapping as a YES retention witness — and the stronger cell
carried its verdict. Resolved toward the hedge.

THE NEWLY-PERMITTED external_research/ IS NOT EXTERNAL LITERATURE. It holds HPA supervisory reviews, C4
diagrams, step_285 and step_25i — KnowledgeOS's own research; entity resolution and record linkage
return ZERO. Labelling it [EXTERNAL THEORY] would MISLABEL it. Its step_25i does produce a real
conflict: it DERIVES identity from Prop/Ref/TruthCond equivalence, against INV-KOS-IDENTITY-001's
"assigned, never derived". Reconciled — 25I states what identity CONSISTS IN, the invariant states it
cannot be COMPUTED — and the reconciliation STRENGTHENS K1: an adjudication is required precisely
because the constitutive criterion is undecidable.

MY PRE-REGISTERED K1-E WAS WRONG; the answer is K1-C, because W1 names the datum and K1-E declines to.
Record now 1 correct of 4, all four on the record.

REUSABLE LESSON #9: a derived kernel cell's own FORCING WITNESS outranks any later corpus artifact about
a similar-sounding concept. Reconstruct the derivation BEFORE importing new evidence — P-36 and P-37
spent two audits on §25S while W1 named the payload in one line.

ONE NEXT UNRESOLVED QUESTION: Does K1 — and therefore the whole 11-cell kernel — govern THEORY-REGISTER
  artifacts, DOMAIN knowledge, or both? Every P-08 witness W1-W7 is drawn from the theory-construction
  register. If the kernel is theory-register-scoped, P-29 through P-38's domain-level evidence has been
  analogical throughout.

NO SCHEMA v3 — NO NEW CELL — NO CANONICALIZATION — NO ARCHITECTURE — NO three_model_convergence
INSPECTION — NO CROSS-TRACK COMPARISON — WELL-FOUNDEDNESS STILL DEFERRED.
```

# `P-47` — Persistence Kernel Minimality & Irreducibility Proof Audit

**2026-09-08 · Lane T.** After [`64` `P-46`](./64-P46-THEORY-BOUNDARY-INTENTIONAL-DESIGN-OR-GOVERNANCE-GAP-AUDIT.md).

# 1. Executive verdict

$$\boxed{\begin{array}{c}\mathbf{C\ —\ MINIMALITY\ REMAINS\ UNRESOLVED.} \textbf{ ⛔ But with a material advance the series had left on the table:}\\[6pt] \boxed{\textbf{⭐⭐⭐ The } \mathbf{one\ live\ conditional\ redundancy} \textbf{ is } \mathbf{DISCHARGED.} \textbf{ } K2 \textbf{ moves from } \mathbf{UNDERDETERMINED} \textbf{ to } \mathbf{FORCED.}}\\[4pt] \textbf{⇒ } \mathbf{11\ cells\ —\ 10\ forced} \;\longrightarrow\; \mathbf{11\ cells\ —\ ALL\ 11\ FORCED\ (cell\text{-}wise,\ conditionally).}\end{array}}$$

⭐⭐ **Why it remains `C` nonetheless:** the count is **axis-relative** — `P-09.3`'s own finding:
**11 cell-wise · 5 subject-wise · 7 as `P-09.2` counted**. ⭐⭐⭐ **Set-inclusion minimality (`B`) now
holds; cardinality (`A`) and ontological (`D`) minimality do NOT.**

$$|K| = 11 \textbf{ — unchanged, and still } \mathbf{[REC]}. \textbf{ ⛔ Mathematical advance is not governance adoption.}$$

# 2. ⚠️ Three corrections declared before execution

**(a)** ⭐ **`P-46`'s *"the only load-bearing qualification left"* was too strong** — well-foundedness,
terminality and acyclicity remain **deferred**, and `Accepted ⇒ Warrant`, `PossibleSame`, the
natural-key tension, `K4b-ii`'s corroboration arm, register deletion, Pair D and `ω`'s partiality are
all still open. ⭐ **The minimality hedge is the major unresolved CARDINALITY qualification, not the
only load-bearing one.**

**(b)** ⭐⭐⭐ **§4's test is a BICONDITIONAL, and as written it refutes `K1` by construction.** `P-19`
proved *"every constructible `K1` pair either uses the circular denotation argument or changes `K2`"*.
⇒ **With `⇔`, `K1` fails as a test artifact, not as a finding.** ⭐ **The pair test is therefore used as
SUFFICIENT for independence, not NECESSARY**, with presupposition arguments admitted as a second ground
*(`P-09.3` §13, `P-19` §4)*. ⚠️ **Cost, stated: "proved" here can only mean *"independent under one of
two admissible grounds"*, ⛔ not *"exhibits a minimal pair"*.**

**(c)** ⭐ **§13's option `A` cannot absorb the conditionality** — `K1` rests on presupposition, `K2` on
`R0`. ⭐⭐ **Reported explicitly rather than folded into a clean "proved".**

⛔ **No `three_model_convergence/`. No cell added or removed. No redesign, canonicalization, Schema v3,
architecture, or governance change. `EKS-12` not reopened.**

# 3. The exact claim, reconstructed from primaries

$$\boxed{\begin{array}{ll}P\text{-}08\ \S18 & \mathbf{"5\ is\ a\ LOWER\ BOUND\ under\ the\ present\ evidence,\ not\ a\ proof\ of\ minimality."}\\[3pt] P\text{-}09.3\ \S20 & \boxed{\mathbf{11\ cells\ —\ 10\ forced,\ } K2\ \mathbf{UNDERDETERMINED}}\\[3pt] P\text{-}09.3\ \S20 & \textbf{"⛔ eleven is } \mathbf{not} \textbf{ claimed as minimal … } \mathbf{semantic\ minimality\ is\ NOT\ established}\textbf{"}\\[3pt] P\text{-}09.3\ \S20 & \textbf{"the count is } \mathbf{AXIS\text{-}RELATIVE}\textbf{: 11 cell-wise · 5 subject-wise · 7 as } P\text{-}09.2 \textbf{ counted"}\end{array}}$$

⭐⭐ **So the hedge was never a single claim.** `P-08` declined *cardinality* minimality; `P-09.3`
declined *semantic* minimality **and** named the axis problem. ⛔ **Neither was inherited from `P-46`'s
summary — both were re-read.**

# 4. The four minimalities, kept apart

| | notion | verdict |
|---|---|---|
| **A** cardinality — *no set of <11 independent capabilities suffices* | ⭐⭐ **`[OPEN]` — refuted as *stated*, since 5 and 7 are legitimate counts on other axes** |
| ⭐ **B** set-inclusion — *every cell necessary; removing any one fails* | ⭐⭐⭐ **NOW HOLDS, conditionally — §5** |
| **C** representation — *same obligation, different shape* | ⭐ **`[EMP]`** that this is possible; ⛔ does not reduce capability count |
| **D** ontological — *11 genuinely distinct capabilities, not naming artifacts* | ⭐⭐ **`[OPEN]` — the axis-relativity is exactly this doubt** |

⭐⭐⭐ **`P-08` claims none of A–D. It claims a LOWER BOUND, which is weaker than all four.**

# 5. ⭐⭐⭐ The discharge — and nobody in 39 audits connected these two documents

| step | source | statement |
|---|---|---|
| 1 | `P-09.3` §13 | ⭐ *"`K2` **CONDITIONALLY REDUNDANT** … it fails **only** because `K3` is restricted to non-monotone transitions. **If `O-P08-1` resolves toward regression-permitted, `K3` becomes UNIVERSAL and `{K3a,K3b} ⊨ K2`**"* |
| 2 | `INTAKE-001` | ⭐⭐ **`O-P08-1` RESOLVED: *"MD-018 permits no regression — the status chain is monotone forward-only"*** |
| 3 | `INTAKE-001` §3 | ⭐ *"`K3` scope — **EXCEPTIONAL — non-monotone transitions only**"* ⇒ **`K3` is NOT universal** |
| ⭐⭐⭐ 4 | `INTAKE-001` | $\boxed{K2 \textbf{ is not merely retained under } R0 \textbf{ — it is the } \mathbf{SOLE\ BEARER\ of\ status\ history}\textbf{, and prior statuses need not persist at all.}}$ |

$$\boxed{\begin{array}{c}\textbf{⭐⭐ The antecedent of the redundancy is } \mathbf{FALSE.} \textbf{ ⇒ } K2 \textbf{ is not redundant — it is } \mathbf{strongly\ forced.}\\ \boxed{\textbf{And a targeted search confirms: } \mathbf{no\ audit\ from\ 26\ through\ 64\ ever\ linked\ } INTAKE\text{-}001 \textbf{ to } P\text{-}09.3\textbf{'s } K2 \textbf{ conditional.}}\end{array}}$$

⚠️ **Three conditions on the discharge, none suppressed:** `R0` is **`[DER-S]`** *(derived from an
admitted governance stipulation, ⛔ not `[EMP]`)* · `INTAKE-001`'s **`X3` revocation condition** —
*"if Lane M revises MD-018's text, **no mechanism propagates that to Lane T**"* · and the ruling's scope
is **the direction of MD-018's `status_chain` only**.

# 6. Irreducibility matrix — grounds, not assertions

| cell | ground | conditional? |
|---|---|---|
| ⭐ **`K1`** | ⭐⭐ **PRESUPPOSITION** — *"to record `X` changed from `a` to `b` you must already have `X`'s identity; every other capability's records are **indexed by** identity"* | ⛔ **unconditional** *(`P-09.3` §13)* |
| ⭐⭐ **`K2`** | ⭐⭐⭐ **sole bearer of status history under `R0`** | ⚠️ **conditional on `R0` + `X3`** |
| **`K5`** | *"no amount of internal retention entails external availability"* | ⛔ **unconditional** |
| **`K3a`** | prior **value** — `P-08` §17's insufficiency proof | forced |
| **`K3b`** | ⭐ a warrant is an **adjudication**; `P-08` §6 forbids regenerating decisions | forced |
| **`K4a-i`** | prior **proposition** — `W4`, `INV-9` withdrawn-not-deleted | forced |
| **`K4a-ii`** | the **supersedes** link | forced *(⚠️ `P-18`'s `n=2` argument was refuted in `P-20`; the cell survives on `W4`)* |
| **`K4b-i`** | withdrawn **ground** | forced |
| **`K4b-ii`** | **invalidated-by** link — an adjudication | forced ⚠️ *(corroboration arm `[OPEN]`)* |
| **`K4c-i`** | retired **identity** — `Π3`: retained propositions leave the **resolvability** distinction unmade | forced |
| **`K4c-ii`** | **retired-into** reference | forced |

$$\boxed{\textbf{⭐⭐ All 11 have a ground. } \mathbf{9\ by\ minimal\ pair,\ 1\ by\ presupposition\ } (K1),\ \mathbf{1\ by\ a\ discharged\ conditional\ } (K2).}$$

⭐ **The five prior counterexample failures were checked and none is reused:** `P-19`'s circular `K1`
pair *(withdrawn)* · `P-34`'s evidence-changing pair *(invalid)* · `P-35`'s record-identity pair
*(invalid)* · `P-36`'s prediction *(wrong)* · `P-38`'s R/D contamination *(corrected)*. ⭐⭐ **A failed
counterexample is treated as evidence against the proof, not for the cell.**

# 7. Collapse test — pairwise and multi-cell

| pair | verdict |
|---|---|
| `K1` / `K4c` | ⭐ **neither entails the other** — `K4c` covers merge-distinguishability, `K1` item-level continuity; **the transition sets differ** |
| `K3a` / `K4a-i` | ⭐ *retain the previous content of a thing that **survived*** vs *retain a thing that **did not survive*** ⇒ **not duplicates** *(`P-19` §5)* |
| `K3b` / `K5` | *keep our justification* vs *keep its external target inspectable* ⇒ **different kinds** |
| `K3b` / `K4a-i` | ⭐⭐ **`P-33`: a `Corroborate` transition is INVISIBLE to `K4a-i`** ⇒ not reducible |
| ⭐ **`{K3a,K3b}` ⊨ `K2`** | ⭐⭐⭐ **the only multi-cell encoding ever found — and its antecedent is now FALSE (§5)** |
| antecedent × link, across 4 subjects | ⚠️ **`[OPEN]` — this is the axis question, §8** |

# 8. ⭐⭐⭐ Why `A` and `D` still fail — the axis problem

$$\boxed{\begin{array}{lll}\textbf{cell-wise} & K1,K2,K5 + (2\ \text{roles} \times 4\ \text{subjects}) & \mathbf{11}\\ \textbf{subject-wise} & K1,\ K2,\ K5,\ \textit{retention-antecedent},\ \textit{retention-link} & \mathbf{5}\\ P\text{-}09.2\textbf{'s count} & & \mathbf{7}\end{array}}$$

⭐⭐ **All three are defensible descriptions of the same structure.** ⇒ **"is 11 minimal?" is
ill-posed until an axis is fixed, and no corpus witness fixes one.** ⭐⭐⭐ **That, not cell
independence, is what blocks a minimality proof** — and it is `P-09.3`'s finding, re-confirmed rather
than re-discovered.

# 9. Transition-coverage
⭐ Every cell has a **forcing transition** in `𝒯_KOS` *(`τ1` status · `τ2` re-disposition · `τ3` new
definition · `τ6` source re-marking · retirement/merge)*, and `P-09.3` recorded *"⛔ no unresolved case
hidden"*. ⚠️ **`τ4` glyph-membership remains `[OPEN]` pending `Q3`** ⇒ **coverage is complete over the
CLOSED part of the transition universe only.**

# 10. Stratification
⭐⭐ **R-only throughout.** ⛔ No S or D witness was used to establish R necessity. ⭐ `P-30`'s
acceptance material, `§25S`'s identity work and `T9`–`T14` are all **`[ANALOGY ONLY]`** here and none is
load-bearing.

# 11. DDD
⭐ Each cell preserves a distinct **obligation**, not a distinct class: `K1` identity-continuity ·
`K2` current field/relation values *(`P-35`)* · `K3a` prior values as `(prior→successor)` pairs ·
`K3b` transition warrant · `K4a`–`K4c` antecedent/link across proposition, ground and reference ·
`K5` external checkability. ⛔ **No aggregate manufactured; a different name never established a
different capability.**

# 12. Statistical discipline
⛔ **Nineteen audits' worth of survival is NOT evidence of minimality** — it is evidence of
*not-yet-refuted*. ⭐ Minimality is **structural**, not frequency-based. ⚠️ **And the series' own
correction rate (~30 corrections across 40 artifacts) argues for caution, not confidence.**

# 13. Verdict and what this audit did NOT prove

$$\boxed{\mathbf{C} \textbf{ — the 11-cell lower bound survives and FIRMS; a rigorous minimality proof cannot be constructed.}}$$

⛔ **Not proved:** that fewer than 11 capabilities cannot suffice *(the axis question)* · that the 11
are ontologically distinct · that `K2`'s discharge is unconditional *(it rests on `[DER-S]` `R0` and is
revocable under `X3`)* · that `K1`'s presupposition ground is equivalent to a minimal pair · anything
about **well-foundedness, terminality or acyclicity**, which remain **deferred**.

⭐ **Proved, conditionally:** **set-inclusion minimality** — every one of the 11 has a ground, and the
sole known multi-cell encoding is now closed.

# 14. Status register
**`[EMP]`** `P-08` *"lower bound … not a proof of minimality"* · `P-09.3`'s `11 = 10 forced + K2
underdetermined`, its axis-relativity and its *"semantic minimality is NOT established"* ·
`INTAKE-001`'s no-regression ruling and `K2`-as-sole-bearer box · `X3`'s revocation condition.
**`[DERIVED]`** ⭐⭐ **`K2` discharged from underdetermined to forced** · set-inclusion minimality holds
conditionally · the axis problem blocks `A` and `D`.
**`[DER-S:R0]`** `K2`'s forcedness — from an admitted governance stipulation.
**`[QUALIFIED]`** ⭐ **`P-46`'s *"only load-bearing qualification"*** · `P-18`'s `n=2` argument for
`K4a-ii` *(refuted in `P-20`; cell survives on `W4`)*.
**`[OPEN]`** ⭐⭐ **cardinality and ontological minimality (the axis)** · `τ4`/`Q3` · `K4b-ii`'s
corroboration arm · `Accepted ⇒ Warrant` · `PossibleSame` · natural-key vs assigned · register deletion
· Pair D · `ω`'s partiality · `Ind_ρ`'s slots · ⛔ **well-foundedness / terminality / acyclicity —
DEFERRED.**
**Governance:** ⭐ `|K| = 11` remains **`[REC]`** — ⛔ **mathematical advance changes nothing about
adoption.**

# 15. ⭐ Backlog — one ticket warranted
⭐⭐ **`EKS-13`** — `K2`'s forcedness now depends on another lane's document *(MD-018)* with **no
change-propagation mechanism** *(`X3`)*. ⛔ **That is a real operational exposure, not a research
question**, and it is registered.

### 16. ONE NEXT UNRESOLVED QUESTION

$$\boxed{\textbf{Can an AXIS be fixed from corpus evidence — or is the choice between 11, 7 and 5 cells irreducibly a } \mathbf{modelling\ decision}?}$$

⭐⭐ **All three counts describe the same structure and all three are defensible.** ⭐⭐⭐ **If no corpus
witness fixes the axis, then cardinality minimality is not merely unproved but *unprovable from the
corpus* — `[UNDECIDABLE]` rather than `[OPEN]` — and `P-08`'s hedge would be permanent by construction
rather than by omission.**

```
C — MINIMALITY REMAINS UNRESOLVED, BUT WITH A MATERIAL ADVANCE THE SERIES HAD LEFT ON THE TABLE.
|K| = 11 UNCHANGED AND STILL [REC].

THE ONE LIVE CONDITIONAL REDUNDANCY IS DISCHARGED. P-09.3 §13 had found exactly one: "K2 CONDITIONALLY
REDUNDANT ... it fails ONLY because K3 is restricted to non-monotone transitions. If O-P08-1 resolves
toward regression-permitted, K3 becomes UNIVERSAL and {K3a,K3b} entails K2." But O-P08-1 WAS RESOLVED,
in INTAKE-001: "MD-018 permits no regression — the status chain is monotone forward-only." So K3 stays
non-monotone-only, the antecedent is FALSE, and INTAKE-001 goes further: "K2 is not merely retained
under R0 — it is the SOLE BEARER of status history." A targeted search confirms NO AUDIT FROM 26
THROUGH 64 EVER LINKED THESE TWO DOCUMENTS. K2 therefore moves from UNDERDETERMINED to FORCED, and the
bound firms from "10 forced" to "all 11 forced" cell-wise.

WHY IT IS STILL C: THE COUNT IS AXIS-RELATIVE. P-09.3's own finding — 11 cell-wise, 5 subject-wise, 7 as
P-09.2 counted — means "is 11 minimal?" is ill-posed until an axis is fixed, and no corpus witness fixes
one. So set-inclusion minimality (B) NOW HOLDS conditionally, while cardinality (A) and ontological (D)
minimality do NOT. That, not cell independence, is what blocks the proof.

THREE CORRECTIONS WERE DECLARED BEFORE EXECUTION. P-46's "the only load-bearing qualification left" was
too strong — well-foundedness, terminality and acyclicity remain deferred, among others. §4's test as
written is a BICONDITIONAL, and would refute K1 by construction, since P-19 proved no valid K1 pair
exists; the pair test was therefore used as SUFFICIENT, not NECESSARY, with presupposition admitted as a
second ground — at the stated cost that "proved" can only mean "independent under one of two admissible
grounds". And §13's option A cannot absorb the conditionality of K1 and K2, so it is reported instead of
hidden.

ALL 11 CELLS HAVE A GROUND: nine by minimal pair, one by presupposition (K1), one by a now-discharged
conditional (K2). The five prior counterexample failures were checked and none reused. Three conditions
on the discharge are recorded, not suppressed: R0 is [DER-S], not [EMP]; INTAKE-001's X3 says that if
Lane M revises MD-018 "no mechanism propagates that to Lane T"; and the ruling's scope is the direction
of the status_chain only.

WHAT THIS AUDIT DID NOT PROVE: that fewer than 11 capabilities cannot suffice; that the 11 are
ontologically distinct; that K2's discharge is unconditional; that K1's presupposition ground equals a
minimal pair; or anything about well-foundedness, terminality or acyclicity.

BACKLOG: EKS-13 registered — K2's forcedness now depends on another lane's document with no
change-propagation mechanism. That is an operational exposure, not a research question.

ONE NEXT UNRESOLVED QUESTION: Can an AXIS be fixed from corpus evidence — or is the choice between 11, 7
  and 5 cells irreducibly a MODELLING DECISION? If no witness fixes it, cardinality minimality is not
  merely unproved but UNPROVABLE FROM THE CORPUS — [UNDECIDABLE] rather than [OPEN] — and P-08's hedge
  becomes permanent by construction rather than by omission.

NO CELL ADDED OR REMOVED — NO REDESIGN — NO CANONICALIZATION — NO SCHEMA v3 — NO ARCHITECTURE — NO
GOVERNANCE CHANGE — EKS-12 NOT REOPENED — NO three_model_convergence INSPECTION.
```

# KnowledgeOS — Deferred Architecture Register

> ⭐ **CONSTITUTIONAL (review 2026-08-04): The purpose of this register is to delay architecture until engineering evidence justifies it. It is not itself a source of architecture.**

| | |
|---|---|
| **Kind** | ⭐ **PROJECTION — a governance VIEW** *(consolidates activation criteria already recorded elsewhere; derived, non-authoritative, regenerable; on conflict the cited source wins)* |
| **Authority** | Generated — never authoritative without human review |
| **The pattern this view makes explicit** | **"A deferred concept has an explicit engineering activation criterion."** *(practice made explicit — every row below already operated this way; nothing is minted here)* |
| **Update rule** | rows change only when a criterion FIRES (an engineering event) or an authority re-rules a criterion — never by re-reasoning |

> ⛔ **Standing rule (review 2026-08-04):** no new services, bounded contexts, policies, buses, or layers **unless a documented activation criterion below is actually met.** A firing criterion is an engineering event — the Freeze v2 path. A proposal with no row here and no engineering event goes to `docs/ideas/`.

> ⭐ **Gate families (review 2026-08-04 — refined from "kind of thing" to RISK: every gate protects against a SPECIFIC failure mode, which is what justifies it later):** Value Object → *code complexity* (risk: complexity) · Port → *second implementation* (risk: over-abstraction) · Service → *repeated orchestration* (risk: wrong boundaries) · Bounded Context → *independent business behavior* (risk: wrong language) · Analytics → *sufficient dataset* (risk: invalid conclusions) · Adaptive learning → *large assessment history* (risk: unstable behavior). ⛔ **Cap: ~5–6 canonical families — a governance model with twenty gate types is itself difficult to reason about.** *(Emerged from two absorbed corrections, not from design.)*

> ⭐ **Staging principle — three ORTHOGONAL questions (review 2026-08-04, practice made explicit — `init` answered all three):** **Can we build it?** *(deterministic? → engineering capability)* · **Should we build it now?** *(does its object/use case exist? → prioritization — deterministic ≠ demanded)* · **Should it join the platform?** *(reusable across repositories? → architecture, Track B)*. A row activates only when the first two are yes; the third is a separate, later decision. **`enable` operates on CAPABILITIES, never adapters** *(`enable commit-trigger`, not `enable husky` — the adapter is chosen by environment detection, whose deterministic half already lives in the doctor's checks; the adaptive INSTALL branch stays staged until a non-husky environment exists).*

## The register

| Deferred concept | Current invariant (what protects the need TODAY) | Activation criterion (the engineering event) | Evidence required | Status |
|---|---|---|---|---|
| **DecisionCaptureService** | the shared decision+rationale record format, byte-identical across both CLIs | a SECOND decision-recording surface arrives (dashboard · IDE · GitHub Action) | real duplication across surfaces | **STAGED** |
| **Observation Runtime** | git post-commit hook + manual runners; OBS-1 contract | manual execution becomes a MEASURABLE source of friction/inconsistency *(gate verbatim)* | friction entries in usage/workflow logs | **PARKED** (friction gate) |
| **Event bus / async messaging / event store** | point-to-point JSONL streams | multiple producers AND consumers needing the same observations, making a bus SIMPLER than point-to-point | multi-repo/CI/IDE consumption | **REJECTED FOR NOW** (EDA verdict 2026-08-04) |
| **AssessmentPolicy** (as object) | thresholds as constants in `AssessmentService` (MIN_COMMITS=3 · ≥20% · ≥2%) | a SECOND metric family needing different thresholds | second family actually assessed | **STAGED** |
| **AssessmentEvidence** (as object) | the `basis` field carried on every verdict | MULTIPLE assessment types needing structured evidence | second assessment type exists | **STAGED** |
| **Pattern Register machinery** (candidate lifecycle: evidence → counterexamples → promotion → retirement) | staging table in the OE register (append-only) | staged-candidate volume reaches **~10–15** *(today: 4 — MO-1 · MO-2 · IF-1 · DG-1)* | the count itself | **STAGED** (volume watch) |
| **Evidence Analytics** *(UL name — Python/DuckDB/Postgres = implementation detail. Sub-capabilities: trend analysis · anomaly detection · forecasting · clustering. READS history, never changes what developers are recommended — answers "what happened?")* | deterministic PHP projections (dashboard · funnel · lead times · effectiveness) | **dataset-explicit gate:** recommendation history ✓ + decision history ✓ + timestamps ✓ *(assessments OPTIONAL — trends/forecasts/clusters don't need them)*, at a volume worth analyzing *(dozens of cycles — analytics on n≈1 analyzes noise)* | the required dataset, populated | **GATED** (earlier gate — split + UL-named per reviews 2026-08-04) |
| **Adaptive Recommendation** *(UL name — the learning layer. Sub-capabilities: confidence learning · threshold adaptation · context adaptation · developer-profile adaptation. CHANGES system behaviour — answers "what should happen next?")* | deterministic rules-as-data; *learning consumes assessments, never raw deltas* (frozen) | evidence thresholds *(CANDIDATE values)*: ≥100 completed cycles · ≥50 assessed outcomes · ≥20 accepted-with-improvement · multi-rule span | large ASSESSMENT history *(today: 1 cycle)* | **GATED** (later gate) |
| **Recommendation/Decision/Outcome/Assessment bounded context** | application-level workflow objects + the published Engineering Improvement Cycle process doc | ~6 months of operation giving the concepts lifecycle · invariants · versioning · policies · effectiveness · history · analytics | the watch criteria observed | **WATCHED** |
| **Bucket aging projection** | timestamps already recorded on every stream | operations make AGE meaningful (items old enough that age discriminates) | aging spread in real data | **STAGED** |
| **Flow-efficiency refinement** (distribution over means) | lead-times section (means with n shown) | populations outgrow n≈1 | volume | **STAGED** (refinement, not new) |
| **Platform extraction** (repo separation: collectors/tooling as product) | ADR Repository Separation Strategy (readiness levels; L1 substantially achieved); code-as-if-separated | accidental complexity in-repo · independent ownership+operation · second adopter | the ADR's exit criteria | **GATED** (ADR PROPOSED) |
| **EIS rendering** (the wall KPI) | record fields already make it computable later | computable population stops being n≈1 | accumulated interventions | **GATED** (fills from evidence) |
| **MO-2 promotion** (ownership-before-state heuristic) | staged candidate with the five-question shape quoted | recurrence across MULTIPLE bounded contexts | a second incident, different BC | **STAGED** (n=1) |
| **Engineering Assessment Commission** | backlog entry trimmed to Question/Trigger/Constraints/Collisions (Model: UNKNOWN) | its recorded trigger *(not fired)* | per backlog entry | **PARKED** |
| **Real-Time ObservationTrigger adapter** *(UL-corrected name — the capability is real-time observation triggering, never "filesystem watching"; watchdog/chokidar/VS Code API/JetBrains API are all merely adapters. Review 2026-08-04: polling "acceptable for a spike, not the final solution"; a native adapter requires non-PHP tech — plain PHP has no cross-platform FS-event API)* | the 2s poller — advisory feedback already lands within seconds; the ObservationTrigger Port contract is documented (guide 01), so any adapter drops in | **objective form (review 2026-08-04, upgraded from complaints):** p95 save→feedback latency exceeding an agreed target *(target set once baseline data exists — no threshold invented before data)* · CPU cost of polling measured as material · or an IDE-extension adapter arriving anyway | `watch-usage.jsonl` — latency + per-collector `collector_ms` breakdown, self-collected on every real save *(queue_length/queue_wait_ms staged: the format supports them; logged when concurrent-save pressure actually appears)* | **STAGED** (the runtime is untouched either way — only the adapter changes) |
| **Live decision capture** *(accept/ignore/later from the IDE popup — closing the split loop: today live advisories display, decisions still go through the CLI)* | `recommendation-inbox.php` + `recommendation-decide.php` (one shared record format) | ⭐ **this IS DecisionCaptureService's trigger** — an IDE decision surface is the SECOND decision-recording surface; the two rows activate TOGETHER (service boundary first, then the surface consumes it) | a real IDE integration being built | **STAGED** (converges with the DecisionCaptureService row) |
| **Incremental impact analysis** *(changed class → dependency graph → only impacted recommendations — deeper than the current changed-FILES-only scoping)* | the runtime already analyzes changed files only; measured cost today: ~101ms for one class — no pressure | analysis cost becoming material at real usage scale, visible in `collectors[].runtime_ms` *(the telemetry is the gate's own instrument)* | watch-usage records showing runtime growth | **STAGED** |
| **Telemetry enrichments** *(three sub-items, review 2026-08-04)* — **(a) collector lifecycle counters** (accepted/ignored/deferred per collector): ⭐ check-before-create — **derivable TODAY as a PROJECTION** (decisions join recommendations by rule; rules map to sources) — no schema change needed, a dashboard groupby when wanted · **(b) typed `CollectorTelemetry` VO** replacing the array shape · **(c) trigger-tagged recommendation records** (which trigger produces highest acceptance?) | (a) the effectiveness table + one join · (b) the tested array contract · (c) `trigger` field now in watch-usage records; ⭐ **the acceptance comparison is structurally impossible until a SECOND trigger writes the streams** — file-save is ephemeral BY DESIGN, so only commit-issued recommendations reach decisions | (a) someone actually wanting the per-collector view · (b) ⭐ **REVISED gate (review 2026-08-04): the anonymous array becoming difficult to reason about** — a VO is a stronger TYPE, not architectural expansion; it earns the complexity gate, not the duplication gate *(the second-producer criterion was over-strict — the register's second absorbed row-level correction)* · (c) a second stream-writing trigger existing | per sub-item | **STAGED** (a=projection · b=complexity gate · c=blocked by the ephemeral rule, correctly) |
| **FileSaved / IDE trigger** *(live feedback while coding — another `ObservationTrigger` instrumentation; collectors unchanged)* | **CommitTrigger, ACTIVE since 2026-08-04** (`.husky/post-commit` → canonical script; fires on every commit) | empirical evidence that commit-time feedback is TOO LATE: the same recommendation repeatedly triggered-then-fixed-in-the-next-commit · developers saying "I wish I had seen this while coding" | usage/workflow-log entries showing the pattern | **STAGED** (review 2026-08-04: "an engineering question, not an architectural one") |
| **CI / PR trigger** *(PushTrigger/PRTrigger instrumentation — the external proposal's "Option B"; medium-latency tier per the adopted classes. Surface EXISTS: 6 GitHub Actions workflows, none running collectors)* | CommitTrigger active locally; the weekly scheduled workflow pattern already covers the slow tier | a PR-based / multi-developer workflow actually operating (today: single developer, direct commits) OR evidence the local hook is insufficient (bypassed · inconsistent across machines) | the workflow change itself, or hook-gap observations | **STAGED** (activation is cheap when earned — one workflow file) |
| **Platform Bootstrap — Track A: infrastructure automation** *(init · doctor · upgrade — deterministic, repeatable, NO domain decisions. Correction accepted 2026-08-04: "automation of deterministic work is not speculative architecture" — `laravel new` never needed a second adopter. The prior second-adopter gating of init was OVER-APPLIED and is REVISED here — the first row-level correction this register has absorbed)* | `init` **ACTIVE** (idempotent planner, TDD: fresh state → full plan · this repo → zero actions) · `doctor` **ACTIVE** (8 checks) · husky `prepare` (install) | `upgrade`: versioned migrations actually existing *(no v1→v2 exists yet)* · `config`: configuration beyond rules-as-data actually existing · `enable <collector>`: real toggle variation existing *(today: one fixed set, nothing to toggle)* · **init scope extension** (install rules/config/dashboard templates/optional workflows): a target repo LACKING them — which is the second adopter again *(deterministic ≠ demanded: in this repo they all exist)* · **environment-adaptive install** (npm→husky · no-npm→native hook · GitHub→workflow; capability = CommitTrigger, adapter chosen by detection): a non-npm or non-husky environment actually appearing | per-command determinism, already met for init/doctor; the rest gated on their OBJECTS existing | **ACTIVE** (init + doctor live · extensions staged with per-object criteria) |
| **Platform Bootstrap — Track B: reusable-platform extraction** *(BootstrapPort · shared contracts · Observation Runtime package · standalone distribution · repository split)* | everything runs in-repo today; the port question is answered by "one implementation needs no port" | **THE second-adopter gate** — a real repository adopting KnowledgeOS *(these decisions shape long-term architecture and need cross-repository evidence)* | a real adopting repository, not a synthetic one | **GATED** (converges with the platform-extraction row — three rows, one gate) |

## The register's own deferred fields *(review 2026-08-04 — "not now, later"; the register obeys its own pattern)*

| Future field | Purpose | Activation criterion |
|---|---|---|
| **Activated on** | which engineering event fired the criterion | the FIRST row transition actually occurring |
| **Disposition** | Implemented / Withdrawn / Superseded | same — history begins when there is history |

*Rationale: in two years, "why did we build this?" should be answerable from one row, not from months of logs. The status vocabulary (REJECTED · PARKED · STAGED · WATCHED · GATED) is an emerging spectrum, deliberately NOT formalized — it stays descriptive until transitions give it meaning.*

### ⭐ Transition log — ACTIVATED 2026-08-04 *(its own criterion fired: the first actual row transition occurred)*

| Row | Transition | Activated on (the engineering event) | Disposition |
|---|---|---|---|
| **Platform Bootstrap → Track A (`init`)** | GATED (second adopter) → **ACTIVE** | governance correction accepted (review 2026-08-04): the second-adopter gate was OVER-APPLIED to deterministic infrastructure automation — init's real gate (determinism, steps known from the manual 2026-08-04 sequence) was already met. Built TDD same day; idempotent on this repo | **Implemented** |
| **FileSaved / IDE trigger** | STAGED (criterion: commit-time too late) → **ACTIVE** | **AUTHORITY re-ruling, not a criterion fire** — the Chief Architect commissioned the implementation directly (2026-08-04, "add a new ObservationTrigger adapter"). Built TDD same day: `ChangeSet` (technology-neutral) + `ObservationRuntime.run(ChangeSet)` (reuses collectors/engine unchanged; changed-files-only; trigger-independence pinned by test) + `FileSaveTrigger` + `watch.php` poller + VS Code task installed by environment-aware init. Live results EPHEMERAL (displayed, never stream-written — commit stays the stream writer). UL corrected: capability = **ObservationTrigger**; commit/file-save/PR/CI are adapters. PR/CI adapters remain STAGED — the commission's architecture supports them; their activation evidence is still absent | **Implemented** |
| **IDE-extension adapter (VS Code, minimal)** | STAGED → **ACTIVE** | **AUTHORITY commission** (2026-08-04, ordered steps 1–5: JSON API + minimal VS Code extension, ephemeral, no persistence): `observe.php --json` (the adapter API — any editor calls it) + `vscode-knowledgeos/` (plain JS, `onDidSaveTextDocument`, popup + output channel; decision buttons deliberately absent until DecisionCaptureService). JetBrains/other IDEs stay evidence-gated (step 7) | **Implemented** |
| **Real-Time ObservationTrigger adapter** | STAGED → **ACTIVE (VS Code)** | ⭐ **FIRST CRITERION-FIRED TRANSITION in this log** — the row's own named condition *"or an IDE-extension adapter arriving anyway"* fired: native `onDidSaveTextDocument` events replace polling for VS Code users (zero poll latency; the 2s poller remains the fallback adapter for other contexts). The p95-latency branch of the criterion never needed to fire | **Implemented** (poller retained as fallback) |
| **Session ownership (`knowledgeos dev`)** | unstaged → **ACTIVE** | **INCIDENT-FIRED demand** (2026-08-04): a real development session ran with the watcher never started — live feedback silently didn't exist. `dev.php`: VERIFY (doctor; refuses unready environments) → PROVE (chain self-test, per-stage traces — the first failing stage is the defect) → OBSERVE (watch loop; phase renamed per review). Orchestration only; zero collector/rule logic. The runtime stays the center; the session is an adapter around it. **Zero-memory startup: the folderOpen task now runs the FULL dev lifecycle** — open the workspace, the session verifies, proves, and observes with no human memory involved | **Implemented** |
| **Full dev-stack ownership** *(`knowledgeos dev` also owning PHP server · queue worker · Vite · Laravel logs · graceful shutdown — the `next dev`/`cargo watch` model)* | `dev.php` owns the OBSERVATION session; the app stack is started by existing conventions (artisan serve · npm run dev) | developers actually wanting one-command stack ownership (demand observed, not assumed — this expands KnowledgeOS into general dev tooling, a scope decision) | requests/friction from real sessions | **STAGED** |

## How to use this view

A review asking *"should we build X now?"* checks one row: **has the activation criterion fired?** Yes → the firing event is the evidence; proceed through readiness (EP-01). No → the current invariant continues to protect the need; the proposal waits. **Consistency replaces judgment-per-proposal.**

---

*Derived 2026-08-04 from: CONTEXT.md staging records · `docs/ideas/2026-08-observation-runtime.md` · the OE register annotations · the recommendation-engine and metrics spike plans · ADR Repository Separation Strategy · the EDA-proposal verdict. Regenerate when a criterion fires. This projection decides nothing.*

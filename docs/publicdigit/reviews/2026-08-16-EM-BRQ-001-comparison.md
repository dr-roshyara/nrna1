# `EM-BRQ-001` — COMPARISON: Governance first pass vs blind Verification pass

**Type:** Governance comparison (Session 2, per the signed sequence) · **Date:** 2026-08-16
**Inputs:** first pass @ `e95cce5e` · V-BRQ-1 report (this commit) · frozen corpus `5172d3e2…` — **every divergence adjudicated by reading the frozen text, never by preferring a pass.** ⛔ **Nothing is resolved here; the disposition is the PO's. The first-pass report is NOT rewritten — its errors are adjudicated here and stand there as history.**

## 0 · Headline

> ## **THE BLIND PASS OUTPERFORMED THE FIRST PASS.**
> **7 of 20 classifications diverged. Every one was adjudicable from the frozen text — and in ALL 7 the Verification reading was correct.** **It found the two ⚫ items the first pass missed (both CONFIRMED by text check), overturned one first-pass NEW gap with an adopted worked example, caught one first-pass 🟢 that hid a real gap, and correctly re-mapped three first-pass NEW findings to already-registered questions.**
> **And the convergent core held: both passes independently found the Committee-arithmetic and clock/terminal families FULLY DETERMINATE — the PO's two-readers criterion is satisfied for Model A's operating core (9 scenarios).**

## 1 · ⚫ Both contradictions CONFIRMED against the frozen text

**B-1 — representative-configuration finality has two adopted timings.** VERIFIED: `EM-GOV-017`/`018` (ADOPTED) still say *"before publication"* (lines 128–129), while `EM-GOV-041`/`046` (ADOPTED) say *"at the close of Voting Preparation"*. **The corpus itself already knows:** line 523 records that the clause *"becomes wrong and must be amended to 'before Voting begins'"* — **the amendment was identified but the rule rows were never conformed.** **Class: act-application/record-integrity defect producing a live contradiction on the frozen text. Repair: a conforming act on `017`/`018` — owed, not new policy.** *(No Model A committee-side outcome in this suite is disturbed.)*

**B-2 — `EM-GOV-025` carries status ADOPTED while its own text says "⛔ CANNOT BE ADOPTED."** VERIFIED (line 174 + the row's own clause). **Class: record-integrity defect — one row asserting its own inadoptability. Repair: a status-reconciliation act on the `025` row.** The joint no-usable-model consequence is already registered (`EM-OPEN-094`) and is not new.

## 2 · The adjudicated divergences — all seven against the first pass

| Scenario | G | V | Adjudication (from frozen text) |
|---|---|---|---|
| **S-19** | 🔴 NEW ("033 temporal scope") | 🟢 pass | **V CORRECT.** `EM-GOV-057`'s adopted text contains the worked case verbatim: *"with 2 available, 2 accepting PASSES."* The specific rule governs; **G's N-6 is WITHDRAWN — the corpus answers.** |
| **S-14** | 🟢 | 🔴 NEW | **V CORRECT.** `EM-GOV-014` Part 1 names the **Chief** as recovery initiator; `EM-GOV-026` appoints the Chief **in the very phase that halted** — no Chief exists yet. G's pass assumed an actor into existence. **Finding CONFIRMED by text.** |
| **S-18** | 🟢 | ⚫ frame | **V CORRECT** (§1, B-1). Both passes agree the asked question is determinate (filling permitted/required); G read `046` only and missed `017`/`018`. |
| **S-10 · S-13 · S-16** | 🟢 | 🔴 KNOWN | **V CORRECT.** Cells G glossed are genuinely open and already registered: the `EM-GOV-004` correction-boundary list ("every boundary remains open") at S-10/S-16 · the lifecycle state of a non-voting halted election (`EM-OPEN-021`!) and the recovery path (`042`) at S-13. **G classified on "core mechanics determinate" while cells stood UNRESOLVED — over-generous.** |
| **S-01 · S-04** *(class agreed 🔴; split diverged)* | NEW | KNOWN | **V CORRECT.** G's N-1 → already `EM-OPEN-037`(a)/`030` · G's N-7 → `054` Q3 residue · G's N-2 → `042`(g). **Three G "NEW" findings were mapping misses, exactly what the KNOWN/NEW split exists to catch.** |

## 3 · Convergence — co-discovered independently (the strongest evidence class)

**Three gaps found by BOTH passes, blind to each other:**
1. **Inoperative onset/determiner** *(G N-4 ≡ V N-1)* — no definition of *"unable to function"*, no named determiner, and V adds the sharpening: `EM-GOV-060`'s non-retroactive pause makes the halted clock's remainder **incomputable** without the onset moment.
2. **Committee-side "unavailable" vs "vacancy"** *(G N-3 ≡ V N-2)* — `056` converts unavailability into a fillable vacancy with no boundary and no determiner.
3. **Cast-vote status across mid-gate replacement** *(G N-5 ≡ V N-3)* — stands/lapses/re-cast undefined; one seat could express two positions in a denominator of 3.

**Also convergent: 9 scenarios 🟢 in both passes (S-02/03/05/08/09/11/12/15/17)** — arithmetic, clocks, terminal rule, inaction probe, version-binding negative clause.

## 4 · Adjudicated final classification (comparison verdict, for the PO)

| | Count | Scenarios |
|---|---|---|
| 🟢 | **10** | S-02 03 05 08 09 11 12 15 17 **19** |
| 🔴 KNOWN | **6** | S-01 04 10 13 16 20 |
| 🔴 NEW | **3 scenarios / 4 findings** | S-06 (onset · unavailable-vs-vacancy) · S-07 (cast vote) · S-14 (recovery initiator pre-Chief) |
| ⚫ | **2 record-integrity** | B-1 (`017`/`018` vs `041`/`046`) · B-2 (`025` self-contradictory status) |

## 5 · What this says about the method (registered as evidence, not promoted)

**The two-pass design earned its cost in one run:** a 35% divergence rate, every divergence text-adjudicable, both ⚫ items found only by the blind reader. **The first pass's characteristic failure was familiarity — it assumed actors into existence (S-14), classified on remembered intent rather than frozen text (S-18/S-19), and re-discovered registered questions as new (mapping misses).** The blind reader had no memory to trust and therefore read.

## 6 · On the PO's desk (nothing executed here)

**① Two record-repair acts:** conform `017`/`018` to the owed amendment (B-1) · reconcile `025`'s status (B-2). **② Disposition of the FOUR adjudicated NEW gaps** *(the earlier seven-candidate list is superseded by this comparison: three co-discovered + one text-confirmed; three withdrawn/remapped)*. **③ The Model A qualification verdict** — Governance's input: the operating core is two-reader determinate; the failures are silences and two record defects, not conflicts between substantive rules.

**Traceability.** First pass `e95cce5e` · suite `9b9d4d2a` · freeze `6e7af05c` · V-BRQ-1 report + attestation · text verifications this document · A-3.

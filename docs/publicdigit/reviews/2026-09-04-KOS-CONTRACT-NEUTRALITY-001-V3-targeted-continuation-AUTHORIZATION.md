# `KOS-CONTRACT-NEUTRALITY-001` — V-3 targeted continuation: **AUTHORIZED** · grant `G-KOS-CONTRACT-V3-TARGETED-CONTINUATION`

**Work item:** `KOS-CONTRACT-NEUTRALITY-001` · **Date:** 2026-09-04
**Recorded by:** Governance — `claude-code-session:e8f324f1-25ea-4281-a95a-483401312e3e`
*(recording the PO/ARB's authorization; Governance does not authorize)*

> ⛔ **V-3 remains OPEN.** This grant authorizes narrow continuation on exactly three
> already-named open items. It does not accept either existing V-3 determination, does not
> rule on whether dynamic member access is in scope, and authorizes **no** implementation,
> contract modification, fixture modification, or runtime code change.

---

## 1 · The authorization (verbatim)

In response to the Pass-1 handoff (`2026-09-04-...-PASS-1-PO-ARB-escalation.md`, Decision 1,
Option 2), the PO/ARB's own words:

> *"V-3 remains OPEN. Authorize targeted continuation limited to D-1, D-4 and D-5. No
> implementation, contract modification, fixture modification, or runtime code change is
> authorized by this act."*

## 2 · What was written, and what deliberately was not

**Written:** one grant — `G-KOS-CONTRACT-V3-TARGETED-CONTINUATION` · `AUTHORIZED` ·
`registeredBy: governance`. Performed by the same lane and actor as Pass 1: **same actor,
separate authority** — the pattern already proven by Option C (Pass-1 authorization).

**Deliberately not written:** no new lane · no change to `G-KOS-CONTRACT-V3-ARCH` or
`G-KOS-CONTRACT-V3-ARCH-AMD1` (both remain `AUTHORIZED`, unchanged, proposal-only) · no
change to `G-KOS-CONTRACT-PASS1-RECONCILE` or its `AMD1` · no acceptance of either existing
V-3 determination · no closure of the V-3 assignment.

## 3 · The bounds, as the PO/ARB set them

**Authorizes exactly one thing:** a proposal, for PO/ARB adoption, resolving three items the
decisions-registration already named as open:
- **`D-1` — representation:** how the dynamic-member-access fact is represented once
  recorded (currently `OPEN — a contract decision`).
- **`D-4` — enumerated list:** the bounded enumeration of in-scope dynamic-access
  mechanisms (currently `does not exist — architecture work`).
- **`D-5` — vocabulary:** the naming/vocabulary for representing them (currently
  `not chosen — architecture work`).

**Does not authorize:** implementation · contract modification (`expected.json` or any
pinned decision) · golden fixture modification · application/runtime code change · changing
`G-KOS-CONTRACT-V3-ARCH`/`AMD1` · closing the V-3 assignment · reopening any item the
decisions-registration already closed · deciding whether dynamic member access is in scope
at all (that determination stands as previously registered).

**The containment rule, carried onto this grant:** if resolving `D-1`/`D-4`/`D-5` would
require touching any do-not-modify item (contract, fixture, code), **that is a finding to
report, not work to perform.** Continuation beyond this proposal requires separate
authorization.

## 4 · Deliverable required

A **proposal** — explicitly not an acceptance, not an implementation — addressing `D-1`,
`D-4` and `D-5` only, grounded in the existing V-3 determination and decisions-registration
records, for PO/ARB adoption.

**Traceability:** Pass-1 handoff `2026-09-04-...-PASS-1-PO-ARB-escalation.md` (Decision 1) ·
Pass-1 evidence determination `2026-09-04-...-PASS-1-evidence-determination.md` · V-3
decisions-registration `2026-08-19-...-v3-decisions-registration.md` · V-3 independent
determination `2026-08-19-...-V3-architecture-determination.md` · `G-KOS-CONTRACT-V3-ARCH`
(+`AMD1`) · `G-KOS-CONTRACT-PASS1-RECONCILE` (+`AMD1`) · lane
`S5-architecture-pass1-evidence-reconciliation`.

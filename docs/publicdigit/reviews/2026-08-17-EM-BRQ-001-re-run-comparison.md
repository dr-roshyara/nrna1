# `EM-BRQ-001` — RE-RUN COMPARISON (S-05–S-09): Governance re-pass vs `V-BRQ-2`

**Type:** Governance comparison · **Date:** 2026-08-17 · **Inputs:** re-pass @ `199e8043` · V-BRQ-2 report (committed here) · corpus `441550ed…` @ `b065ba9d`.
**Control noted from V's attestation: the frozen suite header cites the ORIGINAL corpus hash; both readers derived against the UPDATED corpus as commissioned — the suite carries scenario text only, no rules, so the control holds.**

## 0 · Result

> ## **CONVERGENT ON 4 OF 5 — and the three adopted cluster rules are DISCHARGED BY BOTH READERS INDEPENDENTLY. ⚫ 0 in both passes. The one divergence (S-06) adjudicates AGAINST the Governance re-pass, again.**

**Convergent 🟢 (both readers, independently):** **S-05** (no vacancy, no filling obligation, gate passes) · **S-07** (cast vote stands; one seat, one position) · **S-08** (vacancy chain: factual onset → restoration clock → cancellation) · **S-09** (remainder computable from the recorded onset; pause-not-reset). **The cluster's three questions — vacancy boundary, Inoperative onset, cast-vote survival — are now two-reader determinate. `064`/`065`/`066` did the work they were adopted to do.**

## 1 · The S-06 divergence, adjudicated from text

**G said 🟢** (no vacancy events → gate unsatisfiable → *halted* under `052` → halted clock). **V said 🔴 KNOWN:** definitively **not Inoperative** (`065` counts vacant seats: zero) — **but whether an open, presently-unsatisfiable gate is a "halt" at all is not determined by adopted text.**

**Adjudication: V IS RIGHT, and the divergence itself is the proof.** `EM-GOV-052`'s trigger is *"where an acceptance gate cannot be satisfied"* — and the corpus's own neighbouring usage (`EM-GOV-051`, adopted) reserves the halt trigger for a gate that ***"becomes mathematically impossible"***. **Two members temporarily absent leave the gate unsatisfiable NOW but not mathematically impossible — they may return, no seat is vacant.** One reader took *cannot* as instantaneous, the other as enduring; **two readers applying one adopted sentence reached different clock consequences, which is the definition of not-determinate under this exercise's own criterion.** **Comparison verdict: S-06 (no-vacancy branch) = 🔴 KNOWN — `EM-OPEN-095`① (what IS a gate during its interval: in-progress or halted?) with `053` (no time bound) and `102` adjacent. No new row needed.** *(The vacancy branch of S-06 stays convergent-determinate.)*

**G's error class, third occurrence, named:** familiarity — the re-pass read `052` through the intent remembered from drafting the recovery rules, not through the words. *(Registered as method evidence; still not promoted.)*

## 2 · V's record-hygiene finding — verified, Repair C PREPARED (one line)

**Verified:** adopted `EM-GOV-057`'s in-row annotation still says the 2-unavailable case is *"recoverable through `EM-GOV-056`"* — written before `064`, it now misleads: **temporary unavailability is not a fillable vacancy any more.** **Repair C (conforming annotation repair, no rule text touched):** conform the annotation to *"recoverable through `EM-GOV-056` where seats are VACANT (`EM-GOV-064`); temporary unavailability is not fillable."*
> **Adoption line:** *"I adopt record repair C as prepared — a conforming annotation repair only."*

## 3 · Where the qualification now stands — the verdict input for the PO

| Item | State |
|---|---|
| Cluster NEW gaps ①–③ | ✅ **DISCHARGED — two-reader determinate under the adopted rules** |
| ⚫ anywhere | **0** (both re-run passes; the two originals repaired and discharged) |
| NEW ④ — pre-Chief recovery initiator | 🔴 open, separate commission (unchanged) |
| KNOWN opens touched by the tested core | `053` · `095`① *(now carrying S-06)* · `110` (naming; implementation-blocking only) · `077`/`066`/`094` walls · the S-01/04/10/13/16/20 mappings |

> **Under the PO's own gate rule — *"Architecture cannot start until the adopted rules have survived the scenario set, OR every remaining failure has been explicitly accepted as an intentional unresolved dependency"* — the final Model A verdict now requires exactly two things: ① disposition of NEW ④, and ② an explicit PO acceptance (or resolution) of the KNOWN unresolved dependencies listed above.** **Nothing else stands between the corpus and the verdict.**

**Traceability.** Re-pass `199e8043` · V-BRQ-2 report + attestation · `EM-GOV-051`/`052`/`057`/`064`/`065`/`066` texts · `EM-OPEN-053`/`095`/`102` · A-3.

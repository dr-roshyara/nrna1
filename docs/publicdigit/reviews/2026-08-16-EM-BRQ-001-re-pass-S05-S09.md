# `EM-BRQ-001` — GOVERNANCE RE-PASS: S-05–S-09 against the updated corpus

**Type:** Qualification re-pass (Governance lane) · **Date:** 2026-08-16
**Inputs:** updated corpus sha256 `441550ed…` @ `b065ba9d` (post `064`–`066` + `056` amendment) · same frozen suite @ `9b9d4d2a` · same scheme. **Only the five affected scenarios; all other results stand.**

### S-05 · 1 temporarily unavailable, 2 accept — 🟢 DETERMINATE
Under `064`: temporary unavailability is **not a vacancy** → ⚠️ **no filling obligation arises** (the pre-amendment `056` had mandated filling — this is the intended behaviour change) → the member expresses no Committee Vote for the decision (`064`); denominator 3, required 2, accepts 2 → **passes** (`057`). Recorded per `005`. **Every cell now cites adopted text; the old observation is discharged.**

### S-06 · 2 temporarily unavailable, 1 remains — 🟢 DETERMINATE (two branches, both derivable)
**Branch as posed (no vacancy events):** all 3 seats non-vacant → **NOT Inoperative** (`065`: unable-to-function counts *vacant* seats, and there are none) → max accepts 1 < 2 → gate unsatisfiable → **halted at the gate** (`052`) → **halted clock alone runs** (`062`) → ends by: members returning (gate decidable again) · a vacancy event converting the branch · or expiry → terminal (`063`). **Branch if the absences become recorded vacancy events:** at the second event non-vacant 1 < 2 → **Inoperative at that recorded moment** (`065`) → halted clock pauses with a computable remainder (`060`) → restoration clock runs (`062`) → filled: resume at the gate (`059`(c)) · not filled: cancelled (`058`). **The clock-selection question that was N-①/N-④ is discharged: which clock runs is now derivable at every moment from recorded events.** *(Residue unchanged and KNOWN: an occupied-but-undecided gate has no time bound — `EM-OPEN-053`.)*

### S-07 · accept cast → resignation → seat filled mid-gate — 🟢 DETERMINATE
Resignation **is** a vacancy event, reason recorded (`064`) → org authority fills (`056` as conformed) → **the cast acceptance STANDS; the seat's position for this decision is spent; the replacement may not re-cast** (`066`) → denominator 3 unchanged (`057`) → the gate concludes on the standing vote plus the other members' votes. One seat, one position (`066`). **N-③ discharged.**

### S-08 · Committee cannot decide; no restoration in period — 🟢 DETERMINATE
The stipulated incapacity maps to `065`'s arithmetic condition (vacancy events leaving non-vacant < 2) → **Inoperative at the recorded causing event — the onset is now a fact, not a finding** → restoration clock runs from it (`060`/`062`, parameter `059`(a)) → expiry unrestored → **cancelled** (`058`). ⚠️ **Boundary now explicit, recorded as KNOWN rather than new: mere INDECISION with all seats occupied is NOT inability to function — that is the unbounded-gate case, `EM-OPEN-053`.** *(Naming residue `110` unchanged.)*

### S-09 · restored in time; earlier halted period outstanding — 🟢 DETERMINATE
Resume at the unresolved gate (`059`(c)); the halted clock resumes with **exactly the remainder computed from the recorded Inoperative onset** (`065` + `060`), no new time (`061`), each clock only under its condition (`062`). **V-BRQ-1's incomputable-remainder finding is discharged by derivation, not by assertion.**

## Result
**5/5 🟢 on the Governance re-pass.** The three adopted rules discharge N-①/②/③ in derivation; the S-05/S-06 boundary (absence vs vacancy) and the S-08 boundary (incapacity vs indecision) both land on adopted text; residues stay KNOWN (`053`, `110`, external authority). **Not final — the blind re-verification runs next on the same frozen inputs; divergence is a finding.**

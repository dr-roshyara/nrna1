# Registration — `EM-IMPL-001` PROVENANCE FORK: a built increment exists; the record holds no authorizing acts

**Type:** Governance registration (Session 2) · **Date:** 2026-08-17
**⛔ This registration commits DOCUMENTATION ONLY. The 63 untracked files are neither committed, deleted, modified nor executed by Governance.**

## 1 · The facts, exactly

* **In the working tree, untracked:** `app/Contexts/Election/Domain/OperatingCore/` — **56 pure-PHP files** (the three aggregates, VOs incl. `TerminalStatePlaceholder` rendering *Election Discontinued*, policies P-1…P-7, condition types, events marked non-canonical, ports incl. `OrganisationalAppointmentAuthority` **with no adapter and no operation signatures**, repository interfaces, exceptions) · `tests/Unit/Contexts/Election/OperatingCore/` — **7 files / 41 tests**, extended from the earlier quarantined 7 under the same direct channel.
* **Nothing is committed; no tracked file is modified.** The lane reports RED-before-production captured, GREEN 41/2412, regression neutral (its testimony — **unverified here**).
* **How it happened:** the killed lane was **resumed by the user directly** and worked ~8 hours under the user's own instructions (its report references corrections the user gave and an authorization the user stated to it). **That channel is outside this record.** The governance record holds **NO signature, NO grant, NO START** for `EM-IMPL-001`.

## 2 · What Governance can and cannot say

**Can say:** the artifact set matches the readiness report's boundary as read (events + non-protocol ports IN); the D-1 port carries **no operation signatures** — a stricter faithfulness than the design demanded, on the stated ground that signatures would fix a form `EM-OPEN-049` leaves open. **Cannot say:** that any authorizing act occurred — an agent's testimony of an authorization is not a registrable act (A-3; twice already refused in this cycle).

## 3 · The one ruling this needs (PO, one line) — two dispositions, the precedent supports either

> **(A) RATIFY** — the seq-3 precedent: the PO declares the acts they performed in the direct channel (signature — including the events/ports boundary IN — grant, START), Governance registers them **with the provenance defect standing as evidence, never repaired away**; then the RED→GREEN two-commit structuring proceeds under the ratified acts, followed by C-5 independent verification.
> **(B) QUARANTINE** — no acts were performed: all 63 files join the standing quarantine (untracked · unmodified · undeleted · never evidence) for later disposition.

## 4 · Registered regardless of disposition — the lane's substantive finding

> **`OPEN ∧ INOPERATIVE` CAN arise, contrary to the design §2b commentary:** 3 seats, required 2 — seat A accepts (the vote **stands**, `EM-GOV-066`), then A and B vacate: the standing accept + C's possible accept keep the threshold **achievable on recorded facts** (`068` → OPEN) while non-vacant 1 < 2 (`065` → Inoperative). **The tension is in the design's derived COMMENTARY, not in the adopted rules — both classifications derive cleanly and `059`(b) already requires combined conditions to be representable.** **First input for the verification lane; resolved by nobody yet.**

## 5 · Standing state until the ruling

`EM-IMPL-001` **unsigned in the record** · grant/START **none in the record** · 63 files **untracked and untouched by Governance** · quarantine registration `54a547ea` **overtaken in part by the user's own direct action** (the 7 test files were extended under the user's hand — fact recorded, no rule violated *by Governance's registration*, disposition merged into §3).

**Traceability.** Task-notification testimony (not an act) · tree inspection this registration · `3fecc319` + `54a547ea` · seq-3 ratification precedent · A-3.

# EPIC-001 Retrospective — Record of Rulings (2026-07-11)

**Kind:** retrospective record — **recommendations + ARB decisions** (NOT design). Authority chain: *implementation → evidence → analysis/recommendation (this document) → ARB decision → standard*. This document RECOMMENDS; only the ARB column carries decisions. **Inputs:** `EPIC-001_Retrospective_Input.md` (12 observations · 7 promotion candidates · 3 deletion candidates) · `../architecture/design/EPIC-002_Context_Dependency_Map_Draft.md`.
**Authority:** analysis conducted under the ARB's instruction (2026-07-11, Step 1); recommendation language corrected per ARB standing instruction of the same day (*recommend governance decisions, never declare them*). ARB decisions recorded in the rightmost column as they are issued. Promotion ladder enforced throughout: *observation → repeated observation → practice → candidate standard → approved standard* — insufficient evidence ⇒ explicit deferral, never approval-by-momentum.

---

## 1. Promotion candidates — recommendations + ARB decisions

| # | Candidate | RECOMMENDATION | Evidence basis (ladder position) | ARB decision |
|---|-----------|--------|----------------------------------|--------------|
| P-1 | *"Quality gates are permitted to reveal latent defects. They are not responsible for introducing them."* | **Recommended for Promotion** — approved engineering principle; rule text to live ONCE in `Implementation_Process_v1.1_Draft.md` (scheduled follow-up). | 4 independent instances: 7A (Deptrac exposed config-encoding issue) · 7C (Infection exposed latent PHPUnit-9 coverage schema) · F-7D-1 (gate exposed Composer timeout) · F-7D-2 (validation exposed untrustworthy measurement model). Repeated observation, already load-bearing in ARB rulings. | **CONFIRMED — PROMOTED** (ARB review 2026-07-11: "deserves to become a permanent engineering principle") |
| P-2 | Evidence-Preconditions checklist for long-running experiments | **Recommended: Defer** (explicitly — neither approve nor reject). Re-evaluate at the next retrospective after a second independent use. | Single observation (F-7D-2 execution contract). One successful experiment ≠ candidacy. | **CONFIRMED — DEFERRED** (ARB review 2026-07-11: "I agree with the deferrals") |
| P-3 | *"The platform owns orchestration, not the third-party tool."* | **Recommended for Promotion** — approved engineering principle (same landing path as P-1). | 3 independent committed instances: two-step Infection invocation (IDD §2c-iii) · stable-interface R3 owning invocation semantics (7D) · CI workflows containing zero gate logic (7E). | **CONFIRMED — PROMOTED** (ARB review 2026-07-11: "another durable engineering principle") |
| P-4 | EP-01-Light form (Objective · Classification · Risk · Files · Expected evidence · Not changing) | **Recommended: Adopt as working practice** (one rung: repeated observation → practice). Standard-hood re-evaluated at the next retrospective. | Used twice, both ARB-approved without rework (F-7D-2 experiment resume · threads repair). Already operative via `.claude/MEMORY.md` standing rule. | **APPROVED — WORKING PRACTICE** (explicit ARB decision 2026-07-11). **Scope (ARB condition):** permitted ONLY below IDD weight — documentation, engineering process, tooling, quality gates, governance records, low-risk engineering; NEVER for domain models, bounded-context boundaries, business rules, public contracts, architectural decisions, or constitutional behaviour. Engineering-Standard status reconsidered after additional independent cycles + a future retrospective. Verbatim decision text in the session log 2026-07-11. |
| P-5 | PASS-AFTER-CORRECTION qualification lifecycle (F-…/CR-…/OQ-… separation) | **Recommended: Defer for the product track** — adopt on first product-track qualification that encounters an in-run finding (trigger-based), not before. | Approved on the engineering-platform track (OQ records); product-track uses: zero. | **CONFIRMED — DEFERRED** (ARB review 2026-07-11: "I agree with the deferrals") |
| P-6 | Documentation-consolidation rule: **IDD = implementation decisions · ADR = architectural decisions · retrospective = lessons · CONTEXT = current state only** | **Recommended: Adopt as the documentation rule.** The consolidation *sweep* is scheduled as Stream B documentation work (dependency map §6) — an EPIC-000 work item, not executed inside this session. | ARB-directed at PB-007 closure; evidence O-10 (rulings duplicated across four record types). | **APPROVED — the documentation ownership rule is adopted as an ENGINEERING STANDARD** (explicit ARB decision 2026-07-11: IDD = implementation decisions · ADR = architectural decisions · retrospectives = lessons + governance recommendations · CONTEXT = current operational state only). **ENG-005 NOT activated** — the consolidation sweep stays a Proposed backlog item, priority Low, decoupled from the governance decision; activate only on measurable triggers (duplication causing mistakes · onboarding difficulty · contradictory records · slow navigation · noticeable maintenance cost). |
| P-7 | Measurement-reporting convention: a quality/mutation number is always reported **with its execution model** | **Recommended: Adopt as practice, scoped to mutation/quality metrics** (records existing reality: the format is already in use in IDD §2c/§2e, ENG-004, PROGRAM_STATUS). Generalization to all engineering measurements: recommended deferral (single originating incident). | One originating finding (F-7D-2), format repeated across 4 documents. | **APPROVED — ADOPTED as practice, scoped to mutation/quality metrics** (explicit ARB decision 2026-07-11); generalization deferred. |

## 2. Deletion candidates — recommendations + ARB decisions (deletion goal: ≥1 retirement = success)

| # | Candidate | RECOMMENDATION | Basis | ARB decision |
|---|-----------|--------|-------|--------------|
| D-1 | Per-ticket estimated-WBS mechanics (`(est.)` counts, 103-item style) | **Recommended for Retirement** — retire for future epics. Tracking method going forward = what EPIC-001 actually used: IDD slices + session-log gate records + closure rulings. Process-text change recorded as a PI item for `Implementation_Process_v1.1_Draft.md`. | O-9: four consecutive tickets tracked by slices; estimates never matched reality; the sync had to supersede them anyway. | **CONFIRMED — RETIRED** (ARB review 2026-07-11: "absolutely the right decision") |
| D-2 | Unused hooks/scripts | **Recommended: Resolve by reference — no separate retirement here.** Inventory finding: the one known-broken asset (AST-008 plan-renamer: documented broken `jq` shim, fires only on PlanCreate, contradicts the descriptive-filename convention) already has an owner — platform-track slice **C2 (unwire + deprecate)**, re-sequenced post-qualification (R-25). Double ownership is refused. All other hooks show positive usage evidence during EPIC-001 (dev-guide reminder and discipline tripwire fired and were heeded). | AIP plan C2 record · session-log usage evidence. | **OVERRIDE — RETIRE AST-008 NOW** (explicit ARB decision 2026-07-11): unwire executed by this retrospective's actions, superseding C2 sequencing for the unwire step (machine-local file removal stays with the R-36 release cycle). All other hooks retained on positive usage evidence. |
| D-3 | Gate-work mirrored as parallel debt rows (the F-1/F-2 pattern) | **Recommended for Retirement** — capability work is tracked as tickets only; the debt table is reserved for genuine defects/debt and never mirrors planned ticket scope. PI item for v1.1 draft. | O-8: F-1/F-2 duplicated PB-007's scope on two boards and went stale independently. | **CONFIRMED — RETIRED** (ARB review 2026-07-11: "I also like the deletions") |

**Deletion goal: SATISFIED on confirmed decisions** (D-1, D-3 retired — both ARB-confirmed).

## 3. Standing questions — answered from evidence

- **Used (and earned their keep):** IDD-per-ticket with ARB freeze · RED-first TDD · fitness suites · the review loop (propose → refine → freeze) · session logs as reconstruction source · evidence labels (Observed/Derived/Interpreted) · EP-01-Light (late arrival, immediately useful).
- **Unused / broken:** per-ticket WBS trackers after PB-003 (→ D-1) · AST-008 (→ C2).
- **Hurt:** the shared test database (cross-session race O-6 AND the F-7D-2 false-kill mechanism — one root cause, two symptoms) · boards drifting 5 closures behind implementation (O-8) · ruling duplication across record types (O-10 → P-6).
- **Simplified development:** the stable gate interface (one command) · owner-hosts-the-guard · opaque refs (ADR-T16) — which made migration sequencing a value decision instead of a topological constraint (dependency map, load-bearing observation).
- **Surprised us:** F-7D-2 — the attractive number was the artifact (Test Strength 96→65) · `--threads=max` silently degrading to 1 · the first-ever full-suite run exposing legacy debt invisible to per-slice gates (O-11).

## 4. Scheduling outcomes (recorded for Step 2 board reflection — no new design)

1. **Stream B documentation consolidation** (P-6 sweep: stale c4 · superseded tables · ruling de-duplication) — new EPIC-000 item, scheduled after formal closure.
2. **F-7C-1..3** (dead test class + 2 non-parsing scaffold files) — small EPIC-000 cleanup batch; **F-7C-4/5** (legacy suite debt) — EPIC-006 territory; **F-7C-6** (57 risky) — remains recorded, framework-level.
3. **ENG-004** (Mutation Ratchet 1, TEST_TOKEN sub-item) — stays backlog; earliest sensible slot is after EPIC-002 Strategic Discovery.
4. **First real CI run** — at next push (PB-007 §5 open item). **Deployment doc** — precondition for any production milestone.
5. **Architecture Gap Analysis** as repeatable per-BC activity (map §6) — noted as a process candidate; enters v1.1 draft discussion with the PI items above.

## 5. Formal closure statement (recommended — explicit ARB confirmation PENDING)

> **Recommendation: EPIC-001 (Greenfield Core) is ready to be formally closed.** ARB decision: **PENDING** — an earlier closure declaration based on a "continue" message was retracted (*continue ≠ governance decision*); closure takes effect only on an explicit ARB closure statement. Implementation: 7/7 tickets closed with executed evidence (correction loop end-to-end over the real path · blocking merge gate PASS and wired into CI · validated mutation baseline). Qualifications: Architecture · DDD · Trustworthiness · EP-02 — all passed on executed evidence (PB-007 IDD §6). Retrospective: conducted; recommendations issued on all candidates (2 recommended for promotion — both ARB-CONFIRMED · 3 recommended for adoption — PENDING · 2 recommended deferrals — both ARB-CONFIRMED · 2 recommended retirements — both ARB-CONFIRMED · 1 resolve-by-reference — PENDING); deletion goal satisfied on confirmed decisions. **Reopen conditions:** defects · compatibility fixes · retrospective promotions — nothing else. Board propagation of this closure is Step 2.

---
*Next step (after review of this record): **Step 2 — Formal EPIC-001 Closure** (board propagation only). EPIC-002 does not begin in either step.*

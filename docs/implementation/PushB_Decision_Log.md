# Push B Decision Log

**Status:** living · opened with Push B Architecture Blueprint v1.0 (2026-07-06) · **permanent instrument** (ARB direction — kept beyond Push B)
**Rule:** every implementation decision made during Push B records {Decision · Reason · Related ADR · Alternative rejected · Impact}. No undocumented design drift. Reviewed at the Architecture Review Gate and per-PR via `Architecture_Review_Checklist.md`.
**Impact column codes:** contexts touched · ADRs touched · migration required? · breaking?

| # | Date | Decision | Reason | Related ADR / doc | Alternative rejected | Impact |
|---|------|----------|--------|-------------------|----------------------|--------|
| D-01 | 2026-07-06 | Push B covers the FULL loop through `ChallengeResolved` | Chief Architect directive; Readiness Review scope; ADR-T20's "Push C" label superseded | ADR-T20 · Round50-07 · Release 1.1 Readiness Review | Splitting `Adjudicated→Resolved` into a separate Push C | Contestation+Adjudication+Election · no ADR change · no migration · not breaking |
| D-02 | 2026-07-06 | Dismissed outcome: Contestation self-resolves (T4 then T5'); Election stays silent | Round50-03: "Upheld drives ElectionCorrectionApplied; Dismissed → no correction"; required consequences on Dismissed = none | Round50-03 · ADR-T20 | Election emitting `ElectionCorrectionApplied(correctionType=Accept)` on Dismissed — contradicts Round50-03; `Accept` reserved for Upheld-but-accept rulings | Contestation+Election · none · none · not breaking |
| D-03 | 2026-07-06 | Inbox dedupe key = `(event_id, consumer_context)` | Same event legitimately consumed by BOTH Election and Contestation; per-consumer idempotency required | ADR-T4 · Round50-08 (open question RESOLVED) | Global `event_id` PK (blocks the second legitimate consumer) | all consumers · ADR-T4 realized · new `inbox_events` migration · not breaking |
| D-04 | 2026-07-06 | Relay hydration via event-type registry populated by context service providers | Closes G-1; hardcoded `match` cannot scale; unknown types must dead-letter loudly | ADR-T3 · ADR-T5 | Extending the hardcoded `match` (every new event edits shared infra) · reflection-based hydration (implicit contracts) | Shared infra + all contexts · none · none · not breaking (FeePaid migrates unchanged) |
| D-05 | 2026-07-06 | Retry POLICY frozen (bounded retries → dead-letter); retry NUMBERS configurable (default 5) | Operations tunes counts without an ADR; policy is architectural, number is operational | ADR-T3 · Blueprint §13 | Hardcoded attempt count | Shared infra · none · config key added · not breaking |
| D-06 | 2026-07-06 | TenantId/OrganisationId + ElectionId propagated on every event, outbox, inbox, and dead-letter row | Incident investigation filters by tenant first; election-scoped queries without aggregate joins | Blueprint §14 · Chief Architect review | Envelope-only identifiers (AggregateId alone) | all contexts · none · inbox columns · not breaking |
| D-07 | 2026-07-06 | ARB conditions incorporated into v1.0 BEFORE freeze: CI/BI separation + CI-2 split into BI-1/BI-2; Decision Authority (§10.1); Recovery Policy (§7.1); Security (§18); Performance (§19); PR-traceability governance rule; this Impact column; Traceability maturity scale; Architecture_Review_Checklist.md | ARB verdict "Approved with minor recommendations" conditioned approval on incorporation; freeze follows incorporation | Blueprint §20 gate record | Freezing first and patching via v1.1 (would ship an unconditioned v1.0) | documentation set · none · none · not breaking |

| D-08 | 2026-07-06 | `EventHydrator::hydrate(array $payload)` receives the decoded payload only (not the `OutboxEvent` Eloquent row); schema-version dispatch/upcasting is the hydrator's internal concern | Keeps hydrators free of shared-infrastructure model coupling; ADR-T5 versioning localized where the event is owned; Blueprint §6 specified the registry mapping but not the hydrator signature | ADR-T3 · ADR-T5 · Blueprint §6 | Passing the `OutboxEvent` row (couples every context to shared Eloquent model) · registry keyed on (type, version) pairs (premature — no v2 exists; hydrator-internal dispatch suffices per ADR-T5) | Shared infra + Adjudication · none · none · not breaking |

| D-09 | 2026-07-06 | `FeePaidHydrator` migrated verbatim to Membership (wire contract unchanged); relay's `hydrateFeePaid` + hardcoded `match` DELETED — no parallel mechanisms; `UnregisteredEventType` caught before generic failure handling → immediate `markFailed`, no reschedule | Blueprint §6 (registry is the ONLY hydration path) + §7 F2 (missing registration is not transient — retrying cannot fix it); Chief Architect: "remove the branch rather than leaving two mechanisms in parallel" | ADR-T3 · Blueprint §6/§7 · CI-boundary (Shared infra no longer imports Membership domain) | Keeping the match as fallback (silent drift, two hydration authorities) · routing UnregisteredEventType through the 5-retry path (5 pointless retries + 25min delay before dead-letter) | Shared infra + Membership · none · none · not breaking |

| D-10 | 2026-07-07 | Retry timing owned SOLELY by redrive (scheduler), not delivery cadence: `consume()` never re-invokes a parked row; a dedicated `InboxExecutionEngine` (not `Inbox::reattempt`) holds the shared run+classify logic that both consume and redrive delegate to; time is injected via existing `App\Domain\Shared\Clock\ClockInterface` (reused, not invented) | Deterministic recovery (Rule 4); prevents redelivery-storm re-invokes before parked_until; avoids Inbox becoming a God Service (architect R1); execution receives time, never discovers it (architect R2) | ADR-T1/T4 · Blueprint §7/§7.1 · ER-03/ER-04 | (a) `Inbox::reattempt()` — grows Inbox into a God Service · (b) new greenfield Clock port — duplicates a shared abstraction (ER-03) · (c) sliding deadline — never dead-letters | Shared infra + all future consumers/replay · none · none · not breaking |

| D-11 | 2026-07-07 | PB-003 completion splits into **C6A Messaging Architecture Verification** (objective, executable fitness tests) + **C6B Platform Capability Certification** (architectural judgment). A new **Platform Capability Certification** gate sits between PB-003 Implementation Review and PB-004: PB-003 is "Verified" only when verification passes AND certification answers all 7 reuse questions affirmatively. Architecture tests verify PROPERTIES (cardinality/uniqueness), never concrete class names. Strict-clock: `scopeParkedDue()` default `now()` removed — decision inputs receive time via `ClockInterface`; ambient time allowed only for audit stamps (`processed_at`) | PB-003 is no longer a ticket — it is the platform Messaging Infrastructure Foundation every future context depends on; "all tests green" ≠ "reusable capability" (AKB: architecture as long-lived asset); property-based tests protect intent without freezing implementation names; ambient time on a decision path is temporal entropy (replay hazard) | ADR-T1/T4 · Blueprint §6/§7/§8 · `Messaging_Architecture_Verification.md` · Process v1.0 (fold-in needs v1.1) | (a) scope C6 to two file-scans (leaves Shared unguarded) · (b) class-name-bound tests (freeze implementation) · (c) keep `now()` default (latent ambient-time leak on the re-drive decision path) | Shared infra + all future consumers · none (Process fold-in deferred to v1.1) · none · not breaking |

| D-12 | 2026-07-07 | **Messaging Platform capability defined** — POINTER: this bundled entry was **split into single-question ADRs** `ADR-MP-01…05` (`docs/adr/ADR-MP-Messaging-Platform.md`): MP-01 definition (Platform Capability implemented using a Generic Technical Subdomain) · MP-02 ownership model · MP-03 executable-architecture ownership (owner-hosts-the-guard) · MP-04 constitutional preservation (anonymity + tenant isolation preserved, not owned) · MP-05 deferred decisions (ordering not owned; outbox port deferred). The generalizable pattern is captured in `docs/architecture/patterns/Platform_Capability_Pattern.md` (principles: `docs/architecture/principles/Platform_Governance_Principles.md`, PGP-01…05); Messaging is its first instance | one decision per ADR (D-12 was too large); architecture describes enduring concepts (the template) before instances | `ADR-MP-*` · ADR-T3/T4/T5/T8/T11/T16 · `Messaging_Platform_Strategic_Model.md` + `..._Review.md` · pattern `patterns/Platform_Capability_Pattern.md` · principles `principles/Platform_Governance_Principles.md` | keeping five independent governance decisions bundled in one record (hard to evolve) | Shared infra + all future consumers · realized as ADR-MP-01…05 · none · not breaking · **Architecture debt: AD-M1 (relocate anonymity guard → constitutional suite), AD-M2 (outbox port symmetry)** |

*(subsequent implementation decisions appended below during Push B)*

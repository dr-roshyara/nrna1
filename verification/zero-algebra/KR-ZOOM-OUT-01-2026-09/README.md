# `KR-ZOOM-OUT-01` — what does Zoom-out do?

**Status:** RUN · **FAILED AS PRIMARY CONFIRMATORY / DIAGNOSTICALLY INFORMATIVE** (ratified 2026-09-05) · non-adjudicated · Theory v1.2 FROZEN · kernel NOT SELECTED

Five rival meanings — `M0` loss · `M1` restoration · `M2` abstraction · `M3a` integration ·
`M3b` revision — plus first-class `MX`. Run against a pre-registration frozen **before** execution.

```bash
python3 code/run_out01.py     # -> data/results_out01.json
```

## Outcome

> **PRIMARY `E5` = 0.067 / 0.068 against a 0.10 floor — BORDERLINE, not claimed.**
> **`M1` passed the 0.60 single-winner rule and is WITHDRAWN as an artifact** — its rate equals the
> no-determination rate to three decimals.
> **⚠️ The calibration gate FAILED (0.294 / 0.265 vs 0.30–0.80).**

**Read `RESULTS.md` §0, §4 and §5 before citing any number.**

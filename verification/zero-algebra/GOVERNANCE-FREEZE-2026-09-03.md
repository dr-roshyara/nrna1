# GOVERNANCE FREEZE — 2026-09-03

**Adopted from review as the canonical research-governance state.**

---

# 1. The transition — no shortcuts

```
        EXPERIMENT  ──▶  AUDIT  ──▶  ADJUDICATION  ──▶  THEORY v1.3
```

> **No shortcut from experiment directly into theory or kernel implementation.**
> **Theory v1.2 remains FROZEN.** No update, no algebra declaration, no kernel modification until an
> audit has been executed, reviewed and **explicitly adjudicated**.

**Current gate: `KR-REP-REDUCTION-AUDIT-2026-09` — EXECUTED.** `14/16 PASS`; **2 failures, one of
which refuted a headline claim.** See `KR-REP-REDUCTION-2026-09/AUDIT-2026-09.md`.
**No adjudication to v1.3 is proposed on this evidence.**

# 2. The orthogonality principle

```
                    KR-ZERO   ⊥   KR-REP-REDUCTION
```

**`⊥` means ANALYTICALLY INDEPENDENT FOR NOW — not mathematically proven unrelated.**

- **KR-ZERO** — relational eliminability
- **KR-REP-REDUCTION** — inquiry preservation under transformation

**They are kept strictly decoupled as orthogonal analytical systems.**

# 3. The directionality principle

> ## Existing evidence constrains the next experiment; the next experiment does not retroactively validate the existing evidence.

**No `X₃` outcome may modify a KR-ZERO conclusion.** If a later experiment and an earlier one
disagree, that is **a finding to report, not a licence to revise the earlier result.**

# 4. Frozen distinctions

```
        Representation  ≠  Recoverability  ≠  Realization

        Zero  ≠  Preservation Criterion
```

**…unless future evidence establishes a relationship under explicitly defined conditions.**

# 5. Mandatory notation

> **`Ĥ(Q|R) = 0` must NEVER be silently promoted to `H(Q|R) = 0`.**
>
> The first is an empirical observation on a tested sample; the second is a claim about a population
> under stated assumptions. **This distinction is mandatory in all future experiment reports**, and is
> audited (`KR-REP-REDUCTION-AUDIT` check `1.4`, PASS).

# 6. Classification adjustment

**`R4` "Tripartite Structural Separation" is classified primarily `[EXP]`, with `[THEORY]` supplying
the formal distinction.**

> **The experiment provides the empirical evidence for the separation; the mathematical vocabulary
> explains it.** `[EXP / THEORY]` as a single compound tag is defensible but **conflates evidence with
> interpretation**; separating them is cleaner and is adopted.

# 7. What is NOT claimed

**No general Knowledge Algebra. No Knowledge Extraction calculus.** The operations exist; **their
algebraic properties are not established** — that is Phase 3, which is **NOT READY** pending
`VG-8`–`VG-10` on the `X₃` carrier.
